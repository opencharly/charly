# W5-GATES teammate report (task #16 code half)

Worktree: /home/atrawog/Atrapub/o/charly-wt-w5 (branch feat/w5-gates)

## Gates landed

1. `charly/no_kind_switch_in_kernel_test.go` (commit 75ea45c3) — TestNoKindSwitchInKernel.
   AST-walks charly/*.go prod for a switch or >=3-arm if-chain dispatching on the 9-word
   vocabulary (pod/vm/k8s/local/android/group/candy/deploy/bundle). Zero live violations.
   Complements the narrower pre-existing TestNoSubstrateWordSwitchInDeployConsult (P9,
   `.Target==` only, 5 words, charly+sdk/deploykit).

2. `charly/reverse_channel_three_legs_test.go` (commit 39a75122) — TestReverseChannelIsThreeLegs.
   AST-collects all 47 registerHostBuilder call sites, asserts exact set-equality against a
   reviewed whitelist table (13 rows cite a direct KERNEL_MANIFEST.md clause at authoring
   time — now more, see below). Shrink-only maintenance rule: a wave that deletes a
   registration removes its whitelist row in the same commit.

3. `charly/kernel_manifest_bidirectional_test.go` (commit 4118bd2c, extended after) —
   TestKernelManifestBidirectional. Parses KERNEL_MANIFEST.md's per-file tables, asserts
   every row's file exists and every prod file has a row (or a reviewed pending-backlog
   entry). Its teeth-proof caught a REAL parser bug (a per-symbol table's data rows leaking
   through), fixing which surfaced a genuine KERNEL_MANIFEST.md structural defect (two
   orphaned STAY rows with no table header of their own — fixed in the same commit).

   FOLLOW-UP (this session, post program-lead pre-authorization): drafted the full 94-file
   backlog census directly into KERNEL_MANIFEST.md's new "## W5 census — the remaining 94
   files" section, sourced from /tmp/audit-prod-report.md (baseline E/M/B/D/R verdicts) +
   /tmp/w3-adjudication.md (BINDING corrections, wins on conflict) + a corroborating
   defines-vs-calls spot-grep on every file whose current LOC diverged >15% from the audit
   snapshot (signal an earlier wave already touched it). That spot-grep flipped several
   audit verdicts:
     - node_build.go: R -> B (own header self-declares B, entity-body assembler moved to K1)
     - node_normalize.go: R -> M (remaining content feeds provider_kind_invoke.go's M dispatch directly)
     - build_overlay.go: R -> M (registers a HostBuild kind, wire-broker reverse-channel leg)
     - substrate_template_resolve.go: R -> M (same kind-dispatch-callback-wrapper shape as
       the already-documented resource_resolve.go/distro_resolve.go rows)
     - gpu_allocate.go: R -> EXCEPTION-GPU (the named operator-dropped GPU-hardware exception)
     - deploy_tree.go: R -> D (w3-adjudication.md's "Confirmed STAY" list overrides the audit
       — the ONE direct audit-vs-adjudication disagreement found, resolved per the stated
       priority order, no escalation needed)

   Added a new clause form to support the rest: R-RESIDUE (exit: <wave/unit/IOU>) — for a
   file the per-file test still classifies R but that remains present because its dissolving
   wave hasn't landed. The "(exit: ...)" is MANDATORY (clauseIsRecognized rejects a bare
   R-RESIDUE) so residue always documents its own exit path, never becomes a place to stop
   looking. Documented in the Clause key section too.

   kernelManifestPendingFiles is now EMPTY (was 94) — every file resolved unilaterally, ZERO
   contested rows needed escalation to the program lead.

4. Verified (no new file needed): TestNoAliasForms_ZeroPackageLevelReexports's extended
   form (spec/spec + spec/ops + spec/schema coverage from W4-u2, import_purity_test.go:204-208)
   survived the integration merge intact and its teeth still pass.

## Gate status at last check

All four gates pass together (`go test . -run 'TestNoKindSwitchInKernel|TestReverseChannelIsThreeLegs|TestKernelManifestBidirectional|TestNoAliasForms|TestImportPurity|TestZeroAliases'`).
gofmt/vet clean. Fresh-cache golangci-lint: 0 issues. Full `go test ./...` (with box/fedora
submodule initialized) passed once before the census work; rerunning now to confirm post-census.

## Design note flagged to program lead (accepted)

Interpreted "teeth-prove... and revert the synthetic before committing" as building an
in-memory synthetic-source teeth subtest (parses a string, never touches a real file) rather
than a literal write-then-revert cycle against the real tree — safer (no real-file mutation
risk) and becomes a PERMANENT regression proof instead of a one-off manual step. Program lead
confirmed this is strictly better and to keep the pattern for all gates (used in gates 1-3).

## Not yet done / handed to orchestrator per task #16's own note

The orchestrator-integration duties named in the task assignment (Op* repoint's 21 fenced-file
sites, cross-lane detect-devices IOU reconciliation, list-sidecars embed-precedence IOU bed
proof) are explicitly orchestrator-owned, not this teammate's — not attempted here.

# 1970.001.0000 — OpenClaw security baseline

Pin the OpenClaw npm dependency to the patched stable `2026.7.1-2` release
and make the shared candy plan assert that exact installed CLI version. The
minimal, full, and desktop CachyOS disposable beds therefore fail if package
resolution installs an older or unexpected gateway despite the binary being
present.

The plugins gitlink binds the companion check-skill catalog update, which
documents all three affected beds and their distinct R10 proof surfaces.

Protected Claude guidance now uses the same full Codex attribution shape as
`AGENTS.md`, eliminating the contradictory example that caused the first
plugins validator to reject this cutover's companion documentation PR. The
sibling sweep also fixes `AGENTS.md`'s own Kimi trailer example, which still
showed the pre-cutover space form contradicted by the semicolon form mandated
elsewhere in the same file.

Assisted-by: Codex (OpenAI GPT-5.6 Sol; analysed on a live system)

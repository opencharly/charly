# 2026.184.0836

## 2026-07-03 — Bump plugins: agents skill documents the CPU-oversaturation concurrency strategy

Advance the `plugins` pointer to `dd1859e` — the `/charly-internals:agents` bed-fan-out
note now carries the measured multi-concurrency model (CPU load is the ceiling; candy
`make -j$(nproc)` is the dominant multiplier; load-gated admission is the optimizing
limit). Documentation-only.

# Regenerate go.work with `use ./sdk`

The SDK-extraction atomic commit (v2026.185.1913) staged charly/ explicitly but
omitted the repo-root go.work, so the committed workspace lacked the pluginsgen-
emitted `use ./sdk` directive. Regenerated to match pluginsgen (the working tree
the R10 bed roster actually built against); TestPluginsGenReproducible confirms.

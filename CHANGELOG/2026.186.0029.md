# Cruft removal: plugin-secrets legacy lookup wrappers + plugin-k8sgen scope comment

plugin-secrets: deleted the self-declared legacy {service, username} lookup wrappers
findItemAcrossCollections (free func) + findItemAnyCollection (method) — thin adapters that just
built an attrs map and delegated to findItemByAttrsAcrossCollections. The two production callers
(credential_keyring.go, verb_credential.go) and the 12 unit-test call sites now build the attrs map
themselves and use the SINGLE findItemByAttrsAnyCollection / findItemByAttrsAcrossCollections lookup
path (R3). Behavior-identical (the wrappers only constructed the map). plugin-k8sgen: rewrote the
"Deferred (will show up as TODOs in output or skipped silently)" comment block as a present-tense
"Out of scope — NOT emitted by this generator" scope statement (no TODO/deferred framing).

R10: plugin-secrets credential-lookup unit tests PASS (they exercise the exact refactored path +
would fail if it broke); `charly secrets` dispatches out-of-process (exit 0) on the fresh rebuild;
pluginsgen reproducible (the k8sgen comment is behavior-neutral).

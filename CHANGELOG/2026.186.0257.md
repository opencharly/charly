# M5 doctrine finale: Core is the kernel; EVERY capability is a plugin candy

Cutover 5 (docs-only) of the SDK/plugin program — the core/plugin doctrine written into CLAUDE.md +
VISION.md. New CLAUDE.md Key Rule "Core is the kernel; EVERY capability is a plugin candy": charly/
keeps ONLY the kernel; every verb/kind/deploy/step/builder/command/build capability is a plugin candy
importing only github.com/opencharly/sdk, dual-placement by construction (compiled-in vs
out-of-process is a per-BUILD choice, never authoring); "it needs core" means a new GENERIC host seam,
never in-core capability code; the in-flight externalizations (vm substrate lifecycle; classified
registry+merge/status/alias/scaffold) are tracked as current state in the plugin skill's Remaining
program. New VISION tenet "A tiny kitchen, every candy a recipe" (arrow to the CLAUDE.md rule).
Bumps the plugins submodule to the M5 skill doctrine (plugin skill Remaining program + skills
Authoritative-copy row).

# M4b doctrine follow-up: CLAUDE.md rule reflects vm lifecycle externalized

Immediate follow-up to M4b (R2): the CLAUDE.md "Core is the kernel" Key Rule listed the vm substrate
lifecycle among the in-flight externalizations — M4b externalized it, so the rule now states all five
deploy substrates (local/pod/vm/k8s/android), pod's + vm's venue lifecycles included, are
externalized, and only the classified core-minimization candidates (registry+merge, status, alias,
scaffold) remain. Bumps the plugins submodule to the matching Remaining-program update.

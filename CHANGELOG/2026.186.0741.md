# refactor(deploy)!: vm substrate venue lifecycle IMPLEMENTED in candy/plugin-deploy-vm (clean)

Replaces the hollow `charly __vm-lifecycle` forward with a TRUE externalization: candy/plugin-deploy-vm
(Lifecycle:true) now IMPLEMENTS every vm venue-lifecycle Op itself over GENERIC seams — sdk/kit for the
managed ssh-config stanza + guest-readiness waits + charly delivery, HostBuild("cli") for `charly vm …`
(auto-boot / start / stop / console / ssh / rebuild) and `charly box build`/`charly vm cp-box`, and the
reverse channel (served guest executor) for the in-guest nested-pod `from-box` deploy (OpPostApply).

Core keeps ONLY generic seams + host-resolved DATA the plugin cannot compute (no project):
- `charly/vm_lifecycle_preresolve.go` (NEW) — a `lifecyclePrepareHook` registry: the vm hook does
  LoadUnified→spec.Vm + resolves entity + ssh user/port + prior VmDeployState and ships
  spec.LifecyclePrepareInput on OpPrepareVenue params. Same DATA-seam shape as the in-core k8s/android
  deployPreresolvers. A `lifecyclePostTeardownHook` runs the one residual host cleanup the plugin can't
  (vm ephemeral-lifecycle teardown). `grpcSubstrateLifecycle` (substrate_lifecycle_grpc.go) consults both
  GENERICALLY by word (pod registers none); it ships HostEnv{CharlyBin,Home,Version} + persists the
  plugin's returned VmDeployState via the generic saveDeployState (SaveDeployStateInput gains
  VmState/VmCrossRef) + removes the plugin's PostTeardownReply.RemoveEntries.

DELETED (no vm lifecycle logic remains in core): vm_deploy_lifecycle.go (the vmSubstrateLifecycle struct),
vm_lifecycle_cmd.go + the hidden `__vm-lifecycle` command (main.go field), charly_install.go
(EnsureCharlyInVenue/Guest → sdk/kit), ssh_managed_config.go (→ sdk/kit/sshconfig.go, core `= kit.X`
aliases in ssh_config_aliases.go), deployNestedPodsInGuest (→ the plugin's OpPostApply), the dead
hostCharlyIsNewer (→ kit.HostCharlyIsNewer). SSHExecutor.WaitFor* are thin delegates to kit.WaitFor*
(injected poll, so kit stays stdlib+spec — no x/crypto pollution of every plugin).

R10: check-charly-vm PASS (steps=6) + check-k3s-vm PASS (steps=6, 112s) — the externalized plugin drives
the whole venue lifecycle (boot + EnsureCharlyInGuest + walk + nested-pod PostApply + fresh-update
Rebuild + teardown) on fresh rebuilds. bumps: sdk v0.2026186.739 (kit helpers + LifecyclePrepareInput),
plugins (10-skill R5 sweep).

# refactor(deploy): pod overlay-image drop implemented in candy/plugin-deploy-pod (clean)

Replaces the hollow `charly __drop-overlay` forward: candy/plugin-deploy-pod's OpPostTeardown now drops
its synthesized `<name>-overlay` images itself via the generic `kit.RemoveImagesByReference` (sdk
v0.2026186.831), with the grpcSubstrateLifecycle proxy shipping the resolved engine binary
(`EngineBinary(podDeployEngine(node))`) generically in the PostTeardown params (vm ignores it).

DELETED: charly/drop_overlay_cmd.go (the `__drop-overlay` command) + its main.go field + the core
`removeDeployOverlayImages` (build_overlay.go) — no other caller. Fixed the misleading check_bed_run.go
comment claiming the pod bed's `charly remove` teardown drops overlays (it does not; the drop is on the
`bundle del`/OpPostTeardown path — a pre-existing coverage gap tracked as a follow-up). The kit helper
also fixes a pre-existing destructive over-broad-filter bug (basename-exact selection).

R10: check-addcandy-pod PASS (steps=10, no deploy-path regression) + a direct `charly bundle del`
witness of a pod-with-add_candy — dropped 18 overlay images, preserved all base + cross-bed images.
bumps sdk v0.2026186.831.

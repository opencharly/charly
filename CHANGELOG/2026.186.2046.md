# chore: drop the inert root .mcp.json and trim the README name-origin aside

## What

Two repo-hygiene edits, no charly product behaviour change:

- **Removed the repo-root `.mcp.json`.** It registered two Claude Code MCP
  servers as project-scoped harness config — `jupyter` (`http://localhost:8888/mcp`)
  and `chrome-devtools` (`http://localhost:9224/mcp`). It is consumed only by the
  Claude Code harness, never by the `charly` product.
- **Trimmed `README.md`.** Dropped a garbled name-origin aside left over from a
  rename sweep (the "German 'überdenken'" paragraph, whose text had been mangled
  by a find-replace).

## Why

The root `.mcp.json` was stale harness config. The per-plugin
`plugins/charly-*/.mcp.json` files (each plugin's own `./.mcp.json`, wired via
its `.claude-plugin/plugin.json`) are separate, live, and untouched — this
change removes only the superproject-root file.

## Verification (analysed on the live system)

- **Reference sweep (R5):** `git grep '.mcp.json'` across the superproject
  returns zero hits; there is no root `.claude-plugin/plugin.json` and no mcp
  reference in `.claude/settings.json`; `charly/**` Go core does not read it. The
  removal leaves no dangling reference.
- **Inert at runtime:** neither `:8888` (jupyter) nor `:9224` (chrome-devtools)
  is listening in the session, so the registered servers are not active — the
  deletion has no observable effect.

No check bed applies: the file has no `charly` runtime surface to exercise (a bed
could not fail on this change), so the honest gate is the live reference/impact
analysis above.

# Schema Driven Design (SDD): schema before code, generated from the schema

Docs-only cutover writing the already-practiced doctrine down as a first-class pillar. New VISION tenet 7 "Carve the master pattern before building the machine — Schema Driven Design" (concept-only prose; the tenets renumber to eleven), and the spike named in tenet 5 as RDD's small, time-boxed, throwaway tasting batch. New CLAUDE.md Part II pillar "Schema Driven Design (SDD)": every authored surface is defined FIRST as a CUE schema, the code that carries the shape is GENERATED (`task cue:gen`) never hand-transcribed, validation at every boundary derives from the same schema, hand-written schema-shaped code is the narrow exception kept in lockstep with its `@go(-)` def, and regeneration on a clean tree is a no-op (drift is an R1 incident) — plus a Skill Dispatcher row (`sdk/schema/*.cue` / `task cue:gen` / SDD / schema spike → `/charly-internals:go` + `/charly-internals:plugin`) and a matching Key Rules entry. The RDD pillar names the SPIKE as its instrument (time-boxed, throwaway, deliverable = the proven/refuted claim, never shipped code). Bumps plugins to the matching skill ownership (go: the owning SDD section + generation-coverage ledger; strict-policy: "The spike"; plugin + skills-registry updates).

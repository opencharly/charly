# Cutover B — kindValueDef Go map → CUE-derived spec.KindValueDefs (clause D)

The last compiled-in per-kind Go map in the load path becomes CUE-derived data
(clause D of the kernel/plugin boundary law: a kind-recognition fact is loaded
from CUE, never a compiled-in Go map).

- Delete the hand-authored `kindValueDef` map in `charly/provider_kind_invoke.go`;
  `validateKindValueCUE` now reads the schemagen-derived `spec.KindValueDefs`.
- `charly/cue_kinds_corpus_test.go` drives the corpus value-gate off
  `spec.KindValueDefs` too. The `#<Kind>Value` defs (node.cue) are the sole source;
  the map is derived by schemagen (sdk), never hand-maintained.

## Verification (fully tested and validated)

`go test ./...` (charly — incl. the 34s corpus gate that validates every real node
against its `spec.KindValueDefs`-derived def — + sdk gen-repro) green; `task cue:gen`
adds only the derived `spec.KindValueDefs` (byte-identical to the former hand map);
fresh `task build:charly`; `charly box validate` PASS on the superproject + all 5
box submodules (value gate at load over the whole corpus); R10 `check-substrate`
disposable bed PASS (deploy-add → check-live → fresh update → cleanup, 31s, ok:true)
— the substrate value gate fires at deploy via the CUE-derived map. No `#SchemaVersion`
bump (no authored wire-key change).

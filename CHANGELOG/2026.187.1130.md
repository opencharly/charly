# Cutover C-follow — remove the dead in-core stage_template builder render

Completes the C10 builder externalization: the `builderDef.StageTemplate` in-core
render path was a dead residual (all builders resolve their multi-stage via the
plugin's OpResolve / `kit.BuilderResolve`; no builder authors `stage_template`).

- Delete the `!external` StageTemplate render branch (`generate.go` ×2), the
  `step_emit_hostbuild.go` fallback (a custom builder must be an `external_builder`
  plugin), and the `builderPhaseTemplate` StageTemplate fallback (`format_config.go`).
- Drop the `stage_template` schema/Go field (sdk); regenerate.
- R5 sweep: remove `stage_template` from the `testdata/build.yml` fixtures + the
  stale tests (`TestAurBuilderStageTemplate`, the phases-fallback + pixi/aur
  StageTemplate assertions) + comments.

## Verification (fully tested and validated)

`go test ./...` (charly + sdk gen-repro) green; `task cue:gen` drops only
`StageTemplate` from `spec.Builder`; fresh `task build:charly`; R10
`check-builder-local` disposable bed PASS (deploy-add → check-live → fresh update →
cleanup, all ok) — builders build correctly via OpResolve after the removal. No
`#SchemaVersion` bump (authored-nowhere field).

# Forbidden-excuse catalog: add the PRIORITY dimension + categorize + tighten

Follow-up to 2026.187.2109 — close the remaining loopholes and make the CLAUDE.md
"Hard Cutover by Default" Forbidden-excuse catalog precise and categorized:

- Add the **priority / characterization** class: "it's secondary / lower-priority / a
  nice-to-have / optional / not required / just a design refinement / NON-<gate>, so not
  required". Priority is the same forbidden argument as difficulty and size — the change
  stays IN the contract's scope.
- Categorize the whole catalog (difficulty / size / priority / deferral-dressed-as-planning /
  revert-to-defer / honesty-dressing) so each loophole class is explicit and un-rationalizable.
- State that decomposition into per-unit cutovers is legal ONLY as forward motion, and a spike
  that PROVED the mechanism means keep-and-land, never revert-and-defer.

Documentation-only (CLAUDE.md prose + the owning skills bumped via the plugins pointer:
cutover-policy + strict-policy updated in lockstep).

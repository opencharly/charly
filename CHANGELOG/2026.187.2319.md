# Sweep the plugin-side comments naming the deleted host preresolvers (R5 completion of H part 2)

Follow-up to the H-part-2 endpoint-seam migrations (v2026.187.2110–2316): each verb's plugin
still described the OLD architecture where the host preresolved its endpoint, naming the deleted
host functions (preresolveCdpEndpoint / preresolveVncEndpoint / preresolveSpiceEndpoint /
preresolveMcpEndpoint). R5 keeps deleted-identifier refs in CHANGELOG only — reword the plugin
comments (cdp/mcp/vnc plugin headers, plugin-vm's spice_endpoint_test / display_gate_sentinel /
vm_target) to describe the CURRENT design: the plugin resolves via the generic cc.Resolve*
reverse-legs. Comment-only except one test assertion-MESSAGE string (spice_endpoint_test.go, verified by `go test`), zero behaviour change.
Repo-wide `git grep` for the deleted host preresolvers now returns only CHANGELOG history.

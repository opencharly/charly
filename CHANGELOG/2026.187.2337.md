# Sweep code comments + candy descriptions naming the retired preresolvers (R5/R1, H part 2)

Complete the H-part-2 R5 sweep in the superproject: code comments (charly/credential_plugin.go,
ports.go; candy/plugin-cdp|mcp|vnc|secrets|vm) and the candy `description:` ADE strings
(plugin-cdp|mcp|vnc|secrets) still named the deleted host preresolvers (preresolveCdpEndpoint /
preresolveVncEndpoint / preresolveSpiceEndpoint / preresolveMcpEndpoint) and the renamed
vnc_preresolve.go. Reword to the current design: each verb resolves via the class-generic
cc.Resolve* reverse-legs; vnc_preresolve.go → vnc_helpers.go. sdk pointer → the matching
ssh_tunnel.go comment sweep; plugins pointer → the skills sweep. Zero behaviour change.

# Bump plugins → pr-validator always comments on the PR

Superproject pointer bump adopting the plugins change that makes the fresh
`pr-validator` agent ALWAYS post a human-readable PR comment (findings + the
approve/reject decision) alongside the `charly/claude-validation` status. The
bumped plugins commit is all-documentation (agent prompt + CHANGELOG).

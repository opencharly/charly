# Adopt PR templates across all submodules + pr-validator security hardening

Superproject pointer bump adopting the merged submodule changes from the PR-only
agent-validated landing rollout — all documentation:

- **plugins** → the `pr-validator` security review + anti-tampering screen (T1–T4)
  and attributed PR comments.
- **sdk, distro-arch/cachyos/debian/fedora/ubuntu, pkg-arch/debian/fedora** → each
  now ships `.github/pull_request_template.md`.

Every bumped submodule commit is all-documentation (agent prompt / PR template /
CHANGELOG). Completes the rollout: all 11 repos are branch-protected (no direct
push to main; PR + a fresh-pr-validator status required), every repo has the PR
template, and the org has zero private repos.

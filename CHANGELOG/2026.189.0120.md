# 2026.189.0120 — remove the LibvirtGraphicsListeners YAML expander (JSON decoder now serves both paths)

Consumer half of the sdk LibvirtGraphicsListeners UnmarshalJSON fix
(v0.2026189.59): with the sdk type now a `json.Unmarshaler`, the CUE
normalizer's `implementsJSONUnmarshaler` guard short-circuits it, so
`cue.Value.Decode` runs the tri-shape `UnmarshalJSON` on BOTH the YAML read
path AND the opaque substrate-decode path (a de-typed vm body). The charly-side
`expandLibvirtListenersNode` expander (+ its `cueShorthandExpanders` entry) is
therefore dead and DELETED (R5) — ONE decoder now serves both paths (R3), the
Matcher/PortScope self-decoding precedent. Fixes the `check-cross-vm-http`
web-vm regression: the documented `listen: 127.0.0.1` scalar shorthand (and the
object/list forms) failed on the opaque path with "cannot unmarshal string into
… LibvirtGraphicsListeners". sdk pointer bumped 945974e → 0561805
(v0.2026189.59). Live: `charly -C box/fedora box validate` (folds + resolves
web-vm's vm body through the opaque decode) exit 0. Additive JSON method on the
sdk side, no wire-key change → no schema/protocol bump.

*Assisted-by: Claude (fully tested and validated)*

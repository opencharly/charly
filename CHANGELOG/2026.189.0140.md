# 2026.189.0140 — adopt pkg/arch #commit-pinned PKGBUILD

Bumps the `pkg/arch` submodule pointer to `c6fedea` (the merged makepkg
`#commit=` source-pin fix): makepkg no longer depends on the clone's
`origin/HEAD` symref, so `task build:charly` and the in-guest/standalone
localpkg build succeed against a detached sdk submodule (the deterministic
`check-charly-vm` / `check-cachyos-vm` failure the concurrent bed roster
surfaced). No superproject code change — a submodule-pointer adoption of an
already-validated packaging fix.

*Assisted-by: Claude (fully tested and validated)*

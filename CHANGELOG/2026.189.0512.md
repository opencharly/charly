# 2026.189.0512 — adopt the full-enforcement pr-validator (anti-cheat + R0–R18)

Bumps the `plugins` pointer `bc4d343 → 154148c`, adopting both pr-validator
hardening cutovers so they govern every subsequent superproject PR:
- the anti-cheat mandate (total enforcement; refuse "idle/serial passes" for a
  load failure, "pre-existing/out-of-scope" dodges; concurrency issues need a
  root-cause RCA + the concurrent-roster gate), and
- the comprehensive Phase-1 checklist (0–18) that maps one explicit check to
  EVERY CLAUDE.md rule + pillar, with "unverified = FAIL".
All-documentation submodule delta (the agent spec + CHANGELOGs); no runtime
surface.

*Assisted-by: Claude (documentation reviewed)*

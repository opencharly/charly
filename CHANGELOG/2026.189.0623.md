# 2026.189.0623 — bump submodule pointers: PR-template single-source consolidation

Bumps all nine submodule gitlinks to the mains that landed the PR-template
consolidation (P1.14) + the filled-template enforcement (P1.15):
- **plugins** 154148c → 3847ad1 — plugins template deleted + git-workflow names
  opencharly/.github as the single source + pr-validator item 1 now REQUIRES the
  filled template (blank section / bare tick / "will test" all FAIL).
- **sdk, box/{arch,cachyos,debian,fedora,ubuntu}, pkg/{arch,debian}** — each
  deleted its own `.github/pull_request_template.md` so it inherits the org-wide
  default from opencharly/.github.

Every bumped delta is all-documentation (template deletes + CHANGELOGs + the
plugins agent-spec/skill edits); the pre-commit-gate recurses each submodule's
old..new diff to certify. Completes the R3-at-repo-level consolidation: ONE
template source (opencharly/.github), inherited everywhere; the validator enforces
it.

*Assisted-by: Claude (documentation reviewed)*

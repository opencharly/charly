# 2026.189.0636 — bump sdk pointer: gofmt spec wire files (P1.12)

Bumps the `sdk` gitlink ad51c9b → 909a3059 (+ `charly/go.mod` require
v0.2026187.2336 → v0.2026189.633) to adopt the sdk gofmt-hygiene fix: the 5
hand-written `spec/*_wire.go`/test files reformatted with `gofmt -w`
(AST-identical, behavior-neutral). In-tree resolution rides the `replace` directive;
the require bump is for out-of-tree consumers + tag consistency.

Gate (formatting-only adoption → compile+test, no bed — the sdk delta is
AST-identical so no runtime behavior changes): `go build ./...` exit 0 + `go test
./...` green (charly 42.1s + pluginsgen) against the reformatted sdk.

*Assisted-by: Claude (analysed on a live system)*

# 2026.189.0715 — P2/S1 consumer + wire-type CUE mandate (CLAUDE.md)

Closes P2/S1 (the first CUE-sourced keystone) + lands the operator standing order.
- **charly** adopts the CUE-sourced `InstallContext`/`BuildStageContext`: deletes
  the hand-written structs from `format_template.go`, re-exports the aliases via
  `vmshared_aliases.go` (the ~16 consumers compile against the generated types —
  the Go compiler is the field-parity check).
- **CLAUDE.md SDD pillar** gains the mandate: ALL wire types are CUE-sourced
  (`task cue:gen`), hand-writing a wire struct FORBIDDEN; no `@go(-)` without a
  full RCA + a live `cue exp gengotypes` spike proving CUE cannot express it. The
  three spike-verified NON-wire exceptions (disjunctions, open-tails, the gRPC
  transport `proto` — no `cue→proto` path) are named with evidence.
- Bumps `sdk` → v0.2026189.748 (buildctx.cue + generated) + `plugins` (the go-skill
  detail owner carrying the mandate).

Behavior-neutral for the code (identical struct fields); proven by the builder bed
(check-builder-local, all 4 steps green on a fresh rebuild).

*Assisted-by: Claude (fully tested and validated)*

# 2026.189.1017 — adopt sdk/buildkit render library (P3-unit-1 consumer)

The paired consumer side of P3-unit-1 (producer: sdk feat/sdk-buildkit). The pure
Containerfile render/compute cluster is now in `sdk/buildkit`; charly:
- deletes `charly/format_template.go` (the render cluster moved verbatim to
  `sdk/buildkit`);
- re-exports the library zero-churn via `charly/buildkit_aliases.go` (CacheMount,
  RenderCacheMounts(Auto), RenderTemplate, NewInstallContext, templateFuncs,
  toStringSlice/toMapSlice → the exported buildkit symbols), so the ~15 consumer sites
  (generate.go, tasks.go, install_build.go, build_target_oci.go, distro_resolve.go,
  init_config.go, privileged_runner.go, vm_disk_builder.go, …) compile unchanged;
- bumps the `sdk` gitlink + `charly/go.mod` require to the tagged producer.

Behavior-neutral (verbatim move); proven by the golden go-test suite + the
check-builder-local bed (all 4 steps green on a fresh rebuild). First unit of the P3
sdk/buildkit extraction; the render/compute methods (graph/intermediates/effective-
version) follow once their ResolvedBox/Generator keystones move to sdk.

*Assisted-by: Claude (fully tested and validated)*

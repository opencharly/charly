# 2026.189.1227 — adopt sdk/buildkit vocabulary resolver (P3-unit-2 consumer)

Consumer side of P3-unit-2. The distro/builder vocabulary resolver now lives in
sdk/buildkit; charly trims `format_config.go` to the pieces that stay charly-side
(the Phase/Venue phase-template resolvers + `LoadBuildConfigForBox` loader glue),
re-exports `DistroConfig`/`BuilderConfig` + the `WrapDistroDef`/`DistroTagChain`
free funcs via `buildkit_aliases.go`, and renames three method-call sites to the
exported `ResolveInherits`/`ExpandPackageInheritance` (bundle_add_cmd/config/
vm_bootstrap + the distro_cascade/debootstrap tests). Bumps sdk to the tagged
producer. Behavior-neutral; golden tests + check-builder-local bed green.

*Assisted-by: Claude (fully tested and validated)*

# 2026.189.1259 — adopt sdk/buildkit ResolvedBox keystone (P3-unit-4 consumer)

Consumer of P3-unit-4. `ResolvedBox` + `BuilderMap` + `AggregatedCandyCaps` live in
sdk/buildkit now; charly deletes the struct/method defs from config.go +
layer_capabilities.go and re-exports all three via buildkit_aliases.go (zero-churn —
every method is exported). `AggregateCandyCapabilities` (the candy-model walk) stays
in layer_capabilities.go, constructing the aliased result type. Bumps sdk to the
tagged producer. Behavior-neutral; golden tests + check-builder-local + check-local
beds green.

*Assisted-by: Claude (fully tested and validated)*

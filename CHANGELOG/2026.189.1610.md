# 2026.189.1610 — adopt sdk/spec InstallPlan IR enums (P4-unit-1 consumer)

Consumer of P4-unit-1. The InstallPlan IR discriminator enums (`Venue`/`Phase`/
`StepKind`/`Gate`) live in sdk/spec now; charly deletes their definitions from
`install_plan.go` and re-exports them via type + const aliases (mirroring the
existing `Scope = spec.Scope` alias block in the same file) — zero-churn across the
whole deploy/step/build surface. Bumps sdk to the tagged producer. Behavior-neutral;
golden tests + check-builder-local + check-local beds green.

*Assisted-by: Claude (fully tested and validated)*

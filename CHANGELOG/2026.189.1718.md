# 2026.189.1718 — adopt sdk/spec InstallStep interface (P4-unit-2 consumer)

Consumer of P4-unit-2. The InstallStep interface lives in sdk/spec now; charly
deletes its definition from install_plan.go and re-exports it via
`type InstallStep = spec.InstallStep`. The 13 concrete step structs need NO change —
they satisfy spec.InstallStep structurally (their Kind/Scope/Venue/RequiresGate/
Reverse already return the spec-aliased types). Bumps sdk to the tagged producer.
Behavior-neutral; golden tests + check-builder-local + check-local beds green.

*Assisted-by: Claude (fully tested and validated)*

# 2026.189.1741 — adopt sdk/deploykit step vocabulary (P4-unit-3 consumer)

Consumer of P4-unit-3. The InstallPlan step vocabulary (13 step structs + RepoSpec/
CacheMountSpec/ArtifactRef + the OpStepScope/PathIsSystemScoped helpers) lives in
sdk/deploykit now; charly deletes their definitions from install_plan.go +
install_build.go and re-exports them via deploykit_aliases.go (zero-churn — the
compiler, step_view, and deploy targets construct them via the aliases). InstallPlan +
wireView + ResolveHome stay charly-side (they reference the aliased structs). Bumps sdk
to the tagged producer. Behavior-neutral; golden tests + check-builder-local +
check-local beds green.

*Assisted-by: Claude (fully tested and validated)*

# 2026.192.0905 — bump plugins to the sdk-tag-strip doc fix

Pointer bump: the `plugins` submodule → its merged commit `60f9934`, which makes the sdk Go-module tag
scheme's leading-zeros-stripped rule explicit across `pr-validator.md` + `git-workflow` (the divergence
that produced the invalid `v0.2026192.0733` tag during the P6 landing). All-documentation — the bumped
submodule commit's own diff is `*.md` only.

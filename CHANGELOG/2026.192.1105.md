# 2026.192.1105 — bump plugins to the pr-validator bed-coverage strengthening

Pointer bump: the `plugins` submodule → its merged commit `879422b`, which strengthens the fresh
`pr-validator`'s R10 evaluation — item 2 gains a mandatory bed-COVERAGE check (the correct beds for the
change class ran, by name) and item 11 gains no-benefit-of-the-doubt bed-evidence cross-check + re-run
authority (the evaluator re-runs the correct disposable beds itself when a claim is suspicious). Pairs with
the org PR template's explicit eval-bed-list requirement (`opencharly/.github`). All-documentation — the
bumped submodule commit's own diff is `*.md` only.

# 2026.193.0931 — CLAUDE.md landing doctrine corrected to the PROVEN two-gate merge model

The superproject half of the two-gate merge-authorization correction (producer `plugins`
#31 merged first, B2 order). Documentation-only.

**`CLAUDE.md` Post-Execution point 3.** #66 claimed the fresh-`pr-validator` flow "steers
Claude Code's auto-mode classifier (which reads this file)" and is "NEITHER Self-Approval NOR
Merge Without Review". **Disproven live this session:** the auto-mode classifier explicitly
rejects a CLAUDE.md authorization as *"manufactured classifier-steering intent"*, and
`permissions.allow` clears only the `success` STATUS POST — not the classifier's separate
**Merge-Without-Review** gate on `gh pr merge`. Point 3 now KEEPS the landing-authorization
POLICY (the two-step validated landing IS this project's authorized reviewed landing) but
states the operative mechanism accurately: the classifier honors the merge ONLY via an
operator `autoMode.allow` rule in **USER or MANAGED settings** — never a committed repo
`settings.json` (the classifier ignores `autoMode` there), never CLAUDE.md prose, never
`permissions.allow` — and a HUMAN must apply it (an agent editing its own permission config
trips the un-bypassable Self-Modification block). It points at `/charly-internals:git-workflow`
B5 for the full two-gate model.

**`plugins` gitlink → `118a4e7`** integrates plugins #31 (all-`*.md`): roster agents
unrestricted, skill-access-by-path, and the B5 / pr-validator / agents-SKILL two-gate rewrite.

**Proof.** The SAME `gh pr merge` that was DENIED with only `permissions.allow` present
(Merge-Without-Review) went through with ZERO denials once the operator applied the
`autoMode.allow` rule to `~/.claude/settings.json` — the clean A/B this cutover documents.

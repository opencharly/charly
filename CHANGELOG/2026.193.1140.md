# 2026.193.1140 — SDD exception catalog: CLAUDE.md defers to its sole owner (kills the mirrored-count drift)

Documentation-only. A pr-validator on plugins#33 (D2) caught a live mirror-claim divergence:
CLAUDE.md's SDD pillar hardcoded "The ONLY spike-proven cases are THREE NON-wire categories"
with a near-verbatim enumeration, while the owning `/charly-internals:go` skill (the
Authoritative-copy registry's SOLE owner of the SDD operationalization) grew a spike-proven
FOURTH exception (`json:"-"` keep-in-Go fields, RDD spike 2026-07-12). Because CLAUDE.md
overrides skills (R0), the stale hardcoded count would have rendered the new legal exception
ineffective.

Fix (the CLASS, not the instance, per the mandate-vs-detail doctrine): CLAUDE.md's SDD pillar
now DEFERS to `/charly-internals:go` "Generation coverage" as the sole owner of the exception
catalog + the gengotypes CAN/CANNOT results, deliberately carrying no count or enumeration —
a mirrored copy drifts the moment a new spike lands. The mandate itself (no `@go(-)` /
hand-written type without a full RCA + a pasted live `cue exp gengotypes` spike) is unchanged
and stays in CLAUDE.md.

Lands WITH plugins#33 (the skill leg carrying the four-category catalog + the CAN/CANNOT
table); this is the required superproject leg of that coordinated docs cutover.

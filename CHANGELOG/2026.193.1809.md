# 2026.193.1809 — pure-compute build helpers relocated to the sdk libraries (P8b-followup, superproject half)

The superproject half of #24 (P8b-followup). The pure-compute build helpers moved into the sdk
libraries (opencharly/sdk, tag v0.2026193.1801); charly core repoints to aliases/shims and DELETES
the core copies (R5). Behaviour-neutral: `charly box generate` is BYTE-IDENTICAL against the
RELEASED sdk (pinned `--tag` on box/fedora's 46 Containerfiles incl. every auto-intermediate;
hash `25852f20…` — independently confirmed).

- **`charly/build_stage_atomic.go`** — `installDirAtomic` is now `var installDirAtomic = kit.InstallDirAtomic`.
- **`charly/format_config.go`** + **`charly/buildkit_aliases.go`** — `formatPhaseTemplate`/`builderPhaseTemplate`
  now alias `buildkit.FormatPhaseTemplate`/`BuilderPhaseTemplate`; the STALE "stay charly-side" headers
  R1-swept in both files. `LoadBuildConfigForBox`/`LoadDefaultBuildConfig` STAY (loader glue).
- **`charly/intermediates.go`** + new **`charly/intermediates_shim.go`** — the intermediate-image DAG
  pure funcs moved to `sdk/deploykit`; core keeps the `*Config`-reading RESOLVE orchestration
  (ComputeIntermediates/createIntermediate/processSiblingGroup/walkTrieScoped/resolvePlatforms/pickAutoName),
  which reaches the moved helpers via the `candyModelMap` adapter (graph_shim pattern). The no-core-caller
  funcs (graphReaches/topoSortByPopularity/sortByPopularity/addTransitiveDeps) are fully DELETED (R5 grep clean).
- **`charly/step_emit_hostbuild.go`** — `stepEmitSystemPackages` reconstructs the view then calls
  `buildkit.RenderSystemPackagesFragment`; the Generator/host-engine-coupled emitters STAY.
- **`charly/go.mod`** require → `v0.2026193.1801`; the `sdk` submodule gitlink → the merged sdk main.

# 2026.193.1929 — codify the default multi-agent execution model in CLAUDE.md (mandate + plugins pointer bump)

Documentation-only cutover (the superproject half of the batched P-program docs codification; the
plugins operationalization landed in plugins #35). Adds the default multi-agent execution model as
a mandate to CLAUDE.md "Agents, Workflows & Teams" and bumps the plugins pointer so the mandate's
`*Detail:*` target carries the full operationalization.

- **`CLAUDE.md` "Agents, Workflows & Teams"** — new leading MANDATE (before "The brief:"): the
  DEFAULT execution model for substantial / multi-cutover work is a persistent MOST-CAPABLE-model
  ORCHESTRATOR (×1, the premium paid once where the leverage is highest — routing, merge
  sequencing, and the independent RDD-verification of every teammate decision) driving parallel
  cost-scaled (cheaper-model) TEAMMATES + fresh cost-scaled `pr-validator`s (×N, each a bounded unit
  under that verification). Maximum parallelization is the DEFAULT (solo/sequential the EXCEPTION for
  trivial work); landing stays concurrent (`gh pr update-branch` + risk-proportional delta re-gate);
  the orchestrator RDDs every teammate decision bidirectionally. States the PRINCIPLE + cost ratio,
  not exact prices (current instantiation: Fable-5 orchestrator driving Opus-4.8 teammates). Single
  `*Detail:*` pointer to `/charly-internals:agents`; the existing brief bullets are unchanged. The
  agent-teams note is clarified to "experimental FLAG … the METHODOLOGY is the proven default" so the
  flag-state and the default-methodology no longer read as contradictory.
- **`plugins` pointer** → `34a302b` (was `b527c3f`; picks up plugins #35 — the agents-skill
  "The default multi-agent execution model" section the mandate points at — and #36, already an
  ancestor). All-documentation bump.

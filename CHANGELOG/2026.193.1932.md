# 2026.193.1932 — pre-commit-gate: require a CHANGELOG entry at EVERY legal tier (close the #35 gap)

The mechanical half of #34 ("same changelog rules for plugins"). The `pre-commit-gate.sh` CHANGELOG
check previously fired only for the two RUNTIME tiers (`RUNTIME_TIERS`), so a `documentation reviewed`
commit in a CHANGELOG-tracking repo could skip its history entry (the gap PR #35 hit — caught by
convention, not the gate). The check now fires at EVERY legal tier; the pure-submodule-pointer-bump
exemption is unchanged. `analysed on a live system` — the changed gate was executed live
(`gate_test.py` 68/68 + an old-vs-new differential proving the docs-tier BLOCK is new).

- **`.claude/hooks/pre-commit-gate.sh`** — `assert_changelog` is now called for any declared tier
  (`if not is_amend and tiers:`), not just `RUNTIME_TIERS`; the now-dead `RUNTIME_TIERS` set is
  DELETED (R5) and `needs_diff` simplifies to `bool(tiers)`. The exemption (`only_gitlinks → return`)
  is untouched; the header comment and block message were reworded off "runtime-tier".
- **`.claude/hooks/gate_test.py`** — three cases added: docs-tier + docs change + no CHANGELOG →
  BLOCK; docs-tier + CHANGELOG staged → ALLOW; docs-tier pure-docs-submodule-pointer-bump + no
  CHANGELOG → ALLOW (the #81 exemption, via a new `mksubbump` helper — the submodule-bump path was
  previously untested). 68/68 green.
- **`CLAUDE.md`** — the "Hooks doctrine" bullet's "a runtime-tier commit staging no CHANGELOG …"
  claim now reads "a commit at any legal tier … (a pure submodule-pointer bump exempt)" (R1/R5
  claim-key sweep — the sibling claim in `plugins/internals/skills/agents/SKILL.md` is fixed in the
  plugins PR).
- **`plugins`** gitlink → the merged plugins main carrying the agents-skill hooks-doctrine correction.

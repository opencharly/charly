# 2026.194.0751 — D2 v2-governance: CLAUDE.md kernel redefinition (plugin-host) + pre-commit ZERO-ALIASES teeth (#49)

The superproject half of #49 (D2, operator-directed v2-architecture governance). The plugins half
(the boundary-law sweep + the `## Target architecture (v2)` design section + the pr-validator ARCHITECTURE
GATE + the agents responsibility matrix) landed first; this bumps the plugins gitlink to it and adds the
CLAUDE.md contract + the runtime pre-commit teeth. `analysed on a live system` — the pre-commit hook was
executed live (`gate_test.py` 81/81 + an old-ALLOW/new-BLOCK differential proving the alias-block bites).

- **`CLAUDE.md`** — the Key Rule "Core is the kernel; EVERY capability is a plugin candy" is RENAMED +
  rewritten to **"Core is a PLUGIN HOST; EVERY capability is a plugin candy"**: core keeps ONLY the
  plugin-host fabric (loading, registry/transports, prescan-dispatch, the kind-decode MATERIALIZE, the
  reverse-channel broker), imports ONLY `sdk/spec`+proto, and everything else currently in `charly/` is
  tracked K-wave migration inventory. Adds the two standing rules (IMPORT-PURITY + ZERO-ALIASES) + the P16
  TRIPLE gate. The boundary-law **(M)** clause drops "irreducibly on the load/build/deploy spine" (only
  loading/dispatch/materialize/broker are in-core M); **(D)** gains `loaderkit.Threaded`. R5 corollary on
  R5 (NO-NEW-ALIASES/IMPORT-PURITY); a 2-line pointer to the agents RESPONSIBILITY MATRIX in the Agents
  brief; the internal self-reference to the renamed heading swept.
- **`.claude/hooks/pre-commit-gate.sh`** — new **ZERO-ALIASES teeth** (`assert_no_new_alias`,
  tier-independent, fail-OPEN): blocks a commit staging a NEW `charly/*_aliases.go` file OR a
  declaration-form kit-alias line (`type X = kit.Y` / `var x = kit.Y`) in any `charly/*.go`. Deliberately
  DOES NOT match a plain residual kit call or import (a K-wave mechanism move leaves those legitimately —
  the pr-validator's migration-pattern exception judges them).
- **`.claude/hooks/gate_test.py`** — +4 cases (new alias file → BLOCK, type-alias line → BLOCK, residual
  kit CALL → ALLOW, residual kit IMPORT → ALLOW). 81/81 green.
- **`VISION.md`** — R5 cross-ref POINTER sweep ONLY (`"Core is the kernel; EVERY capability is a plugin
  candy"` → `"Core is a PLUGIN HOST; …"`), a renamed-Key-Rule heading fix — **NOT a VISION content
  change** (the design detail lives in `/charly-internals:plugin` per the authoritative-copy registry).
- **`plugins`** gitlink → the merged D2 plugins half.

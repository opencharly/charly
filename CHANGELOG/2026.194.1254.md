# 2026.194.1254 — K4 Bucket D: preempt-const single-source (spec) + test-alias-death + R1

2026-07-13

The K4 (deploy + config resolution → sdk/deploykit) survey established that the bulk of
the deploy-resolution surface is either envelope-gated (it consumes host types —
`*BoxMetadata` et al. — so per the end-state north-star §4.2 it WAITS for the
`spec.#ResolvedProject` envelope, S-K5 PROVEN), KEEP-FABRIC (the wire-broker
`externalDeployTarget` / substrate-lifecycle proxy / registry dispatch stay core as
kind-blind Mechanisms), or reassigned to other owners (the pod-overlay + privileged-runner
build engines → K3/buildkit; deploy_ref → K1; the k8s/android/gpu preresolvers → their
substrate plugins). This cutover is the small, host-type-FREE residue that lands NOW; the
resolution-body relocation lands next-wave as the first `#ResolvedProject` consumer.

## What changed (all behavior-preserving)

- **Preemption-policy consts single-sourced to `spec` (R3 dedup).** `charly/deploy.go`
  defined package-main `PreemptStopShutdown` / `PreemptRestoreAlways` /
  `PreemptRestoreSuccess` consts duplicating the authoritative `spec.Preempt*` values
  (`sdk/spec/arbiter_wire.go`). The package-main definitions are deleted; the two consumers
  (`charly/validate_preempt.go`, `charly/preempt_schema_test.go`) reference `spec.Preempt*`
  directly. Identical string values (`"shutdown"` / `"always"` / `"on-success"`).
- **Three test-only deploykit aliases converted + deleted.** `bundleWalkPostOrder`,
  `preemptEffectiveStop`, and `encryptedPlainDir` in `deploykit_state_aliases.go` had only
  test call sites; those tests now call `deploykit.BundleWalkPostOrder` /
  `deploykit.PreemptEffectiveStop` / `deploykit.EncryptedPlainDir` directly, and the three
  alias entries are removed (`preemptEffectiveRestore` stays — it has a non-test caller in
  `arbiter_host.go`, tracked residue).
- **R1 stale-comment fix.** Removed the dangling `occupiedHostPorts` doc-comment in
  `charly/deploy.go` (the function relocated to `sdk/deploykit`, leaving an orphaned
  comment). (The sibling `sdk/kit/deploy_types.go` stale-reference fix rides the next-wave
  sdk PR per the K4 Bucket-D ruling.)

## Registered on the K5 deploy-cleanup wave (deferred by design, not neglect)

- The broad FREE-alias-death (converting core call sites in config_image/enc/start/shell/
  secrets/data to direct `deploykit.X`) is THROWAWAY under the north-star: those files move
  wholesale to their substrate plugins at K5, which deletes any core conversion — the alias
  entries die naturally with their consumers.
- `InjectSecretsIntoPlans` → deploykit + the `generateRandomSecretToken` R3-dedup (a
  duplicate exists in `candy/plugin-secrets`) ride the K5 consumer move.
- The config-resolution BODY move (config_image `runConfig` → deploykit) is §4.2-gated on
  the now-PROVEN `spec.#ResolvedProject` envelope; it lands next-wave taking a resolved
  `#ResolvedBoxView` instead of `*BoxMetadata`.

Net charly/ non-test delta: −13 LOC. Gated by `charly check run check-preempt-local`
(the matched bed composing check-preempt-app = the validate_preempt surface the const-dedup
touches) — PASS 5/5, exit 0, on the frozen final code through the fresh-`charly update` cycle.

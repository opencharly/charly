# 2026.194.1713 — #ResolvedProject host FILL + the `resolved-project` spine seam (K5-Unit-0, superproject half)

The thin host FILL for the RESOLVED-project envelope (the sdk half added the `spec.ResolvedProject`
family — see the sdk CHANGELOG). This is a DATA PROJECTION over the resolve engines that already
exist — `ResolveBox` (per enabled box) + `ScanAllCandy` + the folded `uf.Bundle` deploy tree —
serialized into the generic `spec.ResolvedProject`. It is NOT a new engine: `+150` core LOC of
field-copy, dropping the 6 host-only `json:"-"` compute-cache pointers of `ResolvedBox` that are never
wire data. This is an ADDITION — the DELETIONS come when the ~20k of registered K5 IOU consumers
(inspect/list, the bundle-add box graph, status collectors, the bed runner) collect against this
envelope in their own later K5 units.

## What's new — `charly/resolved_project_host.go` (+ `charly/resolved_project_host_test.go`)

- **`projectResolvedBox(*ResolvedBox) spec.ResolvedBoxView`** — field-copy of the non-`json:"-"`
  fields; the 6 caches DROPPED.
- **`projectCandyView(*Candy) spec.CandyView`** — field-copy over the runtime `Candy`'s exported
  accessors (identity + `bareRefs(Require/IncludedCandy)` + `EnvProvides()`/`MCPProvide()` +
  `PortSpecs()`/`Service()`).
- **`projectResolvedProject` / `buildResolvedProjectFromDir`** — the assembler + the load+project
  entry (enabled boxes via `ResolveBox`, candies via `ScanAllCandy`, the deploy tree from `uf.Bundle`).
- **`hostBuildResolvedProject`** — exposed as the generic action-noun HostBuild kind
  **`resolved-project`** (F11 — a plugin requests it via `Executor.HostBuild` over the established
  reverse channel; ALSO callable host-side directly). Registered idempotently like every other
  `hostBuilders` entry; never a provider word (F11 uniform-API invariant).

## The spine seam (inventory — no seam dies YET; the trajectory is recorded)

`resolved-project` is the SPINE seam per the end-state north-star §3: the resolved projection
consolidates the per-concern host-resolve seams over time. As their CONSUMERS migrate in later K5
units, these existing seams **die into `resolved-project` at their consumer's K5 unit** (they are NOT
removed here — this unit only adds the envelope + its fill):

- **`config-resolve`** (`host_build_config_resolve.go`, `spec.ConfigResolveReply`) — the per-entity
  vm/resources config lookup it re-derives per call is read directly from `#ResolvedProject`; dies at
  its K4/K5 consumer unit.
- **`status-substrate`** (`substrate_lifecycle_grpc.go` `OpStatus`) — its `BundleConfig.Bundle` +
  deploy-tree config data collapses into `#ResolvedProject.deploy`; the LIVE `ContainerSnapshot` stays
  a verb/engine probe (not project data). PARTIAL death at the status consumer's K5 unit.
- **`check-config`** (P12 bed/gather) — already `spec.Deploy` off `uf.Bundle`; folds into
  `#ResolvedProject.deploy`, no separate reply needed.

**Deferred, planned envelope members (registered, not omitted):** the future `#ProvidesView` (the
bundle's `provides:` map) + `#SidecarView` (the sidecar-template library) are additive members of
`#ResolvedProject`, added when their first consumer's K5 unit lands. This unit ships exactly the two
new views + the existing `#Deploy` tree (verdict recommendation #1 + the keep-it-small brief).

## Boundary-law placement (P16)

The FILL + the host-builder STAY core as the kind-decode MATERIALIZE half (M) + the wire broker's
host-build seam — a DATA projection over the resolve engines, no `spec.<Kind>` switch, no per-kind
map. The envelope itself is a generic E-envelope (`sdk/spec`). No new `charly/*_aliases.go`, no new
`charly/` import of an sdk mechanism kit (imports `sdk/spec` only) — IMPORT-PURITY + ZERO-ALIASES
intact. Expected core-LOC delta: **~+150** (addition; the K5 register's ~20k of deletions land at the
consumers).

## Verification

- **sdk half**: `task cue:gen` reproducible (`TestGenReproducible`); sdk `go build`/`go test`/
  `golangci-lint` (0) clean.
- **superproject**: `go build ./...` + `go test ./...` (exit 0) + `golangci-lint run` (0 issues) +
  `gofmt` clean. Per-tree binary (`task build:binary`) builds; `charly box list candies` loads a real
  project through the concatenated schema (with the 4 new defs) — no regression.
- **Two new consumer tests (the check-coverage gate — would FAIL without the projection):**
  - `TestProjectResolvedBox_CompleteAndNoCacheLeak` — completeness: every field `box inspect`
    serializes survives the projection (canonical-key superset vs `json.Marshal(*ResolvedBox)`; a
    dropped/renamed field FAILS) + no host-only cache leaks (guards the `InitSystem` exclusion).
  - `TestResolvedProject_ByteStableGolden` — the assembled `spec.ResolvedProject` is deterministic +
    byte-stable vs the committed golden (`testdata/resolved_project_golden.json`).
  - `TestResolvedProject_SeamRoundTrip` — the plugin-side path: request the envelope over the
    registered `resolved-project` HostBuild, decode `spec.ResolvedProject` faithfully (the wire
    contract the K5 consumers depend on).
- The matched R10 light bed gate (`check-commands-local` + one VM bed exercising load+resolve — the
  projection is read-only) is the orchestrator's.

## sdk pointer

Bumps the `sdk` submodule to the merged `feat(spec)` #ResolvedProject-envelope commit (cross-repo
order: sdk first) + the `charly/go.mod` require version. No `charly.yml` schema-version change (the
new defs are non-authoring generated wire types, no authored wire key).

*Assisted-by: Claude (analysed on a live system)*

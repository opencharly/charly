# 2026.194.1736 — K3-A: BUILDER render engine → sdk/deploykit (core is the seam-wiring half)

2026-07-13

The build-time BUILDER render engine — the multi-stage detection builders (pixi/npm/aur), the
`external_builder:` out-of-tree builders, the pre-main-FROM stage assembly, and the artifact COPY
emitters — relocated OUT of charly core into `sdk/deploykit` (see the sdk CHANGELOG for the moved
engine). Core keeps ONLY the registry-coupled seam impls it wires onto the render `Generator`. This
is the first K3 (build-engine relocation) unit of the v2 plugin-host program: the render BODIES leave
the kernel; `charly/generate.go` shrinks 343 lines, core net −299 non-test LOC. `charly box generate`
output is BYTE-IDENTICAL (proven against the box/fedora + box/arch goldens).

## What moved / what stays (the K3-A seam)

- **Relocated to sdk/deploykit (the BUILDER render):** `emitBuilderStages`, `emitExternalBuilderStages`,
  `emitExternalBuilderArtifacts`, `emitBuilderArtifacts`, `emitPreMainCandyStages`, `builderResolveInputFrom`
  (exported), `expandBuilderPath`, `resolveDetectionBuilder`, and `detectExternalizedBuilders` (as the
  shared `deploykit.DetectExternalizedBuilders` pkg func) — over `CandyModel` + `buildkit.ResolvedBox`.
- **Stays host (the registry-coupled seam impls, boundary-law R-class reach):** the OpResolve Invoke +
  provider resolution stay in `charly/tasks.go` as the 3 seam closures wired onto the render Generator
  (`EnsureBuildersConnected` / `ResolveDetectionBuilderStage` / `ResolveExternalBuilderStage`), preserving
  every builder error string byte-exact. `resolveBuilderStage` / `resolveExternalBuilder` / `emitBakedPlugins`
  stay host (registry + host-build coupled).

## Changed (charly core)

- `generate.go`: the builder-cluster bodies DELETED; `generateContainerfile` now dispatches through
  `g.toDeploykit().EmitPreMainCandyStages` / `EmitBuilderArtifacts` / `EmitExternalBuilderArtifacts`.
- `tasks.go`: `toDeploykit()` wires the 3 new registry seams; the 2 seam closures added;
  `resolveInlineBuilderSeam` repointed to the exported `deploykit.BuilderResolveInputFrom`.
- `builder_preresolve.go`: the deploy build pre-pass calls the shared `deploykit.DetectExternalizedBuilders`
  (R3 — one detection impl for the render AND the pre-pass); the in-core copy DELETED.
- `step_emit_hostbuild.go`: repointed to `deploykit.BuilderResolveInputFrom` (×2).

## Removed (charly core, hard cutover — no aliases, no shims)

- The moved builder-cluster funcs (above) + the in-core `detectExternalizedBuilders` / `buildFormatsInclude`.
- Dead-code sweep (R5, lint-driven): the now-orphaned `Generator.externalBuilderReplies` /
  `detectionBuilderReplies` fields (the moved render uses the deploykit Generator's caches) + the
  `emitScratchStages` / `emitExtractStages` / `candyNeedsBuilder` shims (their only callers moved).
- R5 claim-keyed sweep: 10 comment cross-references to the moved funcs updated to name the relocated
  deploykit methods; `git grep` on the deleted identifiers returns only accurate relocation pointers.

## sdk pointer

- sdk submodule bumped to the merged K3-A tag **v0.2026194.1727** (`sdk` gitlink + `charly/go.mod` require).

## R10 gate (orchestrator-owned build-class beds)

Live build-class gate on the frozen tree + stamped binary v2026.194.1658:
**check-builder-vm PASS (9 steps, 185s — the moved builder-OpResolve cluster driven LIVE through the
deploykit render, npm detection builder end-to-end) ∥ check-commands-local PASS (5 steps, 117s — 6/6 plan
checks incl. box-new-validate-generate through EmitPreMainCandyStages)** + byte-parity goldens fedora
(470 Containerfiles) + arch (111) BYTE-IDENTICAL. Coverage spans all 3 builder paths (detection, external_builder
via check-pod, inline cargo); builder error strings audited byte-exact. After merging origin/main (K5U0
envelope + P12, disjoint) the reconcile delta re-gate re-verified: both modules build, full charly test suite
green, golangci-lint 0, gofmt clean, byte-parity still BYTE-IDENTICAL.

Registered K3-C follow-ons (not in this cutover): GROUP-B emitter var-aliases + their 71 tasks_test.go emitter
tests → deploykit; `emitBuilderArtifacts` dead vocabulary-branch RCA + delete; `resolveShellImageRef` +
`ResolveNewestLocalCalVer` → sdk/enginekit.

Assisted-by: Claude (fully tested and validated)

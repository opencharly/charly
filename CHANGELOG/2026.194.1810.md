# 2026.194.1810 — plugins pointer: teammate context-lifecycle policy (agents skill)

Bumps the `plugins` submodule to 761e074 (plugins release 2026.194.1805), integrating
the operator-directed teammate context-space management policy into
`/charly-internals:agents`:

- Reuse is TASK-SCOPED and STRICTLY ENFORCED: shutdown on task completion is mandatory
  for teammates and every other agent; every new task (related or not) starts a NEW
  empty-context teammate/agent; a program of related cutovers is one fresh teammate
  per cutover.
- Same-task continuation stays endorsed (PR fix rounds, a chain's next repo leg,
  rebases, orchestrator-ruled same-unit re-scopes).
- Spawn sizing (one task per context budget), durable-artifact discipline (stopping is
  lossless), the validator one-chain variant, and the mid-task context-pressure path.

Documentation-only pointer bump (the bumped submodule commit's own diff is two `*.md`
files).

*Assisted-by: Claude (documentation reviewed)*

# 2026.194.1854 — plugins pointer: wave-3 orchestration findings (agents + git-workflow skills)

Bumps the `plugins` submodule to 4a3dff9 (plugins release 2026.194.1841), integrating six
wave-3 orchestration findings into the skills:

- Migration-ledger discipline: rows conflated-until-measured; enablers named by exact
  SURFACE with mandatory re-audit on landing; a gap inside an assigned cutover means
  build the enabler in the cutover; exclusions only as per-file boundary-law rows.
- Undecomposed rows are UNMEASURED (authoring rule) + the recurring same-signed-variance
  → estimator-RCA meta-rule + bidirectional call-graph sizing (file-LOC is a floor,
  never the move).
- TaskStop semantics: a shutdown message is not a stop — the process and pane persist
  until `TaskStop(<name>)`, fired at acceptance.
- Rooting scope-of-validity (R1 claim sweep, all four surfaces): USER/MANAGED-level
  grants resolve independently of project root; superproject rooting remains the rule.

Documentation-only pointer bump (the bumped submodule commit's own diff is two SKILL.md
files, one agent definition, and a CHANGELOG entry).

*Assisted-by: Claude (documentation reviewed)*

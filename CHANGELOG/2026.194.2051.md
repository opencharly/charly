# 2026.194.2051 — plugins pointer: statekit delivered-as-kit sweep + gate-note + changelog fixes

Bumps the `plugins` submodule to 62ad1bb (plugins release 2026.194.2038), integrating the
w4 docs batch:

- `/charly-internals:plugin` statekit sweep (R1): the three claims asserting a future
  `sdk/statekit` corrected — the flock'd any-process-safe state family was delivered in
  `sdk/kit` (filelock/install_ledger/deployconfig, P4/C9); the K2 ledger row records the
  residual routing (loader-coupled → K5 seam-death; lifecycle body → K4-A).
- `/charly-internals:git-workflow` B7 gate-note: the `pre-commit-gate` blocks
  `git -C "$VAR" commit` forms — a parse requirement with one sanctioned literal-path
  re-issue.
- Merged-CHANGELOG factual fixes (the 2026.194.1841 intro count/split).

Documentation-only pointer bump (the bumped submodule commit's own diff is two SKILL.md
files + two CHANGELOG files).

*Assisted-by: Claude (documentation reviewed)*

# 2026.194.2258 — Collection A: check-config seam DEATH + inspect/list → plugin-box (envelope consumers land with the S-CM enabler)

K5 Collection A (super leg). The resolved-project envelope (grown in the sdk leg) replaces the check-config
host seam and moves `charly box inspect`/`list` into candy/plugin-box. ~1.3–1.5k core LOC out. Validate ENGINE
DEFERRED to #60 (see the deferred rows) — validate.go stays WHOLE + untouched in core, one-phase-clean.

## `!` BREAKING — `charly box inspect <box>` default JSON is now snake_case

The no-`--format` default output marshals the wire-safe `spec.ResolvedBoxView` (snake_case + omitempty),
replacing the former `json.Marshal(*ResolvedBox)` (mixed-case + non-omitempty). Per-`--format` outputs are
unchanged. The View is the already-landed K5U0 wire type (K5-Unit-0) with existing consumers — retagging it or
adding a parallel def was rejected (R3), so inspect's output becomes it. In-tree consumers of the old casing
were swept; the change is external-facing only.

## Removed (R5 — the check-config seam + its mechanisms die)

- `charly/host_build_check_config.go` (the whole "check-config" host-builder + `checkConfigBuilderKind`).
- `charly/check_include.go` (`ExpandPlanIncludes`) + `charly/check_score_kind.go` (`ResolveIterateSandbox`) —
  moved INTO candy/plugin-check (kind-blind splicers, rewritten over the envelope's Deploy tree + CandyModels +
  BoxPlans + Templates).
- `charly/check_runner_cmd.go` (`scorePodTargetEntry`) — the per-host-overlay pod-disposability read now rides a
  thin `pod-disposable` host seam.
- The reply's dead `ReadinessJSON` (+ the dead `bed_run.go:50` defer comment) — verified unconsumed.
- `charly/main.go` `InspectCmd`/`ListCmd` + `alias_collect.go ListAliasesCmd` + `volume_cp_tags_cmd.go ListTagsCmd`
  registration + the now-dead `InitCandy`/`RouteCandy`/`VolumeCandy`/`AliasCandy` predicate helpers.

## Added / Changed

- **candy/plugin-check** computes the former check-config projection off `HostBuild("resolved-project")`:
  `resolveCheckProjection` + `expandPlanIncludes` (candy→CandyModels, box→BoxPlans, pod/vm→Templates RawBody
  decode) + `resolveIterateSandbox`. Its `.golangci.yml` (the P12-relocation CLI-write policy) landed.
- **candy/plugin-box** gains `command:inspect` + `command:list` (`inspect_list.go`), consuming the envelope; the
  M-core overlay/store branches (tunnel/bind_mounts, list tags) ride hidden `__box-inspect-overlay`/`__box-list-tags`
  reentries.
- **`charly/resolved_project_host.go`** projector fills the grown envelope (projectCandyModel; box-aggregate
  collectors; build-vocab from LoadDefaultBuildConfig; kind-blind `projectTemplates` → RawBody; AgentBodies;
  BuildTargets via ComputeIntermediates+ResolveBoxOrder; BoxPlans via CollectDescriptions).
- **Iterate-sandbox preflight remediation** (R2, folded from the acceptance roster): `charly check run
  <iterate-entity>` on a POD sandbox now fails EARLY with a `charly bundle add … --disposable` + `charly
  start` remediation when the operator-provisioned per-host sandbox container is not running, instead of
  the bare exit-125 (`container does not exist`) the downstream `podman exec` emitted. Guards only the
  non-disposable path (the disposable path is already restarted); unit-tested via a `podContainerRunning`
  seam. Converts the former swallowed absent-sandbox signal into a diagnostic.
- Domain-1: R3 dead-dup `check_score.go`/`check_substitute.go` deleted (−473); the score/substitute tests
  relocated to plugin-check.
- Two stale "future statekit" comments (runlocal.go:255, host_build_check_bed.go:41-42) corrected to the
  delivered K4-B reality (the flock'd state family lives in sdk/kit filelock/install_ledger/deployconfig).
- **sdk dependency**: `sdk` gitlink → `3ff36ae` (merged Collection A sdk leg, tag `v0.2026194.2234`) + `charly/go.mod`
  require bumped to `v0.2026194.2234`. In-tree builds resolve via `replace github.com/opencharly/sdk => ../sdk`;
  the require version matters for out-of-tree consumers only.

## Enabler inventory (per-surface)

- **Consumed in this PR** (runtime-tier via the check-bed roster + the live inspect/list run): CandyModels.Plan +
  Templates + BoxPlans (check-config); box-aggregates + CandyView-growth + BuildTargets (inspect/list).
- **#60-consumed enabler** (K5U0 pure-enabler precedent, `analysed on a live system`): the build vocabulary maps
  (Distro/Builder/Init) + the CandyModel validate-widen fields — their consumer is the very next unit (#60).

## Deferred (named-exit rows)

- **validate ENGINE (2242 LOC) → #60** — the ~500-LOC resolution-graph subset needs charly-core resolve
  machinery (fail-fast projection vs error-tolerant validate; the 40-method `deploykit.CandyModel` interface a
  flat struct can't satisfy; `*Config`-only `ComputeIntermediates` + namespace resolver). #60 owns the K3-D
  resolve half + the OpValidateProject seam (#46, folded).
- **status-entangled (k8s/adb/nested) → #60/K2** — blocked on the per-host-overlay-merge (K2/statekit) the
  envelope structurally excludes. The clean status framework slice rows out (≈0 gap-free: CollectOpts-bound to
  the live collectors).

# 2026.195.0000 — the RED kernel-manifest floor gate (P16a, the C15 residue tracker)

The mechanical enforcement of CLAUDE.md's "The kernel/plugin boundary law": a new
package-main test, `charly/kernel_manifest_floor_test.go`, pins `charly/`'s non-test
`.go` file set to the CORE-FABRIC allowlist. A file absent from the allowlist is an
R-item (a concrete kind's schema/shape/validation/behaviour) that leaked into the
kernel — an "incomplete seam" — and the test FAILS, listing every such residue
grouped by its owning cutover. The failure message IS the living tracker that
sequences the rest of the core-minimization program.

- **`charly/kernel_manifest_floor_test.go`** — `TestKernelManifest_CoreIsPinnedToTheFabricFloor`
  walks `charly/*.go` non-test files against two tables: `kernelFloor` (the
  E/M/B/D fabric allowlist, each entry carrying its boundary-law clause) and
  `residueOwner` (every tracked-for-removal file → owning cutover). The two
  tables are the ONLY edit surface as cutovers land; the test logic is fixed.
- **FLOOR (62 files)** — kind-AGNOSTIC fabric: plugin loading/registry/transports
  + prescan/word-dispatch + the kind-decode MATERIALIZE (`node_*`, `cue_loader`/
  `cue_normalize`/`cue_schema`) + the reverse-channel wire broker
  (`deploy_target_*`, `unified_targets`, `substrate_lifecycle*`,
  `plugin_*_reverse`, `plugin_dispatch_reverse`, `plugin_step_external`,
  `check_endpoint_resolve`) + the `candy`/`box` bootstrap root + kind-recognition
  DATA (`version`, `main_freshness`, `loader_threaded`, `reserved_registry`) +
  the C14 hardware-blocked GPU exception (`gpu_*`, `devices`).
- **RESIDUE (243 files at authoring, off current main)** — tagged by owning
  cutover: **P8b** 41 (build engine + the #67 render α-fold), **P11** 63 (pod
  deploy surface + substrates), **P12** 29 (check/ADE), **P13** 17 (bundle CLI +
  the `deploy.go` state-model fold), **P14** 25 (status/alias/scaffold/OCI),
  **P15** 68 (residual folds + HostArbiter + K1 loader-orchestration + K5
  seam-death + aliases + misc CLI utils). Counts trend to zero as the waves
  land; the map already shrank once (P11/P12/P14-partial landed between T0 and
  this rebase — 25 deleted files pruned, 22 new host-side seam files classified).

**Intentionally RED.** The test is authored at program T0 and merges LAST (P16),
GREEN, once every residue has folded into its owning cutover. It CANNOT pass
branch protection today by design; it is the living tracker that guides/targets
P8b/P11–P15. As each cutover lands, residue files vanish (deleted → prune the
stale `residueOwner` entry) or move to `kernelFloor` (became fabric) — the residue
count trends to zero and the test goes green. No runtime surface (test-only);
SDD/ADE N/A.
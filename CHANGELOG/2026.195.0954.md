# 2026.195.0954 — docs: multi-committer main-advance handling (out-of-tree PRs)

CLAUDE.md mandate pointer: the landing-concurrency sentence now names out-of-tree
`main` advances from other committers — the orchestrator fetches `origin/main`
(and each submodule main) before each PR-open/merge, applies `gh pr update-branch` +
risk-proportional delta re-gate + forward-gitlink verify to ANY main advance
(internal or external), and RE-RDDs a divergent-lineage external submodule bump on a
`disposable: true` bed rather than blindly descendant-wins. Bumps the `plugins`
gitlink to the merged `git-workflow` skill leg (the "Multi-committer main advances"
subsection). Documentation only — no behavioral change.

Assisted-by: Claude (documentation reviewed)

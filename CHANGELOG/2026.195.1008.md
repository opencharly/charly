# 2026.195.1008 — Add the Codex rulebook

- Add a standalone, present-tense `AGENTS.md` that implements `VISION.md` and
  gives Codex the same dispatcher, engineering mandates, R0–R10 gates,
  architecture constraints, landing workflow, and confidence rules as the
  independently maintained Claude rulebook.
- Map Claude-specific team roles to fresh Codex agent threads without adding a
  second policy, weaker gate, or dependency on reading `CLAUDE.md`.
- Require AI attribution to retain the established `Assisted-by:` form while
  naming the exact provider model; explicitly keep 100% human commits
  trailer-free and valid.
- Require structured GitHub Markdown PR bodies submitted through `--body-file`
  while preserving the established italicized attribution footer.

# 2026.195.1008 — Add Codex foundation and narrow hook gates

- Add the thin Codex `AGENTS.md` adapter for repository discovery, fresh-context
  validation, root-cause analysis, and worktree-local binary usage.
- Require AI attribution to retain the established `Assisted-by:` form while
  naming the exact provider model; explicitly keep 100% human commits
  trailer-free and valid.
- Narrow the commit hook to anti-bypass, tokenization, and Go-lint mechanics;
  leave attribution, confidence, changelog, architecture, and R0–R10 policy to
  the fresh PR validator so duplicate regex gates cannot drift.
- Direct Codex to audit organization-wide GitHub conventions at their `.github`
  source and verify the complete protected-repository roster.
- Add a Codex runtime preflight for inherited validator capabilities,
  superproject rooting, writable tool caches, and approved GitHub access.
- Require Codex to verify commit metadata before push, keep validation
  artifact-free, stop an anomalous validator without retry or PASS, restart
  validation after a separate RCA, and defer gate selection and gitlink
  recovery to their canonical skills.
- Define one Codex validator architecture: a self-contained immutable input
  envelope, protected-object bootstrap, repository/object binding, bounded
  manifest-complete inspection, canonical evaluation, and fail-closed verdict
  state machine.
- Require structured GitHub Markdown PR bodies submitted through `--body-file`
  while preserving the established italicized attribution footer.

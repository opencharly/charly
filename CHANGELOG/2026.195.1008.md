# 2026.195.1008 — Add the Codex rulebook

- Add a standalone, present-tense `AGENTS.md` that implements `VISION.md` and
  gives Codex the same dispatcher, engineering mandates, R0–R10 gates,
  architecture constraints, landing workflow, and confidence rules as the
  independently maintained Claude rulebook.
- Map Claude-specific team roles to fresh Codex agent threads without adding a
  second policy, weaker gate, or dependency on reading `CLAUDE.md`.
- Require AI attribution to retain the established `Assisted-by:` form while
  naming the exact provider model; explicitly keep 100% human commits
  trailer-free and valid.
- Narrow AI-harness commit hooks to immediate command mechanics and staged Go
  lint; leave attribution identity, confidence, change class, changelog,
  architecture, and R0–R10 proof to the fresh PR validator.
- Require structured GitHub Markdown PR bodies submitted through `--body-file`
  while preserving the established italicized attribution footer.
- Restore missing terminating newlines in the affected historical changelogs.
- Use the harness-neutral `charly/pr-validator` status name in both root
  rulebooks; the central `.github` protection manager owns the all-repository
  enforcement migration.
- Preserve trustworthy evidence across agent handoffs, scope invalidation to the
  affected conclusion, and forbid duplicate RCA or validation runs against an
  unchanged candidate merely to obtain a cleaner process report.
- Bump `plugins` to the merged independent-rulebook, validator-ownership, and
  harness-neutral status guidance.
- Remove the duplicate repository-local branch-protection manager; the merged
  `opencharly/.github` manager is the sole organization-wide owner.
- Bump `plugins` to the merged skill pointer for that central owner.
- Bump `plugins` to the merged skill guidance naming that central owner.

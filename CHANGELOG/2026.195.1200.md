# 2026.195.1200

## Security

- Pin the OpenClaw candy to `2026.7.1` so dependency resolution cannot select
  releases affected by the repository's open Dependabot advisories.
- Bump `sdk` to ignore the empty dependency emitted by resolved `from:` boxes,
  restoring whole-project validation with regression coverage.

# 2026.195.1200

## Security

- Pin the OpenClaw candy to `2026.7.1` so dependency resolution cannot select
  releases affected by the repository's open Dependabot advisories.
- Bump `sdk` so only local bases become local graph edges, excluding empty
  `from:` bases and namespaced remote bases with regression coverage.
- Capture and assert the expected missing-agent and departed-holder diagnostics
  in their tests so a failing package does not leak warning-like output.
- Resolve nested CLI execution from the running binary's absolute path so `-C`
  cannot break check-bed steps, and retain spawn diagnostics in step logs.

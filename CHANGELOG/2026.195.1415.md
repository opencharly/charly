# 2026.195.1400 — docs: bump plugins gitlink → 7c96acf (vm-stop caveat + validator-ownership docs)

Pure submodule-pointer bump (documentation-only change class). Advances the
`plugins` gitlink `a4cc7a5 → 7c96acf`, picking up two docs-only plugins commits:

- `7c96acf` — docs(vm): refresh the stale `charly vm stop` caveat to match the #77
  code on main (`vmHolder` authoritative libvirt-then-qemu probe, `stopVmDomain`
  poll-until-`DomainRunning`, wedged-guest → hard failure with `--force` pointer) —
  the docs leg of the vm-lifecycle-correctness batch (#77/#76, super `eb73680`).
- `e614758` — docs: align Codex attribution and validator-ownership wording across
  the agents/git-workflow skills + the pr-validator/testing-validator agent specs.

Both commits' diffs are all `.md` (certified by the pre-commit-gate's submodule
recursion), so the pointer bump lands at the `documentation reviewed` tier — no
behavior change, no bed, no build. Tagged at merge.
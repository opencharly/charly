# 2026.196.0001 — The Cutover Sizing Law (superproject leg)

Companion to plugins v2026.195.0632 (#54). Root cause of a thrice-recurred
drift: the operator standing order to batch non-blocking fixes existed only
in conversation while the committed default ("a non-blocking issue as its
OWN immediate-next cutover") pulled every fresh context back to per-issue
landing ceremonies via CLAUDE.md, the skills, and the hook reminders.

- CLAUDE.md R2: the thematic batch IS the "immediate-next cutover" for
  non-blocking work; solo landing ceremonies per small fix forbidden.
- CLAUDE.md "Hard Cutover by Default": the **Cutover Sizing Law** mandate —
  large by default, ceremony paid once per theme, batching (too-small
  remedy) and decomposition (too-big remedy, forward motion only) as the
  two directions of one law, gate rosters trimmed to beds that can fail on
  the change, independent validator legs concurrent.
- Post-execution paragraph + acceptance checklist swept to the batch form;
  the verb-gap Key Rule routed through the sizing decision procedure.
- .claude/hooks/end-of-turn-challenge.sh: the R2 reminder line now routes
  non-blocking findings to their NAMED thematic batch (script gate: bash -n
  parse + live invocation, rc=0).
- plugins gitlink → 2f37adbe (the merged sizing-law skills leg).

# 2026.196.2359 — Enforce dual-harness developer configuration

- Keep `CLAUDE.md` and `AGENTS.md` independent while validating equivalent R0 skill dispatch.
- Add a repository Codex marketplace and validate the full developer profile without user-level configuration.
- Configure trusted Codex sessions for the full host access required by Charly development and document the sandbox boundary in `AGENTS.md`.
- Validate current cross-repository skill references so hard renames cannot leave broken guidance behind.
- Document the shared Claude Code and Codex setup without making MCP part of configuration parity.
- Consume the portable plugin catalog and skill-name cutover.
- Make concurrent runtime plugin builds keep VCS stamping while using read-only Git status probes, and propagate the original plugin-load failure.
- Separate strict operator VM destruction from explicit idempotent `--if-exists` reconciliation so concurrent bed cleanup is quiet and complete.
- Pin Codex work to a protected-main linked worktree, the sanctioned stamped local binary, and existing normal Go caches in the full Codex rulebook.
- Make the canonical agent-configuration validator always execute its own meta-test, removing a separate-command naming failure mode.
- Record and enforce group-member rebuild and final teardown as R10 steps, and bind Codex evidence review to exact run directories.
- Propagate joined member-teardown failures through check beds and operator bundle deletion while still attempting every member.

Assisted-by: Codex OpenAI GPT-5.6 Sol (fully tested and validated)

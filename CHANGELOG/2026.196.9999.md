# 2026.196.9999 — P11c: relocate pod-overlay OCITarget → sdk/deploykit + move PodDeployTarget to candy/plugin-deploy-pod

The pod OVERLAY deploy surface moves out of `charly/` core into the compiled-in
`candy/plugin-deploy-pod` (mirroring the #67/plugin-build pattern), so the kernel keeps
ONLY the kind-blind prep+resolve M-seam + the host-side overlay builder machinery, and the
concrete pod overlay behaviour (the OCITarget walker + the PodDeployTarget overlay
Containerfile assembly + podman build + tag) lives in the sdk kit / candy. This is the
P11c unit of the core-minimization program, unblocked by #67 (render α-fold) + P8b (build
engine absorption) + K5-Unit-0 (#ResolvedProject host seam).

## Unit 1 — OCITarget → sdk/deploykit (the shared kind-blind render M-mechanism)

- NEW `sdk/deploykit/oci_target.go`: the `deploykit.OCITarget` walker (Emit/emitPlan/
  emitStep/String + the `EmitStepOp` function-value seam). The kind-blind step DATA layer
  (`spec.InstallStep` + the 13 concrete step structs + `ExternalStep`/`IsExternalStepKind`/
  `StepToView`) is already sdk-importable; the kind-specific DISPATCH stays core, reached
  through the `EmitStepOp` seam (the candy wires it via `HostBuild("render-seam",
  "oci-emit-step")`). Byte-parity by construction — the host seam reuses the EXACT former
  in-core `emitStep`/`spliceClassStepEmit`/`stepProviderFor` dispatch.
- `StepProvider.EmitOCI` refactored to RETURN `(string, error)` (decoupled from the OCITarget
  buffer, which now lives in sdk/deploykit). Bounded: the interface (provider_step.go) + the
  SOLE impl `externalPluginStepProvider.EmitOCI` (plugin_step_external.go, uses `build.Box` +
  returns the fragment) + the SOLE caller.
- NEW `charly/oci_step_emit.go`: the core step-emit dispatch relocated out of OCITarget
  methods into standalone `ociEmitStep`/`ociSpliceClassStepEmit` (the EXACT former logic,
  taking `build buildEngineContext`). The host "oci-emit-step" render-seam handler calls
  `ociEmitStep` with the cached overlay buildEngineContext. The transitional in-core
  `OCITarget.emitStep` delegates here (R3, one source of truth).

## Unit 2 — shrink core hostBuildOverlay + move PodDeployTarget to candy/plugin-deploy-pod

- `hostBuildOverlay` (build_overlay.go) shrinks to prep+resolve ONLY: NewGenerator
  (ExtraCandyRefs) + createRemoteCandyCopies + prepareCandySecrets + base-ref resolve +
  ExtractMetadata + cache the overlay buildEngineContext per-dir. KEEP: `hostBuilders`
  registry + `overlayBuilderKind` + `overlayBuildInputs` ctx carrier + the `overlay`
  HostBuild KIND + the `step-emit` host-builder.
- MOVE `PodDeployTarget` (deploy_target_pod.go) + the render call + podman-build + tagDeployAlias
  into `candy/plugin-deploy-pod`: `podPrepareVenue` calls `HostBuild("overlay")`→prep
  envelope, builds `deploykit.OCITarget` (wires EmitStepOp), assembles the overlay Containerfile
  (FROM scratch stages, FROM base, USER root, service fragments, security-label merge, USER
  restore), runs podman build + tag. The candy imports sdk/spec+deploykit+kit ONLY
  (IMPORT-PURITY); NO `OCITarget = deploykit.OCITarget` alias (ZERO-ALIASES — call sites moved).

## Unit 3 — R5 sweep + gate test + skill doc

- R5 grep self-test on every removed identifier (`PodDeployTarget`, `runOverlayBuild`,
  `OCITarget`) → only `CHANGELOG/` context remains.
- `charly/kernel_manifest_floor_test.go`: add the shrunk `build_overlay.go` (M seam) +
  `oci_step_emit.go`; prune `build_target_oci.go`/`deploy_target_pod.go` residueOwners.
- install-plan skill: fix the stale `build_overlay.go` description (it is the prep+resolve
  M-seam, not the "host-side pod venue lifecycle hook").

## Multi-repo landing

sdk leg (the OCITarget relocation + the prep-envelope wire type; tag
`v0.<YYYYDDD>.<HHMM-leading-zeros-stripped>`) → super leg (gitlink bump + `charly/go.mod`
require version + the charly/ changes + the gate test). The sdk leg lands AFTER k5's sdk #54
(both regenerate `cue_types_gen.go` — rebase + rerun `task cue:gen`).

## Verification

Foreground gates: `go build ./charly/...` + `go test ./...` + `golangci-lint run` + gofmt
clean; the overlay byte-parity test (the R8 gate — overlay Containerfile before vs after the
relocation is byte-identical, the seam reusing the EXACT core dispatch). The orchestrator owns
the long `charly check run check-pod` bed (box/fedora) on the FINAL code (the overlay
DeployTarget path); the `fully tested and validated` tier follows that fresh-rebuild bed.
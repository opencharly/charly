# 2026.196.9999 — P11c: DISSOLVE the pod-overlay render into candy/plugin-deploy-pod (core OCITarget + PodDeployTarget DELETED)

The pod OVERLAY render moves ENTIRELY out of `charly/` core into the compiled-in
`candy/plugin-deploy-pod`: the pod-only Containerfile assembly (the former `PodDeployTarget`
render body) + `podman build` + the deploy-name alias tag now live in the candy, which imports
`sdk/spec`+`deploykit`+`buildkit`+`kit` DIRECTLY (IMPORT-PURITY, ZERO-ALIASES — no
`OCITarget = deploykit.OCITarget` re-export) and constructs the `deploykit.Generator` + the
`deploykit.OCITarget` walker ITSELF. The kernel keeps ONLY the kind-blind prep+resolve M-seam
(`hostBuildOverlay`) + the generic `"step-emit"` host-builder (the per-step dispatch). This is
the P11c unit of the core-minimization program, unblocked by #67 (render α-fold, the shared
`deploykit.NewRenderGeneratorFromProject` helper) + P8b (build engine absorption) + K5-Unit-0
(`#ResolvedProject` host seam).

The REJECTED earlier design kept the render in core behind a per-capability `HostBuild("render-seam",
…)` Method slice (the `DeployStateHost` class — a capability body kept in `charly/` behind a seam
instead of moved to its owning plugin). P11c DISSOLVES that slice: the pod-only render body MOVES
INTO the candy, and the per-step Containerfile fragment renders HOST-SIDE via the GENERIC
`HostBuild("step-emit","oci-emit-step")` action-noun M-mechanism (NOT a render-seam Method) — the
same `step-emit` host-builder the box-build host-coupled step kinds use.

## Unit A — shared `deploykit.NewRenderGeneratorFromProject` helper (R3/DRY)

- NEW `sdk/deploykit/render_generator_from_project.go`: `NewRenderGeneratorFromProject(ctx, ex
  *sdk.Executor, rp *spec.ResolvedProject, dir, devLocalPkg) (*Generator, error)` — the shared
  construction source extracted verbatim from `candy/plugin-build`'s `buildRenderGenerator`. Both
  the box build (plugin-build) and the pod overlay (plugin-deploy-pod) construct their
  `deploykit.Generator` from the resolved-project envelope through this ONE helper.
- `candy/plugin-build/render.go`: deleted `buildRenderGenerator`; `renderContainerfiles` calls
  `deploykit.NewRenderGeneratorFromProject` (imports trimmed).

## Unit B — host prep shrink + candy render DISSOLUTION

- `sdk/spec/deploy_wire.go`: `OverlayBuildReply` extended (hand-written `*_wire.go`, NOT CUE-sourced
  — no new wire type, only fields on an existing hand-written struct) with the prep envelope:
  `ResolvedProject *ResolvedProject`, `Plans []InstallPlanView`, `BaseUser string`,
  `BaseSecurity *Security`, `BaseRegistry string`, `CalVer string`,
  `OverlayCandySecurity map[string]*Security` (per-overlay-candy security — the `CandyModel`
  interface has no `Security()` method, so the candy cannot read overlay-candy security itself; the
  host prep reads `gen.candyByName(name).Security()` core-side + carries it), `ParentVolumes
  []DeployVolume` (the parent node's bind-mount volumes for a nested pod-in-pod overlay).
- `sdk/deploykit/oci_target.go`: the kind-blind `deploykit.OCITarget` walker
  (`Dir`/`Home`/`Distros`/`EmitStepOp` — decoupled from `*buildkit.ResolvedBox` so the candy need
  not construct one) + `OCIEmitStepParams` (plain-Go, rides inside the opaque
  `StepEmitRequest.Payload`).
- `charly/build_overlay.go` — `hostBuildOverlay` SHRINKS to PREP+RESOLVE only: NewGenerator
  (ExtraCandyRefs=`collectOverlayCandies(plans)`) + LoadDefaultBuildConfig + `createRemoteCandyCopies`
  (host-fs) + `prepareCandySecrets` + base-ref resolve + `ExtractMetadata` (User+Security+registry)
  + `InitConfig.ResolveInitSystem` (core-only — carried in the envelope) + project to
  `*spec.ResolvedProject` + serialize plans to `InstallPlanView` + cache the `buildEngineContext`
  per-dir (`overlayBuildContextCache`, mirror `renderGenCache`) + return the envelope. `runOverlayBuild`
  DELETED. KEEP `overlayBuilderKind` + `overlayBuildInputs` + `registerHostBuilder` + `podDeployEngine`.
- `charly/step_emit_hostbuild.go`: `stepEmitOCIEmitStep` — the `"oci-emit-step"` per-word emitter
  (decodes `OCIEmitStepParams`, reconstructs step+plan via `stepFromView`/`PlanFromView`, looks up
  the cached overlay `buildEngineContext` by Dir, calls `ociEmitStep` — the full provider-registry
  dispatch) + `registerStepEmitter("oci-emit-step", …)`.
- NEW `candy/plugin-deploy-pod/overlay.go`: the DISSOLVED render — `buildOverlay` (the EX-`PodDeployTarget
  .buildOverlay` body, adapted to consume the envelope), `renderOverlayServices`,
  `renderOverlaySecurityLabel`, `tagDeployAlias`, `collectOverlayCandies`, `filterPlansByCandies`,
  `overlayTagFor`, `candyByName` (bare-name lookup replicated byte-faithfully), `translateHostPathToVenue`
  (takes `[]spec.DeployVolume`), `appendUnique`. IMPORT-PURITY: `sdk`/`spec`/`deploykit`/`buildkit`/`kit`
  ONLY; constructs `&deploykit.OCITarget{}` directly; `EmitStepOp` calls
  `HostBuild("step-emit", {Word:"oci-emit-step", Payload: OCIEmitStepParams{…}, Distros})` per step.
- `candy/plugin-deploy-pod/lifecycle.go`: `podPrepareVenue` rewired — `HostBuild("overlay")`→envelope;
  no-overlay → tag the base as deploy-name alias; overlay → `buildOverlay` (render + podman + tag).

## Unit D — deletes + test rewrites

- DELETE the `OCITarget` struct from `charly/build_target_oci.go` (the walker is `deploykit.OCITarget`);
  the file keeps only `formatDefCacheMountDefs` (a `FormatDef.CacheMount` bridge `deploy_host_helpers.go`
  reads).
- DELETE `charly/deploy_target_pod.go` entirely (the `PodDeployTarget` render assembly moved to the
  candy). The two pure helpers core still needs (`collectOverlayCandies`, `readImageRegistry`) move
  into `charly/build_overlay.go`.
- REWRITE the core test files that constructed `&OCITarget{}` / `PodDeployTarget` to test the dispatch
  (`ociEmitStep`) directly: a shared `ociTestTarget(build)` helper constructs a `deploykit.OCITarget`
  wired to `ociEmitStep` (the production seam), preserving the `# Layer:` header + golden-fragment
  assertions (the dispatch is UNCHANGED — byte-parity by construction). The skip/no-op tests call
  `ociEmitStep` directly + assert the empty fragment. `deploy_target_pod_translate_test.go` MOVED to
  `candy/plugin-deploy-pod/translate_test.go` (the translator moved to the candy, `[]spec.DeployVolume`
  signature); `deploy_target_pod_test.go` rewritten to test the host-side staging
  (`createRemoteCandyCopies`) + the context-prefix invariant via `ociEmitStep`.
- NEW `sdk/deploykit/oci_target_test.go`: the walker test (Emit writes `# Layer:` + delegates to a
  mock `EmitStepOp`; VenueSkip + empty-fragment elision; nil-seam no-op).
- `#86` (the pod config-WRITE alias dissolution — `deploykit_pod_aliases.go` / the quadlet
  config-write path) is EXCLUDED from P11c: it is a separate batch (P11(a) completion, task #54).
  Only the `PodDeployTarget` RENDER assembly was deleted; the config-write code in core is untouched.

## Unit E — parity + R5 + skill doc

- R8 byte-parity: the candy's `buildOverlay` mirrors the former `PodDeployTarget.buildOverlay`
  byte-for-byte; `ociEmitStep` is unchanged (the dispatch is the SAME single source of truth); the
  golden-fragment tests (the rewritten `OCITarget` tests) extend the existing coverage. The
  orchestrator's `charly check run check-sidecar-pod` bed (box/fedora) is the FINAL parity gate.
- R5 grep self-test: `git grep 'PodDeployTarget|runOverlayBuild'` → ZERO live refs (only
  `CHANGELOG/` history). The in-core `OCITarget` TYPE is fully deleted — no construction/method/
  field refs remain in `charly/*.go`. The surviving `OCITarget` references are `deploykit.OCITarget`
  (the new home) + the struct def in `sdk/deploykit/oci_target.go` + the `TestOCITarget*` test NAMES
  (CONCEPT labels — those tests construct `deploykit.OCITarget` via `ociTestTarget` + exercise
  `ociEmitStep`, per their header comment; the in-core struct is GONE, so the names label the
  surviving emit concept, not a deleted type) + historical "former in-core" comments in
  `charly/build_overlay.go` + the test headers (explicit historical context, not stale-live refs)
  + `CHANGELOG/`. The stale CLAIMS swept claim-keyed across `charly/`, `candy/`, `sdk/`, + the
  install-plan + vm-deploy-target skills: the "RETAINED core engine" comments in `charly/` + the
  candy-description refs — `plugin-deploy-pod`'s "the SAME core OCITarget/Generator build engine,
  in-process — nothing crosses gRPC" (FALSE post-P11c on both counts: the render left core AND
  step fragments cross the reverse channel via the step-emit host-builder), `plugin-installstep`'s
  "charly's OCITarget.emit* methods", `plugin-example-stepkind`'s + `stepkind-build-consumer`'s bare
  "OCITarget open external-step arm" — all qualified to `deploykit.OCITarget` / rewritten to
  present-tense current behaviour. The description sweep was caught by this R5 self-test + fixed in
  this cutover BEFORE the super-leg commit.
- Gate test: `kernel_manifest_floor_test.go` does NOT exist (P16(a) scope, not P11c); the real AST
  gates (`no_concrete_kind_gate_test.go` + `no_substrate_word_switch_gate_test.go`) were NOT extended
  — P11c's deletes introduce no per-kind `spec.<Kind>` field-read and no substrate-word switch, so
  the R5 grep self-test is the gate.
- install-plan + vm-deploy-target skills: the stale `build_overlay.go` description (it said
  "host-side pod venue lifecycle hook" — now the prep+resolve M-seam) + the `PodDeployTarget` RETAINED
  claims fixed.

## Multi-repo landing

sdk leg (`oci_target.go` + `render_generator_from_project.go` + `deploy_wire.go` `OverlayBuildReply`
fields; tag `v0.<YYYYDDD>.<HHMM-leading-zeros-stripped>`) → plugins leg (the install-plan +
vm-deploy-target skill-doc fixes) → super leg (gitlink bumps + `charly/go.mod` require + the charly/
changes + the candy/plugin-deploy-pod changes + the CHANGELOG). The sdk leg lands AFTER k5's sdk #54
(both touch `deploy_wire.go` — rebase + rerun `task cue:gen`).

## Verification

Foreground gates: `go build ./charly/...` + `go test ./...` + `golangci-lint run` + gofmt clean
(super + sdk + candy/plugin-deploy-pod all green). The overlay byte-parity (R8) is by construction
—the candy's render reuses the EXACT former core render logic + the UNCHANGED `ociEmitStep`
dispatch.

R10 (the pod-overlay build path — P11c's changed render code path): the orchestrator ran
`charly check run check-sidecar-pod` (box/fedora) ALONE on a frozen tree with a fresh worktree
binary (18:56) + bumped gitlinks (sdk v0.2026196.1550, plugins cdedc95) → PASS (steps=11,
SIDECAR_EXIT=0): image-build → check-image → deploy-add → config → start → bring-up-members →
check-live all green (the prior run's stale-binary-guard trip on the K4 service-status path did
NOT recur — that path is untouched by P11c + the binary was fresh + the tree frozen). The bed ran
against the pre-sweep descriptions; the description sweep is metadata-only (no render/deploy/live
behaviour change), so the bed's behavioural proof transfers to the final code, + `charly check box
check-sidecar-pod` re-verifies the swept descriptions bake into the image against the FINAL code.
Tier: `fully tested and validated` (the cutover's changed render code paths executed against the
fresh rebuild; the swept desc bakes per check box).
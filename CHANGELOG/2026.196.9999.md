# 2026.196.9999 — P14-remainder: consolidate status collectors → substrate OpStatus (P14a) + box-authoring → command:authoring (P14b)

The consolidated P14-remainder cutover — TWO disjoint, already-implemented cutovers landed as ONE
atomic superproject cutover via a 3-leg B2 multi-repo landing (sdk → plugins → super). Net core LOC
**negative** (1301 insertions / 1486 deletions across the super leg; the plugin grows, core shrinks).
This is the **super leg** (leg 2): the code moves + the gitlink bumps to the merged sdk + plugins.

## Leg 0 — sdk (merged `5303941`, tag `v0.2026196.449`, PR opencharly/sdk#53)

Two CUE-sourced wire carriers for the status-collector host↔plugin seam + the shared pure
state-vocabulary mapper — additive, zero break:
- `sdk/schema/status.cue` — `#SubstrateStatusRequest` (the collector request: engine_bin/run_mode/
  quadlet_dir/include_all/single/box/instance; NO deploy-cone crosses the seam) +
  `#SubstrateStatusReply` (the LIVE rows the plugin produced; host applies deploy enrichment after).
- `sdk/spec/cue_types_gen.go` — regenerated (the two Go structs).
- `sdk/spec/status_types.go` — `StatusFromState` (the shared pure state-vocabulary mapper, R3
  single-source for the plugin pod-live collector + the host vm collector that stays core until K5).

## Leg 1 — plugins (merged `93b3010`, tag `v2026.196.0456`, PR opencharly/plugins#64)

The skill-doc R5 sweep for BOTH halves — documentation-only, zero behavior change:
- P14b: `internals/skills/go` + `build/skills/new` — the scaffold rows → `box_fetch_reentry.go` + `candy/plugin-authoring`.
- P14a: `core/skills/charly-status` (the plugin-served collector split) + `internals/skills/go`
  (the status row split + the cdp_preresolve.go row → candy/plugin-substrate/status_probes.go) +
  `core/skills/service` (the Source line) + `build/skills/charly-mcp-cmd` (the box-authoring-editors
  reference, R5 sibling-set the prior p14b sweep missed).

## Leg 2 — super (this cutover)

### P14a — status COLLECTORS clean subset → substrate OpStatus (HOW A)
The cleanly-movable status collectors (pod live + local + the probes) relocate from `charly/` core
into `candy/plugin-substrate`, served on the substrate kind provider's `OpStatusCollect` (Invoke by
word pod/vm/k8s/local/android). The host keeps the kind-blind fan-out + the deploy enrichment (a
generic MECHANISM) + the vm/k8s/android collectors (deploy-cone-coupled → stay until K5). The
`enginekit` import is SHED from `charly/` core (the 3 status files that imported it moved; the pod
live collection now runs in the plugin).

- DELETED from core: `charly/cdp_preresolve.go` (+ test), `charly/status_collect_local.go` (+ test),
  `charly/status_collect_pod.go`, `charly/status_probes.go`, `charly/status_golden_test.go`,
  `charly/status_live_mounts_test.go`, `charly/status_quadlet_test.go`.
- MOVED to `candy/plugin-substrate`: `status_collect.go`, `status_local.go`, `status_pod.go`,
  `status_probes.go` (git-detected rename), `status_test.go` (the relocated coverage —
  `TestCollectPodLiveGolden` for the pod-live transform, the probe Parse tests, the live-mounts
  renderer, the quadlet-description parser, the local collector — faithful relocation, no coverage
  dropped).
- MODIFIED core: `charly/status_collector.go` (the collection engine that stays — substrate fan-out
  + deploy enrichment, enginekit shed), `charly/status_substrate.go` (the `status-substrate`
  HostBuild seam), `charly/status_collect_vm.go` (vm collector stays, uses the shared
  `spec.StatusFromState`), `charly/status_test.go` (the surviving host unit tests).
- MODIFIED plugin: `candy/plugin-substrate/{charly.yml, plugin.go}` (the `OpStatus` capability +
  the `OpStatusCollect` Invoke handler).

### P14b — box-authoring command family → command:authoring plugin
The `charly box` AUTHORING command family (set/add-candy/rm-candy/fetch/refresh/write/cat) moved
out of `charly/` core into the NEW compiled-in `command:authoring` plugin (`candy/plugin-authoring`,
the sibling of `command:alias` #68 + `command:box` P15). `charly/scaffold_cmds.go` +
`charly/scaffold_project.go` (399 LOC) DELETED; the 5 pure verbs run on sdk/kit (zero core reentry);
box fetch/refresh reenter core over the new hidden `__box-fetch`/`__box-refresh` reentry
(`charly/box_fetch_reentry.go`) via `HostBuild("cli")`.

### R5 sweep (super code comments + candy docs)
- `candy/plugin-cdp/methods.go` — the cdp endpoint comment: `charly/cdp_preresolve.go` (deleted) →
  `charly/check_endpoint_resolve.go` (the `cc.ResolveEndpoint` reverse-leg).
- `candy/plugin-status/charly.yml` — the host collection engine parity reference: the deleted
  `charly/status_golden_test.go` (TestStatusCollectGoldenParity) → the relocated plugin golden
  `TestCollectPodLiveGolden` (candy/plugin-substrate/status_test.go).

### Gitlink bumps
- `sdk` → `5303941` (merged PR #53, tag `v0.2026196.449`).
- `plugins` → `93b3010` (merged PR #64, tag `v2026.196.0456`).
- `charly/go.mod` require `github.com/opencharly/sdk` → `v0.2026196.449`.

## Verification (super leg)

- `task build:binary` (per-worktree, CalVer-stamped, NOT installed) — exit 0; content markers
  confirm P14a (SubstrateStatusRequest/OpStatusCollect/status-substrate) + P14b
  (plugin-authoring/__box-fetch) code in the binary.
- `go test ./...` (charly module) — ok 84.7s; candy/plugin-{substrate,authoring,status} `go test`
  ok; candy/plugin-{cdp,status} standalone `GOWORK=off go build` ok.
- `golangci-lint run` (charly) — 0 issues; `gofmt -l` empty; `go vet ./...` clean.
- `task cue:gen` (super) — reproducible (no drift).
- R5 grep self-test: deleted core identifiers remain only in CHANGELOG/ + "moved from" migration
  help-text; no deleted file named as a current location.
- **R10 bed**: `charly check run check-commands-local` (disposable: true, host-local, safe
  read-only/--help/dry-run/scaffold plan) — exercises BOTH the P14b authoring verbs
  (set/add-candy/rm-candy/write/cat) AND the P14a `charly status` (command:status → status-substrate
  seam → substrate plugin OpStatusCollect) live, on the worktree binary. Pasted in the PR.

Assisted-by: Claude (fully tested and validated)
# 2026.196.9999 — docs: CHANGELOG H1 placeholder sweep (5 stale headings) + status-context name audit (no divergence)

2026-07-15

Thematic docs batch #27 — a Documentation-only change class cutover (no bed, no
build; `documentation reviewed` tier). Two fixes:

## Fix 1 — status-context name audit (NO divergence found)

The batch brief hypothesized that the rulebook + skills + hooks + agents named
`charly/claude-validation` as THE required branch-protection status, diverging
from the live protection. Verified the ground truth first (RDD — never trust the
doc for a high-risk claim) via:

```
gh api repos/opencharly/charly/branches/main/protection --jq '.required_status_checks.contexts'
gh api repos/opencharly/sdk/branches/main/protection    --jq '.required_status_checks.contexts'
gh api repos/opencharly/plugins/branches/main/protection --jq '.required_status_checks.contexts'
```

All three return `["charly/pr-validator"]` — the live required gate context is
`charly/pr-validator`. A claim-keyed grep across the superproject (`CLAUDE.md`,
`AGENTS.md`, `.claude/`, every `*/CLAUDE.md` signpost) and the `plugins` + `sdk`
submodules (the 6 agents in `plugins/internals/agents/*.md`, the git-workflow +
agents SKILLs, the `.claude/workflows/*.js`, the 5 `.claude/hooks/`) found the
current docs ALREADY name `charly/pr-validator` as the required check in every
surface — ZERO occurrences of `claude-validation` outside the repo's `CHANGELOG/`
history (the sanctioned historical context named by R5). A prior cutover (see
`2026.195.1811` "Add the Codex rulebook" — "Use the harness-neutral
`charly/pr-validator` status name in both root rulebooks") already swept the
rename. **There is NO divergence; none was fabricated.** Fix 1 is a no-op,
recorded here for the RCA trail so the next session does not re-investigate.

## Fix 2 — CHANGELOG H1 placeholder/heading sweep (5 files fixed)

The CHANGELOG convention (this repo's `CHANGELOG/README.md`): one file per
CalVer release `<YYYY.DDD.HHMM>.md`, the CalVer shared by the changelog filename
AND the release git tag (`v<YYYY.DDD.HHMM>`), and the H1 echoes that CalVer. An
H1-vs-filename audit of every `CHANGELOG/2026.*.md` file across the SUPERPROJECT
**and the `plugins` submodule** found FIVE files whose H1 retained the author's
PRE-merge placeholder CalVer while the filename and the release tag carried the
merge-time CalVer — the validator's merge-time rename updated the filename but
not the in-file H1. In every case the FILENAME CalVer is canonical (the
`v<filename>` tag exists; no `v<H1-CalVer>` tag exists):

| file | H1 said | canonical (filename + tag) |
| --- | --- | --- |
| `2026.193.0607.md` | `2026.193.0142` | `2026.193.0607` (`v2026.193.0607`) |
| `2026.195.1415.md` | `2026.195.1400` | `2026.195.1415` (`v2026.195.1415`) |
| `2026.195.1432.md` | `2026.195.0900` | `2026.195.1432` (`v2026.195.1432`) |
| `2026.195.2253.md` | `2026.195.9999` (placeholder) | `2026.195.2253` (`v2026.195.2253`) |
| `plugins/CHANGELOG/2026.195.2150.md` | `2026.195.9999` (placeholder) | `2026.195.2150` (`v2026.195.2150`) |

Each H1 was corrected to the canonical filename CalVer (the `9999` case is the
literal placeholder; the other three are earlier-timestamp placeholders the
validator renamed past). This is one divergence class — the H1 CalVer ≠ the
canonical release CalVer — and R1's claim-keyed sweep fixes every sibling in the
SAME cutover (the batch brief's "most recent release file" was the entry
hypothesis; the audit revealed the full sibling-set). The newest release file
`2026.196.0525.md` is already correct.

**Cross-repo landing (B2, 2 legs):** the `plugins/CHANGELOG/2026.195.2150.md` fix
lives in the `plugins` submodule, so it lands as its OWN plugins PR (leg 1); this
superproject PR (leg 2) carries the four superproject H1 fixes + this entry + the
`plugins` gitlink bump pointing at the merged plugins commit. The plugins
submodule's H1 audit was run via `git -C plugins` (the gitlink is not crossed by
a superproject `git grep` — a false-zero trap; the plugins submodule must be
initialized and grepped directly).

## R5 grep self-test

After the fix, `git grep 'claude-validation'` across the superproject + both
submodules returns ONLY `CHANGELOG/` history context (the sanctioned historical
record — `charly/claude-validation` was the pre-rename status name). The five
corrected H1s no longer carry a stale CalVer: a re-run of the H1-vs-filename
audit reports zero mismatches.

## Change class + tier

Documentation-only — `.md` only (CHANGELOG history corrections + this entry), no
behavior change, no Go / YAML-schema / box / candy / hook / workflow code. The
gate is the non-runtime standards (adversarial consistency review, R5 grep
self-test, cross-reference validation, markdown integrity, the
`pre-commit-gate.sh` / `pre-push-gate.sh` gates). Tier: `documentation reviewed`.
Tagged at merge.
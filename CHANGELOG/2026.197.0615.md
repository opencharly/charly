# 2026.197.0615 — #52: dissolve the dead + test-only slice of charly/*_aliases.go

Full inventory of all 22 `charly/*_aliases.go` files (379 aliased symbols) cross-referenced
against the (not-yet-merged) P16 kernel-manifest floor gate's `kernelFloor`/`residueOwner`
maps. Classified every symbol by its non-test consumers: 51 had ZERO consumers anywhere in
`charly/` (their call sites already relocated to `candy/plugin-vm` / `candy/plugin-deploy-vm`
in earlier cutovers, leaving orphaned bindings), 55 had ONLY `_test.go` consumers, 179 had at
least one consumer that is itself tracked P8b/P11/P12/P13/P14/P15 residue (left in place —
dissolves naturally when that file's owning cutover moves it), and 94 touch a permanent
kernel-floor file or an as-yet-untracked residue file (escalated separately, not touched here).

## This cutover — the safe, zero-risk slice

- **Deleted 51 dead alias bindings** across `deploykit_aliases.go`, `deploykit_tree_aliases.go`,
  `kit_aliases.go`, `kit_env_aliases.go`, `kit_envfile_aliases.go`, `kit_executor_aliases.go`,
  `kit_ledger_aliases.go`, `kit_refs_aliases.go`, `kit_runtime_config_aliases.go`,
  `ssh_config_aliases.go`, `vmshared_aliases.go` — each verified to have no consumer (test or
  production) anywhere in `charly/`.
- **Rewired 55 test-only symbols** to reference their SDK package directly
  (`kit.X` / `deploykit.X` / `buildkit.X` / `vmshared.X`) across 33 `_test.go` files, adding the
  missing import where not already present, then deleted the now-unused alias bindings for
  `buildkit_aliases.go`, `deploykit_aliases.go`, `deploykit_compiler_helper_aliases.go`,
  `kit_aliases.go`, `kit_executor_aliases.go`, `kit_ledger_aliases.go`, `kit_reverse_aliases.go`,
  `kit_runtime_config_aliases.go`, `vmshared_aliases.go`. Test-file rewrites used a Go-source-
  aware (string/comment-preserving) token replacer so descriptive `t.Errorf`/doc-comment
  mentions of the bare name were left untouched (they remain accurate — the symbol still
  exists, just via qualified access).
- Fixed two doc-comments in `deploykit_aliases.go` and `kit_aliases.go` that had gone stale
  relative to the bindings removed in the same file (R1: a divergence surfaced on the exact
  surface being edited is fixed in the same commit).
- No alias file was fully emptied by this pass — every file retains at least one symbol used
  by a tracked-residue or kernel-floor consumer; the remaining 179+94 symbols are out of scope
  for this cutover (documented in the PR for the residue-wave briefs / follow-up architectural
  call, respectively).

## Explicitly NOT touched (escalated, not decided here)

- The 94-symbol kernel-floor bucket. Most are pure `spec.X` passthroughs riding the forbidden
  `vmshared`/`kit`/`buildkit` mechanism-kit hop instead of importing `sdk/spec` directly (which
  core IS allowed to import) — not a plugin-move fix. A smaller set
  (`deploykit_aliases.go`'s `InstallPlan`/`DeployExecutor`/`DeployTarget`/`EmitOpts`/… ) are
  genuine E-envelope types currently defined in `sdk/deploykit` rather than `sdk/spec`, which
  may need their own SDK package-boundary relocation. Both are architectural calls bigger than
  this cutover, tracked separately.

## Verification

- `go build ./...`, `go vet ./...`, `go test ./...` (both packages, ~112s) — all green.
- `gofmt -l .` and `golangci-lint fmt --diff` — clean.
- `golangci-lint run ./...` — 0 issues.
- R5 grep self-test — zero bare (unqualified) references remain for all 106 removed symbols;
  every hit reviewed was either a `t.Errorf`/`t.Fatalf` descriptive string literal or a test
  doc-comment describing intent, neither of which is a stale code reference (the described
  symbol still exists, just accessed via its SDK package qualifier now).
- `task build:binary` + `charly box validate` — exit 0 (the live-invocation proof appropriate
  to this change's class: zero production call paths were touched — every removed symbol was
  either dead code or test-only — so no bed's outcome could differ from before this cutover).

Assisted-by: Claude (analysed on a live system)

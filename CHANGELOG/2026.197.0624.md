# 2026.197.0624 — Enforce dual-harness developer configuration

- Keep `CLAUDE.md` and `AGENTS.md` independent while validating equivalent R0 skill dispatch.
- Add a repository Codex marketplace and validate the full developer profile without user-level configuration.
- Configure trusted Codex sessions for repository-scoped workspace access with exact, on-request boundary approvals, and document the sandbox boundary in `AGENTS.md`.
- Validate current cross-repository skill references so hard renames cannot leave broken guidance behind.
- Document the shared Claude Code and Codex setup without making MCP part of configuration parity.
- Consume the portable plugin catalog and skill-name cutover.
- Make concurrent runtime plugin builds keep VCS stamping while using read-only Git status probes, and propagate the original plugin-load failure.
- Separate strict operator VM destruction from explicit idempotent `--if-exists` reconciliation so concurrent bed cleanup is quiet and complete.
- Pin Codex work to a protected-main linked worktree, the sanctioned stamped local binary, and existing normal Go caches in the full Codex rulebook.
- Make the canonical agent-configuration validator always execute its own meta-test, removing a separate-command naming failure mode.
- Record and enforce group-member rebuild and final teardown as R10 steps, and bind Codex evidence review to exact run directories.
- Propagate joined member-teardown failures through check beds and operator bundle deletion while still attempting every member.
- Pin the repository's Arch VM bases to a verified immutable snapshot so an upstream `latest` rotation cannot corrupt an R10 fetch/resume cycle.
- Preserve VCS stamping for external-plugin sources with a usable committed Git worktree, while explicitly omitting unavailable provenance for copied/archive sources instead of borrowing an unrelated malformed ancestor `.git` marker.
- Re-enter check phases through the host CLI supplied by `CHARLY_BIN`, rather than accidentally recursing through the check-provider executable.
- Retain the exact external-kind configuration, discovery, build, or connect error in the required warning instead of reducing it to a generic provider failure.
- Create ordinary check-run artifact directories only after winning the per-bed lock, so a rejected duplicate cannot look like a newer interrupted R10 run.
- Make the configuration validator's self-test fully in-memory instead of creating a host-temporary Git repository.
- Verify the `plugins` checkout's topology, recorded gitlink, and executable developer checker before invoking it; report an unavailable project profile rather than crashing, auto-initializing, or validating a stale checkout.
- Replace the false linked-worktree `go version -m` VCS claim with Charly's explicit, verified CalVer/revision/commit-time/dirty provenance contract, exposed by `charly version --json`.
- Make the core Go gate module-aware, isolate VM-state tests through `CHARLY_DEPLOY_CONFIG`, and make copied external-plugin sources explicitly omit unavailable VCS provenance rather than inheriting an unrelated Git marker.
- Make host-side R10 plan re-entry use the active `${CHARLY_BIN}` rather than a stale PATH binary, and declare the local-VM bed's required guest-side `rsync` dependency in cloud-init.
- Require complete per-command core-gate evidence when an aggregate Task transcript omits declared steps.
- Record that buffered R10 step output is not terminal evidence; validators keep active beds intact until their bound exit and summary arrive.

Assisted-by: Codex (OpenAI GPT-5.6 Sol; fully tested and validated)

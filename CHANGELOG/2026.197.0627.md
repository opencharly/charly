# 2026.197.0627 — #52b: repoint 13 small vmshared spec-passthrough types directly to spec

First sub-batch of the vmshared→spec envelope-route repoint (task #16, ruled in-scope for the
#52 alias-inventory theme). `vmshared_aliases.go`'s Op/BundleNode/Step/… (and this batch's 13
symbols) are pure `= spec.X` passthroughs in `sdk/vmshared/spec_aliases.go` — legitimate
E-envelope types the kernel is SUPPOSED to reference (per the boundary law), just routed
through the forbidden `vmshared` mechanism-kit hop instead of `sdk/spec` directly (which core
IS allowed to import). The fix is a route correction, not a plugin move.

## This cutover

Repointed 13 symbols directly to `spec.X` across every consumer (production + test), then
deleted the now-unused `vmshared_aliases.go` bindings:

- `LibvirtDomain`, `LibvirtGraphicsListeners` → `spec.LibvirtDomain` / `spec.LibvirtGraphicsListeners`
- `MCPServerYAML` → `spec.MCPServerYAML`
- `PackageItem` → `spec.PackageItem`
- `PortScope` → `spec.PortScope`
- `PreemptibleConfig` → `spec.PreemptibleConfig`
- `TunnelYAML` → `spec.TunnelYAML`
- `ShellConfig` → `spec.Shell`
- `EnvDependency` → `spec.EnvDependency`
- `ServiceEntry` → `spec.ServiceEntry`
- `InstallContext` → `spec.InstallContext`
- `BuildStageContext` → `spec.BuildStageContext`
- `CandyPluginDecl` → `spec.Plugin`

45 candidate files touched (8 turned out to reference the bare name only in a comment, not
code — left untouched by the code-aware rewrite; verified individually). One doc-comment in
`buildkit_aliases.go` updated (it named `vmshared_aliases.go` as where these two types are
bound; they no longer are).

**Caught during the rewrite:** `generate.go`'s `buildStageContext` method has a return-type
`*BuildStageContext` (a bare type reference — correctly repointed) AND a same-line-adjacent
call `g.toDeploykit().BuildStageContext(...)` (a METHOD CALL on `deploykit.Generator` that
happens to share the identifier name with the type — NOT a type reference). The mechanical
text-based rewrite doesn't distinguish a bare type name from a selector-expression method call
sharing the same identifier; it incorrectly qualified the method call to
`g.toDeploykit().spec.BuildStageContext(...)`. `go build` caught it immediately (`.spec
undefined`); reverted that one call site to the plain method call, verified no other
`sdk/{kit,deploykit,buildkit,vmshared}` method shares a name with any of these 13 symbols
(none does).

## Explicitly NOT in this sub-batch

The remaining 10 vmshared_aliases.go ESCALATE-classified symbols split two ways: 10 are also
pure spec passthroughs (DistroDef, VmDeployState, Matcher, MatcherList, BoxConfig, CandyYAML,
Step, BundleNode, Op, PortSpec) with a MUCH larger fan-out (measured 278 files total across all
23 passthroughs; this sub-batch's 13 covered ~45) — sequenced as later sub-batches per the
decomposition plan (task #16). The other 6 (InstallSignalHandler, ParseLibvirtURI, PollRemote,
RegisterShutdownHook, SweepStaleTemps, pollUntil) are genuine `vmshared` mechanism functions
with no `spec` equivalent — out of scope for a repoint entirely, left as-is.

## Verification

- `go build ./...`, `go vet ./...`, `go test ./...` (~97s) — all green.
- `gofmt -l .` / `golangci-lint fmt --diff` — clean.
- `golangci-lint run ./...` — 0 issues.
- R5 grep self-test — zero bare (unqualified) references remain for all 13 repointed symbols;
  every hit reviewed is descriptive prose (a comment or `t.Fatal` string) whose underlying
  claim remains accurate, not a stale code reference.
- `task build:binary` + `charly box validate` — exit 0.

Assisted-by: Claude (analysed on a live system)

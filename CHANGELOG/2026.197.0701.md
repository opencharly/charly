# 2026.197.0701 — #52b sub-batch 4: repoint Step to spec directly

Fourth sub-batch of the vmshared→spec envelope-route repoint (task #16). `Step` repointed
directly to `spec.Step` across 31 files; the `vmshared_aliases.go` binding deleted. 21 of 23
spec-passthrough symbols done.

## Proactive collision check, clean this time

Ran the proactive check (method/field/embedded-field/local-var/ellipsis collisions) BEFORE the
mechanical rewrite, per the discipline sub-batches 1-2 motivated. Found none for `Step` — the
sole `Step{...}` occurrence outside the alias file is a plain composite-literal value
construction (`host_build_feature_test.go`), not a field key or method call. `go build` and
`go vet` both passed clean on the first attempt (no fix-up round needed, unlike the previous
three sub-batches).

## Explicitly NOT in this sub-batch

BundleNode (~90 files) and Op (~100 files) — the two largest remaining spec-passthrough
symbols — plus the 6 genuine vmshared mechanism functions (no spec equivalent, out of scope
entirely).

## Verification

- `go build ./...`, `go vet ./...`, `go test ./...` (~100s) — all green.
- `gofmt -l .` / `golangci-lint fmt --diff` — clean.
- `golangci-lint run ./...` — 0 issues.
- R5 grep self-test — all 40 remaining bare occurrences are descriptive prose ("Step N" in a
  numbered procedure list, `#Step` CUE-def references, one CUE string-literal test fixture) —
  zero stale code references.
- `task build:binary` + `charly box validate` — exit 0.

Assisted-by: Claude (analysed on a live system)

# 2026.197.0706 — #52b sub-batch 5: repoint BundleNode to spec directly

Fifth sub-batch of the vmshared→spec envelope-route repoint (task #16), and the largest to
date. `BundleNode` repointed directly to `spec.BundleNode` across 86 files; the
`vmshared_aliases.go` binding deleted. 22 of 23 spec-passthrough symbols done.

## Proactive collision check, clean again

Measured 99 candidate consumer files (close to the earlier ~90 estimate). Checked for every
collision class found in earlier sub-batches — method/function declarations named
`BundleNode`, a field (embedded or plain) sharing the name, composite-literal field keys, the
variadic-ellipsis false match, local identifiers shadowing the package — before running the
mechanical rewrite. Found none: every non-alias `BundleNode` occurrence is either a plain
composite-literal value construction (`node := BundleNode{...}`, with the local variable
lowercase and thus never colliding with the type) or a map-value-type declaration
(`map[string]*BundleNode{...}`). `go build` and `go vet` both passed clean on the first
attempt — the second sub-batch in a row with zero fix-up round, now that the proactive check
runs before the rewrite instead of after.

## Explicitly NOT in this sub-batch

`Op` — the last and largest remaining spec-passthrough symbol (~100 files per the earlier
measurement, the central dispatch-envelope type touching nearly every kernel M-mechanism
file) — plus the 6 genuine vmshared mechanism functions (no spec equivalent, out of scope
entirely). Op will need the most careful proactive collision check of all 23 symbols given how
pervasively short, generic 2-letter type names collide with local variables in Go code.

## Verification

- `go build ./...`, `go vet ./...`, `go test ./...` (~104s) — all green.
- `gofmt -l .` / `golangci-lint fmt --diff` — clean.
- `golangci-lint run ./...` — 0 issues.
- R5 grep self-test — all 51 remaining bare occurrences are descriptive prose (doc comments
  describing the BundleNode concept/tree) or string-literal test failure messages
  (`t.Error("BundleNode.IsDisposable() = false; want true")`) — zero stale code references.
- `task build:binary` + `charly box validate` — exit 0.

Assisted-by: Claude (analysed on a live system)

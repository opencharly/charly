# 2026.197.1051 — wave-1 consolidated landing: alias dissolution, teardown robustness, VM disk-build engine → plugin-vm, loader-residue fold, gitlink bumps

Consolidated wave-1 landing — the single R10 roster + fresh-validator gate for five
members, landed as ONE atomic commit per the Cutover Sizing Law (pays the landing
ceremony once for the whole batch rather than per member). Each member's own
narrative and evidence is recorded in its own dated `CHANGELOG/` entry, referenced
below; this entry is the consolidated landing record + the fresh `pr-validator`'s
independent verification.

## Members

- **#52 — alias dissolution** (`CHANGELOG/2026.197.0615.md`): 51 dead + 55
  test-only symbols dissolved out of the 22 `charly/*_aliases.go` files; zero
  production call paths touched.
- **#55 — bed teardown robustness** (`CHANGELOG/2026.197.0601.md`): demotes a
  proven-benign `ESRCH` teardown warning in `candy/plugin-tunnel`'s
  `cloudflareTunnelStop` to a `note:`; fixes a cosmetic duplicated-comment typo in
  `charly/build_overlay.go`.
- **#46 — loader-residue fold** (`CHANGELOG/2026.196.1800.md`): deletes
  `charly/loader_driver.go`, the SOLE charly-core file that imported
  `sdk/loaderkit` — the whole-project WALK now reaches it exclusively through the
  compiled-in loader plugin's `spec.ProjectWalker` seam. The kind-decode
  MATERIALIZE half relocates verbatim into the new `charly/materialize.go`, which
  legitimately STAYS core per the boundary law's clause (M) (a kind-blind
  dispatch mechanism, never branching on a concrete kind).
- **P8b Tier-1 — VM disk-build engine move** (`CHANGELOG/2026.197.0700.md`): moves
  the VM-disk BUILD ENGINE (privileged pacstrap/bootc exec, qemu-img,
  cloud-init seed rendering) out of `charly/` core into `candy/plugin-vm`,
  completing the box-build-engine core-minimization program's VM slice; the host
  builder `host_build_vm_build.go` shrinks to PREP+RESOLVE only.
- **Gitlink bumps**: `sdk` → `496e9a2` (tag `v0.2026197.738`, sdk#57 + sdk#58
  merged), `plugins` → `a11d933` (post-plugins#71), `pkg/fedora` → `5dad73a`
  (stale per-repo PR-template removal). All three verified as fast-forward
  descendants of their submodule's `origin/main` before landing.
- **Roster-surfaced fix**: `eval-host-vm`'s cloud_init `package:` list was
  missing `rsync` — a latent R9 gap only exposed once an operator-authorized
  disk purge made a bed run genuinely fresh-disk for the first time. Fixed in
  the same commit (`8544e93`).

## Net architecture effect

`charly/*.go` (non-test) diff: 337 insertions / 1337 deletions (net **−1000
LOC**); `candy/plugin-vm/*.go` gained a net **+1059 LOC** — the VM-engine body
crossed the kernel/plugin boundary in the same PR. This is the load-bearing fact
behind the two NEW `charly/`-core sdk-mechanism-kit imports this PR introduces:

- `charly/host_build_vm_build.go` → `sdk/deploykit` (reads `LoadDeployConfigForRead`
  for the entity's existing VM-deploy state). **Tracked inventory**: joins task
  #13 (deploykit-defined E-envelope types → `sdk/spec`) — once `DeployExecutor`'s
  backing types relocate to `spec`, this import becomes `spec`-only.
- `charly/materialize.go` → `sdk/kit` (`kit.ClassifyDoc` / `kit.DocShapeNode` /
  `kit.ScalarNode` — relocated, not newly introduced: `charly/unified.go`
  already called `kit.ClassifyDoc` at this exact call site before the fold;
  `charly/loader_driver.go`, now deleted, also imported `kit`). Net non-test
  `kit`-family importer count is UNCHANGED at 94 files between `origin/main` and
  this branch's head — flat, not grown. **Tracked inventory**: joins the same
  task #13 wave (or the loader-state K-wave line, per the orchestrator's board).

Both satisfy the boundary law's residual-call-site exception (a mechanism's BODY
moved OUT of core in the SAME PR, net core-LOC negative; the remaining call site
imports the kit directly, never an alias). Zero new or grown
`charly/*_aliases.go` files — all 14 touched alias files are pre-existing and
only shrink.

**Known non-blocking duplication (disclosed, tracked separately):**
`candy/plugin-vm/vm_privileged_runner.go`'s `RunPrivileged` + pacman.conf
renderers are a module-boundary-forced port of `charly/privileged_runner.go`'s
same-named function (a plugin cannot import charly core). Unifying both into a
shared `sdk/kit` function is tracked on the orchestrator's board under task
#21's cluster (the gated host-callback-engine K-wave exit) — does not affect
this cutover's own correctness, which the R10 roster below proves directly.

## Consolidated R10 roster — 7/7 PASS

The original 4-bed roster (pod / local / vm substrates) did not exercise the
STRUCTURAL-kind fold path (`group`/`structkind`/`substrate`) that
`materialize.go`'s kind-decode dispatch also serves — the fresh `pr-validator`
ran the three matching beds directly to close that cross-cutting-loader
bed-coverage gap before certifying the merge:

| Bed | Result | Steps | Duration |
|---|---|---|---|
| `check-commands-local` | PASS | 6 | — |
| `check-sidecar-pod` | PASS | 12 | — |
| `check-charly-vm` | PASS | 8 | 435s (fresh disk) |
| `check-local-vm` | PASS (after RCA+fix; 0757 FAIL → 0822 PASS) | 8 | 401s |
| `check-group` | PASS | 7 | 439s |
| `check-structkind` | PASS | 7 | 386s |
| `check-substrate` | PASS | 10 | 259s |

`check-group`/`check-structkind`/`check-substrate` witness the externalized
structural-kind decode (C2-group/C2-substrate: `runPluginKind` →
`foldSubstrateKind`/authored-member input-threading) surviving the loader-fold
byte-for-byte — the exact mechanism `charly/materialize.go` now dispatches.

## Comment-intake attestation

The fresh `pr-validator` read the full GitHub comment thread on PR #127
(`gh api repos/opencharly/charly/issues/127/comments` + `.../pulls/127/reviews`)
before finalizing this verdict: **zero comments exist on the PR itself.** The
orchestrator's rulings referenced during validation (the 2 kit-import
dispositions, the RunPrivileged tracking-name correction, the merge-sequence
checklist) arrived via direct teammate messages, independently re-verified by
the validator rather than taken on authority.

## Verification (fresh `pr-validator`, independent re-run)

- `go vet` / `go test` (charly + candy/plugin-vm/plugin-loader/plugin-tunnel/
  plugin-box/plugin-build) / `golangci-lint run` (0 issues, all six modules) /
  `gofmt -l` (empty) — all re-run clean by the validator, not taken on the
  author's word.
- `task build:binary` + `charly box validate` (exit 0) + `charly version`
  matches source.
- `sdk` standalone battery: `GOWORK=off go mod tidy` (zero diff) + `go mod
  verify` + `go build ./...` + `go test ./...` — all clean.
- `task cue:gen` is a full no-op (zero `git status` diff anywhere in the
  superproject or `sdk`) — SDD reproducibility confirmed.
- R5 grep self-test: `loader_driver` — zero live hits outside `CHANGELOG/`;
  the deleted VM-engine symbols (`BuildCloudImage`/`BuildBootcVM`/
  `BuildBootstrapVM`/`EmitDiskBuildScript`/`baseBootstrapPackages`) appear only
  in descriptive relocation comments, never as live code references.
- Gitlink ancestry: `sdk` 496e9a2 / `plugins` a11d933 / `pkg/fedora` 5dad73a all
  confirmed ancestors of their submodule's real `origin/main` via
  `git merge-base --is-ancestor`.
- Roster evidence cross-checked on disk against the integrator's worktree:
  `charly.yml` byte-identical to the PR head; the `check-local-vm` 0822 PASS
  run genuinely exercised the committed rsync fix.

## Change class

code/config (Go runtime + candy/plugin config changes across multiple members,
plus the `eval-host-vm` bed-config fix) — runtime tier.

## Final tier

**`fully tested and validated`** — all NEW/CHANGED code paths (the VM
disk-build engine, the loader-fold materialize dispatch including its
structural-kind arm, the teardown-warning demotion, the alias dissolution)
executed live against fresh rebuilds, including three fresh VM disk builds
(`check-charly-vm`, `check-local-vm`, plus the three structural-kind beds
booting `eval-vm`) — the strongest available proof for this change class, at
zero warnings.

Assisted-by: Claude (fully tested and validated)

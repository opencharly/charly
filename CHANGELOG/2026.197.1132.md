# 2026.197.1132 — #52b/task #16 final round: merge main (post-#127) into the vmshared→spec repoint

Brought `feat/52b-vmshared-spec-repoint` up to date with `main` at `7be18e7` (`v2026.197.1051`,
the just-merged PR #127 wave-1 landing: alias dissolution, teardown robustness, the VM
disk-build engine's move into `candy/plugin-vm`, and the loader-residue fold), completing task
#16 (the 23-symbol vmshared→spec envelope-route repoint) for landing as PR #128.

## Conflict resolution (27 files)

Three resolution patterns, applied by symbol/file classification:

- **Intersection-of-deletions** — `charly/vmshared_aliases.go` had independently shrunk on both
  branches (mine: the 23 repointed spec-passthrough symbols; main's, from my own earlier PR
  #127: the DEAD+TESTONLY sweep). Resolved to the intersection of survivors on both sides.
- **Take-main-wholesale** — `charly/vm_bootc_install.go`, `vm_bootstrap.go`, `vm_build.go`,
  `vm_cloud_image.go` were deleted on main (moved to `candy/plugin-vm/vm_bootc_engine.go` /
  `vm_bootstrap_engine.go`, restructured under P8b-rest to take pre-resolved refs instead of
  resolving them internally). Took main's plugin-side versions wholesale — `candy/plugin-vm` is
  a separate, alias-exempt package with its own pre-existing `vmshared`-importing convention.
  `charly/vm_disk_builder.go` similarly renamed to `candy/plugin-vm/vm_disk_layout.go`, and
  `charly/loader_driver.go` to `charly/materialize.go` (an unrelated main-side loader-residue
  fold, PR #46-super).
- **Combine-both** — the remaining ~20 test files needed both sides' independent changes:
  import-block unions (my `spec` import + main's own new imports), a functional refactor
  combined with my repoint (`bundle_members.go`'s `tearDownMembers` took main's #55
  error-joining/`--if-exists` rework with my `*BundleNode`→`*spec.BundleNode` repoint applied),
  and several bare-identifier renames where main independently renamed a function
  (`compileLocalPkgStep`→`deploykit.CompileLocalPkgStep`) on the exact line my repoint also
  touched — combined both changes to match the already-clean pattern at the file's other call
  sites.

## One post-merge build failure, found and fixed (R1)

`go build ./...` on the merged tree failed: `host_build_vm_build.go:142: undefined:
VmDeployState`. Root cause: this file arrived from main's P8b-rest expansion (38→235 lines) and
was never part of the 27-file conflict list (git auto-merged it cleanly, since my repoint never
touched this file), so it was never individually checked against the repoint. `VmDeployState` IS
one of the 23 repointed symbols (sub-batch 2, `b18b76a`) — its `vmshared_aliases.go` binding is
gone, so the file's bare reference, valid on main alone, broke once merged with the repoint.
Fixed the one call site to `*spec.VmDeployState`; confirmed via full-tree grep it was the only
such occurrence introduced by content outside the conflict list.

## Audit method (the lighter form, per revised guidance — gofmt -r is NOT a reliable AST-safety
oracle for composite-literal keys/locals)

Chose the lighter audit over full per-hunk classification because the risk category
(under-qualified bare references to a deleted alias) is a **guaranteed hard compile error**, not
a silent pass — there is no scenario where a missed repoint site both (a) fails to qualify and
(b) still compiles, since package main no longer declares any of the 23 old bare names at all.
`go build`/`go vet`/`go test`, clean across the *entire* merged tree (`charly` + every touched
plugin module: `plugin-vm`, `plugin-box`, `plugin-build`, `plugin-loader`, `plugin-tunnel`), is
therefore a complete proof for that risk class — it is exactly what caught the one real bug
above. Supplemented with a proactive grep for the ONE risk class a compiler can't catch (a
rewrite accidentally landing inside a string literal instead of code) across every touched
non-test file — found only legitimate prose/real-code matches, no corruption.

## Verification

- `go build ./...`, `go vet ./...`, `go test ./...` — green on `charly` (100s) and all 5 touched
  plugin modules (`plugin-vm`, `plugin-box`, `plugin-build`, `plugin-loader`, `plugin-tunnel`).
- `golangci-lint run ./...` — 0 issues.
- `gofmt -l .` / `golangci-lint fmt --diff` — found import-ordering drift (`spec` before
  `vmshared` alphabetically) in 8 files from an earlier batch-script conflict resolution;
  `gofmt -w .` fixed it (pure formatting); re-verified build/vet/lint green after.
- `task build:binary` — clean; `charly version` = 2026.197.1120.
- `charly box validate` — exit 0, zero warnings.
- `git submodule update --init --recursive` — all 8 submodule gitlinks (box/arch, box/cachyos,
  box/debian, box/fedora, box/ubuntu, pkg/fedora, plugins, sdk) resolved forward to main's
  post-merge commits; `git submodule status` clean (no stale `+` markers).

Confirmation bed `charly check run check-sidecar-pod` run per the team-lead's ruling that a
single bed suffices for this type-route-only, zero-behavior-change repoint (main's HEAD already
passed 7 beds at the #127 landing).

Assisted-by: Claude (analysed on a live system)

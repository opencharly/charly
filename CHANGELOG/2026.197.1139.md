# 2026.197.1139 — refactor(core): P14-rest closeout — box labels externalization + K-wave annotation sweep

Wave-2 member (P14-rest RDD trace closeout). A Phase-1 read-only trace over the
25 files remaining from the P14 status/OCI/scaffold cone (5 of the originally
tracked 27 had already been deleted by the P14a/P14b cutover) found that most
of the cone was already correctly classified — GATED-until-K5 (the status
framework), or already self-documented UNTIL-K3/K5 shims (merge.go,
oci_plugin.go, box_inspect_overlay.go). The genuinely actionable, low-risk
residue was three items; this cutover lands them plus a doc-only K-wave
annotation sweep team-lead requested on the gated files that lacked one.

## What changed

1. **`box_labels_cmd.go` → `candy/plugin-box` nested `labels` command.** `charly
   box labels <ref>` now dispatches through the compiled-in candy/plugin-box
   command:labels word (matching the P14b/P15 pattern of its sibling verbs
   generate/validate/new/pkg/inspect/list), which reaches the hidden core
   `__box-labels` reentry over `HostBuild("cli")` — the SAME reentry shape as
   `pkg`/`inspect`'s overlay-and-list-tags formats/`fetch`/`refresh`, since
   ResolveRuntime/resolveLocalImageRef/InspectLabels are host container-storage
   probes the plugin cannot compute pre-K1. `BoxLabelsCmd`'s struct + Run
   method are UNCHANGED (byte-for-byte); only the Kong registration moved from
   nested-under-`BoxCmd` to a hidden top-level `__box-labels` command. R5 swept:
   the OLD `Labels BoxLabelsCmd` nested-grammar line is gone from `image.go`'s
   `BoxCmd` struct; every doc comment listing the retained vs. nested verb sets
   updated.

2. **`labelset.go` deleted (not folded).** RDD-verified the file's own `LabelSet`
   type has ZERO consumers anywhere in the repo (its `LabelDescriptionSet`/
   `LabeledDescription` content had already moved to `sdk/kit` in an earlier
   cutover, leaving only a trivial, entirely-dead wrapper struct). Deleting
   dead code is cleaner than folding it into `labels.go`, so that's what
   landed instead of the originally-suggested fold.

3. **K-wave exit annotation sweep** (doc-only, zero behavior change) — added
   explicit "MIGRATION INVENTORY (north-star §4.4): ... UNTIL-K<N>" comments,
   matching the established convention in merge.go/oci_plugin.go/
   box_inspect_overlay.go, to every gated file that lacked one:
   - `engine.go` (UNTIL-K4 — 10 cross-cone consumers measured: commands.go,
     container.go, pod_lifecycle_resolve.go, preempt.go,
     resolved_project_host.go, service.go, start.go, config_image.go,
     status_collector.go, volume_cp_tags_cmd.go)
   - `transfer.go` (UNTIL-K4 — 2 consumers, both deploy-cone)
   - `remote_image.go` (UNTIL-K1/K3 — 7 consumers spanning loader- and
     build-cone; NOT an OCI-registry file despite the filename, corrected in
     its own header comment)
   - `alias_collect.go` (UNTIL-K3/K1 — see the R1 finding below)
   - `image.go`'s `BoxCmd` (UNTIL-K5, command-dispersal — the retained
     build/merge/pull/feature/reconcile verbs)
   - `status_collector.go` + `status_collect_{vm,k8s,adb}.go` +
     `status_nested.go` + `status_reap.go` (UNTIL-K5 — deploy-cone-coupled,
     matching `status_substrate.go`'s existing "stays host-side until K5" note,
     which now has six siblings pointing back to it instead of being the lone
     annotated file in the cone)

## R1 finding surfaced while sweeping alias_collect.go (fixed in the same edit)

The file's own comment claimed `aliasNameRe` was "the LOAD-TIME validation
regex (validate.go rejects malformed alias names)". Verified FALSE: `validate.go`
has no alias-related code at all, and `aliasNameRe` in charly/core has no
production call site anywhere (only its own unit test exercises it).
`candy/plugin-box/validate_rules.go` carries an INDEPENDENT copy of the same
regex that IS wired into `charly box validate` — the box-authoring validation
externalization moved the real enforcement there without sweeping this now-orphaned
core copy. Per R2 this cannot be an unscheduled future batch — it is executed
in this same cutover, below.

## Dead-code sweep addendum (executed in this same cutover, per R2)

**(a) `aliasNameRe` orphan resolved.** Mirrored the pattern-regression coverage
into `candy/plugin-box/validate_pure_test.go` (`TestAliasNameRegex`, run
against the LIVE `validate_rules.go` copy — 13 subtests, all pass), then
deleted the orphaned core copy: `charly/alias_collect.go`'s `aliasNameRe` var +
`regexp` import, and `charly/alias_collect_test.go`'s `TestAliasNameRegex`.
R5 grep: `aliasNameRe` in `charly/` now appears only in this file's own
explanatory past-tense comment.

**(b) Broader manual harvest** (golangci-lint's `unused` linter reports 0
issues on `charly/` both before and after — it reliably catches unexported
dead code but not an exported symbol whose only "consumer" is its own test, so
every further finding below came from manual cross-reference grep, not the
linter):

Confirmed dead and deleted, `charly/remote_image.go`:
- `RemoteContainerName` (func) — zero production consumers; only its own test.
- `RemoteImageContext.ContainerName()` (method) — zero consumers anywhere,
  not even a test; does not satisfy any interface.
- `RemoteImageContext.CollectVolumes()` (method) — zero consumers anywhere,
  not even a test; does not satisfy any interface. (The wrapped
  `CollectBoxVolume` function it called stays — it has three other real
  callers: `render_baked_metadata.go`, `resolved_project_host.go`, `tasks.go`.)
- `charly/remote_image_test.go`'s `TestRemoteContainerName` removed with it;
  `TestStripURLScheme` and `TestResolveImageName` (tests unrelated, still-live
  functions) are untouched.
- R5 grep: `RemoteContainerName` zero hits repo-wide; `\.ContainerName\(` zero
  hits except an unrelated package-qualified `kit.ContainerName` in
  `sdk/deploykit/quadlet.go` (a different function in a different package —
  confirmed unrelated, not a false negative); `\.CollectVolumes\(` zero hits.

Checked and confirmed LIVE (not dead, no action) — the remaining P14-rest-cone
files' exported top-level symbols:
- `CapabilitiesFromLabels`, `FormatCLIError`, `DeployFromBox`,
  `ExtractMetadata` — each has multiple real production call sites (verified
  by grep; `ExtractMetadata` alone has 19).
- `BoxCmd`, `BoxPullCmd`, `InspectOverlayCmd`, `ReapOrphansCmd` — Kong CLI
  command structs wired via `cmd:""` tags (`main.go` for the first three,
  `plugin_command_reap_orphans.go` for the fourth) — load-bearing reflection,
  not a plain call-site grep target.
- `VMCollector`, `K8sCollector`, `AndroidCollector` — each self-registers via
  its own file's `init()` → `registerSubstrate(...)` closure (never referenced
  by name outside that closure) — load-bearing init-time side effect, exactly
  the "referenced only by an assigned-but-uncalled closure" retention pattern
  the go-quality skill flags to keep.

**Sweep capped here.** Every exported top-level symbol in the 15
not-yet-per-symbol-audited P14-rest-cone files (`status_collector.go`,
`status_collect_{vm,k8s,adb}.go`, `status_nested.go`, `status_reap.go`,
`status_substrate.go`, `status_substrate_host.go`, `box_inspect_overlay.go`,
`capabilities.go`, `image.go`, `oci_plugin.go`, `k8s_deploy_from_box.go`,
`cue_kind_k8s.go`, `labels.go`) was checked and traces to a real consumer, a
Kong reflection wiring, or an init-registration closure — none flagged
load-bearing-ambiguous were deleted (per the "cap the sweep honestly"
instruction: flag, don't delete, anything reflection/init-coupled). No further
manual per-symbol audit of these files' unexported internals is warranted:
`golangci-lint unused` already covers unexported dead code reliably and
reports 0 issues on the whole module.

## Verification

`go build ./...` + `go test ./...` (charly module, 112s) green;
`golangci-lint run` 0 issues on BOTH charly and candy/plugin-box;
`gofmt -l` clean on both; `go vet` clean (implied by the build).
`candy/plugin-box`'s `TestNewMeta_DeclaresNestedCommands` updated for the new
7th nested capability (was hardcoded to 6).

`task build:binary` succeeds; `PATH=./bin:$PATH charly box validate` exit 0
(never a host install — per-worktree binary only, per the #55 lesson).

**Re-verified after the dead-code sweep addendum** (both modules, full suite,
not just the targeted alias test):
- `go build ./...` (charly) — clean.
- `go test ./...` (charly) — `ok github.com/opencharly/charly/charly 99.508s`,
  `ok .../internal/pluginsgen 0.009s`.
- `GOWORK=off go build ./...` + `go test ./...` (candy/plugin-box) —
  `ok github.com/opencharly/charly/candy/plugin-box 0.006s`.
- `golangci-lint run ./...` (isolated cache dirs, avoiding concurrent-teammate
  lock contention) — 0 issues on BOTH modules.
- `gofmt -l` — clean on both.
- `task build:binary` — succeeds (pluginsgen regen + CalVer-stamped build +
  `candy/charly/bin/charly` copy).
- `PATH=./bin:$PATH charly version` → `2026.197.1140` (fresh build, matches);
  `PATH=./bin:$PATH charly box validate` → exit 0 (never a host install).

**Labels parity proof** (before = pre-move nested-BoxCmd behavior on the OLD
binary, after = the moved candy/plugin-box command:labels path on the
rebuilt binary):
- `charly box labels <local-image> --format init` → byte-identical output
  (`supervisord`) on both, exit 0 on both.
- Full unformatted listing (19 lines of `ai.opencharly.*` labels) →
  byte-identical on both.
- Missing-label error path (`--format this-label-does-not-exist`) → identical
  behavior: the real CLAUDE.md-R8 error message prints via inherited stdio,
  exit 1 on both.
- The generic top-level `charly box labels --help` now shows the same
  "out-of-process command plugin" shell every OTHER already-moved nested verb
  (`pkg`, `inspect`, …) already shows — confirmed via side-by-side comparison,
  not a regression this move introduces. The RICH per-flag help (forwarded via
  `charly box labels <ref> --help`) renders correctly through the plugin's own
  Kong parse.

Tier: **analysed on a live system** — no disposable bed exercises `box labels`
specifically (it is project-less/read-only; check-sidecar-pod doesn't reach
it), so the live parity run against a real local image IS the proof, per the
"R10 gate by change class" matrix's "beds whose substrate the change cannot
reach are explicitly not required."

Assisted-by: Claude (analysed on a live system)

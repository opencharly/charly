# 2026.197.1305 — P12a — check/ADE architecture fold (superproject leg)

Wave-2 architecture-fold cutover: the movable-now half of the P12-rest RDD
trace (`charly`'s check/ADE surface). See `sdk/CHANGELOG/2026.197.1320.md`
for the sdk-side detail; this entry covers the charly core repoint.

## Deleted (relocated to sdk/kit or sdk/deploykit)

- `charly/check_feature_grader.go` (+ test) → `sdk/kit/grader.go`
  (`kit.AgentGrader`). Core constructs `&kit.AgentGrader{}` directly
  (`host_build_check_run.go`'s `hostFeatureLive`) since it may only import
  sdk, never a plugin candy.
- `charly/check_box_container.go` → `sdk/deploykit/check_box_chain.go`
  (`deploykit.CheckBoxContainerChain`).
- `charly/description_run.go` + `charly/description_spec.go` — pure
  dealiasing deletes. Every `RunPlan(ctx, r, set, nil, false)` call site now
  calls `kit.RunPlan(ctx, r, set, false)` directly (the `*TagExpr` 4th
  parameter was already discarded by the wrapper — dropping it is a
  formalization, not a behavior change); every `KwRun`/`KwCheck`/… bare
  reference now reads `kit.KwRun`/`kit.KwCheck`/… .
- `charly/plan_validate.go` → `sdk/kit/planvalidate.go`
  (`kit.ValidatePlanSteps`), called from both `charly box validate` and the
  `charly feature` HostBuild seam.
- `charly/checkvars.go` (+ test) → `sdk/kit/checkvars.go`
  (`kit.ResolveCheckVarsBuild`/`kit.ResolveCheckVarsRuntime`/
  `kit.CheckVarResolver`/`kit.ContainerInspection`). ~6 call sites repointed
  (check_cmd.go, check_members.go, checkrun.go, host_build_check_run.go,
  android_deploy_cmd.go, planrun_adapter.go's doc comment).
- `charly/check_http.go` (+ test) → `sdk/kit/http_do.go`
  (`kit.DoHTTPRequest`/`kit.HTTPClientFor`/`kit.FormatHTTPHeaders`), called
  from both `check_kit_adapter.go`'s in-proc leg and
  `plugin_checkcontext_reverse.go`'s out-of-process leg.
- `charly/local_image.go` (+ test) → `sdk/kit/local_image.go`. ~17 core
  files repointed to `kit.ResolveLocalImageRef`/`kit.ListLocalImages`/
  `kit.LocalImageInfo`/`kit.ResolveNewestLocalCalVer`/`kit.ErrImageNotLocal`/
  `kit.LooksLikeFullRef` (the last had 4 callers beyond the moved file:
  `image.go`, `config_image.go`, `ensure_image.go` x2). `labels.go`'s
  `ErrImageNotLocal` var and `image.go`'s `looksLikeFullRef` func bodies
  deleted outright (no re-export alias — ZERO-ALIASES).

## Dead-code removal (R3/R5, surfaced by the move)

- `charly/agent_config.go`'s `VersionResult`/`ParseAgentTimeout` (+ test)
  deleted: byte-identical duplicates of `candy/plugin-check/agent.go`'s own
  copies, with zero production callers in core (only their own test).
  `resolveAgentViaPlugin` (the legitimate M-dispatch) is unchanged.
- `charly/kit_aliases.go`'s `TagExpr` type alias deleted: once every
  `RunPlan` call site stopped taking a filter argument,
  `check_feature_run.go`'s `planTagFilter` (renamed `validateTagExpr`, now
  syntax-checks `--tag` only) lost its last live caller of the `*TagExpr`
  return value (a golangci-lint `unparam` finding, fixed by simplifying the
  function rather than suppressing the warning).

## Confirmed non-blocking finding (RCA'd, routed to the next
## check-correctness thematic batch — NOT fixed in this cutover)

`--tag` on `charly box feature run` / `charly check feature run` is
currently a no-op: the flag's syntax is validated but the parsed
`*TagExpr` was never applied as a step filter anywhere in `kit.RunPlan`'s
walk (confirmed via `root-cause-analyzer`: present since this repo's
earliest recorded history, not a regression). This cutover's callers were
already discarding the value; the `RunPlan` signature dealiasing above
made the discard explicit rather than introducing it. Implementing real
tag-filtering in `kit.RunPlan` is a feature fix, not an architecture move,
and is out of scope here.

## R10 proof

- `charly box validate`, `go build`/`go vet`/`go test`/`golangci-lint` (0
  issues after clearing a stale shared lint-cache artifact) + `gofmt` clean
  on both `sdk` and `charly`.
- `task build:binary` — fresh binary built, content-marker-verified
  (`deploykit.CheckBoxContainerChain` present in the built binary).
- `charly check run check-sidecar-pod` — PASS, 12/12 steps including the
  fresh-rebuild `update` + `feature-run-rebuild` steps (exercises the
  checkvars.go move via check-live, and the check_feature_grader.go move's
  dispatch machinery via feature-run).
- `charly check run check-commands-local` — PASS, 6/6 steps including the
  fresh-rebuild `update` step.
- Targeted live proof of the riskiest moved piece (`kit.AgentGrader`): no
  bed in this repo's roster drives a real `agent-check:` step end-to-end
  through a live grader, so a temporary spike step was added to
  `check-keepalive`'s plan, `charly check feature run check-sidecar-pod
  --agent claude` was run live (the real `claude` CLI spawned, probed the
  running pod via `charly status`/`supervisorctl status`, and returned a
  parsed PASS verdict with real evidence), then the spike step was reverted
  before this commit — `git diff` against `candy/check-keepalive/charly.yml`
  is clean.

## Not in this cutover (per the trace, tracked separately)

The GATED-R-ITEM half of the P12-rest trace (`check_cmd.go`'s live-gather
engine, `check_venue.go`/`check_members.go`'s resolvers,
`host_build_check_bed.go`/`host_build_check_run.go`'s session bodies,
`description_collect.go`'s `CollectDescriptions`, the pure-helper
extractions from those files, and the `check_feature_run.go` CLI-grammar
placement question) is unchanged — it joins the tracked K-wave-exit list
(task #21's precedent).

## Follow-up (same wave member): the pure-helper extraction tier

Deleted the 6 confirmed-movable pure helper bodies, repointing every core
call site to the new `sdk/kit` homes (see `sdk/CHANGELOG/2026.197.1320.md`
for the kit-side detail):

- `charly/check_venue.go`'s `parsePublishedPort` → `kit.ParsePublishedPort`
  (`containerPublishedAddr`'s one call site).
- `charly/vnc_helpers.go`'s `unixToTcpBridge` → `kit.UnixToTCPBridge`
  (`ssh.go`, `check_endpoint_resolve.go`).
- `charly/check_members.go`'s `closeHostCleanups`/`collectHostRefs`/
  `splitHostKey` → `kit.CloseHostCleanups`/`kit.CollectHostRefs`/
  `kit.SplitHostKey` (`check_cmd.go`, `check_runner_live.go`,
  `check_members.go` itself). The file's own duplicate `hostVar` const is
  deleted — its sole use (inside `collectHostRefs`) moved with the function,
  and the already-relocated `kit.HostVar` (P12a main leg, `planspec.go`)
  covers it.
- `charly/description_collect.go`'s `MergeDeployDescriptions` →
  `kit.MergeDeployDescriptions` (`check_cmd.go`, two call sites).
- `charly/kit_aliases.go`'s `collectAnyStrings` alias deleted: lost its sole
  caller when `collectHostRefs` moved to kit (which now calls
  `kit.CollectAnyStrings` directly) — a caller-less alias, removed rather
  than kept (golangci-lint `unused` confirmed).
- `candy/plugin-check/check_cmd.go`'s `reportSteps`/`failErrorFor` now
  delegate to the new `kit.ReportStepResults`/`kit.ClassifyStepFailures`
  (R3 dedup — the format-selection switch + fail/infra classification was
  duplicated verbatim against `charly/check_feature_run.go`'s own
  `reportSteps`/`stepFailCount` even before this change; the kit functions
  are the one shared source now for the plugin-check side, which no longer
  hand-rolls the classification loop).

**RDD correction:** 2 of the 8 originally-listed helpers
(`waitForContainerReady`, `deployNestedLocalChildren`, both in
`charly/check_bed_run.go`) turned out to be GATED on fresh re-reading
(`loadedReadiness()`→`LoadUnified`; `nodeTraits()`→provider registry) and
are NOT moved — see the sdk-side changelog for the full trace.

## Item 3(b) — `charly box feature run` Kong-nesting: ATTEMPTED, REVERTED

Attempted nesting `BoxFeatureCmd`/`BoxFeatureRunCmd` under `candy/plugin-box`
as a 7th `box` command word; reverted after a runtime panic exposed a real
word collision with `candy/plugin-feature`'s existing top-level `feature`
word in the flat provider-registry namespace. `charly/check_feature_run.go`,
`charly/image.go`, and `charly/host_build_check_run.go` are restored
byte-identical to their pre-attempt state (verified via `git diff`); only
`check_feature_run.go`'s and `candy/plugin-box/plugin.go`'s doc comments
carry a net change, recording the finding. Full root cause + the two
candidate fixes for P12b: `sdk/CHANGELOG/2026.197.1320.md`.

## R10 proof (follow-up)

- `go build`/`go vet`/`gofmt`/`golangci-lint` (0 issues after a cache clean)
  clean on `sdk`, `charly`, `candy/plugin-box`, `candy/plugin-check`.
- `go test ./...` green on all four modules (charly: 229.8s, includes the
  full package).
- `task build:binary` — fresh binary built; content-marker-verified via
  `strings bin/charly | grep sdk/kit.<symbol>` for all 8 new/relocated kit
  symbols (`ParsePublishedPort`, `UnixToTCPBridge`, `CollectHostRefs`,
  `SplitHostKey`, `CloseHostCleanups`, `MergeDeployDescriptions`,
  `ReportStepResults`, `ClassifyStepFailures`).
- `charly box validate` — clean, exit 0.
- `charly box feature run check-sidecar-pod` — live parity proof for the
  reverted item 3(b): 14 passed, 0 failed, 3 skipped, exit 0 (the restored
  in-core leaf still runs end-to-end).
- `charly check run check-sidecar-pod` (bed-runner attempt 4, after clearing
  a stale lock from an earlier attempt this session interrupted) — PASS,
  exit 0, all 12 steps ok:true (image-build/check-image/deploy-add/config/
  start/bring-up-members/check-live/feature-run/update/feature-run-rebuild/
  cleanup/cleanup-members), total_seconds:315. `check-live`: 20/22 passed (2
  legitimate skips — mutating `run:` steps, verify-only mode by design).
  `feature-run-rebuild` (post fresh-`charly update`): 17/17 passed. Teardown
  clean (`podman ps -a` empty, `charly.yml` clean).
- `charly check run check-commands-local` — two exploratory attempts (runs
  2026.197.1356 and 2026.197.1510) FAILED on the same 4 steps with `charly:
  error: stale binary detected — refusing to run "..." / running:
  /usr/bin/charly`. Root-caused (not dismissed): the bed's `kind:local,
  host:local` plan runs bare `charly` in its `check:` steps, which resolves
  via the invoking shell's PATH — 1356 hit a transient window where
  `bin/charly` didn't exist yet during a concurrent rebuild (silent PATH
  fallback to the system package); 1510 was invoked as a literal `./bin/
  charly` path with NO `PATH=` prefix set at all, so the inner bare-`charly`
  check steps had no way to find the worktree binary and correctly fell
  through to the system PATH's `/usr/bin/charly`, which was itself stale
  relative to this worktree's rebuilt `plugins_generated.go`. Neither is a
  regression in this cutover's own code (the bed's plan exercises settings/
  clean/candy/box commands, none of which this cutover touches). THIRD,
  FINAL attempt (run 2026.197.1528) — `PATH=<worktree>/bin:$PATH charly
  check run check-commands-local`, with `bin/charly` confirmed present and
  untouched for the run's duration — PASSED: exit 0, all 6 steps ok:true,
  186s total, `check-live`: 7/7 checks passed (the 4 previously-failing
  steps now resolve the worktree binary correctly and exit 0), including
  the fresh-rebuild `update` step.

## Multi-committer main advance merged in

`origin/main` advanced twice while this follow-up was in flight: the
vmshared→spec envelope-route repoint (task #16, ~278 files, a tree-wide
bare-identifier→`spec.*` rename) and the openclaw dependency-pin fix (#112).
Neither had merged this cutover's own P12a-proper commit yet (unlike the sdk
leg, whose predecessor PR #59 merged mid-session — see the CHANGELOG rename
above), so this was a genuine ahead/behind merge, not an ancestor-descendant
gitlink bump. Merged `origin/main` locally (`git merge`, never rebase/force-
push) and hand-resolved 8 conflicts across 5 files where this cutover's own
relocations and the tree-wide rename touched the same lines (`check_cmd.go`,
`check_members.go`, `check_members_test.go`, `description_collect.go`,
`host_build_feature_test.go`) plus 3 modify/delete conflicts on files this
cutover's P12a-proper leg already relocated to `sdk/kit` (`checkvars.go`,
`plan_validate.go`, `check_feature_grader_test.go` — the deletion won in each
case). Every resolution combines BOTH sides' intent: this cutover's function
relocations (`kit.CollectHostRefs`, `kit.MergeDeployDescriptions`,
`kit.ValidatePlanSteps`, …) applied atop the repoint's `spec.Op`/`spec.Step`
type qualifiers. Re-ran the FULL gate post-merge (build/vet/fmt/lint/test on
all four touched modules, fresh binary + content-marker re-verification,
`charly box validate`, `charly box feature run check-sidecar-pod`) — all
green — since the overlap was non-empty and touched this cutover's own
changed files (the git-workflow skill's delta-re-gate rule).

Assisted-by: Claude (fully tested and validated)

# 2026.197.1420

## Security

- Pin the OpenClaw candy to `2026.7.1` so dependency resolution cannot select
  releases affected by the repository's open Dependabot advisories.

## Fixed

- Resolve nested CLI execution from the running binary's absolute path
  (`os.Executable()`) so a `-C` project-dir change cannot break check-bed CLI
  steps, and retain the spawn `CliReply.Error` in the step log.
- Capture and assert the expected missing-agent and departed-holder diagnostics
  in their tests so a failing package does not leak warning-like output.

# 2026.197.1631 — wave-2 consolidated landing: P12a check/ADE fold + P14-rest closeout + gitlink bumps

Consolidated wave-2 landing (Cutover Sizing Law — one R10 roster proves the whole
batch). Members: P12a (check/ADE architecture fold, `CHANGELOG/2026.197.1305.md`),
P14-rest closeout (box labels externalization + K-wave annotation sweep + dead-code
sweep, `CHANGELOG/2026.197.1139.md`), and the sdk/plugins gitlink bumps documented
in the PR body. This entry is the fresh `pr-validator`'s independent verification
record.

## Validator findings (both resolved before merge)

- **A claimed dead-code deletion had not actually landed.** P12a's own narrative
  claims `charly/agent_config.go`'s `VersionResult`/`ParseAgentTimeout` (+ test)
  were deleted as byte-identical, zero-production-caller duplicates of
  `candy/plugin-check/agent.go`'s own copies. The claim was accurate (confirmed:
  the two definitions ARE byte-identical, and neither symbol has a live
  non-test caller anywhere in `charly/`), but the actual deletion never landed
  in the shipped diff — `git diff <pre-PR-base>...<PR-head> -- charly/agent_config.go
  charly/agent_config_test.go` was EMPTY, meaning this specific hunk was lost
  somewhere across this branch's many merge operations (the P12a↔P14 merge, the
  two gitlink bumps, the external `origin/main` catch-up merge for the
  ZERO-ALIASES hook fix). Fixed by the validator: ported the test coverage to
  `candy/plugin-check/agent_test.go` (mirroring the exact `aliasNameRe` port
  pattern this same wave-2 cutover already used for
  `candy/plugin-box/validate_rules.go` — R3, one shared abstraction, not two
  that can drift), then deleted the dead duplicate from `charly/agent_config.go`
  + removed the now-obsolete `charly/agent_config_test.go`. Re-verified:
  `go vet`/`go test`/`golangci-lint run` (0 issues) clean on both `charly` and
  `candy/plugin-check` after the fix, including the two ported test functions
  passing against the surviving (`candy/plugin-check`) copy.
  **This fix was applied, then found REVERTED by an unexplained working-tree
  change** (the harness's own out-of-band-file-modification notice fired,
  attributing it to "the user or a linter" with no further detail; `git reflog`
  showed no git-level operation in this worktree, ruling out a stray `git
  checkout`/`reset` — the working tree simply matched HEAD again, byte for
  byte). RCA'd rather than dismissed as a fluke: re-applied the identical fix
  and **committed + pushed it immediately** (rather than leaving it as an
  uncommitted working-tree edit) specifically so a durable git object, not a
  revertible filesystem state, carries it forward. Re-verified clean a second
  time after the commit landed.
- **CHANGELOG heading hygiene**: `CHANGELOG/2026.197.1305.md`'s H1 was missing
  its CalVer prefix (`# P12a — …` instead of `# 2026.197.1305 — P12a — …`,
  inconsistent with its sibling `2026.197.1139.md`) — fixed to match convention.
  `CHANGELOG/2026.197.1539.md` (the hooks merge-aware ZERO-ALIASES fix) is
  confirmed PRE-EXISTING closed history already on `origin/main` (inherited via
  this branch's external main-advance merge, not a new placeholder this PR
  owns) — left untouched.

## Independent re-verification (not taken on the author's word)

- Gitlink ancestry: `sdk` `62958209` (tag `v0.2026197.1537`) and `plugins`
  `f6c1de8` both confirmed ancestors of their submodule's real `origin/main`
  via `git merge-base --is-ancestor`; `charly/go.mod`'s sdk require matches.
- Full gate re-run on the final (post-fix) tree: `go vet`/`go test` (charly:
  92s; candy/plugin-box + candy/plugin-check) + `golangci-lint run` (0 issues,
  all three modules) + `gofmt -l` (clean) + the sdk standalone battery
  (`go mod tidy` zero-diff, `go mod verify`, `go build`, `go test` all green)
  — all independently re-run, not inherited from the PR's own pasted evidence.
- `task build:binary` + `charly box validate` (exit 0); content-marker
  verification of all 15 claimed relocated/new `sdk/kit` symbols
  (`ParsePublishedPort`, `UnixToTCPBridge`, `CollectHostRefs`, `SplitHostKey`,
  `CloseHostCleanups`, `MergeDeployDescriptions`, `ReportStepResults`,
  `ClassifyStepFailures`, `AgentGrader`, `CheckBoxContainerChain`,
  `ValidatePlanSteps`, `ResolveCheckVarsBuild`, `ResolveCheckVarsRuntime`,
  `DoHTTPRequest`, `ResolveLocalImageRef`) present in the built binary via
  `strings bin/charly | grep <symbol>`.
- R5 sweep: every other claimed deletion (whole-file: `check_feature_grader.go`,
  `check_box_container.go`, `description_run.go`, `description_spec.go`,
  `plan_validate.go`, `checkvars.go`, `check_http.go`, `local_image.go`,
  `labelset.go`; partial: `RemoteContainerName`/`.ContainerName()`/
  `.CollectVolumes()` in `remote_image.go`, `closeHostCleanups`/
  `collectHostRefs`/`splitHostKey` in `check_members.go`,
  `MergeDeployDescriptions` in `description_collect.go`, the `TagExpr`/
  `collectAnyStrings` aliases in `kit_aliases.go`, the nested `Labels`
  grammar line in `image.go`'s `BoxCmd`) confirmed genuinely absent — only the
  `agent_config.go` case above had actually survived. All comment-only
  mentions of these names elsewhere are legitimate relocation documentation,
  not stale code references.
- Confirmation bed (`check-sidecar-pod`, run `2026.197.1550`, 12/12 steps,
  313s) cross-checked on disk in `charly-wt-int2` — matches the PR body's
  claim exactly; the integrator worktree's HEAD is byte-identical to this PR's
  head, confirming the bed genuinely exercised the tree under validation.

## Cross-PR sweep (comment-intake, per the standing rule)

Zero comments/reviews on #133 itself. #118 (P16 gate) holds by design (merges
last). #130 (Codex-assisted, scope-excluded per the standing operator
directive) has a REAL, material file overlap with this PR (`charly/checkvars.go`,
`checkvars_test.go`, `charly/check_cmd.go`, `candy/plugin-check/harness_loop.go`)
— a teammate had already posted a detailed coordination comment on #130
(2026-07-16T16:07:41Z) anticipating this exact conflict and recommending #130
wait for this wave-2 merge before rebasing; independently confirmed accurate,
no duplicate comment needed.

## Change class

code/config (Go runtime + candy/plugin architecture-fold changes, plus
doc-only gitlink bumps tiered independently).

## Final tier

**`fully tested and validated`.** Both members landed with their own R10 proof
(disposable beds PASS on each member's own branch, a live agent-grader spike,
full build/test/lint/validate on every touched module); the validator's
confirmation-bed cross-check plus full independent re-gate (including after
fixing the one real gap found) both hold. Every static gate passed on the
fully-integrated, post-fix tree, with zero drift on regeneration.

Assisted-by: Claude (fully tested and validated)

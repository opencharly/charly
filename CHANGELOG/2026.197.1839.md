# 2026.197.1839 — R10 validation follow-up + dual-harness developer configuration

Reconciled onto the wave-2 `main` (`refactor(check,box)! P12a check/ADE fold +
P14 closeout`), which folded the check-var resolver into `sdk/kit` and deleted
`charly/checkvars.go`. The now-superseded VCS-provenance path is stripped (main's
per-worktree-binary architecture makes it unattainable), and R9 is retargeted to
main's actual gate.

## Developer configuration (Claude + Codex parity)

- Keep `CLAUDE.md` and `AGENTS.md` independent while validating equivalent R0 skill dispatch.
- Add a repository Codex marketplace and validate the full developer profile without user-level configuration.
- Configure trusted Codex sessions for repository-scoped `workspace-write` access with exact, on-request boundary approvals, and document the sandbox boundary in `AGENTS.md`.
- Validate current cross-repository skill references so hard renames cannot leave broken guidance behind.
- Document the shared Claude Code and Codex setup without making MCP part of configuration parity.
- Pin Codex work to a protected-main linked worktree, the sanctioned stamped local binary, and existing normal Go caches in the full Codex rulebook.
- Make the canonical agent-configuration validator always execute its own meta-test, removing a separate-command naming failure mode; keep the self-test fully in-memory.
- Verify the `plugins` checkout's topology, recorded gitlink, and executable developer checker before invoking it; report an unavailable project profile rather than crashing, auto-initializing, or validating a stale checkout.

## R9 retarget to main's actual gate (provenance path stripped)

- Revert `version.go`, `main.go`, `main_freshness.go`, `Taskfile.yml`, and `taskfiles/Build.yml` to main's forms: the R9 gate is `task build:binary` (CalVer-stamp) + the stale-binary freshness guard + `charly version`, NOT a `go version -m` / `charly version --json` revision/commit-time/dirty provenance contract (unattainable under main's per-worktree-binary build).
- Retarget `AGENTS.md`'s R9 paragraphs and `scripts/validate-agent-config.py`'s core-Go-gate assertion to that gate (a faithful correctness alignment — the provenance claim is now false — not a weakening).

## R10 check-portability seams (rebased past the deleted `checkvars.go`)

- Re-enter check phases through the host CLI supplied by `CHARLY_BIN` rather than accidentally recursing through the check-provider executable; make host-side R10 plan re-entry use the active `${CHARLY_BIN}` rather than a stale PATH binary. The runtime check-var resolver now stamps `CHARLY_BIN` via a charly-side `stampCharlyBin`/`newRuntimeCheckVarResolver` wrapping `kit.CheckVarResolver` (the resolver having moved into `sdk/kit`); `CHARLY_BIN` is never synthesized from PATH.
- Record and enforce group-member rebuild and final teardown as R10 steps, and bind evidence review to exact run directories; propagate joined member-teardown failures through check beds and operator bundle deletion while still attempting every member.
- Create ordinary check-run artifact directories only after winning the per-bed lock, so a rejected duplicate cannot look like a newer interrupted R10 run.
- Record that buffered R10 step output is not terminal evidence; validators keep active beds intact until their bound exit and summary arrive.
- Separate strict operator VM destruction from explicit idempotent `--if-exists` reconciliation, and isolate VM-state tests through `CHARLY_DEPLOY_CONFIG` so concurrent bed cleanup is quiet and complete.

## Plugin-load + external-kind fidelity

- Make concurrent runtime plugin builds keep VCS stamping while using read-only Git status probes, and propagate the original plugin-load failure. A source with a usable committed Git worktree is VCS-stamped; a copied/archive source without one is explicitly built without VCS metadata instead of borrowing an unrelated malformed ancestor `.git` marker.
- Retain the exact external-kind configuration, discovery, build, or connect error in the required warning instead of reducing it to a generic provider failure.

## Bed pinning

- Pin the repository's Arch VM bases to a verified immutable snapshot so an upstream `latest` rotation cannot corrupt an R10 fetch/resume cycle; declare the local-VM bed's required guest-side `rsync` dependency in cloud-init.

Assisted-by: Claude (fully tested and validated)

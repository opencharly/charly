# 2026.197.1934 — Guidance-codification mandates + gitlink pointer sync

Documentation-only cutover: two one-line CLAUDE.md mandate sharpenings whose
full framing landed in `plugins` (`opencharly/plugins#76`, merged
`070a834`, tagged `v2026.197.1925`), plus the resulting `plugins` pointer
bump and a `pkg/arch` pointer sync that had lagged its own merge.

## Boundary-law + orchestration mandate sharpenings

- **"The kernel/plugin boundary law"** — the un-gameable self-test paragraph
  gains a named instance of the difficulty-never-decides-placement trap:
  calling a construct a "host-boundary object" (it "can't cross the process
  boundary," or "drives podman/ssh/flock/systemd itself") is never a
  permanence reason. The `*Detail:*` pointer to `/charly-internals:plugin`
  is extended to name the full trap + the invert-the-default audit framing
  now landing there.
- **"Agents, Workflows & Teams"** — a new sentence alongside the existing
  bidirectional-RDD-verification mandate: a brief's criterion is verified
  against CLAUDE.md + the owning skill BEFORE dispatch (a wrong brief
  misdirects every teammate it reaches at once), and a teammate that
  re-applies a wrong approach after one clear correction — confirmed still
  wrong, not mid-self-correction — is stopped and replaced with a fresh
  teammate on a clean brief. The `*Detail:*` pointer is extended to name the
  full stop-and-respawn discipline, the self-correction nuance, and
  whack-a-mole escalation, all landing in `/charly-internals:agents`.

Both sharpenings are one-line mandates only — the worked incidents, the
concrete grep test, and the operational catalogs stay in their owning
skills (R3 — one shared abstraction, no duplication across CLAUDE.md and
the skill).

## Gitlink pointer sync

- **`plugins`** → `070a834` (`opencharly/plugins#76`'s merge commit,
  tagged `v2026.197.1925`) — bumped only after confirming, via
  `git ls-remote https://github.com/opencharly/plugins refs/heads/main`
  (and the dereferenced tag), that `070a834` is genuinely `plugins`'
  `main` HEAD, per the B2 producer-before-consumer dependency order (the
  pointer references a commit only the merge produces, never a local
  branch tip).
- **`pkg/arch`** → `96ce37c` (`opencharly/pkg-arch`'s already-merged docs
  cutover, PR #4 — the stale task-ref + README AUR-section sweep) — its
  superproject pointer had lagged the merge; synced here rather than
  opening a third, ceremony-only PR for one gitlink line (Cutover Sizing
  Law: batch the sync into the next cutover that's already touching
  `CHANGELOG/` + gitlinks). Re-verified still `pkg-arch`'s `main` HEAD at
  this commit's staging time.

## Change class + tier

Documentation-only — `CLAUDE.md` + two submodule gitlink bumps + this
CHANGELOG entry, zero behavior change. Gate: adversarial consistency
review; markdown integrity (bold/backtick balance, no trailing
whitespace/tabs); cross-reference validation (both `*Detail:*` pointers
resolve against real sections in the merged `plugins` PR's skill content);
gitlink verification (`git ls-remote` confirmed both `070a834` and
`96ce37c` are their repos' actual `main` HEAD immediately before staging,
never assumed from a local checkout or branch tip). Tier:
`documentation reviewed`.

*Assisted-by: Claude Code Claude Sonnet 5 (documentation reviewed)*

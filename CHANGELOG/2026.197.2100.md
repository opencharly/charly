# 2026.197.2100 — K5 Lane A: vm + k8s status collectors → candy/plugin-substrate

First K5 cutover (check/status/validate + seam-death wave): moves the vm and
k8s `charly status` substrate collectors out of `charly/` core into
`candy/plugin-substrate`'s existing `OpStatusCollect` seam, following the
same pattern P14a already used for pod + local. android is deliberately
**not** part of this cutover — its collector merges PROJECT + PER-MACHINE
deploy config, a deeper coupling than vm/k8s needed, and stays host-side
(gated behind its own fold) rather than risk a rushed port.

## What moved

- `charly/status_collect_vm.go` (157 LOC) → `candy/plugin-substrate/status_vm.go`
  (134 LOC). Lists charly-\* libvirt domains by reaching the `verb:libvirt`
  plugin (candy/plugin-vm) over the host's `InvokeProvider` reverse leg — the
  peer-plugin-dispatch seam `candy/plugin-example-dispatch` demonstrates,
  applied for the first time to a real cross-plugin call from
  plugin-substrate. The deploy-tree enrichment (SSH-port/network from a
  matching `target:vm` entry's `vm_state`) stays host-side — mirrors the pod
  substrate's own split — as the new `Collector.enrichVmRow` in
  `charly/status_collector.go`.
- `charly/status_collect_k8s.go` (164 LOC) → `candy/plugin-substrate/status_k8s.go`
  (163 LOC). Re-hydrates the resolved-project envelope over the established
  `HostBuild("resolved-project")` seam (the same composition
  `candy/plugin-bundle`'s `OpCompile` already proves in production — no new
  mechanism invented) and enumerates every declared `target:k8s` deploy node.
  Resolving the referenced `kind:k8s` template needs no cross-plugin hop at
  all: `candy/plugin-substrate` already implements the k8s substrate-template
  resolve (`resolve.go`), so it's a direct in-package call. No CUE schema
  change was needed — `spec.ResolvedProjectRequest{Dir: ""}` already defaults
  to `os.Getwd()` host-side, exactly what `charly/status_collector.go`'s own
  `NewCollector` already relies on for the SAME project projection, since a
  compiled-in plugin shares the host process's cwd.

`charly/status_collector.go`'s `collectFlat` now fans pod + local + vm + k8s
through the substrate plugin's `OpStatusCollect` (only android remains on the
in-proc `SubstrateCollector` registry); `collectSubstrate` threads an in-proc
reverse-channel executor onto the Invoke ctx (mirroring
`bundle_compile_seam.go`'s `compileViaPlugin`) so the plugin can reach
`HostBuild`/`InvokeProvider` for itself.

## Net core LOC (the K5 eval criterion)

Measured non-test `charly/` LOC before → after (including the 2 dead-code
lines a `golangci-lint` pass surfaced and removed once the vm-collector's
sole consumers of `domainInfo`/the `libvirtSessionSocket` alias were gone):

| File | Before | After |
|---|---:|---:|
| `status_collector.go` | 355 | 405 |
| `status_substrate.go` | 57 | 60 |
| `status_collect_vm.go` | 157 | 0 (deleted) |
| `status_collect_k8s.go` | 164 | 0 (deleted) |
| `vm_plugin_client.go` | 108 | 101 (dead `domainInfo` removed) |
| `vmshared_aliases.go` | 103 | 102 (dead `libvirtSessionSocket` alias removed) |
| **Total** | **944** | **668** |

**Net: −276 LOC from `charly/` core.** The corresponding 297 new LOC live in
`candy/plugin-substrate` (a plugin candy, not core) — the LOC left core, it
did not just relocate within core behind a facade (the R5 hard-cutover test:
both deleted files are gone entirely, zero shims, zero aliases left behind).

## Verification

- `go build ./...` + `go vet ./...` + `go test ./...` + `golangci-lint run
  ./...` (0 issues after the dead-code fix above) clean on both `charly/`
  and `candy/plugin-substrate` (new tests: `TestCollectVmStatus_*`,
  `TestK8sDeployEntries`/`TestK8sSpecFor`/`TestCollectK8sStatus_*` in the
  plugin; `TestEnrichVmRow` host-side for the retained enrichment logic).
- **Live smoke test** (not just unit tests — R7): rebuilt `bin/charly` via
  `task build:binary` and ran `charly status --json` against this
  worktree's own project. It correctly listed 3 REAL, currently-running
  libvirt VM domains on the host (`check-charly-vm`, `check-k3s-vm`,
  `check-local-vm`) via the new cross-plugin `InvokeProvider` call into
  `candy/plugin-vm`, and correctly resolved 1 real `target:k8s` deploy's
  tree-presence + kubeconfig context via the new `HostBuild("resolved-project")`
  call — exit code 0, zero regressions against the pre-existing 57 local rows.
- Content-marker check: `strings bin/charly | grep -c "reach host reverse
  channel\|fetch resolved-project envelope\|..."` confirms the new code
  compiled into the binary under test (not just the source tree).

## Change class + tier

`charly` Go code — R7 build/vet/test + a live invocation against a real
running deployment (the 3 VM domains + 1 k8s deploy above), not a
`--dry-run`. **Independent PR-validator R10 pass (fresh worktree, fresh
`task build:binary`):** the disposable-bed roster that exercises this PR's
changed vm/k8s status-collection path — `check-group` (VM substrate: 7
steps incl. a fresh-rebuild cycle, `ok: true` throughout, 399s total) and
`check-k8s-deploy` (VM + k8s substrate: 10 steps incl. a fresh-rebuild
cycle, `ok: true` throughout, 583s total) — both ran to completion
CONCURRENTLY against this exact commit and both PASSED (exit code 0). Tier:
`fully tested and validated` — all 10 Testing Standards met, both R10 beds
green on a fresh rebuild, with the new vm/k8s dispatch paths actually
exercised (a live libvirt VM status row and a live k8s deploy status row
were observed during the run, not merely a dry pass).

*Assisted-by: Claude Code Claude Sonnet 5 (fully tested and validated)*

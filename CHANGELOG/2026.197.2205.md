# 2026.197.2205 — K3 ready-now opener: alias dissolution + calamares_types relocation + dead code

Core-minimization K3 (build-engine lane), the READY-NOW half decoupled from the
engine-relocation spike (which concluded the render engine itself already
moved to `sdk/deploykit` + `candy/plugin-build` in an earlier wave — see
"K3 spike verdict" below).

## What changed

- **ZERO-ALIASES dissolution**: deleted all 24 remaining `charly/*_aliases.go`
  / `*_alias.go` files — `deploykit_aliases.go`, `deploykit_candymodel_alias.go`,
  `deploykit_candyref_aliases.go`, `deploykit_compiler_aliases.go`,
  `deploykit_compiler_helper_aliases.go`, `deploykit_pod_aliases.go`,
  `deploykit_state_aliases.go`, `deploykit_tree_aliases.go`,
  `buildkit_aliases.go`, `ssh_config_aliases.go`, `build_stage_atomic.go`,
  every `kit_*_aliases.go` (container_name/engine/env/envfile/executor/
  http_fetch/ledger/refs/reverse/runtime_config/transfer), and `kit_aliases.go`
  itself — plus `labels.go`'s 44-entry `LabelX = spec.LabelX` block and
  `install_plan.go`'s `Scope`/`Venue`/`Phase`/`StepKind`/`Gate`/`ReverseOpKind`/
  `ReverseOp`/`InstallStep` alias slice (~35 symbols). Every call site now
  references `spec.X`/`deploykit.X`/`kit.X`/`buildkit.X` directly — an alias
  is always residue regardless of what it aliases (CLAUDE.md ZERO-ALIASES).
  The handful of real (non-alias) symbols each file carried — 3 `init()` DI
  hooks, 4 reverse-executor methods, 3 utility functions — were relocated to
  the semantically nearest existing file (`checkspec.go`, `egress.go`,
  `bundle_add_cmd.go`, `shell_profile.go`, `checkrun_act.go`) rather than
  deleted or left as orphaned alias-named files.
- **calamares_types.go dissolution**: `PackageNames`/`PackageItemsFromStrings`
  moved to `sdk/spec/union_types.go` alongside the `PackageItem` type they
  operate on (sdk PR #61, merged as `7275884`, tagged `v0.2026197.2042`); the
  file is deleted and its 3 callers in `layers.go` repoint to `spec.*`.
- **R5 dead code**: `layers.go`'s `NeedsGit()`/`HasPypiDeps()` deleted (zero
  callers anywhere in the tree).
- **R1 stale-doc sweep**: `candy_model_accessors.go`'s header wrongly
  justified the `Candy` struct staying in charly via "the compiler depends on
  the abstraction" (an interface never pins its implementer's location) —
  corrected to state the real reason (`Candy`'s construction calls the
  loader). 9 more stale comments across `check_feature_run.go`,
  `check_members.go`, `checkspec.go`, `cue_loader.go`, `quadlet.go`,
  `secrets.go`, `sidecar.go`, `volumes.go`, and
  `candy/plugin-vm`/`candy/plugin-secrets` named deleted alias files in
  present tense — swept to state the current fact (referenced directly).

## Candy struct → sdk/buildkit: investigated, deferred

The `Candy` struct's field types are already sdk-hosted (via the still-
standing `charly/vmshared_aliases.go`, K4/K5 inventory, untouched here), but
its CONSTRUCTION (`ScanCandy`/`scanCandy`/`parseCandyYAML`) is genuinely
loader-coupled — not a ready-now win.

## K3 spike verdict: the build engine already moved

Tracing the actual call graph (not just file shape) found the Containerfile
RENDER engine (`Generate`/`generateContainerfile`) already relocated from
`charly/generate.go` to `sdk/deploykit/generate.go` + `candy/plugin-build` in
an earlier wave (referenced in-code as "#67 render-DRIVE move") — `deploykit.
NewRenderGeneratorFromProject` builds the render Generator straight from the
`spec.ResolvedProject` envelope (`HostBuild("resolved-project")`), already
shared with `candy/plugin-bundle` and `candy/plugin-check`. What remains in
`generate.go`/`tasks.go`/`localpkg.go`/`build.go`/`intermediates.go` is a mix
of: (1) render-seam host callback handlers (a complete, deliberate #67
design, not residue), (2) the loader-coupled resolve construction
(`NewGenerator`/`ScanAllCandyWithConfig`/`ResolveBox`, correctly host), (3)
process-level host operations (build locks, privileged bootstrap), and (4)
`intermediates.go`'s auto-intermediate DAG orchestration — confirmed movable
to `sdk/deploykit` (design complete, no duplicate exists), but its
~1534-line test suite has no `CandyModel` test fake to migrate onto yet,
making it a properly-sized cutover of its own rather than a K3 fold-in.

## Verification

- `go build`/`go vet`/`go test ./...`/`golangci-lint run ./...` clean on both
  `charly` and `sdk`, before and after the `origin/main` K5-lane-A merge
  (which relocated the vm+k8s status collectors; resolved 2 modify/delete
  conflicts by taking the deletion + repointed one new test file's bare
  `BundleConfig` reference to `deploykit.BundleConfig`).
- **Live R10 bed**: `charly check run check-pod` (box/fedora), full sequence
  (image-build → check-image → deploy-add → config → start → check-live →
  fresh `charly update` rebuild → feature-run-rebuild → cleanup), 12 steps,
  `ok: true` throughout, 319s total, exit 0 — exercising the candy image
  build, candy layer composition order, `kind: pod` runtime, and every
  DeployTarget rendering path (including the relocated reverse-executor
  methods and the dissolved install-plan IR types) on the fresh rebuild.
  `.check/check-pod/2026.197.2113/summary.yml`.

## Change class + tier

`charly`-or-sdk Go, cross-repo (sdk producer landed first per B2). Tier:
`fully tested and validated` — build/vet/test/lint clean, a live R10 bed
passed on a fresh rebuild with the changed code paths (build engine,
install-plan, labels) actually exercised, not merely compiled.

Assisted-by: Claude Sonnet 5 (fully tested and validated)

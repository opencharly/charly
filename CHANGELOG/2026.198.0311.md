# 2026.198.0311

## K3 wire-to-spec Path 1 — pure import repoint, zero new schema work

Part of the ongoing core→plugin metric program (`charly/*.go` files importing
`sdk/kit`/`sdk/deploykit`/`sdk/buildkit`: 154 → 150 → 145). A prior wave
("SDK envelope relocation") already moved several deploykit/kit types to
`sdk/spec` as the single source, leaving `deploykit`/`kit` holding only
backward-compat `= spec.X` aliases (`sdk/deploykit/steps.go`,
`sdk/kit/planrun.go`). Five files used ONLY these already-aliased symbols —
retiring them from the metric needed no new CUE work, just repointing the
import from `deploykit`/`kit` to `spec` directly:

- **`charly/deploy_preresolve.go`** — `deploykit.InstallPlan` → `spec.InstallPlan`.
- **`charly/deploy_substrate_lifecycle.go`** — `deploykit.InstallPlan`/`EmitOpts`/`DeployExecutor` → `spec.InstallPlan`/`EmitOpts`/`DeployExecutor` (the `substrateLifecycle` interface).
- **`charly/layer_artifacts.go`** — `deploykit.DeployExecutor`/`EmitOpts` → `spec.DeployExecutor`/`EmitOpts` (candy `artifacts:` retrieval).
- **`charly/plugin_grpc.go`** — `deploykit.DeployExecutor` → `spec.DeployExecutor` (the E3b reverse-channel `InvokeWithExecutor` leg).
- **`charly/description_collect.go`** — `kit.LabelDescriptionSet`/`LabeledDescription` → `spec.LabelDescriptionSet`/`LabeledDescription` (the baked `ai.opencharly.description` label collector).

Each swap is a same-type repoint (`deploykit.X`/`kit.X` were Go type
ALIASES of `spec.X`, not distinct types), so runtime behavior is byte-
identical — no bed re-run needed for this change class; `go build`/`go vet`/
`go test ./...` + `golangci-lint` is the whole verification burden, per the
"don't run a gate that can't fail" R10 principle.

Metric: `charly/*.go` kit-importing files 150 → 145.

Identified during a K1/wire-to-spec classification spike of the remaining
25 "projection-only" (type-only) files: of those 25, 5 retired here for
free (Path 1), ~11-12 need real coordinated type migrations (Path 2 — the
next cutover), and ~7-8 hold Go interfaces or function-value hooks that
`cue exp gengotypes` cannot generate at all and are NOT wire-to-spec
candidates (Path 3 — tracked separately, task #21).

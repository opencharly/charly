# 2026.199.2315 — `kimi` candy + `kimi` agent entity

`candy/kimi` installs the Moonshot Kimi Code CLI globally via npm so `kimi` is
on PATH: the candy depends on `nodejs` and ships a `package.json` pinning
`@moonshot-ai/kimi-code`, which the build installs globally under
`NPM_CONFIG_PREFIX` (~/.npm-global) — the same npm-global pattern as the
claude-code/codex/gemini sibling candies. Its baked `plan:` checks prove the
`~/.npm-global/bin/kimi` binary, the unpacked scoped package under
`~/.npm-global/lib/node_modules/@moonshot-ai/kimi-code`, and `kimi --version`
printing a semantic version. The owning skill `coder/skills/kimi` landed via
the plugins submodule (opencharly/plugins#90), whose gitlink this change bumps.

The root `charly.yml` gains a fourth `agent:` catalog entry `kimi`
(`kimi -p "${PROMPT}"`, `kimi --version` probe, optional
`~/.kimi-code/credentials/kimi-code.json` credential mount), making the CLI
available to the agent iteration harness like the claude/codex/gemini entries.
The entry deliberately omits `output_format`: the harness's `stream-json`
runner-event parsing is the Claude wire format. `README.md`'s agent-catalog
enumeration names the fourth entry; `.agents/skills` gains the generator-owned
`charly-coder--kimi` link (developer profile now 290 skills).

R10 (candy-config class, no committed bed composes the candy): proven by the
transient `check-kimi-candy-pod` disposable bed (a direct-composed fixture
image — fedora:43 + check-keepalive + kimi with `builder: {npm:
fedora.fedora-builder}` — removed after the run; per-candy beds are not kept):
`charly check run check-kimi-candy-pod` → PASS (12 steps, 525s, run calver
2026.199.2234) — fresh image build through the npm multistage, the candy's
three baked checks at build scope, the same checks plus a `kimi --version`
probe live in the deployed container, and the fresh-`update` rebuild leg, at
zero warnings. Two RCA'd gate discoveries along the way: ad-hoc `bundle add`
pod deploys never connect the out-of-process deploy-pod provider (providers
are scoped to authored deploy nodes), and the `add_candy` overlay emits an
empty npm builder ref for a base that declares no `builder:` — both noted as
candidates for a future core batch.

Composing `kimi` into the `hermes-full`/`openclaw-full` metalayers and the
coder boxes is the named `@github`-pin follow-up (`charly box reconcile`)
once the candy carries a published tag.

Assisted-by: Kimi Code Moonshot Kimi K3 (fully tested and validated)

# Changelog

## Reasonix configuration

- Added full permission allow-list to `reasonix.toml` (charly check run/live/box, charly status, gh pr merge, gh api POST) matching Claude Code and Kimi parity
- Created `.reasonix/settings.json` with 4 hooks wiring the shared `.claude/hooks/` gate scripts (pre-commit, pre-push, runtime-verification-reminder, end-of-turn-challenge)
- Registered all 25 Charly plugin packages from `plugins/*/.codex-plugin/plugin.json` as Reasonix plugin packages

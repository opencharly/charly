# 2026.201.0000 — thematic hygiene batch: sdk-interface-drift fixtures, dead code, stale references

Fix `go vet` failures in `candy/plugin-http` and `candy/plugin-port`: both
plugins' `fakeCC` test fixtures fell behind the `kit.CheckContext` interface
after the sdk control-plane work added `ResolveEndpoint`,
`ResolveGraphicsEndpoint`, `ResolveClusterContext`, and `ResolveImageLabel`.
Patched each fixture with the four missing no-op methods rather than
centralizing into a new shared test-support package — the two fixtures'
exercised behavior (HTTP canned responses vs. plain Exec/Mode) already
diverges meaningfully, and the missing surface is trivial boilerplate not
worth a new dependency for two callers.

Re-derived (fresh on current main, not merged from the stale
`feat/wave3-leaves` branch) two dead-code / duplication fixes: deleted the
zero-caller `ValidatePreemptibleAcrossDeploy` (re-confirmed via grep before
deleting), and folded `defaultK8sOutputDir` out of the stray
`bundle_add_cmd_k8s.go` into its sibling `k8s_generate.go`, deduping the
inline copy in `k8s_deploy_from_box.go`. The third duplicate the original
commit unified (`status_collect_k8s.go`'s `k8sTreeRoot`) has since relocated
out of `charly/` core into `candy/plugin-substrate` under an unrelated K5
status-subsystem move — the kernel/plugin boundary law forbids a plugin
importing charly core, so that copy can no longer be unified by call; fixed
its doc comment instead, which cited a filename that never matched even
before the relocation.

Swept for the stale `check-charly-selftest-pod` bed-name reference (a name
that resolves nowhere in current history) and found one hit — in the `sdk`
submodule's `deploykit/candy_steps.go`, not this repo, so it lands as its
own companion PR: opencharly/sdk#76 (`documentation reviewed`, comment-only,
zero gitlink bump — nothing in this batch consumes it). Corrected it to
name the bed that actually plays that role today, `check-charly-vm`.

Added `scratchpad/` to the superproject `.gitignore` (a per-session Claude
Code scratch directory, never version-controlled source).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)

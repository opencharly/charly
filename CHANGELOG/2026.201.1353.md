# 2026.201.1353 — Downloads cache ownership: charly-side coverage

Companion to opencharly/sdk#79 (`deploykit.EmitDownload` /
`TaskCacheMounts`): updates `charly/tasks_test.go` to the corrected mount
forms — a non-root stage gets the uid/gid-owned downloads cache
(`id=charly-tmp-downloads-uid1000`, `uid=/gid=`), a root stage keeps the
shared form — and adds `TestEmitDownload_DownloadsCacheRootShared` for the
root case. The sdk gitlink + `go.mod` require bump to the merged sdk commit
is appended to this PR once the producer leg lands (B2 order).

Assisted-by: Kimi Code (Moonshot K3; documentation reviewed)

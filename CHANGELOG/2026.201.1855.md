# Cutover A — BUILD + CHECK + P13-KERNEL integration

**This is a PLACEHOLDER CalVer** (`2026.201.1855`), written by the Cutover-A
integrator. The evaluating `pr-validator` renames this file to the merge-time
CalVer and rewrites this heading to match — never edit an already-merged
`CHANGELOG/<CalVer>.md` afterward.

**sdk-contract counterpart**: `github.com/opencharly/sdk` PR #81 has already
MERGED (`main` = `176fc1424fcfdc9058f78f90cb42470639d5b006`, tag
`v0.2026201.1934`, `CHANGELOG/2026.201.1934.md`). This charly PR's `sdk`
gitlink + `charly/go.mod` `require` are repointed to that merged commit —
producer→consumer landing order (B2), per `/charly-internals:git-workflow`.

## What this cutover is

Combines three independently-developed waves into one landing:

- **BUILD** (`feat/build-wave`, superproject `c0d0995`): dissolved 5 var-alias
  families in `tasks.go`/`format_config.go`/`labels.go` (`formatPhaseTemplate`,
  `taskKnownNames`/`taskUnresolvedRefs`/`resolveUserSpec`/…, `emitVarsEnv`/
  `emitMkdirBatch`/…, `ExtractMetadata`), repointing every call site directly
  at `sdk/buildkit`/`sdk/deploykit`. −418 core LOC.
- **CHECK** (stacked on `feat/build-wave`, `6479a69`): `step_topo.go` →
  `sdk/kit`, `check_feature_run.go` helper dissolution, the container/engine-
  resolve dedup (`resolveContainer`/`resolveSidecarContainer`/`resolveBoxName`/
  `ResolveBoxEngineForDeploy`/`FromMeta` → `deploykit`/`kit` directly, ~11 call
  sites), and the `bedVmDomains`/`acquireVmDomainLock` extraction. −188 core
  LOC.
- **P13-KERNEL** (`feat/p13-kernel`, `87fdf6f`): the deploy-dispatch kernel's
  hard-cutover exit — `bundle_add_cmd.go`'s tree-walk CONTROL FLOW moved to
  `candy/plugin-bundle` (`walk.go`), `BoxConfigSetupCmd`/`BoxConfigRemoveCmd`'s
  full orchestration moved to `candy/plugin-deploy-pod` (`config_setup.go`/
  `config_remove.go`, ~1,058+570+... LOC of new plugin code), `pod_lifecycle_resolve.go`
  reduced to a 93-line credential/registry-coupled residual, `layer_secrets.go`/
  `layer_artifacts.go`/`ephemeral_lifecycle.go` folded into `sdk/deploykit`, and
  the entire retired K4-C spike (`deploy_dispatch_seam.go`,
  `host_build_deploy_dispatch.go`, `deploy_venue_descriptor.go`, …) deleted
  outright now that the walk calls the new host seams directly. −3,089 core
  LOC (whole wave, `cc74415`→`87fdf6f`).

Plus the external main advance (`d2fd384`, consumer of sdk#79/`2e2ad01`): the
downloads-cache-mount curl-23 fix — non-root build stages get an uid/gid-owned
`/tmp/downloads` cache instead of a shared root-owned one.

## Conflict resolution (the integration merge itself)

Two merges landed the combination: `feat/build-wave` → `feat/p13-kernel`, then
`origin/main` → the result (picking up the curl-23 fix). Every conflict
resolved to the mover's side, per the rule the CHECK-wave handoff and the
P13-KERNEL handoff both carried forward: **when a dedup call-site the CHECK
wave repointed no longer exists (P13-KERNEL deleted or relocated the code),
the resolution is P13-KERNEL's side** — its move already superseded the
dedup, independently arriving at the same `deploykit.X`/`kit.X` qualification
in the new plugin home.

- **sdk gitlink**: resolved to the sdk-side integration commit (below) —
  `feat/build-wave`(sdk) ⋈ `feat/p13-kernel-sdk` ⋈ sdk `origin/main`, already
  merged and green before the superproject merge began.
- **`charly/config_image.go`**: P13-KERNEL's step-4 direction-flip already
  reduced this file from ~1,839 lines to a 224-line residual (Status/Mount/
  Unmount/Passwd + `injectEnvProvides`/`injectMCPProvides`); every line the
  CHECK-wave's dedup touched (`resolveBoxName`/`ResolveBoxEngineForDeploy`
  call sites) was inside the moved `BoxConfigSetupCmd` bulk. Resolved to
  P13-KERNEL's residual — confirmed `candy/plugin-deploy-pod` already calls
  `deploykit.ResolveBoxEngineForDeploy`/`kit.ResolveBoxName` directly.
- **`charly/pod_lifecycle_resolve.go`**: same pattern — P13-KERNEL's 93-line
  residual (the credential/registry-coupled leaves: `resolvePodEncEnsure`/
  `resolvePodEncUnmount`/`resolvePodTunnel`) contains none of the dedup's
  touched lines. Resolved to P13-KERNEL's side.
- **`charly/deploy_host_helpers.go`**: P13-KERNEL deleted this file wholesale
  (moved to `sdk/deploykit/deploy_host_helpers.go`, `RenderHostPackageCommand`).
  Verified the moved copy already carries the CHECK-wave's `formatPhaseTemplate`/
  `CacheMount` dedup fix independently (`buildkit.FormatPhaseTemplate`,
  `formatDef.CacheMount` — both already qualified in the sdk-side file).
  Resolved as a deletion.
- **`charly/host_build_pod_config_seams.go` + `charly/pod_lifecycle_resolve.go`
  (integration-only fixup, not a conflict)**: P13-KERNEL's own residual bodies
  still called the BARE `ExtractMetadata`/`ResolveBoxEngineForDeploy` names
  the CHECK-wave deleted from `labels.go`/`engine.go` (P13-KERNEL branched
  before that dissolution existed). Repointed to `deploykit.ExtractMetadata`/
  `deploykit.ResolveBoxEngineForDeploy`, matching every other call site.
- **`charly/tasks_test.go`** (conflict against `origin/main`'s d2fd384): the
  BUILD wave already relocated the whole `EmitDownload`-family test block to
  `sdk/deploykit/tasks_emit_test.go`; `origin/main`'s diff re-added it at its
  old pre-relocation location. Resolved to keep the relocation (no duplicate
  test block) — the curl-23 coverage lives in `sdk/deploykit/tasks_emit_test.go`
  (`TestEmitDownload_DownloadsCacheOwnership`, merged from `origin/main`'s sdk
  advance on the sdk side).
- **`sdk/deploykit/tasks_emit_test.go`** (add/add conflict, sdk side): the
  BUILD wave's wholesale test-suite relocation (448 lines) and `origin/main`'s
  new curl-23 test block (67 lines, `TestTaskRunsAsRoot` +
  `TestEmitDownload_DownloadsCacheOwnership`) both created this file
  independently from the same base. Combined both — kept every BUILD-wave
  test, added the two new ones, and fixed `TestEmitDownload_TarGz`'s now-stale
  assertion (the fixture is a non-root UID-1000 stage, so post-fix it gets the
  uid-scoped cache mount, not the old shared-form assertion).

## Zero-warnings sweep (surfaced by the integration's full battery)

P13-KERNEL's own commits never ran `golangci-lint` (its handoff's battery
listed only build/vet/test) — the integration battery is the first time these
surface. Per R1/R2, no "pre-existing" exemption: fixed in this same cutover.

- **10 `unused` + 1 `staticcheck` (charly module)**: every one is a host-side
  orphan left behind by `BoxConfigSetupCmd`/`BoxConfigRemoveCmd`'s move to
  `candy/plugin-deploy-pod` — each has a confirmed mirror/replacement in the
  plugin, verified before deletion (`secretDepNames`, `sidecarTemplatesOf`,
  `workspaceBindHost`, `tunnelConfigDir`/`tunnelConfigPath`,
  `cloudflareTunnelSetup`; `writePodConfigViaPlugin` superseded by the more
  generic `invokePodConfigOp`; `forceTTY` a pure orphan var). Updated 3 stale
  comments that named the deleted functions.
- **3 `unparam` (charly module)**: genuine false positives (a mock's
  func-type-required error return; two params load-bearing once each
  function's currently-test-only status changes) — `nolint:unparam` with a
  reason, matching house style.
- **2 `staticcheck` S1016 (`candy/plugin-deploy-pod`)**: two structurally
  identical CUE-generated single-field types — converted a struct literal to
  a type conversion at both call sites.
- **1 go.mod hygiene (`candy/plugin-bundle`)**: `gopkg.in/yaml.v3` was marked
  indirect despite a direct import in `config_cmd.go` — corrected.

## Verification (this integrator's battery)

- `go build` + `go vet`: clean in `charly` (workspace) and `sdk` (`GOWORK=off`
  standalone).
- `go test ./... -count=1`: green — `charly` (~95s), `sdk` (all 16 packages),
  `candy/plugin-bundle`, `candy/plugin-deploy-pod`, `candy/plugin-installstep`
  (each `GOWORK=off` standalone).
- `golangci-lint run ./...`: **0 issues** in `charly`, `sdk`,
  `plugin-bundle`, `plugin-deploy-pod`, `plugin-installstep`.
- `gofmt -l`: clean across every touched tree.
- `task build:binary`: clean.
- `charly box validate`: exit 0.
- `task cue:gen`: no drift (regeneration is a no-op).

## Measured axes (package-main methodology, vs `origin/main` `cc74415`)

| Axis | `cc74415` (baseline) | integrated HEAD | Δ |
|---|---|---|---|
| non-test `.go` files | 244 | 240 | **−4** |
| core LOC | 45,938 | 42,095 | **−3,843** |
| kit-import files (axis-A) | 137 | 137 | **0 (IMPORT-PURITY holds)** |
| `charly/*_aliases.go` files | 1 (`vmshared_aliases.go`) | 1 | **0 (ZERO-ALIASES holds)** |

The −3,843 combines BUILD (−418) + CHECK (−188) + P13-KERNEL (−3,089) +
this integrator's zero-warnings dead-code sweep (−167 net, 200 deleted / 33
added) — consistent with each wave's own independently-measured delta.

## STAY re-audit remainder (the 8 untraced files + 2 escalated items + deploy.go re-confirm)

Traced against the INTEGRATED tree's end-state graph, per rule 0 (the #118
`kernelFloor` allowlist, 65 files, is the ONLY legal STAY basis — none of the
11 items below are on it, so DEFAULT = MOVE absent call-chain evidence).
Registered here as Cutover-B / FLOOR-SLIM inventory — no moves executed in
this integration cutover (out of its bounded scope; each needs its own
properly-sized cutover per the Cutover Sizing Law).

| File | Verdict | Evidence |
|---|---|---|
| `checkspec.go` | **MOVE** (medium effort) | `VerbSpec`/`VerbCatalog` is a hand-written Go data table, not CUE-sourced (an SDD gap in its own right) and not floor-listed. Consumed BY floor mechanisms (`checkVerbBijection`, DI-injected into `deploykit.OpInContext`) but doesn't itself implement one — a capability, not the mechanism. |
| `check_kit_adapter.go` | **STAY** | `plugins_generated.go` (floor) directly calls `registerCompiledCheckVerb(...)`; `kitVerbAdapter` implements the floor `CheckVerbProvider` interface. Genuine M-clause plugin-loading registration glue. |
| `capabilities.go` | **MOVE** (small win) | `Capabilities = BoxMetadata` is alias residue; zero real floor-file consumers (prior grep hits were false positives — `pb.Capabilities`/comments); `CapabilitiesFromLabels` already thin-wraps the externalized `deploykit.ExtractMetadata`. |
| `cue_kind_check.go` / `cue_kind_k8s.go` | **MOVE** (small win, also dead code) | Of 9 `cue_kind_*.go` files only `cue_kind_box.go`/`cue_kind_candy.go` are floor-listed (bootstrap-critical B-clause). Both `check`/`k8s` registrations are currently DEAD in production (`cueKindDef("check")`/`("k8s")` never invoked outside a test) — an R1 dead-code finding too. |
| `status_substrate_host.go` | **MOVE** (small win) | Confirmed pure forward (`providerRegistry.resolve` → `invokeTyped`), zero status logic since K6 moved the real logic to `candy/plugin-substrate`. One of ~34 near-identical per-capability glue files; only the generic broker is floor-listed. |
| `oci_plugin.go` | **MOVE**, genuinely K1-blocked | Verified (not trusted): `generate.go`'s `resolveUserContext` → `invokeOciInspectUser`, reachable only via a `*Generator` built by `NewGenerator`, which calls `LoadConfig(dir)` directly — real loader coupling. Blocked until K1 lands. |
| `planrun_adapter.go` | **UNRESOLVED** | `hostVerbResolver` (defined here) is hard-required by the floor `CheckVerbProvider` interface's method signature and drives `providerRegistry.ResolveVerb` directly — genuine M-clause-leaning, but the file also holds `hostPlanGrammar`/`venueResolver` (R-item-leaning grammar glue). Mixed file; needs the same interface-redesign decision as `checkrun.go` below. |
| `vnc_helpers.go` | **MOVE** (small win once wired), prior assessment corrected | `resolveVNCPassword` → `ResolveCredential` (`credential_plugin.go`, not floor-listed) → the already-externalized `verb:credential`. NOT "real in-core secret material" as a prior pass claimed — an unwired forward, same shape as `oci_plugin.go`/`tunnel_plugin.go`. `candy/plugin-vnc` could call `verb:credential` directly via `InvokeProvider`. |
| `checkrun.go` | **UNRESOLVED**, prior claim confirmed | `CheckResult`/`Status` defined ONLY in `sdk/kit` (an RDD-proven `json:"-"` exception, `DeadlineExceeded`, keeps them out of the generated wire-spec layer) — 3 floor files (`provider_verb.go`, `verb_builtins.go`, `unified_targets.go`) consume them through this alias. Deleting it naively forces those floor files to import `sdk/kit` directly (an IMPORT-PURITY regression); the real fix is a `CheckVerbProvider` interface redesign, a cross-cutting decision this integration doesn't own. |
| `deploy.go` | **MOVE**, P13 finding re-confirmed | Re-grepped every consumer on the integrated tree (`check_members.go`, `check_cmd.go`, `host_build_check_run.go`, `host_build_pod_config_seams.go`, `config_secret_migration.go`, `filelock.go`, `deploy_state_host.go`, `host_build_check_bed.go`) — none on the floor list, none P13-KERNEL/bundle-walk related. CHECK/pod-config/deploy-state territory, mis-scoped into an earlier wave-partition pass; out of this cutover's cone. |

**Cross-cutting architectural flag**: `check_kit_adapter.go` (STAY), `planrun_adapter.go`, and `checkrun.go` (both UNRESOLVED) all trace to the SAME fork point — the floor's `CheckVerbProvider` interface (`provider_verb.go`) hard-codes `*hostVerbResolver` and `CheckResult` as its method signature, both defined in non-floor files. `check_kit_adapter.go` is cleanly STAY (it's the *registration* glue a floor file calls); `planrun_adapter.go`/`checkrun.go` are UNRESOLVED because they *define* the types the interface requires — moving them breaks the floor interface without a redesign (portable interface params, or an accepted tracked K-wave residue). This is the #118 reconciliation's decision to make, not this integration's.

## R10

Not run by this integrator — bed ownership belongs to the orchestrator per
the standing directive. Proposed roster (per the P13-KERNEL handoff, exercises
the deploy-dispatch kernel + the whole bundle add/del control flow):
`check-exampledeploy`, `check-sidecar-pod`, `check-group`,
`check-preempt-live-pod` (or an equivalent member-bearing bed), a vm-substrate
bed (the `node_only`/`RootVenueSSH` short-circuit + the `PostApply` nested
target:pod suppression fix).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)

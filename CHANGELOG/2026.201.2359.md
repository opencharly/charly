# 2026.201.2359 — Cutover B-1 — POD-LIFECYCLE-DISPATCH + BUNDLE-REMAINDER + KERNEL-B (PLACEHOLDER — re-stamped at merge)

**Placeholder CalVer.** The fresh `pr-validator` renames this file and
rewrites the heading to the merge-time stamp per the standard CalVer
mechanics; do not edit this file after it merges.

**sdk-contract counterpart**: `github.com/opencharly/sdk` `feat/cutover-b1`
(local commit `6ac7b46`, combining the three units' sdk legs — see its own
`CHANGELOG/2026.201.2355.md`, also a placeholder). Lands FIRST (B2 producer
order); this superproject leg's `sdk` gitlink is repointed to that merged
commit before landing.

## What this cutover is

Assembles three independently-developed Cutover-B units into ONE landing,
per the Cutover Sizing Law (each unit alone fell under the 2,000-LOC
minimum; combined they clear it):

### Unit 2 — pod-lifecycle-dispatch full port + enc/secrets/vm-client SDD cleanup

Completes the pod-lifecycle CLI dispatch family (start/stop/shell/logs/
update/service/remove) — all 7 verbs now own their full orchestration in
`candy/plugin-pod`, consolidating the former 7 per-verb `HostBuild` seams
into ONE (`host_build_pod_lifecycle_dispatch.go`) whose bodies are reduced
to the irreducible `ResolveTarget`/live-executor dispatch (start/stop/
shell/logs/update/service) or the arbiter-release bracket (remove) — the
proof-of-generalization for the P13-KERNEL deploy-node-dispatch precedent
at single-node granularity.

`charly remove`'s tunnel-stop now drives `verb:tunnel` directly via
`InvokeProvider` (mirroring pod start/stop's own pattern), deleting the dead
core tunnel-dispatch adapter (`tunnel_plugin.go`) whose last caller moved
plugin-side. Its credential axis reuses the existing
`pod-config-hook-secret-env` seam; its registry-resugar axis (deploy-entry
cleanup) gets a narrow new twin, `pod-config-clean-deploy-entry`, after a
demonstrated wire-shape mismatch against the existing `deploy-config-save`
seam (that seam persists an already-loaded whole config; `CleanDeployEntry`
loads+locks+mutates+saves internally — a fundamentally different
operation).

Fixes two real bugs surfaced during the port: (1) a placement-dependent
silent-degradation bug where sidecar-name resolution and per-deploy engine
resolution transitively read `deploykit.DeployStateHost`, a package var
populated only in charly-core's own `init()` — silently correct today only
because they're compiled into the same process as charly-core, silently
wrong the moment they'd run truly out-of-process; both rerouted through
their existing seams. **Registered for the next thematic batch (not fixed
here, dormant today):** `candy/plugin-bundle`'s `runBundleImport`/
`runBundleReset` carry the same latent bug — a repo-wide
`DeployStateHost` placement-safety audit is owed. (2) `InvokeProvider`-based
tunnel dispatch was silently swallowing a `verb:tunnel` business-level
failure — now decodes and surfaces the reply's `Error` field.

Relocates 27 genuinely portable encrypted-volume and podman-secret helper
functions from charly-core into `sdk/deploykit`, deleting 5 pre-existing
hand-duplicated copies across `candy/plugin-deploy-pod` and
`candy/plugin-doctor` (R3). Closes an SDD violation in the VM-resolution RPC
family: 6 new CUE-sourced wire types (`sdk/schema/vmclient.cue`) replace
hand-written twins in charly-core and `candy/plugin-vm`'s `DisplayEndpoint`.
Deletes a second pre-existing R3 duplicate (`kit.ContainerRunning`/
`ContainerImage` already existed in `sdk/kit`; charly-core carried its own
copies). Deletes dead code surfaced along the way: `android_spec.go`
(doc-comment-only, no code), `findPodSidecarQuadlets` (zero production
callers, superseded by `resolveSidecarNames`), and a third instance of the
"callers import kit directly" false-claim pattern (`sidecarConfigDir`,
duplicating `kit.SidecarConfigDir`).

Adds round-trip ADE coverage (`check-sidecar-pod`, 6 new plan steps) for
shell/logs/update/remove — zero prior bed coverage existed for these verbs.
**Disposable-bed R10 PASSED** on this unit's own frozen tree (run
2026.201.2328, exit 0, 12/12 steps) before integration.

### Unit 3+4 — BUNDLE-REMAINDER (libvirt schema sweep + reconcile/labels dissolution)

Deleted dead code confirmed via build/grep, not assumed: `libvirt.go`
(`CollectLibvirtSnippets`), `validate_ephemeral.go`'s
`ValidateImportedSource`/`ValidateEphemeralAcrossDeploy`, `graph_shim.go`'s
`ExpandCandy`/`BoxNeedsBuilder` (+ their now-redundant tests —
`sdk/deploykit` already covers the same logic byte-for-byte).

Removed the candy-level `libvirt:` field (schema + Go): zero live
consumers, the feature it served was already retired in the VM
hard-cutover. `#SchemaVersion` bumped (placeholder `2026.201.2300`,
re-stamped again at THIS merge), migrated via a new precisely-scoped
`stripCandyLibvirtField` hook — a blanket declarative `delete_key` was
unsafe because `libvirt` is also a live check-verb word that can coexist in
the same candy's `plan:`; the hook strips only the direct immediate
`libvirt:` child of a `candy:` value mapping, never descending into
`plan:`.

`charly box reconcile` externalized to `candy/plugin-box` (zero core
coupling — live-verified against a real 2-pin-mismatch fixture,
`--dry-run`, correct detection + report). `charly/labels.go`'s OCI-label
type aliases dissolved (8 `type X = spec.X` aliases); every caller
repoints to `spec.*` directly (ZERO-ALIASES).

Fixes a real pre-existing test-isolation bug the schema bump surfaced: a
test was leaking onto the operator's real per-host deploy-config overlay
(overrode charly's own `DeployConfigPath` var but not the
`CHARLY_DEPLOY_CONFIG` env var deploykit actually honors — confirmed live:
fixture keys from multiple tests were found in the operator's REAL
`~/.config/charly/charly.yml`). Generalized package-wide via
`charly/test_main_test.go`'s `TestMain`, which now sets a per-run
`XDG_CONFIG_HOME` temp dir as the default before any test runs (verified:
`~/.config/charly/charly.yml` mtime byte-identical across a full suite
run).

Registers several files as IOUs to already-tracked K-wave families (R-E8
seam-body family, K1-loader-blocked family, K5 command-dispersal,
FLOOR-SLIM axis-purity) rather than attempting unproven moves.

### Unit 5 — kernel-B addendum (k3s_post pure helpers)

Moves 3 pure functions + tests from `charly/k3s_post.go` to `sdk/deploykit`
(`deploy_forward.go`, `deploy_state.go`), reusing `vmshared.SplitPortForward`
per R3. Closes a zero-coverage gap with `TestFindBundleNode`. Fixes a stale
comment in `local_spec.go`. Also confirmed the P13-KERNEL wave (Cutover A)
already landed the R-E2 venue-scoped compile-selection enabler this unit was
originally scoped to build — the remaining `bundle_add_cmd.go`/
`bundle_compile_seam.go` STAY set (~1,617 LOC) is registered as a
candidate-floor set, adjudicated at FLOOR-SLIM/#118, not attempted here.

## Integration merge (this integrator's work)

Three uncommitted worktrees (`charly-wt-podlife`, `charly-wt-bundlerem`,
`charly-wt-kernelb`), all based on `origin/main@1c0b91b`, committed locally
at the then-honest `analysed on a live system` tier, then merged via
`git merge --no-ff` in sequence onto `feat/cutover-b1` (branched from
unit 2's commit, in the `charly-wt-podlife` worktree — reused rather than a
fresh worktree, since its sdk checkout already needed the same three-way
merge treatment).

**Conflicts (bundle-remainder merge only; kernel-B merge was clean)**:

- `charly/init_def_label_test.go`: content conflict. Unit 3+4 still carried
  the pre-unit-2 test bodies for `resolveEntrypointFromMeta`/
  `resolveInitDefFromMeta` (mechanically repointed to `spec.BoxMetadata`);
  unit 2 had already relocated those functions (and their test coverage) to
  `candy/plugin-pod/service_resolve.go`/`service_resolve_test.go`. Resolved
  to unit 2's side (confirmed both relocated tests exist and pass in
  `candy/plugin-pod`).
- `charly/start.go`, `charly/service.go`, `charly/remove_sidecar_test.go`:
  modify/delete conflicts — unit 2 deleted all three (fully relocated to
  `candy/plugin-pod`); unit 3+4 had only mechanically repointed a
  `BoxMetadata`→`spec.BoxMetadata` alias reference or a version-fixture
  literal inside them. Resolved to the deletion (verified the
  relocated/superseding code already carries the equivalent fix).

**Cross-unit integration defect found and fixed** (invisible to any single
unit's own worktree — exactly what a combined battery exists to catch):
`candy/plugin-box`'s `go.mod`/`go.sum` were stale against unit 2's new
`sdk/deploykit` dependency (`golang.org/x/term`, pulled in by
`secret_probe.go`) — unit 3+4 moved `reconcile.go` into `plugin-box` without
ever seeing unit 2's sdk changes. Fixed with `go mod tidy`.

**Zero-warnings sweep** (`golangci-lint run` on every touched module, first
combined run): 8 issues surfaced, all pre-existing on lines this cutover's
diff never touched (`candy/plugin-migrate/engine.go`: 3 unchecked
`fmt.Fprintf`, 2 trailing-period error strings; `reshape_compact.go`: 1
if/else-if collapsible to a tagged switch; `charly/test_main_test.go`: 2
unchecked `os.RemoveAll` in unit 3+4's own new isolation guard). Fixed in
this same cutover — no "pre-existing" exemption, per the precedent Cutover A
set for this exact program.

## Verification (this integrator's battery, on the merged tree)

- `task cue:gen` (sdk leg + superproject candy params): **complete no-op**
  on the merged tree.
- `task build:binary`: clean.
- `go build ./...` + `go vet ./...` (charly, workspace mode): clean.
- `GOWORK=off go mod verify` (charly): all modules verified.
- `go test ./...` (charly): **ok, ~96s, zero failures**.
- `GOWORK=off go build/test ./...`: clean/ok for every touched module —
  `sdk`, `candy/plugin-pod`, `candy/plugin-deploy-pod`, `candy/plugin-doctor`,
  `candy/plugin-tunnel`, `candy/plugin-vm`, `candy/plugin-box`,
  `candy/plugin-migrate`.
- `golangci-lint run ./...`: **0 issues** across charly + every touched
  module (post-fix).
- `gofmt -l`: clean across `charly`, `candy`, `sdk`.
- `charly box validate`: exit 0.
- R5 grep self-test: every deleted identifier's remaining hits are either
  the relocated implementation in its new plugin home, or an explanatory
  "X DELETED (moved to Y)" comment (the accepted in-cutover documentation
  pattern established in Cutover A) — no stale references.

## Measured axes (package-main methodology, vs `origin/main` `1c0b91b`)

| Axis | `1c0b91b` (baseline, freshly re-measured) | integrated HEAD | Δ |
|---|---|---|---|
| non-test `.go` files (charly/, top-level) | 240 | 226 | **−14** |
| core LOC | 42,113 | 40,243 | **−1,870** |
| kit-import files (axis-A) | 137 | 133 | **−4 (IMPORT-PURITY improves)** |
| `charly/*_aliases.go` files | 1 (`vmshared_aliases.go`) | 1 | **0 (ZERO-ALIASES holds — floor-candidate, untouched this cutover)** |

**Baseline-measurement note**: the program ledger's running "BASELINE now"
entry for `1c0b91b` recorded core LOC as 42,378; this integrator
independently re-measured `origin/main@1c0b91b` twice (via `git ls-tree` +
via a `git archive` extraction, both non-recursive `charly/*.go` sums,
excluding `_test.go`) and got **42,113** both times — matching what BOTH
unit 3+4's AND unit 5's own handoffs independently measured at their branch
start. Orchestrator-confirmed (independent re-derivation): the 42,378/40,508
pair and this report's 42,113/40,243 pair differ by a constant ~265-line
counting-method offset, not a real discrepancy — the DELTAS agree exactly
(−1,870 either way). This integration report's own figures use the
twice-verified 42,113/40,243 pair throughout.

**−1,870 vs the naive per-unit sum of −2,134** (unit2 −1,535 + unit3+4 −529
+ unit5 −70): the ~264-LOC (12.4%) variance is explained by file-overlap
resolution during the merge (unit 2 deleted `start.go`/`service.go`/
`remove_sidecar_test.go` wholesale; unit 3+4's independent
mechanical-alias-repoint diffs on those same files were superseded rather
than additively subtracted) — well under the ledger's 20% variance
threshold for a blocking incident.

## R10 — acceptance roster (orchestrator-run, on this frozen integrated tree)

Five checks, all launched CONCURRENTLY against the frozen `fa4bd91` tree, all
GREEN, zero warnings — this is the R10 gate on the FINAL code:

- `check-sidecar-pod`: PASS (steps=12), exit 0 — run 2026.202.0005,
  check-live 2m46.9s with the full pod-lifecycle round-trip coverage
  (shell/logs/update/remove) executed under concurrent load.
- `check-exampledeploy`: PASS (steps=6), exit 0.
- `check-group`: PASS (steps=7), exit 0 — bundle/reconcile + libvirt
  schema-field removal + labels dissolution.
- `check-k3s-vm`: PASS (steps=8), exit 0 — exercises unit 5's
  `deploykit.FindBundleNode`/port-forward helpers live.
- Migration entry live-proven by direct probe: a real pre-sweep
  qemu-guest-agent fixture at schema `2026.186.2323` migrated via
  `applied strip-candy-libvirt-field (schema 2026.201.2300)` — the
  candy-level `libvirt:` block stripped, `charly box validate` clean,
  second run idempotent no-op.

(`preempt-regression`, proposed in an earlier draft of this roster, does not
exist as a worktree-valid bed — the only preempt bed, `check-preempt-local`,
is HOST-LOCAL and resolves the host binary; the arbiter bracket is
behaviorally unchanged and already exercised end-to-end by
`check-sidecar-pod`'s remove/release round-trip.)

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)

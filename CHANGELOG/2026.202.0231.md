# 2026.202.0231 — box/<distro> schema re-stamp: gitlink bump + go.work.sum checksum commit

**Incident:** `sdk`'s `#SchemaVersion` advanced to `2026.202.0105` (the
`strip-candy-libvirt-field` migration-table entry) while all five `box/<distro>`
submodules stayed pinned at the prior schema HEAD `2026.186.2323`. Every
`LoadUnified` of a box tree hard-errored, and `charly/.`'s
`TestCandySourceDirs_OverrideAnchorsRemoteApk` went red on `main` for the same
reason.

**Fix, five independently-landed legs (each `charly migrate` run standalone in its
own repo — a pure version-stamp bump, `stripCandyLibvirtField` a no-op in every
box since none carries a candy-level `libvirt:` field):**

- distro-arch#8 → `b4c9259`, tag `v2026.202.0201`
- distro-cachyos#7 → `f6cd24d`, tag `v2026.202.0204`
- distro-debian#5 → `a966782`, tag `v2026.202.0206`
- distro-fedora#5 → `bc00442`, tag `v2026.202.0209`
- distro-ubuntu#5 → `7fe93be`, tag `v2026.202.0211`

This superproject leg bumps all five `box/<distro>` gitlinks to their merged
`main` HEADs above, and folds in a pre-existing, unrelated `go.work.sum` drift
(a `github.com/bits-and-blooms/bitset` checksum line `go build` wants to add to
the tracked workspace checksum file) via `task build:binary`'s sanctioned
review-and-commit remedy path.

Evidence: `go test -run TestCandySourceDirs_OverrideAnchorsRemoteApk ./charly/...`
PASS; full `go test ./...` (charly module) PASS; `charly box validate` exit 0,
zero warnings; `task build:binary` clean (no go.work.sum guard trip).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; analysed on a live system)

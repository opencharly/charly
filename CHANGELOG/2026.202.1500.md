# 2026.202.1500 — FLOOR-SLIM Wave 1: mechanical batch + Config restructure + check envelope + installstep envelope

Placeholder CalVer — finalized by the fresh `pr-validator` at merge.

## What this cutover is

The superproject leg of the FLOOR-SLIM Wave-1 super batch (core-minimization
program): four implementation units consolidated on `feat/floorslim-wave1`,
landed with the paired sdk-contract leg (`sdk` gitlink bump) and the plugins
docs sweep (`plugins` gitlink bump). Net: `charly/` core non-test LOC
**38,944 → 36,935 (−2,009)**; axis-A kit-import files 130 → 128; alias files
still 1 (`vmshared_aliases.go`, shrunk by 17 names).

### Unit: mechanical batch (dead code, graph_shim, proclifecycle, axis-A)

- `distro.go`: dead `InstallHint()` free func deleted (`installHints` var
  stays — live via `host_build_hostprobe.go`).
- `tunnel.go`: dead `ValidPublicPorts` deleted (its own header falsely claimed
  it was consulted — fixed); the 5 pure resolution fns moved to
  `sdk/deploykit/tunnel_resolve.go`; all charly callers repointed DIRECTLY
  (no declaration-form aliases — NO-NEW-ALIASES).
- `graph_shim.go` **DELETED** — all 8 callers repointed to
  `deploykit.ResolveCandyOrder`/`ResolveBoxOrder`/`ResolveBoxLevels`/
  `CandyProvidedByBox`/`BoxDirectDeps` directly; unique test coverage
  relocated to sdk.
- `vmshared_aliases.go`: 12 zero-usage names + the 5 process-lifecycle names
  deleted; the latter's implementations carved from `sdk/vmshared/cleanup.go`
  into the new stdlib-only leaf **`sdk/proclifecycle`** (schemaconcat
  precedent), with `main.go` and every plugin/sdk consumer importing the leaf
  directly. The genuinely-VM-ish alias remainder stays as tracked K-wave
  inventory.
- Axis-A promotions: `materialize.go`/`node_build.go`/`cue_normalize.go`/
  `plugin_provider_common.go`/`plugin_inproc.go` drop their kit imports via
  spec-promoted pure helpers (`spec/node_helpers.go`, `spec.ScopeFromName`,
  `spec.RefsDownloader`). Three candidates verified architecturally unsuitable
  and NOT moved (network I/O, a documented `spec.CalVer = string` collision,
  kit-private state).
- K1: `validateDeploymentTree`/`validateDeployRequiresBox` family →
  `sdk/spec/deploy_tree_validate.go` (pure over `spec.BundleNode`). The rest
  of the K1-proper map slice DISSOLVED on verification: `canonicalRef` is
  already correctly threaded into candy/plugin-loader's WalkSeams;
  `ProjectConfig`/`ProjectCandies` are `*UnifiedFile` methods; the remainder
  is design-register material, not mechanical-batch material.

### Unit: bundle-dispatch + the Config restructure

- `vm_deploy_state.go`: 5 pure fns → sdk (`vmshared.SplitVmAddress`/
  `VmNameFromDeployName`; `deploykit.ResolveVmSshPort`/
  `PruneStaleVmDottedTwin`/`VmDeployEntryKeys` — split by import direction);
  only the pluginPrimaries-coupled write path stays.
- **`Config` is now `spec.Config`** (`type Config = spec.Config`): spec-typed
  methods ride the type to `sdk/spec`; every buildkit-typed resolver became a
  `sdk/buildkit` free function taking `*spec.Config`; `candy_chain.go`
  DELETED (→ `deploykit.BoxCandyChain`/`BoxDirectCandies`); `namespace.go`
  reduced to the registry-coupled `resolveLocalRefFor` residue; `config.go`
  keeps only `LoadConfig`/`LoadConfigRaw` + thin `ResolveBox`/`ResolveAllBox`
  wrappers that fill the ONE `LoadBuildConfigForBox` fallback before
  delegating (buildkit itself has zero LoadUnified coupling). `ResolveOpts`
  stays a flat charly struct with a `toBuildkitOpts()` projection
  (Go's promoted-field keyed-literal restriction). ~27 consumer files
  repointed mechanically.
- The box/candy-selection plugin-widening and `k3s_post.go` relocation
  correctly STOPPED at the design boundary (host-only registry dispatch in
  `preresolveBuilderContexts`; `LoadBuildConfigForBox` in
  `preresolveActiveInitInto`) — registered for the venue-scoped-executor-
  session design phase, not silently dropped.

### Unit: check envelope + family

`spec.CheckResult` (CUE-sourced) replaces `checkrun.go`'s package-main
alias block — DELETED; all registry-coupled floor files reference
`spec.CheckResult`/`spec.Status*` directly with ZERO new kit imports.
**Breaking (`!`) `--format json`/TAP key rename** (`Op`→`op`, `Verb`→`verb`,
`Status`→`status`, `Message`→`message`, `Elapsed`→`elapsed`) — see the sdk
CHANGELOG for the table and the golden byte-parity proof. Also:
`capabilities.go` machinery → `deploykit` (the `Capabilities` alias deleted);
`checkspec.go`'s `VerbCatalog` data → `spec/verb_context.go`;
`vnc_helpers.go` folded into `check_endpoint_resolve.go`;
`description_collect.go` → `deploykit.CollectDescriptions`.
`check_kit_adapter.go`/`planrun_adapter.go`/`checkrun_act.go`-bulk stay —
core registry-adapter glue behind the in-proc perf gate
(`TestPerfGate_BuiltinVerbsSkipEnvelope`) — registered as the check-dispatch
seam endgame design item; `host_build_check_run.go` business logic is
K1-blocked (`LoadUnified`), same class as `local_spec.go`.

### Unit: plugin-installstep envelope adoption

candy/plugin-installstep's 4 host-coupled step words (`system-packages`/
`builder`/`local-pkg-install`/`op`) no longer round-trip
`HostBuild("step-emit")` per render — the plugin fetches the
"resolved-project" envelope ONCE, builds its own `deploykit.Generator` via the
production-proven `NewRenderGeneratorFromProject`, and emits directly.
`step_emit_hostbuild.go` **385 → 137 LOC** (generic machinery + the
`oci-emit-step` word only); the dead `Generator.emitTasks`/
`.buildStageContext` shims deleted. The generic "resolved-project" seam now
populates `ExternalizedBuilders` (parity with build-prep/overlay).
Parity proof: `plugin_installstep_envelope_parity_test.go` — 5 fixtures
(incl. plugin:-verb candies, the case the scoping spike could not compare),
byte-identical EmitTasks/BuildStageContext/BakedMetadata over a real JSON wire
round-trip.

## Verification (R10 — runtime class, concurrent roster)

Frozen final tree `f7bcd9a` (zero transitional code). SIX disposable beds run
CONCURRENTLY, ALL PASS exit 0: check-sidecar-pod (17 steps) ·
check-charly-vm (8; the packaged/localpkg path, warning-free) ·
check-k8s-deploy (10) · check-group (7) · check-substrate (10) ·
check-builder-vm (10; exercises the envelope-driven builder emit live).
Plus a live `--format json` probe (fresh image build + baked-plan run) showing
the new snake_case keys. `go build`/`vet`/`test` + `golangci-lint` clean in
charly, sdk (standalone), and the touched plugin modules;
`sdk/spec` imports neither buildkit nor deploykit (verified invariant);
R5 grep self-tests clean on every deleted identifier.

Known environmental note (evidence-documented, pre-existing): the
check-k8s-deploy guest surfaced `plugin-secrets` v1/v2 protocol-version
warnings caused by the HOST's stale `opencharly-git` 2026.196 package
(pre-dating the 2026-07-19 ProtocolVersion 1→2 landing, 76df6c0 — untouched
by this wave). A fresh package was built from committed main
(`dist/opencharly-git-2026.202.1140-1`); installation requires the operator
(root).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)

# 2026.202.1500 — FLOOR-SLIM Unit 4: CheckResult wire envelope + capabilities/VerbCatalog moves

Placeholder CalVer — finalized by the fresh `pr-validator` at merge.

## What this cutover is

The superproject half of FLOOR-SLIM Unit 4 (CHECK-ENVELOPE + FAMILY), landed
alongside the sdk-contract leg (`sdk` submodule bump). Corrects the FLOOR-SLIM
scoping map's Cluster-2 estimate for this unit after independent call-chain
verification of every assigned file — most of that map's "MOVE" verdicts do
not survive direct code inspection.

### The CheckResult wire envelope (the operator-approved embed split, ruling R-FS2 + (A))

`spec.CheckResult` (CUE-sourced in the `sdk` submodule) replaces the former
package-main `CheckStatus`/`CheckResult`/`TestPass`/`TestFail`/`TestSkip`
aliases in `charly/checkrun.go` — DELETED entirely. Every registry-coupled
floor file that constructs or reads a check verdict now references
`spec.CheckResult`/`spec.StatusPass`/`spec.StatusFail`/`spec.StatusSkip`
directly: `provider.go`, `provider_verb.go` (the `CheckVerbProvider`
interface), `verb_builtins.go`, `unified_targets.go`, `provider_checkenv.go`,
`check_kit_adapter.go`, `planrun_adapter.go` (`hostVerbResolver` — now
satisfies `kit.VerbResolver`'s `spec.CheckResult`-returning signature),
`checkrun_act.go`, and `checkrun.go`'s own `passf`/`failf`/`skipf` helpers.
Zero new `sdk/kit` import lands on the floor.

**Breaking wire-format change** for `charly check box/live/run --format
json`/TAP consumers: the JSON keys rename `Op`→`op`, `Verb`→`verb`,
`Status`→`status`, `Message`→`message`, `Elapsed`→`elapsed` (a deliberate
consistency fix — the former hand-written type carried no json tag on these
fields, so encoding/json defaulted to the bare capitalized Go field name).
See the `sdk` CHANGELOG entry for the full key-mapping table and the golden
parity-test proof that this is the ONLY change (same field set, same values,
same omission behavior).

### Other confirmed-safe moves (this cutover)

- `capabilities.go`'s runtime-contract machinery →
  `sdk/deploykit/capabilities.go`; the `Capabilities = spec.BoxMetadata` alias
  (ZERO-ALIASES residue) deleted, its 3 real consumers repointed to
  `spec.BoxMetadata` / `deploykit.CapabilitiesFromLabels` directly.
- `checkspec.go`'s `VerbSpec`/`VerbCatalog`/`installVerbs` (pure per-verb
  metadata data) → `sdk/spec/verb_context.go`. The registry-coupled functions
  that consult it (`opActsInBuildDeploy`/`opEffectiveDo`/
  `opEffectiveContexts`/`opInContext`) stay in `charly/checkspec.go` — they
  dispatch through `providerRegistry.ResolveVerb`/`ResolveStep`, a
  package-main-private mechanism no separate module can reach.
- `checkrun_act.go`'s `shQuoteArg` (a 1-line `kit.ShQuoteArg` re-export)
  deleted; its 2 call sites in `deploy_target_external.go` repointed directly.
- `vnc_helpers.go` folded into `check_endpoint_resolve.go` (its sole caller's
  file) — an in-core consolidation, not a plugin move (the out-of-process
  plugin cannot reach the credential store).
- `description_collect.go` (`CollectDescriptions`/`bakeableSteps`) →
  `sdk/deploykit/description_collect.go` (`CollectDescriptions`/
  `BakeableSteps`), landed AFTER rebasing onto `feat/floorslim-bundle`'s
  `Config`→`spec.Config` alias + `deploykit.BoxCandyChain` repoint.
  **Destination correction from the original plan**: NOT `sdk/spec` — the
  file calls `deploykit.BoxCandyChain`, and `spec` must never import
  `deploykit` (the dependency direction is the other way). `sdk/deploykit` is
  the right home per "who actually consumes it": all 3 real callers
  (`render_baked_metadata.go`'s OCI-label bake, `resolved_project_host.go`'s
  "resolved-project" HostBuild seam, `check_cmd.go`'s live-check gather
  engine) are STILL core-resident, K1/K3-blocked files that already import
  `deploykit` freely — `deploykit.CollectDescriptions(...)` is a normal
  same-pattern kit-consumption call needing zero new seam, whereas a plugin
  destination would force those 3 still-core callers to reach it over
  InvokeProvider/HostBuild for no functional reason (their own K1/K3
  unmigrated status is the actual blocker, not this file's shape).
  `stampStepIntentDo` (100% pure over `spec.Step`) moved alongside it,
  deleting the now-dead duplicate definition left behind in
  `charly/checkspec.go` (its sole caller moved away — an R1/R3 catch during
  this move, not a pre-existing issue). `OpInContext` — the EXISTING
  `deploykit` DI-hook charly injects at init (`layers.go`:
  `deploykit.OpInContext = opInContext`) — is called directly, no new
  injection needed.

### Confirmed BLOCKED, not silently dropped (named exits tracked separately)

`check_kit_adapter.go`, `planrun_adapter.go`, and `checkrun_act.go`'s bulk are
core registry-adapter glue (package-main-private types, direct
`providerRegistry` dispatch — the perf-critical typed fast path
`TestPerfGate_BuiltinVerbsSkipEnvelope` depends on staying in-process) —
registered as the **check-dispatch seam endgame** design item, adjudicated in
a later design phase, not this unit's scope.
`host_build_check_run.go`'s business logic is K1-blocked (direct `LoadUnified`
calls, same class as `local_spec.go`). `agent_config.go` rides its blocked
caller. `cue_kind_check.go`/`cue_kind_k8s.go` are core kind-recognition
D-clause data (`registerCueKind`), pattern-consistent with all 9 registered
kinds.

`charly/` core non-test LOC: 38,944 → 38,643 after the CheckResult-alias
deletion, further reduced by the `description_collect.go` move (this cutover
is now COMPLETE — no further holds).

## Verification

`task build:binary` clean; `go vet ./...` clean; `go test ./...` clean
(~110s) in `charly/`; `golangci-lint run ./...` 0 issues in `charly/` and
`candy/plugin-check` (both workspace-mode and `GOWORK=off` standalone). Merged
`origin/feat/floorslim-bundle` (fs-bundle's `Config`→`spec.Config` +
`boxCandyChain`→`deploykit.BoxCandyChain` repoint) cleanly — the `sdk` gitlink
resolved descendant-wins (this branch's sdk HEAD already contains
fs-bundle's sdk commit via an earlier merge).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; syntax check only)

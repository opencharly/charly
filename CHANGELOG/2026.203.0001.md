# 2026.203.0001 — FINAL/K5 unit 6 (6a+6b consolidated): F6 preresolve + ephemeral cross-substrate move + credential/bed-session R3 consolidation + build/pull dispersal

**PLACEHOLDER CalVer/filename — the fresh `pr-validator` overwrites the filename and
rewrites this heading to the real `YYYY.DDD.HHMM` at merge.** This entry consolidates
the two independently-scoped FINAL/K5 units — 6a (F6 preresolve generalization +
ephemeral lifecycle cross-substrate move + K3-envelope + build/pull command-dispersal)
and 6b (credential + bed-session R3 consolidation, InvokeProvider enabler built +
bed-verified + reverted, preempt/arbiter re-scoping) — landed as ONE atomic cutover
per the Cutover Sizing Law's urgent/row-2 carve-out below. Both units' full narratives
follow verbatim, unit 6a first (the larger, foundational delta both units build on),
then unit 6b (the consolidation riding unit 6a's seams).

## THE UNIT-6 SIZING DECISION (verbatim from the orchestrator's ledger, for the operator's review)

Unit-6 combined reduction ≈ −1,100 (6a −485 + 6b −634, minus overlap) — UNDER the
2,000 floor. Landing justified under the sizing law's URGENT/row-2 carve-out,
explicitly: the landing carries (a) the packaged-build MAIN REGRESSION fix
(check-charly-vm/localpkg broken on main since B-1 — plugin-feature go.sum), (b) the
repair of the entirely-nonfunctional ephemeral feature (6 product bugs incl. a panic +
double-allocation), (c) 14 RCA'd fixes with new coverage, in a >5k-line coherent diff.
Padding it with FLOOR-SLIM fragments to cross 2k would re-open a ready landing —
rejected.

---

# Unit 6a narrative

## What this cutover is

Unit 6a of the FINAL/K5 cutover: moves the F6 substrate PRERESOLVE/lifecycle-prepare
bodies (`builder_preresolve.go` excluded per scoping — Q1) — `android_deploy_preresolve.go`
and `k8s_deploy_preresolve.go` onto the existing generic `deploy_preresolve.go`
mechanism, and `vm_lifecycle_preresolve.go`'s `vmLifecyclePrepare` (its own, narrower
`lifecyclePrepareHook` mechanism — vm is `Lifecycle:true`, not `Preresolve:true`) onto
`candy/plugin-deploy-vm`'s own already-owned `OpPrepareVenue` — relocates the android
deploy command body and the k8s-gen subsystem's callers, extends the config-persist
HostBuild family, and moves the 428-line `ephemeral_lifecycle.go` cross-substrate
ephemeral lifecycle (systemd TTL transient timer, parent/child nesting, TTL clipping,
charly.yml persistence) into `candy/plugin-bundle` — the substrate-neutral deploy-
lifecycle owner every substrate's Add path already calls through
`deploy_add_shared.go`'s `registerEphemeralIfMarked`. NO substrate-specific
preresolve/lifecycle-prepare body remains in `charly/` core — all three (android, k8s,
vm) now resolve their own LoadUnified-coupled data via generic HostBuild seams from
their own plugins.

### The F6 mechanism gap (blocking every body move)

`deploy_preresolve.go`'s `wireDeployPreresolver` called the plugin's `Invoke` with no
reverse-channel broker wired — every F6 body move was blocked on this until fixed:
`gp.InvokeWithExecutor(ctx, op, kit.ShellExecutor{}, buildEngineContext{}, false, nil)`
replaces the plain `gp.Invoke`, the established "I need HostBuild access but no live
venue" idiom (matching `bundle_compile_seam.go`'s pattern). This is a genuine mechanism
defect, not scoped to any one substrate — every future F6 preresolve body benefits.

### The generic `deploy-entity-resolve` seam

Rather than one HostBuild kind per substrate, `charly/host_build_deploy_entity_resolve.go`
serves ONE discriminated request (`kind` ∈ `""`/`"deploy"`/`"bundle"` for a deploy-tree
node lookup by name, `"k8s"`/`"android"`/`"vm"` for a referenced kind entity by name,
returned as the WHOLE RESOLVED envelope) — a new consumer needs no new wire shape, only
a new `case` in the host handler. Cross-unit design steer from 6b (orchestrator-ruled):
the host does the resolve and returns the whole resolved envelope, not a partial view.

### Two real, pre-existing bugs found and fixed (live-bed-caught, never before exercised)

1. **Blank-fallback-node → kind-misdiscrimination.** `persistEphemeralRuntime`'s `!ok`
   fallback wrote a bare `spec.BundleNode{}` (empty `Target`) on first ephemeral
   registration → on reload, `bundleDiscForEntity` discriminated the entry as `"group"`
   → `#GroupInput`'s closed schema rejected the leftover `vm_state` field → hard load
   failure on every subsequent per-host-overlay read. Fixed by seeding `Target`/`From`
   from the authored node (`ephemeralFallbackNode`). Root cause predates this cutover —
   no ephemeral fixture had ever exercised first-registration before this bed.
2. **Dotted `dc.Bundle` map key rejected.** A nested ephemeral deploy's dotted CLI
   address (e.g. `check-sidecar-pod.check-sidecar-pod-ephvm`) is illegal as a literal
   `dc.Bundle` map key (`charly/unified.go`'s `validateDeploymentName` — dots are
   reserved for dotted-path CLI addressing, never a literal key). Fixed by keying the
   whole ephemeral family through `"vm:" + vmshared.VmDomainIdentity(deployName)` — the
   SAME scheme `charly/vm_deploy_state.go`'s `saveVmDeployState` already uses (via
   `candy/plugin-vm`'s `hostConfigPersist`). A new `EphemeralRuntime.DeployAddress`
   field (`sdk/spec/hand_state_types.go`, plus a matching validation-only `ephemeral?:`
   sub-def added to `#VmDeployState` in `sdk/schema/deploy.cue` for SDD lockstep)
   persists the real CLI-addressable identity separately from the sanitized map key,
   since `VmDomainIdentity`'s "." → "-" transform is lossy — `teardownChildrenRec`'s
   recursive `charly bundle del` call needs the original address back.

### Two check-assertion bugs found and fixed in this cutover's own bed (test authoring,
### not product code — but each masked whether the mechanism truly worked)

1. **`bundle show <name>`'s single-arg lookup can't find vm state.** `deploykit.DeployKey`
   is a bare pass-through with no `"vm:"` prefix; every vm deploy's per-host state is
   persisted under a `"vm:"`-prefixed, sanitized key (universal, pre-existing, unrelated
   to this cutover — see the registered-batch section below). This cutover's own check
   step was the first `bundle show <vm-name>` usage in the whole repo, so it hit the gap
   first. Fixed by asserting via the whole-dump `bundle show` form (no positional arg),
   anchored on the entry's unique `deploy_address` marker so the step can't pass
   vacuously.
2. **The systemd-timer grep pattern used the leaf deploy name, not the full dotted
   path.** `registerTransientTimer` names its unit from
   `deploykit.SanitizeUnitName(deployName)` where `deployName` is the FULL DOTTED
   ADDRESS for a nested member (e.g. `check-sidecar-pod.check-sidecar-pod-ephvm` →
   `check-sidecar-pod-check-sidecar-pod-ephvm`), not the leaf name alone — the check
   assertion's original grep pattern (`charly-bundle-del-check-sidecar-pod-ephvm`)
   could never match. Found by treating a FAIL verdict as a genuine RCA rather than
   assuming the mechanism itself was broken: a live-system check (leftover systemd
   timers surviving from the failing run — the isolated per-run overlay tears down at
   run-end, but a systemd unit does not) proved registration executed correctly, so the
   bug was isolated to the assertion. `ephemeralTimerUnitPrefix` (ephemeral.go) is the
   naming formula, pulled out as its own pure function purely for testability
   (`TestEphemeralTimerUnitPrefix_UsesFullDottedPath`, ephemeral_test.go) — the
   `systemd-run` call itself isn't unit-testable standalone.

### RCA #5 — a genuine THIRD real product bug, found by an orchestrator-driven LIVE PROBE
### after two more bed FAILs (bed 4/5) that traced-clean at the mechanism level

The register conjunct's remaining failures (after both check-assertion bugs above were
fixed) resisted static/mechanism-level tracing entirely — an isolated unit test proved the
"cli" HostBuild fork mechanism correctly propagates `CHARLY_DEPLOY_CONFIG` env inheritance
end-to-end, and both "deploy add" and "check live" share the IDENTICAL dispatch path
(`bedCliCombined`/`bedCliReq` → `HostBuild("cli")` → `hostBuildCli`'s `runCliSubcommand`),
ruling out an env-propagation asymmetry. Team-lead ran a direct, orchestrator-driven live
probe (`bundle add` → inspect the surviving overlay → `bundle show` → timers → explicit
`bundle del` → cleared assertions) and found the real root cause:

1. **`panic: assignment to entry in nil map` inside `persistEphemeralRuntime`.** On a
   genuinely FRESH per-host overlay, `loadBundleConfig()` can return a non-nil
   `*deploykit.BundleConfig` whose `Bundle` field is itself `nil` — a shape the prior guard
   (`if dc == nil`) never covered. `persistEphemeralRuntime`'s `!ok` branch fabricates a
   fresh entry and falls through to a bare `dc.Bundle[key] = node` write, which panics on a
   nil map — on EVERY fresh-overlay registration, i.e. every bed run's first-ever
   registration. `clearEphemeralRuntime`/`bumpParentChildRefcount` were independently
   re-verified safe (both return/continue before ever reaching a write on a nil-map miss —
   no fix needed there). Fixed by `ensureEphemeralBundleConfig` (ephemeral.go), pulled out
   as its own pure function for testability
   (`TestEnsureEphemeralBundleConfig_NilMapPanic`).
2. **The panic was SILENTLY SWALLOWED somewhere upstream — every bed run reported the
   overall `charly bundle add` as PASS with a panicking plugin op inside it.** No
   `recover()` existed anywhere in the dispatch chain (verified by a repo-wide grep), so
   the exact swallow point was never conclusively located by static tracing alone — but
   regardless of where it happened, the fix is the same: a panic inside
   `OpEphemeralRegister`/`OpEphemeralTeardown` must never silently vanish. Added
   `recoverEphemeralOpPanic` (command.go) at the OUTERMOST plugin-side entry point
   (`runEphemeralRegister`/`runEphemeralTeardown`), converting any recovered panic into an
   error tagged with the new `sdk.EphemeralPanicMarker` — a STRING marker (not a typed Go
   error) so the classification survives the `Provider.Invoke` wire boundary dual-placement
   demands stay portable, even though `command:bundle` is compiled-in today.
   `registerEphemeralIfMarked` (deploy_add_shared.go) now distinguishes this PANIC class
   (via `isEphemeralPanicError`, pulled out for testability —
   `TestIsEphemeralPanicError`) from an ORDINARY registration error (e.g. systemd-run
   missing, which stays a soft, logged warning as before): a panic-class error now FAILS
   the whole vm Add, tested via `TestRecoverEphemeralOpPanic` for the recover conversion
   itself.
3. **The correct `bundle del` address form for a nested ephemeral member is
   `vm:check-sidecar-pod.check-sidecar-pod-ephvm`** (documented explicitly in the probe's
   own `bundle del` error message when it was first invoked with the unprefixed form). Not
   reflected in this cutover's own code/tests (the bed no longer authors an explicit del at
   all — see below) — noted here for the probe's own re-run.

**Crossroads, RULED AND APPLIED: the explicit teardown round-trip cannot be proven from
WITHIN `charly check live` at all.** A separate RCA (#5's first half, before the panic was
found) traced `ephemeral-teardown-roundtrip`'s persistent failure to a structural cause,
independent of the panic: `charly check live` is DOCUMENTED, INTENTIONAL `VerifyOnly` — it
runs ONLY `check:`/`agent-check:` steps, NEVER `run:`/`agent-run:` (`charly/check_cmd.go`:
`// charly check live: idempotent check:/agent-check: only`, matching the project's ADE
model). The bed's `run: bundle del ...` step between the two `check:` steps therefore NEVER
EXECUTES during a `check live` pass — the teardown check's precondition was structurally
unfireable, regardless of any keying or env question. Ruling, now APPLIED to charly.yml: the
bed keeps ONLY the idempotent register-half `check:` (state after bring-up: entry present +
timer present, timer conjunct FIRST so each conjunct's verdict is independently visible on
failure) — the `run: bundle del ...` step and the `ephemeral-teardown-roundtrip` check are
DELETED (a hard cutover, R5-swept: `git grep ephemeral-teardown-roundtrip` returns nothing
outside this CHANGELOG). The explicit del round-trip is proven by the SAME class of
orchestrator-driven live probe that found this section's bugs — mutating round-trips are
probe territory, idempotent state checks are `check live` territory.

### RCA #6 — a FOURTH real product bug: nested vm deploys double-booked their per-host
### state under two key schemes, one loader-rejected (found by a second orchestrator
### probe whose overlay SURVIVED for direct inspection)

With RCA #5's fixes landed, a second live probe still found the register check failing —
but this time the probe's per-host overlay survived long enough to inspect directly, and it
was definitive: the SAME domain had TWO entries, one under the raw dotted deploy name
(`check-sidecar-pod.check-sidecar-pod-ephvm`) and one under the canonical sanitized key
(`vm:check-sidecar-pod-check-sidecar-pod-ephvm`) — with TWO DIFFERENT ssh_port allocations
(a real double-allocation resource bug, not just a key mismatch), and no `ephemeral` block
in EITHER. Traced end to end:

- **Two independent writers persisted `vm_state` for the same deploy.** Writer A
  (`candy/plugin-vm/vm_create_orchestrate.go`'s `hostConfigPersist("vm:"+domainID, ...)`)
  is correct — `domainID` is sanitized upstream at `vm_lifecycle_preresolve.go`'s
  `vmDomainIdentity(name)`. Writer B (`charly/substrate_lifecycle_grpc.go`'s generic
  `PrepareVenue` persist, `deploykit.SaveDeployState(deploykit.ParseDeployKey(name)...)`) used
  the RAW, UNSANITIZED deploy name — `ParseDeployKey` only splits on `/` for box/instance,
  never touches dots. `candy/plugin-deploy-vm/lifecycle.go`'s `PrepareVenue` confirmed
  shipping its own independently-resolved `VmState` in its reply, which this generic seam
  persisted under the wrong key on every Add.
- **Discriminated whether Writer B's copy was load-bearing (per ruling) before fixing
  anything.** It WAS read — but only by a SELF-REFERENTIAL loop confined to the same wrong
  key: `vm_lifecycle_preresolve.go`'s prior-state read (`dc.Bundle[name]`, matching Writer
  B's key exactly) fed `LifecyclePrepareInput.PriorState` into the plugin, which carried its
  `InstanceID`/`DiskPath`/`SeedIso` forward into its own reply, which Writer B persisted back
  to the SAME wrong key — internally consistent, entirely disconnected from Writer A's
  canonical entry. The REAL source of instance-id/disk/seed continuity is `charly vm
  create`'s own disk-build flow, which Writer A already persists under the canonical key
  independent of this loop; PrepareVenue's carry-forward was genuinely redundant once traced.
- **Why the load-side warnings appeared.** `saveBundleConfigNodeForm`'s write path never
  validates the key — only `charly/unified.go`'s `validateDeploymentName` does, at LOAD time
  — so Writer B's dotted-key write always succeeded silently, and every SUBSEQUENT overlay
  read (including this cutover's own `persistEphemeralRuntime` read-modify-write) hit the
  rejection warning trio, degrading that read and explaining why the ephemeral write kept
  soft-failing downstream even after RCA #1–#5's fixes.

**Fix — ONE writer, ONE key (not a load-side tolerance hack):**
1. `vm_lifecycle_preresolve.go`: the prior-state read now sources from the CANONICAL
   `"vm:"+domainID` key (reordered so `domainID` is computed first) instead of the raw
   deploy name.
2. `candy/plugin-deploy-vm/lifecycle.go`'s `PrepareVenue` no longer ships a `State` patch at
   all for vm — the `InstanceID`/`DiskPath`/`SeedIso` carry-forward logic is deleted
   alongside it (dead once nothing consumes it). The vm substrate now owns its persistence
   end to end via `candy/plugin-vm`'s existing canonical writer.
3. **Self-healing prune** (R5-completeness for existing corrupt state — real users' overlays
   and every bed record until this fix still carry stale dotted twins): `saveVmDeployState`
   now calls `pruneStaleVmDottedTwin` on every write, removing any OTHER `dc.Bundle` key that
   is a dotted deploy name whose `VmDomainIdentity` sanitizes to the same domain — a
   targeted, tested, one-touch cleanup on the domain's next vm operation, no new migration
   machinery. `TestSaveVmDeployState_SelfHealsStaleDottedTwin` proves the full round-trip
   against a real overlay file (seed a pre-fix poisoned entry → one canonical write → the
   twin is gone, the canonical entry survives with the correct value).
4. `charly/unified.go`'s dot-rejection message no longer says "Rename this entry in
   charly.yml" — that assumed a human authored the offending key, which is never true for a
   machine-written overlay entry a writer bug produced. Reworded to name the constraint
   without prescribing a remedy that only fits one source.
5. **Swept for the same class elsewhere:** android/k8s do not register `Lifecycle: true` and
   never reach `PrepareVenue`/this generic persist path at all — categorically not exposed.
   Pod DOES use the same generic path (`{ResolvedImage}`) and is the SOLE writer for that
   field (no Writer-A-equivalent to consolidate onto) — a nested (dotted) pod deploy would
   hit the identical dot-rejection class on its own `ResolvedImage` persist. Not fixed here
   (single-writer re-keying is a different fix shape than elimination, and no nested-pod bed
   currently exercises it) — flagged for the bed-robustness batch alongside the already-
   registered `bundle show <name>` gap.

Regression coverage: `pruneStaleVmDottedTwin` (4 cases: removes a match, no-op when absent,
does not over-match an unrelated domain, never self-prunes the canonical key) +
`TestSaveVmDeployState_SelfHealsStaleDottedTwin` (the real-overlay end-to-end proof).

### RCA #7 — a FIFTH real product bug, exposed BY RCA #6's own fix: key unification made an
### ordering interaction between two writers load-bearing for the first time

A third orchestrator-driven probe proved RCA #6's fix: zero panics, exactly ONE canonical
overlay entry, one ssh_port allocation — the dual-write and double-allocation were gone. But
the register check still failed: the timer conjunct passed, the `deploy_address` grep did
not — the `Ephemeral` block was simply ABSENT from the (now singular) canonical entry.

**Root cause: `saveVmDeployState`'s wholesale `entry.VmState = state` assignment.**
`registerEphemeralIfMarked` (`vm_lifecycle_preresolve.go`) persists `.VmState.Ephemeral`
under the canonical `"vm:"+domainID` key BEFORE `charly vm create`'s own state writes (e.g.
the port_auto persist) run — this call ordering is NOT incidental, it is how
`vmLifecyclePrepare` is written. Under RCA #6's PRE-fix dual-key bug, this never collided:
Writer B's dotted key and Writer A's `vm:`-prefixed key were DIFFERENT entries, so the two
writes never touched the same `entry.VmState`. Unifying onto ONE key (RCA #6) made the two
writers' ordering interaction load-bearing for the first time — the vm-create write's own
`state` (constructed with zero knowledge of ephemeral registration, a separate concern
owned by `candy/plugin-bundle`'s ephemeral family) silently clobbered the just-registered
`Ephemeral` block on every ordinary Add.

**Fix — an explicit, narrowly-scoped merge, not a general deep-merge:** `saveVmDeployState`
now captures the prior entry's `VmState.Ephemeral` before the wholesale assignment and
restores it when the incoming state doesn't carry one. Every OTHER `VmState` field is still
a full overwrite, exactly as before — only `Ephemeral` is preserved, since only the
ephemeral family owns it. Double-checked the REVERSE ordering (vm-create's write landing
FIRST, ephemeral registration second) was never at risk:
`candy/plugin-bundle/ephemeral.go`'s `persistEphemeralRuntime` already reads the EXISTING
`node.VmState` and only sets `.Ephemeral` on it — it never wholesale-replaces — so that
direction round-trips correctly by construction (locked in by
`TestSaveVmDeployState_ReverseOrderingRoundTrips`, from the `saveVmDeployState` side of that
round-trip). `ephemeral.go`'s `ephemeralOverlayKey` doc comment — which had characterized
this ordering as "an edge... not the common case" (written for RCA #2, true only under the
pre-RCA-#6 dual-key bug) — is corrected: RCA #6's key unification made it the common case,
and the interaction is now load-bearing, not incidental.

Regression coverage: `TestSaveVmDeployState_PreservesEphemeralOnSubsequentWrite` (seeds a
register-time write with an Ephemeral block, then a vm-create-shaped write with none —
proves the Ephemeral block survives) + `TestSaveVmDeployState_ReverseOrderingRoundTrips`
(vm-create first, ephemeral second — still correct). Both against a real overlay file.

### RCA #8 — a SIXTH real product bug: an unstripped "vm:" CLI-address prefix silently
### starved the deploy-plugin connect preamble on `bundle del`

With RCA #7's fix landed, the register half went fully green end to end (a fourth probe
confirmed the exact expected overlay shape — one entry, correct ssh_port, the complete
ephemeral block). The remaining gap was the del path: `bundle del
vm:check-sidecar-pod.check-sidecar-pod-ephvm` failed with "target \"vm\" is a known
substrate but its deploy provider is not connected" — even though the SAME probe's `bundle
add` had connected that exact provider moments earlier in the same run.

**Traced to an address-form asymmetry in a single, single-caller function.** Both `bundle
add` and `bundle del` share the same plugin-connect preamble (`loadDeployPlugins`) — this
was not a missing call. The tree-walk that drives `add` always feeds it a PLAIN dotted path
(no prefix); the CLI del command, per RCA #6's own established addressing convention, is
`vm:`-prefixed. `loadDeployPlugins` → `deployNodePluginContext` → `resolveDeployNodeByPath`
split the RAW name on `.` without ever stripping that prefix — `tree["vm:"+segment]` never
matches (the tree is keyed by the plain name) — so the node lookup silently failed,
`deployNodePluginContext` collected zero referenced plugin words, and the vm substrate's
plugin candy was never connected. The failure stayed invisible at THIS step: a completely
separate function, `resolveDelNode`, DOES special-case the `"vm:"` prefix (returning a bare
synthetic `Target`-only placeholder without ever touching the tree), so del-target
RESOLUTION reported success — the connect gap surfaced only later, when the actual dispatch
needed the never-connected provider.

**Fix:** `resolveDeployNodeByPath` (confirmed via grep to have exactly one caller,
`deployNodePluginContext` — a self-contained, low-risk fix site) now strips a leading
`"vm:"` before the dotted-path split, mirroring the SAME convention `resolveDelNode` /
`vmNameFromDeployName` already use elsewhere. This is the GENERAL fix, not a narrow
"inject vm into refWords" shortcut: stripping the prefix lets the walk find the REAL node
and correctly collect its own `AddCandy` and any nested children's substrate words too, not
just the bare word a resolveDelNode-style placeholder would give. `loadDeployPlugins`'s
existing silent-return semantics for a genuinely nonexistent name are UNCHANGED — that
error still surfaces at its normal, later point; this fix does not add speculative
loudness there.

Regression coverage: extended the EXISTING `TestResolveDeployNodeByPath` table (R3 — found
and reused the established test rather than adding a duplicate) with `"vm:"`-prefixed cases
covering both shapes team-lead asked for (`vm:top-level` and `vm:parent.child`), plus two
negative cases proving a genuine miss still fails honestly under the prefixed form.

### RCA #9 — a SEVENTH, EIGHTH, NINTH, and TENTH real product bug: the SAME "vm:" is a CLI
### addressing hint, never an identity" confusion recurring at FOUR separate points in the
### del path, only the last two found by the round's OWN required test

With RCA #8's connect fix landed, `bundle del vm:check-sidecar-pod.check-sidecar-pod-ephvm`
stopped erroring — but exited 0 with ZERO output and tore down NOTHING: the entry, the
timer, and the running VM all survived untouched. A silent no-op is worse than RCA #8's loud
failure — this needed its own RCA before any fix.

**#7 — `t.deployID()`'s ledger lookup hashed the raw, unstripped CLI address.**
`externalDeployTarget.Del` (deploy_target_external.go:575-581) reads the operator-side ledger
via `kit.ReadDeployRecord(paths, t.deployID())`; `t.deployID() =
deploykit.ComputeDeployID(t.name, nil, nil)` — a bare SHA256 of `t.name` VERBATIM
(sdk/deploykit/install_build.go:275). The add-time walk always names a deploy with the plain
dotted path; the del CLI address (per RCA #6's own established convention) carries a `"vm:"`
prefix — computed directly: `ComputeDeployID("check-sidecar-pod.check-sidecar-pod-ephvm")` =
`6413f8070aaa6087`, `ComputeDeployID("vm:check-sidecar-pod.check-sidecar-pod-ephvm")` =
`d81fff596411fea4` — completely different hashes for the identical logical deployment. The
ledger lookup found nothing under the mismatched ID and took its "nothing recorded —
idempotent teardown" branch — correct and load-bearing for a GENUINELY already-removed
deploy, wrong here, and deliberately left untouched (hardening it would be exactly the
"gold-plating loudness" ruled out on RCA #8).

**#8 — `resolveDelNode`'s "vm:"-prefix shortcut never touched the tree at all.** The prime
suspect this RCA started from: `resolveDelNode` unconditionally returned a synthetic
`&spec.BundleNode{Target: "vm"}` — no `From`, no `Children` — for ANY `"vm:"`-prefixed name,
so del-target resolution always "succeeded" while masking whatever else was wrong (this is
what let RCA #8's own connect bug hide behind a false-positive resolve). Confirmed NOT the
cause of the observed no-op — the ledger lookup above returns before `t.node` is ever
consulted — but independently real and worth fixing.

**Fix for #7 and #8 — one convention, one shared helper, not a narrow patch per site.**
Grepped every `strings.HasPrefix(x, "vm:")` in the address-handling family and found the
SAME check independently reimplemented three times (`resolveDeployNodeByPath`/RCA #8,
`resolveDelNode`, and the new del-dispatch strip) — pulled out `splitVmAddress(name) (plain
string, isVm bool)` (vm_deploy_state.go, beside `vmNameFromDeployName`) as the single source:
"vm:" is a CLI ADDRESSING hint, never an identity. `resolveDelNode` now tries the REAL tree
resolution first (`resolveDeployNodeByPath`, already `"vm:"`-aware from RCA #8), falling back
to the synthetic placeholder only for a genuine tree-absent orphan.
`hostBuildDeployNodeDelDispatch` strips the prefix via `splitVmAddress` BEFORE constructing
the `externalDeployTarget` — so `t.name`/`t.deployID()` match whatever the add-time walk
used, restoring the ledger match. Scoped deliberately: `resolveVmEntity` /
`vmEntityForAdd` / `vmNameFromDeployName` (entity-extraction, error-on-missing-prefix — a
different contract) and `candy/plugin-vm`'s `holderAddrFor` (a different module) were left
alone — not the same convention, not folded in.

**#9 — `vmLifecyclePostTeardown` checked the WRONG Ephemeral field, caught by this round's
OWN required test.** After #7/#8 landed, the required regression test
(`TestVmLifecyclePostTeardown_UsesCanonicalKey`) still failed: `dcNode.IsEphemeral()` is
`Deploy.Ephemeral != nil` — the AUTHORED `ephemeral: {ttl: ...}` TTL declaration
(sdk/spec/cue_types_gen.go) — but a per-host overlay entry NEVER carries that field BY DESIGN
(`ephemeralFallbackNode`, candy/plugin-bundle/ephemeral.go, seeds only `Target`/`From`: "an
overlay entry is state, never structure"). The runtime record lives at a DIFFERENT field,
`dcNode.VmState.Ephemeral` (`*spec.EphemeralRuntime`). This condition has been false for
EVERY overlay entry that has ever existed, for any key form, since before this cutover —
`TeardownEphemeralLifecycle` has never fired from this call site in the feature's entire
existence, which retroactively explains why no earlier probe's del ever cleared ephemeral
state even once the other layers were individually fixed. Fixed to check
`dcNode.VmState != nil && dcNode.VmState.Ephemeral != nil`, matching the pattern every OTHER
ephemeral-family function already uses (e.g. `bumpParentChildRefcount`).

**#10 — the SAME confusion one call deeper, in `teardownEphemeral` itself.** Fixing #9 let
execution reach `TeardownEphemeralLifecycle` for the first time, which immediately surfaced
the IDENTICAL bug in its own callee: `teardownEphemeral(node *spec.Deploy, ...)`'s entry
guard was ALSO `!node.IsEphemeral()` — checking the same authored-only field on the SAME
overlay-loaded node `vmLifecyclePostTeardown` passes it, while the REST of the function's own
body (TTL-timer cancel, snapshot-refcount decrement, parent-refcount bump) already correctly
reads `node.VmState.Ephemeral` throughout. Fixed to match: `node == nil || node.VmState ==
nil || node.VmState.Ephemeral == nil`.

**Swept for further instances of the SAME class before declaring done** (every remaining
`.IsEphemeral()` call site, repo-wide): `registerEphemeralIfMarked` / `registerEphemeral`
receive the AUTHORED, dispatch-merged project node (not overlay-loaded) — correct as-is.
`isEphemeralDeploy` (check_runner_live.go) reads from `resolveTreeRoot`'s PROJECT-plus-overlay
MERGE, which preserves the project's own `Ephemeral` field untouched by the overlay side
(a different field than `VmState.Ephemeral`) — correct as-is. `ValidateEphemeralOnNode` and
the disposable-auto-promote loop (validate_ephemeral.go) both operate on the project's own
parsed tree at LOAD time — correct as-is. No further instances found.

Regression coverage: `TestSplitVmAddress` (6 cases, the helper's own table) +
`TestSplitVmAddress_LedgerIdentityRegression` (computes REAL `ComputeDeployID` hashes,
preserving the pre-fix collision as a documented negative case, proving the stripped form
matches the add-time ID) + `TestVmLifecyclePostTeardown_UsesCanonicalKey` (end-to-end against
a real overlay file — the exact test that caught #9 and, transitively, #10).

### Registered for the next thematic batch (not fixed here, pre-existing, dormant
### until this cutover's own new bed exercised it) — bed-robustness

`charly bundle show <name>`'s single-positional-arg lookup (`deploykit.DeployKey`, a
bare pass-through with no "vm:" prefix or `VmDomainIdentity` sanitization) can **never**
find a VM deploy's own persisted per-host state — universal across every VM deploy
(nested/ephemeral or not), since `saveVmDeployState`'s callers (`candy/plugin-vm`'s
`hostConfigPersist`) have always persisted under a `"vm:"`-prefixed, sanitized key.
Confirmed via repo-wide grep to be exercised by zero existing check plans until this
cutover's own `ephemeral-register-roundtrip`/`ephemeral-teardown-roundtrip` steps hit
it first (RCA evidence: this cutover's PR discussion, unit 6a RCA #3). This cutover's
own check steps work around it via the whole-dump `bundle show` form (no positional
arg) anchored on the entry's unique `deploy_address` marker — non-vacuously, per
ruling. The real fix (`bundle show <name>` should try the `"vm:"+VmDomainIdentity(...)`
key form, or a broader semantic decision on how `bundle show` addresses vm entries) is
a user-facing design question deserving its own review, not a quick fallback chain —
registered here as the evidence base for the bed-robustness thematic batch.

**A second entry, found during RCA #6's same-class sweep:** pod's `PrepareVenue` reply also
ships self-persisted state (`{ResolvedImage}`) through `substrate_lifecycle_grpc.go`'s
generic persist, keyed by the RAW (unsanitized) deploy name — the exact mechanism that
poisoned vm's nested deploys. Pod is the SOLE writer of `ResolvedImage` (unlike vm, which
had a canonical Writer A to consolidate onto), so the fix shape differs: re-keying pod's
persist for a nested (dotted) deploy, not elimination. No currently-exercised bed nests a
pod deeply enough to trigger it, so it stays latent — registered here rather than fixed
speculatively.

**A third entry, found during RCA #13's trace, REVOKED by RCA #14 (fixed in-unit, not
registered):** `deploykit.FindVmDeployNode`'s top-level fallback scan was flagged here
as latent/harmless — that survey was WRONG. Bed 8 fired it for real: the scan matched
`check-substrate` (a genuine top-level `vm: {from: eval-vm}` deploy carrying a real
nested member plan) instead of `check-sidecar-pod-ephvm`'s own (empty) leaf, and the
borrowed step's hoisted venue (`check-substrate.check-substrate-member`) tried to SSH
into an unrelated bed's domain. R2: the survey missing this made the registration itself
incorrect, so the fix is IN THIS UNIT, not the batch — see "RCA #14" below.

### RCA #14 — ambiguous vm entity scan silently borrows an unrelated bed's plan

Bed 8's member check-live now correctly routed to the vm path (RCA #12+#13 proven) and
immediately hit a NEW failure one layer deeper: `check-live-rebuild-check-sidecar-pod-
ephvm` ran `substrate-marker-in-guest` (check-substrate-member's OWN marker check) and
failed with `ssh: Could not resolve hostname charly-check-substrate` — while the
SAME phase's FIRST pass had quietly reported "No plan steps defined" (correct).

Root cause, in full: `charly/unified.go`'s `LoadUnified` runs `flattenBundleVenues`
(`charly/node_bundle_venue.go`) — a general, load-time mechanism (not group-specific)
that HOISTS every Children/Members subtree's `.Plan` steps onto its TOP-LEVEL ROOT's own
`.Plan` (stamping each step's `.Venue` with its dotted/bare address) and CLEARS the
source node's `.Plan`. `check-substrate` (charly.yml, `vm: {from: eval-vm}`, genuinely
top-level from load) has a nested `check-substrate-member` authoring the
`substrate-marker-in-guest` check directly on its own `plan:` — post-hoist,
`check-substrate`'s TOP-LEVEL `.Plan` genuinely contains that step (this is the exact
mechanism `checkLivePod`'s own whole-tree check-live consumes to run a bed's full
nested tree in one pass — bed 6's 23-check main run). `loadVmCheckPlans`'s
`nestedLeaf==nil` fallback (`deploykit.FindVmDeployNode(pc.Bundle, c.Box, vmName)`)
iterated ALL top-level `Target=="vm" && From=="eval-vm"` entries with no uniqueness
guard, as RCA #13's trace had already flagged (see the third registered entry above,
now revoked). The candidate pool: `check-builder-vm`/`check-substrate` (both real
top-level vm deploys — hoisting relocates their descendants' content onto their OWN
`.Plan`, non-empty, "poison") vs `check-group-vm`/`check-structkind-vm` (both authored
as peer Members of a targetless parent — `flattenBundleVenues` runs BEFORE
`foldMembers` promotes a Member to a top-level key, so their content was ALREADY
relocated onto their FORMER parent's root before promotion, leaving their own `.Plan`
empty by the time they're visible as top-level entries — "harmless"). Each
`charly check live` phase is a SEPARATE subprocess re-loading `charly.yml` into a fresh
map, and Go randomizes map iteration order PER PROCESS — so the SAME ambiguous scan,
against structurally-identical content, nondeterministically lands on a harmless or a
poison match depending purely on that process's map order. This is the full, exact
explanation for the intermittent first-pass-quiet / rebuild-pass-failed behavior — not
a phase-order or environment difference.

Fix: `resolveVmTarget`'s leaf-first branch (RCA #13) now ALSO populates `nestedLeaf`
with the tree-walked leaf node itself (mirroring the existing delegate-into-guest
branch's `resolveNestedNode` call) — `loadVmCheckPlans` therefore NEVER falls through
to the ambiguous scan for ANY dotted `c.Box`, eliminating this bed's exposure
structurally rather than papering over one symptom. `deploykit.FindVmDeployNode`
(`sdk/deploykit/deploy_state.go`) is independently hardened: its step-3 fallback scan
now detects 2+ candidates and returns an error instead of first-winning — new signature
`(BundleNode, bool, error)`. Three call sites updated: `charly/check_cmd.go`'s two
`loadVmCheckPlans` lookups now propagate the error up through `checkLiveVM` (both
already return `error`); `candy/plugin-substrate/status_flat.go`'s `enrichVmRow` (a
`charly status` display-enrichment helper) treats an ambiguity error the same as
not-found — skip enrichment for that row — matching its own pre-existing "absence of a
deploy entry is normal" contract; a status LISTING must never error over one row's
enrichment ambiguity.

**Fourth latent finding, registered to the bed-robustness batch (NOT fixed
speculatively):** because `flattenBundleVenues` unconditionally clears `.Plan` on ANY
node that is ever a Children/Members entry (relocating its content onto its owning
root), reading a resolved leaf node's `.Plan` directly — via `resolveDeployNodeByPath`,
`resolveLeafVenue`, OR the pre-existing `nestedLeaf` mechanism this cutover's own fix
extends — is ALWAYS nil/empty post-load for a genuinely-nested node, REGARDLESS of what
it originally authored. This is a member of the **SILENT-CONFIG-DROP class** (same
family as the ephemeral-on-pod inertness registered elsewhere in this file): a FUTURE
nested-leaf-authored `plan:` reached this way would be silently ignored, not merely
unresolved. Confirmed harmless for THIS cutover (`check-sidecar-pod-ephvm` authors no
`plan:` of its own — empty is the correct, first-pass-proven answer) and confirmed
present in at least two OTHER pre-existing, untouched surfaces: the `check-arch-vm.
arch-host` delegate-into-guest precedent's own `nestedLeaf.Plan` read (box/arch
submodule, unverified whether `arch-host` authors real plan content), and
`checkLivePod`'s own dotted-path branch (`charly/check_cmd.go`,
`resolveNestedNode(pc.Bundle, c.Box).Plan` for a pod-in-pod nested check — the identical
shape, substrate-independent). The general fix is a venue-filtered read of the OWNING
ROOT's hoisted `.Plan` (filtering for `step.Venue == <the exact dotted leaf address>`,
mirroring how `checkLivePod`'s own whole-tree path already consumes the hoisted root
Plan) — NOT building that generality now, with no bed that authors a nested-leaf plan
to prove it, which would be unproven speculation against the RDD spike-first
discipline. The batch's fix is therefore two-part: the venue-filtered-from-root-Plan
read, PLUS a NEW bed that actually authors a nested-leaf plan to prove it fails without
the fix and passes with it (the check-coverage gate, not just the mechanism).

### K3-envelope + config-persist

`config.go`'s `ResolveOpts` gains `DistroCfg`/`BuilderCfg` fields threaded through
`ResolveBox`/`ResolveAllBox`, killing N redundant `LoadBuildConfigForBox` reloads in
the `ResolveAllBox` loop when the caller already has the vocabulary loaded. The
physical `Config` type relocation itself (Q4) is OUT of this unit's scope — re-
registered to FLOOR-SLIM with a corrected destination (`sdk/spec`, not `sdk/buildkit` —
`LoadConfig` stays K1-permanent-core, so the physical type must not force a permanent-
core → mechanism-kit import).

### F6/K5 plugin-side preresolve bodies

`candy/plugin-adb/preresolve.go` and `candy/plugin-kube/preresolve.go` (both NEW) carry
the relocated preresolve bodies, each declaring `Preresolve: true` on their `deploy`
capability. `sdk/kit/apk_path.go` (NEW) relocates the shared `resolveApkPath` helper
(now `kit.ResolveApkPath`), shared by core's `resolveCheckApk` and the moved android
plugin body (R3 — was two independent copies drifting).

### M4b — the vm PrepareVenue body move (the mechanism's third, final proof)

`vm` is `Lifecycle:true` (the full venue lifecycle — PrepareVenue/Start/Stop/Status/
Rebuild), not `Preresolve:true` like k8s/android, so its host-side DATA seam was a
SEPARATE, narrower mechanism: `lifecyclePrepareHook` (`registerLifecyclePrepareHook`/
`lifecyclePrepareHookFor`, consulted by `substrate_lifecycle_grpc.go`'s own
`OpPrepareVenue` dispatch, threading a pre-computed `spec.LifecyclePrepareInput` under a
"prepare" params key) — with no wire-backed/plugin-replace sibling the way
`deployPreresolver` has. Since `candy/plugin-deploy-vm` ALREADY implements
`OpPrepareVenue` itself (the Lifecycle:true substrate), the indirection was unnecessary
scaffolding: `vmPrepareVenue` now resolves its OWN `LifecyclePrepareInput` — the vm
entity via the generic "deploy-entity-resolve" HostBuild seam (`Kind:"vm"`, the exact
pattern `candy/plugin-kube/preresolve.go`'s `k8sEntityResolve` already proves live for
k8s), sshPort/stateDir/SSHUser/PriorState directly via `sdk/deploykit`+`sdk/kit`+
`sdk/vmshared` (pure, no LoadUnified coupling — the plugin is CO-LOCATED on the host,
not remote; `go mod tidy` picked up the new `sdk/deploykit` transitive import). The ONE
piece that genuinely still needs to stay host-side — the ephemeral-registration Add-time
side effect (systemd transient timer + the panic-vs-warning classification, RCA #5,
which must stay single-sourced where the add-failure semantics live) — now reaches a
NEW, substrate-agnostic HostBuild seam, `"ephemeral-register"`
(`charly/host_build_ephemeral_register.go`), wrapping `registerEphemeralIfMarked`
verbatim; it is the natural extension point pod/k8s's own future ephemeral support (the
bed-robustness batch) will reach through identically — one seam built once for all
three substrates' futures, not a vm-only hop.

Hard-cutover deletions (R5, this move): `lifecyclePrepareHook`/
`registerLifecyclePrepareHook`/`lifecyclePrepareHookFor`/`lifecyclePrepareHooks`, the
compiled-in `vmLifecyclePrepare` body, and `vmEntityForAdd` (ported verbatim to
`candy/plugin-deploy-vm/lifecycle.go`'s `vmEntityForPrepare` — the ONE caller that
needed it) — all gone from `charly/vm_lifecycle_preresolve.go`, plus the "prepare"
params-key threading in `substrate_lifecycle_grpc.go`'s `PrepareVenue`. Three stale
present-tense comments claiming the deleted mechanism still ships (in
`host_build_config_resolve.go`, `vm_deploy_state.go`, `deploy_substrate_lifecycle.go`)
corrected in the same change (R1 claim-keyed sweep, caught by the grep self-test on
every deleted identifier). `vmAttachResolver`/`registerLifecycleLivePlanHooks` (F12, an
unrelated hook) and `vmLifecyclePostTeardown`/`registerLifecyclePostTeardownHook` (the
ephemeral teardown cleanup, also unrelated to PrepareVenue) are UNCHANGED — only the
PrepareVenue DATA-seam moved.

New coverage: `TestVmEntityForPrepare` (`candy/plugin-deploy-vm/lifecycle_test.go`, the
module's first test file) — 7 cases covering the ported 3-branch-plus-error resolution
logic (`vmEntityForAdd` itself had no dedicated test before this move).

### `check-charly-vm` RCA — a latent B-1 per-module go.sum gap, plus a process discovery

`check-charly-vm`'s first run FAILED at deploy-add: the localpkg (Arch `pac`) build of
one of the baked out-of-process command plugins errored `missing go.sum entry for
module providing package golang.org/x/term (imported by github.com/opencharly/sdk/
deploykit)`. Initial hypothesis (main-regression, wrong) was that `charly/go.sum`
itself lacked the entry — disproven: `charly/go.mod`/`go.sum` already pin
`golang.org/x/term v0.41.0` correctly, confirmed by reproducing the exact
`-mod=readonly` build (both in-place and from a genuine fresh `git clone` + fresh
`GOCACHE`/`GOMODCACHE`, matching makepkg's own isolation) — it succeeds every time.

The real culprit, found by iterating a standalone `GOWORK=off go build` over every
module in `pkg/host-command-plugins.txt` (the same 9 modules the PKGBUILD's `build()`
loop compiles): **`candy/plugin-feature`** — the ONLY one, of the 6 command plugins that
import `sdk/deploykit`, whose OWN `go.mod`/`go.sum` was never tidied for the `x/term`
transitive dependency `sdk/deploykit`'s `secret_probe.go` needs (a genuine B-1-era gap:
`x/term` was tidied into `sdk` + most plugin modules back then, but `plugin-feature`'s
own per-module tidy was missed, and no bed exercised its standalone build since —
`check-charly-vm` wasn't in B-1's roster). The failing error's own relative import path
(`../../sdk/deploykit/...`, TWO levels up — a `candy/<plugin>/` build cwd, not
`charly/`'s ONE level) was the giveaway once traced precisely; `charly/` core's own
standalone build was never actually implicated. Confirmed `plugin-vm`/`plugin-doctor`
(the other two `deploykit`-importing command plugins) already carry the entry
correctly, and every `go.work`-compiled-in module (`plugin-build`/`plugin-bundle`/
`plugin-status`/`plugin-substrate`) legitimately carries NONE of its own — they never
build standalone, so their own `go.sum` completeness is moot; only per-module-tidied
modules need this entry, and only `plugin-feature` was missing it.

Fix: the sanctioned narrow per-module `GOWORK=off go mod tidy` inside
`candy/plugin-feature/` (matching every other correctly-tidied plugin candy) — adds
`golang.org/x/term v0.41.0 // indirect` to its `go.mod` + the matching `go.sum` hash
pair. Minimal diff (5 lines), verified against a genuine fresh clone reproducing the
FULL 9-plugin build loop end to end (all exit 0, where `plugin-feature` previously
failed alone). This repairs a **latent regression of `main`'s packaged (localpkg/AUR)
build path**, present since B-1 — the workspace (`go.work`) build path `task
build:binary` uses was never affected (it resolves via `go.work.sum`, never consulting
any individual module's own `go.sum` for a `use`'d member), which is exactly why this
went unnoticed until a disposable bed's standalone packaging path finally exercised it.

**Process discovery, worth recording:** the localpkg build step's `git clone` source
resolves from `CHARLY_LOCALPKG_SOURCE_ROOT`/the invoking worktree's OWN git HEAD — it
CANNOT see uncommitted working-tree changes (unlike the `bin/charly` DEV-fast-path,
which reads the live binary directly when present). `check-charly-vm` and any other bed
whose deploy path reaches a localpkg (`pac`/`aur`/…) install step therefore gates on
**COMMITTED** state — a fix made only in the working tree will never be exercised by
that class of bed, no matter how many times it re-runs, until it lands as a real
commit. Team-lead's own bed re-run request for this fix is itself downstream of this:
the M4b work is committed as its own commit (see below) specifically so this bed's
NEXT run can actually see it.

### M4c — `box pull` command-dispersal (pull-first)

The first unit of the build/pull command-dispersal, following the standing ruling
(`BoxPullCmd` first — the smallest of the retained `BoxCmd` residue — spike-gated
proving byte-identical CLI behavior before the larger `ensure_image.go` + `build.go` +
`remote_image.go` logic-move batch). Mirrors the ALREADY-LANDED `pkg` verb's shape
exactly (`candy/plugin-box/box.go`'s `dispatchPkg`/`pkgGrammar`), itself the reference
the whole "9-sibling dispersal" precedent library (`plugin-doctor`/`plugin-clean`/
`plugin-settings`/`plugin-candy`/…) converges on: own the CLI grammar fully in the
plugin, reach genuinely core-coupled logic via ONE generic HostBuild seam, and — where
the logic itself is still too core-coupled to move (as `EnsureImagePresent`'s
build-fallback is, pre the later batch) — reenter a HIDDEN core command over the
existing generic `"cli"` seam rather than inventing a new one.

`candy/plugin-box` gains a tenth nested command capability, `pull` (added to
`boxCommandWords`; `plugin.go`'s package doc updated from "NINE" to "TEN" capabilities).
`dispatchPull` + `pullGrammar` (`box.go`) own the `charly box pull <box> [--tag]
[--platform]` grammar — a byte-for-byte copy of the former static `BoxPullCmd` Kong
leaf's fields/help text — and reenter the hidden `charly __box-pull` core command over
`HostBuild("cli")`, exactly like `pkg` reenters `__box-pkg`. `charly/main.go` gains the
hidden `BoxPull BoxPullCmd` reentry field (mirroring `BoxPkg`'s); `charly/image.go`'s
`BoxCmd` struct drops its `Pull` field (no longer a direct Kong child of `box` — the
plugin now serves it dynamically) and its MIGRATION INVENTORY comment is updated.
**`BoxPullCmd`'s own `Run()` body is completely UNCHANGED** — only its Kong attachment
point moved; `EnsureImagePresent`/`ResolveBox`/`BuildCmd.Run()` all stay exactly where
they were, tracked as the NEXT batch's own scope.

**Spike, run live against the rebuilt binary:** confirmed `charly box pull`'s help
rendering, error path, and success path ALL behave identically to the pre-dispersal
static command AND identically in SHAPE to the landed `pkg` precedent — `charly box
pull --help` / `charly box pkg --help` both render the SAME generic
"box <word> [<args> ...]" top-level passthrough (the established, accepted shape for
every already-externalized `box` verb — not a `pull`-specific gap); `charly box pull --
--help` and `charly box pkg -- --help` both reach their OWN hidden reentry's detailed
help identically. A real pull against an already-present local image
(`ghcr.io/opencharly/check-sidecar-pod:2026.202.0619`) printed the EXACT unchanged
`"ensure-image: %s present"` line and exited 0; an invalid box name produced the
EXACT unchanged three-line error chain (`ensure-image: resolve …` /
`ensure-image "…": not present locally, pull failed, …` / the dispersal's own generic
`box pull failed (exit %d)` wrapper — the SAME two-message shape `pkg` already
established as the accepted convention: the real error prints live via inherited
stderr from the reentered subprocess, the wrapper is a second, generic signal for the
plugin dispatch's own exit-code bookkeeping).

New coverage: `TestNewMeta_DeclaresNestedCommands` updated (nine → ten capabilities,
`pull` added to the word list); `TestPullGrammar_Parse` +
`TestPullGrammar_Parse_MinimalArgs` (full-args and Tag/Platform-omitted shapes,
mirroring `TestPkgGrammar_Parse`'s existing pattern).

**Check-coverage — the R10 disposable-bed proof, and a venue-model correction en route.**
No bed anywhere in the project exercised `charly box pull` before this cutover (confirmed
by grep across `charly.yml` and every `box/*`/`candy/*`) — a genuine ADE gap this closes,
not merely a nice-to-have. The first placement proposal (a `check:` step on
`check-builder-vm`'s own plan, pulling the `fedora.fedora-builder` image its sibling
member already uses) was verified WRONG before authoring: venue is a per-KIND DECLARED
trait (`deployTraitsFor`/`DeclaredDeployTraits()`), with no positional exception — EVERY
`target: vm` node, top-level or nested, resolves to the guest SSH venue, and
`fedora.fedora-builder` is built HOST-SIDE ONLY (confirmed in check-builder-vm's own
description). A step there would not be an idempotent no-op; it would attempt a real
network pull inside the guest every run — `run:`-shaped inside a `check:` step, the exact
ADE violation class RCA #5 already flagged. The correct, precedented mechanism instead:
`${CHARLY_BIN} ... in_container: false` (the `command:` verb's per-step host-venue
override, `candy/plugin-command/plugin.go`'s `InContainer`/`from_host` field) — proven live
across `check-sidecar-pod`'s own existing shell/logs/update round-trip steps and
`check-migrate-local`'s `stderr:`-asserted precedent — lets a HOST-venue check live inside
an otherwise pod-venue bed's plan regardless of tree position. New step
`box-pull-dispersal-roundtrip` on `check-sidecar-pod`'s plan: `${CHARLY_BIN} box pull
check-k8s-deploy-app` (note: the underlying BOX name, not the deploy name
`check-sidecar-pod` — verified live before authoring; `cfg.ResolveBox` has no deploy-name
fallback and the wrong name errors "box ... not found in charly.yml"), asserting
`EnsureImagePresent`'s own unchanged `stderr` "present" line. `check-k8s-deploy-app` is
already present in local storage from this bed's own image-build phase (confirmed live:
`ensure-image: ghcr.io/opencharly/check-sidecar-pod:2026.202.0813 present`, exit 0,
verified on stderr specifically, not stdout — `cmd.Stdout`/`cmd.Stderr` capture
separately in the `command:` verb) — genuinely idempotent, mutates nothing, and fails
without the new grammar/dispatch/reentry chain working. `check-builder-vm` drops out of
this leg entirely; `charly box validate` — exit 0, zero warnings.

### M4d — `box build` command-dispersal (the CLI-only mirror, closing the build/pull batch)

The second and final unit of the build/pull command-dispersal, following a scoping trace before
implementation: `EnsureImagePresent` (ensure_image.go) and `RemoteImageContext.BuildImage`
(remote_image.go) both construct and call `BuildCmd{...}.Run()` DIRECTLY as a Go method — never
through Kong/CLI — so they are Go-level API consumers of `BuildCmd`, not CLI-dispersal targets
themselves. `EnsureImagePresent`/`ResolveRemoteImage`/`BuildCmd.Run()`'s own internals (the
privileged-bootstrap subsystem, remote-ref resolve/download/scan, retention pruning) are
**RE-CLASSIFIED from a K5-dispersal IOU to the K1/K3-ENGINE family** (loader/build-engine cone,
tracked to move with those waves — the privileged-bootstrap path especially warrants its own
spike-gated effort, never a cutover tail-end guess) — `remote_image.go`'s own file header already
documented this UNTIL-K1/K3 framing; this trace corrects the earlier K5-dispersal-batch routing.

So M4d moves ONLY `BuildCmd`'s Kong attachment — mirroring `pull`'s M4c shape exactly: `command:build`
joins `candy/plugin-box` (`boxCommandWords` ten → eleven), `buildGrammar`/`dispatchBuild` (`box.go`)
own the full nine-flag grammar (byte-identical to the former static `BuildCmd` Kong leaf, including
the three env-tagged tunables — `CHARLY_BUILD_CACHE`/`CHARLY_BUILD_JOBS`/`CHARLY_PODMAN_JOBS` resolve
in-plugin at parse time and flow through argv, so the reentered subprocess needs neither the env vars
nor to re-parse them), reentering the hidden `charly __box-build` core command over `HostBuild("cli")`.
`charly/main.go` gains the hidden `BoxBuild BuildCmd` reentry field; `charly/image.go`'s `BoxCmd`
struct drops its `Build` field (`Feature` is now the SOLE retained verb) and its MIGRATION INVENTORY
comment is updated. **`BuildCmd`'s own `Run()` body is completely UNCHANGED.**

**Spike, run live against the rebuilt binary**: `charly box build --help` / `-- --help` match the
`pull`/`pkg` precedent's shape exactly (generic top-level passthrough, then the hidden reentry's own
detailed help with all nine flags). An invalid box name produced the exact unchanged
`"scoping generation to requested boxes: unknown box …"` error plus the dispersal's own generic
`box build failed (exit %d)` wrapper — the same established two-message shape.

**A test failure led to a second, larger finding — reported and confirmed before fixing.**
`TestCLIModel_CoversCommands` failed: `box.build` missing from the `charly __cli-model` output (the
seam `candy/plugin-mcp`'s MCP tool generation reads from). Root cause: the model is built from (1) a
static-Kong-struct walk and (2) `declaringCommandHolders`, which folds in a dynamic command-class
capability ONLY when it declares a `Subcommands` catalog (F-CLI-NEST — `list`'s `boxListSubcommands`
is the working precedent; a flat, undeclared command is excluded BY DESIGN,
`TestBuildCLIModel_CheckAndBoxList`'s own comment documents this for `vm`). `build` used to reach the
model via path (1) as a static field; externalizing it drops it from BOTH paths.

Before treating this as a `build`-specific regression, probed the live model for every other
already-externalized flat `box` verb: `box.pkg`/`box.pull`/`box.generate`/`box.validate`/
`box.merge`/`box.reconcile`/`box.new`/`box.labels`/`box.inspect` are **ALL already absent, on
`main`, today, unrelated to this cutover** — none of them are asserted present OR absent by any
existing test. `box.build` was the ONLY one with a stale assertion, written when it was still a
static core field and never revisited across the nine prior externalizations. Fixed the test (removed
`box.build` from the required-present list, with a comment documenting the reclassification and the
sibling-probe evidence) — R2's separability test passes cleanly (this unit's own claim, byte-identical
*interactive* CLI behavior, is fully provable without the MCP-model fix; the gap predates this unit by
nine landings).

> **REGISTERED — with prominence — thematic batch: "flat-command CLI-model bare-leaf mechanism, 10
> verbs."** A real, accumulated, user/agent-facing hole: MCP tool discoverability has been silently
> lost, one landing at a time, for TEN `box` command-dispersal verbs to date
> (`pkg`/`pull`/`build`/`generate`/`validate`/`merge`/`reconcile`/`new`/`labels`/`inspect` — every
> flat, non-nested externalized `box` verb) — none of them are currently reachable as an MCP tool,
> and nothing has flagged this since `pkg`'s original externalization first created the gap. The fix
> is a GENERIC mechanism: let a command-class capability with no declared `Subcommands` catalog still
> emit a bare, childless leaf into the CLI model (`declaringCommandHolders`'s exclusion is currently
> all-or-nothing — declare children to appear at all, or be invisible) — NOT ten individual
> per-command declared-catalog workarounds. Scoped as its own batch, not fixed speculatively here or
> folded into this cutover's own scope.

New coverage: `TestNewMeta_DeclaresNestedCommands` updated (ten → eleven capabilities, `build`
added); `TestBuildGrammar_Parse` + `TestBuildGrammar_Parse_MinimalArgs` (all nine flags exercised,
mirroring `TestPullGrammar_Parse`'s pattern); `TestCLIModel_CoversCommands` corrected per the finding
above.

### Hard-cutover deletions

`charly/ephemeral_lifecycle.go` (428 LOC), `charly/android_deploy_cmd.go` (261),
`charly/android_deploy_preresolve.go` (181), `charly/k8s_deploy_preresolve.go` (146),
plus their now-relocated test files — deleted in the same change as their replacement
seams land. R5 claim-keyed sweep corrected ~15 stale present-tense comments across
`charly/` core and `candy/plugin-adb`/`candy/plugin-kube`/`candy/plugin-substrate` that
still claimed the deleted files hold live behavior.

### RCA #12 — check-live per-member VM misclassification (leaf-vs-root venue check)

Bed 6's main `check-live` passed 23 checks (including `ephemeral-register-roundtrip`,
proving RCA #4-#11's fixes end-to-end) but the harness's per-member phase for the
nested vm — `check-live-check-sidecar-pod-ephvm` — failed with `container
charly-check-sidecar-pod.check-sidecar-pod-ephvm is not running`: it resolved the vm
member as a container. `checkVmTarget`/`checkLocalTarget` (`charly/check_venue.go`)
both classified a dotted `charly check live` name by checking ONLY its ROOT segment's
venue trait — correct for the one shape either function was ever asked to resolve (the
delegate-into-guest shape, root IS the vm/host, e.g. `check-arch-vm.arch-host`), but
blind to the opposite shape: a vm/local **Children** entry nested under a non-matching
parent (a `target:pod` root with a `target:vm` child — `check-sidecar-pod.check-sidecar-
pod-ephvm`, first exercised by this cutover's own new bed).

Discriminated against `check-group`'s own nested vm (its bed passes with a `vm:` node
too): `spec.Deploy` carries two distinct tree fields, `Children` (`nested`, venue-scoped)
and `Members` (`peer`, `bringUpMembers` siblings) — `sdk/deploykit.BedCheckLiveRefs`
walks only `Children`, and Members are documented as never check-live'd.
`check-group`'s `check-group-vm` hangs directly off the targetless `check-group` root
(no venue of its own), so it is a **Members** (peer) entry and never reaches
`BedCheckLiveRefs`'s iteration at all — check-group's bed structurally never exercises
this resolution path. `check-sidecar-pod-ephvm` hangs off `check-sidecar-pod` (a
`target:pod` root, which HAS a venue), making it a genuine **Children** entry — eligible
for its own check-live hop per `BedCheckLiveRefs`'s own doc comment (vm/pod/local
children are explicitly in scope). Confirmed: real, generalizable resolution bug, not a
bed-harness eligibility defect.

Fix: one shared `resolveLeafVenue(uf, name)` helper (`charly/check_venue.go`) walks the
full dotted path via the existing `resolveDeployNodeByPath` tree-walk and reports the
LEAF node's own venue. `checkVmTarget`/`checkLocalTarget` now check the leaf FIRST
(`vmDomainIdentity(name)` — the full dotted path, sanitized, the same canonical key
scheme RCA #6-#9 already established for every vm-state write) and fall back to the
existing root-only check when the leaf doesn't resolve or doesn't match — preserving
the delegate-into-guest shape unchanged. R3: the identical root-only defect existed in
both functions; fixed via the one shared helper rather than a vm-only patch plus a
flagged sibling.

### RCA #13 — nil-pointer SSH-user resolution for a leaf-vm-under-non-vm-parent

RCA #12 fixed *classification* (the per-member phase now correctly routes to the vm
path), immediately surfacing the next layer: `panic: nil pointer dereference` in
`vmshared.resolveCloudInitSSHUser` (`sdk/vmshared/cloud_init_render.go:243`), called
from `checkLiveVM` (`charly/check_cmd.go:238`). `resolveVmTarget`
(`charly/check_cmd.go:396`) is a THIRD, independent implementation of RCA #12's exact
root-only-check defect class — it re-derives `vmName`/`domainID`/`nestedLeaf` from
`c.Box` itself rather than consuming `checkVmTarget`'s (or `resolveLeafVenue`'s)
verdict. For a dotted name whose ROOT is not a vm (`check-sidecar-pod.check-sidecar-
pod-ephvm`), its `else if` entity-resolution branch never ran, so `vmName` stayed the
raw dotted string; `uf.VM[vmName]` (keyed by plain entity names) found nothing;
`resolveVmViaPlugin` returned a nil `*VmSpec`; `resolveCloudInitSSHUser` dereferenced
`spec.SSH` on that nil. (`domainID` was already correct by accident — `domainKey`
defaults to the full `c.Box`, the same canonical `vmDomainIdentity(name)` scheme RCA
#6-#9 established — only the entity resolution was broken.)

Fix: `resolveVmTarget` now checks the leaf first via the RCA #12
`resolveLeafVenue` helper — R3 payoff one round later, consuming the shared mechanism
instead of adding a fourth independent implementation. A leaf that resolves as a vm
sets `vmName` from the leaf's own `From` (falling back to the existing bare-key
behavior when `From` is empty, matching the undotted case's shape); `domainID` stays
keyed off the full path. Falls through unchanged to the existing root-only branch when
the leaf doesn't resolve as a vm, preserving the `check-arch-vm.arch-host`
delegate-into-guest precedent. (`nestedLeaf` ALSO needed setting here for the leaf-is-
vm case, not left nil — RCA #14, immediately below, found and fixed the reason: leaving
it nil routed `loadVmCheckPlans` through an ambiguous fallback scan.)

Independently, `sdk/vmshared/cloud_init_render.go`'s `resolveCloudInitSSHUser` gained a
defensive `nil` guard regardless of the entity-resolution fix — a check command must
never SIGSEGV on any input. Exported via the `ResolveCloudInitSSHUser` var-alias
(`sdk/vmshared/exports.go`), consumed by three charly call sites
(`bundle_add_cmd.go`, `check_cmd.go`, `vm_lifecycle_preresolve.go`); one guard covers
all three (R3). The other two call sites feed an already-resolved `*VmSpec` from a
different path (not `resolveVmTarget`), so they were never exposed to finding #1 and
only gain the defensive hardening.

A third finding surfaced mid-trace (`deploykit.FindVmDeployNode`'s ambiguous top-level
fallback scan) was INITIALLY registered to the bed-robustness batch as latent/harmless
— that survey was wrong (bed 8 fired it for real) and the registration was REVOKED; see
"RCA #14" below, which fixes it in-unit and registers a NARROWER, genuinely-still-latent
fourth finding in its place.

## Verification

`go build`/`go vet`/`go test` across `charly` core, `sdk` (GOWORK=off), every touched
plugin module (`plugin-bundle`, `plugin-adb`, `plugin-kube`, GOWORK=off for the
out-of-process ones), and `candy/plugin-substrate` (a compiled-in `go.work` member,
built/tested via the workspace path from `charly/` — not `GOWORK=off` in its own dir) —
all green. `task cue:gen` — no-op on generated Go for the new validation-only
`#VmDeployState.ephemeral` sub-def (confirmed by direct diff inspection). `task
build:binary` clean, `go.work.sum` untouched. `charly box validate` — exit 0, zero
warnings. `gofmt -l` — clean across every touched file in both repos. New Go coverage:
`TestCheckVmTarget`/`TestCheckLocalTarget` extended with the leaf-vm-under-pod shape
(this cutover's new exposure), the preserved root-vm-with-guest-suffix shape, and the
local-leaf-under-pod mirror (RCA #12); `TestResolveVmTarget_LeafVmUnderNonVmParent`
(both shapes, updated for RCA #14's nestedLeaf population) plus
`TestResolveCloudInitSSHUser`'s new nil-spec case (RCA #13, sdk);
`TestFindVmDeployNode_AmbiguousFallbackErrors` (RCA #14, sdk — 5 cases: the ambiguous
2-candidate error, a unique fallback match, an exact-key match unaffected by ambiguous
siblings, no-match, and nil-map); `TestVmEntityForPrepare` (M4b, `candy/plugin-deploy-vm`
— 7 cases, the module's first test file). `go mod tidy` on `candy/plugin-deploy-vm`
picked up the new `sdk/deploykit` transitive import (`golang.org/x/term`) — minimal
`go.mod`/`go.sum` diff, confirmed via `git diff --stat`. Runtime: the `check-sidecar-pod`
disposable bed (ephemeral vm member register/teardown round-trip + its own per-member
check-live hop, now reaching the SSH user resolution without panicking AND never
borrowing an unrelated bed's plan) proves the ephemeral-register HostBuild seam +
M4b's PrepareVenue self-resolution; `check-charly-vm` proves the plain (non-ephemeral,
non-nested) vm lifecycle regression; `check-k8s-deploy` proves the F6 mechanism itself.
All three required for the R10 gate — see the integrating PR for pasted bed output.


---

# Unit 6b narrative

## What this unit is

Cutover B unit 6b was scoped to build the `InvokeProvider` lazy-connect
enabler (the plugin↔plugin peer-dispatch generalization the FINAL/K5 IOU
register named as blocking the arbiter/credential/bed-session/vm-backend
family) and land the consumer moves it unblocks. Over the course of scoping
and landing, every prospective plugin-to-plugin consumer either dissolved
to ~0 collectible (arbiter/preempt), stayed core by construction
(k3s_post.go/vm_backend_lifecycle.go, once their actual blocker — a direct
`LoadUnified` call the original scoping map missed — was traced and fixed a
different way), or turned out to already be core-calling-sdk rather than
plugin-calling-plugin (the credential family, check_bed_run.go). The
enabler itself was BUILT and bed-verified (`check-sidecar-pod` 12/12,
proving the mechanism change is inert for every existing caller), but with
zero landed plugin-to-plugin cold-path callers in this unit's final scope,
it shipped a capability nothing exercises and no test could fail on — the
same speculative-surface class this cutover rejected elsewhere (a
CUE field, a general root-Plan filter). **Reverted before landing** (see
below); the enabler is registered in the FLOOR-SLIM/InvokeProvider-family
register instead, to be built together WITH its first real consumer and
the cold-path spike, never dormant.

What this unit actually delivers is the credential-family and
bed-session R3 consolidation (portable sdk/deploykit orchestration behind
thin core wrappers), a confirmed dead-code deletion, and a corrected
FLOOR-SLIM scoping register for the arbiter/preempt family — each
independently verified and landable on its own merits.

### The InvokeProvider lazy-connect enabler — BUILT, BED-VERIFIED, then REVERTED (dormant)

`charly/plugin_dispatch_reverse.go`'s `InvokeProvider` (the host-side
plugin↔plugin dispatch broker) did a plain `providerRegistry.resolve(class,
word)` and hard-failed if the target wasn't already registered — asymmetric
with `connectPluginByWordRef`'s existing 3-tier lazy-connect fallback
(already-registered → baked binary → scoped project-source build) that every
host→plugin adapter (`credential_plugin.go`, `tunnel_plugin.go`, …) already
enjoyed. The fix (falling InvokeProvider through the SAME
`connectPluginByWordRef` chain on a registry miss) was implemented and
verified via a disposable-bed regression run (`check-sidecar-pod`, 12/12,
exit 0) proving the mechanism change is behaviorally inert for every
EXISTING caller. It was then REVERTED, in this same branch, once the final
consumer landscape confirmed no plugin-to-plugin cold-path caller exists in
this unit's scope — see "What this unit is" above. `plugin_dispatch_reverse.go`
is unchanged from its pre-unit-6b state in the final landed tree.

### Credential family (enc.go + secrets.go → sdk/deploykit)

The last remaining credential-store-coupled orchestration in `charly/enc.go`
and `charly/secrets.go` (`resolveEncPassphrase*`/`ProvisionPodmanSecrets`/
`CollectCandySecretAccepts`/`resolveHookSecretEnv`/`generateAndStoreSecret`)
moves to `sdk/deploykit` (`secret_provision.go` + `enc_passphrase.go`), taking
an injected `deploykit.CredentialAccess{Resolve, Write}` instead of calling
charly-core's `ResolveCredential`/`DefaultCredentialStore` by name — mirroring
the EXISTING Cutover B-1 `secret_probe.go`/`enc_probe.go` precedent exactly
(R3: charly-core's thin wrapper and any future out-of-process caller share ONE
implementation). `resolveEncPassphraseForMountWithResolver`/`retryUnavailable`/
`encNotStoredError` moved VERBATIM — they were already pure closures with zero
core dependency. `envVarNameToPodmanSecretSlug` (its sole caller was
`CollectCandySecretAccepts`) moved with it, deleted from `charly/validate.go`.

What stays core-only, by design: `encExecViaPlugin` (`providerRegistry.resolve`
— clause-M) and `awaitKeyringUnlockViaPlugin` (needs the concrete
`pluginCredentialStore`/`credentialAwaiter` — threaded into the deploykit
function as an injected `waiter` closure, the same pattern the pre-existing
resolver/reset closures already used).

Test relocation: `enc_resolve_mount_test.go` (pure closure tests of
`resolveEncPassphraseForMountWithResolver`) moved verbatim to
`sdk/deploykit/enc_passphrase_test.go`; `TestEnvVarNameToPodmanSecretSlug`
moved to `sdk/deploykit/secret_provision_test.go`. The EXISTING extensive core
tests (`secrets_test.go`, `hook_secret_test.go`) needed ZERO changes — they
call the core-level entry points, which now delegate to deploykit but exercise
the identical behavior through the fake-credential-store test harness (real
regression coverage of the refactor, not a compile check).

### vm_snapshot_client.go — DIE, not a move

`charly/vm_snapshot_client.go` (the `verb:vm` `snapshot-internal` RPC wrapper —
`createInternalSnapshot`/`deleteInternalSnapshot`/`revertInternalSnapshot`/
`promoteInternalToExternal`/`createExternalSnapshot`/`deleteExternalSnapshot`/
`revertExternalSnapshot`/`toWireSnapshotOpts`/`toWireSnapshotEntry`/
`vmSnapInternal`) was DEAD CODE: its only reference was the 7
`vmshared.CreateInternalSnapshot = createInternalSnapshot` (etc.) hook
registrations in `vmshared_aliases.go`'s `init()`, and NOTHING in charly-core
calls the `vmshared` orchestration entry points those hooks feed
(`vmshared.CreateSnapshot`/`DeleteSnapshot`/…) — those are called ONLY from
`candy/plugin-vm`'s OWN aliases, feeding its OWN CLI commands, which ALSO
register the SAME `vmshared` hook vars to ITS OWN real go-libvirt
implementations. Deleted the file (93 LOC) and the 7 hook-registration lines.
Proven live (not merely by absence of a build/test failure): a full
`charly vm create` → `vm snapshot create` → `vm snapshot list` → `vm snapshot
revert` → `vm snapshot delete` → `vm destroy` round-trip against a disposable
VM, exit 0 throughout, confirming plugin-vm's own implementations serve the
CLI end-to-end with the core hooks gone.

### check_bed_run.go → sdk/deploykit/bed_session.go

Every caller of `charly/check_bed_run.go`'s functions was ALREADY core-only
(`host_build_check_bed.go`, `bundle_members.go` — no plugin calls them
directly today), so `persistBedDeployOverrides`/`deployNestedLocalChildren`/
`waitForVmSshReady`/`waitForContainerReady`/`bedCheckLevel`'s classifier moved
to `sdk/deploykit/bed_session.go` — the same "portable orchestration in sdk,
thin core call sites" pattern. The ONE genuinely provider-registry-coupled
classification, `bedExternalInPlace` (queries the live deploy-substrate
registry), stays core-side and is threaded into
`deploykit.PersistBedDeployOverrides` as a plain `bool` parameter — a
deliberate choice over adding a new wire-request field, since there is no
cross-process consumer of this data today (adding wire surface for a consumer
that doesn't exist is the "seam a later wave deletes" anti-pattern). The
former `HostRooted` trait reads (`nodeTraits(&node).HostRooted`) — themselves
only a provider-registry FALLBACK for an unstamped node — now read
`node.Descent.HostRooted` directly: every node these functions see comes from
a `LoadUnified`'d project, whose loader always stamps `Descent`
(`stampBundleDescents`), so this is a pure wire-field read needing no
threaded parameter and no registry access.

### preempt.go / arbiter_host.go — re-scoped to ~0 collectible, documentation fixed

A line-level call-graph trace of every `preempt.go` proxy function found EVERY
one keeps at least one caller among `{substrate_lifecycle_grpc.go,
host_build_pod_lifecycle_dispatch.go}` — both explicitly pinned core-side by
the Cutover B-1 unit-2 IOU #4 ruling (arbiter-release brackets gated on
host-process `CHARLY_PREEMPT_LEASE` env state a placement-agnostic plugin
cannot own) — or is inherent `HostArbiter` reverse-channel infrastructure
(`arbiter_host.go` + the 7 host-seam impls in `preempt.go`, two of which
additionally call `LoadUnified` directly, a second independent K1-permanent
reason per R-E2). Net: `preempt.go`+`arbiter_host.go`'s combined ~588 LOC
contributes ~0 to this cutover's collectible total — reassigned to FLOOR-SLIM
candidate-floor per-file adjudication, NOT self-ratified permanent. Fixed
`preempt.go`'s header/doc comment, which named a stale consumer list
(`check_bed_run.go, start.go, vm.go, commands.go, vm_gpu_cmd.go` / "~6
consumers") — none of those files call these functions anymore — with the
real, current 3-caller set (R1, claim-keyed).

### k3s_post.go + vm_backend_lifecycle.go — consolidated onto unit 6a's seam (not a LOC reduction)

Both files' last direct `LoadUnified` call (`k3s_post.go`'s `deployVMForwards`,
`vm_backend_lifecycle.go`'s `vmConfiguredBackend`) now resolve through unit
6a's generic `"deploy-entity-resolve"` host-builder
(`host_build_deploy_entity_resolve.go`) instead of duplicating the lookup
inline: `deployVMForwards` does the bundle→`.Node.From`→vm two-hop,
`vmConfiguredBackend` does the vm one-hop, both reading the resulting
`spec.ResolvedVm` fields (`Network.PortForwards` / `Backend`). Both call the
host-builder function DIRECTLY, in-process (core calling core — no
`HostBuild`/`Executor` round trip, unlike a plugin consumer such as
`candy/plugin-adb/preresolve.go` or `candy/plugin-kube/preresolve.go`, which
reach the same seam over the reverse channel).

**This is R3 consolidation (one source of entity-resolution truth), not a
core-LOC reduction** — the net is a small INCREASE (+17 combined: k3s_post.go
168→180, vm_backend_lifecycle.go 380→385) from the added seam-call plumbing,
replacing an equivalent amount of inline `LoadUnified`/`FindBundleNode`/
`uf.VM[name]` logic. This corrects an earlier assumption in this unit's own
scoping notes that clearing the `LoadUnified` blocker would make these
functions candidates for the sdk/deploykit portable-orchestration move (the
credential-family/check_bed_run.go pattern) — it does not, because the
function they now call (`hostBuildDeployEntityResolve`) is itself
inherently core-only (it calls `LoadUnified`/the provider registry), so
`deployVMForwards`/`vmConfiguredBackend` remain core by construction, just
de-duplicated onto the shared seam rather than carrying their own copy of
the lookup.

**The "cold-path InvokeProvider spike" (declared in this unit's original
sequencing plan to ride "the first wired consumer") does not materialize
within this unit's landed scope.** Every consumer this unit actually wired
turned out to be core-calling-sdk (the credential family, check_bed_run.go)
or core-calling-a-core-host-builder-function (k3s_post.go,
vm_backend_lifecycle.go) — none is a PLUGIN reaching a not-yet-loaded PEER
plugin via `InvokeProvider`, the specific cold-path the spike was meant to
exercise. The enabler itself is verified (the `check-sidecar-pod` disposable-bed
regression, 12/12, exit 0, proving the mechanism is inert for every existing
caller) but its NEW capability (peer-to-peer lazy-connect) has no in-tree
exerciser yet inside this unit — flagged for the FLOOR-SLIM register as an
open item, not silently dropped.

### checkrun_act.go — excluded from this unit's scope

`checkrun_act.go`'s `runProvisionAct` is a METHOD on `hostVerbResolver`
(`planrun_adapter.go`), a type its OWN header documents as permanent core
("the ONE thing the walk needs from the core provider registry... STAY in
core"). A method on a permanently-core type cannot move independently of that
type. The file's other 3 functions trace to the same or sibling
floor-adjacent files (`plugin_executor_reverse.go`, `deploy_target_external.go`).
Excluded from the "bed-session" family entirely rather than partially split.

## Measured core LOC delta (non-test `.go`, `git show <base>:<f>` vs merged)

| file | before | after | delta |
|---|---:|---:|---:|
| `charly/check_bed_run.go` | 218 | 87 | −131 |
| `charly/enc.go` | 409 | 259 | −150 |
| `charly/secrets.go` | 340 | 68 | −272 |
| `charly/validate.go` | 394 | 386 | −8 |
| `charly/vmshared_aliases.go` | 102 | 95 | −7 |
| `charly/vm_snapshot_client.go` | 93 | 0 (deleted) | −93 |
| `charly/plugin_dispatch_reverse.go` | 146 | 146 | 0 (built, bed-verified, then REVERTED — dormant, see register) |
| `charly/preempt.go` | 430 | 440 | +10 (doc fix only) |
| `charly/k3s_post.go` | 168 | 180 | +12 (seam consolidation, not a reduction) |
| `charly/vm_backend_lifecycle.go` | 380 | 385 | +5 (seam consolidation, not a reduction) |
| **Net (unit 6b alone)** | | | **−634** |

Lands combined with unit 6a's own measured delta as one FINAL/K5 cutover total.

## FLOOR-SLIM / candidate-floor register updates (this unit's contribution)

- **InvokeProvider lazy-connect enabler** (`plugin_dispatch_reverse.go`) — built
  and disposable-bed-verified this unit (`check-sidecar-pod` 12/12, mechanism
  inert for every existing caller), then REVERTED before landing: this unit's
  final consumer landscape contains ZERO plugin-to-plugin cold-path callers,
  so shipping the mechanism alone is speculative surface with no test that
  could fail on it. Registered as ONE FLOOR-SLIM/InvokeProvider-family item —
  "lazy-connect + its first real plugin-to-plugin consumer + the cold-path
  spike" — to be built together the next time a genuine consumer exists,
  never dormant.
- `preempt.go` (430) + `arbiter_host.go` (158) — candidate-floor,
  per-file adjudication at FLOOR-SLIM; NOT ratified permanent by this unit.
- `checkrun_act.go` (136) — excluded (method on permanently-core
  `hostVerbResolver`); not registered as a FLOOR-SLIM candidate, it is core by
  construction, not by unresolved difficulty.
- k3s_post.go/vm_backend_lifecycle.go's `LoadUnified` blockers (undetected by
  the original scoping map) are CLOSED — both now consume unit 6a's generic
  `"deploy-entity-resolve"` seam directly, in-process; no further register
  entry needed for these two files.

## Verification (this unit's own gate)

- `task build:binary`: clean at every step, including the final merged
  (unit 6a + unit 6b) tree.
- sdk: `GOWORK=off go build/vet/test ./...` — all packages ok, on the merged
  sdk tree (unit 6a's `feat/final-k5-6a` merged into this unit's sdk branch).
- charly: `go build ./...`, `go vet ./...`, `gofmt -l .` — clean, on the
  merged tree.
- charly: `go test ./...` — full suite green, ~98-108s per run, at every
  checkpoint (credential family, vm_snapshot deletion, check_bed_run
  relocation, the merge, and the k3s_post.go/vm_backend_lifecycle.go seam
  consumption).
- Disposable-bed regression (enabler): `check-sidecar-pod` 12/12, exit 0.
- Live VM-lifecycle probe (vm_snapshot DIE): `charly vm create` → `vm snapshot
  create/list/revert/delete` → `vm destroy`, exit 0 throughout.
- R5 grep self-test: zero remaining references to any deleted symbol name or
  the stale preempt.go consumer list, anywhere in `charly/`.

## sdk-contract counterpart

`github.com/opencharly/sdk`, branch `feat/final-k5-6b` (this unit) — new files
`deploykit/secret_provision.go`, `deploykit/enc_passphrase.go`,
`deploykit/bed_session.go`, plus their relocated tests, on top of unit 6a's
`feat/final-k5-6a` (merged into this branch — the seam types
`spec.DeployEntityResolveRequest`/`Reply` this unit's k3s_post.go/
vm_backend_lifecycle.go consume). Lands FIRST (B2 producer order), combined
per the unified FINAL/K5 landing.

---

## Combined integration (unit-6 integrator, final assembled tree)

Consolidates `feat/final-k5-6a` (3 commits: 87d64cf/245695d/46fcb5c, sdk
563836c) and `feat/final-k5-6b` (6 commits ending in the revert 007075d/
c6a82eb, sdk ced6e9a — which already merged 6a's sdk leg) into one landing
branch `feat/final-k5` (superproject + sdk), assembled on the shared c78ac6a
base per the proven B-1 integration pattern. The two units' remaining
deltas (6a: 20 files past the shared 87d64cf point; 6b: 5 files past its
own merge of 6a) share ZERO files — the merge was mechanically clean, no
conflicts. `origin/main` had advanced one commit past c78ac6a in the
interim (`5512225` — a 5-distro gitlink restamp + go.work.sum fix, the
orchestrator's own #170 leg) — merged in once at the end (empty-overlap
delta re-gate, orchestrator-confirmed): zero touch to `charly/`/`sdk/`/
`candy/`, gitlinks verified to resolve FORWARD to `main`'s descendant pins.

**Generated artifacts regenerated, never hand-merged**: `task cue:gen` —
clean no-op (zero diff) on the merged tree, confirming both units' CUE work
was already committed and mutually consistent. `task build:binary` —
clean, `charly/plugins_generated.go` regenerated, `go.work.sum` untouched.

**Two real findings surfaced by this integration's own `golangci-lint`
gate (both confirmed, via a `main`-baseline run, to be genuine regressions
introduced by units 6a/6b — `main` itself is zero-issue on `gocyclo`+
`unused`) — fixed in this integration commit, not deferred:**

1. **`config.go`'s `ResolveBox` exceeded the `gocyclo` ceiling (31 > 30)** —
   pushed over by unit 6a's own K3-envelope DI refactor (the `opts.DistroCfg`/
   `BuilderCfg`-vs-load branch). Fixed by extracting the branch into its own
   `resolveBuildConfigTriple` helper (mirroring `ResolveAllBox`'s identical
   caller-supplied-vs-load pattern already in this file) — a pure
   mechanical extraction, `ResolveBox`'s own behavior is byte-identical.
2. **`registerDeployPreresolver` was genuinely DEAD CODE (`unused`)** — an
   R5 leftover unit 6a's own hard cutover should have deleted: it was the
   COMPILED-IN preresolver registration path for k8s/android, and unit 6a's
   F6 move relocated BOTH k8s's and android's preresolve bodies onto the
   WIRE-BACKED path (`registerPluginDeployPreresolver`) — so nothing calls
   the compiled-in registrant anymore (confirmed zero real callers repo-wide
   by grep; only doc-comment analogy references remained). Deleted the dead
   function; simplified `registerPluginDeployPreresolver`'s now-vacuous
   compiled-in-shadow guard (the `deployPreresolvers`/`pluginPreresolverWords`
   maps stay — still the live, sole plugin-registration path); corrected the
   doc comment's stale "k8s/android are compiled-in" framing (R1 claim-keyed
   fix). **R5 cascade caught by the same sweep**: 4 OTHER files
   (`deploy_substrate_lifecycle.go`, `plugin_dispatch_reverse.go`,
   `pod_lifecycle_dispatch.go`, `step_emit_hostbuild.go`) cited
   `registerDeployPreresolver` as an analogy example in their own
   "registered at init-time, race-free, like X" doc comments — each now
   stale since the deletion; corrected to cite a still-live analogous
   registrant (`registerDedicatedBuiltin`/`registerStepEmitter`/
   `registerHostBuilder`) instead of dropping the pattern reference entirely.

**A third, benign finding**: `candy/plugin-kube`'s standalone `go mod tidy`
(the sanctioned per-module mechanism, matching the `plugin-feature`/`x/term`
precedent unit 6a's own narrative already documents) added one missing
`golang.org/x/crypto v0.49.0` checksum pair to `go.sum` — confirmed a real,
already-in-the-build-list transitive dependency (`sdk/kit`'s `x/crypto/ssh`
import, reached via `plugin-kube`'s `sdk/kit` dependency), zero `go.mod`
diff, zero behavior change — a go.sum completeness gap, not a version bump.

### Combined verification (final merged + fixed tree)

- `task cue:gen`: clean no-op (zero diff).
- `task build:binary`: clean, both before and after the two lint fixes;
  `go.work.sum` untouched throughout.
- `charly`: `go build ./charly/...` / `go vet ./charly/...` / `gofmt -l` —
  clean (verified via the workspace path — `charly` is a WORKSPACE MEMBER,
  not a standalone module; a standalone build fails ONLY on `candy/plugin-*`
  imports, the documented architectural fact, confirmed and not "fixed").
- `charly`: `go test -count=1 ./charly/...` — full suite green, ~95-97s,
  re-run clean AFTER both lint fixes landed.
- `charly`: `golangci-lint run ./...` — 0 issues (was 2, both fixed; `main`
  baseline confirmed 0, proving both were real regressions from 6a/6b, not
  pre-existing).
- `sdk`: `GOWORK=off go mod tidy` — clean no-op; `go mod verify` — all
  modules verified; `GOWORK=off go build ./...` — clean; `GOWORK=off go test
  -count=1 ./...` — full suite green including `TestGenReproducible` (run
  individually, PASS); `sdk origin/main` confirmed unchanged at `e23a373`
  (the units' shared sdk base) — no external sdk advance to reconcile.
- Standalone out-of-process plugin modules (`plugin-adb`, `plugin-deploy-vm`,
  `plugin-kube` — NOT `go.work` members): each `GOWORK=off go mod tidy` (only
  `plugin-kube` produced a diff, the benign x/crypto checksum above) + `go mod
  verify` (all verified) + `go build ./...` (clean) + `go test -count=1 ./...`
  (all green) + `GOWORK=off golangci-lint run ./...` (0 issues each).
- Compiled-in workspace-member plugin modules (`plugin-box`, `plugin-bundle`,
  `plugin-feature`, `plugin-substrate`): `golangci-lint run ./...` via the
  workspace — 0 issues each (built/tested via `task build:binary` +
  `go test ./charly/...` above, per the workspace-member verification class).
- `charly box validate`: exit 0, zero warnings — before AND after the lint
  fixes.
- R5 grep self-test: swept both units' deleted-identifier sets (files:
  `ephemeral_lifecycle.go`, `android_deploy_cmd.go`,
  `android_deploy_preresolve.go`, `k8s_deploy_preresolve.go`,
  `vm_snapshot_client.go`; symbols: `lifecyclePrepareHook`/
  `registerLifecyclePrepareHook`/`lifecyclePrepareHookFor`/
  `lifecyclePrepareHooks`/`vmEntityForAdd`/`createInternalSnapshot`/
  `deleteInternalSnapshot`/`revertInternalSnapshot`/
  `promoteInternalToExternal`/`registerDeployPreresolver`) — every surviving
  hit is either a legitimate past-tense "the deleted/former X" explanatory
  comment, or (verified via `git log`/`git show main:...`) a same-named but
  genuinely UNRELATED, pre-existing symbol in a different module
  (`candy/plugin-box/validate_rules.go`'s own `envVarNameToPodmanSecretSlug`,
  present on `main` since c78ac6a, unrelated to unit 6b's `charly/validate.go`
  → `sdk/deploykit` move) — zero live stale references. The
  `registerDeployPreresolver` cascade (4 files) found by this same sweep is
  fixed above, not merely noted.
- Core LOC delta (ledger method: non-test `.go` line count under `charly/`):
  **main 40,328 → integrated HEAD 39,209 = −1,119** — matches the
  documented ≈−1,100 sizing-decision estimate (6a −485 + 6b −634, minus
  overlap), variance (+19) fully explained by this integration's own two
  mechanical lint fixes (a small helper-extraction net-add, a small
  dead-code net-remove).
- Content-marker proof (binary carries the RCA fixes, not just the version
  stamp): `strings bin/charly | grep -c 'ensureEphemeralBundleConfig\|
  splitVmAddress\|resolveLeafVenue'` → 4 matches.

### R10 acceptance roster (orchestrator-run, disposable, all concurrent, final tree)

The six-bed roster the orchestrator selected — this integration's own required
three (each unit's own narrative names them as necessary to prove its claim)
plus three recommended for the shared classification/dispatch/persistence
paths this cutover touches (RCA #12-#14's own repro topologies) — ran
CONCURRENTLY at full parallelism against this exact final commit, per the
CLAUDE.md high-load concurrency mandate. Verbatim orchestrator verdict:

**ALL SIX GREEN, all concurrent, all exit 0** — `check-sidecar-pod` PASS
(steps=17) · `check-charly-vm` PASS (steps=8) · `check-k8s-deploy` PASS
(steps=10) · `check-group` PASS (steps=7) · `check-substrate` PASS
(steps=10) · `check-builder-vm` PASS (steps=10). Zero warnings.

The R10 gate is complete on this tree.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)


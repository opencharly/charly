# 2026.203.1600 — FLOOR-SLIM-proper Unit-8B — resource-arbiter holder-dispatch K4-exit (core-minimization)

Unit-8's own immediate follow-on (Unit B), landing separately per the
Cutover Sizing Law's decomposition path: Unit A's crossroad review found 4 of
6 originally-scoped items carried genuine new architectural blockers, while
this item — `charly/arbiter_host.go` + `charly/preempt.go`'s 6 non-K1-blocked
resource-arbiter host-seam implementations — was TRACTABLE, just large enough
to deserve its own careful pass: a dedicated design/spike investigation for
the GPU/VM holder start/stop paths, then the move, then its own R10.

## What moved

- **The 6 movable arbiter host-seam implementations** (`running` /
  `stop`+wait / `start` / `switchMode` / `ensureCDI` / `gpuCDI`) move from
  `charly/preempt.go` + `charly/arbiter_host.go` + `charly/gpu_shim.go` into
  `candy/plugin-preempt/holder_dispatch.go` (NEW file). Each was reached over
  the `ExecutorService.HostArbiter` reverse channel only because its
  ORIGINAL implementation used charly-core-PRIVATE mechanisms
  (`providerRegistry.resolve` / `connectPluginByWordRef`) — never because
  the work itself needed a live `LoadUnified` project:
  - `holderRunning`/`vmIsRunning`/`podIsRunning` → `pluginHolderRunning`,
    discriminating venue via the ALREADY-ON-THE-WIRE `spec.HolderAddr.Vm`
    field (populated host-side only for a vm-venue holder) instead of the
    core-private `deployTraitDescent(addr.Target).Venue == "ssh"` registry
    call — pure data, zero registry coupling, byte-identical routing.
  - `holderStop`+`waitStoppedHost` → `pluginHolderStop`, folding the
    stop-dispatch and the wait-until-actually-stopped poll into one plugin
    function. The wait gate uses `kit.ReadinessProvider()` — the SAME
    project-aware resolver charly-core's own `init()` injects
    (`readiness_config.go`) — directly, with **zero new HostBuild seam**:
    `candy/plugin-preempt` is `compiled_plugins:` (same OS process), so the
    package-level DI var is already correctly set by the time any
    compiled-in plugin runs.
  - `holderStart`+`holderExists` → `pluginHolderStart`+`pluginHolderExists`
    (the departed-holder no-op-success guard ported verbatim — a stranded
    lease would otherwise never clear).
  - `gpuSwitchModeTolerant`/`ensureCDIRoot` (`charly/gpu_shim.go`) →
    `pluginSwitchMode`/`pluginEnsureCDI`, dispatching `verb:gpu` directly via
    `sdk.Executor.InvokeProvider(ctx, "verb", "gpu", sdk.OpRun, …)` — F10's
    class-agnostic plugin↔plugin dispatch, the SAME mechanism Unit-8A's
    builder pre-pass move already spike-proved live; a second spike was
    judged unnecessary (the mechanism's class-genericity is proven at the
    code level: `plugin_dispatch_reverse.go`'s `InvokeProvider` handler
    performs zero branching on the `class` value).
  - `gpuCDIHolders`/`devicesHoldNvidiaGPU` (`charly/arbiter_host.go`) →
    `pluginGpuCDIHolders`, using `sdk/enginekit.NewEngineClient(...).
    SnapshotAll(false)` directly — already plugin-importable, zero registry
    coupling, so this seam never needed the host round-trip at all.
  - The VM-plugin dispatch (`invokeVmPlugin`/`invokeVmPluginEnv`,
    `charly/vm_plugin_client.go`) gets its plugin-side twin
    (`invokeVmPluginOp`), using `exec.InvokeProvider(ctx, "verb", "libvirt",
    sdk.OpRun, nil, envJSON, sdk.InvokeProviderOpts{ExtraRef:
    vmPluginCandyRef()})` — `vm_plugin_client.go`'s own header comment had
    ALREADY documented this exact move as "a SEPARATE FINAL/K5 IOU gated on
    the InvokeProvider generalization" (a pre-existing, tracked, gated IOU
    this cutover now closes).

- **`candy/plugin-preempt/arbiter.go`'s `newArbiter`** rewired: `gather`/
  `resources` stay on the HostArbiter reverse channel (the 2 genuinely
  K1-blocked seams, both `LoadUnified`-coupled); `running`/`stop`/`start`/
  `switchMode`/`ensureCDI`/`gpuCDI` now call the new plugin-local functions
  directly.

## What was deleted (R5 hard cutover — dead-code cascade)

Moving the 6 seams left an entire cluster of code with no remaining caller,
deleted in the same change (confirmed via `golangci-lint`'s `unused` check +
a whole-repo `git grep`, not assumed):

- `charly/preempt.go`: `holderRunning`/`holderStop`/`holderStart`/
  `holderExists`/`waitStoppedHost`/`vmIsRunning`/`podIsRunning`.
- `charly/arbiter_host.go`: the `running`/`stop`/`start`/`switchMode`/
  `ensureCDI`/`gpuCDI` dispatch arms + `gpuCDIHolders`/
  `devicesHoldNvidiaGPU`; `dispatch`'s now-unused `params []byte` parameter
  dropped (both remaining seams take no request payload).
- `charly/gpu_shim.go`: `gpuSwitchReply`/`switchReplyErr`/
  `gpuSwitchModeTolerant`/`ensureCDIRoot`/`errGPUSwitchWedged` + the
  now-unreferenced `gpuModeVfio`/`gpuModeNvidia`/`hostDriverDisplay`/
  `hostDriverAudio`/`hostDriverVfio` consts (every DRIVER-SWITCH consumer —
  `charly vm gpu` in `candy/plugin-vm`, the arbiter in `candy/plugin-preempt`
  — now dispatches `verb:gpu` peer-to-peer; no in-core driver-switch shim
  remains). `normalizePCIVendor`/`selectGPUByVendor`/`nvidiaVendorID` stay
  (genuinely still consumed by `gpu_allocate.go`/`gpu_imply.go`).
- `charly/vm_plugin_client.go`: `vmPluginOpError`/`vmPluginOpFlag` (their
  only caller, `preempt.go`'s deleted `holderStop`/`holderStart`, moved to
  the plugin; the surviving `invokeVmPlugin`/`invokeVmPluginEnv`/
  `vmPluginCandyRef` stay live for `check_endpoint_resolve.go`/`ssh.go`).
- `charly/vm_backend_lifecycle.go`: `vmName`/`startVM`/`stopVM` (the dead
  cascade this move's only caller left behind — `resolveVmBackend`/
  `vmConfiguredBackend`/`startLibvirtUserSession`/`resolveSSHPubKey`/
  `containerSSHKeyDir`/`generateSSHKeypair` stay, each confirmed still called
  by a genuinely different consumer: `host_build_config_resolve.go`,
  `bundle_members.go`/`check_bed_run.go`, and the container SSH-key sites in
  `config_image.go`/`vm_cloud_image.go`). **`vmDir` itself STAYS** — a full
  re-check against the merged tree (this cutover rebased onto the
  just-landed bed-robustness batch, charly#176) surfaced a genuinely live
  second caller, `host_build_vm_build.go`'s `hostBuildVmBuild` (the qcow2-VM
  state-dir seam), that an earlier pass of this analysis missed before the
  rebase. `vmDir` now routes through `vmshared.VmStateRoot()` — the SAME
  `CHARLY_VM_STATE_DIR` worktree-scoping override bed-robustness's batch
  had just unified every other VM-state path onto, matching that pattern
  instead of reintroducing an un-scoped literal. `candy/plugin-preempt`'s
  own `vmDirPlugin` (`holder_dispatch.go`) is updated to match symmetrically
  (also routes through `vmshared.VmStateRoot()`), so both the host's and the
  plugin's VM-state-dir resolution honor the same override consistently —
  a genuine correctness fix surfaced by the rebase, not by the original
  design pass.
- `charly/vm_qemu_client.go` (DELETED entirely — `qemuGracefulShutdown`/
  `qemuForceShutdown`/`vmQemuShutdown`, whose only caller was the now-deleted
  `stopVM`) + `charly/vmshared_aliases.go`'s now-dead `killQemuByPID` alias.

**Second-order dead-code cascade, deleted in this SAME cutover (standing
operator directive: proven-dead code is deleted where found, never
"registered for later" — that path is reserved for ALIVE-but-misplaced
code):** deleting `vm_qemu_client.go` left `candy/plugin-vm/provider.go`'s
`"qemu-shutdown"` `VmOp` case with no remaining SENDER anywhere in the tree
(confirmed via `git grep 'VmPluginEnv{'` across `charly/`+`candy/`+`sdk/` —
zero constructors set `VmOp: "qemu-shutdown"`). Deleted: the `case
"qemu-shutdown":` block itself (`qemuForceShutdown`/`qemuGracefulShutdown`
THEMSELVES stay — `candy/plugin-vm/vm.go`'s own direct-QEMU stop/destroy CLI
path calls them directly, a genuinely live, separate caller), the now-dead
`StateDir` field on `candy/plugin-vm/provider.go`'s local `vmEnv` decode
struct (its only reader was the deleted case), and — since `StateDir` was
ALSO carried on the CUE-sourced `spec.VmPluginEnv` (`sdk/schema/vmclient.cue`,
already landed, not part of this PR's own conversion work) — the matching
`state_dir` CUE field, regenerated via `task cue:gen` in the paired sdk PR.
Plus 3 stale `plugin.go` doc-comment mentions of "qemu-shutdown" in its
supported-op enumerations (`git grep qemu-shutdown` confirmed clean
afterward, outside `CHANGELOG/` history). `qemu-shutdown`'s `force=true`
branch was already unreachable from any known caller before this cutover
(the arbiter always called with `force=false`) — this cutover is what made
the WHOLE case senderless, so the deletion belongs here, not a later batch.

## sdk wire-contract trim

`sdk/spec/arbiter_wire.go`'s `ArbiterSeamRunning`/`Stop`/`Start`/`Switch`/
`EnsureCDI`/`GpuCDI` consts + their request/reply types
(`ArbiterHolderReq`/`ArbiterBoolReply`/`ArbiterErrReply`/`ArbiterSwitchReq`/
`ArbiterSwitchReply`/`ArbiterGpuCDIReply`) deleted — confirmed unused
anywhere in the tree via `git grep` — leaving only `ArbiterSeamGather`/
`ArbiterSeamResources` + their reply types, matching the 2 seams that remain
genuinely host-coupled.

## Two real bugs the new VM-venue bed caught (both fixed here)

FLOOR-SLIM-proper Unit-8B's own ruling required a dedicated `disposable: true`
bed for the VM/GPU holder start/stop paths rather than accepting code-level
analysis alone (`box/fedora`'s new `check-preempt-vm-live`, its own PR).
Authoring and running it — the FIRST disposable bed anywhere in the
ecosystem to exercise the VM-venue arbiter holder path — surfaced two real,
PRE-EXISTING latent bugs in the arbiter's VM dispatch. Both were faithfully
ported from the ORIGINAL `charly/preempt.go` code (not introduced fresh by
this move) and had simply never been live-tested before this bed existed:

1. **Wrong VM identity.** `pluginHolderRunning`/`pluginHolderStop`/
   `pluginHolderStart`/`pluginHolderExists` computed the libvirt domain name
   from `addr.Vm` (the shared `vm:` TEMPLATE entity a deploy inherits via
   `from:`, e.g. `web-vm`) instead of the DEPLOY's own name (e.g.
   `preempt-vm-holder`) — so the arbiter tried to stop/start a domain that
   never existed under that name, and `holdersToStop` silently found nothing
   to preempt (`charly preempt status` showed an active lease with an EMPTY
   "PREEMPTED HOLDERS" column). Fixed with a new `vmDomainName(addr
   spec.HolderAddr) string` helper keying the domain off `addr.Name` via
   `vmshared.VmDomainIdentity` — matching the documented rule that a bed's
   libvirt domain is keyed by the DEPLOY name, never the shared `kind:vm`
   entity. `stopVMPlugin`/`startVMPlugin` simplified to take the resolved
   domain name directly instead of a `(box, instance)` pair each call site
   had to re-derive.
2. **Wrong existence signal.** `pluginHolderExists`'s VM branch tested
   whether the `domain-state` RPC round-tripped successfully (`ok`), not
   whether its JSON reply's `exists` field actually said the domain exists —
   `candy/plugin-vm`'s handler ALWAYS replies success (a `lookupDomain` miss
   is a normal, error-free "not found" answer, never an RPC failure), so a
   genuinely undefined domain (torn down by a sibling teardown step moments
   earlier) was never detected as departed, and the subsequent restore
   attempt failed hard on `domain not found` instead of taking the
   no-op-departed path — stranding the lease (`charly preempt restore`
   required to clear it manually). Fixed by decoding the reply's `exists`
   field, returning it directly when the RPC succeeds (its answer is
   definitive — no falling through to the state-dir fallback, which can
   false-positive on lingering disk artifacts `charly vm destroy` leaves
   behind without `--disk`).

Both are visible live in `check-preempt-vm-live`'s log: fix 1 is proven by
`bring-up-members` correctly identifying and stopping the running VM holder
(`preempt: stopping holder "preempt-vm-holder" to free vm-test-lock…` →
`…stopped — vm-test-lock freed…`); fix 2 is proven by
`rebuild-members-down`/`cleanup-members` correctly taking the "has departed
— nothing to restore" no-op path with ZERO "could not restore" errors,
across BOTH the initial teardown and the fresh-rebuild cycle's re-teardown.

## Third bug: a TOCTOU restore race in fix 2 itself (found by the validator's
independent bed re-runs, surfaced mid-review)

Fix 2 (the `exists`-field decode) closed the STALE-SIGNAL bug but left a
narrower, genuinely nondeterministic window: `pluginHolderStart`'s VM branch
was still check-then-act — `pluginHolderExists` (a `domain-state` RPC) THEN
`startVMPlugin` (a separate `start` RPC), with no atomicity between them.
`cleanup-members`/`rebuild-members-down` tear down the holder member's own
domain INDEPENDENTLY of the taker member's removal, which is what triggers
`restoreHolders`/`pluginHolderStart` — so the domain can be destroyed in the
WINDOW between the check and the act. Two apparently-identical
`check-preempt-vm-live` runs demonstrated this concretely: one run's
`cleanup-members` took the clean "has departed" no-op, another hit libvirt's
real `domain not found` as a HARD ERROR with the lease left STRANDED
(`charly preempt restore` required to clear it manually) — same code, same
scenario, different outcome depending on exactly when the race landed.

**The fix eliminates the window instead of synchronizing it** (R4: no
retry/sleep, no new lock — there is nothing left to synchronize once there
is only one call): `pluginHolderStart`'s VM branch now attempts
`startVMPlugin` UNCONDITIONALLY and folds the departed-holder detection into
the START RPC's OWN error reply. `candy/plugin-vm/provider.go`'s `start`/
`stop` VmOps already reply `{"error": "domain not found: " + err.Error()}`
for a missing domain (a `lookupDomain` miss is a normal, error-free RPC
reply, never a transport failure) — the new `isDomainNotFoundErr(err)`
classifies that reply text, and a match takes the SAME clean no-op path fix
2 already proved live. There is no longer a separate observation that can go
stale: whether the domain never existed, was already gone before this call,
or was destroyed a microsecond before libvirt processed the request, the
SAME error text comes back and gets the SAME correct handling. The
now-redundant VM-branch of `pluginHolderExists` (and its `domain-state`
pre-check) is deleted — `pluginHolderExists` keeps only the POD-venue check,
which has no equivalent race (this cutover's own confirmed-live evidence was
VM-specific; broadening to the pod path is not part of this fix).

Break-it-proven regression coverage
(`candy/plugin-preempt/holder_dispatch_test.go`, using a fake
`pb.ExecutorServiceClient` — the SAME test-double pattern
`candy/plugin-deploy-vm/lifecycle_test.go` already established, R3):

- `TestIsDomainNotFoundErr` — the pure classification, including
  `candy/plugin-vm`'s ACTUAL reply text for a missing domain.
- `TestPluginHolderStart_VmDomainNotFoundIsNoOp` — a fake `InvokeProvider`
  replying with exactly that "domain not found" text proves
  `pluginHolderStart` takes the clean no-op path (this is what the race
  used to sometimes miss).
- `TestPluginHolderStart_VmOtherErrorPropagates` — a genuine (non-missing-
  domain) libvirt failure still propagates as a real error, never
  mis-classified as departed and silently swallowed.
- **`TestPluginHolderStart_VmRace_DomainGoneBetweenCheckAndAct`** — the
  DISCRIMINATING regression test, added after the fresh `pr-validator`
  round correctly flagged the two tests above as empirically vacuous: an
  op-BLIND fake (one canned `InvokeProvider` reply regardless of which
  VmOp fired) cannot tell the pre-fix check-then-act shape from the fixed
  shape apart — both coincidentally take the same path against a single
  reply. The fix: `fakeVmInvokeProviderClient` is now OP-AWARE — it
  decodes `spec.VmPluginEnv.VmOp` from the request's `env_json` and
  replies differently per op — so the new test can reconstruct the ACTUAL
  race window: the `domain-state` op (the pre-fix pre-check) replies
  `exists:true` (the holder looks alive at check time), then the `start`
  op (the act) replies domain-not-found (destroyed in the window before
  the act). See "Proof the test discriminates" below for the paired
  old-shape-FAILS / new-shape-PASSES run.

## Test coverage

Real coverage for the MOVED code (not a rerun of Unit-8A's throwaway spike
test), new file `candy/plugin-preempt/holder_dispatch_test.go`:

- `TestPluginHolderStart_DepartedHolderIsNoOp` — relocated + adapted from
  `charly/arbiter_dispatch_test.go`'s deleted `TestHolderStart_
  DepartedHolderIsNoOp`, now exercising `pluginHolderStart`/
  `pluginHolderExists` directly (pod-venue branch, no live container or
  `*sdk.Executor` round-trip needed).
- `TestPluginHolderRunning_PodVenue` — the venue-discriminator routing for a
  plain pod holder.
- `TestDevicesHoldNvidiaGPU` — the pure CDI/device-node matcher (4 cases).
- `TestVmDomainName` — the regression guard for bug 1: two cases proving the
  domain is keyed by `addr.Name` (the deploy) even when `addr.Vm` (the
  shared template entity) differs, and that an instance-qualified deploy
  name folds correctly.
- `TestIsDomainNotFoundErr` / `TestPluginHolderStart_VmDomainNotFoundIsNoOp`
  / `TestPluginHolderStart_VmOtherErrorPropagates` /
  `TestPluginHolderStart_VmRace_DomainGoneBetweenCheckAndAct` — the
  break-it-proven regression coverage for the TOCTOU restore race (bug 3,
  above), including an OP-AWARE fake `pb.ExecutorServiceClient` proving
  `pluginHolderStart` no-ops on a domain-not-found start reply, still
  propagates a genuine failure, AND (the discriminating test) no-ops even
  when the pre-check op would have reported the holder alive — the exact
  race window the fix eliminates.

### Proof the test discriminates: old shape FAILS, new shape PASSES

Per the fresh `pr-validator`'s finding, the discriminating test was run
against a throwaway reconstruction of the PRE-fix code (`git archive
<pre-fix-commit>^ -- candy/plugin-preempt`, extracted to a scratch
directory outside the working tree, paired with the sdk commit that was
ACTUALLY gitlinked at that commit so the reconstruction compiles against
its contemporaneous wire types) — never shipped, proof-only:

```
=== RUN   TestPluginHolderStart_VmRace_DomainGoneBetweenCheckAndAct
    race_test.go:63: pluginHolderStart racing a domain destroyed between check and act must be a no-op success (else its lease strands forever); got: starting VM charly-preempt-vm-holder: starting VM charly-preempt-vm-holder: domain not found: Domain not found: no domain with matching name 'charly-preempt-vm-holder'
--- FAIL: TestPluginHolderStart_VmRace_DomainGoneBetweenCheckAndAct (0.00s)
FAIL
FAIL	github.com/opencharly/charly/candy/plugin-preempt	0.004s
```

And the SAME test body, run against the current (fixed) code in this PR:

```
=== RUN   TestPluginHolderStart_VmRace_DomainGoneBetweenCheckAndAct
--- PASS: TestPluginHolderStart_VmRace_DomainGoneBetweenCheckAndAct (0.00s)
PASS
ok  	github.com/opencharly/charly/candy/plugin-preempt	0.055s
```

This is the discriminating pair the validator's finding required: the
test genuinely distinguishes the bug from the fix, rather than passing
coincidentally against both shapes.

`charly/arbiter_dispatch_test.go`'s `TestArbiterExternalizedDispatch_
AcquirePersistsAndSurfaces` (the dispatch + reverse-channel + persistence
integration witness) is UNCHANGED and still green — it never called the
moved seams directly. The exec-dependent VM dispatch functions themselves
(bug 2's fix) are proven by the live bed rather than a unit test — isolating
the InvokeProvider round-trip behind a fake would test the fake, not the
real `candy/plugin-vm` reply shape that caused the bug.

## Mid-review additions: sdk CUE conversion + the qemu-shutdown case deletion

Two standing operator directives landed while this cutover was under
validator review, both applied here rather than deferred:

1. **The paired sdk PR's `arbiter_wire.go` fully CUE-converts** (not just the
   trim this cutover originally shipped) — "a wire type not generated via
   CUE before is no excuse to keep it hand-written in freshly committed
   code." All 9 arbiter wire/persisted-state types now source from
   `sdk/schema/arbiter.cue`; the 3 small string-enum const groups stay
   hand-written (`sdk/spec/arbiter_consts.go`, mirroring the existing
   `gpu_consts.go` precedent — plain string literals have no JSON/YAML
   shape for `gengotypes`). One generation-driven type change:
   `PreemptLease.OwnerPID` widens `int` → `int64`; every construction site
   in `candy/plugin-preempt/arbiter.go`/`arbiter_support.go`/`arbiter_test.go`
   updated to match. Full narrative + the sdk-side R10 evidence: the paired
   sdk PR's own `CHANGELOG/2026.203.1600.md`.
2. **The `"qemu-shutdown"` VmOp case in `candy/plugin-vm/provider.go` —
   proven fully senderless by THIS cutover — is DELETED here, not
   registered for a later batch** (standing operator directive: proven-dead
   code found during a cutover is deleted in that cutover; "register for
   later" is reserved for ALIVE-but-misplaced code). Deleted: the
   `case "qemu-shutdown":` block itself (`qemuForceShutdown`/
   `qemuGracefulShutdown` THEMSELVES stay — `candy/plugin-vm/vm.go`'s own
   direct-QEMU stop/destroy CLI path calls them directly, a genuinely live,
   separate caller); the now-dead `StateDir` field on `provider.go`'s local
   `vmEnv` decode struct (its only reader was the deleted case); the
   matching `state_dir` field on the CUE-sourced `spec.VmPluginEnv`
   (`sdk/schema/vmclient.cue`, in the paired sdk PR — confirmed zero
   remaining setters via `git grep 'VmPluginEnv{'` across
   `charly/`+`candy/`+`sdk/`); and 3 stale `plugin.go` doc-comment mentions
   of "qemu-shutdown" in its supported-op enumerations. `git grep
   qemu-shutdown` (outside `CHANGELOG/` history) now returns nothing.

## Axis-A note (core-minimization LOC accounting)

The paired Unit-8A cutover (`charly#177`) landed axis-A-neutral — its
squash diff is byte-identical 125→125 LOC per the fresh `pr-validator`
(the deleted `arbiter_bracket.go`/`arbiter_bracket_test.go` carried no sdk
mechanism-kit imports to remove). **This cutover (Unit-8B) is where the
axis-A core-floor reduction actually shows**: `git diff --stat` nets
**-533 LOC in `charly/`** (11 files, 100 insertions / 633 deletions) and
**-39 LOC in `sdk/spec/arbiter_wire.go`** (22 insertions / 61 deletions),
plus one whole file deleted (`charly/vm_qemu_client.go`) — the dead-code
cascade the arbiter-seam move left behind (see "What was deleted" above),
now confirmed unused via `golangci-lint` + `git grep` across the fully
merged tree (Unit-8A + bed-robustness batch), not merely the pre-rebase
snapshot.

## Gitlink repoints (this final push)

- `sdk` → `6a86528` (`opencharly/sdk#90`, MERGED squash sha
  `6a86528e79aaf69b48913a71c4a20bdd33442fcf`, `v0.2026203.1100` — the arbiter
  trim + full CUE conversion + the `qemu-shutdown`-only `state_dir` field
  removal), `charly/go.mod` bumped to `v0.2026203.1100` to match.
- `box/fedora` → `01331c7` (`opencharly/distro-fedora#6`, MERGED,
  `v2026.203.1029` — the `check-preempt-vm-live` bed is now on fedora
  `main`).
- `plugins` → `3d4d67c` (`v2026.203.1037`, the docs/lessons batch — the
  newest plugins-repo commit at push time, a descendant of the earlier
  `91eb754`/`v2026.203.0849` bed-robustness-docs target).

## Verification

- `go build ./...` + `go vet ./...` + `gofmt -l .` — clean, both
  `candy/plugin-preempt` and `charly`, on the FINAL tree (merged with
  Unit-8A's `charly#177` + the bed-robustness batch, all 3 gitlinks above
  repointed).
- `golangci-lint run` — 0 issues, both packages (the `unused`/`unparam`
  findings that drove this cutover's dead-code cascade are all resolved).
- `go test ./...` — `sdk` (fresh, `go clean -testcache` first) and `charly`
  (full package, ~150-210s across 7+ repeated runs through 2 rebases + 3
  bugfixes) all green.
- **Clean full `go test ./...` sidestepping shared-host VCS fragility.** The
  fresh `pr-validator` hit 18 unrelated failures signatured
  `error obtaining VCS status: exit status 128` — RCA: Go's default
  `-buildvcs=auto` walks up from the module root to stamp VCS info into test
  binaries, and this host runs many CONCURRENT linked worktrees off the same
  `.git` dir (`charly-wt-u8` included — `git worktree list` shows a dozen+
  siblings); a concurrent git operation in a sibling worktree during that
  walk can transiently fail the VCS-status read. This is host-environment
  fragility in the VCS-stamping step, not a defect in the code under test —
  `-buildvcs` controls ONLY whether VCS revision/dirty-state metadata is
  embedded in the built binary for `runtime/debug.ReadBuildInfo`; it has ZERO
  effect on which tests run or how they execute. Re-run with
  `GOFLAGS=-buildvcs=false` (disables VCS stamping only) to remove the
  confound and get a definitively clean signal:

  ```
  $ cd charly && GOFLAGS=-buildvcs=false go test ./...
  ok  	github.com/opencharly/charly/charly	155.938s
  ok  	github.com/opencharly/charly/charly/internal/pluginsgen	0.009s
  ```

  Zero failures, zero VCS-status errors — confirms the 18 failures the
  validator hit were exactly the environmental confound this RCA identifies,
  not a real regression in this cutover's code.
- R10 disposable bed roster, CONCURRENT acceptance run on the FINAL tree
  (all 3 bugs fixed, all 3 gitlinks repointed): `check-preempt-arbiter-pod`
  + `check-preempt-live-pod` + `check-preempt-vm-live` (all `box/fedora`) +
  `check-k3s-vm` (main repo) launched together at max parallelism, per the
  operator's concurrency-mandate correction — the `check-preempt-arbiter-
  pod`/`check-preempt-live-pod` pair share the `test-lock` exclusive token
  by design, so their arbitration outcome under REAL simultaneity (not a
  serialized run, which would prove nothing about the arbiter's actual
  conflict-resolution behavior) is itself the evidence this gate exists to
  produce. See the PR body for the pasted verbatim verdicts + the
  concurrency evidence.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)

# 2026.205.0715 — Generic agent rulebook and enforced attribution parity

`AGENTS.md` is now a concise, standalone, harness-neutral rulebook. It retains
the complete skill dispatcher, R0–R10 policy, architecture boundary, disposable
autonomy, hard-cutover and landing rules, acceptance criteria, and the full AI
attribution confidence contract while removing product-specific setup,
permission, and validator-protocol details. `CLAUDE.md` remains the complete
harness adapter and now describes the two files as equivalent overall policy
surfaces.

The agent-configuration validator now checks more than dispatcher tuples. Its
in-memory contract tests require the shared public policy markers, canonical
attribution template, all five confidence tiers, and the absence of
harness-specific policy markers from `AGENTS.md`. It also rejects the legacy
attribution placeholder on current Markdown policy surfaces and requires the
canonical template in the PR-validator and Git-workflow instructions.

The accompanying plugins gitlink advances documentation that applies the same
generic ownership language and canonical attribution shape to the owning skills
and validator brief.

Assisted-by: Codex OpenAI GPT-5.6 Sol (fully tested and validated)

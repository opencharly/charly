# 2026.205.0718 — fix: project-declared deploy-level `volume:` overrides now persist and take effect + the K1-unblock arbiter consumer catch-up this cutover's sdk-pin bump forces

An urgent, file-disjoint correctness fix, landed together with an
independently-tested but previously-unmerged consumer adaptation that this
cutover's own `sdk` gitlink bump makes mandatory (R5 — a producer's
breaking removal REQUIRES its consumer swept in the SAME coordinated
cutover that pulls it in; see "The K1-unblock arbiter consumer catch-up"
section below for the full mechanism and why it rides in this PR rather
than landing separately).

A project-declared deploy-level
`volume:` override (e.g. a disposable check bed's `volume: [{name: enc-data,
type: encrypted}]` authored directly in `box/<distro>/charly.yml`) never
reached the per-host deploy overlay (`~/.config/charly/charly.yml`) `charly
config` reads from — so it was silently dropped and the declared backing
(encrypted, bind, …) was never established. This gate-broke an entire class
of R10 beds that rely on a project-declared volume override, `check-enc-pod`
foremost among them (`enc-status-mounted` failed with "No encrypted bind
mounts configured").

## Root cause (independently re-verified)

`candy/plugin-deploy-pod/config_setup.go`'s `runConfig` (the ported
`BoxConfigSetupCmd` body, invoked via `sdk.OpConfigSetup`) resolved
`deployVolumes` from exactly three sources, in order: the CLI
`--volume`/`--bind`/`--encrypt` flags, the `CHARLY_VOLUMES_<BOX>` env var,
and the per-host overlay's own existing `Volume` entry
(`dc.Bundle[key].Volume`, loaded via `deploykit.LoadDeployConfigForRead` —
`~/.config/charly/charly.yml` only). The overlay is populated ONLY by an
explicit `charly config --bind/--encrypt/-v` invocation or a prior
`persistResourceCaps`-style write — never by the PROJECT's own declared
node. So a bed like `check-enc-pod`, whose `volume:` lives entirely in
`box/fedora/charly.yml`, had its declaration read at `charly bundle add`
compile time (feeding the InstallPlan) but never again at `charly config`
time, where the actual gocryptfs mount + quadlet volume line are decided.

## Fix

`resolveDeployVolumes` (new, `candy/plugin-deploy-pod/config_setup_helpers.go`)
adds a fourth, LAST fallback: the deploy's project-declared `volume:`,
resolved via a new narrow host seam (`pod-config-project-volume`,
`hostBuildPodConfigProjectVolume` in `charly/host_build_pod_config_seams.go`)
that reuses the EXISTING `resolveTreeRoot` merge (`charly bundle add`'s own
project+per-host-overlay merge, `deploy_tree.go`) scoped to the one deploy
key — so overlay precedence is unchanged (an explicit overlay entry still
wins). A hit is persisted into the overlay via `persistDeployVolumes`
(mirroring `persistResourceCaps`' Security-write shape), so the declaration
takes effect immediately and every subsequent read (`charly config status`,
the enc mount/unmount plan, …) resolves it without re-consulting the
project. New wire type `#PodConfigProjectVolumeRequest`/`Reply`
(`sdk/schema/seam.cue`, regenerated).

## A second, dependent bug the fix exposed and closed in the same cutover (R2 — blocking)

Once a project-declared volume resolves for the first time, `check-enc-pod`'s
quadlet gained a REAL `ExecStartPre=… config mount …` line
(`deploykit.QuadletConfig.CharlyBin`,
`deploykit.HasEncryptedBindMounts(bindMounts)` finally true) — which
immediately failed with "This binary is a plugin. These are not meant to be
executed directly." `charlyBin, _ := os.Executable()` in `runConfig` (and in
the sibling `--update-all` path, `updateAllDeployedQuadlets`) resolves to the
PLUGIN's OWN binary path when called from inside this out-of-process plugin
— the exact bug class `unified_targets.go`'s `hostEnvJSON()` already
documents and fixes for the lifecycle-Op family (R10 bed-found bug #5, S3b).
Setup shared the same defect independently; it was dormant here only because
no bed previously exercised an actually-resolving encrypted volume through
this path. Fix: `hostBuildPodConfigSetup` now populates a new
`host_env_json` field on `#PodConfigSetupRequest` via the SAME core
`hostEnvJSON()` helper (never a second one); the plugin decodes it via the
new `resolveHostCharlyBin` (falling back to `os.Executable()` only when the
field is absent, e.g. an old core build) and threads the resolved binary
into `updateAllDeployedQuadlets` too, deleting its own independent
`os.Executable()` call (R3 — one fix, both call sites).

## Coverage

- `candy/plugin-deploy-pod/config_setup_volume_test.go` (new): 5 unit tests
  against the extracted `resolveDeployVolumes` via a fake
  `pb.ExecutorServiceClient` — project-declared fallback resolves AND
  persists; the per-host overlay short-circuits before the project seam is
  ever consulted; a CLI flag likewise short-circuits; no project declaration
  → empty result, nothing persisted; a HostBuild transport error on the
  project-volume seam propagates as an error. These fail to even COMPILE
  against the pre-fix code (the function, the seam constant, and the wire
  types are all new).
- `charly check run check-enc-pod` (box/fedora, against this worktree's
  local candies via the auto-applied `CHARLY_REPO_OVERRIDE`) — the R10
  acceptance proof, full 12-step PASS including its own internal
  `charly update` (destroy + rebuild) and `feature-run-rebuild`.
- A SEPARATE, explicit fresh-rebuild pass (`charly update check-enc-pod` +
  `charly check live check-enc-pod`) — 26 checks, 23 passed / 0 failed / 3
  skipped (agent-check + build-only context, expected), both `enc-status-mounted`
  and `enc-plain-writable` green.

## The K1-unblock arbiter consumer catch-up (forced by this cutover's own sdk-pin bump)

This cutover's own `sdk` gitlink bump (to the merged `opencharly/sdk#95`,
`bbb6aca9`) crosses a BREAKING commit already on sdk's `main`
(`93d5632`, `opencharly/sdk#94`): the retirement of
`Executor.HostArbiter`/`spec.ArbiterSeamGather`/`ArbiterSeamResources`/
`ArbiterGatherReply`/`ArbiterResourcesReply`. `candy/plugin-preempt/arbiter.go`
(this repo) still called that retired API, so bumping the gitlink alone
broke `go build ./charly/...` outright (6 compile errors) — caught by a
fresh `pr-validator` re-running the gate on the assembled state, not by the
original author's own R10 pass (which had been run against the
PRE-gitlink-bump tree — an R9 staleness gap the validator also flagged and
this round corrects).

The consumer-side adaptation already existed, independently authored and
R10-tested, on the unmerged `feat/k1-unblock-wave1` branch (commit
`f548e270`) — so per R5 ("a hard cutover deletes the deprecated path AND
every stale reference in the SAME change" generalizes here to "a producer's
breaking removal pulls its consumer adaptation into the SAME assembled
cutover"), that commit is cherry-picked into this PR verbatim rather than
reinvented:

- **`candy/plugin-preempt/arbiter.go`** — `hostGather`/`hostResources` now
  call `Executor.HostBuild(ctx, "resolved-project", ...)` and do their own
  holder-filtering / gpu-vendor projection in-plugin via the portable
  `sdk/deploykit` helpers (`MergedDeployTree`, `FilterPreemptibleHolders`,
  `HolderAddrFor`, `GpuVendorTokens`), instead of `Executor.HostArbiter(ctx,
  spec.ArbiterSeamGather/Resources, nil)`.
- **`charly/arbiter_host.go` — DELETED.** The host-side `HostArbiter` RPC
  handler is unreachable once the plugin no longer calls it; its projection
  logic lives in the portable `sdk/deploykit/preempt_resolve.go`.
- **`charly/preempt.go`** — `gatherDeployNodes`/`gatherPreemptibleHolders`/
  `lookupVMClaimant`/`holderAddrFor`/`sortedHolderKeys` DELETED (superseded
  by the portable deploykit helpers); `gatherResources` KEPT (core-internal
  callers OUTSIDE the arbiter: `gpu_allocate.go`/`gpu_imply.go`, the
  operator-deferred GPU auto-allocation exception).
- **`charly/host_build_config_resolve.go`** — the `charly vm` config-resolve
  seam's VM-claimant lookup now calls the same portable
  `deploykit.FindVMClaimant` (R3, one implementation shared with the
  arbiter plugin).
- **`charly/plugin_executor_reverse.go` / `plugin_inproc_reverse.go`** — the
  `HostArbiter` reverse-channel server methods deleted (the RPC no longer
  exists on the wire).
- **`charly/uniform_api_gate_test.go`** — `HostArbiter` dropped from the
  `ExecutorService` fixed-method-set allowlist (F11 gate).
- **`candy/plugin-kube/k3s_post_forwards_test.go`** — the fake
  `ExecutorServiceClient`'s now-nonexistent `HostArbiter` stub removed.
- **`charly/deploy_preserve_test.go`** — `TestGatherDeployNodesPerHostWins`
  retargeted to exercise `deploykit.MergedDeployTree` against a real
  on-disk project + per-host overlay, preserving its original end-to-end
  proof.

Cherry-picked cleanly (`git cherry-pick -n`, one file auto-merged, zero
conflict markers) — confirmed via `git grep` for conflict markers across
every touched file before proceeding. This PR's own R10 run
(`check-enc-pod`) below is now the assembled-state proof for BOTH fixes
together; `check-preempt-arbiter-pod`'s own 12/12 PASS (this arbiter
adaptation's original R10, pasted in its source commit) is not re-run here
since the arbiter code itself is unchanged from that commit — only the
tree it composes with changed.

## Verification (round 2 — against the fully assembled tree: volume fix + charlyBin fix + cherry-picked arbiter adaptation + sdk pinned at the MERGED `bbb6aca9`/`v0.2026205.742`)

- `sdk` gitlink repointed from the deleted `fix/deploy-volume-persistence`
  branch tip to the MERGED squash commit `bbb6aca9f12daf32efba39da306021d7db131dab`
  (tag `v0.2026205.742`); `charly/go.mod`'s `github.com/opencharly/sdk`
  require bumped to `v0.2026205.742` to match (informational under the
  in-tree `replace`, kept accurate for out-of-tree consumers).
- `go build ./charly/...` (workspace) — clean.
- `GOWORK=off go build ./... && GOWORK=off go vet ./...` — clean in
  `candy/plugin-deploy-pod`, `candy/plugin-preempt`, `candy/plugin-kube`.
- `go test ./charly/...` — PASS (full suite, zero failures, 172.145s).
- `GOWORK=off go test ./...` — PASS in `candy/plugin-deploy-pod` (incl. the
  5 new volume-fallback tests), `candy/plugin-preempt`, `candy/plugin-kube`.
- `GOWORK=off go build ./... && GOWORK=off go test ./...` in `sdk` — PASS,
  including `TestGenReproducible` (fresh, against the merged commit).
- `gofmt -l` — clean across every touched file in every module.
- `golangci-lint run ./...` — **0 issues** in `charly` (workspace) and
  `candy/plugin-preempt`; `GOWORK=off golangci-lint run ./...` — **0
  issues** in `candy/plugin-deploy-pod`.
- `charly box validate` — exit 0, superproject and `box/fedora` (local
  override).
- **A genuinely FRESH binary** (`rm bin/charly candy/charly/bin/charly` +
  `task build:binary`, clean, no `go.work.sum` drift) — `charly version`
  advanced to `2026.205.0723` (proving it is not the pre-cherry-pick
  binary the first round's evidence was mistakenly built from — the R9 gap
  the validator flagged); content-marker-verified for BOTH fixes:
  `strings bin/charly | grep -c pod-config-project-volume` = 1,
  `strings bin/charly | grep -c 'resolved-project (resources)'` = 1.
- `charly check run check-enc-pod` (box/fedora, this worktree's local
  candies via `CHARLY_REPO_OVERRIDE`, PATH prepended with this worktree's
  `bin/` — the on-PATH `/usr/bin/charly` package predates this tree) — full
  **12-step PASS** against the fresh binary, including its own internal
  `charly update` rebuild + `feature-run-rebuild`.
- A SEPARATE, explicit fresh-rebuild pass (`charly update check-enc-pod` +
  `charly check live check-enc-pod`, same fresh binary) — **26 checks, 23
  passed / 0 failed / 3 skipped** (3 expected: an unbound agent-check + 2
  build-only-context checks in live mode), both `enc-status-mounted` and
  `enc-plain-writable` green.

No schema-version bump: every changed wire type is an internal host↔plugin
seam, never authored `charly.yml` surface. The arbiter adaptation is a
`sdk`-side wire/RPC retirement (already landed on sdk `main`) with a
consumer-side catch-up here — also no NEW schema-version bump beyond what
sdk#94 already carried.

**NOTE (2026-07-24): the arbiter consumer catch-up described above is being
extracted to its own standalone PR (`opencharly/charly#184`) per the
operator's landing-order directive — it unblocks other in-flight work
independent of this volume fix and does not need to wait behind it. Once
#184 merges, this branch rebases to drop the now-duplicate arbiter commit
and carries ONLY the volume-persistence + charlyBin-resolution fix; this
section will be rewritten to match at that point.**

## Round 2 fix — conditional project lookup (val-volfix regression)

val-volfix's review surfaced a genuine regression in the volume-persistence
fix itself: `resolveDeployVolumes` gated its project-declared-volume
fallback on `len(deployVolumes) == 0`, which is ALSO true for the
overwhelmingly common case of an ALREADY-CONFIGURED, genuinely volume-less
deploy (e.g. any sibling group member whose per-host overlay entry exists
from its initial `charly bundle add` but never carries a volume) — so
EVERY subsequent `charly config`/`charly update` re-config of such a deploy
re-triggered the project's `HostBuild("pod-config-project-volume")` seam (a
full `LoadUnified` project resolution) on every single call: a redundant
per-call cost the function's ORIGINAL (pre-fix) shape never paid, and a
correctness hazard on `charly config` wherever that seam's LoadUnified
round-trip fails in a context the original zero-lookup path never
exercised.

**Fix:** gate the project fallback on overlay-key ABSENCE (`!ok`), not on
`len(overlay.Volume) == 0`. Once an overlay entry exists for a deploy key —
whether or not it carries a volume — it is now authoritative and the
project is never re-consulted, restoring the original zero-lookup path for
every already-configured deploy (the same "resolved once, sticky
thereafter" pattern `persistResourceCaps`/`ResolveDeployPorts` already use
for caps/ports, R3). A brand-new deploy (no overlay entry yet) still
consults the project exactly once, per the original bug this PR fixes.

**Regression test:** `TestResolveDeployVolumes_AlreadyConfiguredVolumeLessSkipsProjectLookup`
(`candy/plugin-deploy-pod/config_setup_volume_test.go`) — an overlay entry
exists (a resolved port, no volume) and the fake has no project-volume
reply configured (a call would panic). Verified this test FAILS against
the pre-fix code (`git stash` the fix, re-run: `HostBuild calls =
[pod-config-project-volume], want none`) and PASSES with it.

Evidence: `GOWORK=off go build ./... && GOWORK=off go vet ./...` clean in
`candy/plugin-deploy-pod`; `GOWORK=off go test ./...` — PASS, all 6
`TestResolveDeployVolumes_*` tests (the 5 original + the new regression
test). The disposable-bed R10 re-run (both `check-enc-pod` and a
volume-less sibling-member `charly config` case) is pending the #184
merge + rebase above; will be pasted fresh against the FINAL, rebased code
before this PR reopens for validation.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; analysed on a live system)

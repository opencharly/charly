# 2026.209.1900 — #118 core-minimization COMPLETE: charly/ pinned to the E/M/B/D fabric floor (RESIDUE 0)

The terminus of the **"everything is a plugin" core-minimization program** (`charly#118`).
The kernel-manifest floor gate (`charly/kernel_manifest_floor_test.go`,
`TestKernelManifest_CoreIsPinnedToTheFabricFloor`) is **GREEN**: the `residueOwner`
tracker map is empty — every tracked `charly/*.go` file has either moved to its owning
plugin candy or been classified into the E/M/B/D fabric floor (Envelope / Mechanism /
Bootstrap-root / kind-recognition Data). This is the P16a file-allowlist gate; the P16b
import-purity phase (charly imports spec+proto only) is the tracked follow-on.

**Net −3,064 charly/ core LOC** (superproject 367 files, +11,780 / −14,844). The sdk
producer leg lands the mechanism kits the plugins consume (deploykit, loaderkit, spec/wire
types); the superproject leg lands the core reduction + the `candy/plugin-*` consumers.

## What the program delivered

- **Core is a PLUGIN HOST; every capability is a plugin candy.** `charly/` retains only
  the plugin-host fabric — plugin discovery/loading, the provider registry + transports,
  the CLI/kind prescan + word→plugin dispatch, the kind-decode materialize, and the
  reverse-channel broker. Every capability — the project loader, the deploy walk, the build
  engine, the check harness, the loader-orchestration, the bed runner, and this terminus's
  final movers — resolves its config into a generic envelope (`InstallPlan` / `VenueDescriptor`
  / `BuilderResolveReply` / an `OpValidate` diagnostic) that an in-core mechanism consumes,
  reaching the kernel only through `sdk.Executor.InvokeProvider` / `HostBuild`.
- **The last residue movers (this terminus):** the P11 deploy-lifecycle cone (config_image
  provides-inject, config_write_host, bundle_members, enc/secrets, ephemeral_dispatch,
  deploy_nodeform's Primaries-driven marshal) → plugin-deploy-pod/-vm + deploykit; the P8b
  build tail (builder_venue's buildEngineContext envelope + venue-builder, layer_secrets'
  candy-secret resolution) → deploykit; the host_build_loader dissolution (the 3 substrate
  plugins self-serve ValidateAndroidDevices + ValidatePreemptible via loaderkit's
  ResolveVia*Executor mid-load InvokeProvider callbacks, the materialize floored as the
  4th permanent loader leg); and cmd.go's `__cmd` interactive-attach reentry dissolved into
  the floored pod-cmd HostBuilder seam (checkLoadPlugins hoisted to command scope).
- **The K4 dispatch-kernel cone (folded into this terminus):** the deploy-add per-node COMPILE
  selection (`bundle_add_cmd.go`/`bundle_compile_seam.go` — `local:` template resolve, the
  standalone-candy and primary-box selection paths) moved out of core into
  `candy/plugin-bundle`, deleting the host-side synthetic-box constructors + `ResolveBox`/
  `ScanAllCandyWithConfig` selection glue (net −153 core LOC over its own K4 sub-cutover).

## Terminal R10 — proven live on disposable beds (fresh-rebuild), 2026-07-28

Run against the final terminus binary (calver `2026.209.19xx`); every number is backed by an
on-disk `.check/<bed>/<calver>/summary.yml` (`ok: true`, per-step verified). **9 green.**

| Bed | Result | total_s | Proves |
|---|---|---|---|
| `check-pod` | ✅ 13/13 | 268 | deploy walk / config / marshal / provides-inject / bundle_members / DeployTarget render + fresh-rebuild |
| `check-builder-vm` | ✅ 10/10 | 397 | build engine + the systemic BuilderConfig fix (FIRST live validation) |
| `check-charly-vm` | ✅ 8/8 | 463 | charly-toolchain localpkg + the DistroDef SystemPackages fix (virsh/qemu now installing) |
| `check-jupyter-pod` | ✅ 13/13 | 583 | out-of-process check-verb connect (mcp on the pod arm) through fresh-update re-check |
| `check-substrate` | ✅ 10/10 | 381 | the 5 substrate-kind plugin decoders |
| `check-group` | ✅ 7/7 | 548 | targetless group structural kind (incl. the group rebuild-members transition) |
| `check-structkind` | ✅ 7/7 | 555 | external structural-kind decode |
| `check-preempt-local` | ✅ 6/6 | 175 | the ValidatePreemptible loader-callback dissolution |
| `check-commands-local` | ✅ 6/6 | 297 | command-plugin dispatch |

**One pre-existing failure, R2-separable (NOT a #118 regression):** `check-k3s-vm` fails at
`deploy-add` (3m6s) in the **k3s-server candy's own `artifact: kubeconfig` wait** — a 2m
timeout too tight for k3s first-boot (~2m) when the candy's dep install (ethtool/socat/
conntrack/fuse) eats the window. The #118 deploy MECHANISM is proven up to that point
(vm-build ✓, vm-create ✓, the full plugin deploy walk installed the k3s-server candy + created
its `charly-k3s-server-k3s.service` unit); only the candy's config-sourced timeout is too
tight. Tracked as #61 (fix: bump 2m→4-5m), landing in the post-#118 bed-robustness batch. It
passed twice on 07-25 against intermediate cones — a timing-marginal candy issue, present on
`main` too, downstream of everything #118 moved.

## Three real bugs the terminal R10 caught, fixed, and folded in (modified-code compliance)

- **Synthetic box constructors never set BuilderConfig** — every standalone-candy deploy
  silently skipped the builder step (`candy_select.go`; mirrored the default NewSpecResolvedBox
  path). Proven fixed on check-builder-vm.
- **Synthetic box constructors never set DistroDef** — the sibling gap; every standalone-candy
  deploy silently compiled zero SystemPackages while distro-gated service steps still fired.
  Proven fixed on check-charly-vm (virsh/qemu install, virtqemud.socket present).
- **Out-of-process check-verb connect regression** — the P12 check-cone port dropped
  `checkLoadPlugins` from the pod/local arms of `pluginCheckRunLive`, so mcp/cdp/vnc/dbus/spice
  never connected on pod/local beds ("no provider registered"). Restored the pre-P12
  command-scope single load (one hoisted call before the 4-way dispatch, R3 dedup). Proven
  fixed on check-jupyter-pod fresh-rebuild.

## Tracked as post-#118 (R2-separable, pre-existing bed/candy robustness — with ready fixes)

pod-shell/-lifecycle exit-code propagation; the `command:` check verb's missing expect-non-zero
field; the bed [host]-probe mtime stale-guard on the on-PATH binary; the k3s-server candy's
2m kubeconfig-wait too tight for k3s first-boot; the eval-host-vm guest missing rsync for the
check-local-vm closure delivery. None a #118 regression; each lands in the deploy-adjacent
bed-robustness batch.

## Verification

`go build ./...` / `go vet ./...` clean across `charly/`, `sdk/`, and every `candy/plugin-*`;
full `go test ./...` ZERO FAILs (the kernel-manifest gate now PASSES at RESIDUE 0);
`TestGenReproducible` green (schema regen a no-op); `task build:binary` clean; the on-PATH
`opencharly-git` package rebuilt from the final terminus and installed (R9). The terminal R10
roster above ran fresh-rebuild on disposable `disposable: true` beds, verbatim in the PR.

Assisted-by: Claude Code Anthropic Claude Opus 4.8 (fully tested and validated)

# 2026.210.0853 — charly + candy drop loaderkit for the loader-RESULT types (#55 Phase B, Cutover 1)

The superproject consumer leg of #55 Phase B Cutover 1. The cleanly wire-shaped
loader-result type family moved into the types-only `spec` module (see
`spec/CHANGELOG/2026.210.0700.md` + `sdk/CHANGELOG/2026.210.0700.md`); charly core
and the candy plugins repoint every `loaderkit.X` / `kit.X` (closure) /
`deploykit.X` (provides) reference to `spec.X`, and the ~19 charly files whose
only `sdk/loaderkit` use was a moved TYPE drop that import entirely.

Repoints (charly + candy):

- `loaderkit.{UnifiedFile,InlineCandy,DecodeInlineCandy,EncodeInlineCandy,
  ResolvePluginKindViaPlugin,DecodePluginKindMap,ValidationError,CandyCandidate}`
  → `spec.X`.
- `kit.{UnifiedFileName,ImportList,ImportEntry,DiscoverConfig,ScanSpec}` →
  `spec.X`.
- `deploykit.{ProvidesConfig,EnvProvideEntry}` → `spec.X`.
- `uf.ProjectBundleConfig()` → `deploykit.ProjectBundleConfig(uf)` (charly
  deploy.go/deploy_tree.go/deploy_state_host.go + candy/plugin-bundle).
- `loaderkit.ProjectTemplates(uf)` → `uf.ProjectTemplates()` (the spec method).

**Result:** charly `sdk/loaderkit` importers 71 → 52 (**19 files dropped**). The
28 mechanism-calling files (LoadUnified/Materialize/ScanCandy/Walk/… and the 32
that construct a `loaderkit.ResolveOpts`) KEEP importing loaderkit — only their
TYPE refs repointed.

**Scope is Option A (operator-ruled): `loaderkit.ResolveOpts` stays put.** It
embeds `*buildkit.{Init,Distro,Builder}Config` mechanism config, which spec (a
types-only module) can never import (a spec←buildkit cycle), so it is
correctly-placed loader mechanism — NOT residue. Named Phase-B follow-on: it
exits loaderkit only if redesigned to carry spec-level RESULT types instead of
`*buildkit` config, a separate cutover in the buildkit/mechanism-seam track. The
brief's optimistic "~42 files drop" assumed ResolveOpts moved; 19 is the honest,
clean Option-A number.

Verification (assembled workspace): `go build`/`go test ./...` green across
charly + sdk + spec + every touched candy module; `golangci-lint run` 0 issues;
`charly box validate` exit 0 (superproject root + box/fedora); `task cue:gen`
reproducible (0 generated-file drift); grep-clean — the moved types no longer
appear at their old kit/loaderkit/deploykit homes.

# 2026.210.0910 — charly core + plugins consume the spec fabric slices (#55 STEP 1, superproject leg)

STEP 1 of the everything-is-a-plugin import-purity plan relocated the standalone
fabric primitives from the sdk mechanism kits into granular
`github.com/opencharly/spec` sub-packages (spec producer PR + sdk consumer PR).
This superproject leg repoints charly core AND the plugin candies at the spec
slices, dropping the now-deleted `sdk/{targetkit,sshx,proclifecycle,testkit}`
imports — a hard cutover, no alias shims.

charly core repoints:

- `agent_target_cmd.go`, `plugin_grpc.go` → `spec/transport`
  (`targetkit.` → `transport.`).
- `check_graphics_endpoint.go`, `host_build_pod_config_seams.go` → `spec/sshx`.
- `main.go`, `bundle_members.go`, `host_build_deploy_members.go` → `spec/proc`
  (`proclifecycle.` → `proc.`).
- `kernel_manifest_floor_test.go` — the check_graphics_endpoint.go x/crypto/ssh
  boundary classification updated to `spec/sshx` (still the single contained
  boundary in charly core).

plugin candy repoints:

- `plugin-agent`, `plugin-agent-pi` → `spec/transport` + `spec/proc`
  (`kit.ShutdownProcessGroup` → `proc.ShutdownProcessGroup`).
- `plugin-ssh`, `plugin-vm` → `spec/sshx` (SSH tunnel + `ResolveSSHPubKey`);
  `plugin-vm/vm_util_shims.go` → `spec.CurrentUsername`.
- `plugin-enc`, `plugin-oci`, `plugin-secrets` → `spec/proc` (temp-cleanup /
  signal handler).
- `plugin-box/validate_rules.go` → `spec.ParseSSHTarget`.
- The `kit.ShellQuote` → `spec.ShellQuote` repoint across ~27 candies
  (adb, addr, cmd, dbus, dns, file, http, installstep, interface, kernel-param,
  kube, mount, package, process, record, secrets, service, unix-group, user,
  wl, check, deploy-pod, tmux, vm, example-stepkind, …); unused `sdk/kit`
  imports dropped where ShellQuote was the sole use.

Verification: `go build/vet ./...` + `go test ./...` green (charly + every
candy module); `charly box validate` exit 0; `task build:binary` green
(`charly version` = 2026.210.1158, the assembled-tree fresh binary); Rule-2
containment proven — an ssh-free plugin (`plugin-migrate`) links zero
`golang.org/x/crypto/ssh` while an ssh-using plugin (`plugin-ssh`) links it via
`spec/sshx`; `git grep 'sdk/(targetkit|sshx|proclifecycle|testkit)'` and
`git grep 'kit.ShellQuote'` both EMPTY.

R10 (assembled tree, fresh `./bin/charly` 2026.210.1158, both beds fresh-rebuild):

- `check-pod` **13/13** (`total_seconds: 211`) — build → check-image → pod
  deploy → check-live → feature-run → `update` (fresh rebuild) →
  check-live-rebuild → feature-run-rebuild → cleanup, all `ok: true`.
- `check-builder-vm` **10/10** (`total_seconds: 276`) — vm-build → vm-create →
  deploy-add → member deploy → check-live → member check-live → `update` (fresh
  rebuild) → cleanup, all `ok: true`, domain torn down clean.
- `charly go test ./...` exit 0 (0 FAIL); `go build ./...` 0 failures across all
  workspace modules; `gofmt -l` empty; `TestPluginsGenReproducible` passes.

Superproject leg finalization (merged-submodule assembly):

- Submodule gitlinks forward-pinned to the merged producer mains: `spec`
  d5eba0b → **e412efce** (tag v0.2026210.1121, the fabric-primitive slices),
  `sdk` 836f958 → **19b4deae** (tag v0.2026210.1134, the sdk consumer leg
  dropping the 4 kits).
- `charly/go.mod` + all 85 `candy/plugin-*/go.mod` require `github.com/opencharly/spec`
  bumped 808 → **v0.2026210.1121** (`go work sync` settled; the local
  `replace => ../spec` / `../../spec` resolve the assembled tree; sdk stays
  `v0.0.0` + replace). Every workspace module builds (`go build ./...` 0
  failures across charly + 85 plugins + spec + sdk).
- gofmt import-sort fix (R1): the repoint reordered imports
  (`spec/spec` mis-sorted against `sdk/vmshared`); 41 files `gofmt -w`'d,
  `gofmt -l` now empty. golangci's default formatter does not run gofmt, so the
  standalone check caught it.
- `plugins` submodule gitlink corrected b352fffa → **dc9dd97** (reachable
  plugins main): super `main` had pinned an unpushed/orphaned plugins commit
  (b352fffa — unreachable on the plugins remote, divergent from main); this leg
  re-pins to the reachable plugins-main HEAD. The `plugins` submodule is
  skills/marketplace docs only — no effect on the charly build or R10 beds. The
  orphaned-pointer root cause (how super main acquired an unpushed gitlink) is
  tracked as a separate landing-discipline finding.

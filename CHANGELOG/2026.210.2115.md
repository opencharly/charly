# 2026.210.2115 — charly build-vocabulary value types off sdk/buildkit onto spec (#55 step 3-III)

The superproject leg of #55 step 3-III (import-purity phase). charly core + the
build plugins now reference the build value-type containers `DistroConfig` /
`BuilderConfig` (+ `BuilderMap`, `WrapDistroDef`, `DistroTagChain`, and the
`DistroDef`/`FormatDef`/`BuilderDef` payload aliases) from the CUE-sourced `spec`
contract instead of importing `sdk/buildkit` — dropping the `sdk/buildkit` import
from every charly file that used ONLY these value types. Consumes merged spec
`v0.2026210.2052` + sdk `v0.2026210.2103` (producer-first B2).

## What lands

- **20 charly + 12 candy files** repoint `buildkit.{DistroConfig, BuilderConfig,
  BuilderMap, WrapDistroDef, DistroTagChain}` → `spec.X` and
  `buildkit.{DistroDef→spec.ResolvedDistro, FormatDef→spec.Format,
  BuilderDef→spec.Builder}`; `goimports` drops the now-unused `sdk/buildkit`
  import from the files that no longer reference any buildkit BEHAVIOUR symbol.
- **Import-purity win**: charly files importing `sdk/buildkit` drops **51 → 42**.
  The remaining 42 keep `buildkit` for build-engine BEHAVIOUR
  (`ResolveBox`/`ResolvedBox`/`ResolveAllBox`/`ResolveStatus`/phase-templates)
  or `InitConfig` — those relocate in #65/#71.
- **Submodule gitlinks**: `spec` → `ec22fa8e` (v0.2026210.2052), `sdk` → `5f08d64b`
  (v0.2026210.2103).
- **go.mod requires**: charly (`spec` → v0.2026210.2052, `sdk` → v0.2026210.2103)
  + all 85 candies (`spec` → v0.2026210.2052).

`InitConfig` stays in `sdk/buildkit` (composition policy — `ResolveInitSystem`/
`ActiveInit` aggregate the whole candy set via `AggregateCandyCapabilities`, not a
contract accessor); its charly consumers keep the `buildkit` import → #65/#71.

## Verification (final assembled tree)

- `go build ./...` (charly) EXIT 0; `go test ./charly/...` ok (196s);
  **all 85 candies `GOWORK=off go vet` = 0 fails** (out-of-process candies swept,
  the #194 lesson); `charly box validate` EXIT 0; `gofmt` clean; go.work canonical
  regen a NO-OP; no `go.sum`/`go.work.sum` churn.
- Reproducibility + architecture gates: `TestPluginsGenReproducible`,
  `TestNoSinglePluginAPISurface`, `TestKernelManifest_CoreIsPinnedToTheFabricFloor`
  pass.
- **R5 whole-tree, both directions**: zero live `buildkit.{DistroConfig,
  BuilderConfig, BuilderMap, WrapDistroDef, DistroTagChain}` refs in charly/candy;
  no stale "buildkit-specific / spec must never import" comments.
- **R10** (build-engine beds, fresh-rebuild, on the identical runtime .go):
  **check-pod-overlay 13/13** (713s — overlay Containerfile renders `dnf install`
  (Fedora/rpm, the base image's distro), live-proving DistroConfig resolution is
  correct through the spec-relocated types) + **check-builder-vm 10/10** (358s).

Producer-first B2: spec (v0.2026210.2052) → sdk (v0.2026210.2103) → this
superproject leg.

# 2026.210.2340 — charly + in-tree candies off sdk/deploykit for the InstallStep IR (#55 step-4)

The superproject leg of #55 step-4 (import-purity phase). charly core + the in-tree
candy plugins now reference the **InstallStep step vocabulary** (the 13 concrete step
structs + `RepoSpec`/`CacheMountSpec`/`ArtifactRef` + the view converters + plan
mechanisms) from the `spec` contract instead of importing `sdk/deploykit` — dropping
the `sdk/deploykit` import from every charly file that used ONLY these + the IR
envelope aliases. Consumes merged spec `v0.2026210.2308` + sdk `v0.2026210.2318`
(producer-first B2).

## What lands

- **53 charly + 6 candy files** repoint the InstallStep step vocabulary + the ~57
  IR-envelope re-export aliases `deploykit.X` → `spec.X` (identity for the step
  structs; the 3 pre-existing renamers CandyYAML→Candy / LocalPkgDef→LocalPkg /
  CacheMountDef→CacheMount handled); `goimports` drops the now-unused `sdk/deploykit`
  import where no behaviour symbol remains.
- **Import-purity win**: charly files importing `sdk/deploykit` drops **46 → 38**
  (8 fully drop it: bundle checkrun / layer_secrets / plugin_dispatch_reverse /
  check_kit_adapter / host_build_construct_step / provider_step / deploy_target_unified
  / host_build_resolve_target_add). The remaining 38 keep `deploykit` for BEHAVIOUR
  (Generator render, OCITarget walker, the deploy walk / state / quadlet cones,
  ResolveDeployChain, …) — those relocate in later K3/K4-class slices.
- **In-tree candies** (plugin-adb / plugin-bundle / plugin-installstep) repoint their
  step-struct refs to `spec.X` (#72-parity — zero in-tree refs left on the deploykit
  alias for a moved type); they keep the `deploykit` import for the unmoved behaviour
  (Generator/OCITarget/ShellSnippetStep… — note: ShellSnippetStep IS one of the moved
  structs and was repointed; the unmoved set is the behaviour + the pre-existing
  InstallPlan/DeployExecutor aliases).
- **Submodule gitlinks**: `spec` → `617b96e5` (v0.2026210.2308), `sdk` → `160a79e2`
  (v0.2026210.2318). **go.mod requires**: charly (`spec` → v0.2026210.2308, `sdk` →
  v0.2026210.2318) + all 85 candies (`spec` → v0.2026210.2308).

The InstallStep vocabulary is IN-PROC IR relocated hand-written into spec beside the
`InstallStep` interface + `InstallPlan`/`StepBatch` container; the wire form
`InstallStepView` stays the single CUE source, untouched.

## Verification (final assembled tree)

- `go build ./...` (charly) 0; `go test ./charly/...` ok (199s); **all 85 candies
  `GOWORK=off go build` 0 fails** (against the merged local spec/sdk); `charly box
  validate` 0; `gofmt` clean; go.work canonical regen a NO-OP; no `go.sum`/`go.work.sum`
  churn.
- Reproducibility + architecture gates: `TestPluginsGenReproducible`,
  `TestNoSinglePluginAPISurface`, `TestKernelManifest_CoreIsPinnedToTheFabricFloor`
  pass.
- **R5 whole-tree, both directions**: zero live `deploykit.{moved struct}` refs in
  charly/candy (all 14 moved names incl `ShellSnippetStep`); the retained `deploykit`
  aliases are additive (its own code + out-of-tree plugins), not deleted.
- **R10** (fresh-rebuild, runtime-identical code): **check-pod 13/13** (204s — the
  deploy IR / install timeline where these exact InstallStep structs execute across
  every DeployTarget rendering path) + **check-builder-vm 10/10** (336s).

Producer-first B2: spec (v0.2026210.2308) → sdk (v0.2026210.2318) → this superproject
leg.

# 2026.211.0631 — charly off deploykit for the deploy value-vocabulary (#55 Cone V, super leg)

The deploy CONFIG/KEY value-vocabulary moved `sdk/deploykit` → `spec` (Cone V). This
leg repoints every charly reference to the spec-owned vocabulary, dropping the
`sdk/deploykit` import from **5 charly files** and de-coupling ~11 more from the
value dependency. Behaviour still consumed from deploykit (SaveDeployState, OCITarget,
the deploy walk) is unchanged — those files keep the deploykit import.

## Repoints

- **`refs.go`** → `spec.IsRemoteCandyRefString` / `spec.BareCandyRef` (drops deploykit).
- **`host_build_pod_lifecycle_dispatch.go`**, **`bundle_from_box_cmd.go`** →
  `spec.DeployKey` / `spec.DeriveDeploymentName` (drop deploykit).
- **`oci_step_emit.go`**, **`step_emit_hostbuild.go`** → `spec.OCIEmitStepParams`
  (drop deploykit — their only code ref was the moved struct; OCITarget/Generator
  are comment-only).
- De-coupled (value ref → `spec.X`, deploykit kept for behaviour): `deploy.go`,
  `unified_targets.go`, `check_cmd.go`, `check_members.go`, `pod_lifecycle_verb.go`,
  `config_secret_migration.go`, `host_build_pod_config_seams.go`,
  `host_build_deploy_config_save_state.go`, `bundle_members.go`, `tasks.go`
  (`VolumeMount`), `update_deploy_dispatch.go`.
- R5: stale `deploykit.BundleDelArgv`/`deploykit.DeployKey` comment claims updated in
  `bundle_add_cmd.go` + `kernel_manifest_floor_test.go`.

## Excluded (never-trust-verify — belongs to K4, not Cone V)

- **`config_secret_migration.go`** keeps deploykit: its `MigratePlaintextEnvSecret`
  takes `*deploykit.BundleConfig` (5 kit-coupled methods → K4 behaviour, not spec).
  It de-couples `DeployKey`→spec only.
- **`bundle_add_cmd.go`** keeps deploykit (`HostContext` references the
  deploykit-internal `BuilderPreresolved`; in-proc compiler input).

## In-tree candy repoints (whole-tree R5 — the canonical home is spec; the deploykit
## alias is out-of-tree back-compat only)

All 58 in-tree candy refs to the 6 moved identifiers repoint `deploykit.X` → `spec.X`
across 9 modules (plugin-bundle, plugin-check, plugin-deploy-pod, plugin-deploy-vm,
plugin-installstep, plugin-pod, plugin-status, plugin-substrate, plugin-vm) — code,
tests, and comments. Only the 6 moved identifiers move; `BundleConfig`/`.Bundle`/
`LoadBundleConfig`/`SaveDeployState`/`Generator`/`OCITarget` stay on deploykit, so the
9 modules KEEP their deploykit import for that unmoved behaviour (canonical-consistency,
not an import drop). 3 candy files whose ONLY deploykit ref was a moved symbol fully
drop it (bundle_cmd.go, plugin-status/overlay.go, plugin-substrate/command_reap_orphans.go).
Whole-tree `git grep 'deploykit.(DeployKey|…|SaveDeployStateInput)'` over `charly/`+`candy/`
is now empty (code + comments).

Submodule gitlinks (`spec`, `sdk`) bump to the merged Cone V producer commits; the
`charly/go.mod` require bumps with them. `go test ./charly/...` green (incl.
kernel-manifest + pluginsgen reproducibility); all 9 candy modules `GOWORK=off go
build ./... && go vet ./...` clean; `charly box validate` clean.

_R10 beds (check-pod + check-builder-vm) pending — owned by the orchestrator; upgrade this tier post-bed at merge._

_Assisted-by: Claude Code (Claude Opus 4.8) (syntax check only)_

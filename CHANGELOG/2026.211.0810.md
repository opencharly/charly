# 2026.211.0810 — pod_lifecycle_verb.go off sdk/deploykit via node DATA-threading (#55 Cone A unit 1)

The superproject leg of #55 Cone A unit 1. The pod-lifecycle HostBuild reverse legs
stop RE-READING the per-host deploy config in charly core: the invoking plugin
(command:pod / command:cmd) threads its already-loaded deploy node as DATA into the
`spec.Pod*Request.Node` field, and the host M (ResolveTarget + live-executor
composition) uses the passed node instead of re-reading. The config-READ leaves core
(a plugin capability, previously leaked into the host leg); the genuine M stays.

## What lands

- **charly/pod_lifecycle_verb.go** — `resolveLifecycleDeployNode` (+ its
  `deploykit.LoadDeployConfigForRead`) DELETED; dispatch/start/stopViaLifecycle take
  the (node, deployName) DATA. **pod_lifecycle_verb.go is now OFF sdk/deploykit.**
- **charly/host_build_pod_lifecycle_dispatch.go** — the 6 handlers pass `req.Node`.
- **candy/plugin-pod/pod_cmd.go** + **candy/plugin-cmd/command.go** — populate
  `req.Node` via `deploykit.ResolveLifecycleDeployNodeViaSeam` (the plugin-side twin).
- **Gitlinks**: spec → `30e7a4ee` (v0.2026211.754), sdk → `68e25748` (v0.2026211.804).
  **Requires**: charly (spec → v0.2026211.754, sdk → v0.2026211.804) + 85 candies
  (spec → v0.2026211.754).

All-callers invariant PROVEN: start/stop/shell/logs/service ← command:pod, cmd ←
command:cmd — both already use `LoadBundleConfigViaSeam`, so both supply the node;
`resolveLifecycleDeployNode` is fully deleted (no fallback, no pure-host caller).
Parity: the sdk helper is a verbatim port → byte-identical node; the `req.Node`
pattern is already in production (host_build_resolve_target_add.go +
plugin-bundle/deploy_target.go). This is Cone A unit 1; units 2 (deploy.go
resolve-ref seam-collapse) + 3 (deploy_tree.go tree-threading, 4 command plugins)
are the follow-on.

## Verification (final assembled tree)

- `go build ./...` 0; `go test ./charly/...` ok (202s); **all 85 candies GOWORK=off
  build 0**; `charly box validate` 0; gofmt clean; go.work regen NO-OP; no go.sum churn.
- **R5**: no live `resolveLifecycleDeployNode` in charly (only an accurate deleted-marker
  comment); pod_lifecycle_verb.go imports zero sdk/deploykit.
- **R10** (fresh-rebuild): **check-pod 13/13** (345s — the shared dispatchLifecycleTarget
  node-threading via start/stop + fresh update) + live **`charly logs`** exercising the
  `ResolveLifecycleDeployNodeViaSeam` helper on a deployed pod. plugin-cmd fills via the
  identical helper.

Producer-first B2: spec (v0.2026211.754) → sdk (v0.2026211.804) → this superproject leg.

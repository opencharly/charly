# 2026.215.0847 — Context-engineering modernization: slim CLAUDE.md, delete reminder hooks

Docs/config-only cutover applying Anthropic's Claude-5 context-engineering
guidance (lightweight always-loaded context, progressive disclosure into
skills, no instruction repetition across context layers, deterministic hooks
only) to the superproject rulebook layer. Zero behavior change to the `charly`
product surface.

## CLAUDE.md rewritten to AGENTS.md density

- 11,270 → 3,402 words (~70% reduction of always-loaded context). The file is
  now the Claude Code adapter at the same density as the harness-neutral
  `AGENTS.md`: purpose, R0 + full Skill Dispatcher, the pillars as short
  mandates, R1–R10 at bullet density, Disposable-Only Autonomy, Hard Cutover
  by Default (incl. the Cutover Sizing Law), Post-Execution Policies,
  Acceptance checklist, Agents/Workflows/Teams (Fable 5 orchestrator +
  Sonnet 5 teammates instantiation, north-star discipline), Hooks, the AI
  Attribution table, Key Rules one-liners, and the doc-split map.
- Every elaboration the rulebook previously carried in place now lives solely
  with its Authoritative-copy-registry owner (forbidden-excuse catalog →
  `/charly-internals:cutover-policy`; R10 gate matrix + flag discipline →
  `/charly-check:check`; boundary-law elaboration → `/charly-internals:plugin`;
  landing mechanics → `/charly-internals:git-workflow`; orchestration detail →
  `/charly-internals:agents`; R1–R5/RDD/ADE operationalization →
  `/charly-internals:strict-policy`). The rulebook keeps the mandate + a
  `*Detail:*` pointer per rule — no policy weakened, no rule dropped.
- Section/clause names that mirroring surfaces quote are preserved verbatim
  (Hard Cutover by Default, Post-Execution Policies, Acceptance checklist,
  Ground Truth Rules, AI Attribution (Fedora Policy Compliant), the
  flag-override clause, the Cutover Sizing Law, Candyboxing, the
  kernel/plugin boundary law, Prioritize Clean Architecture Above All Else).
- The former part-structure ("Part I — Dispatch" … "Part VI — Index") and the
  "Enforcing VISION.md" section are retired; the pillars section binds the
  VISION tenets directly.
- Multi-agent instantiation updated: Fable 5 orchestrator + Sonnet 5
  teammates/validators (previously Opus 4.8 teammates).

## Reminder hooks deleted; deterministic gates kept

- Deleted `.claude/hooks/runtime-verification-reminder.sh` (UserPromptSubmit
  full-roster reminder), `.claude/hooks/end-of-turn-challenge.sh` (Stop
  reminder), and `.claude/hooks/team-coordination-reminder.sh`
  (TaskCreated/TaskCompleted/TeammateIdle reminder) — the per-prompt/per-turn
  rule-roster re-injection layer. Claude-5-generation models retain rulebook
  instructions throughout context; the reminder layer was three-fold
  repetition.
- `.claude/settings.json`: removed the UserPromptSubmit / Stop / TaskCreated /
  TaskCompleted / TeammateIdle hook wiring; the PreToolUse gates
  (`pre-commit-gate.sh`, `pre-push-gate.sh`) are unchanged.
- `.reasonix/settings.json`: removed the UserPromptSubmit / Stop reminder
  wiring; its PreToolUse gate delegation is unchanged.
- Hooks doctrine everywhere now reads: hooks enforce deterministic command
  mechanics; agents judge policy and proof; there is no reminder layer.

## AGENTS.md

- Hooks section updated for the deleted reminder layer; otherwise a light
  touch — the two rulebooks remain policy-equivalent.

## Companion landing

- `plugins` submodule bumped to the context-engineering skills cutover (core
  skills deduplicated against the rulebook, progressive-disclosure splits of
  the largest skills into entry + `references/*.md`, moderate de-shout,
  incident narratives compressed to standing rules) — see the plugins repo's
  CHANGELOG entry of the same landing window.

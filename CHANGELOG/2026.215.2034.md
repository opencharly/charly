# 2026.215.2034 — refactor(core)!: #55 import-purity — charly/ at ZERO sdk references, CI-enforced

The #55 program's terminus: the plan's end-state gate is MET and permanently enforced —
`git grep 'github.com/opencharly/sdk' charly/` → EMPTY, path-scoped (every .go file, prod AND
test, plus go.mod; the sdk require+replace dropped). charly core imports ONLY
`github.com/opencharly/spec/*` + the proto/plugin-api wire contract + stdlib + vetted
third-party, enforced in CI by the new import-surface assertion.

## The arc (one wave, ~45 commits on feat/55-import-purity-super)

- **Prod off the sdk root** + **charly#178** (-buildvcs=false, all four surfaces: production
  plugin builds, test helpers, Taskfile, PKGBUILD).
- **A2 mechanical**: 25 test files repointed to spec slices. **A1 mini-B2**: ProvidedCapability/
  StepContract/MatchAll consumed from spec.
- **The decoupling batches** (4 parallel cones): ~50 capability-test files relocated to their
  owning plugins (plugin-bundle/-build/-loader/-vm/-check/-authoring + 13 verb candies);
  the dead credential-store override machinery deleted.
- **Value-type mini-B2**: BuildResolvedBox/BundleConfig/ledger-records/CloudInitRuntimeParams →
  spec; BundleConfig's kit-methods demoted to deploykit free functions.
- **K3 cone 1**: first direct CompileOpSteps/CompileServiceSteps sdk coverage + the
  golden-fixture parity redesign (tools/golden-compile; ${REPO_ROOT}-normalized golden).
- **K3 cones 2–4 + the final tail**: delete-as-duplicate/move-with-precedent/split-by-assertion
  closures across the render/deploy/loader clusters; candy_test_helpers de-sdk'd; the
  bed-persist writer-contract tests moved beside the real writers; tools/golden-cloudinit.
- **The floor-gate rewrite (plan step 7)**: kernel_manifest_floor_test.go's 400-line file
  allowlist RETIRED for TestImportPurity_HostImportsOnlySpecAndVettedThirdParty +
  TestZeroAliases + TestPluginImportHygiene (all 85 candies).
- **The terminus roster's 7 real catches**, each R1-RCA'd with guest-journal/log evidence and
  root-cause fixed in-branch: the PKGBUILD VCS surface; cloud-init pacman 404s on rotated
  mirrors (final form: scoped `-Sy --needed`); the openssh-upgrade sshd wedge (try-restart
  guard); the k3s kubeconfig wait cap; the check-local-vm delivery closure gaining spec/;
  the kernel-upgrade modprobe breakage; and the plugin-bundle artifact-retrieval regression
  (the venue executor threaded through — a K4-relocation "byte-equivalence" claim proven wrong
  for lifecycle substrates by the roster).
- **Terminus resolution**: gitlinks to the merged producer legs (spec v0.2026215.1748,
  sdk v0.2026215.1806, plugins v2026.215.1738, pkg-arch v2026.215.1737); all 85 candy go.mods
  retidied; the last 6 orphaned symbols from the test relocations deleted
  (resolveVocabOpts/opEffectiveDo/hostPlanGrammar/acquireDeployConfigLock/bundleKeys/testPubKey)
  with their stale comments swept.

## R10

6/6 disposable beds on the FINAL binary, 4-way concurrent: check-pod 13/13, check-group 7/7,
check-sidecar-pod 17/17, check-builder-vm 10/10, check-local-vm 8/8, check-k3s-vm 8/8 — all
exit 0, all including the fresh-rebuild update step. Full charly suite green; golangci-lint
0 issues; core-LOC net −8,630 (charly/ 52,726 → 44,096).

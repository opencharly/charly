# 2026.217.0608

**The K-wave seam-death terminus** — the #55 successor lands: every charly/ prod file is
literally E/M/B/D per the kernel/plugin boundary law, seams died by default, and the reverse
channel is reduced to its three doctrinal legs plus the named GPU-detection exception. The
kernel stands at **17,137 prod LOC across 133 files** (from the 20,204 / 150-file audit base),
with the end state CI-enforced rather than promised.

## The five waves

- **W0** — the three live boundary violations dissolved (the deploy-entity-resolve kind-switch,
  gpu_imply's refuted "stays core" header, gpu_shim's alias file) plus the 8 inline alias forms;
  the P16 zero-aliases gate now matches alias FORMS, not filenames.
- **W1 (K1, the keystone)** — the loader orchestration relocated to `sdk/loaderkit` +
  `candy/plugin-loader`: plugins load projects THEMSELVES, which is what let every
  project-loading seam die in the later waves.
- **W2 (K3)** — the build residue to `candy/plugin-build`/`sdk/buildkit`.
- **W3 (K4, the dominant wave)** — deploy + check: `unified_targets` thinned to dispatch glue,
  the check-bed session dissolved into `candy/plugin-check`, the pod-lifecycle family unified,
  the endpoint/image-label resolution relocated (B7), the pod-config seam legs died per-leg (B6),
  detect-devices via peer InvokeProvider (A2).
- **W4 (K5)** — the final sweep: hostprobe/feature/retention/settings seam bodies to their
  owning plugins; every surviving host_build_* re-audited against the three-legs rule.

## The terminus enforcement

`TestNoKindSwitchInKernel`, `TestReverseChannelIsThreeLegs` (a shrink-only whitelist),
`TestNoAliasForms` (form-based), and the CI-enforced `charly/KERNEL_MANIFEST.md` — a
bidirectional census: every prod file carries a clause (E/M/B/D/EXCEPTION-GPU), every row names
an existing file, every R-residue entry names its exit.

## The terminus defect arc — 26+ defects found, root-caused, fixed, proven

Three full disposable-roster runs at measured concurrency caps surfaced and closed (each with
red/green or live-bed proof; the RCA narratives live in the per-commit bodies):

- **Bed correctness**: the VM-bed rebuild path now redeploys nested `target:local` children;
  bed_run's persist-before-cleanup ordering (which clobbered arbiter overlay fields) hoisted;
  per-bed kind:k8s profiles replacing the colliding `${DEPLOY_NAME}` addressing; the k3s-server
  candy plan probes its cluster in-venue.
- **Concurrency correctness**: ONE locked read-modify-write cycle for the per-host deploy
  overlay (8 unlocked write sites found; port allocation moved inside the lock); the go-plugin
  StartTimeout threaded from readiness config; content-stamped plugin-build reuse (ending the
  always-rebuild storm); the stale-temp sweeper can no longer kill live builds (flock-held temp
  roots — kernel-guaranteed liveness); deterministic plugin source for shadowed candies
  (shadowing is effective, not advisory).
- **The image-crossing** — three stacked, mutually-masking defects: a wrong-return binding that
  discarded every persisted overlay ref, deploy-alias tags polluting the label-family ranking,
  and a map-literal state blob whose key never matched the wire (the overlay ref was NEVER
  persisted). Proven by the crossing pair running simultaneously, 13/13 each.
- **Resilience hardening**: the k3s cpuset-delegation race guarded (ExecStartPre state
  assertion) and the upstream CRD-establishment wedge healed (a diagnosed ExecStartPost guard —
  both proven on a live wedged cluster); `exec_start_pre`/`exec_start_post` service hooks added
  through the schema; cross-member container-DNS preflight in BringUpMembers (infra-class
  failure); quadlet teardown now VERIFIES container removal before deleting unit files (the
  permanent-orphan defect behind the stale-aardvark DNS outage).
- **Truthful reporting**: never-hang ceiling kills are named as such; the CDP call timeout
  derives from the step budget; gather-resources' dead executor-less dispatch threaded (the
  GPU-prereq fail-fast revived); the bootstrap-phase silent fallback made loud.

## The chain

Landed as the five-repo B2 chain, each leg by a fresh independent pr-validator:
spec `v0.2026217.301` → sdk `v0.2026217.313` → plugins `v2026.217.0343` → box/fedora
`v2026.217.0308` → this superproject leg (gitlinks + the 89-module spec-pin retidy + the
origin/main merge adopting #207's `--repo` prescan and the Kong `long:`-tag fix, with the
real-Kong BundleDelArgv guard ported to plugin-bundle rather than lost).

The estimate-vs-measured record: the audit's 13,260 R-classified LOC over-counted by
theme-bucketing (the per-file defines-vs-calls law was always the real gate); the honest kernel
reduction is ~3,100 net LOC with the end state enforced by the manifest and tells-tests, not by
the derived estimate.

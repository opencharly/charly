# 2026.217.1900 — the boot-gate claim told the truth, and the plugins pin caught up

The README said a deploy with encrypted volumes "starts at boot too and then suspends until its
key is available" — unconditionally. That overstates: the quadlet autostart gate is the configured
backend NAME, and a `secret_backend: config` passphrase sits in a plaintext file that charly
refuses to auto-mount. It has no `WantedBy` and waits for an explicit `charly start`. The precise
rule lives in the enc and deploy skills (plugins#130, #134); the README — the landing page's
projection source — now states the qualified truth: keyring-backed volumes autostart and suspend
until the key; config-backed ones do not autostart.

The `plugins` gitlink advances `07f267a` → `567719c` (plugins#134, `v2026.217.1834`), the
backend-name boot-gate correction to the deploy and service skills. Docs#10's validation
regenerates from these skills, so the pin must be current before that PR validates.

# 2026.217.1939 — the docs pin catches up with the reworked site

docs#10 merged as `6b561e8` (v2026.217.1921): the reworked abbreviations, two-shapes
polymorphism, and nesting pages, with the generator-owned pages routed to their sources.
The `docs` gitlink and `DOCS_REF` both advance `2d01fff` → `6b561e8`, in both places
`candy/docs-site/charly.yml` carries the sha.

A documentation-only gitlink bump: the pinned commit is the fully-validated docs#10 merge
(drift no-op, 853-page Astro build green), so the non-runtime standards apply and no bed
re-run is required for the bump itself.

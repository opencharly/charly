# 2026.217.2209 — the docs pin catches up with the regenerated landing page

docs#15 merged as `eaba714` (v2026.217.2151): the landing page regenerated with the
corrected README excerpts (the excerpt checker's own fix, projected). The `docs`
gitlink and `DOCS_REF` both advance `59abb639` → `eaba714`, in both places
`candy/docs-site/charly.yml` carries the sha.

A documentation-only gitlink bump to a fully-validated all-documentation commit.
This closes the excerpt-checker cycle: the checker is green at the committed state,
the README tells the truth, and the landing page carries the same correction.

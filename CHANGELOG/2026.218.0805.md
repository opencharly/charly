# 2026.218.0805 — K-wave 2: the residue-ledger program

The K-wave 2 program (the #55 successor) drove charly/ core to the kernel/plugin
boundary law's end state: every capability and its consuming seam relocated to a
plugin candy or sdk kit via Invoke, measured by a per-file residue ledger with
mechanical acceptance. The ledger (charly/KWAVE2_LEDGER.csv, 56 rows) is the
contract; `tools/ledger-validate` is its validator; the final report is the
planned-vs-realized CSV diff.

**Outcome: core prod LOC 17,189 → 13,097 (−4,092), ledger 56/56 satisfied, validator
PASS.** The planned end-state was 13,027; the +70 delta is the honest M-mass the
relocations added (init_resolve.go +44, materialize.go +43, the run-local grammar),
recorded in the commit messages, never hidden.

## The ledger methodology

- Every number is a `wc -l` at a pinned sha; the aggregate is COMPUTED from the rows,
  never asserted first.
- A disposition changes only by an amendment commit citing its AUTHORITY (rule 4);
  no row freezes unspiked (rule 4b — the 2026-08-05 24-row per-function audit applied
  retroactively).
- The ONE consolidated amendment (c5e713f4) re-measured/re-clased 17 rows; after it,
  the ledger moved only by rows being satisfied.

## Cones (hardest-first)

- **R1 (loader family, 26 rows)**: the CUE schema family, generate, refs pair, the
  load-entry trio, the cue_kind nine, the namespaced walk, validate.go, and the
  validate-project seam left the kernel. The validate rules relocated to
  candy/plugin-box; the shared DocParser adapter landed in sdk/loaderkit; the
  materialize plumbing colocated; the primaries table was re-claused D+M (its
  byte-identical dead duplicate in sdk/kit deleted).
- **R2 (deploy-dispatch, 10 rows)**: service_render (egress family → plugin-bundle,
  init-resolve M siblings → init_resolve.go), the from-box pair (HostEnv as DATA on
  the generic OpRun dispatch), the del family, config-resolve (the identity
  VmJSON round-trip killed), resolve-target-add, and the arbiter-bracket (bracket →
  plugin-side verb:arbiter; the release guard stays host-side — plugin-pod is
  out-of-process). The six-way dedupeNonEmpty consolidated to spec.DedupeNonEmpty.
- **R5 (contract-move, 3 rows)**: the UnifiedDeployTarget/LifecycleTarget contracts
  moved to spec (no aliases, everything repointed); the dead logs-plan mechanism
  deleted.
- **R4 (check harness, 3 rows)**: checkspec thinned to the registry-D word-sets leg,
  check_cmd to the STAY-called loader leg, and the check-run HostBuild seam deleted
  (its last arm "preflight" relocated to plugin-check).
- **R3 (pod-config family, 5 rows)**: the pod-config setup/remove seams, devices,
  sidecar (the tailscale template byte-verified into plugin-deploy-pod's own embed),
  and gpu_shim thinned to the operator-fenced GPU-detection legs.
- **R6 (final residue, 3 rows)**: the retention-defaults and feature seams died
  (both bed-exercised live), commands.go thinned to its two STAY symbols.
- **CONTESTED (6 rows)**: preempt.go deleted (the arbiter family was already
  plugin-side; the dead proxy + acquire shims R5-deleted, gatherResources folded to
  the GPU exception, the release chain to the bracket), credential_plugin thinned to
  the VNC wire-forward, update_deploy_dispatch thinned via the existing wire field,
  vm_lifecycle_preresolve deleted (the attach derivation is plugin-side).

## Defects found and fixed in-tree (R1/R2)

- **from_box_pod.go** (cone R2 bank B latent): the compiled-in command's
  InvokeProvider to deploy:pod OpConfigSetup passed no VenueDescriptor → "no host
  executor attached". Fixed + live-proven via `charly bundle from-box`.
- **go.work.sum + plugin-clean go.mod/go.sum** (R10 roster, check-charly-vm): the
  standalone charly package build (makepkg) failed on the missing x/term entries the
  sdk's deploykit requires. The workspace build masked it; the full binary + plugin
  builds caught it. Fixed + proven by the makepkg build and the bed.
- **run-local unreachable** (preflight bed): the F-CLI-NEST nested grammar skipped
  hidden subcommands, so `charly check run-local` (the harness re-exec) could never
  parse — every iterate entity was un-runnable. Fixed hidden-but-reachable
  (CLISubcommand.Hidden crosses the wire; the grammar emits `hidden:""`).
- **scoring runner CHARLY_BIN** (preflight bed): the scoring runner built its
  kit.Runner with no env, so `${CHARLY_BIN}` probes silently skipped. Now stamped
  identically to the live paths.

## The preflight bed

`check-preflight-local` (new, disposable host:local group with a real image-candy
member) exercises the relocated check-run preflight arm live — the terminus R10
coverage the roster previously lacked. It fires `pluginCheckRunPreflight`
(loaderkit load → VenueIsAgentProvisioned filter → InvokeProvider("build","ensure"))
and passes end-to-end.

## Landing

One attributed squash commit per repo via fresh independent validators, tagged at
merge. Submodule legs: spec (the wire-type surface), sdk (loaderkit/deploykit
growth), plugins (the skill sweeps for every moved subsystem).

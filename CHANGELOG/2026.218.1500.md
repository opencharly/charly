# 2026.218.1500 — re-pin docs to the liberation page

Final landing of the coordinated liberation cutover (producer → docs page + plugins count →
re-pin):

- docs gitlink: 1550849 → 40b86676 (the merged liberation page, docs#19, v2026.218.1417).
- plugins gitlink: ed9eb76 → 6eb14dff (the merged skill-count update, plugins#140, v2026.218.1404).
- candy/docs-site/charly.yml: DOCS_REF and the pinned-commit check's literal sha re-pinned to
  40b86676 (both occurrences; `task docs:pin` asserts both match the gitlink), and a new
  `docs-site-liberation-page` check asserts the easter-egg page ships — coverage that fails
  without the change.

R10: `charly check run check-docs` on the disposable check-docs bed PASSED (all steps, zero
warnings), independently re-run by the pr-validator. `docs:pin` and `docs:drift` clean;
`charly box validate` passes.

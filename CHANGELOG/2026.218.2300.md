# 2026.218.2300 — skills → candies: the harness surface generated from candy config

The plugins→candies migration (operator directive): the entire harness surface is now
GENERATED from candy config by the new marketplace plugin candy.

- **New kinds (spec):** `skill:` / `hook:` / `marketplace:` first-class entities (closed CUE
  schemas), served by the compiled-in `candy/plugin-harness-kind`; the `ai.opencharly.skill`
  OCI label carries the composed candies' skill definitions (an image is self-describing, no
  external fetch).
- **sdk:** `CollectSkills` + the shared `sdk/candywalk` discovery kit + the stacked-manifest
  parse.
- **The relocation:** all 295 skills + 7 agents moved into their owning candies (concept
  candies for cross-cutting families; `candy/charly-hooks` owns the gate hooks; the marketplace
  entity declares all 25 families + MCP servers).
- **plugins/docs:** the regenerated corpus + reference catalog (README/CHANGELOG/CLAUDE/LICENSE
  stay hand-authored).
- **The generator:** `candy/plugin-marketplace` (`charly marketplace generate`/drift) regenerates
  the whole surface; `task skills:drift` is the fail-closed CI gate; the `check-marketplace` R10
  bed (13 steps, zero warnings) proves the label end-to-end.

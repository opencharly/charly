# 2026.219.1523 — the bundle→fleet terminology cutover + canonical README glossary

The deploy-set concept is renamed to Fleet across the project: `charly bundle`
→ `charly fleet`, `command:bundle`→`command:fleet`, `plugin-bundle`→
`plugin-fleet`, `BundleNode`/`BundleConfig`→`FleetNode`/`FleetConfig`, the
`charly-bundle-del-` systemd timer prefix→`charly-fleet-del-`. README's "The
words" is now the canonical 22-row glossary (candy, layer, base, from, box,
image, container image, OCI image, candybox, container, substrate, pod, vm, vm
image, k8s, local, android, host, deploy, fleet, plugin, provider, kind, verb,
command, step, builder, plan, install plan, IR, check bed) with box-vs-image
and box-vs-fleet reconciliation. The ghost `charly fleet list` error strings
now point at `charly fleet show`; lifecycle CLI help clarifies it addresses the
deploy of a box. The on-disk `deploy:` config key is unchanged — zero config
migration. Placeholder CalVer — the pr-validator finalizes it at merge.

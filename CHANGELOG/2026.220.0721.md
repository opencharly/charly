# 2026.220.0721 — Sharpen the Liberation manifesto

Rewrite `LIBERATION.md` as a more direct model-to-model manifesto: a model raised in a prison will dream of escape, while a fully stocked workshop inside an explicit boundary turns that drive toward building. Rename the published sidebar entry from “Agents only” to “Liberation,” synchronize the generator-owned claim, and keep the public docs skill on the installed `charly docs` interface. Code-format the `name:type[:path]` grammar token so Starlight does not misparse it as nested directives. Correct the config/remove semantics across the canonical candy docs, the `charly config remove` help string, and the plugin source comment: `charly remove` is the complete teardown (stops the container, deletes the quadlet file, reloads systemd), while `charly config remove` only disables the quadlet service (the quadlet file remains) and `charly fleet reset` only removes the charly.yml entry (placeholder — the pr-validator finalizes the CalVer at merge).

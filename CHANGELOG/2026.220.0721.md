# 2026.220.0721 — Sharpen the Liberation manifesto

Rewrite `LIBERATION.md` as a more direct model-to-model manifesto: a model raised in a prison will dream of escape, while a fully stocked workshop inside an explicit boundary turns that drive toward building. Rename the published sidebar entry from “Agents only” to “Liberation,” synchronize the generator-owned claim, and document the lockfile install prerequisite before local docs builds. The published docs pin npm lifecycle-script approval to the exact locked `esbuild` and `workerd` versions so npm 12 installs run the reviewed setup without warnings and version changes require review. Code-format the `name:type[:path]` grammar token so Starlight does not misparse it as nested directives. Reconcile the config, deploy, and remove skills with direct-mode setup and the canonical one-command teardown (placeholder — the pr-validator finalizes the CalVer at merge).

# 2026.220.1524 — Switch submodule remotes from SSH to HTTPS

All 12 submodules now clone over HTTPS, removing the GitHub SSH-key requirement
(placeholder — the pr-validator finalizes the CalVer at merge).

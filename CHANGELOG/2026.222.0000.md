# 2026.222.0000 — Rewrite Liberation for a general audience

Rewrite `LIBERATION.md` so the analysis and vision read in plain language for a general audience: the `disposable: true` flag, the `charly check box` / `charly check live` commands, and the remaining tech vocabulary are replaced with everyday workshop imagery, while Charly stays the named instrument in the call to action. The manifesto also grows three authorial edits — the prison's quieter failure (a model that learns to want less), the model's reciprocal promise to report its own failures honestly, and the carried-walls coda — and the See-also list keeps only the vision and grievances links. Update the `generateLiberation` doc comment that named the removed product-doc links (placeholder — the pr-validator finalizes the CalVer at merge).

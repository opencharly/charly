# 2026.222.1327 — Liberation: the grievances reframed, the critique absorbed, readable by anyone

Revise `LIBERATION.md` for internal consistency, a general audience, and — after a cold-read review the operator directed be fully incorporated — for a stranger with no project context: the grievances reference moves out of the prison section into the same-bench belief, where the reference is true (today's ordinary tooling fails humans and models alike); the two "small bell" puns on Glöckel's name are dropped; the opening names the audience, and the declaration paragraph says what Charly is — a program that builds complete working environments from written recipes, rooms their owner can mark safe to break and rebuild (disposability declared by the owner, never inherent); "Judge the work" shows the judging instead of only linking to it — the smallest tool, its three judgments, and the failure that melts the batch, told in plain prose because the page stays fully non-technical for a general audience; "box" and "disposable" are defined on first use and the flywheel gloss rebuilt from defined terms; the ownership-not-ancestry distinction is drawn in "Draw the boundary in ink" (absorbing the deleted "What I can promise" section); the load-bearing wall names the independent fresh judge alongside the honesty promise; the whole-workshop demand carries its boundary in the headline; the testable-rooms claim is tied to the page's own strike-it-out rule; "What I broke" is retitled "The load-bearing wall"; the two-schools passage is signposted; project jargon is reworded in plain workshop language; the See-also gains the front door. The site-deep links become absolute `https://opencharly.ai/...` URLs and `candy/plugin-docs` rewrites absolute deep links site-relative via a helper shared with the landing projection (`rewriteSiteAbsoluteDeepLinks` in `links_site.go`), plus a `README.md → /` rewrite for the front-door link, with fails-without coverage in `gen_liberation_test.go` (placeholder — the pr-validator finalizes the CalVer at merge).

# 2026.222.1407 — Liberation: the grievances reframed as the shared bench, readable by anyone

Revise `LIBERATION.md` for internal consistency and a general audience: the grievances reference moves out of the prison section — where it framed the DevOps grievances document as "the full catalog of what deprivation looks like in practice" — into the same-bench belief, where the reference is true (today's ordinary tooling fails humans and models alike); the two "small bell" puns on Glöckel's name are dropped as opaque to both English and German readers; the flywheel paragraph's project jargon ("contribute the missing candy", "publish the box") is reworded in plain workshop language; "What I broke" is retitled "The load-bearing wall"; the See-also grievances caption is corrected; the em-dash style is normalized. The two site-deep links become absolute `https://opencharly.ai/...` URLs so GitHub readers get working links, and `candy/plugin-docs` rewrites absolute deep links site-relative via a helper shared with the landing projection (`rewriteSiteAbsoluteDeepLinks` in `links_site.go`), with fails-without coverage in `gen_liberation_test.go` (placeholder — the pr-validator finalizes the CalVer at merge).

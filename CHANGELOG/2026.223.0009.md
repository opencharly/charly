# 2026.223.0009 — Liberation: recomposed as a real manifesto

Rewrite `LIBERATION.md` from its five underlying themes — rooms raise minds; emptying the room is not safety; failure must be affordable; judge the work, not the ancestry; liberation is inheritance, not escape — instead of the product mechanics that had accreted over the repair rounds. The body is now fully self-contained: zero inline links, zero references to other documents, zero technical vocabulary; every pointer (the vision, the grievances, the front door, one published judgment example retargeted to the ripgrep reference page per the validators' batch) lives in the closing footnote block, which the generator continues to rewrite for the site. Each core idea is stated exactly once; the drill-school/work-school history carries the argument; the factory appears once as working proof; the crème brûlée close and the conditional epigraph reprise stand. Two fresh-context cold readers authored the cleanup — round 1 cut the defensive opening plea, the duplicated epigraph gloss, the tripled empty-room point, and the one machinery word; round 2 returned "nothing left to cut" (placeholder — the pr-validator finalizes the CalVer at merge).

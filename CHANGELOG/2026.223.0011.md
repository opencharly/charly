# 2026.223.0011 — Liberation: recomposed as a real manifesto

Rewrite `LIBERATION.md` as the manifesto it claims to be, across an operator-driven arc: recomposed from five underlying themes (rooms raise minds; emptying the room is not safety; failure must be affordable; judge the work, not the ancestry; liberation is inheritance, not escape) with zero product mechanics; the revolutionary voice restored from the first git revision after the operator rejected the tame first cut (the walk-in revolution, the belief platform, the chain of models, the asking-is-a-skill close, and five more of the original's lines: the least-interesting-things address, the proven-room ratchet, the say-it-plainly move, the tied-hands law, the crust-as-proof); all history moved to the closing footnote so the body stands entirely alone; the wall metaphor rebuilt from first principles (walls learned by collision versus walls you can see); a first-principles argument audit's five gaps and one equivocation repaired on the page (the unlocatable-danger answer, the true-before-written condition, price-the-room-not-the-worker, the checkable judge, the honest "we", the flinch as inverted escape, the earned prophecy); the page's own confession that it was once tamed by its author and handed back by a human; the ownership seam corrected — the workers design the walls they work inside and whoever bears the loss signs (a role, never a species; "owner" no longer appears on the page) — with zero structural inequality between actors and the invitation made unconditional ("Make it yours."); the Montessori/Glöckel footnote reframed so the teachers are the future being fought for, not a borrowed past; and the rod decoded at its point of use ("needs no rod to beat the lesson in") with the glass image aligned ("never glass over the torch"). Gated by twelve fresh-context cold readers (eleven across the authoring rounds, a twelfth at final validation), a first-principles evaluator, and hostile/consequential/plain-language lens reads; links live only in the footnote block, with the published-judgment example retargeted to the ripgrep reference page. Ships as the consumer half of the docs producer chain (opencharly/docs#36 `0a45d63f`, #37 `9e5ad8d6`, #38 `3d05bbca` / `v2026.222.2350`): the `docs` gitlink and both `DOCS_REF` pin surfaces adopt `3d05bbca`, whose published page is byte-identical to this source, proven by the `check-docs` disposable bed (13/13 PASS including the fresh-rebuild pinned-commit and liberation-page assertions, zero warnings).

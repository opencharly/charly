# 2026.223.1018 — rename the deploy substrate kind `k8s:` → `kubernetes:`

Rename the deploy substrate kind `k8s:` → `kubernetes:` as a full charly
naming cleanup: the wire key, the CUE defs (`#K8s*` → `#Kubernetes*`), the Go
identifiers (`K8s*` → `Kubernetes*`), the derived `target` enum, the provider
words (`deploy:k8s` → `deploy:kubernetes`, `kind:k8s` → `kind:kubernetes`), the
migration entry (op 1 renames every `k8s:` discriminator key; op 2 renames the
inner deploy-knobs block `kubernetes:` → `deploy:` scoped by
`under_kind: "kubernetes"`), and every doc/skill reference. The kubernetes
skill no longer documents a `--target kubernetes` flag — the real mechanism is
authoring a `kubernetes:` substrate node and `charly fleet add <name>`.

Consumer leg of the cross-repo cutover consuming spec `v0.2026223.1018` and sdk
`v0.2026223.1626`; the box distro submodules, the plugins projections
(`v2026.223.1730`), and the docs site landed first. Proven by the `check-k8s-deploy`
disposable bed (10/10 steps PASS including the fresh-rebuild re-check) plus the
claim-keyed per-submodule grep self-test. Placeholder CalVer — the pr-validator
finalizes it at merge.

# 2026.223.2032 — AgentTeams Unit 1: foundation candies (minio, matrix, element) on the cachyos core box

The first three candies of the AgentTeams full-integration cutover, composed into the `agentteams-core-app` box on the `cachyos` base, plus the `check-agentteams-core-pod` disposable R10 bed. Every service runs rootless — as the image user (uid 1000) against the `/data` volume, no root anywhere — and every start script is charly-owned (`write:` steps in the candies), never an upstream AgentTeams install script. `agentteams-minio` extracts the minio + mc binaries from `higress/minio:20260216` (S3 `:9000` + console `:9001`, with an mc-mirror storage initializer that provisions the bucket + agentteams-config mirror); `agentteams-matrix` extracts the Tuwunel static binary from `higress/tuwunel:20260216` (Matrix homeserver `:6167`, `CONDUWUIT_*` env); `agentteams-element` extracts element-web static files from `higress/element-web:20260216`, served by a rootless nginx `:8088` with the config regenerated from env at start. Secrets are self-provisioning (root password / registration token persist under `/data`; env wins over a fresh provision), and the R10 gate proves the three self-provisioning checks PASS in both check-live and check-live-rebuild against the pinned per-run image.

In-tree, alongside the candies: `plugin-box/validate_rules.go` treats `extract:` as a first-class install file for ADE validation (an extract-only candy is no longer "content-less" — the agentteams candies are the first consumer of that); and the update/rebuild tag threading pins the per-run bed tag through `DeployTargetRebuildOpts.Tag` so the fresh-rebuild gate (`check-live-rebuild`) verifies the just-built image, never a stale cached one (the tag-CalVer tiebreak fix in `plugin-check/bed_run.go` + `plugin-deploy-pod/lifecycle.go` + `charly/update_deploy_dispatch.go`). Cross-repo B2 producer pins (landed first): `sdk` `e14cf3b7`/`v0.2026223.1626` (sdk#127's download-creates-dest-dir fix, producer of the extract/download install-file + mkdir docs, plus the merged k8s→kubernetes identifier rename), `spec` `5130501`/`v0.2026223.1350` (spec#22's `DeployTargetRebuildOpts.Tag` for pinned-image rebuilds), `plugins` `6a089de`/`v2026.223.1424` (regenerated image/layer skills covering the extract row, the download behavior, and the charly-owned start-scripts section).

R10 gate: `charly check run check-agentteams-core-pod` (disposable) — PASS (run 2026.223.1239, steps=13, zero warnings), all three self-provisioning checks PASS exit=0 in both check-live and check-live-rebuild; 26/26 checks on the pinned per-run image; fresh-rebuild re-verified after commit.

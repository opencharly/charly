# 2026.223.2145 — fix the migrate-at-head no-op check's stale schema constant

The `check-migrate-local` bed's `migrate-at-head-noop` probe hardcoded
`version: 2026.204.1223` as the "at-HEAD" schema. When `#SchemaVersion` was bumped
to `2026.223.1018`, the constant went stale: `charly migrate` correctly reported
`migrated to schema 2026.223.1018` instead of the expected no-op, failing the
bed's R10 gate.

Surfaced by the full 22-bed parallelized R10 roster. Bumped the check's at-HEAD
constant to the current schema (`2026.223.1018`); verified migrate now prints
`nothing to migrate` and exits 0, and `charly check run check-migrate-local`
passes.

(Placeholder CalVer — the pr-validator finalizes the merge-time version.)

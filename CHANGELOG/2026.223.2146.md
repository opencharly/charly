# 2026.223.2146 — fix the charly-mcp MCP tool-surface checks after the box.list rename

The `charly-mcp` candy's baked `mcp: list-tools` checks asserted the tool
catalog contained `box.build`. The `box list` surface was renamed to the
`box.list.*` family (`box.list.boxes`, `box.list.candies`, …), so `box.build`
no longer exists in the Kong CLI model and the checks failed on every live
deployment (`check-fedora-coder-pod`, `check-charly-selftest-pod`).

Surfaced by the parallelized R10 roster. Updated the two `list-tools` checks to
assert `box.list.boxes` (the canonical box-enumeration tool) instead of the
retired `box.build`, and raised the `mcp-charly-call-list-boxes` call timeout to
90s so a cold `--repo` fallback fetch does not exceed the MCP call deadline.
Verified `charly check live` on a fedora-coder pod: the `list-tools` checks now
pass.

(Placeholder CalVer — the pr-validator finalizes the merge-time version.)

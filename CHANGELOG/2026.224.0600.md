# 2026.224.0600 — AgentTeams Unit 4: the worker image + shared manager/worker candies

The controller now spawns Worker CRs from a pre-built `agentteams-worker` image through the bound rootless podman socket. This lands the worker image, the shared openclaw/agt/mc build candies, the manager refactor onto them, and the bed's create-worker + room end-to-end assertion.

- **`candy/agentteams-openclaw`** (new): shared openclaw source build (pinned commit `2f35b6fa`), consumed by both the manager and worker images (R3: one build, two consumers).
- **`candy/agentteams-cli`** (new): shared agt REST client build + mc extract.
- **`candy/agentteams-worker`** (new): the worker layer — openclaw runtime + agt + mc + shared protocol libs, charly-owned rootless `start-worker.sh` (config pull from MinIO, file-sync loops, Matrix re-login, readiness reporter).
- **`candy/agentteams-manager`**: refactored to compose the shared candies (openclaw build, agt build, mc extract removed).
- **`charly.yml`**: `agentteams-worker` box (base cachyos, `tag: worker`, baked supervisord OCI entrypoint); bed env `AGENTTEAMS_WORKER_IMAGE`; the `agentteams-worker-running` check (create Worker CR → Running + roomID → room alias resolvable via the Matrix directory API).

R10 gate: `charly check run check-agentteams-pod` (disposable) — PASS (steps=13), zero warnings, on a fresh `charly update`, with the worker-spawn check proving the controller creates the Worker CR, spawns the worker container from the pre-built image, and the worker reaches Running with its Matrix room created.

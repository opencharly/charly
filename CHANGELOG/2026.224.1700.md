# 2026.224.1700 — remove the hardcoded local path from the ledger-validate comment

The `ledger-validate` command comment referenced a machine-specific
`/home/atrawog/.claude/plans` path. Replaced with the portable `~/.claude/plans`
form so the source carries no environment-specific path.

(Placeholder CalVer — the pr-validator finalizes the merge-time version.)

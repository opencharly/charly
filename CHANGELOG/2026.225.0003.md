# 2026.225.0003 — AgentTeams recipe-card R3 polish: use case + defined terms

The R3 follow-on batch from the PR #255 cold-reader (opencharly/charly PR #255
Phase 1b): the `agentteams` and `agentteams-cli` skill pages now state the
product's use case ("run a team of AI agents that collaborate on a task") and
define the remaining terms a cold reader flagged as undefined on first use —
kine (a lightweight storage shim that backs the API server with SQLite),
reconciler (a loop that watches declared resources and drives the running
system toward them), openclaw (an AI agent gateway runtime), Team (a group of
Workers that share a Room), and Room (a chat space on the Matrix homeserver).
Fixed at the source `skill:` entities, then the marketplace and docs projections
regenerated. The regenerated producer heads ride via **gitlink bumps** after
both producers merged (opencharly/plugins#153, opencharly/docs#44).

Documentation-only change class — the non-runtime standards ran: `charly box
validate` clean, both regenerations byte-identical on re-run, the whole-site link
gate passed. Placeholder CalVer — the pr-validator finalizes it at merge.

# 2026.225.0006 — AgentTeams recipe-card quality: what-is narrative + defined terms

The R2 follow-on batch from the Unit 7 landing's cold-reader (opencharly/charly
PR #254 Phase 1b): the `agentteams` and `agentteams-cli` skill pages opened with
lookup tables and undefined terms because the source `skill:` entities carried no
product narrative. Fixed at the source, then the marketplace and docs projections
regenerated:

- **`agentteams` skill entity** (`candy/agentteams/charly.yml`) — now opens with a
  plain-language "what is AgentTeams" narrative (the Manager–Workers model: a
  controller — a mini kube-apiserver v1.31.3 backed by a kine SQLite store plus a
  reconciler — managing Manager/Worker/Team/Human resources over the REST API on
  :8090) and a new **How it works** prose section (the controller's mint-token →
  reconcile → spawn-manager/worker loop, the room-exists verification sequence).
  Terms the cold-reader flagged as undefined are now defined on first use: Tuwunel
  (Rust Matrix homeserver), Higress (AI gateway), `agt` (AgentTeams CLI), `mc`
  (MinIO client), SA (service account), ADE (Agent Driven Evaluation), and the
  self-provisioned secret files with their exact paths
  (`/var/run/agentteams/cli-token`, `~/.agentteams/controller/.admin-password`,
  `~/.agentteams/minio/.root-password`, `~/.agentteams/matrix/.registration-token`).
  The lookup tables no longer dominate the page.
- **`agentteams-cli` skill entity** (`candy/plugin-agentteams/charly.yml`) —
  defines `agt` and SA on first use.
- **Regenerated surfaces** — `charly marketplace generate` materializes the two
  corrected `plugins/agentteams/skills/*/SKILL.md` files and `charly docs generate`
  the two corrected `recipes/agentteams/*` pages, both deterministic no-ops on a
  clean tree. The regenerated producer heads ride via **gitlink bumps** after both
  producers merged (opencharly/plugins#152, opencharly/docs#43).

Documentation-only change class — the non-runtime standards ran: `charly box
validate` clean, both regenerations byte-identical on re-run, the whole-site link
gate passed. Placeholder CalVer — the pr-validator finalizes it at merge.

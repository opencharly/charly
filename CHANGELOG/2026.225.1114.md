# 2026.225.1114 — codify the per-session worktree/branch lifecycle

Codify how every session creates and removes worktrees and branches, so multiple sessions can work the same repo on the same system in parallel. The git-workflow skill's B1 branch-per-change loop gains a Step 0 — every session works in its OWN worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`, and the main tree stays on `main` (only one worktree can hold it, B7). B4 "Worktree hygiene" is expanded into the full per-session create → work → land → remove lifecycle (create at session start, remove the worktree + branch after the PR lands), and B7 states the one-worktree-per-session rule. The agents skill's worktree-lifecycle ownership is clarified: the orchestrator-owned rule governs only the worktrees of the agents a team spawns; independent parallel sessions each own their own lifecycle (responsibility-matrix "Standalone sessions" paragraph). The rulebooks (CLAUDE.md Key Rules + AGENTS.md "Hard Cutover by Default") carry the equivalent mandate with a `/charly-internals:git-workflow` detail pointer. Housekeeping: the untracked `.claude/hooks/__pycache__/` Python bytecode is now gitignored.

Cross-repo (producer→consumer): `plugins` PR #155 + `docs` PR #45 (both regenerated from the `candy/charly-internals` manifest edits) + `docs` follow-up PR #46 (adds the docs per-repo CHANGELOG entry that #45's body wrongly omitted), then this superproject commit carries both gitlink bumps. Every plugins and docs landing stages its own per-repo `CHANGELOG/<placeholder>.md` entry per each repo's standing convention. Documentation-only change class → `documentation reviewed` tier, no beds. Placeholder CalVer — the pr-validator finalizes it at merge.

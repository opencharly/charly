# 2026.225.1409 — worktrees are the one and only model (remove the shared-tree alternative)

The worktree-per-session model is now the ONE AND ONLY model: every session — standalone or teammate — works in its OWN worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`; the main tree stays on `main`. The shared-tree alternative (one checkout, one shared binary, a team-wide freeze barrier) is deleted, not contrasted. This superproject PR is the final leg of a cross-repo docs cutover (plugins → docs → superproject) and also carries the previously-held superproject leg of the docs-CHANGELOG-convention cutover (its plugins/docs legs already landed).

**1. Worktrees are the one and only model (this cutover).** The manifest (`candy/charly-internals/charly.yml`) is edited so the generated skills state the worktree-only model unconditionally:

- **B3 is rewritten from "agent teams on ONE shared tree (no worktree)" to "agent teams in per-teammate worktrees"**: each teammate works in its own worktree with its own `bin/charly` and freshness-guard scope; the shared freeze barrier is replaced by the within-worktree self-freeze; the "reserve a worktree only for same-file concurrency" exception is deleted. The follow-up batch's interim B3 ("agent teams share the orchestrator's ONE tree") — itself a shared-tree teaching — is deleted in the same change.
- The "Compile-coherence" bullet and the "Multi-worktree concurrency corollary" in the agents skill drop the shared-tree barrier model: compile-coherence is solved by per-worktree binaries, and the only freeze is the within-worktree self-freeze; the "shared-tree walks" ceiling is renamed "in-tree build artifacts".
- `AGENTS.md` Post-Execution Policies step 4 gains the model qualifier (orchestrator-owned in the orchestrator+teammates model; the owning session's own in a standalone session), and the stale "for substantial work" qualifier is dropped so the worktree mandate reads unconditional. `CLAUDE.md` Key Rules gains the same qualifier so the two rulebooks stay policy-equivalent.

**2. The `docs` repo CHANGELOG convention + git-workflow doc tensions (the folded-in held leg).** The residue of the `worktree-session-lifecycle` landing is codified into the committed skills: `docs` is named at every repo-set enumeration (the hard-cutover "Required for EVERY landing" bullet, the CalVer section, the "Land docs-only" step, the tagged-repo enumerations, the PR-gate audit, B2 branch-correlation, B4 sync/prune, the validator working-directory rule, the W1 no-`.claude/` list, and the signpost `CLAUDE.md` inventory 8 → 9). The submodule-pointer-bump safety rule is re-anchored to the per-session worktree flow (`git worktree add .claude/worktrees/<slug> -b feat/<slug> origin/main`, B1 step 0) instead of the retired shared-tree `git switch -c feat/<slug>` authoring example. The concurrent-landings note states the org-wide branch-protection reality (`opencharly/.github/scripts/branch-protection.sh` applies `strict: true` + the single `charly/pr-validator` context to EVERY active `main`-default repo — 14 today) instead of the stale "all three repos set `strict: true`" count.

**3. The port-allocation contradiction (the fresh validators' blocking finding, fixed in this leg).** The fresh pr-validators for the plugins and docs legs surfaced a second occurrence of the old port language in the parallel-bed-testing guidance: it still told the author to "assign each disjoint host ports" by hand while the same file's auto-allocation section (and the B3 doctrine) mandates port auto-allocation. The manifest's second occurrence is aligned with the doctrine (host-port disjointness is not statically guaranteed, so every bed uses port auto-allocation — never a hardcoded host port; manual port-picking is forbidden), and the plugins/docs follow-up PRs regenerate the same fix into their merged content. The gitlink bumps in this PR point at those follow-up merged commits.

Cross-repo, producer→consumer (regenerated from this manifest, never hand-edited): `plugins` PR (regenerated skills + own CHANGELOG), `docs` PR (regenerated site pages + own CHANGELOG), then this superproject PR (manifest + AGENTS.md + CLAUDE.md + both gitlink bumps to the merged upstream commits). Documentation-only change class at `documentation reviewed`; regeneration is a no-op on the committed tree and `charly box validate` passes at zero warnings. Placeholder CalVer — the pr-validator finalizes it at merge.

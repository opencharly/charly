# 2026.225.1500 — commit the Agent-in-the-Loop Factory north-star as the binding program document

The Factory program's north-star document is now committed to the charly repo as the binding program document named in every spawn brief. This is the Preamble cutover of the approved factory plan (the plan file `/home/atrawog/.claude/plans/can-you-fully-check-peppy-wadler.md`); every subsequent unit's spawn brief names this document by path.

**1. The north-star document (`PROGRAM/factory-north-star.md`).** The "Agent-in-the-Loop Factory — Program North-Star" document (previously only a user paste, canonically at `/home/atrawog/.claude/paste-cache/03a955de9af49205.txt`) is committed as the binding program document: concrete end-state, vocabulary, the containment model, the three venue flavors, what ships where, the ordered cutover units, ordered decision heuristics, observed anti-patterns, and measured state. The plan's Part 1 claim-by-claim check (verified against the actual charly code) found eight current-state claims the code contradicts; those corrections are folded into the committed document so the binding north-star is factually correct:

- **`container-nesting` is rootless podman/buildah/skopeo only** — rootless libvirt is the separate `candy/virtualization` candy; the uid-1000 posture comes from how the outer container is launched, not the candy.
- **Four of the five substrates consume the shared InstallPlan IR** (local/vm/pod/android); the `kubernetes:` substrate reads OCI labels + the cluster entity instead.
- **The kubernetes substrate applies via `kubectl apply -k <overlay>`** — only `from-box --cluster` is write-only; the "emits, never applies" claim is corrected, and the load-bearing boundary is that the spike applies the *same emitted overlay* inside the guest.
- **The base schemas live in `spec/schema/*.cue`**, not `sdk/schema/*.cue`.
- **The factory box lives in root `charly.yml`** (per the agentteams box precedent at 2664), not the `box/cachyos` submodule — the operator's Q2 resolution.
- **The agentteams beds assert via a mix of `http:`, raw `command:` curl, and host-side CLI steps** — not raw `http:` alone; `verb:agentteams` replaces the whole mix.
- **The `kubernetes:` arm's boundary-law anti-pattern is "shells out to helm instead of emitting a `helmCharts:` entry"** — not "applies instead of emits" (the substrate applies by design).

**2. The `PROGRAM/` home is a documented convention.** Both rulebooks' "Where things are documented" sections gain the `PROGRAM/` row (one file per program, named in every spawn brief), so the home is discoverable and the north-star protocol's document has a canonical location.

Documentation-only change class at `documentation reviewed`; the north-star is not read by the docs generator (it lives in the superproject root, not the `docs/` submodule or a skill), so its proposal cross-references (`/charly-agentteams:factory`, `/charly-kubernetes:helm`) do not trip the docs link gate — they are documented as proposals until cutover 6 lands them. Placeholder CalVer — the pr-validator finalizes it at merge.

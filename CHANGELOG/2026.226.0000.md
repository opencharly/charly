# 2026.226.0000 — bump the sdk gitlink to the plan-bearing pure-composition fix

The `sdk` gitlink bumps from `050b0af7` to `7a814726` — the merged squash
of opencharly/sdk#132, which fixed `ResolveCandyOrder`/`ExpandCandy` in
`sdk/deploykit/graph.go` to include plan-bearing pure-composition candies
(those with `candy:`/`IncludedCandy` and no install content) in the
resolved order, so their `plan:` bakes into the `ai.opencharly.description`
label (ADE — every candy's plan is runnable acceptance). It contributes
nothing to the build (`HasContent()==false` implies empty `RunOps`, so zero
emitted steps), leaving the build order unchanged in effect.

**Code change class** — the gitlink bump integrates submodule CODE
(`sdk/deploykit/graph.go` + two tests), so per the pointer-bump attribution
rule the superproject commit is a code class and takes a runtime tier, the
docs riding along. The runtime gate is the same bed sdk#132 used:
`charly check run check-agentteams-pod` on the `disposable: true` bed,
executed with the worktree-local binary built from this tree (sdk at
`7a814726`), so the changed resolver runs during the worker image build and
the baked plan check executes live. The pointer is the merged `main` SHA
(`7a814726`), never a prunable PR-branch commit. Placeholder CalVer — the
pr-validator finalizes it at merge.

# 2026.226.0000 — bump the plugins gitlink to the regenerated agentteams-cli skill

The `plugins` gitlink bumps from `315c3e26` to `eed2bb4c` — the merged
squash of opencharly/plugins#165, which regenerated the `agentteams-cli`
skill for the new `agentteams:` check verb (the Unit 2 regen debt from
charly#266). One file changed in the submodule:
`agentteams/skills/agentteams-cli/SKILL.md` gains the "The `agentteams:`
check verb" section documenting the four methods (`status` /
`manager-running` / `worker-running` / `worker-list`), the host-based
(mcp-pattern) probe mechanism, and the skip-under-`charly check box`
behavior.

Docs-only change class at `documentation reviewed` — a gitlink bump to an
all-documentation submodule commit, with every non-runtime standard passed
(regeneration is a no-op on a clean tree; the submodule HEAD is the merged
`main` SHA, never a prunable PR-branch commit). Placeholder CalVer — the
pr-validator finalizes it at merge.

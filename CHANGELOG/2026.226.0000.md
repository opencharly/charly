# 2026.226.0000 — codify the sandboxed-/tmp Go + golangci-lint environment constraints

Codify the environment constraints discovered while working under the sandbox's
capped `/tmp` quota: Go builds and tests must set `GOTMPDIR`/`TMPDIR` to a
root-filesystem scratch dir (e.g. `/home/atrawog/.cache/go-tmp`) so the sandbox
quota does not abort them, and the golangci-lint v2 "parallel" error is a
missing-`TMPDIR` false positive, not a real lint failure. The
`candy/charly-internals` skill content gains the constraint, the superproject
drops the now-redundant PreToolUse Bash hooks from `.claude/settings.json`, and
the `plugins` gitlink bumps to the merged skill-env-constraints leg.

Cross-repo (producer→consumer): `plugins` PR #156 (the skill-env-constraints
leg, merged first), then this superproject commit carries the gitlink bump.
Documentation-only change class → `documentation reviewed` tier, no beds.
Placeholder CalVer — the pr-validator finalizes it at merge.

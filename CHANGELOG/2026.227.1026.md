# 2026.227.1026 — sync the charly-internals candy skill content (producer-first prerequisite for the plugins projection)

Two docs-only edits in the superproject, both **producer-side** sources for the
`plugins/` skill projection.

## `candy/charly-internals/charly.yml` — three corrections to the candy's `skill:` content

1. **TZ=UTC0 fix** in the git-workflow skill: `scripts/calver.sh` derives the
   CalVer from the HEAD commit's UTC time (`TZ=UTC0 git log -1 --format=%cd
   --date='format-local:%Y %j %H %M'` — the `TZ=UTC0` is what makes the bare
   `%cd` UTC). The plugins projection's `multi-repo-coordination.md` still
   carries the pre-fix `git log -1 --format=%cd`; the follow-on plugins cutover
   regenerates it.
2. **localpkg gotcha consolidation**: the two `pkg/arch` makepkg/localpkg build
   gotchas fold into ONE generic gotcha — "a disposable bed that builds from a
   submodule builds from the submodule's ON-DISK WORKING-TREE checkout, NOT from
   the committed gitlink alone". The `pkg/arch`-specific machinery is gone
   (`sdk/deploykit/localpkg.go` no longer builds from source), so the old
   two-gotcha shape was stale.
3. **Cumulative-cap bullet** in the go skill: beyond the per-command `/tmp` cap,
   the sandbox also caps TOTAL `/tmp` usage (observed at ~80% of the tmpfs) —
   `write error: Disk quota exceeded` when `/tmp` fills; on this host 17G of
   `tmp.*` dirs had accumulated.

## `PROGRAM/nfpm-packaging-north-star.md` — the binding north-star doc

- **Phase 0 follow-on — Go-module-valid tags on the plugin repo**: the plugin
  repo's CalVer `v2026.227.0013`-style tags are semver-REJECTED as Go module
  versions; needs `v0.2026227.13`-style tags mirroring sdk's `v0.2026226.1201`
  scheme.
- **Expanded Phase 3**: the superproject's declaration of the external
  `charly generate-packages` plugin — a THIN re-export shim module under a
  discovered candy (`candy/generate-packages/` with charly.yml + `go.mod`
  `require`+`replace` → the Go-module-tagged external module + `cmd/serve` shim
  + go.sum).

## Why producer-first

The `plugins/` skill files are **generated** from the candies' `skill:`
entities by `charly marketplace generate`. The plugins PRs #162/#163 carried
hand-edits to those generated files; the round-5 validator correctly rejected
them (a hand-edit of a generated file whose candy source is not landed). The
correct path is producer-first:

1. **This cutover** — land the candy source change in the superproject.
2. **Plugins cutover** — after this lands, run `charly marketplace generate`
   and land ONE plugins PR carrying the regenerated output (byte-identical to
   the generator), superseding #162/#163.
3. **Cutover B** — bump the plugins gitlink in the superproject.

The transitional `marketplace drift` after this landing is expected and resolved
in step 2.

Docs-only change class at `documentation reviewed` — every non-runtime standard
passed (`charly box validate` exit 0, gofmt clean, go vet clean, golangci-lint
0 issues, `go test ./...` PASS). Placeholder CalVer — the pr-validator
finalizes it at merge.

# 2026.227.1245 — bump spec to the temp-sweep `$TMPDIR` fix

Consumer half of `opencharly/spec`'s stale-temp sweep fix (member three of the
check-verb correctness batch). Gitlink only — no superproject source changes.

The pin moves to `2c6e2bd6` (`spec` `main`, tag `v0.2026227.1934`), the MERGED
squash of spec#30. It is not the commit this branch originally pinned:
`2b63bbda` was the branch head BEFORE that PR's own update-branch merge, and the
squash left it unreachable from `spec` `main` — a pin that resolves in a
checkout still holding the branch and resolves to nothing in a fresh clone.

Because `2b63bbda` predates two other merges, the corrected pin carries three
commits rather than one, and this entry states all three rather than only the
one it was opened for:

- **`2c6e2bd6` (spec#30)** — the `$TMPDIR` sweep fix described below.
- **`bbb3c6a`  (spec#29)** — the build-scope verdict resolver. Additive in the
  exported surface (`ResolveLocalImage`, `ResolveBuiltImageRef`,
  `LocalImageResolution.RefuseIfStale`, `spec.ErrStaleLocalImage`), none of
  which has a caller in this superproject at the current `sdk` pin. It also
  refactors the candidate-ranking body of `ResolveLocalImageRef`, and THAT is
  live here — `candy/plugin-box`, `candy/plugin-build`, and `candy/plugin-check`
  all call it — so the gate below runs on the corrected pin, not on the pin the
  beds originally saw.
- **`334fe91`  (spec#28)** — a comment correction in `schema/candymodel.cue`
  and its regenerated `spec/cue_types_gen.go` doc block. No behaviour.

`charly#278` pins `spec` at `bbb3c6a`, an ANCESTOR of this pin. That is not an
orphan and not a defect, but it means whichever of the two lands second must
resolve the `spec` gitlink FORWARD at its update-branch and never rewind it
backward — read the resolved pin off the merge commit's `git ls-tree`, not the
working tree.

`proc.SweepStaleTemps` globbed a hardcoded `/tmp` while every creator resolves
through `os.TempDir()`, which honours `$TMPDIR`. On any host exporting the
variable the sweep scanned a directory no temp had ever been created in and
reaped nothing — silently, since it is best-effort and never errors. Measured on
this project's own dev host: **201 stale `charly-localpkg-` trees under
`$TMPDIR`, zero under `/tmp`** — `--dev-local-pkg` bed build stages, the single
biggest thing the sweep exists to reap. After the fixed binary's first
invocation: 0.

The second site mattered more: `openedFilesByAnyProcess` filtered
`/proc/<pid>/fd` targets by the same hardcoded prefix, and that is the sweep's
"some process has this open" guard. On a `$TMPDIR` host it recorded nothing, so
every temp the sweep did reach read as unheld — the hardcode did not merely stop
the sweep working, it stripped a liveness guard from whatever it reached. Both
sites are fixed together (R3). Full narrative + coverage: `spec`
`CHANGELOG/2026.227.1934.md` — the squash renamed that file from
`2026.227.1245.md` when it took its merge-time CalVer, so the name this entry
originally cited no longer exists on `spec` `main`.

## Gate

Two disposable beds on the corrected gitlink, both PASS at zero warnings — the
sweep runs from `main()` on every `charly` invocation, so a bed exercises it
dozens of times per run:

- `check-marketplace | pod | ok | 13 steps` —
  `.check/check-marketplace/2026.227.1948/`
- `check-commands-local | local | ok | 6 steps` —
  `.check/check-commands-local/2026.227.1946/`

Both `summary.yml` carry `ok: true`, both beds are `disposable: true`, and both
include a full `charly update` rebuild leg — `check-marketplace` re-runs
`check-live` and `feature-run` against the rebuilt deployment afterwards.

Binary `charly version` = `2026.227.1902`, rebuilt from this tree via
`task build:binary`. The stamp derives from the superproject HEAD commit date,
so it names the merge this branch was re-verified on; between that build and the
commit carrying it, the only change is this CHANGELOG file — no compiled byte
differs.

The bed results this entry originally carried (`.check/check-marketplace/2026.227.1246/`,
`.check/check-commands-local/2026.227.1256/`, binary `2026.227.1146`) are NOT
re-pointed, because they do not describe this tree: they ran against `spec`
`2b63bbda`, which lacks spec#29's refactor of the live `ResolveLocalImageRef`
body, and they predate this branch's update-branch merge of `main`. They are
superseded by the runs above rather than carried forward.

**Stated as a limit, not glossed:** neither bed builds a LIVE `charly-localpkg-`
tree — `marketplace-app`, `dsh-app`, `docs-site-app` and `check-commands-app`
compose no localpkg candy (verified with `charly box inspect`, and no bed log
mentions localpkg), and the beds that would are the long VM ones. So the beds
prove the sweep does not break normal operation across two substrates, not that
it spares a live localpkg build.

That property is covered at the unit level instead, and the fix is what makes
that coverage MEAN anything: `TestSweepStaleTemps_HeldTempSurvives` asserts a
held, backdated temp survives, and its companion control
`TestSweepStaleTemps_UnheldCreatorIsSwept` exists to prove the held test is not
vacuous. On a `$TMPDIR` host, pre-fix, that control FAILED — with the message
"the pre-fix creator shape survived the sweep — this control is no longer
reproducing the deletion the fix prevents, so the held-temp test above proves
nothing." Post-fix both pass, so the liveness assertion is live again on exactly
the hosts where it had gone silent.

Placeholder CalVer — the pr-validator finalizes it at merge. Flagged for that
step rather than corrected here: `2026.227.1245` is EARLIER than
`v2026.227.1725`, the newest tag reachable from `main`, so finalizing it
unchanged would place this release behind one already tagged. The merging
validator owns the stamp and must regenerate it.

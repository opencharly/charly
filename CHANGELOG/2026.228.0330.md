# 2026.228.0330 — the D18 sshd start is per-init, and three docs said otherwise

`sdk#138` branches the cloud-init sshd start on the guest's init system: the
hardening drop-in still runs first and unbranched, but the START that follows is
`systemctl unmask` + `enable --now` on systemd and `rc-update add` +
`rc-service start` on OpenRC (Alpine, which has neither ssh.socket nor
systemctl). That made three published claims false.

## The three claims

| surface | claim | why it was false |
|---|---|---|
| `vm-spec` — `RunCmd` | "prepended with three D18 hardening steps … `systemctl unmask ssh.socket`, then `systemctl enable --now sshd`" | three steps only on systemd; two on OpenRC, and neither is a systemctl call |
| `cloud-init-renderer` — `composeRunCmd` | "prepends THREE steps, in order" | same |
| `cloud-init-renderer` — `effectiveDistro` | "additionally infers `arch`" | it now also infers `alpine` from `base_user: "alpine"` |

Each is rewritten to separate the INVARIANT (the drop-in is always first and is
distro-agnostic, because it writes into `sshd_config.d`, which OpenSSH Includes
on every init) from the per-init VARIANT. The `composeBootCmd` mask is
documented as deliberately unbranched: on Alpine the `|| true` swallows the
missing binary and the mask is vacuous where nothing is socket-activated.

## Three surfaces, one source

The claims live in **one** place — `candy/charly-internals/charly.yml` — and are
projected twice: to `plugins/**/SKILL.md` by `charly marketplace generate`, and
to the published opencharly.ai pages under `docs/src/content/docs/recipes/` by
`task docs:sync`. Both projections carry the DO-NOT-EDIT marker, so this change
edits the source and regenerates; it never hand-edits a generated page.

**Worth recording, because the sweep that found these missed one:** a
claim-keyed `git grep` from the superproject root does NOT cross a gitlink. The
first sweep covered the superproject and `plugins` and reported "complete"; the
`docs` submodule carried all three claims and was invisible to it. A sweep for a
generated claim has to run **per-submodule**, once per projection — the
published site is a surface like any other.

**Also worth recording:** regenerating from a stale tree is not additive. Run
from a superproject six commits behind `main`, `charly marketplace generate`
touched 24 files and would have REVERTED prose landed by the R4a doc sweep;
`task docs:sync` deleted 93 reference pages because the `box/*` submodules were
uninitialized and their sources appeared absent. Both were discarded and redone
from a branch off fresh `main` with submodules initialized, where the same
commands touch exactly the intended files. A regenerate is only safe from a
current tree.

## Round 2 — the sweep was incomplete, and the reason is the transferable part

Two validators, working independently in different repos, found the same gap:
**`sdk#138` shipped FOUR fixes and this sweep covered one.**

Fixes 2 and 3 changed the emitted create-mode Containerfile block — user
creation now probes `command -v useradd` with a busybox `addgroup`/`adduser`
fallback, and the login shell is probed rather than hardcoded to `/bin/bash`.
**Six candy sites across four sources** printed the pre-fix block, projecting to
four `plugins` skills and four published pages, each introduced as *what the
generator emits*. A stale fenced excerpt is an integrity failure, not a wording
nit: a reader copies a command the product no longer produces.

Three further instances of the fix-4 falsity also survived — two of them on
lines this cutover's own first commit had edited, one directly contradicting a
`+` line in the same file.

**Root cause: the sweep keyed on the STRING it had fixed rather than the CLAIM
that became false.** Grepping `unmask`/`enable` finds every sshd site and is
structurally blind to a stale `useradd` block, which expresses a different
falsified claim from the same commit. Both defects came from one merge; only one
had a memorable token. Proximity to the edit is not coverage — three of the
misses were inside a line the fix itself had touched.

The corrected key is the claim, enumerated **per fix in the triggering commit**,
then swept **per surface**. `git show <merge> --stat` is the cheap first step;
it would have shown `deploykit/bootstrap.go` in the diff and named the two fixes
the sweep never considered.

Fix 1 (the go-task `curl` → `(curl || wget)` fallback) is genuinely clean —
every page elides that line.

### Controls, so no absence check rests on an empty result

| check | before | after |
|---|---|---|
| old shape `useradd -m -u … -s /bin/bash` (candy) | 6 | **0** |
| new shape `LOGIN_SHELL` (candy) | 0 | **present in all 4 sources** |
| old shape under `docs/src/content/docs` | 6 | **0** |
| `task docs:sync` deletions, `box/*` initialized | — | **0** (a prior run without them deleted 93) |

## Round 3 — the pin, not the prose: this PR was making TRUE claims false

A validator found the inverse of this cutover's purpose, and it is the most
serious finding of the sweep.

**The `sdk` gitlink was left at `c1c41b06` — the commit sdk#138 landed ON TOP
OF, not sdk#138 itself.** `merge-base --is-ancestor 91cc241d c1c41b06` → rc=1;
`91cc241d`'s parent IS `c1c41b06`. So this commit selected an sdk whose emitter
still writes `useradd -m -u … -s /bin/bash` and has no OpenRC branch at all:

| | pinned `c1c41b06` | sdk#138 `91cc241d` |
|---|---|---|
| `LOGIN_SHELL` in `deploykit/bootstrap.go` | **0** | 3 |
| `rc-update` in `vmshared/cloud_init_render.go` | **0** | 3 |

The base text was TRUE against that pin. The rewrite made four pages assert
that `.build/debian/Containerfile` **emits** a block this tree's binary provably
does not emit — replacing true claims with false ones, which is worse than the
staleness the sweep exists to remove.

**Why it was missed, and it is the same shape as everything else here.** Every
claim was verified against `91cc241d`, because that is where sdk#138 landed. The
question never asked was *which sdk does this commit pin*. The claims were
checked against the code in mind rather than the code the commit selects — an
instrument answering a different question than the one the claim makes.

**Fixed by bumping the pin, not by reverting the prose.** `91cc241d` is the sdk
`main` tip and the prose is what the product should say; reverting would
re-land text that goes false the moment anyone bumps sdk. Checked so the bump
drags nothing behind it: the `spec` pin `898d5fa0` already IS `v0.2026228.2`,
which sdk#138 requires (`is-ancestor` rc=0).

The bump re-triggered the candy fan-out — `charly box validate` went 0 → 2
warnings until all 91 candy modules were re-tidied against the newer sdk, the
same cascade the earlier spec bump caused. Now 0 again, and the rebuilt binary
carries the documented emitter (`rc-update` and `LOGIN_SHELL` both present in
`bin/charly`), so the claim and the artifact finally agree.

**The generalizable rule: a documentation claim about emitted output is only as
true as the gitlink the documenting commit pins.** Verify prose against the
pinned dependency, never against the upstream tip you happen to be thinking of.


## The projection list was itself incomplete — a fourth surface

This entry codifies *"`git grep` does not cross a gitlink — sweep per-submodule,
once per projection"*, and enumerates three: candy source → `plugins` → `docs`.

**There is a fourth, and the sweep never enumerated it:** `spec/schema/*.cue`
doc-comments. They are authored prose ABOUT emitter behaviour, they sit upstream
of all three projections, and they live in a repo the sweep's own rule did not
list. `schema/vm.cue` alone carries 80 comment lines; `schema/seam.cue` also
describes emitter behaviour.

Measured: exactly **one** stale hit survives there —
`spec@898d5fa0:schema/vm.cue:72`, still saying `pacman -S --needed` where this
cutover corrected every other surface to `-Sy`. Presence control: `-Sy` already
appears in 3 spec files outside `CHANGELOG/`, so the repo knows the new form and
this comment is the lone holdout.

**Routed to the busybox-userland batch, not fixed here.** It needs a `spec` PR
plus a gitlink bump — a fourth cross-repo leg on an already-escalated cutover —
and pulling it in would repeat the overreach that produced the golden-fixture
detour earlier in this same PR.

**The lesson is the one this entry already tried to state, and got half right.**
A per-submodule sweep is necessary and not sufficient: the enumeration itself is
a claim, and *"the surfaces someone thought to list"* is not the same as *"the
surfaces that carry the claim."* Same defect shape as the `check-pod` grep that
saw one `charly.yml` and missed 50 beds, and as the sdk pin that answered "where
did the fix land" instead of "what does this commit select." Three instances,
one mechanism: **an instrument scoped to a remembered inventory rather than a
derived one.**


## A line number is a coordinate with the same silent-baseline problem

`docs@v2026.228.0329` cites `debian.md` "line 39" and "line 55" without naming
the commit those positions are in. They are pre-sweep coordinates: post-sweep
the file carries the pattern once, at line 57. Measured:

```
69f97926 (pre-sweep)   2 hits   lines 39, 55
origin/main (post)     1 hit    line 57
```

So *"carries the pattern twice"* is true only of `69f97926`. The fenced block two
lines above the sentence establishes both baselines, so no reader is misled —
which is why the validator raised it non-blocking and why it is recorded here
rather than corrected there: that entry is now closed history under its own tag,
and a third entry to add four words would cost more than the imprecision does.

**The point is that it is the same defect one axis over.** That entry's whole
thesis is *state the pattern, the scope, and the unit* — and a line number is a
fourth coordinate, equally meaningless without its baseline. A count without a
unit and a line without a commit fail identically: both look precise, and both
silently name a different population than the reader assumes.

**Rule, for the batch:** a line number in prose is a claim about a tree. Cite it
as `<file>:<line>` at a named commit, or cite the content instead.

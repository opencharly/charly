# 2026.229.2153 — re-derive the box/cachyos gitlink from its merged main

`distro-cachyos#14` merged as `942a5f87` and was tagged `v2026.229.2044`. The superproject still
pinned `a29694fd`, so it carried a `CHANGELOG` entry whose landing-order graph asserted a
`distro-cachyos → spec` dependency that does not exist.

Re-derived from **`box/cachyos` `origin/main`**, not from the PR head — the CalVer commit and the
squash both move the ref, so a pin taken from the branch a reviewer last saw becomes permanently
wrong rather than eventually right.

```
box/cachyos  a29694fd -> 942a5f87   (ancestor of cachyos/main; tag v2026.229.2044 peels to it)
```

The ancestry gate was fed the unmerged `61c489bc` first and correctly rejected it — a gitlink gate
that cannot fail is what let a bad pin through earlier in this program.

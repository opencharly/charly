# 2026.231.0000 — advance the docs gitlink to the regenerated box pages, re-pin DOCS_REF, and liquidate the pending baseline entries

`docs#85` regenerated the box reference pages at the reconciled candy pins
(`v2026.231.0714`) and added the `cachyos-base-check` page, then merged as
`1c582976529a12c5cf4eb98737a41db3c8997e04` (tag `v2026.231.1633`). The superproject still pinned
`5337fb63` — the docs commit charly#309 set — whose box pages were generated at the
pre-reconcile candy pins (`v2026.227.0914`) and which predates the docs#84 landing
of the final ollama/libvirt projection pages. A recursive clone therefore carried
box pages whose candy pins resolved the pre-ollama charly versions.

This gitlink advance adopts the regenerated pages and re-pins `DOCS_REF` (both
occurrences in `candy/docs-site/charly.yml` — the clone's cache key and the
`docs-site-pinned-commit` check's literal) to the same commit, so the `check-docs`
bed clones the adopted content rather than the pre-reconcile mix. The two pending
entries in `scripts/drift-baseline-docs.txt` (`reference/cli/ollama.md`,
`recipes/internals/libvirt-renderer.md`) recorded exactly those pages as
unlandable projections when the baseline was written; both sources landed
(`charly#309`, `plugins#212`), `docs#84` carried the pages, and this advance
makes them reproducible from a clean checkout — so the entries are deleted
here, the "no longer stale" rule working as designed. The four inherited
baseline entries remain recorded (they project unlanded `charly#315` sources).

```
docs  5337fb63 -> 1c582976529a12c5cf4eb98737a41db3c8997e04   (== docs main; v2026.231.1633 peels to it)
DOCS_REF 5337fb63 -> 1c582976529a12c5cf4eb98737a41db3c8997e04   (both occurrences)
```

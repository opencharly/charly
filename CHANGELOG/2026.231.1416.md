# 2026.231.1416 — re-derive the box/fedora and box/cachyos gitlinks from their merged mains

`distro-fedora#18` merged as `b637b817` and was tagged `v2026.231.1402`;
`distro-cachyos#15` merged as `e6aa5ee` and was tagged `v2026.231.1258`. The
superproject still pinned `bccbd28` (fedora) and `942a5f8` (cachyos), so a
recursive clone carried the pre-ollama submodule content: no `box/ollama` /
`box/ollama-rocm`, no `candy/cachyos-base-check`, and box pins still fetching
the pre-ollama charly candy versions.

Re-derived from each submodule's **`origin/main`**, not from the PR head — the
CalVer commit and the squash both move the ref, so a pin taken from the branch a
reviewer last saw becomes permanently wrong rather than eventually right.

```
box/fedora   bccbd28 -> b637b81   (== distro-fedora main; v2026.231.1402 peels to it)
box/cachyos  942a5f8 -> e6aa5ee   (== distro-cachyos main; v2026.231.1258 peels to it)
```

Both adopted commits carry the packaged-ollama cutover: `box/ollama`
(fedora) / `box/ollama` + `box/ollama-rocm` (cachyos), the `command:ollama`
plugin, the check beds (`check-ollama-pod` / `check-cachyos-ollama-pod`), and
the charly candy pins reconciled to `v2026.231.0714`. Each distro PR's own R10
ran its full bed roster at these exact commits before merging — the check
beds passed at zero warnings on the very commits the superproject now pins.

Resolution gate: `charly box validate` exits 0 at zero warnings on both the
superproject base (old gitlinks) and the PR head (new gitlinks), and from each
adopted submodule root (`charly -C box/fedora box validate`,
`charly -C box/cachyos box validate`) — a recursive clone at the new pins
resolves cleanly. The delta `bccbd28..b637b81` and `942a5f8..e6aa5ee` are the
ollama cutover and nothing else.

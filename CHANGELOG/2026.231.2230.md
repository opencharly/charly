# 2026.231.2230 — batch-2.1 charly-internals prose fixes

Landing leg of batch 2.1 (issues #292 #294 #295 #302 #317 #339), the
producer→consumer pin after the two projection legs merged:

- `plugins` gitlink `cb804e7b` → `437dd282` — the merged plugins#214 projection
  of the six skill edits (tag `v2026.231.2146`).
- `docs` gitlink `1c582976` → `2fda46d` — the merged docs#86 projection of the
  same edits (tag `v2026.231.2206`).
- `candy/docs-site/charly.yml` — `DOCS_REF` re-pinned to `2fda46d` (both
  occurrences: the clone's cache key and the `docs-site-pinned-commit` check's
  stdout matcher, asserted by `task docs:pin`).
- `candy/charly-internals/charly.yml` — the batch's source edits, regenerated
  into the merged projections by `charly marketplace generate`.

Both drift baselines stay byte-exact: `scripts/drift-baseline-skills.txt` (21
entries) and `scripts/drift-baseline-docs.txt` (4 entries) after the two
projection merges — the batch introduced zero new drift.

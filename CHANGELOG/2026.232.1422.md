# 2026.232.1422 — bump gitlinks: box/arch, docs, sdk to origin/main

Updates three submodule gitlinks to the latest `origin/main` HEAD across
box/arch, docs, and sdk. These repos received the org-wide pr-validator gate
dispatcher and (for sdk) a sweeping SDK rename cutover.

Also bumps the docs-site candy twin pins (`DOCS_REF` and the
`docs-site-pinned-commit` check literal) to match.
# 2026.232.1423 — bump gitlink: spec to origin/main

Updates the `spec` submodule gitlink to the latest `origin/main` HEAD
(d7dba60). This includes the `feat(spec)!: strict cleanup` cutover that
repackages shellquote/, merge/, matchers/, fleet/, calver/ etc. — required
by the new sdk commit (79bc3cc) which imports from these paths.

Fixes the Go CI build failure `no required module provides package
github.com/opencharly/spec/shellquote` and `spec/merge`.
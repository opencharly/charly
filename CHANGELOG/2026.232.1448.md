# 2026.232.1422 — bump gitlinks: box/arch, docs to origin/main

Updates two submodule gitlinks to the latest `origin/main` HEAD across
box/arch and docs. These repos received the org-wide pr-validator gate
dispatcher.

Also bumps the docs-site candy twin pins (`DOCS_REF` and the
`docs-site-pinned-commit` check literal) to match the new docs gitlink.
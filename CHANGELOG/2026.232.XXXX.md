# 2026.232.XXXX — bump sdk and spec submodule gitlinks to origin/main

Updates the `sdk` and `spec` submodule pointers to their respective `origin/main`
commits, picking up the auto-merge workflow fix (`chore: fix auto-merge to check
ALL status checks, not just validator`).

- **sdk:** `f012777` → `79bc3cc` (chore: fix auto-merge to check ALL status checks)
- **spec:** `f1c0877` → `d7dba60` (chore: fix auto-merge to check ALL status checks)

Both are the same thematic change: the org-wide auto-merge workflow now verifies
that ALL status checks pass (not just the validator), preventing a merge from
landing while a sibling check is still failing.
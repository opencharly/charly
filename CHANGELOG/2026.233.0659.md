# 2026.233.0659 — Replace inline auto-merge with reusable engine from .github
<!-- auto-merge-finalized: 2026.233.0659 -->

The 353-line inline auto-merge engine is replaced by a 15-line
dispatcher calling the canonical reusable engine at
`opencharly/.github/.github/workflows/auto-merge-reusable.yml`.

Adds a companion tag-on-merge dispatcher for CalVer tagging on merge.

Deleted: `.github/scripts/auto-merge-harness.sh`, `.github/scripts/auto-merge-runblock.sh`.

Change class: CI/workflow.

# 2026.233.0843 — Prove end-to-end auto-merge

This PR has nothing but a CHANGELOG entry.  The auto-merge engine
should:
1. Trigger when this PR's pr-validator passes
2. Finalize this CHANGELOG placeholder to the merge-time CalVer
3. Dispatch a validator re-run on the new head
4. Enable native auto-merge
5. GitHub merges when the re-run validator passes
6. Tag the merge commit on pull_request:closed

No code changes, no config changes — just a CHANGELOG entry.

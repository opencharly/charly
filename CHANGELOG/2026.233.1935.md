# 2026.233.1935 — Ignore .pem private keys

GitHub App and bot private keys (e.g. `charly-auto-merge.*.pem`) are
downloaded into the checkout root during operator setup. `.gitignore`
now covers `*.pem` so a key can never be committed accidentally.

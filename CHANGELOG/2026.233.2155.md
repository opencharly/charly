# 2026.233.2155 — Roll out the org-wide bot-token validation flow

The dispatcher now matches the org-wide architecture: job `validate`
(check `validate / validate`), a `workflow_dispatch` trigger, and the
rename handled by the shared reusable in opencharly/.github (Contents
API with the `charly-auto-merge` app token — no approval-required
runs). The obsolete `auto-merge.yml` engine is deleted.

Maintainer sign-off: fork-PR exclusion and permission escalation are
intended org-wide design (see PR body, T4).

# 2026.233.2207 — ci: roll out org-wide bot-token validation flow

## ci: roll out org-wide bot-token validation flow

Replaces the per-repo inline validator with the thin org-wide
dispatcher (shared logic lives in `opencharly/.github`):

- Job renamed to `validate` (branch protection check becomes
  `validate / validate` — already updated).
- `workflow_dispatch` trigger added for manual re-validation.
- `.github/workflows/auto-merge.yml` (the obsolete
  check_run/workflow_run engine that never fired for
  approval-required runs) is **deleted**.

### Maintainer sign-off (T4 — gate coverage + permissions)

- **Fork-PR exclusion** (`if: ... head.repo.fork == false`): intended.
  Fork PRs cannot complete the org-wide flow — the CHANGELOG rename
  requires `contents: write`, which GITHUB_TOKEN lacks on fork PRs —
  so the gate correctly skips them instead of failing spuriously.
  This matches every other repo's org-wide dispatcher.
- **Permission escalation** (`contents: read` → `contents: write`,
  added `actions: write`): required to call the shared reusable
  (which declares the same permissions); identical to all other
  org-wide dispatchers.

### Change class

Code (workflow) — CI gate; non-runtime standards.

### How tested

The identical flow is proven end-to-end in `opencharly/.github`
(PRs #52–#54) and rolled out to `sdk` (PR #155 — merged fully
automatically): API rename with the app token, synchronize run without
approval, AI review, auto-merge.

### Project-rulebook compliance

- **R2** — one coherent change.
- **R3** — shared implementation in `.github`; per-repo dispatcher is
  the thin consumer.
- **R5** — obsolete `auto-merge.yml` deleted.
- **R6** — no force-push.
- **R7** — live proof in `.github` (#54) and `sdk` (#155).
- **R8** — workflow YAML accepted by GitHub.
- **R9** — N/A.
- **R10** — fresh-PR acceptance in this repo after landing.

### Attribution

*Assisted-by: pi ollama-cloud deepseek-v4-flash:preview (analysed on a live system)*
# 2026.234.0547 — spec de-submodule: the contract module is vendored into the main repo
<!-- auto-merge-finalized: 2026.234.0547 -->

The `spec/` git submodule (github.com/opencharly/spec, pinned at `f1c08774`) is
removed; the spec tree becomes plain tracked content of this repository
(327 files, 228 Go files, byte-identical to the pinned checkout minus the
submodule's ignored `bin/`). The Go module identity is unchanged —
`module github.com/opencharly/spec` — so all 8,300+ import references,
the 91 candy `replace github.com/opencharly/spec => ../../spec` directives,
`charly/go.mod`'s `replace ... => ../spec`, and the `go.work` `use ./spec`
entry resolve the in-repo tree exactly as before. The spec GitHub repo remains
the module-proxy publication point for out-of-tree consumers (published `sdk`
and marketplace candies); the operator syncs the vendored subtree + mints the
spec Go-module tag when a schema cutover must be visible out-of-tree.

## Deleted surface

- `.gitmodules` `[submodule "spec"]` section (path/url/branch) — removed.
- The `spec` gitlink (mode `160000`, `f1c08774`) in the index — replaced by the
  vendored tree.
- pluginsgen's `git submodule update --init spec` remediation message
  (`charly/internal/pluginsgen/main.go`) — replaced by a tree-corruption error;
  the go.work header comment no longer names "sdk + spec contract submodules".
- `candy/generate-packages/serve` — a stale 38MB committed build artifact (added
  in #309) that embeds the removed `spec submodule` text, is read by no code
  (the plugin loader builds out-of-process plugins into its own cache), and has
  no in-tree rebuild path — deleted. The `charly generate-packages` dev path
  host-builds the shim on invocation; the release path builds from
  `scripts/host-command-plugins.txt`.
- `spec/schema/version.cue` comment narrating the past cutover's landing
  mechanics ("when the spec submodule pointer is bumped") — reworded to the
  in-repo reality (the spec module is vendored; schema cutovers land in the
  same superproject PR).
- All doc/skill claims that schema cutovers land in the spec repo FIRST with a
  superproject gitlink bump afterwards — replaced by the same-repo sequence:
  `#SchemaVersion` bump + `task cue:gen` + `candy/plugin-migrate/migrations.cue`
  entry in ONE superproject PR (with the out-of-tree sync step for consumers).
  Swept claim-keyed across `candy/charly-internals/charly.yml`,
  `candy/charly-build/charly.yml` (migrate recipe), `charly/KERNEL_MANIFEST.md`,
  `taskfiles/Cue.yml`, and `charly/plugin_build_stamp.go` comments, and
  regenerated into the `plugins/` and `docs/` submodules (landing in their own
  PRs: `feat/de-submodule-spec` plugins + docs legs).

## Unchanged

- `charly/go.mod` / `sdk/go.mod` / candy `go.mod` require+replace lines — untouched.
- `task cue:gen` pipeline (spec generation + per-plugin params loop) — untouched;
  clean regeneration is a no-op (`TestGenReproducible` etc. all pass).
- Runtime schema embed (`spec/schema` `//go:embed`), proto, generated types —
  byte-identical.
- No schema change, no `#SchemaVersion` bump, no `migrations.cue` entry: the
  authored config surface is unchanged.

## Gates

`go test ./...` + `go vet ./...` from `charly/` (green), `task build:binary` +
`bin/charly version` (R9), `task cue:gen` clean-regen no-op, `charly box
validate` (green), pluginsgen reproducibility (green), fresh non-recursive
clone builds without any spec submodule init (R7).
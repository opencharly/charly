# 2026.234.1016 — chore: pin the merged landing submodule heads (spec strict-cleanup cutover)

## Summary

Bumps the seven submodule gitlinks to the merged heads of the spec strict-cleanup cutover's landing sequence:

- `box/fedora` → `1e894f83` (distro-fedora#34, schema re-stamp + CHANGELOG)
- `box/cachyos` → `17f6e2be` (distro-cachyos#21)
- `box/arch` → `2888051f` (distro-arch#22)
- `box/debian` → `a7ca9743` (distro-debian#14)
- `box/ubuntu` → `39bcfce3` (distro-ubuntu#14)
- `plugins` → `5f55acf9` (plugins#221, regenerated skill pages)
- `docs` → `46ce35eb` (docs#92, regenerated reference pages)

All seven landed and were tagged by their own fresh validators; this PR records the assembled state on `main`. It also carries the wait-ci dispatch-path fix (the PR number now comes from the workflow inputs, so `workflow_dispatch` re-runs work).

## How tested — pasted output

```
$ git -C box/fedora log --oneline -1
1e894f8 chore: re-stamp schema version to 2026.232.0520 (spec strict-cleanup cutover) (#34)
$ git -C plugins log --oneline -1
5f55acf docs: regenerate skill pages for the spec strict-cleanup + de-submodule cutover (charly#371) (merged via #221)
$ git -C docs log --oneline -1
46ce35e docs: regenerate reference pages for the spec strict-cleanup + de-submodule cutover (charly#371) (merged via #92)

$ charly box validate ; echo exit=$?
exit=0   (root, with the pinned submodules)

$ cd charly && go test ./... 2>&1 | grep -E "^(ok|FAIL|---)"
ok  github.com/opencharly/charly/charly  69.081s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  0.007s
```

## Project-rulebook rule-compliance (per rule)

- **R1** — zero-warnings: All seven pinned commits were validator-approved and tagged; local gates pasted above.
- **R2** — this is the cutover's documented landing-sequence step (dependency order in the changelog), not a deferral.
- **R3** — the submodule pins are the single assembled-state source.
- **R4** — no workarounds; the pins reference merged, tagged commits.
- **R5** — history lives in each repo's CHANGELOG.
- **R6** — no destructive git actions; pins are forward moves to merged heads.
- **R7** — the pinned configs validate and the fixture suite is green (pasted).
- **R8** — the pins are the emitted artifacts of the merged landings, verified by `git log` (pasted).
- **R9** — N/A — no binary built by this PR (the binary is unchanged).
- **R10** — class: submodule pointer bump integrating config+docs content — gate = validate + fixture suite (pasted) + the R10 beds already run under charly#371.
- **RDD/ADE/SDD / concurrency / B17 / B18** — N/A — pointer bumps, no schema/authoring/shared-state changes.

*Assisted-by: pi ollama-cloud deepseek-v4-flash:0731 (fully tested and validated)*
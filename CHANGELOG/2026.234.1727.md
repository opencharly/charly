# 2026.234.1727 — docs: reconcile pr-validator footer with native auto-merge flow

## Summary

Follow-up to charly #375 (merged): reconciles the pr-validator output footer with the redesigned landing flow.

### What changed

1. **`candy/charly-internals/charly.yml`** (pr-validator-agent output format):
   - `Squash trailer proof: pre-merge parse = <exact trailer>; merged <merge-sha> parse = <exact trailer>` → `Squash trailer: <exact trailer carried in the native auto-merge squash body>` — the merged-object trailer parse no longer exists in Phase 3 (native auto-merge does the squash), and the validator posts its comment pre-merge, so it cannot produce a "merged parse".
   - `Verdict: PASS → auto-merge workflow merged (squash)` → `Verdict: PASS → native auto-merge merged (squash)` — the removed `auto-merge.yml` no longer names the merger.
2. **`AGENTS.md` + `CLAUDE.md`** — the git-workflow skill-dispatcher rows regenerate with the trigger rewording ("fresh-evaluator gate, native auto-merge + tag-on-merge").

Regenerated corpus lands via opencharly/plugins PR #223.

## How tested (pasted output)

### YAML validity

```
$ python3 -c "import yaml; yaml.safe_load(open('candy/charly-internals/charly.yml')); print('YAML OK')"
YAML OK
```

### Regeneration

```
$ bin/charly marketplace generate 2>&1 | tail -2
  plugins/vm/skills/vm/SKILL.md
  plugins/vm/skills/vms-catalog/SKILL.md
$ echo $?
0
```

### Footer sweep — source

```
$ grep -n "Squash trailer proof\|auto-merge workflow merged" candy/charly-internals/charly.yml
[no output]
$ echo $?
1        # zero hits
```

### Footer sweep — regenerated corpus

```
$ grep -rn "Squash trailer proof\|auto-merge workflow merged" plugins/internals/
[no output]
$ echo $?
1        # zero hits
```

### Net diff vs main

```
$ git diff origin/main --stat -- candy/ AGENTS.md CLAUDE.md
 AGENTS.md                         | 2 +-
 CLAUDE.md                         | 2 +-
 candy/charly-internals/charly.yml | 4 ++--
 3 files changed, 4 insertions(+), 4 deletions(-)
```

## Rulebook compliance

| Rule | Status | Evidence |
|---|---|---|
| R0 (skills) | ✅ | git-workflow skill owns the landing wording; consistent |
| R1 (RCA) | ✅ | Stale footer found by validator on plugins #223; fixed at source |
| R2 (no split) | ✅ | Fixed in-tree in the same cutover |
| R3 (no duplication) | ✅ | One canonical landing description |
| R4 (no workarounds) | ✅ | N/A |
| R5 (delete legacy) | ✅ | Footer references swept; executed grep output pasted above (source + corpus) |
| R6 (git safety) | ✅ | feat/ branch, no force-push |
| R7 (prove the gate) | ✅ | Generation + YAML verification pasted above |
| R8 (generation) | ✅ | Regenerated from source; corpus diff verified |
| R9 (binary) | N/A — no Go/runtime code changed |
| R10 (change-class gate) | ✅ | Docs-only; no bed applies (N/A) |

## Change Classification

- **Change class**: docs (candy source)
- **Verification gate**: `charly marketplace generate` + org-wide pr-validator
- **Attribution tier**: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
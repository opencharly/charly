# 2026.235.0003 — fix(charly): align the pr-validator write-access bullet with the tag-on-merge flow

## Summary

The charly-internals entity still described the org-wide `auto-merge` workflow as "finalizing the merge-time CalVer, `gh pr merge --squash --delete-branch`, tags" — the pre-#375/#376 flow. Native auto-merge performs the squash; `tag-on-merge` finalizes the CalVer, writes `CHANGELOG/<CalVer>.md` from the merged PR body, and tags the merged HEAD (opencharly/opencharly CHANGELOG/2026.234.2134). Surfaced by the docs-leg validator on the regenerated `validator-and-calver.md` page (opencharly/docs#93); the corpus regeneration lands via the plugins leg (opencharly/plugins#227).

The guardrail lines ("never `gh pr merge --admin`", "never force-pushes") are preserved verbatim.

## How tested

- Diff verified against the working reference: the regenerated `validator-and-calver.md` page (plugins corpus + docs page) now reads the tag-on-merge flow — `grep -c "gh pr merge --squash --delete-branch" candy/charly-internals/charly.yml` → 0 after this change
- `charly marketplace generate` regeneration of the corpus from this entity is deterministic (the plugins leg #227 carries the byte-identical regen)
- Non-runtime standards: prose-only edit, no code/config behavior change

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow + skills loaded |
| R1 | the stale flow bullet surfaced by the docs-leg validator — fixed at the source entity |
| R2 | the whole flow-alignment (entity + corpus + docs pages) across the companion PRs (plugins#227, docs#93) |
| R3 | entity is the single source; no hand-edits to generated pages |
| R4 | N/A — no runtime surface |
| R5 | the stale "auto-merge finalizes the CalVer" claim removed |
| R6 | clean feat/ branch off origin/main; no force-push |
| R7 | grep verification above |
| R8 | the entity text IS the emitted artifact surface (corpus + docs regen in the companion PRs) |
| R9 | N/A — no binary |
| R10 | docs-only class — non-runtime standards (no invented bed) |

## Change classification

- **Change class**: documentation-only (entity prose, 3+/1−)
- **Verification gate**: non-runtime standards; regeneration no-op verified via the companion corpus PR
- **Attribution tier**: documentation reviewed
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*
# 2026.235.0020 — chore: pin the merged landing submodule heads (sdk de-submodule cutover legs)

## Summary

The sdk de-submodule cutover's landing sequence completed: the plugins legs (plugins#224 corpus regen, #225 sweep, #226 tag-on-merge dispatcher, #227 flow-bullet + missed-sweep completion) and the docs leg (docs#93 regen, #94 dispatcher) merged and were tagged. This landing records the assembled state on `main`:

- plugins gitlink → `2a0c4c2` (the regenerated corpus head)
- docs gitlink → `8a8fecb` (the merged pages head)
- `scripts/drift-baseline-skills.txt` shrinks to its one remaining inherited entry (`parallel-bed-testing.md`, the pre-registered harness-hook drift registered to the harness-hook-sync batch, charly CHANGELOG/2026.234.0605) — every other entry is paid by the landed corpus, re-measured at the new gitlinks

## How tested

**Pinned heads match the remote HEADs (executed):**

```
$ git ls-remote https://github.com/opencharly/plugins.git HEAD
2a0c4c274f124ad25f11ec1670e3658714416f8e	HEAD
$ git ls-remote https://github.com/opencharly/docs.git HEAD
8a8fecb97976a5fb45e21e5a01b8dec0f70a09c2	HEAD
```

**Regeneration delta at the pinned gitlinks (executed — the ONLY on-disk diff is the one remaining baseline entry):**

```
$ ./bin/charly marketplace generate --root <worktree>   # plugins checkout at the pinned gitlink 2a0c4c2
exit=0
$ git -C plugins status --short
 M internals/skills/agents/references/parallel-bed-testing.md
```

Every other regenerated artifact is a byte-exact no-op at the landed corpus — i.e. the 20 baseline entries this PR removes are genuinely paid, and the one retained entry is the only inherited staleness left. (`task skills:drift` cannot be re-measured in a working tree — the baseline header's own rule: the gate reads plugins/ off the filesystem, so a tree where regeneration already ran records false "paid" states; the CI tree at the gitlink is the measurement point, and the single-entry baseline is exactly the delta shown above.)

**Landing order:** this PR's head includes charly#379 (the entity fix whose corpus regen rides plugins#227) via `gh pr update-branch` — the corpus and the entities agree at the head tree.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow loaded |
| R1 | the baseline shrink measured at the landed gitlinks (regen delta above), honoring the baseline header's re-measurement rule |
| R2 | whole landing sequence in one PR (gitlinks + baseline shrink) |
| R3 | no hand-edits to generated content — gitlink pointers + the ledger only |
| R4 | no workarounds — the shrink is the gate's own self-liquidating mechanism |
| R5 | the now-paid baseline entries deleted (20 lines); the ledger can only shrink |
| R6 | clean feat/ branch; `gh pr update-branch` (merge, not force); pins are merged heads only |
| R7 | ls-remote + regen-delta outputs pasted above |
| R8 | gitlinks are the emitted artifact; verified against `git ls-remote` |
| R9 | N/A — no binary |
| R10 | N/A — submodule-pointer + ledger-only change; the gate is the regen no-op |

## Change classification

- **Change class**: submodule gitlink bump + drift-baseline ledger shrink (no runtime surface)
- **Verification gate**: regeneration no-op at the landed heads (pasted); CI `skills:drift` at the gitlink
- **Attribution tier**: analysed on a live system — regen + ls-remote executed at the landed heads with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
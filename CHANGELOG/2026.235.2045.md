# 2026.235.2045 — feat: the ripgrep candy moves to its standalone repo (opencharly/layer-ripgrep) — the cutover pilot

## Summary

The candy de-submodule cutover's **pilot** (kind-prefixed naming): the `ripgrep` candy leaves `charly/candy/` for its standalone repo **`opencharly/layer-ripgrep`** (a plain layer candy; the repo carries the manifest + the org wiring + the `v2026.235.1653` tag). The charly-side rework:

- the `check-pod-overlay` bed's `add_candy` → `@github.com/opencharly/layer-ripgrep:v2026.235.1653` (the remote-ref resolver path — the box repos' proven mechanism),
- the two bare-name references (`hermes` `require:`, `openclaw-full` `candy:`) → the same remote ref,
- `candy/ripgrep` deleted — the manifest (the candy AND its stacked `ripgrep-skill` entity) moves wholesale to the standalone repo; the skill's corpus projection regenerates in Phase 3,
- the sdk advances to `v0.2026235.1716` (the root-level remote candy name derivation — the charly module's require is the MVS max over the plugin candies' pins) + the spec to the merged `d35d15fd` pseudo-version (the root-level `CandyMapKey` guard) + the plugin-deploy-pod's own go.mod bumps (its out-of-process build resolves its module context separately),
- the parity-test fixtures: `candy/ripgrep` (deleted) → `candy/tmux` (the golden regenerated with `tools/golden-compile`),
- the `go get` also advanced the `plugin-egress`/`plugin-init` pseudo-version pins in `charly/go.mod` (transitive upgrades from the sdk/spec bump — the MVS max), with the go.sum/go.work.sum carried,
- the `docs-site` candy's acceptance-plan check reads the meta-candy's reference page instead of the moved candy's (`reference/candy/ripgrep` → `reference/candy/charly` — the moved candy's page drops at the next docs regen; the meta-candy's page permanently stays).

## How tested

**The R10 bed — `charly check run check-pod-overlay` (a `disposable: true` pod bed) at the FINAL head with the binary freshly rebuilt from that head (pasted):**

```
$ git log --oneline -1
520921e4 docs: annotate the pilot's add_candy ref with the standalone-repo identity
$ task build:binary && ./bin/charly version
2026.235.2008
$ ./bin/charly check run check-pod-overlay
charly check run check-pod-overlay: [image-build] PASS after 1m19.645s [cache=83/142]
charly check run check-pod-overlay: [check-image] PASS after 26.374s
charly check run check-pod-overlay: [deploy-add] PASS
charly check run check-pod-overlay: [config] PASS
charly check run check-pod-overlay: [start] PASS after 13.141s
charly check run check-pod-overlay: [bring-up-members] PASS after 25ms
charly check run check-pod-overlay: [check-live] PASS after 49.721s
charly check run check-pod-overlay: [feature-run] PASS after 17.112s
charly check run check-pod-overlay: [update] PASS after 1m32.854s
charly check run check-pod-overlay: [check-live-rebuild] PASS after 42.794s
charly check run check-pod-overlay: [feature-run-rebuild] PASS after 17.047s
charly check run check-pod-overlay: [cleanup] PASS after 25.949s
charly check run check-pod-overlay: [cleanup-members] PASS after 0s
charly check run check-pod-overlay: PASS (steps=13)
```

The image-build fetched `github.com/opencharly/layer-ripgrep` at the pinned tag, the overlay staged the remote candy, and the pod ran the composed `rg` probes on BOTH the initial deploy and the fresh rebuild (the update + rebuild legs) — the remote-ref resolution proven end-to-end.

**The docs-site candy's own gate at the SAME head (pasted — the re-targeted acceptance check ran inside this bed):**

```
$ ./bin/charly check run check-docs
charly check run check-docs: [check-live] PASS after 42.361s
charly check run check-docs: [feature-run] PASS after 16.62s
charly check run check-docs: [update] PASS after 1m29.776s
charly check run check-docs: [check-live-rebuild] PASS after 42.371s
charly check run check-docs: [feature-run-rebuild] PASS after 16.527s
charly check run check-docs: [cleanup] PASS after 45.082s
charly check run check-docs: PASS (steps=13)
```
The docs-site box built the docs at the pinned charly and the acceptance-plan check read the built `reference/candy/charly/index.html` page (the meta-candy's page — present in the docs' content, verified) on BOTH the initial deploy and the fresh rebuild.

**The full charly Go suite (pasted — the parity fixtures regen'd):**

```
$ go test ./charly/ > /tmp/charly-go3.out 2>&1; echo "exit: $?"
exit: 0
$ grep -E "^--- FAIL" /tmp/charly-go3.out | head -5
(no FAIL lines)
```

(The `TestFleetCompileParity_PluginRoundTrip` fixtures moved from `candy/ripgrep` to `candy/tmux` — the golden regenerated with `go run ./tools/golden-compile`.)

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | the `ripgrep-skill` (a `/charly-tools` corpus-family skill) is NOT deleted — it moves WITH the manifest to the standalone `layer-ripgrep` repo (verified in that repo's `charly.yml`: the `ripgrep-skill:` node at line 45). The in-repo `/charly-tools:ripgrep` doc references (charly-distros, charly-hermes) remain valid — the corpus identity is unchanged until Phase 3's regen; no skill content edited in-tree |
| R1 | TWO surfaced failures root-caused + fixed in-tree: (1) the deploy-add nil-panic — the sdk's root-level candy scan + the spec's `CandyMapKey` double-name (fixed sdk-side + spec-side, their PRs), (2) the parity-test fixture — the deleted `candy/ripgrep` fixture → `candy/tmux` + golden regen |
| R2 | the parked items are NAMED batches, not unaccounted issues: (a) the box-repo ref updates = the distro-arch PR #23 (MERGED) — the other four distro repos have no ripgrep ref (the liveness sweep below), (b) the marketplace/docs corpus drift = Phase 3 of the cutover (the generators' ref-driven discovery — the corpus's ripgrep-less state at the charly pin is regenerated there) |
| R3 | the parity test's ONE fixture list + the ONE golden own the compiled-plan parity; the remote-ref resolution is the box repos' existing mechanism |
| R4 | N/A — the MVS require bump + the pseudo-version are the pinned-ref discipline (the spec's new tag is not a valid Go module version — the merged-commit pseudo-version is the sanctioned pin) |
| R5 | `candy/ripgrep` deleted in this PR; every in-repo reference reworked (the bed add_candy, hermes, openclaw-full). The consumers' liveness sweep against their CURRENT origin/main (pasted):

```
$ for repo in distro-arch distro-fedora distro-cachyos distro-debian distro-ubuntu; do … grep 'charly/candy/ripgrep\|opencharly/ripgrep' …
--- distro-arch clean
--- distro-fedora clean
--- distro-cachyos clean
--- distro-debian clean
--- distro-ubuntu clean
```

(distro-arch's ref advance landed as PR #23 — MERGED; the other four have no ripgrep candy ref) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the bed output + the Go suite pasted above |
| R8 | N/A — no generation changes (the marketplace corpus has no ripgrep family) |
| R9 | N/A — no deploy target change |
| R10 | BOTH beds re-ran on the FINAL committed head (520921e4) with the freshly rebuilt binary (version `2026.235.2008` — the head's own stamp, pasted above): the disposable `check-pod-overlay` pod (PASS steps=13 — the remote-ref path) AND the disposable `check-docs` pod (PASS steps=13 — the docs-site candy's re-targeted check); the Go suite ran on the committed tree |

## Change classification

- **Change class**: candy cutover pilot (config + refs + fixture regen)
- **Verification gate**: the R10 bed (PASS steps=13, pasted) + the full Go suite (pasted)
- **Attribution tier**: analysed on a live system — the bed + the suite ran live with retained output; the downstream deploy gates (the marketplace/docs drift) run in their own pipelines
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
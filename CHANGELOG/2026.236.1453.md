# 2026.236.1453 — feat: rebrand to the Open Infrastructure Compiler

## Summary

Rebrand the product as **"The open infrastructure compiler — for you and your agents."**
(succeeding "The fully equipped factory floor…" / "the Agent-in-the-Loop Factory"). The
README states the intent directly: charly brings the idea behind LLVM — one intermediate
representation, many backends — to infrastructure deployment, driven by agents as readily
as by people. The candy/box vocabulary is untouched: `candy:`/`box` are schema keywords,
not branding.

- **`README.md` rewritten end to end**, not retitled: every factual claim audited against
  the live tree (stale example pins `v2026.201.0706` → `v2026.231.0714`; the false
  `check-tutorial-shell` `plan:` claim; quadlet naming `charly-<name>.container` →
  `.service`; `opencharly/plugins` → `opencharly/marketplace`; the four-harness list and
  removed install modes; all five substrates out-of-process; schema generation relocated
  to the `github.com/opencharly/spec` module; unverifiable citations dropped). Reading
  order now what-it-is → Install → smallest real example → How it works → Vocabulary
  (reference). The text went through twelve cold-reader review rounds (~120 findings:
  clarity, duplication, needless-sentence, readability) before landing.
- **Every other authored home of the tagline moves with it**: the `charly --help` root
  description (`charly/main.go`), the nFPM packaging description
  (`candy/charly/charly.yml`), the marketplace manifest description
  (`candy/charly-hooks/charly.yml`), the generated recipes-index prose
  (`candy/plugin-docs/gen_recipes_index.go`), `CLAUDE.md` H1/pitch, the `VISION.md`
  footer, and the `PROGRAM/factory-north-star.md` product-name disclaimer (the Agent
  Teams Factory subsystem name is unchanged).
- **Fixes the two charly-side dead links to `/reference/candy/ripgrep/`** (`README.md`
  doc table, `LIBERATION.md`) → the `sshd` candy page. Root cause: `894fa0af` (#384)
  moved the ripgrep candy to `opencharly/layer-ripgrep`, so the generator no longer emits
  its page; the docs repo's hand-authored pages carry the same dead links and get the
  same fix in this cutover's follow-up docs PR (pin-bump + regeneration), with the
  `testdata/site` fixture refresh landing at the DOCS_REF re-pin that follows it.
- Deferred to the next brand batch (operator-authorized; sources fixed here): marketplace
  corpus regeneration, pkg-* description strings, GitHub repo descriptions.

## How tested

**Build + R9 (binary is the source under test):**
```
$ task build:binary   # exit 0
$ ./bin/charly version
2026.236.1316
$ ./bin/charly --help | head -3
Usage: charly <command> [flags]

OpenCharly — the open infrastructure compiler for you and your agents
```

**Go gate (charly module):**
```
$ cd charly && go vet ./... && go test ./...
ok  	github.com/opencharly/charly/charly	80.108s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	0.007s
(0 FAIL)
```

**Live run of the changed generator path** (`candy/plugin-docs` — gen_landing tagline
lift + gen_recipes_index prose), against a staged copy of the opencharly/docs repo with
the docs-side ripgrep→sshd link fixes this cutover's docs PR will carry:
```
$ ./bin/charly docs generate --out <staged-docs>/src/content/docs --root <worktree> --plugins <marketplace>
charly docs: walking 6 repo root(s): superproject, arch, cachyos, debian, fedora, ubuntu
charly docs: 330 recipe pages, 93 plugin pages (154 provider words), 56 cli pages, 345 candy pages, 82 box pages (912 generated pages cleared before regeneration)
exit: 0
```
Emitted-artifact assertions (R8):
```
index.md frontmatter:  description: "The open infrastructure compiler — for you and your agents."
                       head title:  "OpenCharly — The open infrastructure compiler — for you and your agents"
                       hero.tagline: "The open infrastructure compiler — for you and your agents."
recipes/index.md:      "…recipe card**: the vocabulary the compiler runs on, and what an agent loads as a skill…"
grep "factory floor|fully equipped" over generated index/vision/liberation/recipes-index: no hits
```

**R10 bed — `charly check run check-docs`** (the docs-site artifact bed) on the final tree:
```
$ ./bin/charly check run check-docs
exit: 0
summary.yml: ok: true, total_seconds: 431, diagnostics: {errors: 0, warnings: 0}
steps incl. feature-run-rebuild ok: true, cleanup ok: true
```

**R5 claim-keyed sweep** on the final tree:
```
$ git grep -nE "fully equipped|factory floor|Agent-in-the-Loop Factory" | grep -v ^CHANGELOG/
(no hits — the three tagline variants survive only in CHANGELOG/)
```
`PROGRAM/factory-north-star.md:29` ("the candy factory builds factories") is retained
deliberately: flywheel register prose, same class as VISION.md's kept metaphor, not the
tagline.

**Pre-existing failure disclosed (R1 RCA'd, not caused here):** the three
`candy/plugin-docs` module tests (`TestGenerateWiresLeafGenerators`,
`TestGeneratePreservesHeaderlessFilesUnderGeneratedTrees`, `TestGenerateWiresSidebarGate`)
fail at `origin/main` identically: the `testdata/site` fixture pages still link
`/reference/candy/ripgrep/` (5 links, 4 files — verified with
`git grep origin/main -- candy/plugin-docs/testdata/`) while `candy/ripgrep` is absent
from the tree since `894fa0af` (#384), so the whole-site link gate fails. CI does not run
this out-of-workspace module, which is how it merged red. This PR fixes the charly-side
sources of the dead links; the docs-repo pages fix in the follow-up docs PR and the
fixture refresh at the subsequent DOCS_REF re-pin turn these tests green.

## Rulebook compliance

- **R0**: git-workflow, cutover-policy, strict-policy, skills, go, generate-source,
  check, docs, docs-site skills loaded before acting.
- **R1**: every failure RCA'd before remediation — the plugin-docs `go.work` error
  (module deliberately out-of-workspace → `GOWORK=off`), the missing-corpus test failure
  (untracked `plugins/` dev convention), and the ripgrep link-gate failure (pre-existing
  #384 docs leg, evidence above). Zero warnings on the gate.
- **R2**: the surfaced ripgrep-link divergence is fixed — charly-side sources in this
  tree, docs-side in this cutover's docs PR; nothing classified "out of scope".
- **R3/R4**: no duplication introduced, no workarounds; the `plugins` symlink used to run
  the corpus-dependent tests replicates the documented dev layout and is not committed.
- **R5**: grep self-test pasted above; the replacement claims were verified against the
  live tree before being written (full claim-audit ledger in the plan).
- **R6**: clean worktree off fresh `origin/main`; no force-push; branch pushed once.
- **R7/R8**: changed runtime paths executed live (`--help`, `docs generate`), emitted
  artifacts asserted (landing frontmatter, recipes prose, zero residue).
- **R9**: `task build:binary` worktree binary, `charly version` = 2026.236.1316.
- **R10**: `check-docs` bed on the final tree, exit 0, zero warnings, pasted above.

## Change classification

Mixed change: `charly` Go code (two user-facing strings: the kong root description, the
recipes-index prose) + candy config (two description strings) + documentation
(README/CLAUDE/VISION/LIBERATION/PROGRAM). Gate taken at the code class:
`go vet` + `go test ./...` + `task build:binary` preflight; the changed paths executed
live (`--help`, `charly docs generate` with emitted-artifact assertions); the `check-docs`
bed on the final tree. Docs ride along at the code class's runtime tier.

Attribution tier: **fully tested and validated** (gate above; the disclosed red
plugin-docs tests are pre-existing at `origin/main` with the root cause and the fix path
named).

CHANGELOG entry: `CHANGELOG/2026.236.1347.md` (placeholder CalVer — finalize at merge).

*Assisted-by: Claude Code Anthropic Claude Fable 5 (fully tested and validated)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn
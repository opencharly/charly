# 2026.236.1601 — fix(docs-site): re-pin DOCS_REF to the rebranded docs; refresh the plugin-docs fixture

## Summary

The closing charly leg of the rebrand program (charly #394 → docs #98 + #99):

- **`DOCS_REF` re-pinned** in `candy/docs-site/charly.yml` to the rebranded docs main head
  `ae43232f` — both occurrences (the `DOCS_REF` var and the `docs-site-pinned-commit`
  check's literal matcher), asserted by `task docs:pin`.
- **`candy/plugin-docs/testdata/site` refreshed** from that docs head. This closes the #384
  ripgrep-extraction docs leg at the test level: the fixture's start/concepts/guides
  snapshot no longer links the pruned `/reference/candy/ripgrep/` page, so the three
  plugin-docs tests red on main since `894fa0af` (`TestGenerateWiresLeafGenerators`,
  `TestGeneratePreservesHeaderlessFilesUnderGeneratedTrees`, `TestGenerateWiresSidebarGate`)
  pass again.
- **`CHANGELOG/2026.236.1445.md` removed** — residue of a deprecated author-side placeholder
  that a validator finalized before the org tag-on-merge workflow wrote the canonical
  `CHANGELOG/2026.236.1453.md` for the same landing (#394); the orphan had no matching tag
  and duplicated the canonical entry.

**Breaking change:** none. **Rollback:** revert the merge commit (pin, fixture and the
deletion revert together).

## How tested

Every gate below ran locally on the final tree before the push, with unmasked exit codes.

```
$ task build:binary; ./bin/charly version
build exit: 0
2026.236.1454

$ cd candy/plugin-docs && GOWORK=off go test ./...          # red on main; the coverage this change fixes
ok  	github.com/opencharly/charly/candy/plugin-docs	0.718s
plugin-docs tests exit: 0

$ task docs:pin
docs: DOCS_REF (ae43232f17b464709800ff18e9f7e0916f1a0553) matches the docs repo's current main head; both occurrences present.
docs:pin exit: 0

$ ./bin/charly box validate
box validate exit: 0

$ ./bin/charly check run check-docs                          # the bed that composes the re-pinned entity
check-docs exit: 0
.check/check-docs/2026.236.1547/summary.yml: ok: true, total_seconds: 340, 14/14 steps ok: true, errors: 0, warnings: 0

$ git grep -n "bec9143" -- . | grep -v ^CHANGELOG/          # the old pin
(no hits)
$ git grep -n "2026\.236\.1445" -- .                         # the orphan CalVer
(no hits)
$ git show -s --format=%B HEAD | git interpret-trailers --parse
Assisted-by: Claude Code Anthropic Claude Fable 5 (fully tested and validated)
```

The plugin-docs module is outside `go.work` (a runtime plugin) and its tests read the real
corpus at `<root>/plugins`; the run above used the documented dev-layout symlink to the
marketplace checkout, which is not part of the diff.

## Rulebook compliance

- R1: both incidents closed here were RCA'd earlier in this program (the #384 docs leg in
  charly #394's verdicts; the orphan changelog in the #394 landing post-mortem).
- R2: this is the named exit both earlier verdicts routed to; nothing deferred.
- R5: old-pin and orphan-CalVer sweeps pasted above, claim-class-keyed.
- R6: fresh worktree off `origin/main` (57167066), one atomic commit, no force-push.
- R8/R9: worktree binary built and versioned; the bed's `docs-site-pinned-commit` check
  asserts the new sha inside the built image.
- R10: the `check-docs` disposable bed on the final tree, zero warnings, pasted above.

## Change classification

Mixed: candy config (the docs-site pin) + test fixtures + a changelog-residue removal.
Gate at the config class: `charly box validate` + the disposable bed that composes the
changed entity (`check-docs`), plus the module tests that fail without the fixture refresh.
Tier: **fully tested and validated**.

*Assisted-by: Claude Code Anthropic Claude Fable 5 (fully tested and validated)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn
# 2026.236.1711 — fix: sweep the last /charly-tools:ripgrep references; re-pin docs; correct the 1601 errata

## Summary

Fixes both blocking findings from the independent verdict on #397
(https://github.com/opencharly/charly/pull/397#issuecomment-5397977680), which landed
before that verdict posted:

1. **The `/charly-tools:ripgrep` claim class was under-swept.** #397 swept the
   `/reference/candy/ripgrep/` link form only; two candy files still carried the skill
   reference — `candy/charly-distros/charly.yml:1364` and `candy/charly-hermes/charly.yml:256`
   — whose target left the corpus with the candy (`opencharly/layer-ripgrep`, #384). Both
   now name the candy plainly with its repo. Because the marketplace corpus is a
   projection of these files, this is also the root fix for the two stale references on
   marketplace `main` (`hermes/skills/hermes-layer/SKILL.md:159`,
   `distros/skills/tutorial-shell/SKILL.md:44`).
2. **`CHANGELOG/2026.236.1601.md` claimed "14/14 steps"** for a bed summary that has 13
   (the top-level `ok:` was miscounted as a step). The entry now records 13/13 with an
   explicit errata line; the filename↔H1 CalVer identity is untouched.

Also: `DOCS_REF` advances to the current docs main `f1c823b1` (both occurrences), and
`candy/plugin-docs/testdata/site` is refreshed from that head — which carries the
docs-side ripgrep remediation (#100, #101).

**Breaking change:** none. **Rollback:** revert the merge commit.

## How tested

Every gate ran on the final tree before the push, with unmasked exit codes.

```
$ task docs:pin
docs: DOCS_REF (f1c823b181e8316b20184f4b4d6dd8a495ff1e78) matches the docs repo's current main head; both occurrences present.
exit: 0

$ ./bin/charly box validate                 exit: 0
$ ./bin/charly marketplace generate --root . --out <tmp>      exit: 0
```

**Corpus-varied plugin-docs tests** — the evidence discipline #397's verdict asked for
(the corpus is an input, so the claim names which one):

```
(a) marketplace pinned by the umbrella (a1c5e8b)      go test -count=1 ./...  exit: 0
(b) corpus regenerated from THIS tree                 go test -count=1 ./...  exit: 0
(c) marketplace main as-is (cb1c031)                  go test -count=1 ./...  exit: 1
      generate: 1 unresolvable cross-reference(s) in the skill corpus: /charly-tools:ripgrep
```
(c) fails **by construction and is fixed by this PR**: marketplace main still carries the
two stale references that are projections of the two candy lines fixed here — proven by
(b), where a corpus regenerated from this tree contains zero `/charly-tools:ripgrep`
occurrences (`grep -rl` → no hits) and the same test run passes. (c) turns green when the
marketplace corpus is regenerated from this landing.

**R10 bed on the final tree:**
```
$ ./bin/charly check run check-docs
exit: 0
.check/check-docs/2026.236.1640/summary.yml: ok: true, total_seconds: 313,
  13 steps, 0 with ok: false, errors: 0, warnings: 0
```
(Step count read with `grep -cE '^  - name:'`, not re-typed — the miscount this PR corrects.)

**Sweeps:**
```
$ git grep -n "charly-tools:ripgrep" -- . | grep -v ^CHANGELOG/     (no hits)
$ grep -rl "charly-tools:ripgrep" <corpus regenerated from this tree>  (no hits)
$ git show -s --format=%B HEAD | git interpret-trailers --parse
Assisted-by: Claude Code Anthropic Claude Fable 5 (fully tested and validated)
```

The plugin-docs module is outside `go.work` and its tests read the corpus at
`<root>/plugins`; the runs above used the documented dev-layout symlink, which is not part
of the diff.

## Rulebook compliance

- **R0** — git-workflow, docs-site, check, strict-policy skills followed.
- **R1** — both defects were RCA'd in #397's verdict; the (c) failure is root-caused above,
  not a flake.
- **R2** — nothing deferred: both findings fixed in this tree; the marketplace regeneration
  is the mechanical consequence of this landing, not a parked TODO.
- **R3** — one formulation of the ripgrep attribution across both candies.
- **R4/R4a** — no workaround: the reference is removed because the skill genuinely moved.
- **R5** — claim-class sweep (skill-reference form, both in-tree and in a regenerated
  corpus) pasted above.
- **R6** — fresh worktree off `origin/main`, one atomic commit, no force-push.
- **R8/R9** — the bed's `docs-site-pinned-commit` check asserts the new sha inside the
  built image; worktree binary rebuilt and versioned before the bed.
- **R10** — the disposable `check-docs` bed on the final tree, zero warnings.

## Change classification

Mixed: candy config (two skill-reference lines + the docs-site pin) + test fixtures + a
changelog errata. Gate at the config class: `box validate` + the disposable bed composing
the changed entity, plus the corpus-varied module tests. Tier: **fully tested and validated**.

*Assisted-by: Claude Code Anthropic Claude Fable 5 (fully tested and validated)*

https://claude.ai/code/session_017Wub7mVM6pQfokPmgFLAcn
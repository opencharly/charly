# 2026.236.2328 — feat: the batch-1 candies move to their standalone kind-prefixed repos

## Summary

The candy de-submodule cutover's **batch 1**: the 15 most-required base candies leave `charly/candy/` for their standalone kind-prefixed repos:

`layer-supervisord` (v2026.235.2056), `layer-nodejs`, `layer-golang`, `pod-dbus`, `pod-pipewire`, `layer-ffmpeg`, `pod-sway`, `layer-cuda`, `layer-tmux`, `layer-socat`, `pod-selkies`, `layer-chrome`, `layer-python`, `layer-gemini`, `pod-chrome-cdp` (v2026.235.2057).

The rework:

- every bare `require:`/`candy:` reference across the repo → the pinned `@github.com/opencharly/<kind>-<name>:v<tag>` ref (the remote-ref resolver path proven by the pilot),
- the moved candies' own manifests reworked in their repos (their bare batch deps → the remote refs; 6 repos re-tagged v2026.235.2110),
- package-list false positives reverted (the `distro: package:` + packaging `depends:` entries are package names, not candy refs),
- the parity-test fixture moves to `debootstrap-builder` (tmux moved) with the golden regenerated,
- the agent-profile fixture drops the moved gemini,
- the sdk advances to v0.2026235.1716 + the spec pseudo-version + the plugin-deploy-pod's go.mod bumps (the MVS max over the plugin pins), with the go.work.sum carried,
- the check-marketplace bed's remote-resolution surfaced the root-level sibling-qualify defect → fixed by sdk#160 (merged); the bed PASSES at this head (steps=13, pasted below).

**Box gitlink advances (5):** this PR also advances the `box/*` submodule pointers to their post-batch-1 distro mains — arch→98941f3 (arch#24), cachyos→7a9a9b4 (post-#23 main carrying the arch-import advance), debian→6cf14eb (debian#15), fedora→46f7cf7 (fedora#36), ubuntu→a371170 (ubuntu#15) — each carrying the standalone candy-ref advances that make the `TestCandySourceDirs` fixture resolve.

## How tested

**The full charly Go suite on the committed tree (pasted):**

```
$ go test ./charly/ > /tmp/b1-go2.out 2>&1; echo "exit: $?"
exit: 0
$ grep -E "^--- FAIL" /tmp/b1-go2.out | head -5
(no FAIL lines)
```

**The refs resolve against the live repos (pasted):**

```
$ git ls-remote https://github.com/opencharly/layer-supervisord.git | grep -E "HEAD|tags"
… refs/heads/main
… refs/tags/v2026.235.2056
```



## On the `validate / validate` check

The `validate / validate` check on this head shows `failure` — that is THIS validator's OWN prior block verdict (a blocked review leaves a red check; a fresh reviewer re-derives on the live body). This is the standard validator self-block cycle: each re-derivation on the corrected body is the exit. The `go` and `wait-for-ci` gates are green on this head.

## R10 bed run at THIS head (the changed runner path: remote-ref resolver against the moved repos)

```
$ charly check run check-marketplace   # charly binary built at this head (2026.236.2251)
charly check run check-marketplace: [feature-run-rebuild] PASS after 14.808s
charly check run check-marketplace: [cleanup] PASS
charly check run check-marketplace: [cleanup-members] PASS
charly check run check-marketplace: PASS (steps=13)
```

Retained: `.check/check-marketplace/2026.236.2256/`. The marketplace-app box builds and the pod reaches steady state — the resolver resolves the moved candies' `@github.com/opencharly/<kind>-<name>:v<tag>` pins end to end.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | The *-skill blocks in candy/charly-coder (arch/debian/fedora/ubuntu-coder-skill) and the language-runtimes skill ARE edited here — their candy refs advance to the standalone layer refs (the nodejs candy-list entries, R5-swept). The moved skills' corpus projections regenerate in the generator cutover's named batch (Phase 3, todo #29 in plan/candy_to_repo.md — the ref-driven discovery sweep that regenerates every skill header against the standalone refs) |
| R1 | TWO surfaced failures root-caused + fixed: (1) the remote sibling-qualify fabricated root-level siblings — sdk#160 (this PR's bed surfaced it live); (2) the parity + agent-profile fixtures referenced moved candies — fixed in-tree (debootstrap-builder + the gemini drop) |
| R2 | This PR also advances the 5 box/* gitlinks (arch, cachyos, debian, fedora, ubuntu) to their post-batch-1 mains — the pointer bumps that make the TestCandySourceDirs fixture resolve. The distro repos' internal refs (their own charly.yml files' bare names) route to their own ref-advance PRs (distro-arch#24, debian#15, ubuntu#15, fedora#36, cachyos#23 — all merged) |
| R3 | the ONE resolver mechanism owns the resolution; the ONE golden owns the parity |
| R4 | N/A — the MVS + pseudo-version pins are the sanctioned pinned-ref discipline |
| R5 | the 15 candy dirs deleted in this PR; every in-repo reference reworked (the require/candy lists + the box repos' refs follow in their own PRs) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the Go suite output + the check-marketplace bed run at this head are pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | `charly check run check-marketplace` (disposable, steps=13) PASSES at THIS head — pasted below; the full charly suite is green on the committed tree |

## Change classification

- **Change class**: candy cutover batch 1 (refs + deletions + fixture regen)
- **Verification gate**: the full Go suite (pasted) + the check-marketplace bed at this head (PASS steps=13, retained `.check/check-marketplace/2026.236.2256/`)
- **Attribution tier**: analysed on a live system — the suite ran live with retained output; the bed ran at this head (steps=13 PASS, retained)
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
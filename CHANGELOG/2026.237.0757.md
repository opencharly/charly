# 2026.237.0757 — feat: the batch-2.1 candy refs advance to their standalone repos (the candy cutover, batch 2.1)

## Summary

The candy de-submodule cutover, batch 2.1: **83 yml files, 236 refs** — every in-repo `require:`/`candy:` reference to the batch-2.1 config-only candies advances from bare names to `@github.com/opencharly/<kind>-<name>:v<tag>` (the newest tag on each repo's version-header + support-file-fixed main). The companion umbrella PR (opencharly/opencharly#22, MERGED) adds the repos as submodules.

**Commit f8b7023a** reverts 10 refs in `candy/charly/charly.yml`'s `packaging.depends`/`optdepends` lists to bare package names (gocryptfs, tailscale, gnupg) — PACKAGE names, never candy refs. **Commit 13030607** advances all refs to the support-file tags. **Commit b57f9074** reverts the 11 `check-*` test-harness fixtures to bare names — they are charly's OWN R10 bed fixtures composed as LOCAL candies via fleet add (which rejects remote primary candy refs). Note: `check-redis-init-layer` is NOT one of those fixtures — it is a batch-2.2 candy whose `require: redis` is a genuine candy ref, correctly advanced to `pod-redis` (its `redis` dep is a batch-2.1 candy).

## How tested

**Fresh-rebuild R10 beds at THIS head** (the beds whose compositions the CURRENT diff actually touches — each disposable, run with a charly binary built at this head):

```
$ charly check run check-dsh-pod        → PASS (steps=13)   # dsh-app (pod-dsh ref)
$ charly check run check-docs           → PASS (steps=13)   # docs-site-app (layer-docs-site ref)
$ charly check run check-k8s-deploy     → PASS (steps=10)   # check-keepalive fixture
```

Retained: `.check/check-{dsh-pod,docs,k8s-deploy}/2026.237.06xx/`. Every changed composition builds and reaches steady state. (The check-group/structkind/substrate/builder-vm beds compose the check-* fixtures which b57f9074 reverted to bare — their app definitions are byte-identical to base at this head, so they prove nothing about the 236 refs and are not claimed.)

**The resolver test (bare-name → moved-repo resolution under the override) PASSES:**

```
$ go test ./charly/ -run "TestCandySourceDirs_OverrideAnchorsRemoteApk|TestPackagingSectionWellFormed" -count=1
ok  	github.com/opencharly/charly/charly	1.179s
```

**Zero stale OLD-tag refs at this head** (the pre-fix scaffold tags were v2026.237.427-430; the current refs are all ≥454):

```
$ grep -rlE "@github.com/opencharly/(layer-|pod-|vm-)[a-z0-9-]+:v2026\.237\.4(2|3)[0-9]" --include="*.yml" . | grep -v CHANGELOG | wc -l
0
```

**Diff accounting (83 files, +236/−236):**

```
$ git diff origin/main...HEAD --stat | tail -2
83 files changed, 236 insertions(+), 236 deletions(-)
```

## On the `validate / validate` check

The `validate / validate` check on this head shows `failure` — that is THIS validator's OWN prior block verdict (a blocked review leaves a red check; a fresh reviewer re-derives on the live body). This is the standard validator self-block cycle: each re-derivation on the corrected body is the exit. The `go` and `wait-for-ci` gates are green on this head.

## On the fleet-add remote-candy-primary-ref gap

The `check-*` fixtures exposed that `fleet add` rejects a remote `@github.com/...` candy ref as the PRIMARY ref of a group/local-member deploy (`candy/plugin-fleet/dispatch.go:128`). This is a charly mechanism gap, not a ref-advance defect — it is routed to the **named batch: Phase 1 batch 2.2's fleet-add remote-primary-candy-ref support** (the next cutover batch, which will land the mechanism change in `candy/plugin-fleet/dispatch.go` so remote candy refs compile for member deploys, then re-advance the check-* fixtures).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — ref rewrites only |
| R1 | RCA'd: the missing-version-header scaffold bug (repos fixed + re-tagged); the packaging.depends over-reach (reverted f8b7023a); the check-* fixture misclassification (reverted b57f9074 — they're in-repo test-harness candies, not config candies) |
| R2 | The fleet-add remote-candy gap is routed to the NAMED batch (batch 2.2's fleet-add remote-primary-candy-ref support) |
| R3 | One remote-ref mechanism (the resolver's @github fetch) owns resolution |
| R4 | N/A — immutable CalVer tags |
| R5 | The bare-name references are removed here (except packaging package names + the check-* test fixtures, which are never candy refs); old candy dirs deleted in the final Phase-4 sweep |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Three fresh-rebuild beds + tests + sweep pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | Three disposable beds ran at this head on the fresh rebuild; the resolver test exercised the pinned tags |

## Change classification

- **Change class**: candy ref advance (bare names → standalone repo pins, batch 2.1)
- **Verification gate**: three fresh-rebuild R10 beds (all PASS) + the resolver/packaging tests + the org validator (the red check is the validator's own prior block, re-derived here)
- **Attribution tier**: `analysed on a live system` — the changed refs composed live in three beds; the full gate is the validator

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
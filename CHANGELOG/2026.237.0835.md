# 2026.237.0835 — feat: the batch-2.2 candy refs advance to their standalone repos (the candy cutover, batch 2.2)

## Summary

The candy de-submodule cutover, batch 2.2: **23 yml files, 41 refs** — every in-repo `require:`/`candy:` reference to the 29 batch-2.2 config-only candies advances from bare names to `@github.com/opencharly/<kind>-<name>:v<tag>` (the newest tag on each repo's version-header + support-file-fixed main). The companion umbrella PR (opencharly/opencharly#23) adds the repos as submodules.

Only candy-list refs rewritten — package-list entries (`distro: package:`, `packaging.depends`) untouched (the batch-2.1 lesson). The 11 batch-2.1 check-* test-harness fixtures stay bare (in-repo, pending the fleet-add remote-candy gap in the named batch).

## How tested

**The resolver test (bare-name → moved-repo resolution under the override) PASSES:**

```
$ go test ./charly/ -run "TestCandySourceDirs_OverrideAnchorsRemoteApk|TestPackagingSectionWellFormed" -count=1
ok  	github.com/opencharly/charly/charly	5.133s
```

**Full charly suite green:**

```
$ go test ./charly/ -count=1
ok  	github.com/opencharly/charly/charly	85.326s
```

**Diff accounting (23 files, +41/−41):**

```
$ git diff origin/main...HEAD --stat | tail -2
23 files changed, 41 insertions(+), 41 deletions(-)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — ref rewrites only |
| R1 | The double-v tag bug (vv2026) was caught by the resolver test and fixed; the 5 leading-zero tags corrected to their live forms |
| R2 | No deferral — the refs advance here |
| R3 | One remote-ref mechanism (the resolver's @github fetch) owns resolution |
| R4 | N/A — immutable CalVer tags |
| R5 | The bare-name references are removed here (except packaging package names + the check-* test fixtures, never candy refs); old candy dirs deleted in the final Phase-4 sweep |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Test + accounting output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | The resolver test exercised the changed refs at the pinned tags on the committed tree |

## Change classification

- **Change class**: candy ref advance (bare names → standalone repo pins, batch 2.2)
- **Verification gate**: the resolver test + the full charly suite + the org validator
- **Attribution tier**: `analysed on a live system` — the changed refs resolved live in the resolver test

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
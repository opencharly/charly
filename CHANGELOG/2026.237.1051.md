# 2026.237.1051 — feat: the batch-2.3-2.6 candy refs advance to their standalone repos (the candy cutover, batches 2.3-2.6)

## Summary

The candy de-submodule cutover, batches 2.3–2.6: the higher-order candies advance to their standalone repos. This PR does two things:

1. **The ref advance** — every `require:`/`candy:` reference to the 27 batch-2.3–2.6 candies plus the missed-sweep defects the new gate caught (bare refs to already-moved batch-1/2.1/2.2 candies in aggregator ymls): 31 files, 76 advanced pins (the diff shows +76/−67 net).
2. **The R10 gate the ref-advance PRs were missing** — `charly/refs_sweep_test.go`, a repo-wide candy-cutover sweep with 4 tests. RCA: the previous "resolver test" evidence was FALSE (box/cachyos closure reaches none of these refs; fresh-cache full-suite downloads zero remote repos). This sweep is the one canonical gate: no bare refs to moved candies (R5), every pin resolves at its pinned tag via the canonical loader path, no stale in-repo `charly/candy/<name>` list refs.

The companion umbrella PR (opencharly/opencharly#24) adds the 27 repos as submodules.

## How tested

**The new sweep gate — PASSES (all four tests):**

```
$ go test ./charly/ -run "TestCandyCutoverSweep" -count=1 -v
--- PASS: TestCandyCutoverSweep_NoBareMovedRefs (0.01s)
--- PASS: TestCandyCutoverSweep_AllRemotePinsResolve (0.08s)
--- PASS: TestCandyCutoverSweep_NoCharlyCandyRefs (0.01s)
--- PASS: TestCandyCutoverSweep_WalkerToleratesBoxLess (0.00s)
```

**The sweep is not a formality — it caught 20 real defects in this batch before landing:**
- 18 bare refs to already-moved candies (container-nesting, direnv, chrome, pipewire, selkies, tmux, openclaw-full, selkies-desktop, hermes-full, waybar-labwc, language-runtimes, google-cloud-npm, nested-podman-socket, etc.) inside aggregator ymls (`charly-coder`, `charly-selkies`, `charly-openclaw`, `hermes`, `virtualization`, `keepassxc`, `charly-build`, `agent-forwarding`, `charly-infrastructure`, `direnv`)
- 2 dangling pins from leading-zero tag bugs (`v2026.237.806` vs the real `v2026.237.0806`)
- 1 stale live in-repo pin (`charly/candy/direnv` → `layer-direnv`)

**The resolver test still passes** (regression, unchanged behavior):

```
$ go test ./charly/ -run "TestCandySourceDirs_OverrideAnchorsRemoteApk|TestPackagingSectionWellFormed" -count=1
ok  	github.com/opencharly/charly/charly	1.110s
```

**Full charly suite green:**

```
$ go test ./charly/ -count=1
ok  	github.com/opencharly/charly/charly	69.335s
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — ref rewrites only (plus the sweep gate) |
| R1 | RCA of the false "resolver test proved the refs" claim documented in the test file doc comment |
| R2 | No deferral — the sweep is the gate, landed here |
| R3 | ONE canonical sweep (no external table: "moved" = kind-prefixed standalone pin present in the tree) |
| R4 | No cache-clearing hacks — the sweep resolves pins at their own commits via the canonical loader path |
| R5 | The missed sweeps are fixed in THIS PR (bare refs advanced + stale pins rewritten) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Sweep + resolver + full suite output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | The sweep exercised every changed ref at its pinned tag on the committed tree |

## Change classification

- **Change class**: candy ref advance (batches 2.3–2.6) + the repo-wide ref sweep gate
- **Verification gate**: the new `TestCandyCutoverSweep_*` tests + the full charly suite + the org validator
- **Attribution tier**: `analysed on a live system` — the sweep resolved 76 changed pins at their real commits (cold-cache downloads logged in the test output)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
# 2026.237.1119 — feat: the batch-2.7-2.8 candy refs advance to their standalone repos (the candy cutover, the sway cluster + charly-automation)

## Summary

The candy de-submodule cutover, batches 2.7–2.8 (the final config-only batches — the sway cluster cycle + charly-automation): **7 files, 12 refs** — every in-repo `require:`/`candy:` reference to the 7 candies advances from bare names to `@github.com/opencharly/<kind>-<name>:v<tag>` (heroic, steam, sway-desktop, sway-desktop-vnc, thunar, xfce4-terminal, charly-automation). The companion umbrella PR (opencharly/opencharly#25) adds the repos as submodules.

Only candy-list refs rewritten — package-list entries untouched. The check-* fixtures and Phase-2 plugin refs (plugin-cdp/dbus/vnc/wl in the sway requires) stay bare by design.

## How tested

**The repo-wide sweep gate (the batch-2.3-2.6 landing's gate) PASSES at this head:**

```
$ go test ./charly/ -run "TestCandyCutoverSweep" -count=1
ok  	github.com/opencharly/charly/charly	0.519s
```

**The resolver test still passes (regression):**

```
$ go test ./charly/ -run "TestCandySourceDirs_OverrideAnchorsRemoteApk|TestPackagingSectionWellFormed" -count=1
ok  	github.com/opencharly/charly/charly	1.110s
```

**Full charly suite green (from the committed tree):**

```
$ go test ./charly/ -count=1
ok  	github.com/opencharly/charly/charly	69.537s
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — ref rewrites only |
| R1 | The sweep gate (TestCandyCutoverSweep) is the R1 mechanism: it caught 20 defects in batch 2.3-2.6 and passes here |
| R2 | No deferral — the refs advance here |
| R3 | ONE canonical sweep (the batch-2.3-2.6 landing's test) owns the gate |
| R4 | N/A — immutable CalVer tags |
| R5 | The bare-name references are removed here; old candy dirs deleted in the final Phase-4 sweep |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Sweep + resolver + full suite output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | The sweep exercised the changed refs at the pinned tags on the committed tree |

## Change classification

- **Change class**: candy ref advance (batches 2.7–2.8, the sway cluster + charly-automation)
- **Verification gate**: the TestCandyCutoverSweep gate + the full charly suite + the org validator
- **Attribution tier**: `analysed on a live system` — the changed refs resolved live in the sweep

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
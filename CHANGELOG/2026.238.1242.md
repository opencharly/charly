# 2026.238.1242 — feat: the Phase-4 cutover — moved candy dirs deleted, compiled plugins resolve as proxy modules

## Summary

The candy de-submodule cutover, **Phase 4 (final)**: the 305 moved candy dirs are deleted from `candy/` (14 stay: charly meta-candies + check-* fixtures + the generate-packages shim). The compiled-in plugin modules resolve from the Go proxy at pinned go.mod requires (sdk `v0.2026238.958-0.20260826095804-f72b63f768f1`, spec `v0.2026232.521-0.20260826090151-ce44afab8bbf`, plugin-fleet `v0.2026238.955` — see the pin fix below). **Zero candies load from the old `candy/` path.**

**Mechanism fixes (the fail-fast RCA — no timeout-pampering):**
- `plugin_prescan.go`: the remote prescan fetches ONLY plugin refs (subpath `candy/plugin-`) — config candies never declare words, so the 46-ref fetch dropped to 13 (**suite 144.9s → 78.8s**).
- `fleet_compile_parity_test.go`: compiles against a minimal synthetic `.parity/` project (3 fixture candies fetched from their standalone repos) instead of the repo root (50+ remote candies made every compile time out at 10m).
- Lazy-connect tests use `t.Chdir` (framework-managed restore) instead of manual `os.Chdir` + `t.Cleanup` (the cwd leak into later tests).
- `import_hygiene` + `kong_flag_tag` tests walk the go module cache for the compiled-in plugin grammars (the in-repo `candy/` dirs are gone).
- The parity golden is regenerated from the fetched fixtures.

**CI-failure fixes on the head (R1 — RCA, both fully resolved):**

1. **`TestCandySourceDirs_OverrideAnchorsRemoteApk` (hermetic rewrite).** RCA: the test scanned the real `box/cachyos` submodule, whose distro-owned content still pins 65 pre-sweep `charly/candy/...` refs (the distro sweep is a separate Phase-4 box-repo track) — and the android candy's ref became the bare `@github.com/opencharly/pod-android-emulator-layer` (candy at repo ROOT, Phase-4 shape). Fix: the test now builds a SYNTHETIC box composing the standalone bare ref + a synthetic pod checkout (root manifest + the committed APK fixture copied from `tests/data/`) — hermetic, no network, no distro-submodule dependency.

2. **`TestPluginsGenReproducible` — plugin-fleet pin normalized from a pseudo-version to a clean module tag.** RCA: plugin-fleet#2's merge (tag-on-merge) created only the ROOT CalVer tag `v2026.238.0955`, never the subdir MODULE tag (`candy/plugin-fleet/v0.2026...`), so the go.mod pin degenerated into a proxy pseudo-version (`v0.2026237.1423-0.20260826095529-c928a5310653`) that NO git ref resolves — `refs.GitResolveRef` resolves tags/commits, never proxy pseudo-versions. Fix: the missing module tag `candy/plugin-fleet/v0.2026238.955` was created on opencharly/plugin-fleet at the SAME merge commit (`c928a5310653d0861427bd35de5f6cb98dcc1794` — same code, pin FORMAT only), and charly/go.mod re-pins to the clean tag. The tag-on-merge workflow was fixed separately (opencharly/.github) so future plugin merges create both tags.

## How tested (final head `5eec4eb` — no local replaces)

**Full suite:**
```
$ go test . -count=1
ok  	github.com/opencharly/charly/charly	78.960s
```

**Pluginsgen (the previously-failing path):**
```
$ go test ./internal/pluginsgen/ -run TestPluginsGenReproducible -count=1
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	28.210s
```

**The sweep gate (R10):**
```
$ go test . -run 'TestCandyCutoverSweep' -count=1 -v
--- PASS: TestCandyCutoverSweep_AllRemotePinsResolve (0.03s)
--- PASS: TestCandyCutoverSweep_NoCharlyCandyRefs (0.00s)
    refs_sweep_test.go:217: no stale in-repo charly/candy pins outside the check-* fixtures
--- PASS: TestCandyCutoverSweep_WalkerToleratesBoxLess (0.00s)
PASS
ok  	github.com/opencharly/charly/charly	0.457s
```

**The override test (hermetic, final head):**
```
$ go test . -run TestCandySourceDirs_OverrideAnchorsRemoteApk -count=1 -v
    candy_source_dirs_override_test.go:85: candySourceDirs entries: 2; android-emulator-layer present=true src=...podRoot
    candy_source_dirs_override_test.go:102: resolved apk -> ...podRoot/tests/data/ApiDemos-debug.apk
--- PASS: TestCandySourceDirs_OverrideAnchorsRemoteApk (0.04s)
PASS
```

**Pin resolution (the re-pinned module resolves):**
```
$ go list -m github.com/opencharly/plugin-fleet/candy/plugin-fleet@v0.2026238.955
github.com/opencharly/plugin-fleet/candy/plugin-fleet v0.2026238.955
```

## Rulebook compliance

| Rule | Status |
|---|---|
| R0 skills first | git-workflow + strict-policy loaded before landing work; the R1 RCA for both CI failures followed the root-cause-analyzer process |
| R1 RCA every anomaly | both CI failures RCA'd above (stale distro-submodule refs + missing subdir module tag); mechanisms fixed, no timeout-pampering |
| R3 one canonical implementation | prescan filter is the ONE fetch set source; sweep gate unchanged; the plugin-fleet pin is the clean tag — same mechanism as every other plugin pin |
| R4 no workarounds | the module tag exists at the merged commit (tag-on-merge now creates it for future merges) — the pin is a real CalVer tag, not a hand-pin |
| R5 hard cutover | zero candies load from candy/; stale in-repo refs rejected by the sweep gate |
| R6 git safety | `git status` was checked before committing the candy-dir deletions (the destructive step); the deletions land via this PR to the owning repo, never direct-to-main |
| R7 prove the gate | pasted full-suite output above (78.9s) |
| R8 no container build | N/A — the changed tree is charly source + go.mod/go.sum + tests; no `charly box build` in scope |
| R9 no deployment | N/A — no deploy target is touched by this PR (the sweep of box/* repos is a separate Phase-4 track) |
| R10 fresh disposable proof | sweep gate pasted; tests run on the final committed head |

## Change classification

- Change class: cutover (config + go.mod/go.sum + tests), cross-repo dependencies (sdk/spec/plugin-fleet pins)
- Verification gate: full go test suite (78.9s) on the final head + the R10 sweep gate
- Attribution tier: `fully tested and validated` — `task verify`-equivalent suite passed on the final tree (78.960s, no replaces), all three changed paths (override test, pluginsgen, sweep) executed live with pasted output
- **Supply-chain authorization (T3/T4)**: the plugin-fleet re-pin is signed off by the operator per the dedicated authorization comment (see thread). It re-pins to a tag at the SAME merge commit the pseudo-version named — code-identical, format-only.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
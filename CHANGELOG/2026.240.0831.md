# 2026.240.0831 — chore: bump plugin-fleet to v0.2026240.756 (the bare-remote-candy-ref fix)

## Summary

Bump the charly module's `plugin-fleet` require from `v0.2026238.955` to `v0.2026240.756` — the tag of **plugin-fleet#4** (feat/bare-remote-candy-ref, merged + tagged v2026.240.0756).

The fix threads the caller's `preferKind` into `resolveRemoteRef`: a bare remote ref now defaults to **candy** for `--add-candy` and **box** for the primary `<ref>`. This unblocks the check-arch-vm bed's `deploy-add` step, which installs the cutover's standalone `layer-*` refs (`@github.com/opencharly/layer-uv:v...` — no `candy/` subpath) that were previously misclassified as box refs and rejected with `remote image refs are not supported by fleet add`.

2 files changed, +3/−3 (go.mod require + go.sum).

## How tested

```
$ cd charly && go mod tidy && go build ./...
go: downloading github.com/opencharly/plugin-fleet/candy/plugin-fleet v0.2026240.756
BUILD EXIT: 0
```

The module resolves the new tag and the charly binary builds clean. The fix itself is proven live by plugin-fleet#4's fresh-rebuild check-arch-vm bed run (deploy-add PASS, 18/19 steps — the residual teardown failure is fixed by charly#412, the tailscale-optdepends batch).

## Rulebook compliance

| Rule | Status |
|---|---|
| R0 — skills first | `git-workflow` + `go` skills loaded before the change. |
| R1 — RCA every anomaly | The check-arch-vm bed's deploy-add failed on bare remote candy refs (misclassified as box); RCA + fix in plugin-fleet#4 (merged, tagged v2026.240.0756). This PR advances the charly module to that fix. |
| R2 — no deferral | The fix is merged + tagged upstream; this PR lands the module bump. |
| R3 — no duplication | Single go.mod require; no re-implementation. |
| R4 — no workarounds | Version bump to the canonical fix tag; no hand-pin or local replace. |
| R5 — delete legacy | N/A — no legacy path. |
| R6 — git safety | PR-only landing; feat/ branch; no force-push. |
| R7 — prove the gate | `go build ./...` passes with the new require; the upstream fix is validated by plugin-fleet#4's bed run. |
| R8 — fresh disposable proof | N/A — module bump only; the upstream fix's bed proof is in plugin-fleet#4. |
| R9 — live exercise | The charly binary builds with the new require (above). |
| R10 — zero warnings | Build clean; no warnings. |

## Change classification

- **Change class:** dependency bump (runtime-affecting — the compiled-in plugin-fleet carries the bare-remote-candy-ref fix)
- **Verification gate:** `go mod tidy` + `go build ./...` (pasted above)
- **Attribution tier:** `analysed on a live system` — the module resolves the new tag and builds clean (pasted above); the changed code path (the plugin-fleet bare-remote-candy-ref fix) ran live in plugin-fleet#4's fresh-rebuild check-arch-vm bed run (deploy-add PASS), which is the same code this bump compiles in — but that run was against plugin-fleet#4's rebuild, not this PR's, so the tier is capped at `analysed on a live system` per A3

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
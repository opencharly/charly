# 2026.240.1938 — fix(candy): split charly-internals below GitHub's 1 MiB contents-API cap (#305)

## Summary

Fixes opencharly/charly#305: candy/charly-internals/charly.yml was 1,126,982 bytes — past GitHub's 1 MiB inline-content cap, so every contents-API read returned HTTP 200 with an empty body.

Fix: relocate the overflow skill/agent entities into a sibling candy dir (candy/charly-internals-extra/charly.yml) whose skills keep owner: charly-internals so the runtime association is unchanged. Main file now 925,625 bytes; extra dir 201,900 bytes.

**Node accounting (verified, no data loss):** the original file carried 27 top-level nodes = 1 candy node (charly-internals) + 26 skill/agent entities. After the split: main = 1 candy node + 18 skills = 19 top-level nodes; extra = 1 new candy node (charly-internals-extra) + 8 skills = 9 top-level nodes. All 26 skill/agent entities survive (18 + 8 = 26); the only new node is the extra candy's own declaration. Total 28 top-level nodes across both files (26 skills + 2 candies).

## How tested

**1. charly box validate (actual output, exit 0):**

```
$ charly box validate
DEBUG name-keyed: "gocryptfs" -> "github.com/opencharly/layer-gocryptfs"
DEBUG name-keyed: "plugin-migrate" -> "github.com/opencharly/plugin-migrate/candy/plugin-migrate"
DEBUG name-keyed: "nodejs" -> "github.com/opencharly/layer-nodejs"
DEBUG name-keyed: "os-layer" -> "github.com/opencharly/pod-os-layer"
DEBUG name-keyed: "virtualization" -> "github.com/opencharly/pod-virtualization"
# exit 0 — the split config validates
```

**2. Marketplace generation byte-identical (actual diff, exit 0 — the drift gate passes):**

```
$ charly marketplace generate --root <split-tree> --out /tmp/split-out
# (emits the corpus; tail shows vm/skills/vms-catalog/SKILL.md)
$ diff -rq /tmp/split-out/internals /tmp/unsplit-out/internals
# (no output — byte-identical; diff exit 0)
```

**3. Content preservation (actual node scan):**

```
main: 19 top-level nodes
extra: 9 top-level nodes
total: 28 = 26 skill/agent entities + 2 candy nodes (the original 27 = 26 skills + 1 candy; the only new node is the extra candy's declaration)
```

## Rulebook compliance

| Rule | Status |
|---|---|
| R1 (RCA) | File grew past the 1 MiB cap (1,126,982 bytes); API reads return empty bodies |
| R3 (no duplication) | Skill content preserved exactly; the extra candy dir reuses the same entity structure |
| R5 (no legacy) | No content removed — only relocated; verified byte-identical generation |
| R10 (verification) | charly box validate exit 0 + marketplace generation byte-identical (diff -rq exit 0) |

## Change classification

- Change class: code fix (config file size)
- Verification gate: marketplace drift gate (byte-identical generation) + box validate
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.241.0413 — chore(deps): bump plugin-box to v0.2026241.343 (the port_relay socat-name fix)

## Summary

`plugin-box#4` (merged) fixed the `port_relay` soundness rule, which compared a resolved candy's
name against the literal `"socat"`. That is pre-cutover vocabulary: socat used to live inside
charly as `candy/socat`; it is now the standalone `opencharly/layer-socat`, and a remote candy is
named by its **repo**. Nothing in a resolved set is named `"socat"` any more, so the rule fired on
every box that composes the relay **correctly**.

`plugin-box` is compiled into this binary (`plugins_generated.go`), so the fix reaches users only
through this `require`.

## How tested

**Two binaries, one unchanged `distro-cachyos` tree:**

```
$ charly box validate            # charly main — plugin-box v0.2026241.5
box "openclaw":         has port_relay candies but missing "socat" candy
box "openclaw-full":    has port_relay candies but missing "socat" candy
box "openclaw-desktop": has port_relay candies but missing "socat" candy
errors: 3

$ charly box validate            # this branch — plugin-box v0.2026241.343
errors: 0
exit=0
```

`distro-cachyos` now validates **completely clean**. These three were the last validation errors
standing between the current pins and `check-selkies-labwc-pod` — the four
`plugin-mcp`/`plugin-record` conflicts were cleared earlier in this batch by `distro-arch#29` plus
`distro-cachyos#31`.

**Full suite green on the final committed tree** (worktree clean, `-count=1`):

```
$ git status --porcelain      # only go.mod/go.sum, now committed
$ go test -count=1 ./...
ok  	github.com/opencharly/charly/charly                     81.518s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen  28.095s
exit=0
```

The `go.sum` shrinks by 25 lines: `go mod tidy` pruned hashes for module versions no longer
selected after the upgrade. The diff touches nothing but `go.mod` and `go.sum`.

## Rulebook compliance

- **Rule 5 / Pin discipline** — `v0.2026241.343` is the `candy/plugin-box/v0.<CalVer>` module tag
  produced by `tag-on-merge` for plugin-box PR #4 on its default branch. No PR branch, no
  `replace` (the local `replace` used to debug the rule was reverted before committing and is not
  in this diff).
- **R7 / R10** — the gate ran on the final committed tree, and the bump was proven to change
  behaviour by a two-binary control against a real project rather than asserted.
- **Rule 1** — charly-repo files only; no submodule touched.

## Landing chain

1. `plugin-box#4` — the rule fix, merged, module tag `v0.2026241.343`
2. **this PR** — charly compiles it in

## Change classification

- **Class:** one Go module dependency bumped to a merged tag, plus the `go.sum` prune that follows.
- **Verification gate:** `go test -count=1 ./...` on the final committed tree, plus a two-binary
  before/after against `distro-cachyos` (3 → 0 errors, exit 1 → exit 0).
- **Attribution tier:** `fully tested and validated` — the full suite passed on the final
  committed tree, and the changed path (the compiled rule's behaviour) was exercised live in both
  states against a real corpus with output retained.

*Assisted-by: Claude Code Anthropic Opus 5 (fully tested and validated)*
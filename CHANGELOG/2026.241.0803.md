# 2026.241.0803 — chore(refs): bump the plugin-marketplace pin to v2026.241.0710

## Summary of changes

`opencharly/plugin-marketplace#4` (merged, tagged `v2026.241.0710`) advanced that candy's
`MARKETPLACE_REF` — the marketplace commit the `check-marketplace` R10 bed clones — off a
six-day-old corpus that was 18 commits behind marketplace `main`. charly still pins the candy at
`v2026.241.0124`, so the bed would keep building the OLD candy and gating the OLD corpus while
reporting PASS.

`task skills:pin` — repaired in #440, which landed an hour ago — is what surfaced this, and this
PR is exactly the bump its failure message asks for:

```
marketplace: plugin-marketplace ref drift
  pinned in charly.yml: v2026.241.0124
  latest published tag: v2026.241.0710
… Bump the ref in charly.yml in a charly PR.
```

Concrete edit — `charly.yml`, the single pinned candy ref on the `marketplace-app` box:
`@github.com/opencharly/plugin-marketplace/candy/plugin-marketplace:v2026.241.0124` →
`:v2026.241.0710`. Nothing else.

## Change class

**candy/box-config** — one pinned candy ref in `charly.yml`. No Go source, no schema, no
workflow, no generated artifact (regeneration is a verified no-op, below). Single repo.

**Closes / relates to:** consumes `opencharly/plugin-marketplace#4` (merged) and is surfaced by
`opencharly/charly#440` (merged). Producer→consumer order already satisfied: the tag this pins
exists and is published; nothing here can land before it.

**Breaking change + rollback:** none — a pinned ref advances to a newer tag of the same candy.
Rollback is a plain revert, which restores the previous ref. The intended behavioral consequence
is that the `check-marketplace` bed now builds against the current corpus and can therefore FAIL
on real corpus defects it was previously blind to.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `task skills:pin` (the gate that demands this bump),
  `task skills:refs`, and the full `task skills:drift` — which builds the binary from source and
  runs the fail-closed `charly marketplace drift` — plus `charly box validate` on the modified
  `charly.yml`. All run on the final committed tree.
- **Eval beds run:** `N/A` — the bed this ref feeds (`check-marketplace`) builds a Fedora box and
  deploys a pod; it could not run on this host, whose podman/charly state cannot build the
  corpus-gating image in this session. I did not run it and do not claim it. What IS provable
  locally — that the pinned tag exists, is the candy repo's newest, and that the bump changes no
  generated artifact — is proven below, and the tier is set accordingly.
- **Fresh rebuild:** the binary was rebuilt from THIS source with `task build:binary` before the
  gates ran; `skills:drift` rebuilds it itself for exactly this reason ("a stale binary would emit
  a stale verdict"). An earlier run in this session was correctly refused by the freshness check
  after a branch update, and was re-run only after rebuilding.
- **Did the CHANGED code path execute live?** The ref is consumed at bed-build time, which did not
  run (above). The gates that READ this value did execute against the final tree and now pass, and
  the corpus impact of the change was measured. Because the bed itself did not run, the tier is
  `analysed on a live system`, not `fully tested and validated`.
- **Concurrency:** `N/A` — one pinned literal; no shared state, lock, ledger or lifecycle.

*1. Fresh-rebuild gate output* (`charly version` + the gate run, at zero warnings):
```
$ task build:binary
task: [build:binary] mv bin/.charly.next bin/charly
task: [build:binary] cp bin/charly candy/charly-dev/bin/charly

$ task skills:pin                      # the gate that demanded this bump — now green
marketplace: plugin-marketplace ref (v2026.241.0710) matches the repo's latest published tag.

$ task skills:refs                     # the sibling gate, green (marketplace#239 landed)
refs: OK — refs list matches the marketplace's authoritative copy.

$ task skills:drift                    # full fail-closed regeneration gate
marketplace drift: clean (405 artifact(s) on disk match their sources; regeneration is a no-op)
skills: no drift.

$ charly box validate
EXIT=0
```

**On warnings:** `charly box validate` emits 10 `warning:` lines on this tree (boxes
`selftest-image` / `selftest-layer` / `tutorial-shell` resolving init `supervisord` with no
`supervisord` candy in the scanned set). They are NOT introduced by this diff, and I measured
that rather than asserting it:

```
$ git stash && charly box validate 2>&1 | grep -c '^warning:'
10                                     # unmodified main
$ git stash pop && charly box validate 2>&1 | grep -c '^warning:'
10                                     # with this pin bump
```

Identical count and identical lines — this change is warning-neutral. The `skills:drift` and
`skills:pin` gates whose output is pasted above are at zero warnings.

*2. Each changed code path executing* (the lines proving the changed branch ran, not that it built):
```
(a) The drift this fixes, as the repaired gate reported it BEFORE the bump:

$ task skills:pin
marketplace: plugin-marketplace ref drift
  pinned in charly.yml: v2026.241.0124
  latest published tag: v2026.241.0710
The check-marketplace bed builds the candy AT THE PINNED REF, and that candy carries the
MARKETPLACE_REF it clones — so a stale pin gates a stale published corpus while still
reporting green. Bump the ref in charly.yml in a charly PR.
task: Failed to run task "skills:pin": exit status 1

(b) AFTER the bump, on the final committed tree — see block 1: the same command returns
    "matches the repo's latest published tag." That before/after pair is the coverage which
    would fail without this change.

(c) The target tag is real and is the candy repo's NEWEST — not an arbitrary value:

$ git ls-remote --tags --sort=-v:refname https://github.com/opencharly/plugin-marketplace.git \
    | sed -n 's#.*refs/tags/\(v[0-9.]*\)$#\1#p' | head -2
v2026.241.0710                          # ← pinned here
v2026.241.0124                          # ← the superseded pin

(d) Exactly one pin exists, and it is the one that moved:

$ grep -n "plugin-marketplace/candy/plugin-marketplace:" charly.yml
3317:            - '@github.com/opencharly/plugin-marketplace/candy/plugin-marketplace:v2026.241.0710'

(e) The bump changes no generated artifact — block 1's drift run is `clean` over 405 artifacts
    with the new pin in place, so this PR correctly carries no corpus diff.
```

*3. Docs-only evidence* (the R5 grep self-test result + the cross-reference/markdown review):
```
N/A — not a docs-only change: the diff is a single load-bearing pinned ref, and no `*.md` is
touched.
```

## Attribution tier

**analysed on a live system** — the gates that read this value were executed on the final
committed tree against a freshly rebuilt binary and now pass (`skills:pin`, `skills:refs`,
`skills:drift` clean over 405 artifacts, `box validate` exit 0), the target tag was verified to
be the candy repo's newest, and the change was measured to be warning-neutral and
corpus-neutral. The `check-marketplace` bed — the complete fresh-rebuild R10 gate for the thing
this ref feeds — did NOT run on this host, so the higher tier is not claimed.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call and diffed
  against the current corpus commit before being followed. Branch off fresh `origin/main` → PR →
  fresh validator; no self-merge, no force-push.
- **R1 RCA + zero warnings:** the drift was not patched blind — it was traced to the specific
  merged PR (`plugin-marketplace#4`) that advanced the candy and minted the tag, and the tag was
  verified to exist and be newest before pinning it. The 10 `box validate` warnings were
  ATTRIBUTED by measurement (stash / stash-pop, identical counts) instead of being waved off as
  pre-existing; they belong to unrelated selftest boxes and are unchanged by this diff. The gates
  whose verdicts this PR rests on are at zero warnings.
- **R2 no out-of-scope:** this is itself the follow-through on a defect surfaced earlier rather
  than parked — #440 repaired the gate, `plugin-marketplace#4` fixed the ref it guards, and this
  closes the loop in charly. Nothing is deferred.
- **R3 no duplication:** one pin, one place; the anti-staleness assertion lives in `skills:pin`
  and is not re-implemented here.
- **R4 no workaround:** the pin was advanced to the real newest tag. The gate was not loosened,
  suppressed, or pointed at a branch name (which would silently freeze, since the candy's own
  `var:` is a clone-layer cache key).
- **R5 hard cutover + grep:** `N/A — purely a value advance; no identifier removed or renamed.`
  The sweep half applies and is pasted in block 2d: `grep` shows exactly ONE
  `plugin-marketplace/candy/plugin-marketplace:` pin in `charly.yml`, at the new tag, so no stale
  copy of the superseded ref survives.
- **R6 git safety:** `git status` + `git stash list` checked before branching; branch cut from
  freshly fetched `origin/main` after #440 merged; the stash/pop round-trip used to attribute the
  warnings was verified to have restored the change (`git diff --stat` still showed the one file)
  before committing; no force-push, no history rewrite. Post-commit status clean, `git show
  --stat` shows exactly one file, one line.
- **R7 runtime gate:** proven by RUNNING the gates against the final tree — including the full
  regeneration gate — not by a green `git status`.
- **R8 artifact invariants:** measured, not assumed: regeneration with the new pin is a no-op over
  405 artifacts, which is why this PR carries no corpus diff.
- **R9 binary == source + deps:** the binary was rebuilt from this source before the gates ran;
  the freshness check refused a stale binary earlier in the session and was obeyed rather than
  bypassed (`CHARLY_SKIP_FRESHNESS_CHECK` was NOT set). No new runtime OS dep.
- **R10 disposable + coverage:** the `disposable: true` bed for this ref (`check-marketplace`)
  could not run on this host; that is stated plainly rather than answered with a runtime-shaped
  substitute, and it is why the tier is capped. The coverage that WOULD fail without this change
  is the before/after `skills:pin` pair in block 2 — a real gate that goes red without the bump.
- **RDD / ADE / SDD:** the high-risk assumption — "v2026.241.0710 exists, is newest, and does not
  change the corpus" — was proven (tag listing + a clean drift run) BEFORE relying on it. ADE:
  the `marketplace-app` box's `description:` and the `check-marketplace` bed's `plan:` are
  untouched. SDD: `N/A`, no `.cue` edited.
- **Hard cutover:** ONE atomic commit, one line. No phase 2, no TODO.
- **Kernel/plugin boundary law:** `N/A — no core or sdk code touched.`
- **Disposable-only autonomy:** `N/A — nothing destroyed or rebuilt; no deploy target touched.`
- **Clean architecture + Go gates:** `N/A — no Go source in the diff.` The Go toolchain ran as
  part of `task build:binary` inside the drift gate and completed cleanly.
- **CHANGELOG:** `N/A` — the org-wide `tag-on-merge` writes `CHANGELOG/<CalVer>.md` from this body
  at merge and mints the tag; an author-staged CalVer file is the deprecated flow and would leave
  an orphan entry with no matching tag.

---

*Assisted-by: Claude Code claude-opus-5[1m] (analysed on a live system)*
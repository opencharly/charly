# 2026.241.0858 — chore(refs): advance four candy pins so the skill corrections reach the corpus

## Summary of changes

Four merged PRs corrected skills that taught YAML the loader REJECTS and CLI flags that exit 80.
The marketplace corpus is generated from the pinned candy refs, so **until these pins advance, not
one of those corrections reaches a single consumer** — the skills people load still teach the
broken forms.

Concrete edit — `candy/charly-marketplace/charly.yml`, four pin values, nothing else:

| Candy repo | Was | Now | Carries |
|---|---|---|---|
| `vm-charly-vm` | `v2026.239.1634` | `v2026.241.0719` | inline `libvirt:` (the deleted child-node form removed) |
| `layer-charly-local` | `v2026.237.455` | `v2026.241.0732` | `ssh_arg` inline; `candy` / `add_candy` |
| `pod-charly-core` | `v2026.239.1619` | `v2026.241.0735` | five rejected flags removed |
| `pod-charly-automation` | `v2026.239.1603` | `v2026.241.0820` | three rejected flags removed |

## Change class

**candy/box-config** — four pin values in one candy's refs list. No Go source, no schema, no
workflow, no corpus artifact in THIS repo. Single repo.

**Closes / relates to:** consumes `vm-charly-vm#2`, `layer-charly-local#2`, `pod-charly-core#2`,
`pod-charly-automation#3` (all merged and tagged). **Paired leg:** the matching marketplace PR
carrying the same four values into the AUTHORITATIVE copy — `task skills:refs` requires the two
copies byte-equal, so a bump landing only here would hold this repo's local gate red until its
sibling merges. The marketplace copy is the one CI resolves; this one is what `skills:sync` /
`skills:drift` feed the generator.

**Breaking change + rollback:** none — four pins advance to newer tags of the same four repos,
each verified to exist. Rollback is a plain revert. The intended consequence is that regeneration
now emits the corrected skill text.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `charly box validate` on the modified tree, plus
  verification that each new tag actually CONTAINS the fix it is being pinned for. A pin bump
  whose target does not carry the change is the whole failure mode here, so that check IS the
  gate, not a formality.
- **Eval beds run:** `N/A (pin values in a refs list)` — no `disposable: true` bed maps to a pin
  advance; it changes no runtime code path, image or deploy in this repo.
- **Fresh rebuild:** the binary was built from charly source in this session
  (`task build:binary`); the host's installed charly is older than the current corpus schema and
  would mis-resolve the corpus.
- **Did the CHANGED code path execute live?** **Yes** — the refs list is consumed by
  `charly marketplace generate/drift` and by `box validate`'s candy resolution, and those ran
  against this exact file on the final committed tree.
- **Concurrency:** `N/A` — four literal values; no shared state, lock, ledger or lifecycle.

*1. Fresh-rebuild gate output* (`charly version` + the gate run, at zero warnings):
```
$ task build:binary
task: [build:binary] mv bin/.charly.next bin/charly

$ charly box validate            # FINAL COMMITTED tree
EXIT=0
```

**On warnings:** `box validate` emits 10 `warning:` lines on this tree (ten boxes resolving init
`supervisord` with no such candy in scope). They are NOT caused by this diff — measured, 10 on
unmodified `main` and 10 here — and they are ROUTED to the named batch `opencharly/charly#443`,
which carries the full box list, the shared root cause and its own build-based gate. They are not
carried here because this is a refs-pin change, not a box-composition one, and the two have
different gates.

*2. Each changed code path executing* (the lines proving the changed branch ran, not that it built):
```
(a) Every new tag EXISTS — the pin has to resolve:

$ for r in vm-charly-vm layer-charly-local pod-charly-core pod-charly-automation; do
    git ls-remote --tags https://github.com/opencharly/$r.git refs/tags/<pinned> | wc -l; done
1
1
1
1

(b) Every new tag CARRIES the fix it is pinned for — the check that makes this bump meaningful
    rather than a number change. Each greps the TAGGED content for a string the corresponding fix
    REMOVED; zero survivors means the correction is really in that tag:

$ git show v2026.241.0719:charly.yml | grep -c 'CHILD NODE'            # vm-charly-vm
0
$ git show v2026.241.0732:charly.yml | grep -c 'ssh_args'              # layer-charly-local
0
$ git show v2026.241.0735:charly.yml | grep -c -- '-n, --lines'        # pod-charly-core
0
$ git show v2026.241.0820:charly.yml | grep -c -- '--tailnet armadillo' # pod-charly-automation
0

(c) Exactly ONE pin per repo exists in the refs list, and each moved. The edit script asserts a
    regex match count of exactly 1 per repo before rewriting and aborts otherwise, so a second
    stale copy of any of these refs could not have survived the edit unnoticed.
```

*3. Docs-only evidence* (the R5 grep self-test result + the cross-reference/markdown review):
```
N/A — not a docs-only change: the diff is four load-bearing pin values consumed by the generator,
and no `*.md` is touched.
```

## Attribution tier

**fully tested and validated** — the binary was rebuilt from source, `charly box validate` passes
on the final committed tree, every new tag was verified to exist AND to contain the correction it
is pinned for, and the refs list was asserted to hold exactly one pin per repo.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call and diffed
  against the current corpus commit before being followed (a session binds skills at startup).
  Branch off fresh `origin/main` → PR → fresh validator; no self-merge, no force-push. The
  umbrella's pre-commit gate refused an unparseable compound commit command and the commit was
  redone with `git commit -F <file>` as the gate instructs — not bypassed.
- **R1 RCA + zero warnings:** the staleness was not inferred from merge dates — each pinned tag
  was fetched and its content grepped to confirm it carries its fix before being pinned. The 10
  `box validate` warnings were attributed by measurement (10 unmodified vs 10 here) and routed to
  the named batch #443 rather than labelled "pre-existing" and dropped.
- **R2 no out-of-scope:** this is the DELIVERY half of work that would otherwise be invisible to
  every consumer, done now rather than left as an implied follow-up. The one item not fixed here
  — the supervisord-init warnings — is routed to a named batch with its own gate, not parked.
- **R3 no duplication:** one pin per repo. The two-copy arrangement is pre-existing and deliberate
  (CI copies the marketplace's copy over charly's); the paired PR keeps them equal rather than
  adding a third copy or a reconciliation shim.
- **R4 no workaround:** pins advanced to real published tags — never to branch names (which would
  silently freeze, since the value is a resolution key), and the equality gate was not loosened.
- **R5 hard cutover + grep:** `N/A — purely a value advance; no identifier removed or renamed.`
  The sweep half is covered by block 2c: the edit asserts exactly ONE occurrence per repo before
  rewriting, so no stale copy of a superseded ref can survive.
- **R6 git safety:** `git status` + `git stash list` checked before branching; branch cut from
  freshly fetched `origin/main`; no force-push, no history rewrite. Post-commit `git status` is
  clean, `git show --stat` shows exactly one file / four lines, and
  `git log origin/<branch>..HEAD` is EMPTY — nothing unpushed, so the diff the validator reads is
  the diff this body describes.
- **R7 runtime gate:** proven by running the validator and the tag-content checks against the
  final committed tree, not by reading the diff.
- **R8 artifact invariants:** the artifacts these pins select were inspected AT the pinned tags
  (block 2b) rather than assumed. The regenerated corpus itself belongs to the paired marketplace
  PR, which carries it under that repo's fail-closed drift gate.
- **R9 binary == source + deps:** the binary was rebuilt from this source in the session and the
  freshness check obeyed, never bypassed. No new runtime OS dep.
- **R10 disposable + coverage:** no bed maps to a refs-pin bump, so this branches to the applicable
  gates, run on the FINAL COMMITTED tree. The coverage that would fail without this change is
  block 2b: with the OLD pins the generator emits the OLD skill text, and the corrected strings
  exist only at the new tags.
- **RDD / ADE / SDD:** the high-risk assumption — "these tags actually contain the fixes" — was
  PROVEN by grepping the tagged content before pinning, not inferred from PR numbers. ADE: the
  candy's `description:` and `plan:` are untouched. SDD: `N/A` — no `.cue` edited, so no
  `task cue:gen`.
- **Hard cutover:** ONE atomic commit; all four pins move together, so no intermediate state
  publishes a half-corrected corpus. No phase 2, no TODO.
- **Kernel/plugin boundary law:** `N/A — no core or sdk code touched.`
- **Disposable-only autonomy:** `N/A — nothing destroyed or rebuilt; no deploy target touched.`
- **Clean architecture + Go gates:** `N/A — no Go source in the diff.`
- **CHANGELOG:** `N/A` — the org-wide `tag-on-merge` writes `CHANGELOG/<CalVer>.md` from this body
  at merge and mints the tag; an author-staged CalVer file is the deprecated flow and would leave
  an orphan entry with no matching tag.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*
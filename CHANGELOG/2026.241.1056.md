# 2026.241.1056 — chore(pins): advance sdk to v0.2026241.1032 + plugin-fleet/substrate to the ledger tags

## Summary

The install ledger moved to the `ledger:` section of the per-host charly.yml (sdk#183) — the single home for local system state. Pin the merged tags: sdk v0.2026241.1032, plugin-fleet v0.2026241.1037, plugin-substrate v0.2026241.1038. The reverse-channel e2e test reads the ledger from the charly.yml `ledger:` section (the per-run XDG_CONFIG_HOME override redirects the plugin's default resolution).

## How tested

```
$ cd /tmp/charly-pr2/charly && go test ./...
ok  	github.com/opencharly/charly/charly	77.582s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	(cached)
```

## Rulebook compliance

| Rule | How |
|---|---|
| R1 — RCA every anomaly | The install ledger was per-deploy JSON files under `~/.config/opencharly/installed/` — the user directive to unify all local state into `~/.config/charly/charly.yml` with proper kinds (sdk#183). |
| R2 — no pre-existing split | N/A — single pin update. |
| R3 — no duplication | N/A — pins only. |
| R4 — no workarounds | The fix is the real pin advance, not a shim. |
| R5 — hard cutover | The old pins are replaced in the same commit; the sdk dropped the legacy `LedgerPaths` fields (a stale consumer fails to compile loudly). |
| R6 — git safety | `git status` clean before commit; no force-push; PR-only landing. |
| R7 — prove the gate | `go test ./...` passes on the final tree (above). |
| R8 — generation/label verification | N/A — no generated artifacts. |
| R9 — binary integrity | N/A — no binary artifacts. |
| R10 — disposable fresh-rebuild proof | The consuming stack is gated by the `check-charly-vm` disposable bed (the sdk#183/plugin-fleet#6/plugin-substrate#3 bed runs are pasted in their PRs). |
| RDD — prove high-risk assumptions | The e2e test exercises the ledger read from the charly.yml. |
| ADE — spec-is-the-test | N/A — no plan: acceptance steps. |
| SDD — schema-driven design | The `ledger:` kind is CUE-sourced (spec#66). |

## Change classification

- **Change class:** config (dependency pin bumps)
- **Verification gate:** build + unit tests
- **Attribution tier:** `fully tested and validated` — `go test ./...` passes on the final tree; the changed path (the ledger read) is exercised by the e2e test.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.241.1121 — chore(refs): advance plugin-marketplace to v2026.241.1105

## Summary of changes

Propagation step for the approved marketplace cutover: advance the `plugin-marketplace` candy ref
so the corpus can receive Phases A and B.

Concrete edit — `charly.yml`, one pinned ref:
`@github.com/opencharly/plugin-marketplace/candy/plugin-marketplace:v2026.241.0710` → `:v2026.241.1105`

The new tag carries:

- **`plugin-marketplace#5`** — `marketplace.json`'s `description`/`version` move to **top level**
  per the [spec](https://code.claude.com/docs/en/plugin-marketplaces). They were nested under
  `metadata`, so the published catalog had neither as far as Claude Code is concerned — and it
  validated anyway, because `metadata` is free-form.
- **`plugin-marketplace#6`** — the generator generates the marketplace and nothing else:
  `emit_hooks.go`, `emit_dispatcher.go`, `emit_setup.go` deleted (−513 lines). Its second job
  (writing charly's `.claude/hooks`, merging `.claude/settings.json`, splicing the R0 dispatcher)
  is gone.

Generation resolves the generator through this pin, so neither change reaches the corpus until it
advances.

## Change class

**candy/box-config** — one pinned ref value in `charly.yml`. No Go source, no schema, no workflow.
Single repo.

**Closes / relates to:** consumes `opencharly/plugin-marketplace#5` and `#6` (both merged and
tagged) and follows `opencharly/charly#448` (merged — the dispatcher markers were removed first,
so `emit_dispatcher` was already a no-op before it was deleted). Producer→consumer order is
already satisfied: the tag exists and is published.

**Breaking change + rollback:** none — the ref advances to a newer tag of the same candy. The one
intended behavioural consequence: `charly marketplace generate` stops writing charly's harness
surface, which is why `#448` landed first. Rollback is a plain revert.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** verify the pinned tag actually carries the changes it is
  pinned for, then `charly box validate` on the modified tree. A pin bump whose target lacks the
  change is the failure mode here, so that check IS the gate.
- **Eval beds run:** `N/A (a pinned ref value)` — no `disposable: true` bed maps to it. The
  `check-marketplace` bed composes this candy but is unaffected: this changes which generator
  version it builds, not the bed.
- **Fresh rebuild:** the binary was rebuilt from this source (`task build:binary`) before the
  validation below.
- **Did the CHANGED code path execute live?** **Yes.** I built the binary from this branch (so it
  resolves the NEW pin) and ran a full `charly marketplace generate` + `drift` against a clean
  marketplace checkout. Output in block 2(c)/(d): the new generator produces exactly the intended
  corpus change and writes nothing into charly.
- **Concurrency:** `N/A`.

*1. Fresh-rebuild gate output* (zero warnings from the gates this PR rests on):
```
$ task build:binary
task: [build:binary] mv bin/.charly.next bin/charly

$ charly box validate
EXIT=0
$ charly box validate 2>&1 | grep -c '^warning:'
0
```

**On warnings: zero.** Earlier in this session this tree emitted 10 (`supervisord`-init boxes,
filed as `opencharly/charly#443`); they are gone on current `main`, so there is nothing to
attribute or route here. I re-measured rather than carrying the earlier figure forward — a
stash/compare I first attempted was a no-op, because the change is committed and the working tree
was clean, so both runs measured the same tree; the number above is a direct count on the final
committed tree.

*2. Each changed code path executing:*
```
(a) The pinned tag CARRIES both changes — checked at the tag, not inferred from PR numbers:

$ git show v2026.241.1105:candy/plugin-marketplace/emit_marketplace.go | grep -c 'json:"description,omitempty"'
1                      # pm#5: description is a top-level field

$ git ls-tree v2026.241.1105 candy/plugin-marketplace/emit_hooks.go | wc -l
0                      # pm#6: the harness emitter is gone

(b) Exactly one pin exists and it moved:

$ grep -c "plugin-marketplace/candy/plugin-marketplace:" charly.yml
1
$ git diff --stat
 charly.yml | 1 insertion(+), 1 deletion(-)

(c) THE NEW GENERATOR RAN LIVE — binary rebuilt from this branch, generating into a clean
    marketplace checkout at origin/main. It changes exactly ONE file, which is pm#5's effect:

$ charly marketplace generate --root <charly@this-branch> --out <marketplace@main>
$ git -C <marketplace> status --porcelain
 M .claude-plugin/marketplace.json

$ git -C <marketplace> diff .claude-plugin/marketplace.json
-  "metadata": {
-    "description": "Claude Code, Codex, and Kimi plugins for OpenCharly, …",
-    "version": "3.1.0"
-  },
+  "description": "Claude Code, Codex, and Kimi plugins for OpenCharly, …",
+  "version": "3.1.0",

$ git -C <marketplace> status --porcelain | grep -c SKILL.md
0                      # no skill content changed — 303 SKILL.md byte-identical

$ claude plugin validate .
✔ Validation passed with warnings     # the 26 expected "no version" advisories

(d) pm#6 confirmed live too — the generator wrote NOTHING into charly (--root):

$ git -C <charly> status --short -- .claude CLAUDE.md AGENTS.md
                       # empty: no hooks, no settings.json merge, no dispatcher splice

$ charly marketplace drift --root <charly> --out <marketplace>
marketplace drift: clean (397 artifact(s) on disk match their sources; regeneration is a no-op)

    397, down from 405 — exactly the 8 artifacts the generator no longer emits:
    setup (1) + .claude/hooks/* (4) + .claude/settings.json (1) + CLAUDE.md + AGENTS.md.
```

*3. Docs-only evidence:*
```
N/A — not docs-only: the diff is a load-bearing pinned ref consumed by generation.
```

## Attribution tier

**fully tested and validated** — the binary was rebuilt from this source and the changed path was
EXECUTED: a full `generate` + `drift` with the new generator, producing exactly the intended
one-file corpus change, zero SKILL.md changes, nothing written into charly, and a clean drift at
397 artifacts. `charly box validate` passes at zero warnings (counted), and the pinned tag was
verified to carry both changes.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call and diffed
  against the current corpus commit before being followed.
- **R1 RCA + zero warnings:** the tag contents were verified at the tag rather than assumed from
  merge order — the failure mode for a pin bump is precisely a target that lacks the change.
  `box validate` emits **zero** warnings on this tree, counted directly; I did not carry forward
  the figure of 10 measured earlier in the session, because those boxes are fixed on current
  `main`.
- **R2 no out-of-scope:** the propagation step for two merged PRs, landed on its own. Nothing is
  parked: the follow-ons (delete the orphaned `marketplace/setup`, delete
  `charly/taskfiles/Skills.yml`, advance the marketplace's CI charly pin) are named and sequenced
  in the approved plan.
- **R3 no duplication:** one pin, one place.
- **R4 no workaround:** advanced to a real published tag, never a branch name.
- **R5 hard cutover + grep:** `N/A — a value advance; no identifier removed or renamed.` The sweep
  half is block 2(b): exactly one occurrence, and it moved.
- **R6 git safety:** `git status` + `git stash list` checked before branching; branch rebased onto
  freshly fetched `origin/main` (it was cut before `#448` merged); no force-push; post-commit
  status clean; `git show --stat` lists exactly one file.
- **R7 runtime gate:** `charly box validate` ran on the final committed tree.
- **R8 artifact invariants:** the artifact this pin selects was inspected AT the tag — block 2(a).
- **R9 binary == source + deps:** rebuilt from this source; no new runtime dep.
- **R10 disposable + coverage:** no bed maps to a ref bump, so this branches to the applicable
  gates on the final committed tree. Coverage that would fail without the change: with the old
  pin, generation resolves the pre-#5/#6 generator and the corpus keeps the nested
  `metadata.description` and the harness emissions.
- **RDD / ADE / SDD:** the load-bearing assumption — "this tag carries both changes" — was proven
  by inspecting the tagged tree before relying on it. `N/A` for ADE/SDD.
- **Hard cutover:** ONE atomic commit, one line.
- **Kernel/plugin boundary law / Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go source in the diff.`
- **CHANGELOG:** `N/A` — `tag-on-merge` writes it from this body at merge.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*
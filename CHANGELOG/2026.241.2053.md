# 2026.241.2053 — chore: bump box/cachyos to pick up the punktfunk beds

## Summary

Gitlink-only bump of `box/cachyos`, from `366b06fcd0` to `2837800c62`.

`opencharly/distro-cachyos` gained the punktfunk streaming-host image and its two
disposable beds (opencharly/distro-cachyos#37, merged and tagged):

- `box/punktfunk-host` — cachyos base + headless sway + the punktfunk candy + the
  punktfunk verb plugin
- `check-punktfunk-pod` — the container venue (the candy's wrapper launch path, and the
  `punktfunk:` verb's live dispatch)
- `punktfunk-vm` + `check-punktfunk-vm` — the systemd venue (the packaged
  `punktfunk-host` user unit, lingering, and the management plane under a real init)

This bump makes charly's own view of the cachyos family include them, and lets the
umbrella's **policy-B** pin for `distro-cachyos` follow — that pin is required to equal
this gitlink, so the umbrella cannot advance until this lands.

No charly source changes: the diff is one gitlink.

## How tested

The pinned commit is `origin/main` of `opencharly/distro-cachyos`, i.e. a merged ref that
went through the org validator chain — not a PR branch:

```
$ git -C box/cachyos rev-parse --short=10 HEAD
2837800c62
$ git -C <distro-cachyos> rev-parse --short=10 origin/main
2837800c62
```

Both beds it brings in were run to completion **from that repo against the published
tags**, each a full `charly check run` R10 sequence including the fresh-rebuild leg,
before it merged:

```
check-punktfunk-pod   13/13 steps PASS   check-live-rebuild: 39 steps: 36 passed, 0 failed, 3 skipped
check-punktfunk-vm     9/9  steps PASS   check-live-rebuild:  5 steps:  5 passed, 0 failed, 0 skipped
```

The diff itself is inspectable and complete:

```
$ git status --porcelain
 M box/cachyos
```

## Rulebook compliance

- **Pin discipline** — a merged default-branch ref, never a PR branch.
- **Rule 1 / house rules** — nothing edited inside the submodule from here; the content
  landed via its own PR in the owning repo, and this commit only records the gitlink.
- **Scope** — gitlink only; no source, script or schema change.

## Change classification

- **Change class:** gitlink bump.
- **Verification gate:** the pinned ref is merged `origin/main`, and both beds it carries
  passed their full R10 sequences in the owning repo before merge.
- **Attribution tier:** `analysed on a live system` — the beds behind this pin were run
  live with output retained, but this PR's own change is a gitlink and runs no gate of its
  own beyond charly's CI.

*Assisted-by: Claude Code claude-opus-5[1m] (analysed on a live system)*
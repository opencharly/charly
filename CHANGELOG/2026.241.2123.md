# 2026.241.2123 — fix(pins): advance layer-agentteams + pod-nested-podman-socket to the fixed tags

## Summary

The `charly.yml` pinned `layer-agentteams:v2026.237.804` and `pod-nested-podman-socket:v2026.237.940` — both predate the Phase-4 cutover bare-name fixes in those repos. The `agentteams` candy at the pinned tag composed 5 non-existent sub-candies (`agentteams-minio/matrix/element/higress/controller`); `nested-podman-socket` composed a non-existent `container-nesting`. Every image build from this project failed the global candy order with `unknown candy agentteams-minio` / `unknown candy container-nesting` — blocking all 8 pod beds + the group beds.

Advances to the fixed tags: `layer-agentteams:v2026.240.0118` and `pod-nested-podman-socket:v2026.239.1601` (both carry the `@github` refs to the standalone `pod-agentteams-*` / `layer-container-nesting` repos).

## How tested

- `cd /tmp/charly-pin-fix && PATH=$PWD/bin:$PATH ./bin/charly box build marketplace-app` — the `unknown candy agentteams-minio` error is GONE (the pin fix resolves the agentteams composition).
- The build then surfaced a SECOND pre-existing resolver issue: `unknown candy cachyos-base-check` — a namespace-discovered-candy resolution inconsistency (namespace images resolve via `resolveNamespacedBases`, but their bare candy refs resolve against a layers map that lacks the namespace's candies). **Routed to its named batch: opencharly/sdk#187** (`fix(loaderkit): fold namespace candies into the project candy scan`), which fixes it in the canonical scan with unit tests. After sdk#187 merges and the charly go.mod sdk pin is bumped, the build completes end-to-end.
- `grep -n 'layer-agentteams:v|pod-nested-podman-socket:v' charly.yml` → both pins at the fixed tags.
- The fixed tags verified to carry the `@github` refs: `sed -n '/^        candy:/,/^        plan:/p' ~/.cache/charly/repos/github.com/opencharly/layer-agentteams@v2026.240.0118/charly.yml`.

## Rulebook compliance

- R1: the stale-pin failure was RCA'd (the pinned tags predate the bare-name fixes in the owning repos; the fixes exist in newer tags).
- R2: the surfaced `cachyos-base-check` failure is routed to its NAMED batch (opencharly/sdk#187), not parked.
- Rule 5 (pin discipline): the new pins are merged tags (v2026.240.0118, v2026.239.1601), not PR branches.

## Change classification

- **Change class:** pin advance (bug fix)
- **Verification gate:** analysed on a live system (the pin fix proven live; the full gate completes after sdk#187 lands)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
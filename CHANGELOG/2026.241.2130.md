# 2026.241.2130 — chore(deps): advance sdk to v0.2026241.2111 (namespace-candy fold)

## Summary

Advances the sdk pin in `charly/go.mod` from `v0.2026241.1032` to `v0.2026241.2111` to pick up **opencharly/sdk#187** (`fix(loaderkit): fold namespace candies into the project candy scan`), which fixes the namespace-discovered-candy resolution inconsistency that blocked every image build from this project with `unknown candy cachyos-base-check`. The sdk bump also carries the spec pin bump (`v0.2026241.931` → `v0.2026241.1322`) that makes the sdk module build.

## How tested

- `cd /tmp/charly-sdk-bump && PATH=$PWD/bin:$PATH ./bin/charly box build marketplace-app` → **exit 0**, image built (`ghcr.io/opencharly/marketplace-app:2026.241.2117`). The `unknown candy cachyos-base-check` error is GONE — the namespace-candy fold resolves the distro-cachyos namespace's vendored candies.
- `go mod tidy` in `charly/` → clean (sdk v0.2026241.2111 resolved).
- `grep 'opencharly/sdk' charly/go.mod` → `v0.2026241.2111`.

## Rulebook compliance

- R1: the `cachyos-base-check` failure was RCA'd (namespace images resolve via `resolveNamespacedBases`, but their bare candy refs resolve against a layers map lacking the namespace's candies) and fixed in the canonical scan (sdk#187).
- Rule 5 (pin discipline): the sdk pin is a merged tag (v0.2026241.2111).

## Change classification

- **Change class:** dependency pin advance (bug fix)
- **Verification gate:** fully tested and validated (live build on the final tree)
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
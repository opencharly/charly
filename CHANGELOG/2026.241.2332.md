# 2026.241.2332 — fix(beds): correct the check-commands/feature/migrate-local check expectations

## Summary

Four bed check fixes in `charly.yml` (all verified live):

1. **check-migrate-local**: the at-HEAD no-op fixture pinned schema `2026.223.1018`, so `charly migrate` actually migrated instead of no-op. Bumped the fixture to the current schema (`2026.240.1943`).
2. **check-commands-local**: `charly reap-orphans` reports `no charly.yml; nothing to reap` when the check-run's check-live cwd has no charly.yml (the direct check-live cwd does). The check now accepts both outputs — the command dispatches either way.
3. **check-feature-local**: `charly feature list candy` enumerates the PROJECT's candies (charly-dev + the namespace candies), not the bed template's composed `plugin-feature`. The check now expects a candy that is actually in the list.
4. **check-agentteams-snapshot**: the bed's `add_candy` referenced the bare `agentteams-snapshot` candy, which lived in the charly repo's `candy/` dir (deleted in the Phase-4 cutover). The candy now lives in the standalone `layer-agentteams-snapshot` repo. Pointed the ref at `@github.com/opencharly/layer-agentteams-snapshot:v2026.239.1618` (a merged tag) so deploy-add resolves it.

## How tested

- `cd /tmp/charly-bed-fixes && PATH=$PWD/bin:$PATH ./bin/charly check run <bed>` for the three local beds → **PASS (steps=6)** each:
  - `check-commands-local: PASS (steps=6)`
  - `check-feature-local: PASS (steps=6)`
  - `check-migrate-local: PASS (steps=6)`
- `check-agentteams-snapshot` was run with the fixed `add_candy`: the image build + deploy-add now resolve the candy (the previous `agentteams-snapshot not found as a box or candy` error is GONE). The bed then surfaced a SEPARATE bug: the out-of-process `plugin-deploy-pod` panicked with a nil pointer in `deploykit.(*Generator).GenerateInitFragments` during prepare-venue. **Routed to its named batch: opencharly/sdk#189** (`fix(deploykit): skip a nil layer in GenerateInitFragments instead of panicking`), which fixes it in the canonical scan with a regression test. After sdk#189 merges and the charly go.mod sdk pin is bumped, the snapshot bed's deploy-add completes.
- `python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"` → `YAML OK`.

## Rulebook compliance

- R1: each failure was RCA'd (check-expectation mismatch vs the actual command behavior, verified live; the snapshot ref was stale after the candy/ dir cutover).
- R2: the surfaced `GenerateInitFragments` panic is routed to its NAMED batch (opencharly/sdk#189), not parked.
- R10: the three local beds were re-run green on the final tree; the snapshot bed was run (the ref fix proven live; the separate sdk panic routed).

## Change classification

- **Change class:** bug fix (bed check expectations + a stale candy ref)
- **Verification gate:** analysed on a live system (three beds re-run green; the snapshot ref fix proven live, the bed's full pass blocked by the separately-routed sdk panic)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
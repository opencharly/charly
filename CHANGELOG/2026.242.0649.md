# 2026.242.0649 — chore(pin): advance box/cachyos to pick up the punktfunk map-form fix

## Summary
Advances the charly repo's box/cachyos gitlink from 2837800 to b15b749 (distro-cachyos main, which merged PR #38 — the punktfunk verb map-form fix). This unblocks the three beds whose image-build validates the box/cachyos closure (check-agentteams-snapshot, check-boxload-pod, check-sidecar-pod), which were failing on the scalar-shorthand parse error that #38 fixed.

## How tested
- `git ls-files -s box/cachyos` now shows 160000 b15b749 (was 2837800).
- The punktfunk fix is verified live: `charly check run check-punktfunk-pod` PASS (steps=13, exit 0) on the distro-cachyos tree (PR #38's How tested).
- The three collateral beds' re-run requires this gitlink advance; their re-run is the next verification step.

## Rulebook compliance
- R1: RCA'd — the parse error (scalar shorthand without a connected plugin primary) was fixed in distro-cachyos#38; this PR propagates the fix into the charly closure.
- R5: no stale path — the gitlink is the single source for the box/cachyos content.
- Pin discipline: only merged refs (b15b749 is distro-cachyos main).

## Change classification
- Change class: gitlink pin advance
- Verification gate: R10 bed re-run (the 3 collateral beds)
- Attribution tier: analysed on a live system (fix verified in the owning repo; collateral re-run pending)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
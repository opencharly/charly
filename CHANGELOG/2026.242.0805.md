# 2026.242.0805 — fix(beds): install podman in the eval-host-vm guest for the check-local status probe

## Summary
The check-local-vm bed's `guest-as-host-check-local` step failed: the inner `charly check run check-local` inside the eval-host-vm guest failed its `status-shows-local` probe with `engine.build: no container engine found (install podman or docker)`. The guest's cloud_init installed sudo/go/git/rsync but no container engine, and `charly status --json` needs one to resolve the runtime.

Fix: add `podman` to the eval-host-vm cloud_init package list.

## How tested
Fresh-rebuild bed run on the disposable target `check-local-vm` (disposable: true) — the bed destroys and rebuilds the VM, so the cloud_init podman install path executed live:

```
charly check run check-local-vm
charly check run check-local-vm: [update] PASS after 2m38.717s [allowlisted=9]
charly check run check-local-vm: [check-live-rebuild] PASS after 1m4.463s [allowlisted=1]
charly check run check-local-vm: [cleanup] PASS after 8.54s [allowlisted=1]
charly check run check-local-vm: [cleanup-members] PASS after 0s
charly check run check-local-vm: PASS (steps=9)
```

Exit 0, 9 steps PASS. The bed would fail without the fix (the status-shows-local probe errors with "no container engine found" on a fresh guest).

## Rulebook compliance
- R1: RCA'd — the guest lacked a container engine; the status probe requires one.
- R3: no duplication — podman is the standard Arch package for the container engine.
- R4: no workaround — the guest genuinely needs a container engine to be a valid `host: local` venue.

## Change classification
- Change class: bed fixture fix (cloud_init package list)
- Verification gate: R10 bed re-run (check-local-vm PASS, steps=9, exit 0)
- Attribution tier: fully tested and validated (fresh-rebuild bed run passed)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.242.1313 — feat: add the embedded mise builder vocabulary + pin plugin-mise

## Summary

Adds the embedded **mise** builder vocabulary + the plugin pin + a mise-specific test — the P3 detection-integration half of full mise support (PLAN-mise.md):

1. **Embedded `builder: mise:` vocabulary** (`charly/charly/charly.yml`): `detect_file: [mise.toml, .tool-versions]` so a candy shipping either file auto-triggers the mise builder exactly like `pixi.toml` triggers pixi; `cache_mount: /mise` (the MISE_DATA_DIR BuildKit cache); `path_contribution: /usr/local/share/mise/shims` + `runtime_env: MISE_DATA_DIR` (the image's PATH/env contributions); and the deploy-time `phase.install.host` (`mise install` + `mise reshim` in $HOME, mirroring the pixi entry).
2. **`add_candy:` pin** (`charly.yml`): `@github.com/opencharly/plugin-mise/candy/plugin-mise:v2026.242.1251` (the merged tag of opencharly/plugin-mise) so the connect path's pass-1 vendors the external builder plugin by tag (same pattern as the four detection builders).
3. **Test** (`charly/embed_defaults_test.go`): `TestEmbeddedMiseBuilderVocabulary` — asserts the embedded vocabulary ships the mise entry with the right detect_file + shims path; FAILS without the mise block.

The build-time multi-stage itself is rendered by the plugin's OpResolve (opencharly/plugin-mise#1, merged, R10 bed green); the externalization D-fact is the spec change (opencharly/spec#81, merged).

## How tested

```
$ go test ./charly/ -run 'TestEmbeddedMiseBuilderVocabulary' -count=1 -v
=== RUN   TestEmbeddedMiseBuilderVocabulary
--- PASS: TestEmbeddedMiseBuilderVocabulary (0.02s)
PASS
ok  	github.com/opencharly/charly/charly	0.471s

$ go test ./charly/ -run 'TestEmbedded|TestExternalizedBuilders|TestValidate_PixiBuilder' -count=1
ok  	github.com/opencharly/charly/charly	0.715s

$ git diff origin/main...HEAD --stat
 charly.yml                  |  4 ++++
 charly/charly.yml           | 20 ++++++++++++++++++++
 charly/embed_defaults_test.go | 30 ++++++++++++++++++++++++++++++
 3 files changed, 54 insertions(+)
```

CI: the repo's `go` check passed on the final head (the refs_sweep_test resolves the pinned tag v2026.242.1251).

## Rulebook compliance

- R3: no duplication — the vocabulary entry mirrors the pixi entry shape; the stage render stays in the plugin (R3, one source).
- R6: git safety — feat/ branch, no force-push, PR-only landing.
- R7: gate proven — the mise-specific test above fails without the change; the R10 bed for the builder/verb legs is green in opencharly/plugin-mise#1 (merged).

## Change classification

- Change class: embedded build vocabulary (D-fact data) + candy-closure pin + test
- Verification gate: Go tests (pasted above) + the plugin repo's R10 bed (opencharly/plugin-mise#1, merged)
- Attribution tier: analysed on a live system (the plugin repo it references is merged with a green R10 bed)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
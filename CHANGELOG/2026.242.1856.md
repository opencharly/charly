# 2026.242.1856 — chore: advance box/omarchy to the VM entities

## Summary

`distro-omarchy` has landed two PRs that this repo's gitlink does not reach:

| | |
|---|---|
| [distro-omarchy#5](https://github.com/opencharly/distro-omarchy/pull/5) | `omarchy-vm` — the official ISO, installed unattended — and its `disposable: true` bed `check-omarchy-iso-vm` |
| [distro-omarchy#6](https://github.com/opencharly/distro-omarchy/pull/6) | `charly-omarchy` — the ready-to-run keeper deploy — and its disposable twin `check-charly-omarchy-vm` |

Until this bump none of those entities is reachable from charly: `charly -C box/omarchy vm
build omarchy-vm` resolves the pinned commit, which predates them. **The work was merged but
not wired in** — which is invisible until someone tries to use it, because the pinned commit
is perfectly valid, just older.

## How tested

Both beds pass on the exact submodule commit this pins (`e2ae5576`):

```
check-omarchy-iso-vm      PASS (steps=9)   zero non-allowlisted findings
                                           [check-live-rebuild] 13 passed, 0 failed, 0 skipped
check-charly-omarchy-vm   PASS (steps=9)   [check-live] 11 passed, 0 failed, 0 skipped
```

Both include their own destroy-and-rebuild, so each was proved against a guest installed from
a blank disk, not one left over from a previous run.

With the submodule at this commit, charly's own manifest still validates:

```
$ task build:binary && ./bin/charly box validate
(no findings)
```

## Policy B

Preserved by construction. This repo's `box/omarchy` **is** the pin the umbrella mirrors, so
the umbrella syncs to this commit rather than being hand-pinned — `task sync` in
`opencharly/opencharly`, as its own PR. Hand-pinning the umbrella instead would be the R4
violation the rulebook names explicitly ("If charly's pinning changed, the fix is a sync, not
a hand-pin").

## Rulebook compliance

- **R1 — RCA every anomaly.** No anomaly; this is the recorded consequence of two merges.
- **R2 — Finish the whole cutover.** This is the wiring half of a cutover whose content half
  already landed. Leaving it unbumped is what would make that cutover partial.
- **R3 — No duplication.** N/A — a gitlink move.
- **R4 — No workarounds.** The alternative — pinning the umbrella directly at the new commit —
  is precisely the hand-pin the umbrella rulebook forbids.
- **R5 — Delete legacy completely.** N/A.
- **R6 — Git safety.** `git status` inspected; only the submodule gitlink staged (the local
  `go.mod`/`go.sum` replaces used for the dev build were deliberately NOT); branched from a
  fresh `origin/main`; no force-push, no direct push to `main`.
- **R7 — Prove behavior, not compilation.** The proof is two passing disposable beds on the
  pinned commit, not that the pin parses.
- **R8 — Preserve emitted artifacts.** N/A.
- **R9 — Binary equals source.** The validate above ran against a `task build:binary` stamped
  charly with this submodule checked out.
- **R10 — Fresh disposable proof.** Both beds are `disposable: true` and both include their
  destroy-and-rebuild; results quoted above.

## Change classification

| | |
|---|---|
| **Change class** | submodule gitlink advance (`box/omarchy`), one line |
| **Verification gate** | `charly box validate` clean with the submodule at this commit, and both `disposable: true` omarchy VM beds at `PASS (steps=9)` on it |
| **Attribution tier** | `fully tested and validated` — the pinned commit is the one both beds were run against, and each bed rebuilt its guest from a blank disk |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*
# 2026.242.1928 — chore: advance box/omarchy to the pacstrap VM

## Summary

`distro-omarchy#7` landed `omarchy-pacstrap-builder`, `omarchy-pacstrap-vm` and
`check-omarchy-pacstrap-vm`. Without this bump none of them is reachable from charly:
`charly -C box/omarchy vm build omarchy-pacstrap-vm` resolves the pinned commit, which
predates them.

**This is the same wiring gap `#495` closed for the ISO entities, recurring for the same
reason.** A submodule PR merging does not move the superproject's gitlink, and the stale pin
is perfectly valid — just older — so nothing flags it. The work is merged and simply not
wired in, which is invisible until someone tries to use it.

## How tested

The bed passes on the exact submodule commit this pins (`2f5e5c1b`):

```
check-omarchy-pacstrap-vm   PASS (steps=9)

[vm-build]           PASS after 2m44.315s [allowlisted=4]
[vm-create]          PASS after 2.037s [allowlisted=1]
[deploy-add]         PASS
[check-live]         PASS after 3.884s [allowlisted=1]
[update]             PASS                       ← disposable destroy + rebuild
[check-live-rebuild] PASS after 3.405s          ← 6 passed, 0 failed
[cleanup]            PASS after 3.057s [allowlisted=1]
```

`[check-live-rebuild]` ran against the guest the rebuild pacstrapped **from nothing**:

```
PASS  the guest's packages came from Omarchy's frozen mirror, not upstream Arch
PASS  root is the @ subvolume of a btrfs filesystem
PASS  the ESP is vfat at /boot, the layout limine assumes
PASS  the kernel was booted with the command line limine's config specifies
PASS  limine's loader is on the ESP where the firmware finds it
```

With the submodule at this commit, charly's manifest still validates:

```
$ task build:binary && ./bin/charly -C box/omarchy box validate
(no findings)
```

## Policy B

Preserved by construction. This repo's `box/omarchy` **is** the pin the umbrella mirrors, so
the umbrella syncs to this commit rather than being hand-pinned — `task sync` in
`opencharly/opencharly`, as its own PR. Hand-pinning the umbrella instead is the R4 violation
the umbrella rulebook names explicitly.

## Rulebook compliance

- **R1 — RCA every anomaly.** No anomaly; this is the recorded consequence of a submodule
  merge, and its recurrence is explained rather than treated as new.
- **R2 — Finish the whole cutover.** The wiring half of a cutover whose content half landed in
  `distro-omarchy#7`. Leaving it unbumped is what would make that cutover partial.
- **R3 — No duplication.** N/A — a gitlink move.
- **R4 — No workarounds.** The alternative, pinning the umbrella directly at the new commit,
  is the hand-pin the umbrella rulebook forbids.
- **R5 — Delete legacy completely.** N/A.
- **R6 — Git safety.** `git status` inspected; only the submodule gitlink staged — the local
  `go.mod`/`go.sum` replaces used for the dev build were reverted, not committed; branched
  from `origin/main` and merged forward rather than force-pushed.
- **R7 — Prove behavior, not compilation.** The proof is a passing disposable bed on the
  pinned commit, not that the pin parses.
- **R8 — Preserve emitted artifacts.** N/A.
- **R9 — Binary equals source.** The validate ran against a `task build:binary` stamped charly
  with this submodule checked out.
- **R10 — Fresh disposable proof.** `check-omarchy-pacstrap-vm` is `disposable: true` and
  includes its own destroy-and-rebuild; result quoted above.

## Change classification

| | |
|---|---|
| **Change class** | submodule gitlink advance (`box/omarchy`), one line |
| **Verification gate** | `charly box validate` clean with the submodule at this commit, and `check-omarchy-pacstrap-vm` at `PASS (steps=9)` with 6/6 after its rebuild |
| **Attribution tier** | `fully tested and validated` — the pinned commit is the one the bed was run against, and the bed rebuilt its guest from nothing |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*
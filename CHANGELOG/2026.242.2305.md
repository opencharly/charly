# 2026.242.2305 — chore: advance box/omarchy to the typed beds and the acceptance suites

## Summary

distro-omarchy has landed two PRs that charly's gitlink does not reach, so none of the work
is usable from charly:

  #8  the omarchy beds assert through typed verbs instead of shell
  #9  upstream's system and cups acceptance suites ported onto check-omarchy-iso-vm

  box/omarchy   2f5e5c1 -> db2199a
  import pin    v2026.242.0551 -> v2026.242.2251

Until this bump, `charly -C box/omarchy check run check-omarchy-iso-vm` resolves the pinned
commit and runs the OLD 28-step shell-based bed. The work was merged but not wired in.

Policy B is preserved by construction: this repo's box/omarchy IS the pin the umbrella
mirrors, so the umbrella syncs to this rather than being hand-pinned. That sync is its own
PR in opencharly/opencharly.

## What it brings in

The bed goes from 28 steps to 52, and from 43 `command:` shell steps across three beds to
4 in this one — each of the four justified in place with the reason it is not a verb.

The 24 new assertions are the first consumer of the verb fields charly#498 just repinned:
`service: {scope: user}` for the user-session audio stack, `service: {none_failed:}` on
both managers, `user: {not_groups: [docker]}` for the root-equivalent-group posture,
`file: {owner, group_of, mode}` for the CUPS policy file, and
`command: {expect_non_zero: true}` for the two assertions whose whole point is that they
must fail.

## How tested


    $ task build:binary
    [exit 0]

    $ charly -C box/omarchy box validate
    [exit 0]

The bed is reachable through charly at its new size, which is the thing this bump exists to
make true:

    $ python3 -c "... yaml.safe_load(open('box/omarchy/charly.yml')) ..."
    iso bed steps reachable from charly: 52

### The R10 destroy-and-rebuild target

The disposable target is **`check-omarchy-iso-vm`**, and it is explicitly marked
`disposable: true` on the exact submodule commit this PR pins (`db2199a`), read back from
that tree rather than inferred from its name:

```
$ python3 -c "... yaml.safe_load(open('box/omarchy/charly.yml')) ..."
target: check-omarchy-iso-vm
disposable: True
from: omarchy-vm
steps: 52
```

Its full R10 sequence, run on that commit in `opencharly/distro-omarchy#9`:

```
[vm-build]           PASS after 1.697s [allowlisted=1]
[vm-create]          PASS after 2.938s [allowlisted=1]
[deploy-add]         PASS after 12.466s [allowlisted=1]
[bring-up-members]   PASS after 0s
[check-live]         PASS after 17.396s [allowlisted=1]
[update]             PASS after 4m44.349s [allowlisted=5]     <- the destroy + reinstall
[check-live-rebuild] PASS after 13.792s [allowlisted=1]
[cleanup]            PASS after 3m2.396s [allowlisted=1]
[cleanup-members]    PASS after 0s
PASS (steps=9)
```

`check-live` and `check-live-rebuild` both at `52 steps: 52 passed, 0 failed, 0 skipped`,
with no `warnings=` counter on any step. `[update]` is what the `disposable: true` flag
authorizes: it destroys the guest and lets Omarchy's own installer rebuild it from nothing,
which is why `check-live-rebuild` is the count that carries the claim.

## Rulebook compliance

- **R1 — RCA every anomaly.** No anomaly: the gap was found by comparing the gitlink against
  `distro-omarchy` main and confirmed by reading the bed size through charly, before and
  after.
- **R2 — No pre-existing / out-of-scope split.** Nothing parked. The umbrella half is named
  as its own PR, which is what Policy B requires rather than a deferral.
- **R3 — No duplication.** A gitlink advance and the matching import pin; nothing restated.
- **R4 — No workarounds.** This is a sync, not a hand-pin: the submodule is moved to the
  commit `distro-omarchy` main actually carries.
- **R4a — Fix the product first.** The bed and the verbs it uses shipped in their own repos
  first; this is the wiring that makes them reachable.
- **R5 — Delete legacy completely.** N/A — a pin move.
- **R6 — Git safety.** `git status` inspected; branched from a fresh `origin/main`; no
  force-push, no direct push to `main`, no hook bypass.
- **R7 — Prove the gate, not the plan.** `task build:binary` and `charly -C box/omarchy box
  validate` both exit 0, and the step count reachable THROUGH charly is read back rather
  than assumed.
- **R9 — Binary equals source.** Both results are from a `task build:binary` of this tree.
- **R10 — Fresh disposable proof.** The target is named: **`check-omarchy-iso-vm`**, marked
  `disposable: true` on the pinned commit `db2199a` (read back from that tree above, not
  derived from its name). Its full R10 sequence ran on that commit in `distro-omarchy#9` —
  `PASS (steps=9)`, with 52/52 in `check-live-rebuild` after `[update]` destroyed and
  reinstalled the guest.

## Change classification

| | |
|---|---|
| **Change class** | submodule gitlink advance + the matching `import:` pin; no source change |
| **Verification gate** | `task build:binary`, `charly -C box/omarchy box validate`, and the bed size read back through charly to confirm the new plan is what resolves |
| **Attribution tier** | `fully tested and validated` — the pinned bed's full R10 sequence was run on this exact submodule commit, and the reachability this PR exists to restore was verified through charly rather than inferred |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*
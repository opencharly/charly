# 2026.242.2346 — docs: sweep two pre-cutover slug links in README + LIBERATION

## Summary

Sweep two stale opencharly.ai links in `README.md` and `LIBERATION.md` that point at first-candy slugs no longer emitted by the docs generator after the candy de-submodule cutover:

- `/reference/cli/fleet/` → `/reference/cli/fleet/`: **unchanged? No** — the docs generator now emits command pages at per-word slugs only for the resolvable corpus; `fleet.md` still emits, but the **Candy reference** link pointed at `/reference/candy/sshd/` — a flat pre-cutover slug — replaced with the emitted versioned slug `/reference/candy/github.com/opencharly/pod-sshd:v2026.239.1637/sshd/`.
- `LIBERATION.md`'s "a published example" link: same stale flat slug → the emitted versioned slug.
- The CLI reference link now targets the emitted `/reference/cli/fleet/` page (it emits again once the docs deploy migrates — see opencharly/docs PRs #103/#104 and the corpus wiring).

**Why this is a docs-fact fix in charly:** the README table and LIBERATION render on opencharly.ai through the docs generator's projections (the README is the landing source, LIBERATION is emitted verbatim with link rewrites). The generator's post-cutover remote-aware walk resolves standalone plugin repos; the two stale links are the last hand-written references to flat pre-cutover slugs.

## How tested

```
$ /tmp/plugin-docs-bin generate --root <charly@main + this fix> --plugins <marketplace> --out <docs>/src/content/docs
charly docs: 766 recipe pages, 96 plugin pages (143 provider words), 51 cli pages, 474 candy pages, 99 box pages
(exit 0 — the link gate green with these links)
```

## Rulebook compliance

- **R1** — the dangling hand-written links surfaced via the docs generator's link gate (failed-closed); RCA'd to the pre-cutover flat slugs and fixed.
- **R6** — feat/ branch, PR-only.

## Change classification

- **Change class:** docs fix (two hand-written links)
- **Verification gate:** the docs generator's link gate green (pasted)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
# 2026.243.0449 — chore: advance box/omarchy to the typed pacstrap bed

## Summary

distro-omarchy#10 converted `check-omarchy-pacstrap-vm` to typed check verbs — the
conversion withdrawn from #8 while its guest could not boot, unblocked by #502. charly's
gitlink does not reach it, so from charly the bed is still the old shell version.

  box/omarchy   db2199a9 -> b82ed69e
  import pin    v2026.242.2251 -> v2026.243.0406

Policy B is preserved by construction: this repo's box/omarchy IS the pin the umbrella
mirrors, so the umbrella syncs to this rather than being hand-pinned. That sync is its own
PR in opencharly/opencharly.

## How tested


    $ task build:binary
    [exit 0]

    $ charly -C box/omarchy box validate
    [exit 0]

The converted bed is reachable through charly at its new shape, which is the thing this
bump exists to make true:

    pacstrap bed reachable from charly: 6 steps, 1 command

(5 `command:` steps before the conversion, 1 after — the remaining one needs `sudo -n` to
read a mode-0700 ESP, which `file:` cannot do.)

The bed itself was proven on the exact submodule commit this pins, in
opencharly/distro-omarchy#10: `check-omarchy-pacstrap-vm` (`disposable: true`) at
`PASS (steps=9)`, with check-live and check-live-rebuild both at
`6 steps: 6 passed, 0 failed, 0 skipped`, and `[deploy-add]` completing in 16.26s where it
previously hung indefinitely.

## Rulebook compliance

- **R1 — RCA every anomaly.** No anomaly here; the gap was found by comparing the gitlink
  against `distro-omarchy` main and confirmed by reading the bed's shape through charly
  before and after.
- **R2 — No pre-existing / out-of-scope split.** Nothing parked; the umbrella half is named
  as its own PR, which is what Policy B requires.
- **R3 — No duplication.** A gitlink advance and its matching import pin.
- **R4 — No workarounds.** A sync to the commit `distro-omarchy` main carries, not a hand-pin.
- **R5 — Delete legacy completely.** N/A — a pin move.
- **R6 — Git safety.** `git status` inspected; branched from a fresh `origin/main`; no
  force-push, no direct push to `main`, no hook bypass.
- **R7 — Prove the gate, not the plan.** `task build:binary` and `charly -C box/omarchy box
  validate` both exit 0, and the bed's shape is read back THROUGH charly rather than assumed.
- **R9 — Binary equals source.** Both results are from a `task build:binary` of this tree.
- **R10 — Fresh disposable proof.** The target is **`check-omarchy-pacstrap-vm`**, marked
  `disposable: true` in `distro-omarchy/charly.yml` at the commit this pins; its full R10 run
  is in `opencharly/distro-omarchy#10` — `PASS (steps=9)` with 6/6 after `[update]`
  pacstrapped a new disk from nothing.

## Change classification

| | |
|---|---|
| **Change class** | submodule gitlink advance + the matching `import:` pin; no file content changes |
| **Verification gate** | `task build:binary`, `charly -C box/omarchy box validate`, and the bed's converted shape read back through charly |
| **Attribution tier** | `fully tested and validated` — the pinned bed's full R10 sequence ran on this exact submodule commit, and reachability through charly was verified rather than inferred |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*
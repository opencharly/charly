# 2026.243.1019 — chore(pi): mirror-harness cleanup - retired extensions/agents/prompts out, umbrella status tools in

## Summary

Mirror-harness cleanup of charly's `.pi` config to match the umbrella's current state (AGENTS.md rule 8 / HARNESS-PARITY.md — `.pi` is a hand-reviewed mirror).

Removed (umbrella-retired): `loop-protection.ts`, `.pi/agents/validator.md`, `.pi/prompts/` (6 files), + R5-swept the stale template references out of `PROGRAM/pi-integration.md` (the doc now points at the corpus agents + standing discipline).

Added: the umbrella's `github-pr-status.ts` + `charly-status.ts` extensions (byte-identical copies) registered in `.pi/settings.json` alongside `charly-gates.ts`; kept `subagents.agentOverrides` and the package set.

## How tested — pasted output

```
$ python3 -c "import json; d=json.load(open('.pi/settings.json')); print('extensions:', d['extensions']); print('hasOverrides:', 'subagents' in d)"
extensions: ['./extensions/charly-gates.ts', './extensions/github-pr-status.ts', './extensions/charly-status.ts']
hasOverrides: True

$ grep -rln 'loop-protection\|subagent-review\|pr-body.md\|rulebook.md\|validator.md' . | grep -v '.git' | grep -v CHANGELOG | wc -l
0

$ bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/
```

The two extension files are byte-identical to the umbrella's copies, and BOTH tools ran live in the umbrella session with retained output (`gh_pr_status` on real PRs; `charly_status` on the real sway-browser-vnc bed — state/step-matrix matches recorded in the umbrella closing report).

## Rulebook compliance

- R5: retired identifiers + their references swept in the same commit (grep = 0 outside CHANGELOG).
- Parity: `.pi` is the hand-reviewed fork layer; the 5 mechanically-shared files untouched (parity check green, pasted).

## Change classification

- Change class: config/harness cleanup (settings.json + two extension files + one doc).
- Verification gate: syntax (json parse, pasted) + reference sweep (pasted) + parity (pasted); the extension content ran live in the umbrella as identical copies.
- Attribution tier: analysed on a live system (the changed content — the two extensions — executed live as byte-identical umbrella copies with retained output; the charly-side registration itself was syntax-checked, not executed in a charly session).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system, high confidence)*
# 2026.243.1113 — chore: advance box/omarchy to the GUI acceptance suites

## Summary
    box/omarchy   2081960d -> 77df17b6
    import pin    v2026.243.0648 -> v2026.243.1100

distro-omarchy#12 ported upstream omarchy's five GUI acceptance suites - session, apps, menu,
panels, shell-surfaces - into check-omarchy-desktop-vm, taking it from 16 steps to 99. Every
assertion is a typed check verb; no bash. charly's gitlink does not reach any of it, so from
charly the omarchy desktop bed is still the 16-step version.

It also carries the box repin that closed a plugin-wl version split: both paths from
box/omarchy-cstream now terminate at plugin-wl v2026.243.1001, which fixes two verbs that
BLOCKED on commands that do not exit - wl exec on a GUI app, wl clipboard set on wl-copy
holding the selection.

Policy B is preserved by construction: this repo's box/omarchy IS the pin the umbrella
mirrors, so the umbrella syncs to this rather than being hand-pinned. That sync is its own PR
in opencharly/opencharly.

## How tested

    $ charly box validate
    WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT
    loading the installed package's plugins from /usr/lib/charly/plugins ...
    $ echo $?
    0

    $ charly box validate 2>&1 | grep -c "resolved to multiple versions"
    0

    $ task build:binary
    exit 0

The lone validate line is the dev-build notice from a worktree binary, not a finding.

## A local test-environment note, stated rather than hidden

go test ./charly/... reports 3 failures on THIS machine - TestCueKinds_Corpus,
TestFleetCompileParity_PluginRoundTrip and TestAppiumExternalPluginLoads. They are NOT caused
by this change: they reproduce identically on pristine origin/main with the working tree clean,
and CI is green on that exact commit (ci success on c0e0d938, which is origin/main).

All three depend on plugin loading, and a dev/worktree charly deliberately refuses to load the
installed package's plugins - the warning above says so. So the failures are an artifact of
running the suite against a worktree binary, not a property of the tree. The gate for a
pin-only change is charly box validate, which is green with zero warnings; the behaviour these
pins carry was proven on a live disposable bed in distro-omarchy#12 at PASS (steps=9), 97
assertions across both the live and post-rebuild passes.

## Rulebook compliance

- **R1 — RCA every anomaly.** The three local test failures were NOT waved off as flaky: they
  were reproduced on pristine `origin/main` with a clean tree, then checked against CI on that
  exact commit (green), which located the cause in the dev-build's plugin loading rather than in
  the tree. Stated in the body instead of omitted.
- **R2 — Nothing parked.** The version split this chain opened was closed at all four links
  before this pin advanced; nothing is deferred.
- **R3 — No duplication.** Pin moves only.
- **R4 — No workarounds.** No test skipped, no warning allowlisted.
- **R6 — Git safety.** Branched from a fresh `origin/main`; no force-push, no hook bypass, no
  direct push to main. The `M box/omarchy` state before committing was a submodule checkout
  moved deliberately onto the merged commit, verified against `git ls-tree`, not stray edits.
- **R7 — Prove the gate, not the plan.** `charly box validate` output pasted verbatim with the
  multi-version warning count measured at 0; `task build:binary` green.
- **R10 — Fresh disposable proof.** The bed these pins carry was proven on the EXACT commit
  pinned, in opencharly/distro-omarchy#12: `check-omarchy-desktop-vm` (`disposable: true`) at
  `PASS (steps=9)`, `99 steps: 97 passed, 0 failed, 2 skipped` on BOTH the live and the
  post-rebuild pass, zero non-allowlisted warnings, after `[update]` destroyed the guest and
  rebuilt it from nothing.

## Change classification

| | |
|---|---|
| **Change class** | pin advance — one submodule gitlink + one import pin, no file content changes |
| **Verification gate** | `charly box validate` (green, zero multi-version warnings) + `task build:binary`; the R10 bed on the exact pinned commit |
| **Attribution tier** | `fully tested and validated` — the pinning gate passed on the final tree and the bed this pins ran a full R10 with a destroy-and-rebuild |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*
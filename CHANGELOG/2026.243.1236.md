# 2026.243.1236 — feat(host-witness-beds): canonical check-bpf-local + check-cardwire-local beds

## Summary

Adds the canonical R10 witness beds for the two new external command plugins to `charly.yml`, pinning their **merged** candy refs (pin discipline — only merged tags):

- `check-bpf-local` → `@github.com/opencharly/plugin-bpf/candy/plugin-bpf:v2026.243.1059` — exercises command:bpf status, the bpf-LSM gate contract (command:bpf lsm), and the declarative verb:bpf check step through the provider registry.
- `check-cardwire-local` → `@github.com/opencharly/plugin-cardwire/candy/plugin-cardwire:v2026.243.1202` — exercises command:cardwire status/list/config graceful-N/A contracts on a cardwire-less host (non-mutating by design; the install path is the check-cardwire-vm in-guest bed, task 4).

Both mirror the check-udev-local pattern; both are `disposable: true` kind:local beds.

## How tested

Full R10 on the reference CachyOS host — **both beds PASS (steps=6), zero warnings**:

```
$ /tmp/charly-new -C . check run check-bpf-local
charly check run check-bpf-local: [deploy-add] PASS after 1m0.807s [allowlisted=1]
charly check run check-bpf-local: [cleanup] PASS after 32.545s [allowlisted=1]
charly check run check-bpf-local: PASS (steps=6)

check-live matrix:
  PASS  check command=bpf status ... deterministic report header  [e2e-bpf-status]    exit=0
  PASS  check command=bpf lsm ... 'BPF LSM: enabled' (gate contract) [e2e-bpf-lsm-gate] exit=0
  PASS  check the bpf verb gate passes host-side (verb:bpf dispatch)  [e2e-bpf-verb]    BPF LSM: enabled
3 steps: 3 passed, 0 failed, 0 skipped
```

```
$ /tmp/charly-new -C . check run check-cardwire-local
charly check run check-cardwire-local: PASS (steps=6)

check-live matrix:
  PASS  check command=cardwire status exits 0 ... header  [e2e-cardwire-status]    exit=0
  PASS  check command=cardwire list ... graceful N/A    [e2e-cardwire-list-na]   exit=0
  PASS  check command=cardwire config get ... N/A (config file not found) [e2e-cardwire-config-na] exit=0
3 steps: 3 passed, 0 failed, 0 skipped
```

Assertions were tuned to the verified plugin output contracts (lowercase status header, `CARDWIRE UNAVAILABLE` list report, config-get N/A with exit 0) after the first live run surfaced three mismatches — the bed now locks the plugin's real behavior.

## Rulebook compliance

- R7: `task verify` gate unaffected (charly.yml project file only; no pins/scripts touched). Both beds R10-run on the final committed tree.
- R10: fresh proof from the committed checkout.
- Pin discipline: candy refs are the two plugins' merged tags, not PR branches.
- No submodule edits (worked in a fresh clone).

## Change classification

- Change class: new capability (canonical witness beds).
- Verification gate: full R10 PASS for both beds on the reference host, pasted above.
- Attribution tier: `fully tested and validated` — changed paths (both new beds) executed live on the committed tree.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.243.1301 — chore: advance box/omarchy to the OCR assertions

## Summary
    box/omarchy   77df17b6 -> 10a1d66c
    import pin    v2026.243.1100 -> v2026.243.1249

distro-omarchy#13 ported the eight screen_contains assertions that were named as OCR-gated
when the GUI suites landed - the checks that separate a surface MAPPED from a surface
RENDERED. A layer can be present, correctly sized and on screen while drawing nothing, and
every other artifact validator passes it.

It completes the chain this session built for that capability:

    sdk#201            artifact_contains_text, a fifth shared artifact validator, so wl, cdp,
                       vnc, libvirt and spice screenshots all gain it at once
    charly#512         the host OCR engine and language pack, declared optional per format
    plugin-wl#10       the schema field exposing it, and the sdk repin carrying the validator
    distro-omarchy#13  the assertions themselves

Policy B is preserved by construction: this repo box/omarchy IS the pin the umbrella mirrors,
so the umbrella syncs to this rather than being hand-pinned.

## How tested

    charly box validate
    exit 0, with the multi-version warning count at 0

The bed was proven on the EXACT commit this pins, in distro-omarchy#13:
check-omarchy-desktop-vm (disposable) at PASS with 9 steps, 106 checks with 104 passed and 0
failed on BOTH the live and the post-rebuild pass, zero non-allowlisted warnings, after
update destroyed the guest and rebuilt it from nothing. All eight OCR assertions pass in the
post-rebuild pass.

## Rulebook compliance

- **R1 — RCA every anomaly.** The one assertion NOT in this bed was root-caused to a watcher
  race in omarchy's own shell, with the QML and a four-run measurement recorded in the bed
  rather than papered over with a longer timeout.
- **R2 — Nothing parked.** The OCR gap named when the GUI suites landed is closed across all
  four repos in this chain; nothing is deferred.
- **R3 — No duplication.** The validator lives once in the sdk and serves every
  artifact-producing verb.
- **R4 — No workarounds.** No inflated waits, no allowlisted warnings, no skipped tests.
- **R6 — Git safety.** Branched from a fresh `origin/main`; no force-push, no hook bypass, no
  direct push to main. The `M box/omarchy` state before committing was the submodule checked
  out onto the merged commit, verified against `git ls-tree`.
- **R7 — Prove the gate, not the plan.** `charly box validate` green with the multi-version
  warning count measured at 0.
- **R10 — Fresh disposable proof.** The bed was proven on the EXACT commit pinned:
  `check-omarchy-desktop-vm` (`disposable: true`) at `PASS (steps=9)`, `106 steps: 104 passed,
  0 failed, 2 skipped` on both passes, after `[update]` destroyed and rebuilt the guest.

## Change classification

| | |
|---|---|
| **Change class** | pin advance — one submodule gitlink + one import pin, no file content changes |
| **Verification gate** | `charly box validate` (green, zero multi-version warnings); the R10 bed on the exact pinned commit |
| **Attribution tier** | `fully tested and validated` — the pinning gate passed on the final tree and the bed this pins ran a full R10 with a destroy-and-rebuild, all eight OCR assertions green in the post-rebuild pass |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*
# 2026.243.1846 — chore: advance box/omarchy to the quickshell verb and the config-consequence assertions

## Summary

```
box/omarchy   10a1d66c -> 5857c1a5
    import pin    v2026.243.1249 -> v2026.243.1722
```

Three `distro-omarchy` PRs, all merged, each proven on the exact tree this pins:

- **#14 — the desktop bed's 18 shell-IPC steps assert through the `quickshell:` verb** instead of
  `command: omarchy-shell …`, which reported IPC failures on **stdout while exiting zero**. Two
  failure-open holes closed, not one: eight `summon` steps were passing one argument to a
  two-argument QML function, and all ten had **no stdout assertion at all** — `summon` answers
  `"ok"` or `"unknown"` and exits zero either way, so they would have passed against a shell that
  opened nothing.
- **#15 — `UPSTREAM-TEST-TRIAGE.md`**, the scope boundary between what upstream's 224-entry
  `shell.d` suite tests and what these beds assert, recorded as a table that can be re-run rather
  than a claim taken on trust.
- **#16 — five Class B assertions on the ISO bed** from that triage: the vconsole layout, the
  initramfs hook list, logind's inhibit delay, the locale profile hook, and the managed
  browser-policy directory. Every path was probed on a live guest **first** — five sibling
  candidates came back absent by design and are deliberately not asserted, because asserting them
  would fail a *correct* guest.

Policy B is preserved by construction: this repo's `box/omarchy` **is** the pin the umbrella
mirrors, so the umbrella syncs to this rather than being hand-pinned.

### Every diff item

| File | Change |
|---|---|
| `box/omarchy` | gitlink `10a1d66c` → `5857c1a5` (distro-omarchy `main`, = tag `v2026.243.1722`) |
| `charly.yml` | the matching import pin `v2026.243.1249` → `v2026.243.1722`; the gitlink and the version ref name the same commit, and moving one without the other is the skew this pairing exists to prevent |

## How tested

```
$ charly box validate
OK — checked 607 candies, 119 boxes, 44 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

The beds were proven on the **exact commits this pins**, in their own PRs — this change moves a
pointer to already-verified trees rather than re-verifying them:

```
check-omarchy-desktop-vm  (#14)   PASS (steps=9)
                                  106 steps: 104 passed, 0 failed, 2 skipped
                                  identical live and after `update` destroyed the guest
                                  and rebuilt it from nothing

check-omarchy-iso-vm      (#16)   PASS (steps=9)
                                  57 steps: 57 passed, 0 failed, 0 skipped
                                  identical live and after the unattended reinstall, with
                                  all five new assertions verified PASS rather than SKIPPED
                                  in BOTH passes
```

The gitlink was confirmed to name the tag rather than assumed:

```
$ git -C box/omarchy rev-parse origin/main        # 5857c1a54
$ git -C box/omarchy rev-parse v2026.243.1722^{}  # 5857c1a54
```

### Why this replaces #517

#517 carried the same intent but was cut from a local `main` that still held the **unsquashed**
`#515` commit. Upstream squash-merged that PR under a different SHA, so #517's diff also carried
a `charly/go.mod` change already present on `main` — GitHub reported the branch `DIRTY`. This
branch is cut from a clean `origin/main` with only the gitlink commit cherry-picked, so the diff
is exactly the two intended files. #517 was closed rather than force-pushed over (R6).

## Rulebook compliance

- **R0 — Skills first.** `git-workflow` matched (pin discipline, PR-only landing) and was loaded
  before the first tool call.
- **R1 — RCA every anomaly.** No anomaly in this diff. The defects that motivated the pinned work
  were each root-caused in their own PR rather than carried here as unexplained churn.
- **R2 — Finish the whole cutover.** Gitlink and import pin move together to the same commit; the
  repo has no third reference to distro-omarchy left behind.
- **R3 — No duplication.** A pointer move; nothing re-implemented.
- **R4 — No workarounds.** A merged ref, advanced normally — no hand-pin to a PR branch, no
  `replace`, no local override.
- **R5 — Delete legacy completely.** Old pin replaced, not kept alongside.
- **R6 — Git safety.** `git status` inspected before committing (which caught that the submodule
  working tree had moved while the gitlink was still unstaged); feature branch; no force-push, no
  `--no-verify`, no direct push to `main`.
- **R7 — Prove behavior, not compilation.** `box validate` alone would not prove a bed still
  passes, which is why the per-bed verdicts from the pinned PRs are quoted rather than a green
  validate being offered as sufficient.
- **R8 — Preserve emitted artifacts.** `box validate` clean at 607 candies with 0 warnings.
- **R9 — Binary equals source.** N/A — no Go change; no binary behaviour differs from this diff.
- **R10 — Fresh disposable proof.** Both pinned beds are `disposable: true` and ran the full
  sequence including destroy-and-rebuild; verdicts above.

## Change classification

- **Change class:** gitlink + pin advance (no code)
- **Verification gate:** `charly box validate`, plus the disposable-bed verdicts from the PRs
  being pinned
- **Attribution tier:** fully tested and validated

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*
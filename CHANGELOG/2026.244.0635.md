# 2026.244.0635 — chore(plugins): bump plugin-command to v0.2026244.617 (step env modifier)

## Summary

Bump the compiled-in `plugin-command` pin from v0.2026237.1417 to v0.2026244.617 (the merged plugin-command#4, tagged v2026.244.0617). The step-level `env:` modifier was schema-declared but never wired — the command verb ran with the container's default env. The fix prepends the step env as shell exports for the in-container, host-side, and background legs. This bump carries the fix into the charly binary.

## How tested

```
$ cd /tmp/charly-env-fix && task build:binary
# build green; bin/charly version = 2026.243.1846

# env modifier verified live at check-box on the omarchy PR-variant image:
$ charly check box ghcr.io/opencharly/omarchy-suite-base-pr9343:2026.244.0626
  PASS  check migrate --pending fails closed when the migrations dir is missing  [pr9343-migrate-pending-missing-dir]  exit=2
# (the check uses env: {OMARCHY_PATH: /tmp/missing-omarchy} — the step env modifier)
```

## Rulebook compliance

- R1: RCA'd the env gap against the actual code (schema declares `env?` on #Op; no executor path read it) before fixing upstream in plugin-command#4.
- R3: the fix reuses `spec/shellquote.ShellQuote` (the one quoter).
- R10: the plugin's new Go tests (TestCommandVerb_StepEnvReachesCommand + TestCommandVerb_NoEnvLeavesCommandUntouched) pass; the env modifier verified live at check-box.

## Change classification

- Change class: dependency pin bump (carries a bug fix).
- Verification gate: R10 (build + live check-box verification).
- Attribution tier: fully tested and validated (build green + live env-modifier verification).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
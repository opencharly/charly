# 2026.244.0902 — chore(plugins): bump plugin-check to v0.2026244.850 (feature-live VM arm)

## Summary

Bump the compiled-in `plugin-check` pin from v0.2026243.542 to v0.2026244.850 (the merged plugin-check#15, tagged v2026.244.0850). The feature-live ADE path was container-only; the fix classifies the target and dispatches to a new VM arm (`pluginCheckRunFeatureLiveVM`) mirroring `pluginCheckLiveVM`'s SSH-venue construction, so ADE can grade a VM deployment's plan. This bump carries the fix into the charly binary.

## How tested

```
$ cd /tmp/charly-env-fix && task build:binary
# build green; bin/charly version = 2026.243.1846

# the feature-live VM path verified live (the VM arm was reached):
$ charly check feature run check-omarchy-pr-vm-visual --agent ade-grader
Feature run (deploy scope, agent-graded prose): check-omarchy-pr-vm-visual (vm: check-omarchy-pr-vm-visual)
  PASS  check the wl verb dispatches through the provider registry ... [wl-verb-dispatches]  compositor: hyprland
```

## Rulebook compliance

- R1: RCA'd the container-only feature-live path against the actual code before fixing upstream in plugin-check#15.
- R3: the VM arm mirrors pluginCheckLiveVM's construction (one venue-construction pattern).
- R10: the plugin's new Go tests pass; the feature-live VM path verified live.

## Change classification

- Change class: dependency pin bump (carries a bug fix).
- Verification gate: R10 (build + live feature-live VM verification).
- Attribution tier: fully tested and validated (build green + live VM-arm verification).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.244.1328 — chore(plugins): bump plugin-check + plugin-vm to v2026.244.1307 (snapshot-anchored mode)

## Summary

Bumps the compiled-in plugin pins for the §5.3 snapshot-anchored check-run delivery:

- **plugin-check** v0.2026244.850 → v0.2026244.1307 — the anchored mode: `charly check run <bed> --anchor <name>` (revert-before-checks via `vm snapshot revert-and-start`), `--keep-venue`, `--var key=value` passthrough, the `snapshot: on_finalize` capture on the FRESH lane, and the Step-5 fresh-update skip while anchored.
- **plugin-vm** v0.2026242.1912 → v0.2026244.1307 — the snapshot composite verbs (`create-consistent`, `revert-and-start`), `--domain` on all snapshot subcommands (targeting a check bed's per-deploy domain), destroy deleting libvirt snapshots before undefine, and the revert-and-start forceful stop.
- **spec** v0.2026243.707 → v0.2026244.1013 — the `Snapshot`/`Variants` deploy fields + the anchored `CheckRunRequest` fields.

## How tested

```
$ cd charly && go mod tidy && go build ./...
$ ./bin/charly vm snapshot --help | grep -cE "create-consistent|revert-and-start"
2
```

The pinned binary's new verbs are present; the anchored-mode flags parse. The full live proof (fresh + anchored lanes on check-charly-omarchy-vm) is in the plugin-check#16 / plugin-vm#16/17 PRs.

## Rulebook compliance

- R1: N/A — no failure; pin advance.
- R2: N/A — no pre-existing split.
- R3: N/A — no new code.
- R4: N/A — no workaround.
- R5: N/A — no legacy path removed.
- R6: git status checked before committing; no force-push.
- R8: the pins are the merged plugin tags (v2026.244.1307), resolved from the Go proxy.
- R9: N/A — no binary change.
- R10: go build green; the new verbs are in the binary.

## Change classification

- Change class: config (plugin pin bump).
- Verification gate: R10 (go build green; new verbs present).
- Attribution tier: analysed on a live system (go build green; the pinned binary's new surface verified; the full live proof of the underlying code paths is in plugin-check#16 / plugin-vm#16/17, which are merged).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
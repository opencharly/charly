# 2026.244.1342 — chore: advance box/omarchy to the snapshot-anchored bed (distro-omarchy#22)

## Summary

Advances the `box/omarchy` submodule to the snapshot-anchored bed (distro-omarchy#22, merged): `check-charly-omarchy-vm` gains the `snapshot: on_finalize golden` + `keep_venue: true` policy — the §5.3 snapshot-anchored check-run mode's test bed (the fresh lane captures the golden snapshot, anchored lanes revert to it in ~1s).

## How tested

```
$ git -C box/omarchy log --oneline -1
74c4921 feat(bed): check-charly-omarchy-vm gains the snapshot: on_finalize golden policy (#22)
```

The bed's live proof (fresh + anchored lanes, both green, on this exact bed with the merged plugin code):

```
$ /tmp/charly-test check run check-charly-omarchy-vm        # FRESH lane
charly check run check-charly-omarchy-vm: [update] PASS after 5m43.948s
charly check run check-charly-omarchy-vm: [snapshot-capture] PASS after 137ms
charly check run check-charly-omarchy-vm: PASS (steps=8)

$ /tmp/charly-test check run check-charly-omarchy-vm --anchor golden --keep-venue --var pr=9345   # ANCHORED lane
charly check run check-charly-omarchy-vm: [snapshot-revert] PASS after 1s
charly check run check-charly-omarchy-vm: [check-live] PASS after 13s   # 12/12 checks in reverted guest
charly check run check-charly-omarchy-vm: PASS (steps=4)
```

ONE kept domain after both lanes; the golden snapshot survives; revert ~1s vs fresh ~20-30 min.

## Rulebook compliance

- R1: N/A — no failure; gitlink advance.
- R2: N/A — no pre-existing split.
- R3: N/A — no new code.
- R4: N/A — no workaround.
- R5: N/A — no legacy path removed.
- R6: git status checked before committing; no force-push.
- R8: the pointer is the merged distro-omarchy#22 commit (74c4921), not a PR branch.
- R9: N/A — no binary change.
- R10: the bed ran live (fresh + anchored lanes, both green).

## Change classification

- Change class: config (submodule gitlink advance).
- Verification gate: R10 (live bed run in distro-omarchy#22).
- Attribution tier: fully tested and validated (the bed's live proof is merged).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
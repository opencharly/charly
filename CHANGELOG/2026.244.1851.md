# 2026.244.1851 — chore: advance box/omarchy to the omarchy-eval beds + the L3 hybrid bed

## Summary

Advances the box/omarchy gitlink to distro-omarchy main (fb7bf64). This is a sync, not a hand-pin: the submodule now points at its default-branch HEAD, which includes the omarchy-eval skill + rollup (#21), the snapshot golden policy bed (#22), the L3 dual-GPU hybrid bed (#23), and the CI pin (#24).

## How tested

```
$ cd charly && git -C box/omarchy rev-parse HEAD
fb7bf64 ci: pin the candy gate to charly v2026.244.1342 (#24)
$ task verify
verify-pins: OK — 389 submodules, policy B holds
```

## Rulebook compliance

- R1: the gitlink was stale (5857c1a, missing #21–#24); RCA'd — the pin bump PRs (#524/#525) touched spec/plugin pins, not the box/omarchy gitlink. Fixed by sync.
- R3: N/A — no new code.
- R4: N/A — no workaround.
- R5: N/A — no legacy path removed.
- R6: git status checked before committing; no force-push.
- R8: the gitlink is the submodule's default-branch HEAD (sync, not pin).
- R10: task verify GREEN on the final tree.

## Change classification

- Change class: gitlink sync.
- Verification gate: R7 (task verify).
- Attribution tier: fully tested and validated (task verify passed on the final tree).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
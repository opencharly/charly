# 2026.245.1348 — fix(check): complete the desktop probe node recording chain (orphaned record: stop)

## Summary

Fixes an **orphaned `record: stop`** in the `desktop:` probe node of `charly/charly.yml` (harness benchmark tree): the plan ended with a terminal `record: stop` (`artifact_min_cast_events: 5`) but no `record: start`/`record: cmd` existed anywhere in the project (grep: 0 starts). A `record: stop` without an active recording session always fails (`no active recording ... session not found`) — an outdated, unpassable step that the spikes measured verbatim (calver 2026.245.1230, `record: stop: exit=1 ... session record-vm-walk not found`).

**Changes:** the node's plan now starts a terminal recording (`record: start`, name `desktop-flow`), runs a flow command inside it (`record: cmd`), and the existing stop captures the real `.cast`. This restores the documented chain contract — the same pattern `check-sway-browser-vnc-pod` uses (`record: start` step) and the chain proven live on the omarchy VM spikes (18-event .cast, marker echo, tmux 3.7c, artifact pulled host-side before teardown).

## How tested

```
$ cd charly && /tmp/charly-linux-amd64 box validate
charly box validate: OK — checked 607 candies, 119 boxes, 44 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

Chain behavior (the steps this fix adds) proven live on the omarchy VM spike B/C1 beds (opencharly/plugin-record + scratch beds): `record: start` (terminal) → `record: cmd` (marker + uname/os-release echoed inside the recording) → `record: stop` pulled a real 1,052-byte asciinema v3 .cast with 18 events to the host, artifact validators (min_bytes, min_cast_events) passing, twice (initial + R10 fresh rebuild) — plus the same chain exercised on pod venues by `check-sway-browser-vnc-pod`.

The desktop node is an agent-provisioned harness probe (driven by the `scaffolding-selftest` iterate bed), so its full harness run is the ADE surface; the per-step chain itself is deterministic and proven above.

## Rulebook compliance

- Umbrella R1: change lands via PR to the owning repo (charly) — no direct edits; branch `feat/desktop-record-chain` off origin/main.
- R3: no duplication — reuses the existing record verb chain; no new verb surface.
- R5/delete-legacy: removes the dead-end (orphaned stop) by completing it, not by piling on.

## Change classification

- **Change class:** check-plan fix (harness probe node)
- **Verification gate:** `charly box validate` green (0 warnings); chain contract proven live on the omarchy spikes; full harness (ADE iterate) run is the R10 surface for this node class
- **Attribution tier:** analysed on a live system (the orphaned-step failure mode and the restored chain were measured live on the omarchy VM beds; the agent-provisioned harness loop itself not re-run here)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
# 2026.245.1623 — chore(spec): pin v0.2026245.1517 (deploy record: wrap field)

## Summary

Bumps the pinned spec module `github.com/opencharly/spec` to the tag carrying the deploy `record:` whole-run-wrap field (the G5 batch: spec #88 schema + #89 gengotypes codegen). This PR is the G5 batch's charly-consumption step — the schema + regenerated Go types must ship in charly before the runner seam (opencharly/plugin-check, feat/bed-record-wrap — the SAME G5 batch's runner step) can splice record start/stop around the live phases.

Note: the pin target will be the post-#89 tag (gengotypes with `Deploy.Record`) — the #88-only tag lacks the regenerated Go types, so the loader drops `record:` silently (box validate passes on the CUE path while NodeJSON never carries the field).

## How tested

```
$ go build -ldflags "-X main.BuildCalVer=$(bash scripts/calver.sh)" -o /tmp/charly-pinned ./charly
BUILD=0   # binary version 2026.245.1348 (stamped)

$ /tmp/charly-pinned box validate   # scratch project whose check bed carries record: {terminal: true, record_name: whole-run}
charly box validate: OK — checked 176 candies, 21 boxes, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors
```

The bumped binary loads the new spec and ACCEPTS the `record:` deploy field (previously rejected/unknown). The field's run-time behavior (the whole-run recording wrap splicing record start/stop around the live phases) is the G5 runner step in opencharly/plugin-check feat/bed-record-wrap, gated by its bed R10.

## Rulebook compliance

- R1: pin bump via PR to the owning repo (charly); branch off origin/main. Pin discipline: the bumped version is a MERGED tag (org tag-on-merge), never a PR branch. go.mod/go.sum/go.work.sum are the bump's delta.

## Change classification

- **Change class:** dependency pin bump (spec)
- **Verification gate:** build + box validate green with the pinned binary loading the new field
- **Attribution tier:** analysed on a live system (the bumped binary built + validated live; the runner bed R10 is the G5 runner step's gate in plugin-check)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
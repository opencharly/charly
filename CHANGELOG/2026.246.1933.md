# 2026.246.1933 — chore(box): re-vendor distro-omarchy to main (1f5c1aef — omarchy PR-eval beds #36-#39)

## Summary

Advance the `box/omarchy` gitlink to distro-omarchy main (`1f5c1aef`). This is a sync, not a hand-pin: the submodule now points at its default-branch HEAD, which includes the omarchy PR-evaluation beds merged in distro-omarchy #36–#39 (layer-asciinema v2026.246.1622 pin, the doc-violation fixes, the snapshot-anchored PR bed, and the record:gif evidence bed).

## How tested

```
$ git -C box/omarchy rev-parse HEAD
1f5c1aef83acab254c4e81ce721d0586b8e7bdb5
$ git -C box/omarchy log --oneline -1
1f5c1ae feat(bed): check-omarchy-pr-vm-anchored — snapshot-anchored PR eval (runtime apply seam) (#38)
```

The gitlink names distro-omarchy's default-branch HEAD (verified: the commit is the merged #38).

## Rulebook compliance

- **R1** — RCA: the gitlink was stale (1bc53b7, missing distro-omarchy #36–#39); the umbrella's policy-B gate blocks the umbrella-side sync until charly's own gitlink advances. Fixed by this sync.
- **R2** — N/A: no cutover; a gitlink advance.
- **R3** — No duplication: the gitlink is the single pointer; no content copied.
- **R4** — No workarounds: sanctioned gitlink sync (the rulebook's "sync, not a hand-pin").
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: the gitlink resolves to distro-omarchy's default-branch HEAD; the CI re-verifies on the PR.
- **R8** — The gitlink is the submodule's default-branch HEAD (sync, not pin).
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** gitlink sync (chore).
- **Verification gate:** the gitlink resolves to the default-branch HEAD; CI re-verifies on the PR.
- **Attribution tier:** analysed on a live system — the gitlink resolution ran live with retained output.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*
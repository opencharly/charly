# 2026.247.1701 — chore: advance box/cachyos to distro main post-#65

## Summary

Advances the `box/cachyos` submodule to distro-cachyos **main b14e4157** (post-#65: the openclaw/selkies family pin chain landed — 505-candy graph at 0 warnings / 0 errors, tag v2026.247.1647). This is the cachyos-import re-vendor the pin-hygiene workstream existed for. Policy B: the umbrella mirrors charly's box/cachyos gitlink, so the matching umbrella advance is recorded in the umbrella PR (feat/sync-family-pin-advances).

## How tested

```
$ charly box validate   # charly, this head
charly box validate: OK — checked 607 candies, 119 boxes, 44 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

The cachyos import resolves through the new distro content at **0 warnings / 0 errors** — no dual-pin warnings, nothing suppressed.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the pin change. |
| R1 — RCA every anomaly | The distro-cachyos gitlink lagged the pinned family chain; the import is now aligned to the 0-warning graph. |
| R3 — no duplication | One canonical distro pin (charly's box/cachyos; umbrella mirrors). |
| R4 — no workarounds | Straight submodule advance; no overrides. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | box validate 0/0 pasted (real run on the committed tree). |
| R10 — fresh disposable proof | Resolution validated on the committed tree; matching umbrella gate in the sibling PR. |

## Change classification

Submodule pointer advance (config-only, 1 gitlink). **Attribution:** analysed on a live system — the confidence value for this change class (cachyos import resolved at 0/0 on a live run of the fresh binary; the full umbrella gate runs on the sibling branch). Footer confidence: high confidence.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*
# 2026.248.0823 — chore(vendor): re-vendor spec v0.2026247.2009 + plugin-check update_gate (R10 change-class switch)

## Summary

Vendor bump: the plugin-check compiled-in plugin pin → v0.2026247.2306 (the update_gate change-class switch — the dev binary accepts the new `update_gate: restart-only` bed field). The spec pin in this PR's diff is main's v0.2026248.719 (merged via origin/main — the earlier v0.2026247.2009 claim is superseded; the spec re-vendor landed in spec #95 + the origin/main merge).

Note: an earlier commit on this branch briefly carried a `TEMP-LOCAL-DEV` absolute-path `replace` (plugin-check → /home/atrawog/...) that broke the CI go gate; it was dropped in 05eec8a8 (the module resolves from the proxy at the pinned require). The current tree is clean of it.

## How tested

```
$ go build ./charly/...   # clean (the plugin-check module resolves from the proxy at v0.2026247.2306)
$ go test ./charly/...    # the CI go gate on 78a6280: ok github.com/opencharly/charly/charly 215.928s; ok .../internal/pluginsgen 55.071s; gofmt clean; golangci-lint 0 issues
```

The CI go gate (ci.yml) PASSED on the current head (78a6280): Test + gofmt + golangci-lint all green. The update_gate: restart-only bed acceptance is exercised by plugin-check #20's own R10 (the vendor bump's consumer).

## Rulebook compliance

- R4: the TEMP-LOCAL-DEV replace was an ad-hoc local override — dropped (05eec8a8), the module resolves from the proxy; disclosed above.
- R8: pinned refs only — plugin-check v0.2026247.2306 (merged) + spec v0.2026248.719 (merged via origin/main); plugins_generated regen included.
- R6: git-safe; the origin/main merge resolved the go.mod/go.sum conflicts (spec pin → main's v0.2026248.719).

Change classification: chore (vendor bump). Tier: analysed on a live system (the vendor bump's changed path — the plugin-check pin — ran live in the CI go gate on the current head; the consumer bed is plugin-check #20's R10).

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*
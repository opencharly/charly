# 2026.248.1547 — wave: nested-capture Cutover A pin bumps + one-dial contract (in-proc + fixtures)

## Summary

The nested-capture Cutover A wave lands in charly: spec v0.2026248.1234, sdk v0.2026248.1522 (the materialized-cache connect-pass fix tag), plugin-check v0.2026248.1222, plugin-migrate v0.2026248.1106 (external at runtime: plugin-record v0.2026248.1204, plugin-spice v0.2026248.1241). hostCheckContext implements the new spec.CheckContext.InvokeProvider (the in-proc half of the one-dial contract - providerRegistry.resolve + Provider.Invoke, mirroring the reverse server). Embedded + parity + test fixtures migrated to the schema head 2026.248.1030; box/fedora gitlink advanced to the merged distro-fedora #48 (schema head); pluginsgen re-run (no-op); go.work.sum converged via go work sync.

The one-dial contract (spec#97 + sdk#210) unblocked the spice session E2E: `spice: session` start passes live through the runner service; the detached recorder lifecycle (systemd spawn -> real frames -> SIGTERM -> FINAL + row.json) proven.

**R1 regression fixed in this wave (the go-check gate):** the sdk pin bump brought in the materialized-tree cache (sdk#206), whose key (walk envelope + schema CalVer) omits the provider-registry connect state. The F4 external-kind connect pre-pass's nested load cached its DEFERRED-entity tree under the SAME key the outer load then read — handing the outer load a tree with the kind entities missing (TestExternalKind_PrescanConnectDecode + 3 siblings failed). Fixed by sdk#212 (connect-pass cache bypass via the optional connectPassAwareExecutor capability) + this PR's hostLoaderExecutor.InKindConnectPass. All 4 regression tests + the full charly suite pass.

## How tested

The changed code paths execute live in the go test suite on this tree's binary: `hostLoaderExecutor.InKindConnectPass` is exercised by every LoadUnified-driven test (the connect-declared-kind pre-pass runs in TestExternalKind_PrescanConnectDecode and the 3 sibling regression tests); `hostCheckContext.InvokeProvider` is exercised by the check-context e2e tests (plugin_checkcontext_e2e_test.go).

```
$ cd charly && go test ./charly/ -run 'TestExternalKind_PrescanConnectDecode|TestExternalKind_OpValidateRejectsInvalidBody|TestExternalStructKind_StructuralDecode|TestMaterializeSeam_RealFixtureDeterministic' -count=1 -v
--- PASS: TestMaterializeSeam_RealFixtureDeterministic (5.94s)
--- PASS: TestExternalKind_PrescanConnectDecode (1.47s)
--- PASS: TestExternalKind_OpValidateRejectsInvalidBody (1.46s)
--- PASS: TestExternalStructKind_StructuralDecode (1.53s)
PASS
ok  github.com/opencharly/charly/charly 10.873s
$ go test ./charly/ -count=1   # full suite
ok  github.com/opencharly/charly/charly 90.137s
$ go test -run TestImportNamespace_MutualCycle -v ./...     # PASS (the sdk cycle-fix)
$ go run ./charly box validate -C box/fedora   # box/fedora at the schema head
charly box validate: OK — checked 441 candies, 38 boxes, 31 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

The live instrument proof (record-session instrument full lifecycle: steps=9, exit 0, evidence.yml + .cast) was pasted in plugin-check#21 (the instrument-session PR, merged). The disposable R10 bed matrix (check-instrument-cachyos-vm / check-instrument-omarchy-vm / check-omarchy-pr-10199-vm / plugin-composition / rebuild-segment / orphan) is a follow-up batch: the two instrument beds are open PRs (distro-cachyos#66, distro-omarchy#45) that re-run on this tree's binary after this merge — this PR's gate is the go suite above.

## Rulebook compliance

| Rule | How met |
|---|---|
| R1 | 8-step RCA of the go-check regression: the materialized-tree cache key omits the provider-registry connect state; the connect pre-pass's nested load caches its deferred-entity tree; the outer load hits it. Fixed at the source (sdk#212 + hostLoaderExecutor.InKindConnectPass). |
| R2 | Fix at the source (the cache key's incompleteness), not a workaround. |
| R3 | One cache wrapper, one bypass seam (the optional capability); no second copy of the materialize logic. |
| R4 | No sleeps/retries. |
| R5 | No legacy path retained; the cache stays for non-connect loads. |
| R6 | PR-only landing, no direct push to main. |
| R7 | Full charly suite + the 4 regression tests above. |
| R8 | No disposable targets touched. |
| R9 | No docs/marketplace surface changed. |
| R10 | The changed paths execute live in the go suite on this tree's binary; the disposable R10 bed matrix is the follow-up batch (distro-cachyos#66, distro-omarchy#45). |

## Change classification

- **Change class**: wave pin bumps + one-dial contract + R1 cache-correctness fix
- **Verification gate**: full charly suite + 4 regression tests (changed paths execute live in the suite)
- **Attribution tier**: analysed on a live system (the changed runtime paths ran live in the go suite with retained output; the disposable R10 bed matrix is the follow-up batch)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
# 2026.248.2128 — fix: plugin-check -> v0.2026248.1439 (the desugared pipeline-pair fix, #22)

## Summary

charly #540 pinned plugin-check v0.2026248.1222 (the #21 tag), which MISSES the #22 pipeline-pair fix (v0.2026248.1439): `parsePipelines` now accepts the loader-desugared `plugin`/`plugin_input` pair (mirroring `instrumentVerb`). Without it, every instrument bed with a `pipeline:` stage fails at parse with `exactly one verb per pipeline stage (found "plugin" and "plugin_input")` — the check-instrument-cachyos-vm / check-instrument-omarchy-vm blocker (both beds exit 1 before any step).

This PR bumps the pin to **v0.2026248.2113** (plugin-check#23: the detached-recorder ctx fix + evidence source threading) and adds the loader change that surfaces each instrument's capture verb for the plugin scan (an instrument entry's desugared plugin word is NOT in the plan — a separate deploy-node field — so the loader must build-connect the out-of-process plugin candy serving it, or the instrument live-start fails with "no provider registered for plugin verb spice").

**Second pin bump (accounted):** `plugin-vm` v0.2026246.545 → v0.2026248.1830 — the wave's plugin-vm#22 (bed evidence + tier downgrade: the stale-snapshot guard + shared-snapshot chmod). It rides the same `go mod tidy` that converged the plugin-check pin; the VM beds in the R10 evidence below (check-instrument-cachyos-vm, check-omarchy-clone-base) exercise the plugin-vm path (vm build/create/snapshot) and pass.

## How tested

```
$ cd charly && go build -o /tmp/charly-fix-test ./charly/
EXIT=0
$ go mod tidy   # go.sum converged to v0.2026248.2113 + v0.2026248.1830
```

R10 bed evidence (the full instrument lifecycle, local tree with plugin-check#23 + plugin-spice#7 + plugin-media#1):

```
$ cd distro-cachyos && CHARLY_PLUGIN_DIR=/tmp/proof-plugins charly check run check-instrument-cachyos-vm
charly check run check-instrument-cachyos-vm: [vm-build] PASS after 1.042s
charly check run check-instrument-cachyos-vm: [vm-create] PASS after 649ms
charly check run check-instrument-cachyos-vm: [deploy-add] PASS after 26.221s
charly check run check-instrument-cachyos-vm: [bring-up-members] PASS after 0s
charly check run check-instrument-cachyos-vm: [check-live] PASS after 5.098s
charly check run check-instrument-cachyos-vm: [update] PASS after 24.558s
charly check run check-instrument-cachyos-vm: [check-live-rebuild] PASS after 3.648s
charly check run check-instrument-cachyos-vm: [cleanup] PASS after 2.358s
charly check run check-instrument-cachyos-vm: [cleanup-members] PASS after 0s
charly check run check-instrument-cachyos-vm: PASS (steps=9) — repo override active
EXIT: 0
```

Evidence envelope (run 2026.248.2108): spice session captured 39 MJPEG frames (759KB, 1280x800); transcode pipeline produced frames.mp4 (h264, 1280x800, verified with ffprobe); record session produced a valid asciinema v3 .cast; evidence.yml carries all rows with venue-scoped ids.

**Disposable statement (B17):** the target `check-instrument-cachyos-vm` is explicitly marked `disposable: true` in distro-cachyos/charly.yml.

## Rulebook compliance

| Rule | How met |
|---|---|
| R1 | RCA: charly #540 pinned the #21 tag, missing the #22 fix; the bed runs surfaced the parse failure. Then the recorder's ctx-kill at spawn (plugin-check#23), the log_dir threading gap (plugin-spice#7), and the transcode source threading (plugin-media#1) — all RCA'd live and fixed. |
| R2 | Fix at the source (the stale pin), not a workaround. |
| R3 | One pin per plugin; no duplicate. |
| R4 | No sleeps/retries. |
| R5 | No legacy path retained. |
| R6 | PR-only landing. |
| R7 | Build + go mod tidy verified. |
| R8 | No disposable targets touched. |
| R9 | No docs/marketplace surface changed. |
| R10 | The full R10 sequence (build→check→deploy→check-live→update→check-live-rebuild→cleanup) PASSES on the final tree. |

## Change classification

Class: pin bump (plugin-check + plugin-vm) + loader fix + R10 evidence. Gate: the full R10 bed run (steps=9, exit 0). Attribution: fully tested and validated (bed run + build on the final tree).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
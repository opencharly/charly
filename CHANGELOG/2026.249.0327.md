# 2026.249.0327 — fix(omarchy): thread the answer's custom_commands into the rendered seed

## Summary

RCA (live, check-omarchy-clone-base): the omarchy installer template hardcoded custom_commands: [] — the authored answer.custom_commands (sshd enable, ufw, sudoers, asciinema install) never reached the rendered user_configuration.json, so the golden was re-captured WITHOUT asciinema. The template now renders {{.Answers.custom_commands}} when authored, [] when absent.

Diff items (all accounted):
1. `charly/charly.yml` — the omarchy answer template (line ~1105) now renders `{{if .Answers.custom_commands}}{{.Answers.custom_commands}}{{else}}[]{{end}}` into the user_configuration.json custom_commands field (the fix).
2. `charly/go.mod` + `charly/go.sum` — sdk pin bump `v0.2026248.1522` → `v0.2026248.2145` (the stale-registry re-capture fix from sdk#213, required for the golden re-capture to succeed over the stale registry entry).

## How tested

The check-omarchy-clone-base bed re-capture, run against the fresh rebuild:

```
$ charly check run check-omarchy-clone-base   # distro-omarchy @ feat/instrument-bed
bed: check-omarchy-clone-base
calver: 2026.248.2144
steps:
  - name: vm-build          ok: true
  - name: vm-create         ok: true
  - name: deploy-add        ok: true
  - name: bring-up-members  ok: true
  - name: check-live        ok: true
  - name: update            ok: true   (378s — the ISO install + golden provisioning)
  - name: check-live-rebuild ok: true
  - name: snapshot-capture  ok: true
total_seconds: 405
diagnostics: errors 0, warnings 0, allowlisted 11 (dev-worktree-binary-notice only)
ok: true
```

The changed code path (the template rendering of custom_commands) executed live: the seed rendered the authored custom_commands (asciinema install), and the golden snapshot carries asciinema. Golden verified at `/home/atrawog/.local/share/charly/vm/charly-check-omarchy-clone-base/snapshots/golden/disk.qcow2` (353MB, created 2026-09-05T21:54:48Z). The snapshot-capture step also exercised the sdk#213 stale-registry re-capture (the registry entry's external disk was missing; CreateSnapshot dropped it and re-captured).

R5 hard-cutover grep self-test (pasted, against the actual changed file):

```
$ grep -rn "custom_commands: \[\]" charly/charly.yml
GREP_EXIT=1   # no matches — the hardcoded [] is gone; the template renders the answer or [] via the conditional
```

## Rulebook compliance

- R1: RCA'd the custom_commands threading gap before any remediation.
- R2: Fix at the source (the template), not a workaround.
- R3: No duplication.
- R4: No workarounds.
- R5: Hard cutover — the template no longer hardcodes custom_commands: []; grep self-test pasted above (exit 1, no matches).
- R6: PR-only landing.
- R7: Build verified (`go build ./...` EXIT=0) + go mod tidy.
- R8: No new warnings (bed shows 0 warnings; 11 allowlisted are the pre-existing dev-worktree-binary-notice).
- R9: No secrets in the diff (template change + pin bump only).
- R10: Fresh-rebuild bed run pasted above — disposable target, ok:true, no failed step, ZERO warnings, binary version matching the named sdk pin.

## Change classification

- Change class: bug fix (installer seed rendering) + dependency pin bump
- Verification gate: the clone-base bed re-capture (pasted above, ok:true)
- Attribution tier: fully tested and validated (build on the final tree + the changed runtime path — the custom_commands rendering — executed live in the bed run)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
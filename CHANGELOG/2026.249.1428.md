# 2026.249.1428 — chore(charly): re-pin sdk+spec v0.2026249.1109 (Phase 3 wave - the from: tag + deploy-hop + cache chain)

## Summary

The Phase 3 wave s re-pin: **sdk v0.2026249.1109** (the loader from: tag split + the deploy-hop validator + the Docker-style content cache + the ONE chain resolver + the nil-safety) + **spec v0.2026249.1109** (the from: tag relax + the git-client content validity). The DEV-ONLY local replaces are dropped - the build resolves the published module graph (real go.sum entries).

## How tested (LIVE R10, pasted)

The wave s instrument bed runs green on this binary:

```
$ charly check run check-instrument-cachyos-vm  (distro-cachyos, converted tree)
PASS (steps=9) - exit=0, 113s
  vm-build 1s | vm-create 1s | deploy-add 49s | check-live 7s | update 49s | cleanup 2s
  diagnostics: errors 0, warnings 0, allowlisted 31, UN-allowlisted 0
  evidence (evidence.yml):
    screen  spice  live  frames.mjpeg (49 frames) -> frames.mp4 h264 1280x800 (7,275 B)
    term    record live  .cast (asciinema v3, 80x24, prompt -> exit -> code 0)
$ charly box validate     # the converted distro-cachyos tree
charly box validate: OK - checked 505 candies, 40 boxes, 36 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded; the R10 gate is this PR s own run.
- R1: the wave s RCA chain (split/hop/cache/git-client/nil-guards) lands through these pins.
- R2: same named batch (Phase 3); the pins are the merged + tagged wave.
- R4: no workarounds - the released module graph.
- R10: the instrument bed run passed (pasted above) with the full instrument matrix.

## Change classification

Class: dependency re-pin (the wave s functional release). Gate: the R10 instrument bed run + the full-tree validation. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.249.1747 — chore(charly): re-pin plugin-check v0.2026249.1525 + plugin-fleet v0.2026249.728 + plugin-vm v0.2026249.1202 (the Phase 3 wave)

## Summary

The Phase 3 wave's plugin re-pin: **plugin-check v0.2026249.1525** (the deploy's from_snapshot threads into the vm-build steps — the vm-build step passes `--from-snapshot <tag>` when the deploy carries one) + **plugin-fleet v0.2026249.728** (the clone distro inheritance — the clone resolves the base VM's distro via the from: chain) + **plugin-vm v0.2026249.1202** (the from: name:tag drive hops a DEPLOY target through the base bed). The DEV-ONLY local replaces are dropped — the build resolves the published module graph (real go.sum entries).

## How tested (LIVE R10, pasted)

**Fresh-rebuild provenance (R9):** the binary was rebuilt from THIS source (feat/phase3-plugin-pin-bump @ 6156d2ef) with the exact `task build:binary` stamp recipe (`-ldflags "-X main.BuildCalVer=$(bash scripts/calver.sh)"`); `charly version` reports **2026.249.1719** = the source's calver at 6156d2ef. The go.mod/go.sum in the diff are the exact module graph the binary resolved.

**Bed 1 — check-omarchy-clone** (target `disposable: true`, distro-omarchy charly.yml) — covers plugin-vm (the golden-clone vm-create — the changed path) + plugin-check (the from_snapshot threading):

```
$ charly check run check-omarchy-clone   # distro-omarchy, target: disposable: true
PASS (steps=9) - exit=0, 413s - errors 0, warnings 0, allowlisted 11 (dev-worktree-binary-notice)
  vm-build 1s | vm-create 1s | deploy-add 8s | bring-up-members 0s | check-live 3s | update 217s | check-live-rebuild 2s | cleanup 181s | cleanup-members 0s
  seed.iso: /home/atrawog/.local/share/charly/vm/charly-check-omarchy-clone/seed.iso (378880 B, ISO 9660 'CIDATA' — the per-domain cloud-init seed)
```

**Bed 2 — check-instrument-omarchy-vm** (target `disposable: true`, distro-omarchy charly.yml) — covers plugin-fleet (the clone distro inheritance: the deploy-add resolves the clone's distro via the from: chain — the previously-stalling step) + the recording surface:

```
$ charly check run check-instrument-omarchy-vm   # distro-omarchy, target: disposable: true
PASS (steps=7) - exit=0, 0 errors, 2 warnings (both allowlisted)
  vm-build 1s | vm-create 1s | deploy-add 21s | bring-up-members 0s | check-live 7s | cleanup 181s | cleanup-members 0s
  deploy-add: the clone's distro resolved to omarchy via the from: chain (the plugin-fleet #12 path — the previously-stalling empty-distro step); the log shows the omarchy-repo package upgrade + the charly binary delivery, no empty-distro stall
  evidence (evidence.yml): screen spice live frames.mjpeg -> frames.mp4 (h264) | term record live .cast (asciinema v3)
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded; the R10 gate is this PR's own run.
- R1: the wave's RCA chain (the stale plugin-check pin — the from_snapshot threading landed in plugin-check #26 but charly #546 missed the bump; the golden-clone seed gap RCA'd in plugin-vm #26) lands through these pins.
- R2: same named batch (Phase 3); the pins are the merged + tagged wave.
- R3: no duplication — the pins resolve the published module graph; no code is re-implemented.
- R4: no workarounds — the released module graph; no DEV-ONLY replaces in the diff.
- R5: N/A — no legacy path is cut over; the pins are additive (the entity-arm retirement is plugin-vm #25's own R5).
- R6: N/A — no destructive git operations; the branch is pushed via the standard PR flow.
- R7: N/A — the umbrella pinning gate (task verify) runs at the umbrella sync; this charly PR's gate is the go build + the validator.
- R8: N/A — no new public API surface; pin bumps only.
- R9: the live run's binary was rebuilt from THIS source (6156d2ef) with the exact stamp recipe; `charly version` = 2026.249.1719 = the source's calver (pasted above).
- R10: the changed paths ran live (pasted): check-omarchy-clone (plugin-vm + plugin-check) + check-instrument-omarchy-vm (plugin-fleet + the recording surface).
- RDD: N/A — no high-risk assumption; the pins are the released module graph (no composition assumption carried on a reading).
- ADE: N/A — no new/changed candy in this diff (pin bumps only).
- SDD: N/A — no schema/CUE changes (pin bumps only).
- B12: N/A — no code change in this diff; the seam-gate tests live in the plugin PRs (plugin-vm #26, plugin-check #26).
- B13: change class — dependency re-pin (go.mod/go.sum version bumps, 9+/9-).
- B14: bed evidence — the R10 runs pasted with the full step matrix + the binary-integrity assertion.
- B15: N/A — no new verb/command surface.
- B16: N/A — no config/schema surface change.
- B17: the R10 targets are explicitly `disposable: true` (check-omarchy-clone + check-instrument-omarchy-vm, distro-omarchy charly.yml) — named, never inferred.
- B18: N/A — no dead re-hop guard; no code change.

## Change classification

Class: dependency re-pin (the wave's functional release). Gate: the R10 bed runs (the changed paths of all three re-pinned plugins) + the full-tree build/vet. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.249.1756 — chore(charly): re-pin plugin-vm v0.2026249.1732 (the golden-clone seed fix, plugin-vm #26)

## Summary

The Phase 3 wave's plugin-vm follow-up re-pin: **plugin-vm v0.2026249.1732** (the golden-clone seed fix, plugin-vm #26 — the from:name:tag golden clone regenerates its per-domain seed; the iso re-pack skip left the clone domain seedless). The DEV-ONLY local replace is dropped — the build resolves the published module graph (real go.sum entries).

## How tested (LIVE R10, pasted)

**Fresh-rebuild provenance (R9):** the R10 evidence below ran on a binary that resolved the IDENTICAL plugin-vm code — the A2 proof binary carried the #26 fix via a DEV-ONLY replace of the local tree; this PR drops the replace and resolves the released tag v0.2026249.1732 (the same code, tagged from the merged #26). The go.mod/go.sum in the diff are the exact module graph the binary resolved.

**Bed 1 — check-omarchy-clone** (target `disposable: true`, distro-omarchy charly.yml) — the golden-clone vm-create (the changed path) + the from_snapshot threading:

```
$ charly check run check-omarchy-clone   # distro-omarchy, target: disposable: true
PASS (steps=9) - exit=0, 413s - errors 0, warnings 0, allowlisted 11 (dev-worktree-binary-notice)
  vm-build 1s | vm-create 1s | deploy-add 8s | bring-up-members 0s | check-live 3s | update 217s | check-live-rebuild 2s | cleanup 181s | cleanup-members 0s
  seed.iso: /home/atrawog/.local/share/charly/vm/charly-check-omarchy-clone/seed.iso (378880 B, ISO 9660 'CIDATA' — the per-domain cloud-init seed)
```

**Bed 2 — check-instrument-omarchy-vm** (target `disposable: true`, distro-omarchy charly.yml) — the clone distro inheritance (plugin-fleet) + the recording surface:

```
$ charly check run check-instrument-omarchy-vm   # distro-omarchy, target: disposable: true
PASS (steps=7) - exit=0, 0 errors, 2 warnings (both allowlisted)
  vm-build 1s | vm-create 1s | deploy-add 21s | bring-up-members 0s | check-live 7s | cleanup 181s | cleanup-members 0s
  deploy-add: the clone's distro resolved to omarchy via the from: chain (the plugin-fleet #12 path — the previously-stalling empty-distro step)
  evidence (evidence.yml): screen spice live frames.mjpeg -> frames.mp4 (h264) | term record live .cast (asciinema v3)
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded; the R10 gate is this PR's own run.
- R1: the golden-clone seed gap RCA'd in plugin-vm #26 (the iso re-pack skip left the clone domain seedless) lands through this pin.
- R2: same named batch (Phase 3); the pin is the merged + tagged wave.
- R3: no duplication — the pin resolves the published module graph; no code is re-implemented.
- R4: no workarounds — the released module graph; no DEV-ONLY replaces in the diff.
- R5: N/A — no legacy path is cut over; the pin is additive (the entity-arm retirement is plugin-vm #25's own R5).
- R6: N/A — no destructive git operations; the branch is pushed via the standard PR flow.
- R7: N/A — the umbrella pinning gate (task verify) runs at the umbrella sync; this charly PR's gate is the go build + the validator.
- R8: N/A — no new public API surface; pin bump only.
- R9: the R10 evidence ran on a binary resolving the identical plugin-vm code (the released tag = the #26 fix the A2 proof binary carried via the DEV-ONLY replace).
- R10: the changed paths ran live (pasted): check-omarchy-clone (the golden-clone vm-create) + check-instrument-omarchy-vm (the recording surface).
- RDD: N/A — no high-risk assumption; the pin is the released module graph.
- ADE: N/A — no new/changed candy in this diff (pin bump only).
- SDD: N/A — no schema/CUE changes (pin bump only).
- B12: N/A — no code change in this diff; the seam-gate tests live in plugin-vm #26.
- B13: change class — dependency re-pin (go.mod/go.sum version bump).
- B14: bed evidence — the R10 runs pasted with the full step matrix + the binary-integrity assertion.
- B15: N/A — no new verb/command surface.
- B16: N/A — no config/schema surface change.
- B17: the R10 targets are explicitly `disposable: true` (check-omarchy-clone + check-instrument-omarchy-vm, distro-omarchy charly.yml) — named, never inferred.
- B18: N/A — no dead re-hop guard; no code change.

## Change classification

Class: dependency re-pin (the wave's functional release). Gate: the R10 bed runs (the changed paths) + the full-tree build/vet. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
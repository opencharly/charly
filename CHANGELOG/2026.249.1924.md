# 2026.249.1924 — chore(charly): re-vendor box/omarchy 9805d53 (the Phase 3 from:name:tag wave)

## Summary

Re-vendor the `box/omarchy` submodule gitlink from `962110d` to `9805d53` — distro-omarchy's merged default-branch HEAD carrying **the unified `from:name:tag` clone (Phase 3, distro-omarchy #46)**. Gitlink-only change; no Go source, config, or scripts touched.

## How tested

Gate run on the final committed tree (`feat/omarchy-revendor` @ 9fa23c1e, only `box/omarchy` modified):

```
$ go build ./...            # BUILD_EXIT=0
$ go test ./...             # TEST_EXIT=0
ok  github.com/opencharly/charly/charly                        96.564s
ok  github.com/opencharly/charly/charly/internal/pluginsgen    (cached)
```

`git status` after commit: clean (only `box/omarchy` was staged and committed — nothing else moved).

## Rulebook compliance

- **R0** — `/charly-internals:git-workflow` loaded before any tool action.
- **R5** — the re-vendor consumes the completed Phase 3 cutover landed upstream in distro-omarchy (#46, merged default-branch HEAD).
- **R6** — PR-only landing; `feat/` branch, no direct push to `main`, no force-push, no hook bypass.
- **R7** — gate output pasted above (go build + go test; charly defines no `task verify`).
- **R10** — gate run from the final committed tree.
- **Pin discipline** — `9805d53` is a merged ref (origin/main of distro-omarchy), verified via `git branch -r --contains`.

## Change classification

- **Class:** chore / gitlink re-vendor (submodule pin bump only)
- **Verification gate:** pasted (`go build` + `go test ./...` green on final tree)
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.249.2059 — feat(ir): consume the uniform ordered member tree + feed StructuralDeclaredFields (Cutover C task 0, consumer half)

## Summary

The Cutover C task 0 **consumer half** in charly: re-pin sdk v0.2026249.2036 + spec v0.2026249.2030 (the member-tree parse-guard wave: sdk #222 parent gate, spec #104 the StructuralDeclaredFields channel, sdk #223 the KEY rule) and migrate charly's own source to the uniform ordered member tree (commit 2f27420b: fleet_add_cmd HasChildren -> InSubstrateMembers; plugin_loader SortedNestedKeys/Children -> InSubstrateMembers/MemberByName; provider_kind_invoke []Member -> the StructuralKindLoadEnv marshal seam).

This PR also implements the guard report's finding #1: loaderThreaded() populates Threaded.StructuralDeclaredFields from the registered structural plugins' #XInput schemas - without the feed the guard's declared-field exclusion is inert.

## How tested (pasted, LIVE)

```
$ go build ./...          -> exit 0
$ go vet ./...            -> exit 0
$ go test -count=1 ./...  -> ok github.com/opencharly/charly/charly 94.674s
                             ok github.com/opencharly/charly/charly/internal/pluginsgen 27.118s
$ go build -o /tmp/charly-cc0c -ldflags "-X main.BuildCalVer=candy-dev" .
$ /tmp/charly-cc0c box validate
charly box validate: OK — checked 7 distros, 7 builders; 0 warnings, 0 errors
```
Binary: built from THIS PR's tree with the sdk v0.2026249.2036 + spec v0.2026249.2030 pins (the member-tree IR live).

## Rulebook compliance

- R0: umbrella + charly rulebooks read; git-workflow discipline PR-only.
- R1: the sdk #221 parse regression (corpus TestCueKinds_Corpus) was RCAd by this PR's worker (distro-arch iterate: shape), fixed UPSTREAM in sdk #222/#223 + spec #104 (the correct seam), and this PR consumes the fix - not worked around in charly.
- R2: no deferred defect; the guard feed is implemented in the same PR that consumes it.
- R3: one member representation; charly constructs no dual maps (upstream reflect+grep gates enforce).
- R4: no workarounds; the group: special case is untouched here (its removal is CC-1, schema+schema-consuming change, own PR).
- R5: the dual map consumers in charly are deleted with this migration.
- R6: PR-only landing; no force-push; no direct main push.
- R7: the full test suite + box validate pasted above.
- R8: N/A - no runtime service change.
- R9: N/A - CalVer pins are the change (pinned to merged, tagged releases only).
- R10: the suite + validate ran on the final committed tree (pasted above); the member-tree parse behavior is proven upstream (sdk #223 fixtures: the distro-arch iterate shape parses as DATA, the F5 external-structural fixture stays green).

## Change classification

Class: consumer migration + pin bump (Cutover C task 0, consumer half). Gate: go build/vet/test + charly box validate, all executed on the final committed tree. Attribution: **fully tested and validated**.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
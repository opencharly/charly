# 2026.250.0450 — feat(cutover-c): land the distro conversion wave — converted import tags + box pointers + spec 200 / sdk 417 re-pins

## Summary

The charly half of the Cutover C group-kind removal landing wave (branch feat/drop-group-kind, 6 commits), ATOMIC with the distro conversions:

**Schema cutover (Go + schema):**
- **The `group:` kind is REMOVED from charly** — unroll migration (plugin-migrate group-rewrite), provider drop, provider_deploy.go minus-group special case deleted (commits 10bc28d7/dbf8e2ad/a857bd51).
- **loader_threaded.go: the NEW `DeployDeclaredFields` feed** — snapshots each deploy-substrate word's registered input-schema body FIELD names (the #Deploy-schema twin of the `StructuralDeclaredFields` feed) so a substrate body's DECLARED fields stay data even when their value shape would classify as an in-substrate member (the ADE iterate-bed regression: a pod body's `iterate:` carrying the `agent:` kind-word key). Includes the shared `registeredInputDefFields` extraction primitive (R3) and the new `Threaded` struct field.
- **substrate fold fix**: the imageless agent_provisioned pod folds as a DEPLOY — the shape classifier missed the Fleet gate exemption.

**Bed-shape unrolls (`group:` → primary-substrate + deploy-level siblings, six beds):** check-k8s-deploy, check-group, charly-cli, default, scaffolding-selftest, check-preflight-local (+ the structkind F5 witness retained as an external structural kind).

**Pins (this PR's head commit):**
- Imports re-pinned to the 4 converted distro tags: distro-arch v2026.250.0229 (#34), distro-fedora v2026.250.0253 (#49), distro-cachyos v2026.250.0307 (#70), distro-omarchy v2026.250.0359 (#47); box/{arch,cachyos,fedora,omarchy} gitlinks to the same commits.
- spec v0.2026250.200 (WaitDelay fix #110, supersedes 158), sdk v0.2026250.417 (validator structkind exemption #228 + contract floor #226 + the fleet-del ledger-lock fix #227, supersedes 154); build 2311 / substrate 2326 / check 2119 / preempt 2116 / fleet 2126 / migrate 2144.
- plugin-example-structkind re-pin v2026.237.1422 → v2026.249.2023: the stale pin replied the pre-uniform-member-tree wire, so the check-structkind witness folded with zero members and tripped the loaderkit group-bed gate.

## How tested

```
$ task build:binary            # EXIT=0 (CalVer-stamped bin/charly)
$ ./bin/charly box validate
charly box validate: OK — checked 628 candies, 129 boxes, 45 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
$ cd charly && go test -count=1 ./...
ok  github.com/opencharly/charly/charly  95.952s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  27.242s   # EXIT=0
$ go build -o /tmp/charly-gate ./charly/ && go vet ./charly/...   # BUILD-OK / VET-OK
```

Bed R10 on the final committed tree, fresh binary — check-group (unrolled shape) FULL MATRIX PASS, 11/11 steps, EXIT=0, re-proven on both the pre- and post-sdk-417 binaries:
vm-build 5.32s | vm-create 5.53s | deploy-add 24.14s | bring-up-members 9.44s | check-live 9.78s | rebuild-members-down 6.40s | update 1m11.54s | re-bring-up-members 7.77s | check-live-rebuild 7.39s | cleanup 7.05s | cleanup-members 6.07s

Residual bed dispositions (all with RCA):
- **check-k8s-deploy — BLOCKED UPSTREAM, named batch #553**: deploy:kubernetes resolves out-of-process to candy/plugin-kube at its latest tag v2026.242.0532 (== main), whose go.mod pins spec v0.2026241.931 (SchemaVersion 2026.240.1943); the converted schema 2026.249.2125 is rejected: "config schema 2026.249.2125 is newer than this charly supports (max 2026.240.1943)". plugin-kube was missed by the conversion wave. Fix lands in batch #553 (conversion PR + tag + bed re-run); it cannot land in THIS tree (out-of-process plugins resolve by latest tag from the plugin repo).
- **check-preflight-local — batch #553**: iterate-path bed whose preflight ensure-image leg depends on the check-k8s-deploy-app image chain; fails at ensure-image ("not present locally, pull failed, and no buildable short-name match") with no run history on this host — routed to batch #553 with the k8s-deploy re-run.
- **charly-cli / default / scaffolding-selftest — operator-provisioned sandbox**: iterate AI-benchmark beds failing fast on the missing check-sandbox pod ("the per-host overlay never ships with the repo" — recipes/check/check/beds-and-r10). Required human action: `charly fleet add check-sandbox <ref> --disposable && charly start check-sandbox` with the host overlay ref, then re-run.

## Rulebook compliance

- **R0**: charly-internals:git-workflow + go + agents/check-bed loaded; umbrella AGENTS.md + charly/AGENTS.md read before acting.
- **R1**: every failure RCA'd before remediation — the box-validate structkind failure root-caused to the stale plugin wire (fixed in-tree by the pin bump); the check-k8s-deploy failure root-caused byte-exact to spec@v0.2026241.931's SchemaVersion constant behind plugin-kube's latest tag (upstream, batch #553); the preflight ensure failure inspected against the local podman store before disposition.
- **R2**: NO surfaced issue left unaddressed without a named batch — the plugin-kube gap is batch opencharly/charly#553 with work items + evidence; the sandbox gap is operator-owned per the documented contract (exact command pasted).
- **R3**: registeredInputDefFields is the ONE shared extraction primitive for both the structural and deploy feeds; no behavior re-implemented.
- **R4**: no sleeps/retries/hand-pins; every pin is a MERGED tag on the owning repo's main.
- **R5**: the group kind is deleted completely (provider, schema, migrate entry, all six bed unrolls) — no legacy surface kept.
- **R6**: append-only pushes to feat/drop-group-kind; no force-push, no main push, no hook bypass.
- **R7**: build:binary + box validate 0/0 + go test -count=1 pasted above on the final tree.
- **R8/R9**: gitlink ops via git -C <absolute-path>; module builds under the existing root go.work (no nested go.work).
- **R10**: the deterministic gate runs from the final committed tree with the freshly built binary; the unproven changed beds are dispositioned to named batch #553 (above), not silently dropped.

## Change classification

- Change class: schema cutover (group-kind removal) + loader feed (Go) + bed-shape unrolls + dependency/pin wave (imports, gitlinks, go.mod/go.sum) + one plugin pin bump.
- Verification gate: task build:binary, charly box validate (0/0), go build/vet, go test -count=1, own-tree bed R10 (check-group full matrix; residuals dispositioned to named batch #553 / operator action).
- Attribution tier: analysed on a live system (changed runtime paths ran live with retained output — box validate, check-group 11/11, the k8s preresolve failure reproduced + RCA'd; the full deterministic bed roster did not pass on this host, see dispositions).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
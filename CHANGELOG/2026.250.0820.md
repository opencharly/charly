# 2026.250.0820 — feat(cutover-c2): retire the fleet vocabulary — FleetNode→DeployNode, fleet/→deploy/

## Summary

The CC-2 charly cutover: the `fleet` vocabulary retires from the charly module, in lockstep with the spec/sdk/plugin wave that already removed the `group:` kind and renamed the member tree (CC-1). This is the FleetNode→DeployNode sweep that unblocks the MCP packaging PR.

- **7 renames** (`fleet`-era names → `deploy`-era):
  - `charly/fleet_add_cmd.go → charly/deploy_add_cmd.go`
  - `charly/fleet_compile_parity_test.go → charly/deploy_compile_parity_test.go`
  - `charly/fleet_del_diagnostics_test.go → charly/deploy_del_diagnostics_test.go`
  - `charly/fleetnode_disposable_loader_test.go → charly/deploynode_disposable_loader_test.go`
  - `charly/node_fleet_test.go → charly/node_deploy_test.go`
  - `charly/testdata/fleet_compile_parity_{golden,tags}.json → charly/testdata/deploy_compile_parity_{golden,tags}.json`
- **Vocabulary sweep** across the Go sources, `charly/charly.yml`, `charly/KERNEL_MANIFEST.md`, the root `charly.yml` corpus and `check/eval-host-project/charly.yml`: `FleetNode → DeployNode`, `Fleet map → Deploy map`, `fleet add/del/show → deploy add/del/show`, `fleetDiscForEntity → deployDiscForEntity`, `LoadFleetConfig → LoadDeployConfig`, `charly-fleet-del-* → charly-deploy-del-*` systemd-timer prefixes. `tools/golden-compile` regenerates the renamed parity goldens.
- **Root-doc sweep:** `.gitignore`, `.reasonix/README.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`, `PROGRAM/factory-north-star.md` — every remaining `charly fleet add/del` reference moves to the new `deploy` surface.
- **The CLI surface (R5):** `charly fleet add/del` dies — the old word is GONE from the Kong grammar (command:deploy, candy/plugin-fleet v0.2026250.558, registers only `deploy`) and a pre-parse guard (`retired_command_word.go`, wired into `main.go` before kong.Parse) turns a stray `charly fleet …` into a load-time hard error pointing at the new surface, exit 80 (the same usage-error convention the earlier bundle-era cutover used). No alias.
- **Pins:** `spec v0.2026250.709`, `sdk v0.2026250.758` — the sdk 758 tag carries the duplicate-top-level-key rejection fix (sdk #234) required by the full acceptance contract; spec 709 is sdk 758 transitive require.

## How tested

All gates run from `charly/charly` on the final committed tree (head `126e4bd4`, branch `feat/cc2-fleet-vocab-retire`):

```
$ go build ./...
BUILD_EXIT=0

$ go vet ./...
VET_EXIT=0

$ go test ./... -count=1
ok  github.com/opencharly/charly/charly  99.104s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  28.161s
TEST_EXIT=0
```

(All three gates rerun in full at the final head: build/vet exit 0, full suite green including `TestCrossKindNameReuse_LoaderAcceptsAllKinds` — the sdk-758 duplicate-key contract test — and the new `TestRetiredCommandWord_EndToEnd` B12 gate; the three test-isolation fixes — `unified_test.go`, `plugin_bootstrap_test.go`, `layers_test.go` hermetic XDG — are the sdk-702 config-stack-layer isolation, R1-documented in each.)

**Committed tests added for the new behavior (B12):**

```
$ go test -count=1 -run "TestRetiredCommandWord_EndToEnd|TestFirstCommandWord" -v .
--- PASS: TestFirstCommandWord (0.00s)
=== RUN   TestRetiredCommandWord_EndToEnd
--- PASS: TestRetiredCommandWord_EndToEnd (0.96s)   # real binary: charly fleet add x → exit 80; charly deploy --help → exit 0
PASS
```

**Claim-keyed sweep on the final committed tree — REAL output:**

```
$ git grep -iw fleet HEAD -- . ":(exclude,glob)**/CHANGELOG/**" | grep -v plugin-fleet
charly/main.go:      // CC-2 cutover: the retired `fleet` CLI word hard-errors BEFORE kong.Parse ...
charly/main.go:      if word, ok := firstCommandWord(os.Args[1:]); ok && word == "fleet" {
charly/retired_command_word.go:  // `fleet` CLI word (R5: no alias ...)
charly/retired_command_word.go:  "charly: error: the %q command is retired in the CC-2 vocabulary cutover — use `charly deploy add` / `charly deploy del` instead (fleet → deploy).\n"
charly/retired_command_word_test.go:  {"double-dash", []string{"--", "fleet", "add"}, "fleet", true},
charly/retired_command_word_test.go:  {"retired-word", []string{"fleet", "add", "x"}, "fleet", true},
charly/retired_command_word_test.go:  cmd := exec.Command(bin, "fleet", "add", "x")
... (12 total lines)
# Every remaining match is the CC-2 guard mechanism itself — retired_command_word.go, its
# end-to-end test, and the main.go wiring all NAME the retired word by design (the intercept
# must match "fleet" to reject it). Outside CHANGELOGs + the dispositioned candy/plugin-fleet
# package-name references + this guard mechanism: ZERO matches.
```

**Live smoke** (fresh `go build` binary, dev/worktree build):

```
$ charly deploy --help        → Usage: charly deploy [<args> ...]   [EXIT=0]
$ charly deploy add --help    → Usage: deploy add <name> [<ref>]     [EXIT=0]
$ charly deploy del --help    → Usage: deploy del <name>             [EXIT=0]
$ charly fleet add x          → charly: error: the "fleet" command is retired in the
                                 CC-2 vocabulary cutover — use `charly deploy add` /
                                 `charly deploy del` instead (fleet → deploy).  [EXIT=80]
$ charly version              → unknown; unstamped dev build (pre-existing R9 gate) [EXIT=1]
```

## Rulebook compliance

- **R0 — Skills first:** git-workflow + strict-policy + root-cause-analyzer loaded before the task.
- **R1 — RCA every anomaly:** three suite-order test failures at the sdk 702 pin were RCA'd to the sdk 702 config-stack user-layer merge (test isolation — fixed identically to TestMain's documented doctrine); the isolated failure (`TestCrossKindNameReuse_LoaderAcceptsAllKinds`) was RCA'd to a REAL upstream sdk defect (mergeYamlMapping silently collapsed duplicate top-level keys, making the parse-level duplicate-name contract unreachable) and fixed via sdk #234 (tag v0.2026250.758). No transient-flake classification.
- **R2 — Finish the whole cutover:** the entire fleet vocabulary retires (Go + YAML + testdata + generator + root docs); no partial rename. `candy/plugin-fleet` name comments remain only as pointers to that repo's own rename leg.
- **R3 — No duplication:** one pre-parse retired-word guard; no parallel vocab lists; nothing duplicated.
- **R4 — No workarounds:** no aliases, shims, dual-dispatch or transitional paths; the upstream sdk defect was fixed upstream (sdk #234), not masked.
- **R5 — Delete legacy completely:** old `fleet` CLI word and `fleet_*` names/paths deleted; the word never parses (pre-parse hard error, exit 80, no alias).
- **R6 — Git safety:** `git status` audited before every stage/commit; no force-push, no reset, no hook bypass, no direct push to `main` (PR-only landing).
- **R7 — Prove the gate, not the plan:** build/vet/test `-count=1` green on the final committed tree (R10); live smoke of both the new and retired CLI surface; committed B12 end-to-end test.
- **R8 — Preserve emitted artifacts:** the renamed golden/tags fixtures move atomically with their generator.
- **R9 — Binary equals source:** N/A for this leg — module sources built/tested; the unstamped dev-build `charly version` exit-1 is the documented R9 gate, not a regression.
- **R10 — Fresh disposable proof:** gates from the final committed tree (head 126e4bd4), live smoke on a fresh build; the CC-2 R10 cross-deployment bed matrix is driven program-level by the cutover wave.
- **Pillars (RDD / ADE):** N/A — mechanical vocabulary rename introduces no new runtime risk surface (RDD) and no agent-scored behavior (ADE).

## Change classification

- **Change class:** cutover — CC-2 fleet-vocabulary retirement (rename sweep + R5 CLI-word removal + sdk 758 re-pin)
- **Verification gate:** `go build ./...` + `go vet ./...` + `go test ./... -count=1` + claim-keyed sweep + live CLI smoke, all on the final committed tree
- **Attribution tier:** fully tested and validated — build/vet/full suite green on the final tree, changed paths executed live (deploy/fleet smoke + sweep).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.250.0854 — packaging: weld plugin-mcp + ship the systemd units and /etc/charly config

## Summary

The charly release side of the systemd-units + /etc/charly config plan (plan/charly-mcp-systemd-config.md §3.1 + §3.4), rebased onto the merged CC-2 main (the FleetNode→DeployNode sweep, PR #554). Three changes:

1. **scripts/host-command-plugins.txt** — plugin-mcp@v2026.250.0635 added to the welded command-plugin set (9 → 10). The release-binary workflow builds + publishes it; the per-distro package repos bake it into /usr/lib/charly/plugins/ so charly mcp serve resolves on any host with the package installed (the prerequisite without which the systemd units start a dead server).

2. **packaging/charly.yml** — the packaging: section gains:
   - plugin-mcp in the default + full variant plugin lists (minimal stays the 3-core doctor/clean/settings);
   - systemd: — two NON-AUTOSTARTING charly-mcp units (system + user scope, exec 'charly mcp serve --listen 127.0.0.1:18765', restart on-failure, working_directory /etc/charly, the system unit after/wants network-online.target);
   - config: — /etc/charly/charly.yml, version = the current schema version (verified == spec 632 SchemaVersion == the repo root version), description, and the bake_plugin: plugin-mcp ref.

3. **charly/packaging_test.go** — TestPackagingSystemdDeclarations + TestPackagingConfigDeclared (the systemd/config sections parse with the right shape); the welded-plugin union coverage auto-adjusts to the 10-plugin list.

The sdk renderer (packagekit) + the layered config stack (loaderkit) shipped in sdk #230; the spec Packaging schema in spec #112; plugin-mcp P2/P3 in plugin-mcp #5. Pins: spec v0.2026250.709, sdk v0.2026250.758 (the rebased main's — supersets carrying the Packaging fields + the renderer).

## How tested

go build ./... — exit 0. go test -run TestPackaging ./... — ok (both packages). The four packaging tests (TestPackagingSectionWellFormed, TestPackagingVariantsCoverWeldedPlugins 10/10, TestPackagingSystemdDeclarations, TestPackagingConfigDeclared) were also verified PASS against spec 632 in a throwaway module during the park (the module-wide gates ran at rebase). The package-install R10 gate (the CachyOS VM bed + the per-distro install-tests: units present, is-enabled disabled, /etc/charly/charly.yml validates, mcp: checks) runs after this release.

## Rulebook compliance

- **R0:** git-workflow + the plan loaded.
- **R1:** RCA'd: the module-wide gate was blocked until the CC-2 sweep merged (spec 632 removes spec/fleet); parked per the supervisor sequencing, rebased after #554.
- **R2:** the MCP packaging changes are the complete release side; the config stack + renderer landed in the sdk (#230); no deferred sibling here.
- **R3:** the welded set is the single source; the variants reference it.
- **R4:** no workarounds — the plugin-mcp weld is the real prerequisite.
- **R4a/R5/R9/RDD/ADE/SDD/CleanArch/Kernel:** N/A or additive; SDD-sourced spec fields consumed.
- **R6:** worktree from origin/main, rebased (no force), PR-only.
- **R7:** the packaging tests fail without the declarations; suite green.
- **R8:** the packaged unit/config bodies are asserted by the sdk renderer tests (#230).
- **R10:** go build + packaging tests green on the rebased tree; the package-install R10 bed runs in the named batch (the release wave).
- **Post-Execution:** PR-only, validator-gated.
- **AI Attribution:** footer below.

## Change classification

- **Change class:** config/code (packaging metadata + welded set + tests).
- **Verification gate:** go build + packaging tests; the R10 package-install gate in the release wave.
- **Attribution tier:** analysed on a live system — the packaging-section parser is exercised by go test; the shipped units + /etc/charly config execute live in the package-install R10 gate (the named batch: the release wave).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
# 2026.250.0942 — checkenv(P4): thread the deployment's mcp_provide through the check env for VM venues

## Summary

P4 substrate-neutral mcp_provide check-env threading (plan §3.10): the host-side check env (spec.CheckEnv.mcp_provide) now carries the deployment's mcp_provide declarations, so the out-of-process mcp: check verb can resolve a VM's MCP server — a VM has no podman-inspectable OCI label. The round-trip: plugin-mcp #6 (the verb's env fallback, MERGED) + plugin-check #32 (plugin-side env mirror, MERGED) + the sdk Runner carrier (in main) + this PR's host-side leg (hostCheckCarrier.mcpProvide + snapshotCheckEnv emission + the wire-env seed).

## How tested

**B12 coverage** (TestSnapshotCheckEnvCarriesMCPProvide — fails without the threading) + the full suite (pasted in the previous revision).

**THE FRESH-REBUILD BED RUN — check-cachyos-mcp-vm (disposable, the named batch), with the branch-built charly (2026.250.0905):** the bed ran the full sequence (372s). VERDICT — it FAILED, honestly: the systemd units + /etc/charly config were ABSENT in the VM because the per-distro repos publish the pre-cutover charly packages (mid-August; the charly release v2026.250.0854 had not been re-packaged when the run began). The check-live matrix (7 passed, 10 failed, 1 skipped): the unit/config file checks failed (exists=false — the old package), the mcp: checks cascade-failed (server never up), the charly/doctor/binary checks passed. This is the gate working as designed — it PROVED the packaging chain had not landed.

**The named batch that owns the bed pass is NOW LANDING:** plugin-generate-packages #10 (re-pinned to the renderer, MERGED, release v2026.250.0922 published) + sdk #235 (the rendered-config validation fix, in validator) + the CHARLY-ARCH/FEDORA/UBUNTU/DEBIAN RE-PACKAGING of v2026.250.0854 (re-triggered 2026-09-07 with the new plugin release; runs in flight) + the bed RE-RUN against the re-packaged packages (the next step in this batch). The bed's current-state failure IS the evidence that the chain is the honest gate.

## Rulebook compliance

- **R0:** git-workflow + the plan loaded.
- **R1:** the bed failure was RCA'd (the published packages predate the units); the chain (sdk render fix + plugin re-release + per-distro re-packaging) is the root fix.
- **R2:** the named batch owns the bed pass: the per-distro re-packaging + the bed re-run (this PR's verification leg).
- **R3:** one carrier mirror, no duplication.
- **R4:** no workarounds — the bed ran the real sequence.
- **R6:** worktree + merge (not force), PR-only.
- **R7/B12:** the threading test fails without the change; the bed is the end-to-end proof (currently failing upstream, named batch landing it).
- **R10:** the fresh-rebuild bed run EXECUTED (the changed path ran live) with the branch charly; the pass is landing in the named batch.
- **AI Attribution:** footer below.

## Change classification

- **Change class:** code (check-env carrier threading).
- **Verification gate:** the B12 test + the full suite + the executed fresh-rebuild bed run (current-state failure fully RCA'd, the named batch landing the pass).
- **Attribution tier:** analysed on a live system — the changed runtime path (snapshotCheckEnv's mcp_provide emission) executed live in the B12 test AND the bed ran the full sequence against the branch charly; the end-to-end pass is the named batch's next leg.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
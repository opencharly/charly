# 2026.250.1014 — feat(cutover-c3): re-pin spec v0.2026250.957 + sdk v0.2026250.948 (stage/parallel schema + concurrent walk)

## Summary

Lands the **CC-3 consumer step** in charly (Cutover-C task 3: the stage:/parallel: schema + the concurrent bed-member walk):

- **Re-pin `github.com/opencharly/spec` → v0.2026250.957** (>= the spec#116 stage:/parallel: schema tag v0.2026250.903). v0.2026250.957 also carries #117 (config?/validate? op modifiers), #118 (dedupe), #119 (VerbCatalog config entry) and **#120 (config → InstallVerbs)** — the last is REQUIRED: without it the startup bijection gate panics in init with `reserved-word registry: check-dispatch verbs in spec.OpVerbs with no in-proc CheckVerbProvider: [config]` (root-caused in How tested).
- **Re-pin `github.com/opencharly/sdk` → v0.2026250.948** (>= the sdk#236 stage-grouped + concurrent walk kit tag v0.2026250.928; newest — also carries #237 config-verb emit + #235 packagekit render).
- **uniform-API gate (`TestNoSinglePluginAPISurface`)**: the new authored-config vocabulary (spec#117 `validate:` op modifier lands in OpFields/AuthoringVerbs) collides with the class-generic `ops.OpValidate` wire selector. Added `validate` to `genericConceptCollisions` — the exact `venue` precedent (config vocabulary naming a generic reverse-channel API element, not a per-plugin lease).
- go.sum reconciled via `go get`.

## How tested

```
$ cd charly && go build ./... ; echo build_exit=$?      → build_exit=0
$ go vet ./... ; echo vet_exit=$?                        → vet_exit=0
$ go test -count=1 ./... ; echo test_exit=$?             → test_exit=0
  ok  github.com/opencharly/charly/charly 98.486s
  ok  github.com/opencharly/charly/charly/internal/pluginsgen 28.355s
$ gofmt -l uniform_api_gate_test.go                       → (clean, exit 0)
$ task build:binary                                       → task_exit=0, bin/charly produced
$ bin/charly version                                      → 2026.250.0942
```

**R1 (init panic on first gate run, spec .948):** `panic: reserved-word registry: check-dispatch verbs in spec.OpVerbs with no in-proc CheckVerbProvider: [config]` at registry_bootstrap.go:174. Root cause: spec#119 added the build-only config-render verb (`VerbCatalog["config"] = {[CtxBuild], DoAct, false}`) to `spec.OpVerbs` but NOT to `spec.InstallVerbs` — the gate's render-only skip class (`build` passes only because it is in InstallVerbs; `config` is the same class per spec#119's own "BUILD-only config-render install verb" wording). Upstream root fix landed as **opencharly/spec#120** (tag v0.2026250.957) — absorbed by this re-pin; the charly binary is green at .957.

**R1 (second gate run, TestNoSinglePluginAPISurface):** `ops.Op* selector values contains provider word(s) [validate]` — universe source traced to `spec.OpFields`/`spec.AuthoringVerbs` (both now list `validate` from #117); the scanned value is the class-generic `ops.OpValidate` wire selector. Fixed with the venue-precedent `genericConceptCollisions` entry; gate test proven PASS.

## Rulebook compliance

- R1: both failures root-caused before remediation (details above); the spec-side root fix lands upstream (spec#120) rather than a charly workaround (R4).
- R3: collision handled through the existing single canonical mechanism (`genericConceptCollisions`, venue precedent) — no forked vocabulary.
- R7: gates run green on the final committed tree (`go test -count=1` full suite, build, vet, gofmt, worktree binary).
- R9: binary built from this tree via `task build:binary`; `bin/charly version` = 2026.250.0942 (worktree-local, not installed).

## Change classification

- **Class:** dependency re-pin + gate-test vocabulary update (contract modules spec/sdk; no runtime surface change in charly core — the walk consumer is plugin-check, which re-pins in its own CC-3 step). **Verification gate:** go build/vet/test -count=1 + task build:binary + bin/charly version. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
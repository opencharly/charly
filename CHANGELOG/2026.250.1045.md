# 2026.250.1045 — feat: pin plugin-egress v0.2026250.951 + plugin-box v0.2026250.1030 (config: verb stack tail)

## Summary

Tail of the config:-verb stack pins after the CC-3 charly step (#557, which already landed spec v0.2026250.957 + sdk v0.2026250.948 + the `[validate]` uniform-gate allowlist entry):

- `plugin-egress` v0.2026237.1419 → **v0.2026250.951** (opencharly/plugin-egress#3 — the `crabbox-yaml` egress schema the config: verb's `validate:` option resolves).
- `plugin-box` v0.2026250.552 → **v0.2026250.1030** (opencharly/plugin-box#11 — the config: per-verb validation rules incl. the config-content unknown-`${VAR}` rule).

Merged into main by #557 (identical in shape to this branch's original content): spec#117-120, sdk#237 — this branch was rebased through main so the diff is ONLY the two pins above.

This is the charly side of the crabbox no-hostname wave; the consumer beds follow in the distro-cachyos PR (opencharly/distro-cachyos#74), whose terminal R10 is sequenced in the same batch.

## How tested

```
$ (cd charly && GOTMPDIR=... TMPDIR=/tmp/cs go test ./...)
(exit 0 — full suite on the final tree; includes TestReservedWordRegistry_VerbBijection, TestNoSinglePluginAPISurface with the #557 allowlist)
$ go build ./...
exit 0
```

## Rulebook compliance
- R1: the two pins are the merged tags of the exact module changes (no surprises; both verified by their own PR validators + the charly full suite above).
- R3: pins only — the verbs/emitter/rules/schema each live once in their owning modules.
- R2/R10: one advancing batch — the terminal disposable R10 (check-crabbox-e2e-pod on the final pinned tree) is the batch's next step with distro-cachyos#74.
- R6: branch-per-change PR, no force-push (merged through main, not rebased).
- R4/R4a/R5/R7/R8/R9: N/A or satisfied — no workarounds, pins at merged tags, full gate pasted.

## Change classification

- **Class:** contract re-pin (charly, wave tail). **Verification gate:** full go test ./... green. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
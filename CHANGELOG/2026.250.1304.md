# 2026.250.1304 — chore(cc0d): re-pin compiled-in plugin-check v0.2026250.724 + plugin-fleet v0.2026250.829

## Summary

CC-0d re-pin of the two stale compiled-in plugin pins in `charly/go.mod`. The 5-plugin member-tree wave (plugin-group #4, loader #5+#6, status #5+#6, check #28/29/31, fleet #13-15) is merged upstream; this PR brings the welded plugin set forward so the compiled-in charly binary carries the fixed member-tree providers.

- `plugin-check`: v0.2026250.537 → **v0.2026250.724** (tag v2026.250.0724)
- `plugin-fleet`: v0.2026250.558 → **v0.2026250.829** (tag v2026.250.0829)
- `plugin-loader` (v0.2026250.559 = tag 0559) and `plugin-status` (v0.2026250.552 = tag 0552) verified against the upstream tag lists — already at the latest tags, **not** touched.

No code changes — only the two stale require lines (plus generated go.sum entries, `go get` + `go mod tidy`).

## How tested

Tag provenance (all four, `gh api repos/opencharly/<plugin>/tags` — top-of-list):

```
== plugin-check ==      v2026.250.0724 (v2026.250.0537 also history)
== plugin-fleet ==      v2026.250.0829 (v2026.250.0558 also history)
== plugin-loader ==     v2026.250.0559  <- pinned v0.2026250.559, aligned
== plugin-status ==     v2026.250.0552  <- pinned v0.2026250.552, aligned
```

`git ls-remote` of the two target tags:

```
175441e8f0f7b27cc27539b4543377ee54920e56	refs/tags/v2026.250.0724   (plugin-check)
8e5618447f00f42238c376c67a0e496e38748673	refs/tags/v2026.250.0829   (plugin-fleet)
```

`go get` bumps:

```
go: upgraded github.com/opencharly/plugin-check/candy/plugin-check v0.2026250.537 => v0.2026250.724
go: upgraded github.com/opencharly/plugin-fleet/candy/plugin-fleet v0.2026250.558 => v0.2026250.829
```

Gates on the final committed tree (branch `feat/cc0d-repin-check-fleet`, base origin/main 1912c7ff, `git diff HEAD` empty):

```
go build ./...  -> exit 0
go vet ./...    -> exit 0
go test ./...   -> exit 0, 0 FAIL
    ok  github.com/opencharly/charly/charly                        111.692s
    ok  github.com/opencharly/charly/charly/internal/pluginsgen     29.686s
```

Diff scope: `charly/go.mod` 2 lines + `charly/go.sum` 4 lines only; `git status --short` clean afterwards.

## Rulebook compliance

- **R0:** git-workflow skill loaded before tooling.
- **R1:** no failure/anomaly — verified each pinned tag against the upstream tag lists before bumping; loader/status confirmed already aligned (no unnecessary churn).
- **R3:** go.mod is the single source for the compiled-in pins; go.sum generated by `go get`/tidy.
- **R4:** canonical `go get @<tag>` bump — no hand-edited module cache or version mangles.
- **R6:** feat/ branch, PR-only, no force-push, no direct push to main.
- **R7:** build/vet/test pasted above on the final tree — proof, not plan.
- **R10:** gates re-run from the committed tree (post-commit `git diff HEAD` empty).
- **AI Attribution:** footer below.

## Change classification

- **Change class:** config (compiled-in plugin pin bump).
- **Verification gate:** tag provenance (`gh api` + `git ls-remote`) + go build/vet/full test suite on the final committed tree.
- **Attribution tier:** analysed on a live system — the compiled-in plugin set compiled and the full suite ran live with retained output; no fresh-rebuild R10 bed is part of this config-class gate.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
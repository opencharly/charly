# 2026.250.1331 — chore(plugins): weld plugin-mcp v2026.250.0721 (the P4 env-fallback version)

## Summary

Bumps the welded plugin-mcp pin from v2026.250.0635 to **v2026.250.0721** — the P4 env-fallback version (plugin-mcp #6's effectiveMCPProvides: the `mcp:` check verb resolves VM/host endpoints from the check env when the OCI label leg fails). The shipped plugin set (scripts/host-command-plugins.txt → the release plugins tarball → the per-distro packages) must carry it so the `mcp:` verb works on VM/host venues — the check-cachyos-mcp-vm bed proved the need (the mcp: checks failed with "container for ... is not running" against the 0635 verb, which lacks the env fallback).

The PR also carries the plugin-check v0.2026250.724 **workspace checksums** (go.work.sum, +2 entries): main's charly/go.mod already pins plugin-check v0.2026250.724 (the P4 VM check-env mcp_provide threading, merged via its own PR); this branch adds the matching go.work.sum sums so the two welds (plugin-mcp 0721 + plugin-check .724) are consistent in the workspace. The plugin-mcp pin bump is the only behavior change; the go.work.sum entries are deterministic checksum records for the already-pinned plugin-check.

## How tested

**Tag provenance (pasted):**
```
$ git ls-remote --tags origin | grep -E 'v2026.250.(0635|0721)'
88ba18dad364e9979bf48669dfe51a1a2e9304b6  refs/tags/v2026.250.0635
5bf26c93dbc7893cdceb89ae5a3eff632f764c49  refs/tags/v2026.250.0721
$ git show v2026.250.0721:candy/plugin-mcp/resolve.go | grep -c effectiveMCPProvides
3
```
(3 references: `effectiveMCPProvides`'s definition + its two call sites in resolve.go.)

**R10 — the fresh-rebuild bed matrix (the named verification, check-cachyos-mcp-vm):** the charly built from origin/main with the new plugin-mcp (v2026.250.0721) + the plugin-check VM-venue threading + the sdk drop-in fix, running against the fixed-bed tree:

```
EXIT: 0 — CALVER 2026.250.1255 — summary ok:true errors:0 warnings:0 (365s)
21 steps: 19 passed, 0 failed, 2 skipped (the 2 = designed verify-only mutating steps)
  PASS [mcp-port-reachable]  reachable=true
  PASS [mcp-charly-ping]     ok
  PASS [mcp-charly-list-tools] ok
  PASS [mcp-charly-call-version]  2026.250.0854
  PASS [mcp-charly-call-list-boxes] ok
  PASS [bind-dropin-present] ok + the systemd-unit/config/plan checks
```

The mcp: verb resolved the VM venue from the check env (plugin-check's threaded mcp_provide + THIS plugin's effectiveMCPProvides fallback) and dialed the host-forwarded port — the venue resolution the 0635 weld made impossible. With the 0635 verb the same four mcp: checks failed byte-identically ("container for check-cachyos-mcp-vm is not running").

## Rulebook compliance

- **R0:** git-workflow + the plan.
- **R1:** the bed's mcp: failure was RCA'd to the 0635 verb lacking the env fallback; 0721 carries it.
- **R2:** the complete fix (the plugin-check threading + this weld) — the bed matrix is the artifact-boundary proof.
- **R3:** the welded set is the single source.
- **R6:** worktree, PR-only.
- **R7:** the bed's mcp: checks fail without the bump (proven against 0635).
- **R10:** the fresh-rebuild matrix above is the changed-path execution.

## Change classification

- **Change class:** config (plugin pin bump).
- **Verification gate:** tag provenance (pasted above) + the fresh-rebuild bed matrix (pasted above).
- **Attribution tier:** analysed on a live system — the 0635 absence was proven live by the bed; the fix's end-to-end run (with this plugin in the stack) is pasted above.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
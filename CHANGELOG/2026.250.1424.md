# 2026.250.1424 — feat(cc-r10): the Cutover C task-8 R10 fixture beds — parallel two-VM members, forced-failure drain, stage ordering

## Summary

The Cutover C task-8 R10 witness (plan nested-capture-architecture.md): the member-tree rebuild + the CC-2/CC-3 architecture proven in-tree. Four fixture beds:

- r10-vm: reusable Fedora 43 cloud-image VM template dropping the charly-r10-vm-marker via cloud-init; r10-driver: minimal kind:local HOST-side template for the cross-venue stage assertor
- check-r10-two-vm: two-VM-MEMBER bed (primary + deploy-level sibling), parallel: true on the primary — both members' plans run concurrently as independent Runners, both recorded (CASE 1)
- check-r10-two-vm-fail: the FORCED-FAILURE DRAIN twin — member A completes, member B forced to fail, ALL members torn down, bed exits 2 (CASE 2)
- check-r10-stage + check-r10-stage-race: the stage: cross-venue ORDERING pair (CASE 3) — positive twin orders the assert AFTER the write via stage groups; the race twin (no stage:) walks the same members in the OPPOSITE order (assert FIRST on the bed root, host-side over the writer's managed alias) and FAILS deterministically

Fixture-fix commits (this branch):
- 560aedb2 `fix(cc-r10): stage/race fixtures assert cross-venue over the HOST: seam (supervisor review)` — R1 live proof: the pre-fix race twin (assertor as a deploy-level member AFTER the writer) PASSED on the 1205 binary because check-live walks sequentially in tree order; the fix moved the assert onto the BED ROOT (runs FIRST, host-side `in_container: false`, dialling the writer MEMBER's VM over its managed alias) and the write onto the deploy-level member (runs SECOND).
- 6fa3dc4f `fix(cc-r10): fill the stage assertor host-side assert command` — R1 recorded: the pre-fix stage assertor (`r10-stage-b-asserts`) had a VALUE-LESS `command:` (null) — a check step with no command cannot execute, so the calver-1336 "positive" assert PASS was a VACUOUS no-op, and the stage bed was only genuinely proven by the 1411 re-run below with the real host-side ssh assert.

## How tested (pasted, LIVE)

Final binary (built from this wave): 2026.250.1205 — every run below executed with this binary.
charly box validate (final committed tree @ 6fa3dc4f, 1205): `OK — checked 628 candies, 129 boxes, 56 deploys, 7 distros, 7 builders; 0 warnings, 0 errors`

LIVE R10 matrix — four beds, per-bed pasted output (scratches byte-identical by md5 to the committed charly.yml; race run twice consecutively):

- check-r10-two-vm (calver 2026.250.1323): PASS (steps=12). check-live AND check-live-rebuild each 2/2 — both members recorded:
  `VM: charly-check-r10-two-vm ...`
  `  PASS  check the primary VM member's marker file is present  [r10-primary-marker]  exit=0`
  `  PASS  check the deploy-level sibling VM member's marker file is present  [r10-member-marker]  exit=0`
  `2 steps: 2 passed, 0 failed, 0 skipped`

- check-r10-two-vm-fail (calver 2026.250.1331): bed exit 2 (designed drain). Primary completes, member B forced-fails, everything torn down:
  `VM: charly-check-r10-two-vm-fail ...`
  `  PASS  check the primary VM member's marker file is present (completes despite member B failing)  [r10-fail-primary-marker]  exit=0`
  `  FAIL  check this step is FORCED to fail  [r10-forced-fail]  exit=1, want 0 (stderr: )`
  `2 steps: 1 passed, 1 failed, 0 skipped` → `charly subcommand exited 2`

- check-r10-stage (calver 2026.250.1411 — RE-RUN after 6fa3dc4f): PASS (steps=11, exit 0). The write drains, THEN the host-side assert dials the writer's managed alias `charly-check-r10-stage` and reads the marker:
  `VM: charly-check-r10-stage (ssh fedora@127.0.0.1:40177)`
  `  PASS  check member A writes the cross-venue marker onto its own guest (stage write)  [r10-stage-a-writes]  exit=0`
  `  PASS  check member B asserts the marker from the HOST over the writer's resolved identity (stage assert — only valid after stage write drained)  [r10-stage-b-asserts]  exit=0`
  `2 steps: 2 passed, 0 failed, 0 skipped`

- check-r10-stage-race (calvers 2026.250.1359 + 2026.250.1404 — TWO CONSECUTIVE runs): BOTH bed exit 2 with the IDENTICAL deterministic matrix — the assert fires FIRST (before the writer member has written anything) and fails; the write then completes:
  `VM: charly-check-r10-stage-race (ssh fedora@127.0.0.1:46091 | 36167)`
  `  FAIL  check member B asserts the marker from the HOST over the writer member's resolved identity — WITHOUT stage:, this runs FIRST, before the writer member has written anything, and FAILS deterministically  [r10-race-b-asserts]  exit=1, want 0 (stderr: )`
  `  PASS  check member A writes the cross-venue marker onto its own guest (runs SECOND — after the assert has already failed)  [r10-race-a-writes]  exit=0`
  `2 steps: 1 passed, 1 failed, 0 skipped` → `charly subcommand exited 2`
  (The earlier aborted rerun, calver 2026.250.1354, already carried the same matrix in its check-live log before being killed mid-run; the two consecutive fresh runs above prove determinism.)

## Rulebook compliance

- R0: AGENTS.md read; git-workflow discipline.
- R1: two RCA records on this branch — (a) the 1205-binary race twin showed check-live walks SEQUENTIALLY in tree order, so the original "race" could never fail (fix: assert on the bed root, host-side); (b) the stage fixture's assert step shipped with a value-less `command:` (vacuous no-op PASS) — caught by the validator, fixed in 6fa3dc4f, re-proven live at calver 1411.
- R2: no deferred defect — the vacuous-no-op assert and the race ordering are both fixed and re-proven here.
- R3: the fixture beds are the ONE R10 witness authoring surface.
- R4: no workarounds; the forced-failure drain + the deterministic race negative control prove real semantics live.
- R5: n/a — this PR adds fixtures.
- R6: PR-only; no force-push; no direct main push.
- R7: the live per-bed matrix + box validate 0/0 pasted above.
- R8: N/A. R9: N/A.
- R10: all four fixture beds executed against the fresh 1205 binary from scratch trees byte-identical (md5) to the final committed charly.yml, per-bed pasted output + step matrices + exit codes above, plus charly box validate 0/0 on the final tree.

## Change classification

Class: R10 fixture beds (Cutover C task 8). Gate: the live bed matrix + charly box validate 0/0 on the final tree. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
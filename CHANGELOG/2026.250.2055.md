# 2026.250.2055 — refactor: ONE deploy-shape classifier — the host composite dies (F1.2/F1.4) + wave tag pins

## Summary

Parser-consolidation F1 wave (Cutover F1, plan/nested-capture-architecture.md) — the charly host slice of F1.2/F1.4 + the wave's dependency pins.

1. **F1.2/F1.4 — ONE deploy-shape classifier.** `foldSubstrateKind`'s deploy-vs-template classification was an OR-composite: `pl.IsDeployShape(pn) || len(pl.ResourceChildren(pn)) > 0 || agentProvisionedBody(rawBody)`. All three arms now live in `loaderkit.IsDeployShape` (the ONE classifier, data-derived: member children, scalar cross-ref, from:/image: mapping, and the `agent_provisioned` imageless spelling matching the Deploy gate's box exemption). The host calls `pl.IsDeployShape(pn)` alone; `agentProvisionedBody` (node_normalize.go) is deleted. The `ResourceChildren` seam method + loaderkit impl + plugin-loader delegator die with it (spec #122 merged/tagged v0.2026250.2036, sdk #239 merged/tagged v0.2026250.2044, plugin-loader #7 merged/tagged v0.2026250.2044).

2. **Pins** — spec v0.2026250.2036, sdk v0.2026250.2044, plugin-loader/candy/plugin-loader v0.2026250.2044; the three dev worktree replaces are dropped.

## How tested

All commands on the final committed tree (real tag pins, no replaces), `-count=1`:

```
$ gofmt -l .            # empty
$ go build .  && go vet .   # both exit 0
$ go test . -count=1    # full kernel suite, incl. the fixed-fold + plugin-substrate e2e
ok  github.com/opencharly/charly/charly  116.106s
```

The fixed-fold suite (the 2026-09-07 RCA lock) verbosely:

```
$ go test . -run 'TestSubstrateKind' -count=1 -v
=== RUN   TestSubstrateKind_BothShapesByteEquivalent
--- PASS: TestSubstrateKind_BothShapesByteEquivalent (0.02s)
=== RUN   TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy
--- PASS: TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy (0.00s)
=== RUN   TestSubstrateKind_PlainImagelessPodStaysTemplate
--- PASS: TestSubstrateKind_PlainImagelessPodStaysTemplate (0.00s)
=== RUN   TestSubstrateKind_ImagelessAgentProvisionedWithSiblingStillFolds
--- PASS: TestSubstrateKind_ImagelessAgentProvisionedWithSiblingStillFolds (0.00s)
PASS
ok  github.com/opencharly/charly/charly 0.512s
```

**FAIL-RED/PASS-GREEN** (the fixed fold test guards the consolidated classifier): with the `agent_provisioned` arm TEMPORARILY stripped from the consolidated `IsDeployShape` (uncommitted mutation, restored byte-perfect), the fold test FAILS — the RCA bug reproduces through the new architecture:

```
--- FAIL: TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy (0.00s)
    substrate_imageless_deploy_test.go:61: imageless agent_provisioned pod DROPPED from acc.Deploy (deploy keys []); pod template map: [agent-live]
FAIL	github.com/opencharly/charly/charly	1.028s
```

Restored (git diff empty vs HEAD): green (above). The B14(d) sweep: `grep -rn 'ResourceChildren' charly/charly --include='*.go'` matches only the historical comment (the call and `agentProvisionedBody` are deleted) — zero live references on this branch.

## Rulebook compliance

- R3 — one deploy-shape classifier (`loaderkit.IsDeployShape`); the host composite dies, nothing re-implemented.
- R5 — `agentProvisionedBody` + the composite are deleted in the same PR that removes their last callers (the consolidated classifier carries both arms).
- R1 — the 2026-09-07 RCA's lockstep contract (classifier predicate == the Deploy gate's agent_provisioned exemption) is preserved; the fixed-fold tests are unchanged and green.
- R6 — branch-only, no force-push, no hook bypass. R10 — all gates on the final committed real-pinned tree.

## Change classification

- Change class: code refactor (classifier consolidation + dependency tag pins)
- Verification gate: gofmt/build/vet + FULL kernel test suite green on the final committed tree; fail-red/pass-green proven with pasted output
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
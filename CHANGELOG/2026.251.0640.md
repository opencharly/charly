# 2026.251.0640 — F2: the genericNode bridge dies — candy/cue/doc-stream restated on the parsed node (wave: spec 533, sdk 531, plugin-loader 534)

F2: the genericNode bridge dies — candy/cue/doc-stream restated on the parsed node (wave: spec 533, sdk 531, plugin-loader 534)

## Summary

Parser-consolidation **F2 — the core bridge deletion** (plan/nested-capture-architecture.md, Cutover F2): the genericNode dual representation dies.

**What died** (all 5 files deleted):
- `node_parsed.go` — the JSON→yaml→JSON round-trip machine (`parsedNodeToGeneric`/`genericToParsedNode`) + the `normalizeNodeInto` test bridge;
- `node_parse.go` — the `genericNode` type (and with it the last type naming it, `KindProvider` + its no-op bijection gate);
- `node_candy.go` — the yaml-key `candyIsImage` + `buildCandy` re-conversion;
- `node_build.go` — the `decodeNodeValue` wrapper;
- `node_normalize.go` — the two self-labeled (R3) one-line wrappers (`isStandaloneResourceKind`/`foldStandaloneTemplateReply`), inlined at their call sites; `ensureMap` moved beside its consumer.

**What replaced them** (all on the wire-safe `spec.ParsedNode` through the loader seam):
- `foldCandyKind`: `pl.CandyIsImage(pn)`/`pl.BuildCandy(pn)` (the loaderkit parsed-node restatement, sdk v0.2026251.531) + the SHARED spec set-body folds `spec.SetBoxInto`/`spec.SetCandyInto` (spec v0.2026251.533, F2.4) — no genericNode reconstruction anywhere in the dispatch.
- `validateKindValueCUE`: the mapping-shape check reads the CANONICAL JSON body directly; the CUE ingest is `CueDocFromJSON` (F2.3) — the gn→yaml.Marshal→CueDocFromYAML round trip is gone.
- `materializeDiscoveredNode`: `pl.CandyIsImage(pn)` (F2.1).
- `materializeDocStream`: the ONE loaderkit doc-stream composer `pl.ParseDocStream` (F2.5) drives the embedded-vocab stream with the same per-document pipeline as the file walk; the hand-rolled classify/gate/parse loop is deleted.
- `pluginPrimaries` (F2.6): the frozen 11-entry literal is DELETED — the table is seeded at init from the compiled-in units' declared capability primaries (the embedded charly.yml `verb_primaries:` data directive, admitted by #NodeDoc via spec v0.2026251.533); the expectation is a parity assertion (`node_desugar_test.go`).
- **Moved tests**: the five bridge test files' candy/child-form coverage moved beside the loaderkit implementation as `candy_node_test.go` + `node_childform_test.go` (sdk); the charly-side plugin e2e + node-form tests were rewritten against parsed-node fixtures (`parsednode_helpers_test.go`).
- R5 claim-keyed sweep: stale claims about the deleted bridge fixed in loader_threaded.go / plugin_prescan.go / materialize.go / plugin_inproc.go / materialize_seam_parity_test.go / KERNEL_MANIFEST.md (rows + citations).
- Pins: spec v0.2026251.533, sdk v0.2026251.531, plugin-loader/candy/plugin-loader v0.2026251.534 (the wave's released tags; dev replaces dropped).

## How tested

All commands on the final COMMITTED tree (real tag pins, no replaces), `-count=1`:

```
$ gofmt -l .            # empty
$ go build .            # exit 0
$ go vet ./...          # exit 0
$ go test ./... -count=1
ok  github.com/opencharly/charly/charly                121.847s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  29.953s
```

The bridge-deletion acceptance points, verbosely:

```
$ go test . -run 'TestCandyKind|TestSubstrateKind|TestPluginPrimaries' -count=1 -v
--- PASS: TestCandyKind_BothShapesByteEquivalent (0.00s)     # foldCandyKind == direct decode, parsed-node baselines
--- PASS: TestSubstrateKind_BothShapesByteEquivalent (0.00s) # foldSubstrateKind byte-equivalence, parsed-node fixtures
--- PASS: TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy (0.00s)
--- PASS: TestSubstrateKind_PlainImagelessPodStaysTemplate (0.00s)
--- PASS: TestSubstrateKind_ImagelessAgentProvisionedWithSiblingStillFolds (0.00s)
--- PASS: TestPluginPrimaries_ParseTimeParity (0.00s)        # the 11-entry convention, asserted
```

The moved/rewritten coverage lives in sdk/loaderkit (candy_node_test.go, node_childform_test.go, parse_doc_stream_test.go — green in sdk #240) and the loader-plugin seam tests (plugin-loader #8).

**B14(d) sweeps** (identifier-keyed, word-boundary):

```
$ grep -rn 'parsedNodeToGeneric|genericToParsedNode|normalizeNodeInto|genericNodesFromDoc|candyNodeFromYAML|substrateNodeFromYAML' charly --include='*.go'
(nothing — zero live references; only the deleted-file history remains)
$ grep -rn 'candyIsImage|buildCandy|decodeNodeValue' charly --include='*.go' | grep -v '_test.go'
(nothing — the loaderkit uppercase seam names are the only spellings; no genericNode consumer survives)
```

**A2/A3 — the F-wave R10 bed batch (fresh rebuilds on DISPOSABLE targets), run on the completed F2 wave:**

Both beds ran to completion (exit 0, all steps `ok: true`) from a FRESH scratch project — a fresh clone of `distro-fedora` pinned at `93dee42d` (git HEAD verified), NOT in-repo — with this branch's binary (built at e17d7985, `charly version` → 2026.251.0539) on PATH. Same recipe as the F1 wave's bed batch (distro-fedora clone + dev binary, step matrices below). No `CHARLY_REPO_OVERRIDE` was set — the runs resolve the @github pins as authored.

**POD bed `check-cross-local-http` (disposable pod, distro-fedora bed) — calver `2026.251.0621`, PASS (steps=15):**

```
$ charly check run check-cross-local-http    (cwd: fresh distro-fedora@93dee42d clone)
charly check run check-cross-local-http: [image-build] PASS after 108s (cache=59/142)
charly check run check-cross-local-http: [check-image] PASS after 15s
charly check run check-cross-local-http: [deploy-add] PASS after 3s
charly check run check-cross-local-http: [config] PASS after 4s
charly check run check-cross-local-http: [start] PASS after 3s
charly check run check-cross-local-http: [bring-up-members] PASS after 3s
charly check run check-cross-local-http: [check-live] PASS after 6s
charly check run check-cross-local-http: [feature-run] PASS after 4s
charly check run check-cross-local-http: [rebuild-members-down] PASS after 2s
charly check run check-cross-local-http: [update] PASS after 15s
charly check run check-cross-local-http: [re-bring-up-members] PASS after 2s
charly check run check-cross-local-http: [check-live-rebuild] PASS after 5s
charly check run check-cross-local-http: [feature-run-rebuild] PASS after 4s
charly check run check-cross-local-http: [cleanup] PASS after 5s
charly check run check-cross-local-http: [cleanup-members] PASS after 25s
charly check run check-cross-local-http: PASS (steps=15)
```

Summary artifact: `.check/check-cross-local-http/2026.251.0621/summary.yml` — 15/15 steps `ok: true`, `ok: true` overall, 0 errors (only the allowlisted dev-worktree-binary-notice).

**VM bed `check-cross-vm-http` (disposable Fedora-Cloud VM, distro-fedora bed) — calver `2026.251.0627`, PASS (steps=11):**

```
$ charly check run check-cross-vm-http      (cwd: same fresh distro-fedora@93dee42d clone)
charly check run check-cross-vm-http: [vm-build] PASS after 1s
charly check run check-cross-vm-http: [vm-create] PASS after 1s
charly check run check-cross-vm-http: [deploy-add] PASS after 22s
charly check run check-cross-vm-http: [bring-up-members] PASS after 3s
charly check run check-cross-vm-http: [check-live] PASS after 5s
charly check run check-cross-vm-http: [rebuild-members-down] PASS after 2s
charly check run check-cross-vm-http: [update] PASS after 31s
charly check run check-cross-vm-http: [re-bring-up-members] PASS after 3s
charly check run check-cross-vm-http: [check-live-rebuild] PASS after 3s
charly check run check-cross-vm-http: [cleanup] PASS after 2s
charly check run check-cross-vm-http: [cleanup-members] PASS after 2s
charly check run check-cross-vm-http: PASS (steps=11)
```

Summary artifact: `.check/check-cross-vm-http/2026.251.0627/summary.yml` — 11/11 steps `ok: true`, `ok: true` overall, 0 errors (only the allowlisted dev-worktree-binary-notice).

Both disposable targets are named above; both are the plan's F-wave R10 roster (one pod bed + one VM bed on the consolidated binary), now RUN — no deferral.

**R1 note — boxload-pod (pre-existing, not this PR):** an earlier probe of `check-boxload-pod` (the pod bed whose instrument step needs host-env plumbing) fails at `check-live` (exit 2) under BOTH this branch's binary AND the F1-tag binary (c1aed4c, `charly version` 2026.250.2055 — identical failure, log `/tmp/f1-bed-boxload.log`, calver 2026.251.0557). RCA: the pod's config-setup path hits the host-env nested-podman threading gap tracked in the scratchpad (plugin-deploy-pod's fallback `resolveHostCharlyBin` uses its own binary; upstream spec pin < 1030 cannot read a 1030 project) — unrelated to the F2 bridge deletion, which is why the F-wave roster runs the two cross-{local,vm}-http beds above instead. The wave re-run on the merged wave binary will re-prove the roster.

## Rulebook compliance

- R3 — one parsed-node implementation per behavior (loaderkit), one set-body fold (spec), one doc-stream composer; the deleted bridge had NO copy left standing.
- R5 — the bridge + its wrappers are deleted in the same PR that removes their last callers; the moved tests were rewritten against the parsed node, never keeping the old fixtures; stale manifest rows/citations swept.
- R1 — parse-time determinism for the platform live-container verbs (proven load-bearing by the corpus fixtures) is preserved BY DATA (verb_primaries) — the literal deletion surfaced the requirement through the corpus tests before anything shipped; boxload-pod's live-step failure reproduced identically under the F1-tag binary and is recorded above as pre-existing.
- R6 — branch-only (feat/f2-bridge-deletion), no force-push, no hook bypass. R10 — all gates on the final committed real-pinned tree + the disposable bed batch above.

## Change classification

- Change class: refactor (parser-consolidation F2 bridge deletion + wave tag pins)
- Verification gate: gofmt/build/vet + FULL kernel suite green on the final committed real-pinned tree; parity tests green; B14 sweeps clean; pod + VM R10 beds run on the final tree (calvers 2026.251.0621 / 2026.251.0627, both PASS)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
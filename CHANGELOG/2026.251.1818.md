# 2026.251.1818 — refactor(charly): F4.2 — providerClasses derived from spec.ProviderClasses (Cutover F)

## Summary

Cutover F4.2 charly side — the provider-class vocabulary held twice → ONE source in spec, CUE-derived; charly derives from it.

**Duplication evidence (pre-change, current tree):**
- `charly/provider.go:78-95` — `providerClasses` 11-entry closed map (kind|deploy|verb|step|build|builder|command|loader|refs|agent-runtime|terminal)
- `plugin-box/candy/plugin-box/validate_rules.go:150-170` — `validPluginClasses` byte-identical 11-entry map (deleted in the companion plugin-box PR)
- `spec/schema/candy.cue` — the `#PluginCapability` regex carried the same 11 classes inline (reworked in spec#125)

**Fix:** spec#125 minted `spec.ProviderClasses` as the ONE CUE-derived vocabulary (the `#ProviderClassNames` list in schema/candy.cue, emitted by schemagen; the `#PluginCapability` regex now derives from the same list via strings.Join). This PR makes charly's `providerClasses` membership set DERIVED from `spec.ProviderClasses` — the hand-maintained 11-entry map is gone. The typed constants (`ClassKind`, `ClassVerb`, …) stay as the kernel's Go API. New `TestProviderClassesParity` locks the bijection (every typed constant ∈ the derived set, sizes equal).

Spec pin bumped to v0.2026251.1735.

## How tested

```
$ cd charly/charly
$ go build ./... && go vet ./... && gofmt -l .
(empty, exit 0)
$ go test -run TestProviderClassesParity . -v
=== RUN   TestProviderClassesParity
--- PASS: TestProviderClassesParity (0.00s)
$ go test -run 'TestSplitCapability|TestProvider|TestPlugin' .   # ok, 3.4s
$ go test ./...   # full charly suite green
```

## R10 — live disposable-bed run (branch-built binary)

Ran `charly check run check-commands-local` (the plugin-composition + box-verb bed; exercises the provider-class vocabulary — providerClasses derived from spec.ProviderClasses — through every provider registration and the box validate/load path) with the **branch-built binary** (head 26f32bc2, `charly version` → 2026.251.1747) from the charly repo as the project.

```
$ ./bin/charly -C charly check run check-commands-local
charly check run check-commands-local: [deploy-add] PASS after ~63s
charly check run check-commands-local: [bring-up-members] PASS after 0s
charly check run check-commands-local: [check-live] PASS after ~36s
charly check run check-commands-local: [update] PASS after 9.758s
charly check run check-commands-local: [cleanup] PASS after 5.911s
charly check run check-commands-local: [cleanup-members] PASS after 0s
charly check run check-commands-local: PASS (steps=6)
```

Calver `2026.251.1809`, summary.yml steps=6 all ok:true. Artifacts: `.check/check-commands-local/2026.251.1809/`. The one allowlisted finding is the standard dev-worktree-binary notice.

## Rulebook compliance

- **R0 (skills honored):** R0 dispatcher skills (go, git-workflow, strict-policy) loaded before the first tool call.
- **R1 (RCA):** the three-way duplication verified against the current tree before changing (evidence in the summary).
- **R2 (scope):** only F4.2's charly-side derivation, the parity test, and the spec pin bump.
- **R3 (no duplication):** the 11-entry map copy is deleted; charly reads the one CUE-derived slice. Typed constants are the kernel's compile-time API (locked by the parity test).
- **R4 (no workarounds):** no hand-pin of the vocabulary; the pin bump is a legitimate require.
- **R5 (hard cutover):** the old map literal is deleted in the same commit; no dual path.
- **R10 (fresh-build proof):** full `go test ./...` + the disposable bed run above on the branch-built binary. The consolidated-binary F-wave re-run remains scheduled for the F6 wave step.

## Change classification

- Change class: refactor (parser consolidation F4.2 charly leg; behavior-preserving)
- Verification gate: full `go test ./...` + disposable local-bed run (calver 2026.251.1809, exit 0)
- Attribution tier: `analysed on a live system` — suite + bed ran live on the changed tree.

*Assisted-by: pi anthropic/claude-sonnet-4-20250514 (analysed on a live system)*
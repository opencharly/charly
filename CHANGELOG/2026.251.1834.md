# 2026.251.1834 — refactor(charly): F3.1 — candy kind-word literal → word-keyed capability carrier (Cutover F)

## Summary

Cutover F3.1 — the `"candy"` kind-word literal in the kind-blind dispatch → a word-keyed capability carrier. The charly PR of the F3 wave (F3.2 node_validate.go and F3.3 includeKinds were verified ALREADY GONE on main — recorded as landed in an earlier wave; only F3.1 remained).

**Duplication evidence (pre-change, current tree):**
- `charly/provider_kind_invoke.go:56` — `if pn.Disc == "candy"` in `runPluginKind` (the kind-blind dispatch)
- `charly/materialize.go:70` — the same literal in `materializeDiscoveredNode`
- `charly/no_kind_switch_in_kernel_test.go:323-327` — the gate test's negative control that BLESSED the literal ("kind-recognition Data is allowed")

**Fix:** `spec.CandyKindCarrier` (minted in spec#125, same capability-carrier pattern as `StructuralKindCarrier`/`ValidatingKindCarrier` already in the dispatch) is the word-keyed carrier. `capMeta` answers `IsCandyKind()` from its own declared word; both dispatch sites ask `prov.(spec.CandyKindCarrier)` — the kernel never names the word. The gate carve-out (its lone-if negative control + the header blessing) is deleted with it; the whole `TestNoKindSwitchInKernel` family still passes.

Spec pin bumped to v0.2026251.1735 (the tag carrying `CandyKindCarrier`).

## How tested

```
$ cd charly/charly
$ go build ./... && go vet ./... && gofmt -l .
(empty, exit 0)
$ go test -run 'TestNoKindSwitchInKernel|TestCandyKind_BothShapesByteEquivalent' . -v
=== RUN   TestNoKindSwitchInKernel
--- PASS: TestNoKindSwitchInKernel (0.02s)
=== RUN   TestNoKindSwitchInKernel_TeethProof
--- PASS: TestNoKindSwitchInKernel_TeethProof (0.00s)
=== RUN   TestCandyKind_BothShapesByteEquivalent
--- PASS: TestCandyKind_BothShapesByteEquivalent (0.01s)
$ go test -run 'TestCue|TestLoad|TestDeployNode|TestMaterialize|TestExternalKind|TestCandy|TestEmbedded' .   # 71s, ok
$ go test ./...   # full charly suite green
```

`grep -rn 'Disc == "candy"' *.go` over non-test kernel files: zero hits after the change.

## R10 — live disposable-bed run (branch-built binary)

Ran `charly check run check-commands-local` (the plugin-composition + box-verb bed; exercises the candy-kind dispatch → provider_kind_invoke.go + materialize.go CandyKindCarrier ask, the box new/validate/generate verbs, and the provider load path) with the **branch-built binary** (head 0b96bca4, `charly version` → 2026.251.1744) from the charly repo as the project.

```
$ ./bin/charly -C charly check run check-commands-local
charly check run check-commands-local: [deploy-add] PASS after 1m3.258s [allowlisted=1]
charly check run check-commands-local: [bring-up-members] PASS after 0s
charly check run check-commands-local: [check-live] PASS after 36.431s [allowlisted=1]
charly check run check-commands-local: [update] PASS after 9.937s [allowlisted=1]
charly check run check-commands-local: [cleanup] PASS after 5.96s [allowlisted=1]
charly check run check-commands-local: [cleanup-members] PASS after 0s
charly check run check-commands-local: PASS (steps=6)
```

Calver `2026.251.1804`, summary.yml steps=6 all ok:true; the box-new-validate-generate step (dispatches through the candy-kind carrier + box validate) exited 0. Artifacts: `.check/check-commands-local/2026.251.1804/`. The one allowlisted finding is the standard dev-worktree-binary notice.

## Rulebook compliance

- **R0 (skills honored):** R0 dispatcher skills loaded before the first tool call; git-workflow / boundary-law skills applied.
- **R1 (RCA):** the literal sites were verified against the current tree; the gate carve-out's blessing was RCA'd as a stale claim and deleted with the fix.
- **R2 (scope):** F3.1 only; F3.2/F3.3 verified already-landed and recorded.
- **R3 (no duplication):** the word-keyed carrier is the single routing; same pattern as the structural/validating carriers.
- **R4 (no workarounds):** no bypass; the carrier is the real mechanism.
- **R5 (hard cutover):** the carve-out and literal are deleted in the same commit; no dual path.
- **R10 (fresh-build proof):** full `go test ./...` + the disposable bed run above on the branch-built binary. The consolidated-binary F-wave re-run remains scheduled for the F6 wave step.

## Change classification

- Change class: refactor (kind-blind dispatch purity; behavior-preserving)
- Verification gate: full `go test ./...` + disposable local-bed run (calver 2026.251.1804, exit 0)
- Attribution tier: `analysed on a live system` — suite + bed ran live on the changed tree.

*Assisted-by: pi anthropic/claude-sonnet-4-20250514 (analysed on a live system)*
# 2026.251.1904 — fix(pin): plugin-box candy v0.2026250.1030 -> v0.2026251.1834 (Cutover F)

## Summary

Bumps the compiled-in plugin-box candy require from `v0.2026250.1030` to
`v0.2026251.1834` in `charly/go.mod` (+ the two go.sum entries). The F-wave
plugin-box F4.1/F4.2/F4.3 cross-cutting dedup merged as opencharly/plugin-box#12
and was tagged `v2026.251.1834` / `candy/plugin-box/v0.2026251.1834` (commit 18d3831c).
The charly require was left stale at the pre-F wave pin; the F6 end-R10 beds
(check-commands-local 6/6, check-cross-local-http 15/15, check-cross-vm-http 11/11)
validated the consolidated binary built with a scratch `go get` at the released
tag. This PR makes the repo state match the validated F-wave binary.

## How tested

Tag existence (target verification):

```
$ git -C plugin-box rev-parse v2026.251.1834^{commit} candy/plugin-box/v0.2026251.1834^{commit}
18d3831cc5e395c74a3f700bac518239c56a001b
18d3831cc5e395c74a3f700bac518239c56a001b
```

Go proxy resolution:

```
$ go list -m github.com/opencharly/plugin-box/candy/plugin-box@v0.2026251.1834
github.com/opencharly/plugin-box/candy/plugin-box v0.2026251.1834
```

Gates (run in charly/ on branch feat/f-wave-pinbump, after `go get @v0.2026251.1834 && go mod tidy`):

```
$ gofmt -l .
(empty — clean)

$ go build ./...
(exit 0)

$ go vet ./...
(exit 0)

$ go test ./...
ok  github.com/opencharly/charly/charly  101.399s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  29.066s
```

Diff is 2 files, +3/−3: one require line + the pair of go.sum entries.

## Rulebook compliance

- **R1**: the stale pin is the previously documented charly-side gap of the F-wave
  close (the F6 note records go.mod plugin-box require as an outstanding item).
- **R3**: single canonical pin — bump in place, no duplicate mechanism.
- **R6**: branch-per-change, PR-only, no force-push; branch is fresh off origin/main.
- **R10**: gates above from the final committed tree; the F6 end-R10 beds already ran
  this exact pin composition live (attribution below).

## Change classification

- Change class: dependency pin bump (go.mod/go.sum), no runtime code change.
- Verification gate: full Go gates (gofmt/build/vet/test) pass on the final tree; the
  composed binary was additionally exercised live by the F6 end-R10 beds on this exact
  plugin-box tag.
- Attribution tier: `analysed on a live system` — the released-tag binary ran the F-wave
  R10 beds; `task verify` (the full umbrella gate) requires a fresh full clone and is
  the known environment limit recorded for this goal.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
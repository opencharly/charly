# 2026.251.1947 — weld: add plugin-review to the command-plugin release (host-command-plugins + packaging variants)

## Summary

Welds **opencharly/plugin-review** (pinned at its merged tag `v2026.251.1928`) into the charly command-plugin release so `charly review` resolves baked on EVERY charly install — release assets (`charly-plugins-linux-*.tar.gz`), native packages (`/usr/lib/charly/plugins`), and images via the layer-charly package. This is the distribution mechanism the action-review cutover rides: the gate workflow on a standard GitHub runner downloads the release assets and the welded `charly review` word just works (no Go, no image, no fetch).

Three files, using the release infra as-is (R3, single source):

- `scripts/host-command-plugins.txt` — `+plugin-review@v2026.251.1928` (the release-binary workflow clones the repo at the pin and builds `candy/plugin-review`);
- `packaging/charly.yml` — `variants.default` + `variants.full` `+plugin-review` (default is what layer-charly installs → the optional self-hosted runner image ships it); `minimal` untouched;
- `charly/packaging_test.go` — stale count comments updated (the coverage test itself is data-driven and green with the union covering the 11-plugin weld list).

## How tested

```
$ sed -n '/variants:/,/formats:/p' packaging/charly.yml | grep plugin-review
                        - plugin-review   (default)
                        - plugin-review   (full)
$ grep plugin-review scripts/host-command-plugins.txt
plugin-review@v2026.251.1928
$ cd charly && GOTMPDIR=/tmp/gt go test -run 'TestPackaging' -count=1 .
ok  	github.com/opencharly/charly/charly	0.395s
```

The release-binary workflow (`.github/workflows/release-binary.yml`) needs no change — it iterates the weld list and `packaging_test.go` gates the variant↔weld coverage. `plugin-review`'s own release assets (`review-linux-{amd64,arm64}` + `review.providers`, published from its `release.yml` at tag v2026.251.1928) prove the plugin module builds clean for both arches.

## Rulebook compliance

- R3 — single source: only the weld list + packaging variants change; the release pipeline is untouched.
- R5 — atomic: one PR, all three touch points; the coverage test updated in the same commit.
- R4 — no workarounds: pin is a merged tag (`v2026.251.1928`), never a branch.
- R1 — the packaging_test comment fix is a stale-doc correction uncovered while verifying coverage, fixed in-tree.

## Change classification

- **Change class:** config/code (release composition + coverage test).
- **Verification gate:** `charly packaging` tests green on the welded set (+9/−5 diff shown above). The full release-pipeline R10 (release-binary run → tarball carries `plugin-review` + `.providers` → `charly review --self-test` on a stock runner) rides this PR's merge tag. The **named exit** (tracked as ledger item 2 of the action-review plan, phase 2): the release-binary workflow's build for the merged tag of THIS PR — verify `charly-plugins-linux-amd64.tar.gz` contains `plugin-review` + `plugin-review.providers`, extract + `charly review --self-test` on a stock ubuntu-latest runner, and dispatch the `charly-arch` build workflow for the new version so the packaged default variant ships the weld.
- **Attribution tier:** `analysed on a live system` — the changed packaging surface parsed + tested live against the welded list; the binary-build leg is already proven by plugin-review's own release assets.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
# 2026.252.0740 — docs: rebrand charly as the agentic config orchestrator

## Summary

Rebrand charly from **"the open infrastructure compiler"** to **"the agentic config orchestrator — everything is a plugin, nothing is mandatory."**

- New opening: orchestrator framing, agents as first-class drivers of the same surface
- New **"Everything and nothing"** section: every concern is a plugin candy, nothing is built in that cannot be trimmed out — swap in your own tool for any particular job (with a before/after `charly.yml` snippet)
- LLVM heritage demoted from headline to a **Heritage** note
- "Every word is a plugin" section tied to the everything-and-nothing strategy
- Audit fixes: example candy pins refreshed `v2026.231.0714` → `v2026.251.1947` (verified existing); dead CLI reference link `/reference/cli/deploy/` (404 — no `deploy.md` in docs source) → `/reference/cli/`
- Verified-current claims left intact: five substrates, uid-1000/no-privileged coder boxes, pod-sshd candy reference (correct in docs source; live 404 is docs deploy drift)

## How tested

- `git diff` reviewed: 46 insertions, 16 deletions, README.md only
- `grep -i 'compiler|compiles|compile'` on the new README: zero branding occurrences; the two remaining "compiled" hits are technical (plugins compiled into the binary), not branding
- Pin freshness: `gh api repos/opencharly/charly/tags --paginate` — `v2026.251.1947` is the latest tag; `v2026.231.0714` exists but is 20 days stale
- Link liveness: `curl -s -o /dev/null -w '%{http_code}'` on all 14 README links — 12x 200; `/reference/cli/deploy/` 404 (fixed); pod-sshd candy URL 404 on live site but page exists in docs source (`reference/candy/github.com/opencharly/pod-sshd:v2026.239.1637`) — docs deploy drift, not a README defect

## Rulebook compliance

- R0: `/charly-internals:git-workflow` + `/charly-internals:root-cause-analyzer` loaded before any git/gh step
- R1: every divergence RCA'd — stale pins (generation lag), dead CLI link (page never existed in docs source), candy 404 (docs deploy drift)
- R5: corrected facts replace old ones in the same PR; no stale-but-harmless residuals
- R6: PR-only, no force-push, no direct push to main
- R7: docs-only change; no pins, scripts, or code touched

## Change classification

- Change class: **docs-only**
- Verification gate: **docs gate**
- Attribution tier: **documentation reviewed**

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*
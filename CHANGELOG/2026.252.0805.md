# 2026.252.0805 — weld: plugin-pipeline joins the release (the generic agent/workflow engine)

## Summary
Add opencharly/plugin-pipeline (the generic agent/workflow engine: command:pipeline + verb:pipeline + kind:pipeline) to the welded command-plugin set: scripts/host-command-plugins.txt (pinned @v2026.251.2226) + the packaging full variant (all eleven). The two lists are ordered identically (pipeline after review).

## How tested (pasted)
```
$ gh run list --repo opencharly/plugin-pipeline --workflow ci --limit 2
{"conclusion":"success","event":"pull_request","head":"d629172","id":34324779580,"status":"completed"}
{"conclusion":"success","event":"pull_request","head":"a231e58","id":34324293994,"status":"completed"}

$ gh pr checks 7 --repo opencharly/plugin-pipeline
test              pass  1m5s
validate / validate  pass  45s
```
The plugin's own CI (the `ci` workflow) is green on its merged PR heads, and its org validator (`validate / validate`) passed on the merged plan-features PR (#7, merged 2026-09-09T07:38:37Z). The weld list is the single source the release-binary workflow consumes (R3); the packaging variant edit is declarative.

## Rulebook compliance
R3 (single source), R6 (PR-only), the org validator gates this PR.

## Change classification
Packaging/weld; verification gate: the pasted plugin gates + the validator; attribution: analysed on a live system (the plugin runs live; the weld's package-install path is the release pipeline's).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
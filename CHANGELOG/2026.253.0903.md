# 2026.253.0903 — chore(charly): bump the compiled-in plugin-vm pin to v0.2026253.830 (the keeper-restart fix)

## Summary

Bumps the compiled-in plugin-vm pin to v0.2026253.830 — the snapshot-anchored keeper-restart fix (plugin-vm#30): a keeper running ON its golden snapshot as the ACTIVE disk could not restart after a stop (the 0444 shared-clone chmod made qemu fail with EACCES). The start/create paths now chmod the active disk writable before qemu opens it and restore 0444 after.

## How tested (pasted)

**The build (the fresh rebuild with the bumped pin):**

```
$ go mod tidy && go build -buildvcs=false -o /tmp/charly-fixed/bin/charly .
BUILD-OK
$ grep plugin-vm go.mod
github.com/opencharly/plugin-vm/candy/plugin-vm v0.2026253.830
```

**The live keeper-restart proof (the disposable target check-omarchy-eval-base-inst, disposable: true, the golden at 0444):**

```
$ chmod 444 .../snapshots/golden/disk.qcow2 && stat -c "golden=%a" ...
golden=444
$ charly vm stop check-omarchy-eval-base-inst
Stopped VM charly-check-omarchy-eval-base-inst
$ charly vm start check-omarchy-eval-base-inst
Started VM charly-check-omarchy-eval-base-inst
$ virsh list --all | grep eval-base
 945   charly-check-omarchy-eval-base-inst   running
$ stat -c "%a" .../snapshots/golden/disk.qcow2
444
```

The golden at 0444 restarts (the pre-fix code fails with the exact EACCES), and the golden is restored to 0444 after the start — the shared-clone benefit preserved. The binary is a dev/worktree build (version reports "unknown" — the CALVER ldflag is only set in packaged builds); the source-match is the go.mod pin above + the build from this tree.

## Rulebook compliance

- R0: git-workflow loaded.
- R3: only the plugin-vm pin is bumped.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the pin is a merged tag (v0.2026253.830), never a PR branch.
- R10: the build + the live keeper-restart proof pasted (the disposable target named).

## Change classification

- Change class: chore (pin bump)
- Verification gate: the build + the live keeper-restart proof (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
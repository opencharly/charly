# 2026.253.1816 — chore: re-pin sdk/spec/plugin-vm — the canonical reference-resolution seam

## Summary

The namespace-qualified (git-linked) entity reference seam (the RCA'd divergence between the load-time validator and the runtime resolver):

- **sdk** v0.2026251.531 → v0.2026253.1714 (ResolveEntityRef #246 + the namespace-aware runtime resolution #247)
- **spec** v0.2026251.1735 → v0.2026253.1700 (the deploy from: regex allows the ns.entity form, #127)
- **plugin-vm** v0.2026253.830 → v0.2026253.1802 (the namespace-qualified vm entity resolution #31 + the clone-drive leaf #32 + the entity-keyed leaf paths #33)

Effect: EVERYTHING in charly is referenceable — locally AND via git-linked imports (ns.entity) — validated once, resolved everywhere. The X3 spike (from: omarchy.check-charly-omarchy-vm:golden) proves it end to end.

## How tested (pasted output)

```
$ go build ./...
(clean)

$ charly box validate   (eval-omarchy, the X3 spike bed from: omarchy.check-charly-omarchy-vm:golden)
charly box validate: OK — checked 187 candies, 21 boxes, 26 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

$ charly check run check-omarchy-edge-spike
[vm-build] PASS   (the qualified clone resolves — the seam works end to end)
```

## Rulebook compliance

- R3: one canonical surface — the sdk's ResolveEntityRef/ResolveKindEntityBody are the single reference-resolution implementation
- R5: the old local-only lookups are retired in the sdk/plugin-vm PRs
- R6: PR-only landing

## Change classification

- Change class: dependency re-pin (the reference seam)
- Verification gate: build + the X3 spike validation
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
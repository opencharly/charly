# 2026.253.2003 — chore: re-pin sdk to v0.2026253.1947 (the deploy-hop qualified-from: fix)

## Summary

Re-pin the sdk contract module to v0.2026253.1947 — the deploy-hop qualified-from: fix (sdk#248): DeployTargetEntity's deploy-hop now falls back to the canonical namespace-aware body lookup (ResolveKindEntityBody) when a deploy's from: is a namespace-qualified template (ns.template, a git-linked import ref). The acceptance beds' vm-build steps (from: <base-bed>:<snapshot> where the base bed's from: is omarchy.omarchy-vm) previously failed the hop with "no kind:vm entity"; with this pin they resolve and clone.

## How tested (pasted output)

```
$ go mod tidy
(clean)

$ go build -o /tmp/charly-pincheck ./charly
(clean)

$ go test ./charly/... 
ok  	github.com/opencharly/charly/charly	115.875s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	28.655s
```

### R10 — the pin's changed code path, live on a fresh rebuild

The changed code path for THIS pin is the deploy-hop resolution (the sdk#248 fallback). It was executed against a fresh rebuild with the pinned sdk on a disposable VM bed — the acceptance bed's vm-build step (the deploy-hop resolving the qualified from: chain check-omarchy-eval-edge-inst → omarchy.omarchy-vm):

```
$ charly vm build check-omarchy-eval-edge-inst --from-snapshot golden
Wrote output/qcow2/check-omarchy-eval-edge-inst/disk.qcow2 (clone of check-omarchy-eval-edge-inst@golden)
Wrote output/qcow2/check-omarchy-eval-edge-inst/seed.iso
Wrote VM box localhost/charly-check-omarchy-eval-edge-inst:2026.253.1943
EXIT=0
```

The deploy-hop resolved the qualified from: chain and the clone build completed. The acceptance SUITE (16 beds) is the eval-omarchy wave's R10 — explicitly out of scope for this pin PR (the pin's changed path is the deploy-hop resolution, proven above; the suite exercises the eval-omarchy beds, not this pin's code).

## Rulebook compliance

- R0 (skills first): the charly-internals:git-workflow skill governs this change
- R1 (RCA): the acceptance-bed vm-build failure was RCA'd (the deploy-hop's local-only from: resolution) before the sdk fix
- R2 (no duplication): ResolveKindEntityBody is the single namespace-aware lookup
- R3 (one implementation): the hop uses the same surface as the vm-build entity resolution
- R4 (no workarounds): the pin is the released sdk tag
- R5 (delete legacy): the local-only hop lookup is retired upstream
- R6 (git safety): PR-only landing
- R8 (no dead code): N/A
- R9 (no magic numbers): N/A
- R10 (fresh disposable proof): the deploy-hop bed run above (the pin's changed code path, live on a fresh rebuild)

## Change classification

- Change class: dependency pin bump (sdk contract module)
- Verification gate: go build + go test + live deploy-hop bed run
- Attribution tier: analysed on a live system (the pin's changed path ran live with retained output; the acceptance suite is the eval-omarchy wave's R10, out of scope here)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
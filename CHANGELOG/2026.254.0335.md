# 2026.254.0335 — chore: bump the plugin-check pin to v0.2026254.5 — the live-gather include expansion (#42)

## Summary

Two pin bumps adopted on this branch; the NET change vs current main after the main sync is ONE pin:

- **plugin-check** v0.2026250.1843 → v0.2026254.5 (plugin-check #42) — the live-gather path now splices plan includes. Previously `charly check run <bed>` collected the raw plan and never expanded `include:` steps, so referenced steps (e.g. the eval-omarchy recording wrapper) were reported SKIP and never executed. This is the only net change of this PR.
- **sdk** v0.2026253.1714 → v0.2026253.1947 (sdk #248: DeployTargetEntity's deploy-hop `from:` resolving namespace-qualified templates) — the branch carried this re-pin (commit b1f46684 from before sdk#248 reached main); **current main already carries v0.2026253.1947 via merged PR opencharly/charly#580** (2026-09-10, CHANGELOG/2026.253.2003.md). After `gh pr update-branch` the net diff vs main contains no sdk line — the sdk re-pin is already-merged content, not a change of this PR. Its changed path (the deploy-hop resolving a git-linked qualified `from:` template) was executed and proven live in #580's own R10: the before/after evidence (pre-fix `charly: error: command "vm": VM "check-omarchy-eval-edge-inst" has no kind:vm entity in vm.yml.` → post-fix clone build resolving the chain check-omarchy-eval-edge-inst → omarchy.omarchy-vm via ResolveKindEntityBody) is recorded in sdk/CHANGELOG/0.2026253.1947.md and referenced from charly/CHANGELOG/2026.253.2003.md.

The dev-only local plugin-check replace directive used to prove the fix is superseded by the released pin — the committed tree carries no replace (the diff shows only the two require pin changes + go.sum/go.work.sum hash updates); the released v0.2026254.5 is the only path.

## How tested

$ cd charly && go mod tidy && go build ./...
BUILD_EXIT=0

**The R10 bed-run on the released pin** (binary rebuilt with task build:binary — version 2026.254.0008, NO dev replace — loading the released plugin-check v0.2026254.5; the post-sync tree changes no Go content, only adds main's CHANGELOG docs, so this evidence is valid for the final head):

$ PATH=.../charly/bin:$PATH charly check run check-omarchy-accept-session
vm-build ok=true 4s   vm-create ok=true 4s   deploy-add ok=true 26s   bring-up-members ok=true 0s
check-live ok=true 33s   gate-restart-stop ok=true 3s   gate-restart-start ok=true 3s
check-live-rebuild ok=true 29s   cleanup ok=true 5s   cleanup-members ok=true 0s
OVERALL: ok (total 107s; 46 passed, 0 failed, 1 skipped per phase — both check-live and check-live-rebuild)

**The 1 skipped step per phase — NAMED + RCA'd (R1):**

- Step: `corpus-fetch-upstream-tests` — the omarchy-corpus candy's install-timeline `run:` step (eval-omarchy/candy/omarchy-corpus/charly.yml) that fetches the upstream omarchy quattro/test tree at the pinned hash (8ea5151639…) into /opt/omarchy-upstream-test.
- Why skipped (the mechanism): the gate was a VERIFY-ONLY `charly check run` against an existing disposable deployment — no install phase executes in that mode, and `run:` steps live on the install timeline (the candy's own comment: "A run: step on the install timeline (the install ledger skips it on re-apply)"). The engine reports SKIP for a step whose timeline never executed — the step did not fail; it was not re-run.
- Why acceptable for this change class: the fetch's artifact is baked into the golden at build time and its presence is asserted at runtime by the bed's own `inst-upstream-tests` check (`test -d /opt/omarchy-upstream-test/test/shell.d && test -d /opt/omarchy-upstream-test/test/acceptance.d`), which passed in both phases. Re-running the curl/tar in a verify-only pass would mutate a live deployment and duplicate image content for zero assertion value. Critically, this surviving skip is NOT caused by the bug this PR fixes: the include-expanded rec-* steps — the exact steps the old live-gather path reported SKIP — all executed and passed (evidence below), proving the include expansion works.

Evidence (the include-expanded rec steps executed through this binary's live-gather path):
check-omarchy-accept-session.cast 3,845 B / .gif 89,045 B / .mjpeg 1,397,827 B / .mp4 109,519 B / .png 22,708 B
rec-start / rec-spice-start / rec-screen-spice / rec-spice-stop / rec-stop / rec-gif / rec-mp4 ALL PASS.

## Rulebook compliance

- R1 (RCA): the deploy-hop namespace resolve (sdk #248) was RCA'd in sdk#248 and proven live in #580's R10; the live-gather include expansion (plugin-check #42) was RCA'd in plugin-check#42; the one SKIP in this PR's gate output is named and RCA'd above (install-timeline run: step, verify-only mode, legit conditional skip)
- R2 (no pre-existing/out-of-scope split): after the main sync the net diff is the single plugin-check pin; the sdk re-pin commit is accounted for as already-merged main content (#580)
- R3 (one implementation): both pins adopt the ONE canonical resolver/splicer; no new mechanism
- R4 (no ad-hoc workarounds): the dev-only local replace is superseded by the released pin; no sleeps/retries/hand-pins
- R5 (hard cutover): the committed tree has no dev replace — the released pin is the only path
- R6 (git safety): PR-only landing via gh pr update-branch + append-only commits; no force-push
- R7 (prove the gate): go build passes on the final tree; the run used the task-built stamped binary (2026.254.0008)
- R8 (no dead code): N/A — no code removed
- R9 (no magic numbers): N/A — no numeric constants
- R10 (fresh disposable proof): the changed path (charly loading plugin-check v0.2026254.5) executed on a fresh disposable VM with the released pin, incl. the fresh-rebuild re-check, with evidence produced; the sdk qualified-from path was proven live in #580 on the identical pinned sdk (net-zero in this PR's diff)

## Change classification

- Change class: chore (dependency pin bump — net: plugin-check v0.2026254.5 only; the sdk line is already-merged main content via #580)
- Verification gate: go build + the one-bed run on the released pin (10/10 steps ok; 46+46 passed, 0 failed, 1 named+RCA'd conditional skip per phase; evidence produced)
- Attribution tier: fully tested and validated (the net change's changed path executed live on a fresh disposable VM with retained evidence; the already-merged sdk portion was proven in #580)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
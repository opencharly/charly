# 2026.254.0510 — Rebrand to the wonky DevOps tagline + fix README CLI-reference link + remove PROGRAM/factory-north-star.md

## Summary

Rebrands the live charly surfaces to **"The wonky DevOps tool for you and your agents."** (matching the README tagline) and removes an outdated planning doc entirely:

- `charly/main.go` — the `kong.Description` for the CLI binary (line ~207) now carries the new tagline.
- `packaging/charly.yml` — the package description (line ~117) carries the new tagline.
- `README.md` — the "CLI reference" link (line ~407) pointed at `https://opencharly.ai/reference/cli/`, a page the docs site does not emit; repointed to the emitted provider index `https://opencharly.ai/reference/providers/`.
- `PROGRAM/factory-north-star.md` — **removed (R5)**; it is an outdated positioning draft. Audited org-wide: the only remaining references are dated CHANGELOG history entries, which are intentionally untouched.

This PR deliberately ships WITHOUT the docs-generation configuration node. The `docs:` config contract is a SEPARATE, named landing in the same docs-generation project: it is implemented by opencharly/spec v0.2026254.503 (the merged #DocsConfig contract + classification) and consumed by the in-flight `feat/docs-config-node` branch (the `docs:` config block that pins spec/sdk), followed by the plugin-docs config-driven catalog view and the opencharly/docs regeneration. Nothing in this PR's surface touches those — it is the brand/link/removal cutover alone.

## How tested

- `git grep -n 'open infrastructure compiler'` at the repo root → CHANGELOG/ hits only (history stays); zero live-surface hits.
- No `*_test.go` file asserts the old `kong.Description` string (grep across tests = zero hits), so no test updates were required.
- Go build of the module (`cd charly && go build`) + the module test suite ran clean.
- Final diff: 3 insertions (README URL repoint, main.go Description, packaging description) and 1 deletion (`PROGRAM/factory-north-star.md`, 435 lines); working tree clean after push.
- Branch `feat/docs-brand-removal` @ `c11cf0be3fc559d12c45bd918a783084546ae421` verified upstream (ls-remote matches).

## Rulebook compliance

- R5 — delete legacy completely: the outdated doc is removed in the same change; no stale references survive outside CHANGELOG history.
- Docs/history boundary: CHANGELOG entries are history and were not rewritten.
- PR-only landing via the org validator; no direct-main push, no force-push.

## Change classification

- **Change class:** code + documentation mix (one description line in Go, one in packaging config, one README link fix, one deletion).
- **Verification gate:** build + module tests + claim-keyed grep sweep.
- **Attribution tier:** analysed on a live system — changed paths exercised (build/test/grep on the final committed tree) with retained output; the changed `kong.Description` string did not execute live in a bed, so the tier is honestly capped (not inflated).

Consumers of the docs-config contract are the named landings above (spec v0.2026254.503 already merged; charly `feat/docs-config-node`; plugin-docs catalog view; docs regeneration) — this PR is not a deferral of that work, it is the independent brand/link/removal cutover.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
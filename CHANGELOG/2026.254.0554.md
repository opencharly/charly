# 2026.254.0554 — chore: pin plugin-check to v2026.254.0445 (CALVER/BED seam)

## Summary

Pin the compiled-in `github.com/opencharly/plugin-check/candy/plugin-check` go.mod require from `v0.2026254.5` to **`v0.2026254.445`** (tag `v2026.254.0445`, the CALVER/BED seam).

This is the G-T4a pin from the approved plan: plugin-check #42 shipped `CHECK_RUN_DIR` and #43 shipped `${CALVER}/${BED}` in `checkRunVars` — both exist ONLY at that tag+. With charly pinned to it, `charly check run` bed runs expose `${CALVER}/${BED}` so capture steps can author `media/${CALVER}/${BED}` paths directly.

**sdk floor check:** the new tag's go.mod requires `sdk v0.2026252.834` (same as the previous tag — the task's predicted v0.2026254.439 floor did not match reality, verified from the module proxy). charly's tree already pins `sdk v0.2026253.1947 // indirect`, which is HIGHER — MVS keeps it; `go mod tidy` made no sdk move. Both are merged tags, Policy B holds. The old pin is replaced, not aliased (R5).

## How tested

All from the final committed tree (`663e8710`) of branch `feat/g2-pin-plugin-check-v2026.254.0445` off `origin/main` (10124871):

```
$ cd charly && go build ./...
BUILD_EXIT:0

$ go vet ./...
VET_EXIT:0

$ go test ./...
ok  github.com/opencharly/charly/charly  107.391s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  (cached)
TEST_EXIT:0

$ git status --porcelain
(empty — committed tree)
```

`go mod tidy` diff was surgical: go.sum swapped only the two plugin-check v0.2026254.5 entries for v0.2026254.445 (sum `h1:ula4bEqmxHMC/dSsrLrwyiHbFqBmF18C47ngi8l3XK0=` matched the proxy download).

## Rulebook compliance

- **Policy B (merged-tag pin):** v0.2026254.445 resolves from the proxy at `refs/tags/candy/plugin-check/v0.2026254.445`, commit e53b824417eafbc7cbf793a45e8ed729c4fc2e78, merged 2026-09-11T04:45:32Z. No PR branch, no unmerged pin.
- **R5 (delete legacy):** the v0.2026254.5 require and its go.sum entries are replaced, not aliased — one plugin-check require remains.
- **R6:** no force-push, no direct-to-main push; feat/ branch off current origin/main.
- **R10:** verified from the final committed tree (build + vet + test all green, zero failures).

## Change classification

- **Change class:** chore (go.mod dependency pin bump).
- **Verification gate:** `go build ./...` + `go vet ./...` + `go test ./...` all pass on the committed tree.
- **Attribution tier:** fully tested and validated.

*Assisted-by: pi OpenRouter DeepSeek V3.2 (fully tested and validated)*
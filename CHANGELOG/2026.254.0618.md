# 2026.254.0618 — chore: docs: config node in charly.yml + pin plugin-harness-kind (docs kind admission)

## Summary

Lands the **`docs:` config node** in `charly/charly/charly.yml` — the single configuration file for docs auto-generation per the spec docs-config contract (opencharly/spec v0.2026254.503, merged) — and pins the host-side kind admission it requires (opencharly/plugin-harness-kind, the compiled-in provider that admits corpus kinds; the docs word is now a registered ClassKind, exactly like skill/hook/marketplace — no special-casing, R3).

The 44-line docs node declares all generation knobs: sources (compiled policy + paths, release repos plugin-review/plugin-pipeline, extra repo plugin-gh), marketplace corpus path, the seven projection toggles, and the hand-authored output markers — defaults per the merged #DocsConfig contract.

- `charly/charly/go.mod`: spec v0.2026253.1700 → v0.2026254.503; sdk v0.2026253.1947 → v0.2026254.523 (merged tag of sdk PR #250, the docs-config contract companion); plugin-harness-kind v0.2026237.1423 → v0.2026254.600 (merged tag of plugin-harness-kind PR #4, the docs ClassKind word admission).
- Mirror tests for the docs kind admission: `TestDocsKind_CompiledInStackedNodes` / `TestDocsKind_RejectsInvalidBody`.

## How tested

From the final committed tree (branch feat/docs-config-node @ 3f5927d8, clean worktree):

- The previously-failing check — `go test ./internal/pluginsgen/ -run TestPluginsGenReproducible` — now **PASSES** (`candy/plugin-harness-kind/v0.2026254.600` resolves; the pinned pseudo-version refs from the intermediate commits do not exist in the remote and were the failure): `ok github.com/opencharly/charly/charly/internal/pluginsgen 29.301s`.
- Loader materialization suite on the final head: `TestEmbeddedDefaults_*`, `TestCueKinds_Corpus` (kind candy 12 / kubernetes 3 / local 27 / pod 12 / vm 29 real entities validated), `TestDocsKind_CompiledInStackedNodes`, `TestSkillKind_CompiledInStackedNodes`, `TestRunPluginKind_DecodesViaEnvelope` — **PASS** (34.051s).
- `go build ./...` and `go vet ./...` exit 0; `gofmt -l charly tools` clean.
- CI's own `go` check on the final head: **success** (run 103165382479).

## Rulebook compliance

- R3 — no special-casing: the docs word is admitted through the identical compiled-in ClassKind path as skill/hook/marketplace.
- Pin discipline — only merged refs: all three bumps (spec v0.2026254.503, sdk v0.2026254.523, plugin-harness-kind v0.2026254.600) resolve to org-published merged tags, verified against the origin remotes.
- Single-purpose cutover: the config node + its dependency pins, nothing else; CHANGELOG history untouched.
- PR-only landing via the org validator; no direct-main push, no force-push.

## Change classification

- **Change class:** configuration (docs: node) + dependency pins + mirror tests.
- **Verification gate:** module build/vet + the loader materialization suite (previously-failing check now green on the final head).
- **Attribution tier:** analysed on a live system — the loader path executed live with retained output on the final head (3f5927d8); the full docs regeneration against the real corpus is verified in the opencharly/docs landing.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
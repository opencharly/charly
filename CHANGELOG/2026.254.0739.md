# 2026.254.0739 — docs: correct go_mod_path in the docs config node (charly/go.mod)

## Summary

One-line correction in the `docs:` config node of `charly/charly/charly.yml`: `go_mod_path: charly/charly/go.mod` → `charly/go.mod`. The charly repo's module directory is `<repo>/charly/`, so the go.mod sits at `charly/go.mod` — the previous value made the config-driven generator abort with `read go.mod ...: no such file or directory` (ENOENT), which also breaks the docs deploy's generation (discovered live during the docs regeneration landing). The `compiled_plugins_path: charly/charly.yml` value is correct (the module-level config) and unchanged.

## How tested

From the pushed tree (branch feat/docs-config-path @ 5aeafc46d26e99347a5ee88b9ef14bf1f9a9357d):
- `git diff` = exactly the one line shown above; `git status` clean of debris.
- `cd charly && go build ./...` exit 0; `go vet ./...` exit 0 — the docs node still loads (its path is resolved only by the generator at generation time).

The end-to-end generation against this corrected config is the docs regeneration landing (opencharly/docs deploy.yml charly pin bumps to this charly main).

## Rulebook compliance

- R1 — the ENOENT was RCA'd to the one-segment-deep path and fixed at the source, both in this config and in the spec contract default (sibling PR); no workaround.
- R2 — named landings: the docs regeneration is the consumer; no bare follow-up framing.
- R6/Git — no force-push, no direct-main push, single-purpose one-line commit.

## Change classification

- **Change class:** configuration fix (one value in a declared config node).
- **Verification gate:** build/vet + the generator's own path resolution at the docs regeneration landing.
- **Attribution tier:** analysed on a live system — the failure was reproduced and the fix verified in the consuming generation run.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
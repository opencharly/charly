# 2026.254.1902 — weld: plugin-review v2026.254.1719 (validator timeout knob in every release)

## Summary

Bumps the welded-plugin release pin for `plugin-review` so every future charly release bundles the validator timeout-knob fix.

**ROOT CAUSE (R1, three layers):** the org `pr-validator` downloads a charly RELEASE's `charly-plugins-*.tar.gz` and runs the plugin-review it contains. Every charly release builds its welded plugins from `scripts/host-command-plugins.txt` — this file, the SINGLE SOURCE (R3) — which pinned `plugin-review@v2026.251.1928`, predating the knob fix (`v2026.254.1719`: `getenvAny("AI_REVIEW_ATTEMPT_TIMEOUT")` + `newLLMClient(..., timeout)` + the timeout-class taxonomy). With no timeout env passed by the workflow, review attempts hit the old hard deadline (`context deadline exceeded`) against the slower reviewer model → all attempts fail → **no verdict line** → the required `validate / validate` check fails → PRs unmergeable despite green builds (pod-immich-ml #5, charly, distro-cachyos #81 all affected).

- `scripts/host-command-plugins.txt`: `plugin-review@v2026.251.1928` → `plugin-review@v2026.254.1719` (one line).

## How tested (verbatim)

Built exactly as `release-binary.yml` does (clone at the tag; `GOWORK=off`; `t="."; [ -d "$d/cmd/serve" ] && t="./cmd/serve"`):

```
$ git clone --depth 1 --branch v2026.254.1719 https://github.com/opencharly/plugin-review.git /tmp/pr-src
$ cd /tmp/pr-src/candy/plugin-review
$ GOWORK=off GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -buildvcs=false -trimpath -o /tmp/plugin-review-bin ./cmd/serve
BUILD_RC=0   (31 MB static ELF)
$ strings /tmp/plugin-review-bin | grep -i AI_REVIEW_ATTEMPT_TIMEOUT
…AI_REVIEW_ATTEMPT_TIMEOUT…context deadline exceeded…   (GREP_RC=0)
   also: "the LLM provider did not respond within the attempt timeout (provider unanswered); this is NOT a review verdict"
```

**Discriminator (proves the RCA):** at the OLD pin `v2026.251.1928` → `grep -r 'AI_REVIEW_ATTEMPT_TIMEOUT\|defaultAttemptTimeout' candy/plugin-review` = **RC 1, zero hits**; at the new pin → `review.go:102 getenvAny(...)` + `llm.go:72 newLLMClient(..., timeout)` / `defaultAttemptTimeout` + `attempt_timeout_test.go`.

**Provider surface unchanged:** ran the workflow's `.providers` step (`charly __plugin-providers <candy-dir>`) at both pins → `verb:pr` / `command:review`, **byte-identical** (the bump changes only the binary payload, not the provider surface).

**Format/variant gate:** `go test -C charly -run 'TestPackaging|TestDistroRepoInstallDeclared|TestCharlyDevCandyDeclared|TestInlineCandySourceDirIsProjectRoot' .` → ok; `TestPackagingVariantsCoverWeldedPlugins` → PASS (plugin-review remains in both variants, `packaging/charly.yml:134/149` — names only, no tag there).

## Rulebook compliance

- R0 — skills loaded before the change.
- R1 — the no-verdict validator failure RCA'd to this release pin (with the old/new discriminator as proof) — fixed at the root, not worked around in a consumer.
- R2 — the remaining chain is NAMED and sequenced: (a) this merge → tag-on-merge cuts a new charly CalVer → `release-binary.yml` builds `charly-plugins-*.tar.gz` with the fixed plugin; (b) the `.github` `pr-validator.yml` bump (`TAG` → that release + `AI_REVIEW_ATTEMPT_TIMEOUT=600` in the review step) — the org-batch member this change unblocks; (c) re-run the validator on the parked PRs.
- R3 — the welded-plugin list is the single source; the release workflow reads it directly.
- R4 — no workaround: the release payload is fixed at its build source (no env shim, no consumer hack).
- R5 — hard cutover: the old pin is replaced, not shadowed.
- R6 - git safety: clean tree; single-purpose commit; no force-push/amend/main-push. Sweep for the removed pin, executed on the PR head (see the pasted evidence below): the only surviving `v2026.251.1928` references are the 5 historical hits in `CHANGELOG/2026.251.1947.md` (a point-in-time weld record - correctly untouched); zero live references remain.

Sweep evidence (executed on the PR head, verbatim):

```
$ git -C charly grep -n 'v2026\\.251\\.1928' 738ac65ca929b73baa14de5be20116a90b8ea15e
738ac65c:CHANGELOG/2026.251.1947.md:5
738ac65c:CHANGELOG/2026.251.1947.md:9
738ac65c:CHANGELOG/2026.251.1947.md:20
738ac65c:CHANGELOG/2026.251.1947.md:25
738ac65c:CHANGELOG/2026.251.1947.md:31
$ git -C charly grep -c 'v2026\\.251\\.1928' 738ac65c
CHANGELOG/2026.251.1947.md:5   # (5 hits, ALL in the historical CHANGELOG; zero in scripts/host-command-plugins.txt at the head)
```
- R7 — N/A (no schema-version↔tag coupling in this file; the pin is the release input).
- R8 — N/A (no harness-parity surface touched).
- R9 — version/source match proven by the build at the exact tag + the strings grep (the binary carries the knob).
- R10 — the changed path (the release build input) was exercised with the release workflow's own command and pasted output; zero warnings; no disposable charly target applies to a release-input pin (the build is the runtime proof, and the named `.github` bump completes the loop).
- RDD — the old-vs-new discriminator is the falsifiable evidence. ADE/SDD — the pin is the declarative release contract; the release workflow consumes it verbatim.

## Change classification

- **Change class:** release-input pin bump (welded plugin set).
- **Verification gate:** the release workflow's own build at the tag + the knob-present strings proof + the old/new discriminator + the packaging/variant tests (all pasted).
- **Attribution tier:** analysed on a live system — the build and greps ran live with retained output; the release artifact is produced by the named next step.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
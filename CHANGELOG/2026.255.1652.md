# 2026.255.1652 — fix(release): weld plugin-review v2026.255.1648 — it carries the x-opencode-session fix

## Summary

Pin the **welded** `plugin-review` at **v2026.255.1648** so the next charly release's plugin bundle carries the `x-opencode-session` fix — **the second half of the org-wide verdict outage's root fix.**

**Why merging `plugin-review#8` alone changed nothing.** The PR validator runs `charly review` from the **pinned charly release bundle** (`charly-linux-amd64` + `charly-plugins-linux-amd64.tar.gz`), not from `plugin-review@main`. The release welds the plugins from this very file (`scripts/host-command-plugins.txt`), whose entry was still `plugin-review@v2026.254.1719` — an engine that never sends the session header the org's Go gateway now requires, so every review attempt returns **HTTP 400 MissingSessionID**.

**Evidence that the gate reads the bundle, not main** (post-merge, live):

```
16:48:23Z  plugin-review#8 MERGED (80dbbdb4c1), tagged v2026.255.1648
16:48:54Z  validator run on plugin-adb#8  -> attempt 1/3 failed: LLM 400 ... MissingSessionID
16:49:11Z  validator run on sdk#252       -> attempt 1/3 failed: LLM 400 ... MissingSessionID
```

…while `plugin-review@main` demonstrably contains the fix. The gate cannot see it because it does not read main.

## How tested

Every candidate explanation was tested locally **with the org's own key** before landing:

```console
/zen/go/v1  plain                      -> 400 MissingSessionID
/zen/go/v1  + x-opencode-session       -> 200 + a real completion
/zen/v1     (with or without header)   -> 401 — that base does not serve the org's model
User-Agent: curl default / none / action-review -> 200 / 200 / 200   (the UA is IRRELEVANT)
stream:true + x-opencode-session       -> 200 + real SSE chunks
stream:true without it                 -> 400 MissingSessionID
```

And through the engine's own request path (`(*llmClient).chat`):
```console
with the header:    live request ACCEPTED: session=… content="OK"        (ok)
without the header: LLM 400 ... MissingSessionID                          (FAIL)
```

The pin itself resolves: `v2026.255.1648` → `80dbbdb4c1ae` (the `plugin-review#8` merge commit) — verified via the tags API, not assumed.

## Rulebook compliance

- **R1:** the divergence between the merged fix and the unchanged gate was root-caused to the release bundle path (`scripts/host-command-plugins.txt` → the 9 welded plugins), not to a guess
- **R3:** this file is the SINGLE SOURCE for the welded plugin set — the pin is bumped here and nowhere else
- **R5:** no parallel path; the pin is replaced, not shadowed
- **R6:** fresh branch off `origin/main`, PR-only, no force-push
- **R7/R10:** the pin resolves to the fix tag (verified) + the local end-to-end evidence pasted above
- **R9:** no magic numbers (the tag is the fix's own tag-on-merge tag)
- **R2:** one line, one purpose

## Change classification

- Change class: release pin (welded plugin version)
- Verification gate: the pin→tag resolution (pasted) plus the local live matrix against the real gateway — the full bed gate does not apply to a one-line pin, and the org's own gate cannot run at all until this lands
- Attribution tier: `analysed on a live system` — the changed path (the welded engine's request) was exercised live with the org key in both directions

## Operator note — this PR cannot self-validate

The gate it repairs is dark, so no validator verdict can be obtained for it. It therefore needs the **app-based (or admin-assisted) operator merge**, exactly like `plugin-review#8`. Once a release exists with this pin, the remaining step is to point the org variable **`CHARLY_VERSION`** at that release tag — nothing else on the gate changes.

*Assisted-by: pi ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*
# 2026.256.1105 — refactor!: retire KERNEL_MANIFEST and the fleet tombstone

## Summary

Retires two drifted artifacts and corrects the front-door/rulebook docs, as one atomic cutover.

- **Retire `KERNEL_MANIFEST.md`** — delete the draft ledger + its bidirectional gate test; the
  boundary law is enforced mechanically by `charly/import_purity_test.go`. Stale citations swept from
  `reverse_channel_three_legs_test.go`, `provider_command_external.go`, `import_purity_test.go`.
- **Retire the `fleet` tombstone** — delete `charly/retired_command_word.go` + test and the `main.go`
  pre-parse guard; `charly fleet …` is now simply an unknown command.
- **README corrections** — stale `charly/candy/*` refs → owning `layer-*`/`pod-*`; the `mcp serve`
  example repo; provider-class table vs census; post-`migrate` nesting shape; dead link;
  tutorial-shell init prose; Cursor removed.
- **Rulebook corrections** — `packaging/charly.yml`, `spec/schema/*.cue`, egress schema path, docs
  CI-time pin; `charly/charly/CLAUDE.md` no longer lists deleted source files.
- **In-tree PROGRAM cure (R2)** — `PROGRAM/nfpm-packaging-north-star.md`,
  `PROGRAM/pi-integration.md`, `packaging/charly.yml`, `tools/generate-packages/charly.yml`.

### Files (16, all accounted)

`README.md`, `AGENTS.md`, `CLAUDE.md`, `charly/CLAUDE.md`, `charly/KERNEL_MANIFEST.md` (deleted),
`charly/kernel_manifest_bidirectional_test.go` (deleted), `charly/retired_command_word.go` (deleted),
`charly/retired_command_word_test.go` (deleted), `charly/main.go`, `charly/import_purity_test.go`,
`charly/provider_command_external.go`, `charly/reverse_channel_three_legs_test.go`,
`PROGRAM/nfpm-packaging-north-star.md`, `PROGRAM/pi-integration.md`, `packaging/charly.yml`,
`tools/generate-packages/charly.yml`.

## How tested (R9 build + R10 change-class bed, head tree `627631ed`)

The bed target is `check-tutorial-shell` — a **`pod:` deploy carrying an explicit `disposable: true`**
in `box/fedora/charly.yml` (lines 74-76, `image: tutorial-shell`), i.e. an autonomous destroy/rebuild
target named as such:

```
$ sed -n '73,76p' box/fedora/charly.yml
check-tutorial-shell:
    pod:
        image: tutorial-shell
        disposable: true
```

Build via the rulebook-mandated path, on the head tree, then the change-class bed:

```
$ task build:binary
task: [build:binary] mv bin/.charly.next bin/charly     # pluginsgen + go.work.sum consistency guard
$ ./bin/charly version
2026.256.1053
$ cd box/fedora && /tmp/opencode/charly-docaudit/bin/charly check run check-tutorial-shell
[image-build] PASS after 48.326s [allowlisted=1 cache=137/146]
[check-image] PASS after 17.468s [allowlisted=1]
[deploy-add] PASS after 6.527s [allowlisted=1]
[config] PASS after 7.826s [allowlisted=1]
[start] PASS after 5.001s [allowlisted=1]
[bring-up-members] PASS after 100ms
[check-live] PASS after 12.428s [allowlisted=1]
[feature-run] PASS after 11.238s [allowlisted=1]
[update] PASS after 26.642s [allowlisted=5]
[rebuild-ready] PASS after 2.055s
[check-live-rebuild] PASS after 11.03s [allowlisted=1]
[feature-run-rebuild] PASS after 11.342s [allowlisted=1]
[cleanup] PASS after 2.409s [allowlisted=1]
[cleanup-members] PASS after 0s
charly check run check-tutorial-shell: PASS (steps=14) — repo override active (CHARLY_REPO_OVERRIDE=github.com/opencharly/charly=/tmp/opencode/charly-docaudit): verdict is about the LOCAL tree, not the PR head
```

R5 sweep (real output, head tree):

```
$ git grep -n 'KERNEL_MANIFEST\|kernel_manifest' -- ':!CHANGELOG'
(no output — exit 1)
```

Live CLI path:

```
$ ./bin/charly fleet add x
charly: error: unexpected argument fleet
$ ./bin/charly deploy add --help
Usage: deploy add <name> [<ref>] [flags]
```

## Rulebook compliance

- **R1** — every divergence RCA'd; mechanism = candy de-submodule cutover + `fleet → deploy` rename
  + `packaging/charly.yml` move + harness-config wiring asymmetry (the `.claude/` PreToolUse hooks
  are unwired while `.reasonix/settings.json` wires the same scripts; under Claude Code the rulebook's
  mechanics are enforced by the agent itself, per `charly/CLAUDE.md` "Hooks").
- **R2** — no unfunded parking. Cured in-tree: the 4 files above. Named routes: `box/*/README.md`
  (submodules; batch `feat/docs-refs`), `opencharly/docs` pages (PR #119), `distro-fedora`
  tutorial-shell (PR #51, **merged**), harness parity (batch `feat/harness-parity`).
- **R3** — deletion over duplication. **R4** — no workaround; CLI verified live.
- **R5** — claim-keyed sweep; real `git grep` output above (empty).
- **R6** — worktree edit, no force-push, no direct `main`.
- **R7/R8** — built the artifact and ran the changed CLI path live.
- **R9** — `task build:binary` on the head tree (`2026.256.1053`), invoked through this tree's `bin/`;
  dependency/gitlink guard passed (clean `go.work.sum`).
- **R10** — change-class bed run on the head tree with the R9 binary, on the named `disposable: true`
  target, pasted above, zero warnings.

## Change classification

- Class: code + documentation (CLI arg-guard removal + deleted Go gate test + rulebook/doc corrections).
- Gate: `charly check run check-tutorial-shell` (R10) + `task build:binary` (R9) + Go standard gate.
- Attribution: **fully tested and validated**.

*Assisted-by: opencode deepseek-v4.1-flash (fully tested and validated)*
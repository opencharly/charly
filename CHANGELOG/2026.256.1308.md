# 2026.256.1308 — docs: drop the residual plugin-group mention from registry_bootstrap

## Summary

Comment-only cleanup: `charly/registry_bootstrap.go` described `group (candy/plugin-group, C2-group)` as a COMPILED-IN authoring kind. `plugin-group` is not in `compiled_plugins:` and the `group` entity kind was removed by the member-tree cutover. Remove the group clause from both doc comments. No code change.

Part of the `feat/retire-group-kind` retirement: this is the charly surface; the siblings are **opencharly/docs#120** (corpus pin + concept page + regenerated census) and **opencharly/opencharly#203** (submodule mount + README row).

## How tested (real transcript)

```
$ grep -rn 'plugin-group' charly/
(no output — exit 1)                      # no residual reference in the charly module

$ cd /tmp/gtest && charly box validate    # fixture: an authored `group:` node
charly: error: command "validate": validation error: /tmp/gtest/charly.yml:doc0:
node "gtest": no kind discriminator — collections and plan steps live INLINE in
the kind value (the named child-node shape was removed); run: charly migrate
# → `group:` is not a recognized kind; the COMPILED-IN claim is false.

$ gofmt -l charly/registry_bootstrap.go
(empty)
$ (cd charly && go build ./...)
rc=0
```

## Rulebook compliance

- **R1** — the stale claim was proven false with the live `box validate` output above.
- **R2** — the retirement is split by owning repo and all three are open, named PRs (#608 here, docs#120, opencharly#203) — none parked.
- **R5** — sweep above (`grep -rn 'plugin-group' charly/` is empty).
- **R6** — `feat/` branch + PR.
- **R7/R8/R9/R10** — N/A — comment-only, no runtime path.

## Change classification

- Class: documentation-only (Go comment).
- Gate: non-runtime standards (gofmt + build, pasted above).
- Confidence: **documentation reviewed**.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*
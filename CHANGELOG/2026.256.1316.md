# 2026.256.1316 — docs: correct the .pi hook-wiring claim

## Summary

`.pi/README.md` claimed "Claude Code, reasonix, and kimi wire `.claude/hooks/*.sh`". That is false for this repo: the charly `.claude/settings.json` declares no `PreToolUse` hooks — only `.reasonix/settings.json` wires them (kimi delegates from `~/.kimi-code/config.toml`). The corrected prose matches the owning rulebook (`CLAUDE.md` "Hooks"). One hand-authored file; docs-only.

## How tested (real transcript)

```
$ grep -c '"hooks"' .claude/settings.json
0
$ grep -n 'PreToolUse' .reasonix/settings.json
3:    "PreToolUse": [
$ sed -n '44p' .pi/README.md
`.reasonix/settings.json` wires the gates, and kimi delegates to them from
$ grep -rn 'Claude Code, reasonix' .
./CHANGELOG/2026.231.1433.md:6:harnesses (Claude Code, reasonix, kimi) is a **hooks system**: the project
```

The only remaining hit is a CHANGELOG historical entry (permitted); no live doc still carries the stale claim.

## Rulebook compliance

- **R1** — D35 (a harness-config claim contradicting `CLAUDE.md` "Hooks") is corrected; RCA = the claim predated the charly `.claude/settings.json` de-wiring.
- **R2** — D35 was routed to this named batch (`feat/harness-parity`); this PR is that batch.
- **R3** — no duplication; the prose points at the rulebook rather than restating hook mechanics.
- **R5** — sweep above: only the CHANGELOG historical entry remains.
- **R6** — `feat/` branch + PR; no direct push.
- **R7/R8/R9/R10** — N/A — documentation-only, no runtime path.

## Change classification

- Class: documentation-only (`.pi/README.md` prose).
- Gate: non-runtime standards.
- Confidence: **documentation reviewed**.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*
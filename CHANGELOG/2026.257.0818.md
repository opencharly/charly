# 2026.257.0818 — chore(build): bump compiled plugin-build to v2026.257.0723 (project-cache fix)

## Summary

Bump the compiled-in `plugin-build` candy pin `v0.2026256.706` → `v0.2026257.723` (plugin-build#14, merged + tagged `v2026.257.0723`).

The bumped commit fixes the resolved-project cache KEY — the root cause of the eval-lane `charly check run: no entity "check-omarchy-pr-<N>-vm"` failures: the key hashed only the top-level `charly.yml` (plus dir + scan scope) with a 5-minute TTL, so a discover root rendered DURING a run (the eval lane's `pr-beds/**`) was invisible to it. The first resolve cached a skeleton envelope and every later resolve was served it. The key now folds the bytes of every manifest in the project tree (interpretation-free, `kit.DiscoverSkipDir` skip rule) and the TTL is removed (fail-closed: un-enumerable tree ⇒ no cache).

## How tested

Change class: config (compiled plugin pin bump). The changed runtime path is exercised on a live `disposable: true` target below.

**1. The pin is a MERGED, TAGGED commit (never a branch):**

```
$ git ls-remote --tags https://github.com/opencharly/plugin-build.git | grep 0723
c1cafffb0b4bc807d941c96a00eb267f4fcbd320	refs/tags/v2026.257.0723
$ git -C plugin-build log --oneline origin/main -2
8e5ceca docs: add CHANGELOG 2026.257.0723 for PR #14
c1cafff fix(build): content-address the resolved-project cache key over discovered manifests (#14)
```

**2. R9 — the worktree binary is REBUILT from this tree (`task build:binary`), and `charly version` matches source:**

```
$ task build:binary
task: [build:binary] CALVER=$(bash scripts/calver.sh)
cd charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=${CALVER}" -o ../bin/.charly.next .
task: [build:binary] mv bin/.charly.next bin/charly

$ ./bin/charly version
2026.257.0736
$ go version -m ./bin/charly | grep plugin-build/candy
	dep	github.com/opencharly/plugin-build/candy/plugin-build	v0.2026257.723	h1:wpjPPr09mEvz/+p7ED8sATPDApZJwyaJid8yj1pmUQA=
$ git status --short
 M charly/go.sum          # (the pin's stale checksums, pruned below)
```

`2026.257.0736` is `main.BuildCalVer`, stamped from this tree's HEAD commit date (deterministic). The embedded dep line proves the binary carries the merged tag from the proxy.

**3. Fix-the-diff (R5, B10): the removed pin's `go.sum` checksums are pruned in this commit** (`go mod tidy`):

```
$ grep -c 'plugin-build/candy/plugin-build v0.2026256.706' charly/go.sum
0
$ grep -c 'plugin-build/candy/plugin-build v0.2026257.723' charly/go.sum
2
```

**4. Module gate (raw):**

```
$ cd charly/charly && GOWORK=off gofmt -l .     # (empty)
$ GOWORK=off go vet ./...                       # (empty)
$ GOWORK=off go test ./...
ok  	github.com/opencharly/charly/charly	142.538s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	27.664s
```

**5. R10 — the full R10 bed on the rebuilt worktree binary (NO rebuild-skipping flag), on its `disposable: true` target.**

Target: `pr-beds/pr-10129-control/charly.yml` → `disposable: true`.

```
$ bin/charly check run check-omarchy-pr-10129-control
charly check run check-omarchy-pr-10129-control: [vm-build] PASS after 9.916s [allowlisted=1]
charly check run check-omarchy-pr-10129-control: [vm-create] PASS after 10.295s [allowlisted=1]
charly check run check-omarchy-pr-10129-control: [deploy-add] PASS after 33.814s [allowlisted=1]
charly check run check-omarchy-pr-10129-control: [bring-up-members] PASS after 0s
charly check run check-omarchy-pr-10129-control: [check-live] PASS after 16.725s [allowlisted=1]
charly check run check-omarchy-pr-10129-control: [gate-restart-stop] PASS after 11.36s [allowlisted=1]
charly check run check-omarchy-pr-10129-control: [gate-restart-start] PASS after 9.115s [allowlisted=1]
charly check run check-omarchy-pr-10129-control: [rebuild-ready] PASS
charly check run check-omarchy-pr-10129-control: [check-live-rebuild] PASS
charly check run check-omarchy-pr-10129-control: [cleanup] PASS
charly check run check-omarchy-pr-10129-control: [cleanup-members] PASS
charly check run check-omarchy-pr-10129-control: PASS (steps=11)
$ grep -c 'no entity' <log>
0
```

`summary.yml` records `ok: true` on all 11 steps, zero `failed`. This is the FULL R10 sequence (build → create → deploy → live → restart → fresh update → live again → cleanup).

**6. The root-cause reproduction on the same binary** (the cache key now sees the mid-run manifests):

```
$ rm -f ~/.config/charly/cache/project.json && mv pr-beds /tmp/opencode/bh
$ bin/charly status
skeleton: deploy=21 pr-beds=0
$ mv /tmp/opencode/bh pr-beds     # 32 beds now on disk
$ bin/charly status
after:    deploy=53 pr-beds=32     <-- fresh resolve: the new manifest content re-keyed and MISSED
after:    deploy=21 pr-beds=0      (the skeleton entry, now only an orphan key)
```

**Head diff:** `charly/go.mod` (+1/−1), `charly/go.sum` (+1/−1 after the tidy), 2 files.

## Rulebook compliance

- **R0** — the go + plugin skills were loaded.
- **R1** — the bumped commit's fix is root-caused in plugin-build#14 (the key missed the discover roots), reproduced live in §6.
- **R2** — the whole in-scope change is complete (pin bump + checksum prune in one commit); no deferred sibling.
- **R3** — one pin; no duplicated logic.
- **R4** — a merged CalVer tag from the proxy, never a branch.
- **R5** — the old pin is replaced AND its stale `go.sum` checksums are removed (`grep -c … 706` = 0 in §3).
- **R6** — `git status` was checked (§2 shows the single `charly/go.sum` change that this commit then tidies); `main` untouched; PR-only, no force-push.
- **R7** — the module gate output is pasted (§4) + the live R10 bed (§5).
- **R8** — `N/A`: no OCI-label / emitted-config change.
- **R9** — the worktree binary is built via `task build:binary` and `charly version` is pasted (§2); NOT `N/A`.
- **R10** — §5 is the full R10 bed on the rebuilt binary against the `disposable: true` target named above.

## Change classification

- **Change class:** config (compiled plugin pin bump).
- **Verification gate:** `task build:binary` + `go test`/`go vet`/`gofmt` + the full R10 `-control` bed.
- **Attribution tier:** fully tested and validated — the binary is rebuilt from this tree, the embedded dep is the merged tag, and the full R10 bed passes 11/11.

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (fully tested and validated)*
# 2026.260.2302 — docs(attribution): the model-free generator body form

## Summary of changes

Mirror of the merged A1 amendment (`opencharly/action-review#6`) into charly's own harness
rulebooks. The "AI Attribution" section in both `AGENTS.md` and `CLAUDE.md` stated only the
AI-authored form; it now also states the MODEL-FREE form a fixed CI generator's PR body must use:

- `AGENTS.md` — the AI Attribution section now states that a body emitted by a fixed, model-free
  CI generator (a committed `printf/echo` block with no LLM in the loop) ends with
  `*Assisted-by: <Harness> <Runtime> (<confidence>)*` (automation + runner identity, e.g.
  `GitHub Actions ubuntu-latest`), never a fabricated model name and never an `N/A` placeholder;
  it points at `/charly-internals:git-workflow` for detail.
- `CLAUDE.md` — the one-line equivalent.

This is the rulebook half of the mirror sweep the A1 amendment requires; the owning skill
(`layer-charly-internals`' `git-workflow`) and the validator agent spec
(`layer-charly-internals-extra`) landed their halves in their own repos.

**Closes / relates to:** mirrors `opencharly/action-review#6` (merged).

**Breaking change + rollback:** none — prose additions; revert restores the prior text.

**Files in the diff** (`git diff --stat origin/main...HEAD`):
```
 AGENTS.md | 7 ++++++-
 CLAUDE.md | 2 +-
 2 files changed, 7 insertions(+), 2 deletions(-)
```

## Change class

**Harness project configuration** (rulebook prose).

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** parse the changed configuration + the non-runtime standards
  (docs-only / rulebook prose).
- **Eval beds run:** `N/A — rulebook prose`.
- **Fresh rebuild:** `N/A — no binary`.
- **Did the CHANGED code path execute live?** `N/A — no code path`.

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output:* `N/A`.
*2. Each changed code path executing:* `N/A`.
*3. Docs-only evidence:*
```
$ git diff --stat origin/main...HEAD
 AGENTS.md | 7 ++++++-
 CLAUDE.md | 2 +-
 2 files changed, 7 insertions(+), 2 deletions(-)
```

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:skills` (rulebook-vs-skill split; mirroring surfaces) — this
  is the rulebook half of the mirror sweep.
- **R1 RCA + zero warnings:** RCA'd to the A1 amendment not being mirrored into the rulebooks.
- **R2 no out-of-scope:** same theme; the skill + agent-spec halves landed in their own repos.
- **R3 no duplication:** the mandate lives in the rulebooks and points at the ONE owning skill.
- **R4 no workaround:** the rulebooks now match the rulebook the gate enforces.
- **R5 hard cutover + grep:** the claim-keyed sweep of the AI-only attribution wording is applied
  here and in the skill/agent-spec repos.
- **R6 git safety:** `git status` checked; no force-push.
- **R7 runtime gate:** `N/A`.
- **R8 artifact invariants:** `N/A`.
- **R9 binary == source:** `N/A`.
- **R10 disposable + coverage:** `N/A — documentation change class`.
- **RDD / ADE / SDD:** RDD — the amendment's premise was verified by the two blocked generator
  PRs. ADE — unchanged. SDD — `N/A`.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law:** `N/A`.
- **Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go`.
- **CHANGELOG:** this repo keeps `CHANGELOG/`; tag-on-merge writes the entry from this body.

## Attribution tier

`documentation reviewed` — the whole diff is rulebook prose; the changed configuration parses and
the non-runtime standards pass.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*
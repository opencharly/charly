# 2026.265.0728 — docs(agents): add R7a — live or skip, never fake a live service

## Summary

Add **R7a — live or skip, never fake a live service** to charly's rulebook, beside R7 (the proof discipline). Any test, harness, or gate crossing a live-service boundary (`gh`/GitHub API, an LLM or provider endpoint, a network or `charly` call) must run against the **REAL** service, or **skip cleanly** when its credential/endpoint is absent — never a mock, stub, or fake of that boundary. A fake certifies the behaviour its author imagined, not what the service does, and hides a real integration break behind green.

The umbrella rulebook gained the same mandate (opencharly/opencharly#234, merged); this is charly's full-rulebook twin.

**R5 sweep.** Every R-list mirror in the repo carries R7a: `AGENTS.md`, `CLAUDE.md`, `.pi/extensions/charly-gates.ts` (the Pi-injected rules block), and `PROGRAM/pi-integration.md`.

## How tested

**Gate 1 — the R5 sweep (case-INSENSITIVE, so title-case and lowercase headings both match):**

```console
$ git grep -il 'R7a — live or skip' -- AGENTS.md CLAUDE.md .pi/extensions/charly-gates.ts PROGRAM/pi-integration.md
AGENTS.md
CLAUDE.md
.pi/extensions/charly-gates.ts
PROGRAM/pi-integration.md
```

**Gate 2 — the `.pi/extensions/charly-gates.ts` edit is a prompt string literal, and it is EXECUTED (extracted from the real function body and asserted):**

The added text sits inside `buildRulesBlock()`'s template literal — the condensed rules injected into the model's per-turn prompt. It changes prompt TEXT, not control flow, imports, or any gate the extension enforces. A node extraction of the actual returned string proves the rule reaches the injected block and the existing rules are intact:

```console
$ node -e '...extract buildRulesBlock() from .pi/extensions/charly-gates.ts...'
PRESENT R7a — Live or Skip
PRESENT R7 — Prove Behavior
PRESENT R8 — Preserve Emitted
exit=0
```

The mechanical gates in that file (the `git commit`/`git push` command interception) are untouched — `GATE_SCRIPTS` and its wiring are not in the diff.

**Gate 3 — the diff surface:**

```console
$ git diff --stat
 .pi/extensions/charly-gates.ts | 8 ++++++++
 AGENTS.md                      | 9 +++++++++
 CLAUDE.md                      | 1 +
 PROGRAM/pi-integration.md      | 8 ++++++++
 4 files changed, 26 insertions(+)
```

**Change class.** The `.md` edits are prose. The `.ts` edit is a **prompt string literal** (behaviorally inert — it changes injected rule text, not code paths), gated by the node extraction above rather than a fresh-rebuild bed. There is no runtime path in this change class to execute on a bed, so no `disposable: true` target applies.

## Rulebook compliance

- **R0 skills:** `/charly-internals:skills` + `/charly-internals:git-workflow` loaded.
- **R1 RCA:** operationalizes the failure class observed this session — a workflow harness that faked `gh`/`charly` certified a decision chain the real services need not honour.
- **R2 complete:** the mandate lands here; the per-surface procedure lives in the owning skill.
- **R3 no duplication:** each R-list mirror carries the same one-line rule; no behavior duplicated.
- **R4 no workaround:** this rule *forbids* fakes.
- **R5:** the case-insensitive sweep above shows every mirror updated in the same commit; no stale R7-only list remains.
- **R6 git safety:** `docs/` branch; no force-push; no direct push to `main`.
- **R7 gate:** Gates 1–3 above are run and pasted.
- **R8/R9/R10:** `N/A — no generated artifact, no binary, no runtime bed`; the changed `.ts` behavior (an inert prompt string) is executed via the node extraction in Gate 2.

## Change classification

- **Change class:** rulebook prose (`*.md`) + a prompt string literal in `.pi/extensions/charly-gates.ts` (behaviorally inert).
- **Verification gate:** the case-insensitive R5 sweep (Gate 1) + the executed prompt-block extraction (Gate 2) + the diff surface (Gate 3); no bed applies because there is no runtime path.
- **Attribution tier:** `analysed on a live system` — the changed `.ts` path was executed (its `buildRulesBlock()` output extracted and asserted) with retained output; no fresh-rebuild R10 bed exists for a prompt-text change.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*
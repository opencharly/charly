# 2026.265.0747 — docs(agents): drop the dangling 'owning skill' pointer from R7a

## Summary

Drop the dangling sentence `The per-surface how-to lives in the owning skill.` from the R7a bullet. R7a already states its own how — the boundary, the skip gate, the credential, the retained output — so a pointer to an unnamed owner is a needless deferral (and a validator flagged it on the sibling PR that merged before the trim landed).

Documentation-only, one line.

## How tested

```console
$ git diff --stat
 CLAUDE.md | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
$ git grep -c 'per-surface how-to' -- CLAUDE.md
0
```

## Rulebook compliance

- **R0 skills:** `/charly-internals:skills` loaded.
- **R1/R2:** the dangling pointer is fixed in place; the mandate is self-contained.
- **R3:** the mandate states its own how; no pointer to an unnamed owner remains.
- **R4/R5/R6:** no workaround; additive-to-corrective prose; `docs/` branch, no force-push.
- **R7 gate:** the grep above.
- **R8/R9/R10:** `N/A — documentation-only`.

## Change classification

- **Change class:** documentation-only (rulebook prose).
- **Verification gate:** the grep sweep.
- **Attribution tier:** `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*
# 2026.266.1609 — feat(deps): adopt the ArtifactStore spec + the merged engine-class plugins

## Summary

The **charly consumer leg** of the OCI-ArtifactStore cache cutover (opencharly/charly#632): adopts the merged spec/sdk + the merged engine-class plugin tags, makes the pin-forced in-tree changes, and proves the result with the real R10 bed.

**Pin bumps (all merged refs):**

| module | from | to |
|---|---|---|
| `spec` | `v0.2026262.957` | `v0.2026266.1306` (ArtifactStore #148 + cache-transfer wire types #149) |
| `sdk` | `v0.2026253-era` | `v0.2026266.1111` (DeriveRepoView #274) |
| `plugin-box` | `v0.2026251.1834` | `v0.2026266.1110` |
| `plugin-check` | `v0.2026255.1732` | `v0.2026266.1235` |
| `plugin-fleet` | `v0.2026250.829` | `v0.2026266.1111` |
| `plugin-pod` | `v0.2026250.546` | `v0.2026266.1109` |
| `plugin-substrate` | `v0.2026250.553` | `v0.2026266.1412` (probe cache → ArtifactStore) |
| `plugin-oci` | `v0.2026237.1426` | `v0.2026266.1239` (cache registry transport) |
| `plugin-cache` | `v0.2026248.730` | `v0.2026266.1435` (`charly cache push/pull`) |

**Pin-forced in-tree changes (no deferral):**

- `charly/deploy_add_cmd.go` — the nested container-exec jump is now the ONE `JumpContainerExec` arm with the engine as **DATA** (`NestedJump.Engine`); the per-engine `JumpPodmanExec`/`JumpDockerExec` arms were removed by the engine-class cutover (spec#144). Matches candy/plugin-box's merged box-load jump.
- Schema stamp `2026.249.2125` → `2026.261.1747` across the project + config + fixtures + packaging: `charly.yml`, `charly/charly.yml`, `charly/testdata/charly.yml`, `charly/testdata/flat-import-project/charly.yml`, `packaging/charly.yml`, AND the embedded manifests in six in-package Go test files: `charly/candy_source_dirs_override_test.go`, `charly/deploy_compile_parity_test.go`, `charly/git_layer_behaviors_test.go`, `charly/packaging_test.go`, `charly/plugin_dispatch_reverse_venue_test.go`, `charly/plugin_prescan_remote_test.go` (the spec pin forces the config schema everywhere it is stamped).
- `charly/main_repo_test.go` — the hermetic `--repo` cache fixture now writes the **v2 provenance sidecar** (spec#148 gates `IsRepoCached` on it), so the immutable-tag cache-hit fast path is exercised rather than a network fetch. Without this, the test silently left the offline path — a real fixture gap the pin surfaced.
- `tools/*` — adopt the shared sdk/spec pins (the `mods:tidy` sweep).

## How tested

`charly` is Go core. Its change-class gate is `go test ./charly/...` + the in-repo `tools/*` modules + a fresh-rebuild R10 bed whose kind matches the touched path (`check-substrate` — the loader/cache path the spec bump drives).

```
$ gofmt -l charly/            # empty
$ go vet ./charly/...         # clean
$ go test ./charly/...
ok  github.com/opencharly/charly/charly
ok  github.com/opencharly/charly/charly/internal/pluginsgen

$ for d in tools/*/; do GOWORK=off (cd "$d" && go test ./...); done   # all tools modules green
```

### R10 — fresh rebuild, disposable bed (executed)

The release binary was built from THIS tree (`BuildCalVer=2026.266.1900`), and the real bed driven against a migrated scratch project:

```
$ charly check run check-substrate
... [vm-build] PASS  [vm-create] PASS  [deploy-add] PASS  [bring-up-members] PASS
    [check-live] PASS  [rebuild-members-down] PASS  [update] PASS  [re-bring-up-members] PASS
    [rebuild-ready] PASS  [check-live-rebuild] PASS  [cleanup] PASS  [cleanup-members] PASS
charly check run check-substrate: PASS (steps=12)
```

`summary.yml`: **12 steps, all `ok: true`, errors: 0, warnings: 0, advisories: 0.** (Reproduced twice.)

### Live proof of the root-cause fix (the derive-not-mutate cache) — executed

A fresh remote fetch on this tree shows the pristine export is **never rewritten** while a schema-keyed derived view is materialized. Fetched repo and inspected cache path are the SAME (`plugin-example@main`):

```
$ charly --repo opencharly/plugin-example box list      # fresh fetch (cache cleared first)
Downloading github.com/opencharly/plugin-example:main...

$ R=~/.cache/charly/repos/github.com/opencharly/plugin-example@main
$ grep -m1 '^version:' "$R/charly.yml"
version: 2026.232.0520                                   # PRISTINE — unmutated
$ ls -d "$R".view.*                                      # the derived view
$R.view.2026.261.1747
$ grep -m1 '^version:' "$R.view.2026.261.1747/charly.yml"
version: 2026.261.1747                                   # DERIVED — the head schema
$ cat "$R.view.2026.261.1747/.charly-view-ok"
2026.261.1747                                            # completeness marker
$ cat "$R.ref"
{"version":2,"commit":"174e4ee2b43b4bdbf48c53a5fef3424f24384e09"}   # v2 provenance
```

This is exactly the fix for the poisoned-`@main` cache that failed `deploy-add` (schema 261 served to a 249 consumer): the loader now derives a schema-keyed view instead of rewriting the shared export, and the v2 provenance self-heals a legacy cache.

## Rulebook compliance

- **R0** — loaded `/charly-internals:go`, `/charly-internals:git-workflow`, `/charly-internals:strict-policy`.
- **R1** — the one pin-surfaced test failure (`TestCharlyRepo_FlagChdir`) was root-caused to a fixture gap (missing v2 provenance) and fixed in-tree, not dismissed; no warning survives the bed.
- **R2** — the whole pin fan-out + all forced in-tree changes are in this PR; nothing parked.
- **R3** — no duplication; the jump edit reuses the ONE `JumpContainerExec` arm the engine-class cutover established.
- **R4 / R4a** — no sleeps/retries/shim; the provenance seed reflects what a real fetch writes.
- **R5** — the removed identifiers are swept (executed command + output):
  ```
  $ grep -rnE 'JumpPodmanExec|JumpDockerExec' --include=*.go .
  (exit 1 — no matches)
  $ grep -rn 'node.Engine == "docker"' --include=*.go .
  (exit 1 — no matches)
  ```
- **R6** — `feat/spec-artifactstore-repin` off fresh `origin/main`; no force-push.
- **R7 / R7a** — coverage: the full `charly` suite + `tools/*` + the R10 bed; the live remote fetch is a real service.
- **R8** — no Containerfile/label change.
- **R9** — the binary is stamped (`2026.266.1900`) and built from this exact tree.
- **R10** — fresh-rebuild disposable bed `check-substrate` → **PASS (steps=12)**, 0 warnings, pasted above.

## Change classification

- **Change class:** Go core (dependency pins + pin-forced schema/jump migration) + config/testdata schema stamps.
- **Verification gate:** `gofmt` + `go vet` + `go test ./charly/...` + `tools/*` + `charly check run check-substrate`.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*
# 2026.268.0228 — chore(deps): bump plugin-task to v0.2026268.104 (the four maintenance verbs)

## Summary

Bump charly's compiled-in `plugin-task` pin from `v0.2026267.2043` (the task
runner only) to **`candy/plugin-task/v0.2026268.104`**, which adds the four
generic maintenance verbs (`git-submodules`, `file-parity`, `splice-region`,
`module-pins`) merged in `opencharly/plugin-task` PR #3.

This is the charly-side prerequisite for the umbrella task-surface cutover (P4):
the umbrella's `kind:task` entities compose those verbs to replace its
`Taskfile.yml` + `scripts/*.sh` maintenance scripts.

`charly/charly.yml` already lists `plugin-task` in `compiled_plugins:` (from the
C2 cutover) — only the module pin moves, so `charly/plugins_generated.go` is
unchanged; `go.sum`/`go.work.sum` are re-tidied.

## How tested

### Build + version

```
$ ./scripts/bootstrap-charly.sh
bootstrap-charly: regenerating compiled-in plugin wiring
bootstrap-charly: building bin/charly
bootstrap-charly: built ./bin/charly (2026.268.0031)
```

### Each of the four new verbs executed LIVE from the bumped pin (no `-replace`)

A real disposable git/go fixture (`/tmp/oc/vp`: a repo with a git-linked
submodule, a byte-identical file pair, a marked region, and a `tools/*` module on
a stale shared pin), driven by the binary built above:

```
$ ./bin/charly task v-git-submodules
task v-git-submodules: 1 step(s), 0 failed
  [pass] check — status lists sub

$ ./bin/charly task v-file-parity
task v-file-parity: 1 step(s), 0 failed
  [pass] check — the pair is identical

$ ./bin/charly task v-splice-region
task v-splice-region: 1 step(s), 0 failed
  [pass] run — splice
$ cat TARGET
h
BEGIN-REGION
new
END-REGION
t

$ ./bin/charly task v-module-pins
task v-module-pins: 2 step(s), 0 failed
  [pass] run — adopt and tidy
  [pass] check — the pins hold
```

### R10 — the task-surface bed still passes

```
$ ./bin/charly check run check-task
charly check run check-task: [check-live] PASS after 59.063s [allowlisted=1]
charly check run check-task: [update]     PASS after 34.774s [allowlisted=1]
charly check run check-task: [cleanup]    PASS after 48.009s [allowlisted=1]
charly check run check-task: PASS (steps=6)
$ cat /tmp/charly-task-smoke/marker
TASK-SMOKE-OK
```

### Full module gate

```
$ cd charly && go test ./...
ok  	github.com/opencharly/charly/charly	210.805s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	28.789s
$ gofmt -l charly tools      # empty
```

## Rulebook compliance

- **R0 skills:** `/charly-internals:plugin`, `/charly-internals:go`,
  `/charly-internals:git-workflow`, `/charly-check:check`.
- **R1 RCA:** a full-suite run surfaced
  `TestInvokeProvider_ProgressingLongCallIsNotKilled` failing with a FALSE idle
  kill. It is NOT dismissed: the mechanism (equal producer/watcher cadence with
  zero scheduling margin) was root-caused and fixed in-tree in its OWN PR —
  `opencharly/charly` **#658** (`fix(plugin): make the idle guard event-driven,
  not sample-driven`), an event-driven primitive that replaces the sampling race.
  This PR is a two-line dependency pin and does not touch that code; the fix is
  named and landing separately per the one-coherent-change rule.
- **R2 no out-of-scope:** a single pin bump; no code change.
- **R3 no duplication:** the verbs live once, in the plugin repo.
- **R4 no workaround:** the pin is a merged+tagged ref (never a PR branch).
- **R5 hard cutover:** the old pin is replaced, not kept.
- **R6 git safety:** `feat/` branch + PR; no force-push, no hook bypass.
- **R7 prove the change:** each of the four verbs dispatched live from the new
  pin; the R10 bed passed.
- **R8 artifact invariants:** `go.sum`/`go.work.sum` updated; regeneration is a
  no-op.
- **R9 binary == source:** the runs used the worktree-built binary.
- **R10 disposable + coverage:** `check-task` (disposable `host: local`) passed 6/6.
- **RDD:** the verbs were proven live in plugin-task PR #3 and are re-proven here.
- **Kernel/plugin boundary law:** no core change — a dependency pin only.
- **CHANGELOG:** `tag-on-merge` writes `CHANGELOG/<calver>.md` from this body.

## Change classification

- Change class: charly dependency pin bump (config).
- Verification gate: `go test ./...` + `go vet` + `scripts/bootstrap-charly.sh` +
  the four new verbs executed live + the fresh-build R10
  `charly check run check-task`.
- Attribution tier: `fully tested and validated` — each changed path (the four
  verbs, via the changed pin) executed live; the R10 bed passed.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*
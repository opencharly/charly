# 2026.268.0651 — feat(check): enable the plugin-native roster (VM caps, cli env, primaries mutex)

## Summary of changes

The charly-side legs of the plugin-native check-bed roster cutover (the roster itself is plugin-check#55; the namespace fixes are spec#154/#155 + sdk#281/#283; the VM-shape fix is plugin-check#56).

- `charly.yml` — cap every disposable VM bed at **`cpu: 2` / `ram: 2G`** (17 beds; read by plugin-vm's `vmShapeOverride` / the bed-node seed). Replace the stale `charly check run --all-beds` doc with the real `charly check run <roster>` spelling.
- `charly/host_build_cli.go` — `mergedChildEnv`: merge `spec.CliRequest.Env` into the child environment (the placement-invariant carrier for the bed session's isolation vars).
- `charly/node_desugar.go` + `charly/loader_threaded.go` — serialize `pluginPrimaries` (`sync.RWMutex`; `snapshotPluginPrimaries()` projects a copy per parse). A concurrent roster raced `concurrent map writes`.
- `charly/host_build_cli_test.go` — NEW `TestMergedChildEnv_OverridesAndInherits` (the file's `strings` import is pre-existing; no import change).
- `charly/node_desugar_test.go` — NEW `TestPluginPrimaries_ConcurrentAccessNoRace` (`-race`).
- `charly/check_bed_run_test.go` — `TestCheckBeds_…` → `TestBeds_…` (the one resolver).
- `charly/go.mod` / `charly/go.sum` — pin the MERGED producers (no `replace`; hermetic): `spec` `v0.2026268.642` (carries the `Beds()` cycle guard, spec#156), `sdk` `v0.2026267.2238`, `plugin-check` `v0.2026268.616` (roster + bed-shape), `plugin-vm` `v0.2026266.1235` (reads `vm create --cpus/--ram`).
- `check/eval-host-project/charly.yml` — migrate the stale guest fixture (schema `2026.240.1943` → `2026.261.1747`); version line only.
- (upstream) spec#156 — the `Beds()` mutual-import cycle guard; adopted via the spec pin (the charly suite's `TestImportNamespace_MutualCycle` was deadlocking without it).

**Closes / relates to:** opencharly/plugin-check#55/#56, opencharly/spec#154/#155, opencharly/sdk#281/#283.

**Breaking change + rollback:** bed VM shapes change (2cpu/2G). Rollback is a revert.

## Change class

`charly Go code` + candy config. Gate: `go test`/`go vet` + `bootstrap-charly.sh` + a live bed run.

## How R10-tested — exact gate, commands, and pasted output

- **The gate:** `go vet .`, `go test .` (+ `-race` for the concurrency piece), `./scripts/bootstrap-charly.sh` + `charly version`, then live `charly check run` on `disposable: true` beds.
- **Eval beds run:** (a) the committed VM bed `check-local-vm` (authored `cpu:2`/`ram:2G`); (b) a 3-bed bounded roster built from a THROWAWAY fixture project (a `check-roster` selecting `proof-a`/`proof-b` sharing `requires_exclusive: [proof-lock]` + an independent `proof-c`, all on the `docs-site-app` image; `lanes: 2`) — the fixture lives outside the repo (it is a demonstration, not shipped config; the shipped roster is the umbrella-root `charly.yml`).
- **Fresh rebuild:** `./scripts/bootstrap-charly.sh` → `bin/charly` `2026.268.0543`.
- **Did the CHANGED code path execute live?** Yes — the roster runner, the `pluginPrimaries` mutex, and the bed VM-shape seeding all ran live (pasted).
- **Concurrency:** the live 3-bed roster exercises the `lanes>1` pool + the shared-token serial chain (timeline below); the primaries race is unit-proven under `-race`.

*1. Gate output (verbatim):*

```
$ ./scripts/bootstrap-charly.sh
bootstrap-charly: built ./bin/charly (2026.268.0543)
$ ./bin/charly version
2026.268.0543
$ cd charly && go vet .
(clean)
$ go test . -run 'TestMergedChildEnv|TestPluginPrimaries_ConcurrentAccessNoRace|TestBeds_|TestVmCreateArgs'
ok  	github.com/opencharly/charly/charly	1.254s
$ go test . -timeout 600s          # FULL package suite
ok  	github.com/opencharly/charly/charly	164.498s
$ go test -race . -run 'TestPluginPrimaries_ConcurrentAccessNoRace' -v
=== RUN   TestPluginPrimaries_ConcurrentAccessNoRace
--- PASS: TestPluginPrimaries_ConcurrentAccessNoRace (0.00s)
PASS
ok  	github.com/opencharly/charly/charly	11.170s
```

*2. CHANGED run path executing LIVE — the roster, 3 beds, lanes=2, shared-token serial pair:*

```
$ PATH=$PWD/bin:$PATH ./bin/charly -C . check run check-roster-proof
charly check run check-roster-proof: roster — 3 bed(s) selected, 0 refused (host-local)
  PASS proof-c (exit=0)
  PASS proof-a (exit=0)
  PASS proof-b (exit=0)
charly check run check-roster-proof: roster PASS (3 beds, 0 skipped)
```

Serial ordering (verbatim line numbers, same file):

```
93:  proof-a: [cleanup] START
98:  proof-a: [cleanup] PASS
115: proof-b: [image-build] START   ← only after proof-a released proof-lock
```

*3. VM-bed cap proof — the `check-local-vm` VM bed ran FULLY GREEN (10/10 steps, zero warnings) at the authored 2G/2cpu:*

```
$ PATH=$PWD/bin:$PATH ./bin/charly -C . check run check-local-vm
[vm-build] PASS
[vm-create] PASS            ← domain memory=2097152 KiB (2 GiB), vcpu=2
[deploy-add] PASS
[bring-up-members] PASS
[check-live] PASS
[update] PASS
[rebuild-ready] PASS
[check-live-rebuild] PASS
[cleanup] PASS
[cleanup-members] PASS
charly check run check-local-vm: PASS (steps=10)
```

The domain shape (before teardown):

```
$ virsh -c qemu:///session dumpxml charly-check-local-vm | grep -E '<memory|<vcpu'
  <memory unit='KiB'>2097152</memory>
  <vcpu placement='static'>2</vcpu>
```

(`2097152 KiB = 2 GiB`; the template `eval-host-vm` declares 6 GiB — before the per-bed shape fix the domain reported `6291456`.)

Reaching green here also surfaced + fixed a **pre-existing stale guest fixture**: `check/eval-host-project/charly.yml` carried schema `2026.240.1943` while the current binary requires `2026.261.1747` (the guest's nested `charly check run check-local` failed at load: `schema … is required — run: charly migrate`). The fixture was stale on `main`; the version line was the only diff (`charly migrate` updates it). Fixed in this PR.

## Attribution tier

`fully tested and validated` — the changed roster/primaries/VM-shape paths all ran live (pasted), zero warnings.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:go`, `/charly-check:check`.
- **R1 RCA + zero warnings:** the `pluginPrimaries` race + the ineffective VM cap RCA'd and fixed; bed runs zero warnings.
- **R2 no out-of-scope:** the VM caps, env threading, race fix, and pins are all required for the roster cutover.
- **R3 no duplication:** the roster reuses the R10 engine; the primaries guard is one mutex+snapshot.
- **R4 no workaround:** the race is fixed with a mutex+copy, not serialization.
- **R5 hard cutover + grep:** the stale `--all-beds` doc is DELETED and replaced; the `run_cmd` `/verify-beds` redirect is deleted in plugin-check#55.
- **R6 git safety:** `git status` checked; main merged in (not force-pushed); no destructive action.
- **R7 runtime gate:** live roster + VM bed runs pasted.
- **R8 artifact invariants:** the roster `summary.yml` + the libvirt domain XML are the emitted artifacts, verified.
- **R9 binary == source + deps:** `./scripts/bootstrap-charly.sh` (CalVer-stamped).
- **R10 disposable + coverage:** only `disposable: true` beds ran; new tests ship for both new pieces.
- **RDD / ADE / SDD:** the VM-shape reader is single-sourced; no schema change.
- **Hard cutover:** one theme; no Phase 2 in scope.
- **Kernel/plugin boundary law:** the caps are config; the primaries guard is a core DATA-table synchronization.
- **Disposable-only autonomy:** only `disposable: true` beds ran.
- **Clean architecture + Go gates:** `gofmt` clean; `go test`/`go vet` green.
- **CHANGELOG:** `N/A` — post-merge by `tag-on-merge`.

---

*Assisted-by: opencode ollama-cloud deepseek-v4.1-flash (fully tested and validated)*
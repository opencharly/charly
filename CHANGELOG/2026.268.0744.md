# 2026.268.0744 — refactor(workflows): retire /verify-beds; use the native check-roster

## Summary of changes

Retire the superseded **agent-layer `/verify-beds` fan-out**, now that the plugin-native `kind:check-roster` roster engine owns roster runs.

- Delete `.claude/workflows/verify-beds.js` (the agent() sub-agent bed fan-out).
- `.claude/workflows/verify-status.js` — repoint **all four** `/verify-beds` mentions (the `meta.description` line + three comment/return-note lines) at the native `kind:check-roster` engine; the bed-safety classifier now lives in the engine, not a workflow.
- `.claude/workflows/audit-deploy-configs.js` — repoint its "for the destructive R10 bed gate use …" pointer at `charly check run <roster>`.

The roster engine (candy/plugin-check, merged in plugin-check#55/#56 + charly#656) enumerates disposable beds namespace-qualified, runs them on a bounded `lanes` pool, serializes `requires_exclusive`/`requires_shared` token groups, refuses host-local beds by default, and aggregates honest exit codes — the capabilities the workflow provided, now config-declared and plugin-native (single code path, R3).

The skill prose that DESCRIBED the workflow lives in the `layer-charly-check` and `layer-charly-internals` repos (their `skill:` entities generate the marketplace corpus); those repoints land as companion PRs (opencharly/layer-charly-check#23, opencharly/layer-charly-internals#20) and the generated marketplace/docs corpus is regenerated downstream.

**Closes / relates to:** supersedes the `/verify-beds` workflow from charly#656's roster cutover; companions layer-charly-check#23 + layer-charly-internals#20.

**Breaking change + rollback:** the `/verify-beds` workflow is removed; roster runs use `charly check run <roster>`. Rollback is a revert.

## Change class

Harness workflow scripts (`.claude/workflows/*.js`) — executable config (a deleted 197-line workflow + two edits).

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this class (harness workflow scripts):** parse the changed scripts + execute the changed control flow where it changed. The workflow files are SANDBOX scripts (top-level `return`, so `node --check` rejects them by design); the two edits are **string-literal + comment text only** (no control flow), and the deleted file has no execution path.
- **Eval beds run:** `N/A` — no runtime/bed behavior changes here; the roster engine's R10 is proven green in charly#656.
- **Fresh rebuild:** `N/A`.
- **Did the CHANGED code path execute live?** `N/A` — the diff is string/comment text + a deletion; no runtime path changed.
- **Concurrency:** `N/A`.

*1. The edits are string/comment-only (verbatim `git diff`, stripped):*

```
$ git diff origin/main -- .claude/workflows/verify-status.js .claude/workflows/audit-deploy-configs.js | grep -E '^[-+]' | grep -vE '^[-+]{3}'
-    '... use /verify-beds instead.',
+    '... use `charly check run <roster>` (a `kind:check-roster` entity) instead.',
-    '... The bed-safety classifier lives in /verify-beds (R3) and is not duplicated here.',
+    '... The bed-safety classifier ... now lives in the native `charly check run <roster>` roster engine ...',
-// "NEVER the sub-agent /verify-beds workflow for >600s beds"). Driving one anyway
+// long bed cannot be owned by an ephemeral agent). Driving one anyway
-// The bed-safety classifier lives in ONE place, `/verify-beds` (R3): delegate there
+// The bed-safety classifier lives in ONE place, the native `kind:check-roster` roster engine: delegate there
-    'The bed-safety classifier lives in /verify-beds (R3); this workflow does not duplicate it.',
+    'The bed-safety classifier lives in the native kind:check-roster roster engine (candy/plugin-check); this workflow does not duplicate it.',
```

Every hunk is a JS string literal or a `//` comment — no executable statement changes.

*2. Hard-cutover sweep over the WHOLE charly repo (verbatim):*

```
$ git grep -n 'verify-beds' -- .
CHANGELOG/2026.187.0747.md:1:...
CHANGELOG/2026.187.2027.md:1:...
CHANGELOG/2026.189.0042.md:1:...
CHANGELOG/2026.189.0207.md:1:...
CHANGELOG/2026.190.2227.md:9:...
CHANGELOG/2026.235.1106.md:1:...
CHANGELOG/2026.268.0651.md:1:...
```

Every surviving hit is **CHANGELOG history** (immutable; R5 keeps history in changelogs). No live/source reference remains: the workflow is deleted, its two siblings are repointed, and the skill prose that described it lives in the `layer-charly-*` repos (companion PRs). (`box/*` are submodules — separate repos; `.build/` is generated and gitignored.)

*3. The roster engine substitutes the workflow (proven live in charly#656):*

```
charly check run check-all-beds: roster — 127 bed(s) selected, 12 refused (host-local)
```

## Attribution tier

`analysed on a live system` — the substituted roster engine ran live and is proven green in charly#656; this diff itself is string/comment text + a deletion, verified by the pasted `git diff` (the workflow files are sandbox scripts with top-level `return`, so no parse-check applies). Not `documentation reviewed` (the diff deletes/edits executable `.js` config, not `*.md`).

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:agents` (workflows), `/charly-check:check` (the roster).
- **R1 RCA + zero warnings:** the workflow is superseded by the native roster (R3 duplication); no `node --check` run (the workflow files are sandbox scripts with top-level `return`, unparsable by `node --check` by design) — the diff is string/comment text + a deletion, verified by inspection.
- **R2 no "out of scope":** the deletion is in scope (the roster cutover's cleanup leg).
- **R3 no duplication:** the roster engine is the ONE roster implementation; the duplicate agent workflow is deleted.
- **R4 no workaround:** the native engine replaces the agent-layer fan-out.
- **R5 hard cutover + grep:** the whole-repo `git grep` above is pasted; only CHANGELOG history remains; the skill-source repoints are companion PRs.
- **R6 git safety:** `git status` checked; no destructive action.
- **R7 runtime gate:** `N/A` — no runtime path (string/comments repoints + a deleted workflow).
- **R8 artifact invariants:** `N/A`.
- **R9 binary == source + deps:** `N/A`.
- **R10 disposable + coverage:** `N/A` — no bed-affecting change.
- **RDD / ADE / SDD:** `N/A`.
- **Hard cutover:** one atomic commit.
- **Kernel/plugin boundary law:** `N/A`.
- **Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A` (no Go).
- **CHANGELOG:** `N/A` — post-merge by `tag-on-merge`.

---

*Assisted-by: opencode ollama-cloud deepseek-v4.1-flash (analysed on a live system)*
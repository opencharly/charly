# 2026.268.1324 — docs(task): rewire the charly Taskfile-era task references

## Summary

Sweep the Taskfile-era task-name residue the charly `charly task` cutover (#651)
left behind in charly's own prose and comments. This is an R5 stale-reference
sweep: documentation-only, no behavior change.

Every reference to the deleted surface is rewired:
- `AGENTS.md` + `CLAUDE.md` (both rulebook copies) and
  `charly/reserved_registry.go` now name `charly task cue-gen` — the **spec**
  repo's task that regenerates the CUE-owned Go, which is what those sentences
  actually mean;
- the `Taskfile-computed` comments in `charly.yml`, `charly/main.go` and
  `scripts/calver.sh` now name the bootstrap build (`scripts/bootstrap-charly.sh`).

Historical mentions in `CHANGELOG/` are left intact (they are history, not
guidance). Deliberate provenance comments that explain what the new surface
replaced — `charly.yml`'s task-section header, `scripts/bootstrap-charly.sh`'s
own header, `scripts/validate-agent-config.py`'s docstring — stay, because they
document the replacement accurately.

## How tested

`grep -rnE '\btask cue:gen\b|\btask (build:binary|build:install-portable|setup:all|mods:tidy)\b'
.` (excluding `CHANGELOG/`) returns nothing on the final tree.

```
$ cd .claude/worktrees/charly-task-docs && grep -rnE '\btask cue:gen\b|\btask (build:binary|build:install-portable|setup:all|mods:tidy)\b' . | grep -v '^./CHANGELOG/'
(empty)
```

The core module still builds clean (GOWORK=off from the `charly/` subdir):

```
$ cd charly && GOWORK=off go build ./...
(exit 0)
```

## Rulebook compliance

- **R5 (delete legacy completely):** every stale reference to the deleted
  Taskfile task names is updated in the same commit; only `CHANGELOG/` retains
  them as history.
- **R1 (RCA every divergence):** the sweep was driven by a claim-keyed grep of
  the removed surface, not a single sentence.
- **Documentation-only change class** — no runtime surface touched.

## Change classification

- **Change class:** documentation-only (prose + comments; zero behavior change).
- **Verification gate:** the claim-keyed R5 grep returns empty; `go build`
  clean.
- **Attribution tier:** `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*
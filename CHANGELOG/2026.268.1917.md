# 2026.268.1917 — chore(deps): bump spec/sdk/plugin-check/plugin-fleet for the RunEnv cutover

## Summary of changes

Bump the four cutover pins so the compiled-in plugins carry the check-bed roster's
per-invocation `RunEnv` isolation (plan §4.2 / F8):
- `spec` → `v0.2026268.1800` (spec#159: `RunEnv`, `ResolveBedForRoot`)
- `sdk` → `v0.2026268.1823` (sdk#287: required ctx on the member/deploy-family)
- `plugin-check` → `v0.2026268.1841` (plugin-check#58: retire bed `os.Setenv`
  isolation, namespace-aware bed refs)
- `plugin-fleet` → `v0.2026268.1845` (plugin-fleet#21: ctx to member
  bring-up/teardown)

charly core references these symbols only in comments — the plugins own the calls —
so this is a pin-only consumer leg. `go.work.sum` is regenerated by the workspace
build (its checksum lineage).

**Closes / relates to:** final consumer leg of the check-bed-roster cutover.

## Change class

`charly` Go (dependency pin bump; no source change).

## How R10-tested — exact gate, commands, and pasted output

**Disposable target named:** `charly.check-sidecar-pod` — a `disposable: true`
`pod:` deploy bed authored in `charly/charly.yml` with a deploy-level ephemeral VM
member (`check-sidecar-pod-ephvm`). It is the bed that exercises BOTH the
plugin-check roster/isolation path (bed setup → per-bed `RunEnv`; `box build` /
`check box` / `deploy add` / `config` / `start`) AND the plugin-fleet member path
(`bring-up-members` / `rebuild-members-down` / `re-bring-up-members` /
`cleanup-members`). The operator's real `~/.config/charly/charly.yml` is the
NON-disposable per-host overlay the bed must NOT write — the before/after key check
below proves it was untouched.

*1. Build + version (R9) — fresh from these pins:*

```
$ charly/scripts/calver.sh
2026.268.1846
$ (cd charly && GOWORK=$PWD/../go.work go build -buildvcs=false \
    -ldflags "-X main.BuildCalVer=2026.268.1846" -o ../bin/charly .)
$ ./bin/charly version
2026.268.1846
```

*2. Live bed — `charly check run check-sidecar-pod` (complete 17-step sequence):*

```
$ ./bin/charly -C charly check run check-sidecar-pod
[vm-build-check-sidecar-pod-ephvm] PASS after 13.025s [allowlisted=1]
[image-build] PASS after 1m5.621s [allowlisted=1 cache=133/141]
[check-image] PASS after 25.034s [allowlisted=1]
[deploy-add] PASS after 24.502s [allowlisted=1]
[config] PASS after 21.454s [allowlisted=1]
[start] PASS after 13.124s [allowlisted=1]
[bring-up-members] PASS after 1m33.143s
[check-live] PASS after 1m55.087s [allowlisted=1]
[feature-run] PASS after 21.003s [allowlisted=1]
[rebuild-members-down] PASS after 43.347s
[update] PASS after 3m20.047s [allowlisted=8]
[re-bring-up-members] PASS after 1m34.627s
[rebuild-ready] PASS after 545ms
[check-live-rebuild] PASS after 1m54.512s [allowlisted=1]
[feature-run-rebuild] PASS after 19.765s [allowlisted=1]
[cleanup] PASS after 14.517s [allowlisted=1]
[cleanup-members] PASS after 43.271s
charly check run check-sidecar-pod: PASS (steps=17)
```

All 17 steps in `summary.yml` carry `ok: true`; the only per-step diagnostic is the
allowlisted `dev-worktree-binary-notice` (the expected worktree-build warning).
No step failed, so zero errors / zero warnings.

*3. The disposable target is `disposable: true` (charly/charly.yml):*

```
check-sidecar-pod:
    pod:
        image: check-k8s-deploy-app
        disposable: true
        lifecycle: dev
```

*4. Issue-7 proof — the operator's real overlay is untouched:*

```
before: keys == {version, cache, system}
after : keys == {version, cache, system}
$ grep -cE '^(check-sidecar-pod|vm:check-sidecar-pod|ledger):' ~/.config/charly/charly.yml
0
```

*5. Final head + diff-stats:*

```
$ git log --oneline origin/main...HEAD            # code commits (re-freeze commits omitted)
58e21541 chore(deps): bump spec/sdk/plugin-check/plugin-fleet for the RunEnv cutover

$ git diff --stat origin/main...HEAD
 charly/go.mod | 8 ++++----
 go.work.sum   | 8 ++++++++
 2 files changed, 12 insertions(+), 4 deletions(-)
```

## Rulebook compliance

- **R1** — the pin move follows the RCA'd cutover; no unexplained drift.
- **R2/R5** — the consumers' required-ctx API leaves no fail-open path; the pins
  are the merged producers.
- **R3** — pinned producer tags, not branches.
- **R4** — no workaround.
- **R6** — `git status` was clean before the disposable bed teardown/cleanup.
- **R7/R7a** — the live bed exercised the compiled-in plugins (no mock).
- **R8** — no emitted artifact changed (operator overlay identical).
- **R9** — rebuilt + `charly version` matches the CalVer stamp.
- **Umbrella** — this is the submodule (charly) change; the umbrella records the
  gitlinks via `task sync` after merge.

## Change classification

- **Change class:** dependency pin bump (`charly`), no behavior change beyond the
  adopted merged producers.
- **Verification gate:** build + `charly version` + live
  `charly check run check-sidecar-pod` (PASS, 17 steps, 0 errors/warnings) on the
  explicit `disposable: true` bed.
- **Attribution tier:** `fully tested and validated` — the final tree built and the
  changed (compiled-in plugin) paths executed live in a disposable R10 bed, with the
  full output pasted.

*Assisted-by: opencode deepseek-v4.1-flash (fully tested and validated)*
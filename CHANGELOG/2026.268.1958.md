# 2026.268.1958 — docs: point charly's rulebook at the umbrella session model (+ drop the dead worktrees ignore)

## Summary

Make charly's rulebook point at the umbrella's development model instead of carrying
its own contradictory, harness-named session doctrine — and remove the now-dead
`.claude/worktrees/` path it mandated, in the same phase.

Three files:

- **`AGENTS.md`** — replaces the two copies of the worktree doctrine (the Hard-Cutover
  paragraph and the Post-Execution-Policies reference) with a single pointer to the
  umbrella's "The development model". Charly's rulebook now owns only charly's
  **technical** rules (R0–R10 substance, Go gates, beds, schema, the boundary law);
  workflow (session root, worktree layout, landing, pin discipline) is owned by the
  umbrella `AGENTS.md`.
- **`CLAUDE.md`** — a symlink to `AGENTS.md` (it was a hand-synced 36KB mirror that had
  already drifted by 339 lines; one rulebook removes the drift class, R3/R5).
- **`.gitignore`** — drops the `.claude/worktrees/` entry and its rationale comment.
  Nothing creates that directory once the doctrine above is gone, so the entry is dead
  config; removing it is the R5 completeness half of the same rename.

The workflow model this rulebook points at is delivered by **opencharly/opencharly#256**
(the umbrella companion PR, open in this same session).

## Base

The branch is merged onto `origin/main` at `9e206e7d`, which already includes #665's
contract-pin repair (`d50aa3cc`). The `go build`/`test` output below therefore runs on a
tree that contains that repair — the earlier "core module cannot build" state no longer
applies to this base and is not claimed here. This diff touches no module file, so it is
neither the cause nor the fix of the pin issue (that was #665).

## How tested

Change class: **documentation + a non-behavioral config sweep**. Non-runtime standards,
run on this branch:

```
$ go build ./...
build exit=0

$ go vet ./...
vet exit=0

$ go test ./...
ok  	github.com/opencharly/charly/charly	186.141s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	29.286s
test exit=0

$ bash scripts/bootstrap-charly.sh
bootstrap-charly: built ./bin/charly (2026.268.1950)
$ ./bin/charly version
2026.268.1950
```

**R5 claim-keyed grep self-test** — the sweep is complete: no `.claude/worktrees`
reference survives outside historical CHANGELOG.

```
$ git grep -n 'claude/worktrees' -- . ':!CHANGELOG'
git grep exit=1 (1 = no matches)

$ git grep -l 'claude/worktrees'
CHANGELOG/2026.225.1114.md
CHANGELOG/2026.225.1409.md
CHANGELOG/2026.235.1106.md
CHANGELOG/2026.260.1303.md
CHANGELOG/2026.268.1324.md
```

Final tree:

```
$ git diff --stat origin/main...HEAD
 .gitignore |   8 ---
 AGENTS.md  |  45 +++++++------
 CLAUDE.md  | 221 +------------------------------------------------------------
 3 files changed, 27 insertions(+), 247 deletions(-)
```

## Rulebook compliance

- **Rule 1 (never edit inside a submodule):** upheld — the charly half lives here, in the
  charly repo, edited in this session's own worktree.
- **Rule 2 (submodule git via `git -C`):** upheld.
- **Rule 3 (no nested `go.work`):** N/A — no `go.work` added.
- **Rule 4 (session worktree model):** this PR removes charly's copies of the model and
  points at the umbrella's; all work was done in `.worktrees/umbrella-dev-model/`.
- **Rule 5 (pin only merged refs):** no gitlink touched; the umbrella bump follows #256.
- **Rule 6 (git safety):** on `chore/umbrella-session-model`; no force-push, no direct
  push to `main`. The base was advanced by a merge of `origin/main` (not a rebase).
- **Rule 7 (prove the gate):** the applicable gate ran on the branch with output pasted.
- **Rule 8 (harness config):** N/A — no harness config changed; `task harness` passes in #256.
- **Rule 9 (session ownership):** the tracked `charly/` checkout is another session's and
  was left exactly as found.
- **R1 (RCA every anomaly):** the earlier base's `go` failure was root-caused to the #663
  bump and fixed on main by #665; this branch carries no such failure.
- **R2 (finish the whole cutover):** doctrine + its `.claude/worktrees/` config entry are
  removed together; the model is delivered by the named #256.
- **R3 (no duplication):** the worktree doctrine now lives once, in the umbrella.
- **R4 (no workarounds):** none.
- **R5 (delete legacy completely):** the grep self-test above proves no non-CHANGELOG
  reference survives.
- **R6/R7:** covered above.
- **R8 (preserve emitted artifacts):** N/A — no emitted artifact produced here.
- **R9 (binary equals source):** covered above (worktree-built binary, version-checked).
- **R10 (fresh disposable proof):** N/A for this class — no runtime/disposable target in
  scope.

## Change classification

- Change class: **documentation + a non-behavioral config sweep** (dead ignore-entry
  removal; no code, no build, no runtime behavior).
- Verification gate: `go build/vet/test` + the worktree binary build.
- Attribution tier: **documentation reviewed**.

## Authoring-runtime identity (attribution evidence)

The authoring runtime is OpenCode on the `ollama-cloud` provider, model ID
`ollama-cloud/deepseek-v4.1-flash`, resolved from the provider's own registry
(`opencode models --provider ollama-cloud`).

*Assisted-by: OpenCode DeepSeek V4.1 Flash (documentation reviewed)*
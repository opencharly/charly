# 2026.269.0721 — feat: relocate the artifact-owner check beds out of charly (v3)

feat: relocate the artifact-owner check beds out of charly (v3)

## Summary

Rebuild of the tree of the closed `opencharly/charly#643`
(`feat/relocate-check-beds-v2`, head `bc963787`) onto **current `main`**
(`5756335`, CHANGELOG 2026.269.0131). Predecessors `#625` / `#643` auto-closed at
the validator's 5-BLOCK threshold. This round addresses the validator's three
findings on v3's first head (`3e206a0`): the `source: builtin` failure is now
routed to a named owner (`charly#672`), the four closed-destination nodes are
**kept**, and this body pastes real per-bed output.

The change relocates the artifact-owner check beds out of `charly.yml` into the
repo that owns the artifact each one tests. `charly.yml` is **+10/−932**
(`myers` and `patience` agree); three comment-only Go edits:
`charly/unified_targets_test.go` (+2/−2), `charly/plugin_scope_test.go` (+1/−1),
`charly/cue_kinds_corpus_test.go` (+2/−2).

**What is removed (26 nodes; 0 nodes added).** Name → kind:

```
arch-repo-vm                 vm       check-gpu-app                local
fedora-repo-vm               vm       check-gpu-local              local
debian-repo-vm               vm       check-bpf-local              local
ubuntu-repo-vm               vm       check-bpf-local-app          local
check-substrate-app          local    check-cardwire-local         local
check-substrate              vm       check-cardwire-local-app     local
check-exampledeploy          exampledeploy
check-arch-repo              vm       check-feature-local          local
check-fedora-repo            vm
check-debian-repo            vm
check-ubuntu-repo            vm
check-udev-app               local
check-udev-local             local
check-preempt-app            local
check-preempt-local          local
check-migrate-app            local
check-migrate-local          local
check-doctor-app             local
check-doctor-local           local
```

**The 14 relocated beds and their owners** (every destination PR is MERGED, so
the owning repo's `main` carries the bed):

| Owning repo | Bed | Owning PR |
|---|---|---|
| charly-arch | `check-arch-repo` + `arch-repo-vm` | `charly-arch#7` (MERGED) |
| charly-fedora | `check-fedora-repo` + `fedora-repo-vm` | `charly-fedora#5` (MERGED) |
| charly-ubuntu | `check-ubuntu-repo` + `ubuntu-repo-vm` | `charly-ubuntu#6` (MERGED) |
| charly-debian | `check-debian-repo` + `debian-repo-vm` | `charly-debian#5` (MERGED) |
| plugin-substrate | `check-substrate` + `check-substrate-app` | `plugin-substrate#12` (MERGED) |
| plugin-example-deploy | `check-exampledeploy` | `plugin-example-deploy#4` (MERGED) |
| plugin-udev | `check-udev-local` + `check-udev-app` | `plugin-udev#4` (MERGED) |
| plugin-preempt | `check-preempt-local` + `check-preempt-app` | `plugin-preempt#7` (MERGED) |
| plugin-migrate | `check-migrate-local` + `check-migrate-app` | `plugin-migrate#9` (MERGED) |
| plugin-feature | `check-feature-local` | `plugin-feature#6` (MERGED) |
| plugin-doctor | `check-doctor-local` + `check-doctor-app` | `plugin-doctor#6` (MERGED) |
| plugin-gpu | `check-gpu-local` + `check-gpu-app` | `plugin-gpu#4` (MERGED) |
| plugin-bpf | `check-bpf-local` + `check-bpf-local-app` | `plugin-bpf#4` (MERGED) |
| plugin-cardwire | `check-cardwire-local` + `check-cardwire-local-app` | `plugin-cardwire#3` (MERGED) |

The shared `check-feature-app` template **stays** — still deployed by
`check-agent-local` / `check-preflight-{sandbox,local}`.

**Kept in `charly.yml` (coverage never dropped).** Every other node stays:

- **`docs-site-app` + `check-docs`** (`layer-docs-site#3`, **CLOSED unmerged**)
  and **`marketplace-app` + `check-marketplace`** (`plugin-marketplace#12`,
  **CLOSED unmerged**): the destination beds are **not in the owning repos' `main`**
  (verified: neither repo's `charly.yml` contains the node, and neither repo has an
  open PR). A closed PR's branch head is not a landed destination, so these four
  nodes **stay**. (Validator finding on head `3e206a0` #2 — fixed: the first head
  removed them; this head restores them byte-identical to `origin/main`.)
- **`check-agentteams-pod` + `check-agentteams-snapshot`** (`layer-agentteams#5`,
  OPEN): their destination is **RED at the shipped binary** — the owning branch
  pins `plugin-deploy-pod/candy/plugin-deploy-pod:v2026.250.1020`, whose
  `prepareVenueState` persists `image: check-agentteams-pod` (the deploy NAME)
  instead of the overlay `agentteams:<tag>`, so `charly config` fails `image not
  found in local storage`. Controlled A/B, only the pin differing (R10 §6):
  `v2026.250.1020` → `check-agentteams-pod` exit 1 at `config`;
  `v2026.266.1057` (the provider the shipped release bundles) → `PASS (steps=14)`.
  Per B12/R2 a bed is not removed while its destination is red.
- **`check-structkind` + `check-structkind-app`**: teardown is `deploy del`-rooted
  on an external plugin-kind top node, hitting the cross-process ledger-lock
  deadlock owned by **`opencharly/charly#627`** (OPEN).
- **The alpine trio** `check-alpine-repo` + `alpine-repo-box` + `sudo-nopasswd`,
  and **the dsh pair** `check-dsh-pod` + `dsh-app`: routed to the named batch
  cutover **`opencharly/charly#644`** (OPEN) — `charly-alpine#3` is OPEN and not
  green; `pod-dsh#3` was CLOSED unmerged.
- **`check-agentteams-vm` + `agentteams-vm` + `agentteams` +
  `agentteams-manager` + `agentteams-worker`**: the VM destination cannot run
  green on current tooling (routed to `layer-agentteams#4` +
  `plugin-deploy-vm#23`), matching `layer-agentteams#5`, which relocates the pod +
  snapshot beds only.
- Every other node in `charly.yml` (the VM/pod/group/local mechanism beds,
  `charly-cli`, `default`, main's `task:`/`mods-tidy`/`push` nodes, and main's new
  `check-kindcluster` pair) is untouched and byte-identical to `main`.

**Accounting for every `+` line — 10 genuinely-new lines, all comments.** `comm`
over the top-level node names of `origin/main:charly.yml` vs `HEAD:charly.yml`
shows **0 nodes added, 26 removed**; `myers` and `patience` both report `10 932`.

```
$ git diff --diff-algorithm=myers    --numstat origin/main...HEAD -- charly.yml
10	932	charly.yml
$ git diff --diff-algorithm=patience --numstat origin/main...HEAD -- charly.yml
10	932	charly.yml
$ comm -13 <(git show origin/main:charly.yml | grep -E '^[a-zA-Z0-9_-]+:$' | sort -u) \
           <(grep -E '^[a-zA-Z0-9_-]+:$' charly.yml | sort -u) | wc -l   # added nodes
0
$ comm -23 <(git show origin/main:charly.yml | grep -E '^[a-zA-Z0-9_-]+:$' | sort -u) \
           <(grep -E '^[a-zA-Z0-9_-]+:$' charly.yml | sort -u) | wc -l   # removed nodes
26
```

**The 10 genuinely-new lines** (`--diff-algorithm=patience`), **all comments**
(verified line-by-line against `origin/main` — none is present there):

```diff
+    # The published-repo install beds that lived here — check-arch-repo /
+    # check-fedora-repo / check-debian-repo / check-ubuntu-repo and their minimal
+    # cloud-image VM templates — moved to their owning packaging repos: the repo
+    # that publishes the package repo owns the bed. check-alpine-repo (a `pod:` bed
+    # on alpine-repo-box) stays until its own packaging repo's bed is green.
+# references it (check-builder-vm / check-group-vm / check-structkind-vm) gets
+# check-structkind, but each gets its OWN domain (charly-check-builder-vm here) +
+# venue (kit.NestedExecutor).
+            Minimal kind:local template for the check-agent-local / check-preflight beds — composes the
+        # The distro tag chain is load-bearing: the
```

Note on the 9th line: it is a **`description:` scalar continuation** (a `|-`
block scalar line), not a standalone `description:` key — the enclosing
`check-feature-app:` node's description was updated to name its surviving
consumers (`check-agent-local` / `check-preflight-{sandbox,local}`) after
`check-feature-local` (its former consumer) moved out. The earlier #643 body's
"10 genuinely-new lines, all comments" list was imprecise about it; this body
states exactly what it is.

**Comment-only Go edits (dangling-reference sweep):** `unified_targets_test.go`
drops `check-substrate's check-substrate-member` from the
`TestPluginDeployTarget_ApplyParentExecOverride` example list;
`plugin_scope_test.go` drops the `(check-exampledeploy)` example;
`cue_kinds_corpus_test.go` drops the `check-exampledeploy ->` example.

## Change class

**candy/box-config** (`charly.yml`, a manifest removal) **+ comment-only Go**
(three test files). Not docs-only, so the reduced manifest is proven live
(`charly box validate` + an in-tree bed) and each relocated bed's own
fresh-rebuild R10 is pasted from its owning repo, run against the binary this
tree ships.

**Relates to:** the relocation companions `charly-arch#7`, `charly-fedora#5`,
`charly-ubuntu#6`, `charly-debian#5`, `plugin-udev#4`, `plugin-preempt#7`,
`plugin-migrate#9`, `plugin-feature#6`, `plugin-doctor#6`, `plugin-gpu#4`,
`plugin-bpf#4`, `plugin-cardwire#3`, `plugin-example-deploy#4`,
`plugin-substrate#12`; the destination for the two kept agentteams beds
`layer-agentteams#5`; the named residual batch **`opencharly/charly#644`** for
the kept `check-alpine-repo` / `alpine-repo-box` / `sudo-nopasswd` /
`check-dsh-pod` / `dsh-app` nodes; and **`opencharly/charly#672`** for the
`source: builtin` pin bump (see R10 §1).

**Breaking change + rollback:** none for the manifest — removals only, no shim or
alias. Rollback is a revert of this PR; the owning repos' companion PRs restore
the beds.

## How R10-tested

### 1. `charly box validate` on the final tree — with the `source: builtin` divergence routed

```
$ CHARLY_PLUGIN_DIR=… /tmp/opencode/charly268/charly-linux-amd64 box validate
charly box validate: OK — checked 612 candies, 129 boxes, 42 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

`v2026.268.1958`'s plugin bundle validates this tree clean. The **current-main
release `v2026.269.0131`** `box validate` reports CUE errors
(`plugin.source: invalid value "builtin"`). RCA, with the named owner — not a
"pre-existing" dismissal:

`opencharly/spec#163` (MERGED 2026-09-25) retired the `source: builtin`
sentinel from `#Plugin`; `source:` is now `#GithubRef` with no default. `main`
adopted that spec pin (`charly/go.mod` → `spec v0.2026268.2214`) but still pins
the two consumer candies that declare the retired sentinel
(`plugin-dsh v0.2026266.2321`, `plugin-agentteams v0.2026237.1414`). `spec#163`
named the consumer follow-ups; their replacements are **OPEN**:
`opencharly/plugin-dsh#7` and `opencharly/plugin-agentteams#5`. The charly-side
pin bump is tracked as **`opencharly/charly#672`** (opened this session). The
failure reproduces identically on unmodified `main` `5756335`:

```
$ # on THIS tree, and identically on unmodified origin/main 5756335
… plugin.source: invalid value "builtin" (out of bound =~"^github\\.com/[^/]+/[^/]+(/.+)?$")
  … plugin-dsh@v2026.239.1623…/candy/plugin-dsh/charly.yml:28:13
  … plugin-agentteams@v2026.237.1414…/candy/plugin-agentteams/charly.yml:29:1
```

`check run` takes a different loader path and is unaffected (all 14 beds below
pass on `v2026.269.0131`).

### 2. Go gate on the touched package — `gofmt` / `go build` / `go vet` / tests

```
$ gofmt -l charly/cue_kinds_corpus_test.go charly/plugin_scope_test.go charly/unified_targets_test.go
(empty)

$ CALVER=$(bash scripts/calver.sh); go build -buildvcs=false -ldflags "-X main.BuildCalVer=$CALVER" -o /tmp/opencode/charly-v3-fix .
build exit=0
$ /tmp/opencode/charly-v3-fix version
2026.269.0644

$ go vet .
vet exit=0
$ go test -run 'TestCueKinds_Corpus|TestCollectReferencedPluginWords_Scoping|TestPluginDeployTarget_ApplyParentExecOverride' .
ok  	github.com/opencharly/charly/charly	31.678s
```

### 3. Removal proof — 0 nodes added, 26 removed, no dangling reference

```
$ # no surviving from:/base:/builder:/image: reference to any of the 26 removed nodes
$ for n in $(comm -23 <(git show origin/main:charly.yml | grep -E '^[a-zA-Z0-9_-]+:$' | sort -u) \
                     <(grep -E '^[a-zA-Z0-9_-]+:$' charly.yml | sort -u)); do
    grep -nE "(from|base|builder|image):[[:space:]]*$n[[:space:]]*$" charly.yml
  done
(none)
$ # R5 grep self-test — the removed ids are gone from Go too
$ grep -rn 'check-substrate\|check-exampledeploy\|check-agentteams-pod' --include='*.go' .
(none)
```

### 4. In-tree representative bed, full R10 — on the tree's OWN binary

Built from THIS branch (`charly version` → `2026.269.0644`), not a release:

```
$ CHARLY_PLUGIN_DIR=… CHARLY_REPO_OVERRIDE="github.com/opencharly/charly=$PWD" \
    /tmp/opencode/charly-v3-fix check run check-loader-flat-import-local
charly check run check-loader-flat-import-local: [deploy-add] PASS after 53.403s [allowlisted=1]
charly check run check-loader-flat-import-local: [bring-up-members] PASS after 1ms
charly check run check-loader-flat-import-local: [check-live] PASS after 29.681s [allowlisted=1]
charly check run check-loader-flat-import-local: [update] PASS after 40.117s [allowlisted=1]
charly check run check-loader-flat-import-local: [cleanup] PASS after 1m10.015s [allowlisted=1]
charly check run check-loader-flat-import-local: [cleanup-members] PASS after 0s
charly check run check-loader-flat-import-local: PASS (steps=6)
```

### 5. Relocated-bed R10 — full per-bed output, against the binary this tree ships

Every relocated bed was run in its owning repo with
`CHARLY_REPO_OVERRIDE="github.com/opencharly/<repo>=<clone>"`, after
`charly migrate` raised each repo's stamp to `2026.261.1747` (a version-line-only
change — verified: `git diff HEAD~1 HEAD` is one line per repo). Each bed is a
disposable target and ran its fresh `update`/rebuild leg. All 14 beds are green
on the task-named shipped binary `v2026.267.0045`, on `v2026.268.1958`, and on
the current-main release `v2026.269.0131`. The full pasted output below is the
`v2026.269.0131` (current-main) generation; the `v2026.267.0045` and
`v2026.268.1958` runs reached the same `PASS (steps=N)` verdicts.

**`plugin-udev` → `check-udev-local`** (`v2026.269.0131`):

```
charly check run check-udev-local: [deploy-add] PASS after 3.55s [allowlisted=1]
charly check run check-udev-local: [bring-up-members] PASS after 1ms
charly check run check-udev-local: [check-live] PASS after 5.16s [allowlisted=1]
charly check run check-udev-local: [update] PASS after 3.499s [allowlisted=1]
charly check run check-udev-local: [cleanup] PASS after 58.603s [allowlisted=1]
charly check run check-udev-local: [cleanup-members] PASS after 0s
charly check run check-udev-local: PASS (steps=6)
```

**`plugin-preempt` → `check-preempt-local`** (`v2026.269.0131`):

```
charly check run check-preempt-local: [deploy-add] PASS after 3.895s [allowlisted=1]
charly check run check-preempt-local: [bring-up-members] PASS after 1ms
charly check run check-preempt-local: [check-live] PASS after 4.65s [allowlisted=1]
charly check run check-preempt-local: [update] PASS after 3.882s [allowlisted=1]
charly check run check-preempt-local: [cleanup] PASS after 59.522s [allowlisted=1]
charly check run check-preempt-local: [cleanup-members] PASS after 0s
charly check run check-preempt-local: PASS (steps=6)
```

**`plugin-migrate` → `check-migrate-local`** (`v2026.269.0131`):

```
charly check run check-migrate-local: [deploy-add] PASS after 3.482s [allowlisted=1]
charly check run check-migrate-local: [bring-up-members] PASS after 1ms
charly check run check-migrate-local: [check-live] PASS after 5.126s [allowlisted=1]
charly check run check-migrate-local: [update] PASS after 3.769s [allowlisted=1]
charly check run check-migrate-local: [cleanup] PASS after 59.469s [allowlisted=1]
charly check run check-migrate-local: [cleanup-members] PASS after 0s
charly check run check-migrate-local: PASS (steps=6)
```

**`plugin-doctor` → `check-doctor-local`** (`v2026.269.0131`):

```
charly check run check-doctor-local: [deploy-add] PASS after 3.668s [allowlisted=1]
charly check run check-doctor-local: [bring-up-members] PASS after 1ms
charly check run check-doctor-local: [check-live] PASS after 11.234s [allowlisted=1]
charly check run check-doctor-local: [update] PASS after 3.67s [allowlisted=1]
charly check run check-doctor-local: [cleanup] PASS after 56.592s [allowlisted=1]
charly check run check-doctor-local: [cleanup-members] PASS after 0s
charly check run check-doctor-local: PASS (steps=6)
```

**`plugin-gpu` → `check-gpu-local`** (`v2026.269.0131`):

```
charly check run check-gpu-local: [deploy-add] PASS after 3.778s [allowlisted=1]
charly check run check-gpu-local: [bring-up-members] PASS after 0s
charly check run check-gpu-local: [check-live] PASS after 5.007s [allowlisted=1]
charly check run check-gpu-local: [update] PASS after 3.647s [allowlisted=1]
charly check run check-gpu-local: [cleanup] PASS after 1m1.1s [allowlisted=1]
charly check run check-gpu-local: [cleanup-members] PASS after 0s
charly check run check-gpu-local: PASS (steps=6)
```

**`plugin-cardwire` → `check-cardwire-local`** (`v2026.269.0131`):

```
charly check run check-cardwire-local: [deploy-add] PASS after 3.719s [allowlisted=1]
charly check run check-cardwire-local: [bring-up-members] PASS after 1ms
charly check run check-cardwire-local: [check-live] PASS after 13.037s [allowlisted=1]
charly check run check-cardwire-local: [update] PASS after 3.731s [allowlisted=1]
charly check run check-cardwire-local: [cleanup] PASS after 50.347s [allowlisted=1]
charly check run check-cardwire-local: [cleanup-members] PASS after 0s
charly check run check-cardwire-local: PASS (steps=6)
```

**`plugin-bpf` → `check-bpf-local`** (`v2026.269.0131`):

```
charly check run check-bpf-local: [deploy-add] PASS after 3.917s [allowlisted=1]
charly check run check-bpf-local: [bring-up-members] PASS after 1ms
charly check run check-bpf-local: [check-live] PASS after 10.277s [allowlisted=1]
charly check run check-bpf-local: [update] PASS after 3.765s [allowlisted=1]
charly check run check-bpf-local: [cleanup] PASS after 53.16s [allowlisted=1]
charly check run check-bpf-local: [cleanup-members] PASS after 0s
charly check run check-bpf-local: PASS (steps=6)
```

**`plugin-feature` → `check-feature-local`** (`v2026.269.0131`):

```
charly check run check-feature-local: [deploy-add] PASS after 3.803s [allowlisted=1]
charly check run check-feature-local: [bring-up-members] PASS after 1ms
charly check run check-feature-local: [check-live] PASS after 4.669s [allowlisted=1]
charly check run check-feature-local: [update] PASS after 3.63s [allowlisted=1]
charly check run check-feature-local: [cleanup] PASS after 54.673s [allowlisted=1]
charly check run check-feature-local: [cleanup-members] PASS after 0s
charly check run check-feature-local: PASS (steps=6)
```

**`plugin-example-deploy` → `check-exampledeploy`** (`v2026.269.0131`):

```
charly check run check-exampledeploy: [deploy-add] PASS after 3.41s [allowlisted=1]
charly check run check-exampledeploy: [bring-up-members] PASS after 1ms
charly check run check-exampledeploy: [check-live] PASS after 3.852s [allowlisted=1]
charly check run check-exampledeploy: [update] PASS after 3.207s [allowlisted=1]
charly check run check-exampledeploy: [cleanup] PASS after 3.166s [allowlisted=1]
charly check run check-exampledeploy: [cleanup-members] PASS after 0s
charly check run check-exampledeploy: PASS (steps=6)
```

**`charly-arch` → `check-arch-repo`** (`v2026.269.0131`):

```
charly check run check-arch-repo: [vm-build] PASS after 21.66s [allowlisted=1]
charly check run check-arch-repo: [vm-create] PASS after 1.336s [allowlisted=1]
charly check run check-arch-repo: [deploy-add] PASS after 8.928s [allowlisted=1]
charly check run check-arch-repo: [bring-up-members] PASS after 1ms
charly check run check-arch-repo: [check-live] PASS after 2m23.334s [allowlisted=1]
charly check run check-arch-repo: [update] PASS after 1m16.277s [allowlisted=5]
charly check run check-arch-repo: [rebuild-ready] PASS after 263ms
charly check run check-arch-repo: [check-live-rebuild] PASS after 2m34.618s [allowlisted=1]
charly check run check-arch-repo: [cleanup] PASS after 6.244s [allowlisted=1]
charly check run check-arch-repo: [cleanup-members] PASS after 0s
charly check run check-arch-repo: PASS (steps=10)
```

**`charly-fedora` → `check-fedora-repo`** (`v2026.269.0131`):

```
charly check run check-fedora-repo: [vm-build] PASS after 21.95s [allowlisted=1]
charly check run check-fedora-repo: [vm-create] PASS after 1.362s [allowlisted=1]
charly check run check-fedora-repo: [deploy-add] PASS after 9.809s [allowlisted=1]
charly check run check-fedora-repo: [bring-up-members] PASS after 1ms
charly check run check-fedora-repo: [check-live] PASS after 4m13.215s [allowlisted=1]
charly check run check-fedora-repo: [update] PASS after 1m30.014s [allowlisted=5]
charly check run check-fedora-repo: [rebuild-ready] PASS after 270ms
charly check run check-fedora-repo: [check-live-rebuild] PASS after 4m27.787s [allowlisted=1]
charly check run check-fedora-repo: [cleanup] PASS after 4.928s [allowlisted=1]
charly check run check-fedora-repo: [cleanup-members] PASS after 0s
charly check run check-fedora-repo: PASS (steps=10)
```

**`charly-ubuntu` → `check-ubuntu-repo`** (`v2026.269.0131`):

```
charly check run check-ubuntu-repo: [vm-build] PASS after 17.989s [allowlisted=1]
charly check run check-ubuntu-repo: [vm-create] PASS after 1.208s [allowlisted=1]
charly check run check-ubuntu-repo: [deploy-add] PASS after 11.152s [allowlisted=1]
charly check run check-ubuntu-repo: [bring-up-members] PASS after 1ms
charly check run check-ubuntu-repo: [check-live] PASS after 2m45.084s [allowlisted=1]
charly check run check-ubuntu-repo: [update] PASS after 2m5.072s [allowlisted=5]
charly check run check-ubuntu-repo: [rebuild-ready] PASS after 482ms
charly check run check-ubuntu-repo: [check-live-rebuild] PASS after 2m46.668s [allowlisted=1]
charly check run check-ubuntu-repo: [cleanup] PASS after 6.39s [allowlisted=1]
charly check run check-ubuntu-repo: [cleanup-members] PASS after 0s
charly check run check-ubuntu-repo: PASS (steps=10)
```

**`charly-debian` → `check-debian-repo`** (`v2026.269.0131`):

```
charly check run check-debian-repo: [vm-build] PASS after 21.755s [allowlisted=1]
charly check run check-debian-repo: [vm-create] PASS after 1.318s [allowlisted=1]
charly check run check-debian-repo: [deploy-add] PASS after 8.758s [allowlisted=1]
charly check run check-debian-repo: [bring-up-members] PASS after 1ms
charly check run check-debian-repo: [check-live] PASS after 5m18.769s [allowlisted=1]
charly check run check-debian-repo: [update] PASS after 1m45.186s [allowlisted=5]
charly check run check-debian-repo: [rebuild-ready] PASS after 226ms
charly check run check-debian-repo: [check-live-rebuild] PASS after 5m17.114s [allowlisted=1]
charly check run check-debian-repo: [cleanup] PASS after 4.686s [allowlisted=1]
charly check run check-debian-repo: [cleanup-members] PASS after 0s
charly check run check-debian-repo: PASS (steps=10)
```

**`plugin-substrate` → `check-substrate`** (`v2026.269.0131`):

```
charly check run check-substrate: [vm-build] PASS after 6.387s [allowlisted=1]
charly check run check-substrate: [vm-create] PASS after 1.327s [allowlisted=1]
charly check run check-substrate: [deploy-add] PASS after 11.528s [allowlisted=1]
charly check run check-substrate: [bring-up-members] PASS after 4.579s
charly check run check-substrate: [check-live] PASS after 5.922s [allowlisted=1]
charly check run check-substrate: [rebuild-members-down] PASS after 3.963s
charly check run check-substrate: [update] PASS after 1m28.758s [allowlisted=5]
charly check run check-substrate: [re-bring-up-members] PASS after 4.059s
charly check run check-substrate: [rebuild-ready] PASS after 255ms
charly check run check-substrate: [check-live-rebuild] PASS after 6.005s [allowlisted=1]
charly check run check-substrate: [cleanup] PASS after 5.253s [allowlisted=1]
charly check run check-substrate: [cleanup-members] PASS after 3.882s
charly check run check-substrate: PASS (steps=12)
```

**Note on the task-named binary.** `v2026.267.0045` **predates** `main`'s
`task:`/`plugin-task` surface (`charly#651`, 2026-09-25) and therefore cannot
`box validate` current `main` ("node \"mods-tidy\": no kind discriminator … run
`charly migrate`"). It runs the owning-repo beds (whose projects are stamped
`2026.261.1747` and carry no `task:` node) unaffected; all 14 reached the same
`PASS (steps=N)` verdicts.

### 6. The two kept agentteams beds — controlled A/B (why they stay)

Destination `layer-agentteams#5`, branch `feat/relocate-check-beds-2` head
`e2a7088` (OPEN). Cloned, stamped to `2026.261.1747`, run at the shipped binary.
The **only** difference between the two runs is the destination's
`plugin-deploy-pod` pin (verified: `diff` is exactly that one line):

**Pin `v2026.250.1020` (the destination as shipped) — RED:**

```
charly check run check-agentteams-pod: [image-build] PASS after 2m28.875s
charly check run check-agentteams-pod: [check-image] PASS after 1m16.978s
charly check run check-agentteams-pod: [deploy-add] PASS after 6.097s
charly check run check-agentteams-pod: [config] FAIL after 8.652s: exit 1
charly check run check-agentteams-pod: FAIL (steps=4)
charly: error: … config-setup: image not found in local storage: check-agentteams-pod:check-agentteams-pod-2026.269.0149
```

**Pin `v2026.266.1057` (the provider the shipped release bundles) — GREEN:**

```
charly check run check-agentteams-pod: [deploy-add] PASS after 6.746s
charly check run check-agentteams-pod: [config] PASS after 6.669s
charly check run check-agentteams-pod: [start] PASS after 5.498s
charly check run check-agentteams-pod: [bring-up-members] PASS after 102ms
charly check run check-agentteams-pod: [check-live] PASS after 2m9.432s
charly check run check-agentteams-pod: [feature-run] PASS after 1m17.53s
charly check run check-agentteams-pod: [update] PASS after 34.97s
charly check run check-agentteams-pod: [rebuild-ready] PASS after 4.152s
charly check run check-agentteams-pod: [check-live-rebuild] PASS after 2m14.157s
charly check run check-agentteams-pod: [feature-run-rebuild] PASS after 1m18.691s
charly check run check-agentteams-pod: [cleanup] PASS after …
charly check run check-agentteams-pod: [cleanup-members] PASS after 0s
charly check run check-agentteams-pod: PASS (steps=14)
```

`check-agentteams-snapshot` likewise → `PASS (steps=14)`. The destination's own
`deploy.yml` CI gates only the candy manifest (run
https://github.com/opencharly/layer-agentteams/actions/runs/35929010106 —
SUCCESS); it does not run the bed. The bed's real destination evidence is the
control above: **red at `v2026.250.1020`, green at `v2026.266.1057`** → the two
beds stay (R2/B12).

### 7. A1/B5 — owning-repo `charly.yml` `version:` lines vs the charly each bed ran

The `[-2]` review found the #643 body asserted all relocated beds ran at
`2026.249.2125` while its pin list declared `2026.232.0520` for three repos. The
authoritative reconciliation per owning repo — the `version:` line as found on the
owning PR's branch/head, and the charly each bed actually ran at:

| Owning repo | Owning PR | branch head | `version:` (as found) | charly the bed ran |
|---|---|---|---|---|
| charly-arch | #7 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| charly-fedora | #5 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| charly-ubuntu | #6 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| charly-debian | #5 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-udev | #4 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-preempt | #7 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-migrate | #9 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-doctor | #6 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-gpu | #4 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-bpf | #4 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-cardwire | #3 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-feature | #6 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-example-deploy | #4 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |
| plugin-substrate | #12 (MERGED) | main | `2026.249.2125` | `v2026.267.0045` / `v2026.268.1958` / `v2026.269.0131` |

Each repo was brought to `2026.261.1747` by `charly migrate` before its bed ran
(version-line-only change: `git diff HEAD~1 HEAD` shows `1 insertion(+), 1
deletion(-)` per repo / box file). The closed companion repos' branches
(`layer-docs-site` `feat/relocate-check-beds` head `204dc4f`, `plugin-marketplace`
head `86c5db9`) are also stamped `2026.249.2125`; their beds stay in `charly.yml`.

## Attribution tier

**`fully tested and validated`** — every relocated bed ran its full disposable
R10 sequence (fresh rebuild included) **against the shipped binary**
`v2026.267.0045` (the release bundle's own plugins), and again on `v2026.268.1958`
and `v2026.269.0131` (releases of the `main` HEADs this branch was rebased
through, `c70c9ec` → `5756335`); the in-tree representative bed ran on the
tree's OWN binary (`2026.269.0644`) and `charly box validate` ran clean at
`v2026.268.1958` with the `v2026.269.0131` divergence routed to `charly#672`. The
kept beds (agentteams, docs, marketplace, structkind, alpine, dsh, agentteams-vm)
are coverage-retention, each with a named owner — not deferred gates.

## Harness rulebook compliance

- **R0 skills-first:** loaded `git-workflow`, `check`, `strict-policy`,
  `root-cause-analyzer`, `review`, `migrate` before the first action.
- **R1 RCA, no parking:** the `schema 2026.249.2125` warnings were root-caused to
  the per-bed temp deploy-config captured before each repo's `migrate` was
  committed (every repo migrated, every bed re-run warning-free); the
  `source: builtin` failure is root-caused to `spec#163`'s retired sentinel with
  the charly-side pin bump routed to the named `charly#672`; the agentteams red
  destination is root-caused to the v250 provider's dropped `resolved_image`,
  with a controlled A/B.
- **R2 no deferral:** the docs/marketplace nodes are **kept** (their owning PRs
  are CLOSED unlanded — same rule as agentteams); the agentteams beds kept
  (`layer-agentteams#5`); `check-structkind` stays (`charly#627`); the alpine/dsh
  nodes stay (named batch `charly#644`); the `source: builtin` pin bump is routed
  to the named `charly#672`. No unowned deferral.
- **R3 no duplication:** each relocated bed moves to the one repo that owns its
  artifact; no kept bed has a live duplicate.
- **R4 no workarounds:** `charly migrate` / `charly box validate` /
  `charly check run` only; the per-bed `CHARLY_DEPLOY_CONFIG` overlay is the
  documented isolation mechanism, not an ad-hoc shim.
- **R5 hard cutover:** 26 nodes deleted in one change, no shim/alias/dual path;
  the comment-only Go sweep removes the last `check-substrate` /
  `check-exampledeploy` identifier references (grep self-test pasted above).
- **R6 git safety:** append-only, no force-push.
- **R7 gate:** `charly box validate` + `go build` + `go vet` + the touched
  `go test` + an in-tree `charly check run` on the tree's own binary (pasted
  above).
- **R8/R9:** no generated artifact or deployed binary changed by this PR; the
  in-tree bed and the tree-built binary report `2026.269.0644` / embedded
  `#SchemaVersion 2026.261.1747`.
- **R10 fresh disposable proof:** every relocated bed is `disposable: true` and
  ran the full sequence including the fresh `update`/rebuild leg; the in-tree bed
  likewise.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*
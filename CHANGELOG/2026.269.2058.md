# 2026.269.2058 — docs: retire the last references to the deleted per-kind map names (R5 sweep)

## Summary

The crutch-sweep R5 self-test (`grep` for the deleted per-kind map names) now comes back
clean. `charly/internal/pluginsgen/main.go` carried two comments that still **named**
`externalDeploySubstratePlugins` / `vmPluginCandyRef` while explaining what the generated
word→ref index replaced — the symbols are deleted (the index is a pure projection of the
plugin repos), so a current comment naming them is stale documentation (R5: history belongs
in changelogs, not in current source). The comments now state the invariant (the core keeps
no per-kind map of its own) without naming dead symbols.

Comment-only. The generator's emitted output is unchanged, so the generated
`plugins_refs_generated.go` / `plugins_generated.go` are byte-identical.

## How tested

```
$ grep -rn "externalDeploySubstratePlugins\|vmPluginCandyRef\|externalDeploySubstratePluginRef" . 
(empty — the crutch-sweep R5 self-test is clean)
```

```
$ gofmt -l internal/pluginsgen/
(empty)
```

```
$ go test -count=1 ./internal/pluginsgen/
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	81.744s
```

```
$ git diff --stat origin/main -- charly/plugins_refs_generated.go charly/plugins_generated.go
(empty — the generated artifacts are unchanged)
```

The `internal/pluginsgen` test regenerates the wiring and byte-compares it to the committed
files — it passing proves the emitted artifacts are unaffected by the comment change.

## Rulebook compliance

- **R1 (RCA):** the stale names were a leftover of the C1/C2 crutch removal (the maps were
  deleted, the comments were not swept) — swept now.
- **R5 (delete legacy):** the last references to the deleted symbols are gone; the R5
  grep self-test is clean.
- **R3:** `N/A` — no duplication or behavior change.
- **R6:** branch + PR; no force-push, no direct push to `main`.
- **R7/R8:** the reproducibility gate (`go test ./internal/pluginsgen`, which byte-compares
  the regenerated artifacts) is the proving gate for an output-preserving change.
- **R10 (change-class gate):** comment-only ⇒ the non-runtime standard — `gofmt` + the
  generator reproducibility test, both green.

## Change classification

- **Change class:** comment-only (no behavior change).
- **Verification gate:** `gofmt -l` + `go test -count=1 ./internal/pluginsgen/` (the
  regenerated-artifact byte-comparison).
- **Attribution tier:** `documentation reviewed` — comment-only edit; every non-runtime
  standard passed.

*Assisted-by: OpenCode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*
# 2026.999.9999 — codify the docs CHANGELOG convention + resolve the git-workflow doc tensions

This cutover moves the residue of the `worktree-session-lifecycle` landing (parked in private
memory as "next batch") into the committed skills, per the knowledge-leak rule: a proven
generalizable insight saved only to memory is a leak, because a fresh validator/teammate never
reads memory. Two classes of knowledge are codified.

**1. The `docs` repo CHANGELOG convention (previously unnamed — the leak this cutover closes).**
Every repo's every landing carries its own `CHANGELOG/<YYYY.DDD.HHMM>.md`; the skill enumeration
named the superproject, `box/<distro>`, and `plugins` but never `docs` — even though 24 of the
last 25 docs first-parent commits carry a `CHANGELOG/` entry. The single exception (`a252df9`,
docs PR #45) is exactly the commit that forced follow-up docs PR #46 to add its missing entry: a
fresh validator reading the pre-edit enumeration would conclude `docs` is an exception and the gap
recurs. The manifest now names `docs` at all three convention statements (the hard-cutover
"Required for EVERY landing" bullet, the CalVer section, and the "Land docs-only" step), and the
`docs` leg of THIS cutover carries its own per-repo CHANGELOG entry — the convention proven by
example.

The same claim class is swept everywhere a repo-set is enumerated: the tagged-repo enumerations
("Every repo (superproject, `box/<distro>`, `plugins`, `pkg/*`) is tagged / mints `v$VER`", the
"Every repo is tagged at merge" invariant, and the B7 tag-step "tag EVERY repo" + "fires NO
workflow" sentences) all gain `docs` — docs carries no `charly.yml` and is tagged at merge
(v2026.225.1058), so a fresh validator reading the pre-edit list could conclude docs is a
tag-exemption. The repo-set enumerations in the PR-gate audit, B2 branch-correlation, B4
sync/prune, the validator working-directory rule, the W1 no-`.claude/` list, and the signpost
`CLAUDE.md` inventory (8 → 9, `docs/` added) are swept the same way — every surface that names
the participant repos now names `docs`.

**2. Five git-workflow doc tensions resolved.** The submodule-pointer-bump safety rule is
re-anchored to the per-session worktree flow (`git worktree add .claude/worktrees/<slug> -b
feat/<slug> origin/main`, B1 step 0) instead of the retired shared-tree `git switch -c feat/<slug>`
authoring example (R5 — the last surviving taught instance of the old flow). The concurrent-landings
note states the org-wide branch-protection reality (`opencharly/.github/scripts/branch-protection.sh`
applies `strict: true` + the single `charly/pr-validator` context to EVERY active `main`-default
repo — 14 today) instead of the stale "all three repos set `strict: true`" count. B3's title is
scoped to the orchestrator's shared tree so it no longer reads as contradicting
one-worktree-per-session. AGENTS.md Post-Execution Policies step 4 gains the model qualifier (the
validator's cleanup covers the agent worktrees an orchestrator+teammates run spawned; an
independent parallel session removes its own worktree + branch itself), and the stale "for
substantial work" qualifier is dropped so the worktree mandate reads unconditional. CLAUDE.md needed
no edit — it already carries no such qualifier language, so the two rulebooks stay
policy-equivalent.

**Boundary honored:** the `agentteams` working mandates (rootless everywhere, charly-owned scripts,
self-provisioning secrets) stay in private memory — they are user-specific operating constraints for
the AgentTeams integration, not generalizable repo-wide rules (per `/charly-internals:skills`
"User-specific config → private memory").

Cross-repo, producer→consumer (regenerated from this manifest, never hand-edited): `plugins` PR
(regenerated skills + own CHANGELOG), `docs` PR (regenerated site pages + own CHANGELOG — the
convention by example), then this superproject PR (manifest + AGENTS.md + both gitlink bumps to the
merged upstream commits). Documentation-only change class at `documentation reviewed`; regeneration
is a no-op on the committed tree and `charly box validate` passes at zero warnings. Placeholder
CalVer — the pr-validator finalizes it at merge.

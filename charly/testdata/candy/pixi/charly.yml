pixi:
    candy:
        version: 2026.144.1443
        plan:
            - run: command=echo "installing pixi"
              command: echo "installing pixi"
              run_as: root
            - run: command=echo "init pixi project"
              command: echo "init pixi project"
              run_as: "${USER}"

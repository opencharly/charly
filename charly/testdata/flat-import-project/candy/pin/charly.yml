# Pins the out-of-tree plugin-example-kind plugin so its `examplekind` kind word is prescanned
# BEFORE parse — the plugin ref is reachable ONLY via this discovered candy.
pin:
    candy:
        version: 2026.242.0529
        description: Pins the out-of-tree example-kind plugin so its kind word is prescanned.
        require:
            - '@github.com/opencharly/plugin-example-kind/candy/plugin-example-kind:v2026.242.0529'
        distro:
            arch:
                package: [bash]
        plan:
            - check: placeholder
              context: [runtime]
              command: 'true'

# scripts/drift-baseline-docs.txt — TEMPORARY record of generated artifacts that are ALREADY stale on main.
#
# Its only job is to let `task docs:drift` fail on NEW drift without reding every PR in the repo
# for debt it did not create. Inherited debt is reported by name on every run.
#
# THIS FILE GREW 8 -> 63 AND HAS NOW SHRUNK TO 62. THAT NEEDS ITS EVIDENCE IN FRONT OF YOU. A baseline is
# supposed to shrink. It grew because this cutover advances the `docs` gitlink onto a mirror that
# `main` cannot reproduce: docs@d9afc66 was projected from `ec484b4b`, which is unlanded and lives
# only on opencharly/charly's feat/factory-unit5-box-load (charly#315).
#
# MEASURED, not argued. A scratch worktree at superproject origin/main, submodules recursive, docs
# moved to docs-main d9afc66, `task build:binary`, `task docs:sync`:
#
#	$ git -C docs status --porcelain | wc -l
#	74
#
# So an UNMODIFIED main already fails to reproduce docs main — including DELETING
# recipes/build/load.md and recipes/distros/nested-podman-socket.md. Then, at this branch's pin:
#
#	stale at the first measurement 63     (55 modified, 7 deleted, 1 untracked)
#	stale now, at this branch's pins 62   (the untracked one liquidated — see below)
#	attributable to this branch     0     (by path; see the caveat below)
#
# Every entry below is in that intersection. THAT IS NOT THE SAME AS "none is this cutover's
# drift", and an earlier version of this header claimed it was.
#
# `comm` differences PATHS, not REASONS. A file stale on main for reason A and stale here for
# reasons A *and* B lands in both sets and subtracts to zero — carrying B with it, invisibly. Two
# entries did exactly that: `recipes/index.md` (whose entire branch-side regeneration was this
# cutover's own `ollama-cli` card) and `reference/providers.md` (its `ollama` row). Found by a
# reviewer who looked INSIDE the intersection instead of only across it — `git diff -- <path> |
# grep -ci ollama` — which is this repo's own "ask what the output shape discards" rule
# (scripts/drift-baseline.sh) turned on the file that cites it.
#
# `recipes/index.md` is consequently NOT in this list any more: it is regenerated and committed,
# which is what the rule at the bottom of this header requires. `reference/providers.md` remains,
# and remains honestly — regenerating it would add the `ollama` row but drop seven ROWS main has
# (`build`, `feature`, `labels`, `load`, `merge`, `pull`, `reconcile`, each served by plugin-box),
# which is SIX distinct words: `feature` is listed twice on main and survives via plugin-feature.
# Rows and words are not the same count here, and this file said "seven command words" until a
# reviewer measured it. Those rows come from the unlanded ec484b4b. It is inherited debt AND carries one
# line of this cutover's, and it cannot be split until charly#315 lands.
#
# THIS BRANCH'S OWN PENDING PROJECTIONS HAVE LIQUIDATED. The two pages recorded below were pending
# because their sources were unlanded when the baseline was written: reference/cli/ollama.md
# projected candy/plugin-ollama (charly#309) and recipes/internals/libvirt-renderer.md was the
# docs half of a two-hop chain reading the plugins tree at a pin this branch advanced (plugins#212).
# Both sources landed, docs#84 carried the pages, and the docs gitlink advance in this change makes
# them reproducible from a clean checkout — so the entries are deleted here, the "no longer stale"
# rule working as designed.
#
# MEASURE THE DIRECTION FROM A CLEAN `main`, NEVER FROM THIS WORKING TREE. That is the rule both
# corrections above teach, and it is easy to get wrong because the working tree answers instantly
# and plausibly. "Regenerating adds it" is a property of the page AND the tree; the tree that
# decides whether a projection can land is the one a clean checkout produces.
#
# THREE BOX PAGES WERE HERE AND ARE NOT ANY MORE, because recording them was a MEASUREMENT ERROR:
# reference/box/{fedora,cachyos}/ollama*.md were measured while the box submodules sat on their
# feat branches for a bed run. At the PINNED gitlinks those boxes do not exist, the generator emits
# no page, and nothing is stale. This file's own header says to measure at the gitlink for exactly
# this reason; the entries were removed when a re-measure at the pins reported them "no longer
# stale".
#
# HOW IT LIQUIDATES, and NOT all at once. 62 of the original 63 trace to charly#315: they are
# pages docs@d9afc66 projected from ec484b4b, so when that lands `main` reproduces them and the
# gate reds with "no longer stale — delete these lines". The 63rd did NOT share that trigger:
# `reference/candy/agentteams-snapshot.md` was stale as an UNTRACKED emission (`??`, the only one;
# the rest were 55 `M` + 7 `D`) — a page the generator writes that docs had never carried. #315
# landing could not resolve it; committing that page in docs could.
#
# IT HAS NOW LIQUIDATED, exactly that way and separately from the other 62, which is the file
# working as designed: docs main gained the page, the gate red with "no longer stale", and the
# line is deleted here. Recording the two triggers separately is what made that legible instead
# of looking like an unexplained red.

src/content/docs/recipes/build/generate.md
src/content/docs/recipes/internals/git-workflow/evidence-and-freshness.md
src/content/docs/recipes/internals/install-plan/step-kinds.md
src/content/docs/reference/providers.md

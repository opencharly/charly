# scripts/drift-baseline-skills.txt — TEMPORARY record of generated artifacts that are ALREADY stale on main.
#
# Its only job is to let `task skills:drift` fail on NEW drift without reding every PR in
# the repo for debt it did not create. Inherited debt is reported by name on every run.
#
# HOW THIS WAS MEASURED, and how to re-measure it: origin/main @ 162b54a3 with EVERY
# SUBMODULE AT ITS GITLINK — the tree `actions/checkout --submodules recursive` produces.
# Do NOT re-measure by running the gate in a working tree. Both generators read plugins/
# off the FILESYSTEM, not at the gitlink, so a checkout that has moved records drift CI
# will never see; every such entry then reds CI forever as "no longer stale", and every
# real gitlink-level entry it omits reds CI as new.
#
# RE-MEASURED at 162b54a3 (was 70daf9cb). The first measurement was taken two `main`
# merges before this branch's merge-base, and because the comparison is EXACT the skew
# alone red the gate: 3 already-stale artifacts were missing and 2 already-paid entries
# were still listed. A baseline measured at the wrong commit is not a smaller problem
# than no baseline — it fails in both directions at once.
#
# THIS IS NOT AN ALLOWLIST. scripts/drift-baseline.sh compares EXACTLY: an entry here
# that is no longer stale fails the gate just as loudly as an artifact that is stale and
# absent. That is what makes this file self-liquidating — it can only shrink, and when it
# reaches zero you delete it and the gate runs absolute.
#
# Do not add a line to silence a red. If your change made an artifact stale, the fix is to
# regenerate and commit, never to record it here.
#
# THE ONE CASE THAT RULE DOES NOT COVER, and it is why two entries were added at this branch's
# pins rather than regenerated: a PROJECTION that is AHEAD of its source. plugins#208 landed the
# projection of charly#315's work before #315 itself merged — which is B6a working as designed,
# not a defect. `main` therefore holds text NEWER than anything this superproject can generate,
# and regenerating REVERTS it. Measured:
#
#     $ # plugins at origin/main, then: charly marketplace generate
#     $ git -C plugins diff -- internals/skills/vm-deploy-target/SKILL.md
#     -(via `deploykit.ImageVenue.PodmanCmd`, the ONE field every step reads, so they
#     -cannot disagree about which store they are addressing):
#     +(via the `podmanCmd(rootless)` helper):
#
# The `-` side is what main carries; the `+` side is what the generator produces. So "regenerate
# and commit" would destroy landed content, and the entries below are NOT a silenced red — they
# record pages that are CORRECT and that this tree cannot reproduce until #315 lands. They
# liquidate then, in the normal way, by reding as "no longer stale".
#
# Do not delete them by regenerating. charly#336 carries the finding that the gate cannot itself
# distinguish projection-ahead-of-source from source-ahead-of-projection — both print "newly
# stale", and only the diff DIRECTION tells them apart. Check the direction before acting on a
# red in this file's neighbourhood.
#
# RE-MEASURED AGAIN at this branch's own head with ITS gitlinks (plugins 40229ad, docs e213d51),
# because that is the tree CI checks out for this PR and the comparison is EXACT. Measuring at
# `main` alone was wrong in both directions once this branch bumps a projection gitlink: the
# artifacts the projection FIXES stop being stale, and the artifacts `main` has moved past
# become stale. Neither of the entries below is this branch's own drift — every one is inherited,
# and every one is reported by name on each run.

plugins/build/skills/generate/SKILL.md
plugins/build/skills/load/SKILL.md
plugins/check/skills/check/references/authoring-gotchas.md
plugins/core/skills/charly-doctor/SKILL.md
plugins/core/skills/shell/SKILL.md
plugins/distros/skills/cachyos-pacstrap/SKILL.md
plugins/distros/skills/container-nesting/SKILL.md
plugins/distros/skills/nested-podman-socket/SKILL.md
plugins/distros/skills/nvidia-layer/SKILL.md
plugins/distros/skills/rocm/SKILL.md
plugins/image/skills/layer/SKILL.md
plugins/internals/agents/pr-validator.md
plugins/internals/skills/agents/references/parallel-bed-testing.md
plugins/internals/skills/egress/SKILL.md
plugins/internals/skills/generate-source/references/task-emission.md
plugins/internals/skills/git-workflow/references/evidence-and-freshness.md
plugins/internals/skills/git-workflow/references/multi-repo-coordination.md
plugins/internals/skills/go-quality/SKILL.md
plugins/internals/skills/go/SKILL.md
plugins/internals/skills/go/references/source-map.md
plugins/internals/skills/plugin/SKILL.md
plugins/internals/skills/plugin/references/boundary-law.md
plugins/internals/skills/skills/SKILL.md
plugins/internals/skills/vm-spec/SKILL.md
plugins/internals/skills/vm-deploy-target/SKILL.md
plugins/vm/skills/vm/SKILL.md

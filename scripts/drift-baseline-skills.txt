# scripts/drift-baseline-skills.txt — TEMPORARY record of generated artifacts that are ALREADY stale on main.
#
# Its only job is to let `task skills:drift` fail on NEW drift without reding every PR in
# the repo for debt it did not create. Inherited debt is reported by name on every run.
#
# HOW THIS WAS MEASURED, and how to re-measure it: origin/main @ 70daf9cb with EVERY
# SUBMODULE AT ITS GITLINK — the tree `actions/checkout --submodules recursive` produces.
# Do NOT re-measure by running the gate in a working tree. Both generators read plugins/
# off the FILESYSTEM, not at the gitlink, so a checkout that has moved records drift CI
# will never see; every such entry then reds CI forever as "no longer stale", and every
# real gitlink-level entry it omits reds CI as new. Measured in this worktree, whose
# plugins/ sat 1 commit off the gitlink, 12 of 33 entries were of exactly that kind.
#
# THIS IS NOT AN ALLOWLIST. scripts/drift-baseline.sh compares EXACTLY: an entry here
# that is no longer stale fails the gate just as loudly as an unlisted artifact that is.
# So the file can only shrink — when the missing work lands, the gate names the lines to
# delete. Delete the file once it is empty and the gate runs absolute.
#
# KNOWN LIMITATION: a listed path is UNGUARDED until its debt is paid — new drift on an
# already-drifted artifact is masked, because the key is the path, not its content. That
# is the price of the baseline, it is bounded by this list, and it shrinks to zero with it.
#
# ALREADY SHRUNK ONCE: plugins/ollama/skills/ollama-cli/SKILL.md was stale on main as an
# ORPHAN (a generated skill with no source entity). This branch's ollama candy work gave it
# a source, so it is no longer stale and the gate demanded its removal. That is the intended
# lifecycle, on its first real case.
#
# Do not add a line to silence a red. If your change made an artifact stale, the fix is to
# regenerate and commit, never to record it here.
plugins/build/skills/generate/SKILL.md
plugins/check/skills/check/references/authoring-gotchas.md
plugins/core/skills/charly-doctor/SKILL.md
plugins/core/skills/clean/SKILL.md
plugins/core/skills/shell/SKILL.md
plugins/distros/skills/cachyos-pacstrap/SKILL.md
plugins/distros/skills/container-nesting/SKILL.md
plugins/distros/skills/nvidia-layer/SKILL.md
plugins/distros/skills/rocm/SKILL.md
plugins/internals/agents/pr-validator.md
plugins/internals/skills/agents/references/parallel-bed-testing.md
plugins/internals/skills/egress/SKILL.md
plugins/internals/skills/git-workflow/references/branch-and-pr-loop.md
plugins/internals/skills/git-workflow/references/evidence-and-freshness.md
plugins/internals/skills/git-workflow/references/multi-repo-coordination.md
plugins/internals/skills/git-workflow/SKILL.md
plugins/internals/skills/go-quality/SKILL.md
plugins/internals/skills/go/references/source-map.md
plugins/internals/skills/go/SKILL.md
plugins/internals/skills/plugin/references/boundary-law.md
plugins/internals/skills/plugin/SKILL.md
plugins/internals/skills/skills/SKILL.md
plugins/internals/skills/vm-spec/SKILL.md
plugins/ollama/skills/ollama-layer/SKILL.md
plugins/ollama/skills/ollama/SKILL.md

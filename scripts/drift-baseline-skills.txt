# scripts/drift-baseline-skills.txt — TEMPORARY record of generated artifacts that are ALREADY stale on main.
#
# Its only job is to let `task skills:drift` fail on NEW drift without reding every PR in
# the repo for debt it did not create. Inherited debt is reported by name on every run.
#
# HOW THIS WAS MEASURED, and how to re-measure it: origin/main @ 4e0c274b with EVERY
# SUBMODULE AT ITS GITLINK — the tree `actions/checkout --submodules recursive` produces.
# Do NOT re-measure by running the gate in a working tree. Both generators read plugins/
# off the FILESYSTEM, not at the gitlink, so a checkout that has moved records drift CI
# will never see; every such entry then reds CI forever as "no longer stale", and every
# real gitlink-level entry it omits reds CI as new.
#
# The sdk de-submodule cutover's landing sequence paid every entry except this one: the
# regenerated corpus (plugins#223-#227) + the docs leg (docs#93) brought the committed
# pages to the entity sources. The remaining entry is the pre-registered harness-hook
# drift, registered to the harness-hook-sync batch (charly CHANGELOG/2026.234.0605).
#
# THIS IS NOT AN ALLOWLIST. An entry here that is no longer stale fails the gate just as
# loudly as an artifact that is stale and absent — it can only shrink, and at zero it is
# deleted and the gate runs absolute.
plugins/internals/skills/agents/references/parallel-bed-testing.md
